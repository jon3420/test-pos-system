# fix18-10-hotfix30-B5-R5.4-G1.4 — Completion Report

## 範圍確認

本輪處理：Permanent District Label、Honest Drawable-State Overlay、
Layer Toggle 白色橫條、G1.3.1/G1.3.2 Fixture 時區問題、
`static-audit-g1-2.js` 過時檢查對齊。未重做既有 G1 系列、未開始 G2、
未新增任何新功能、未修改訂單／營收統計口徑。

## 1. 本輪確實修改產品程式碼

**確實修改**：

- `public/js/geo-heatmap.js`：新增 `geoHeatComputeDrawableState()`；
  `geoHeatRenderLayer()` 新增 Permanent District Label 建立邏輯；
  `module.exports` 新增 `geoHeatComputeDrawableState`。
- `public/js/geo-heatmap-ui.js`：新增 `_geoHeatUiOrderMapOverlayMessage()`
  ／`_geoHeatUiRenderOrderMapOverlay()`；`_geoHeatUiRenderCoverageExplanation()`
  結尾新增呼叫；`geoHeatUiSetLayer()` 補上 Order/Visitor Overlay 對稱
  清理；`_geoHeatUiVisitorMapOverlayMessage()` 主要句子改用統一句型
  （既有 Known District 診斷明細保留）；`module.exports` 新增兩個函式。
- `public/css/geo-heatmap.css`：新增 `.geo-heat-layer-toggle`／
  `.geo-heat-layer-btn`／`.geo-heat-map-label`／`.geo-heat-order-map-overlay`
  規則。

**沒有修改**：`utils/geoAnalyticsQueries.js`、`utils/dashboardDate.js`、
`utils/geoAnalyticsFilters.js`、任何訂單／營收統計 SQL、Coordinate API、
Metric Mapping。

## 2. 修改檔案清單

| 檔案 | 修改內容 |
|---|---|
| `public/js/geo-heatmap.js` | Drawable State classifier + Permanent Label |
| `public/js/geo-heatmap-ui.js` | Honest Order Overlay + Visitor Overlay 文案對齊 |
| `public/css/geo-heatmap.css` | Layer Toggle CSS（白色橫條修正）+ Label/Overlay CSS |
| `scripts/lib/geo-heatmap-g131-scope-guard.js` | 新增 G1.4 Allowlist 第二層 |
| `scripts/smoke-hotfix30-b5-r5-4-g1-2-layer-switch.js` | 2 項斷言文字對齊新版 Visitor Overlay 措辭 |
| `scripts/static-audit-g1-2.js` | check 21 過時檢查換成 Layered Scope Guard，check 數量 25→37 |

**新增檔案**：

- `scripts/lib/geo-fixture-time.js`
- `scripts/smoke-hotfix30-b5-r5-4-g1-4-map-label-rendering.js`
- `scripts/static-audit-g1-4.js`

## 3. Fixture 測試檔修改

- `scripts/smoke-hotfix30-b5-r5-4-g1-3-1-coverage-total-dark-theme.js`：
  `insOrder()` 改用 `computeFixtureTimestamp('today')`，取代
  `datetime('now','localtime')`。
- `scripts/smoke-hotfix30-b5-r5-4-g1-3-2-regression-guard.js`：同上，
  含情境 D（Store Isolation）的獨立 INSERT 也一併修正。

## 4. 歷史 static-audit-g1-2.js 對齊

原本 check 21 用「geo-heatmap.js 完全不得出現
`_geoHeatUiApplyLayerExclusivity`／`_geoHeatUiRenderVisitorMapOverlay`
這兩個字串」這種寬鬆比對，來間接驗證「UI 層邏輯沒有誤放進 Engine 檔案」。
G1.4 在 `geoHeatRenderLayer()` 上方新增的合法說明註解裡提到了
`_geoHeatUiApplyLayerExclusivity()`（解釋「這裡為什麼不需要修改」），
觸發了這個過度寬鬆的字串比對，被誤判為「Engine 退化」。

**沒有**：忽略失敗、刪除該項、改成永遠 true、放入 allow-failure、只更新
整檔 hash、以「不在本輪清單」為由跳過、降低 check 總數。

**改為**：呼叫 `scripts/lib/geo-heatmap-g131-scope-guard.js` 的 Layered
Scope Guard，用 12 項細分檢查取代原本 1 項寬鬆檢查（`21`／`21b`～`21m`），
涵蓋 G1.3.1 Allowlist 正確性、G1.4 Allowlist 正確性、Layered
Reconstruction 正確性、還原到同一個 pristine baseline hash、Allowlist
外修改仍會失敗、no second map、no second tile layer、stale request
guard、backward compatibility、businessTotals additive、Permanent Label
additive、Drawable State additive。`static-audit-g1-2.js` 總 check 數
從 25 增加到 **37**（沒有降低，且增加了 12 項語意型檢查）。

## 5. G1.4 Smoke：147/147

`scripts/smoke-hotfix30-b5-r5-4-g1-4-map-label-rendering.js`——要求至少
140，實際 147，FAIL=0，三次重跑結果一致。

## 6. G1.4 Static Audit：52/52

`scripts/static-audit-g1-4.js`——要求至少 50，實際 52，全數 OK。

## 7. 所有 Static Audits 結果

| Audit | 結果 |
|---|---|
| `static-audit-g1.js` | 30 / 30 PASS |
| `static-audit-g1-2.js` | **37 / 37 PASS**（原 25 項，本輪對齊過時檢查後增加至 37 項） |
| `static-audit-g1-3.js` | 32 / 32 PASS |
| `static-audit-g1-3-1.js` | 40 / 40 PASS |
| `static-audit-g1-3-2.js` | 48 / 48 PASS |
| `static-audit-g1-4.js` | 52 / 52 PASS |

## 8. A2：229/229

三次重跑一致，FAIL=0。

## 9. A1.2：189/189

三次重跑一致，FAIL=0。

## 10. 完整 Regression 三輪結果

| Suite | Round 1 | Round 2 | Round 3 |
|---|---|---|---|
| G1 | 212/212 | 212/212 | 212/212 |
| G1.1 | 91/91 | 91/91 | 91/91 |
| G1.2 | 83/83 | 83/83 | 83/83 |
| G1.3 | 117/117 | 117/117 | 117/117 |
| G1.3.1 | 115/115 | 115/115 | 115/115 |
| G1.3.2 | 148/148 | 148/148 | 148/148 |
| G1.4 | 147/147 | 147/147 | 147/147 |
| A7 | 87/87 | 87/87 | 87/87 |
| A5 | 20/20 | 20/20 | 20/20 |
| A4 | 110/110 | 110/110 | 110/110 |
| A2 | 229/229 | 229/229 | 229/229 |
| A1.2 | 189/189 | 189/189 | 189/189 |
| B2 | 620/620 | 620/620 | 620/620 |
| B3 | 157/157 | 157/157 | 157/157 |

三輪 assertion count 完全一致，無 UNIQUE constraint、無測試 DB 殘留、
無跨次污染、exit code 每次穩定為 0。

**Baseline Comparison**：
- Pre-existing failures：0
- G1.4 new failures：0
- G1.4 fixed failures：G1.3.1／G1.3.2 fixture 時區 flaky（測試 Harness
  修正，非產品 Bug）；`static-audit-g1-2.js` check 21 過時檢查（測試
  Guard 對齊，非產品 Bug）

## 11. node --check：215/215

全專案 215 個 `.js` 檔案（不含 `node_modules`／`public/vendor`）全部通過
語法檢查。

## 12. Fixture 三次穩定性

G1.3.1 Smoke：115/115、115/115、115/115（三次結果完全一致）
G1.3.2 Smoke：148/148、148/148、148/148（三次結果完全一致）

無時間相關 flaky（已透過 `midpointLocalString` 純函式額外驗證台灣
23:59 前後、00:01 前後兩種邊界情境）。

## 13. Manual Visual 狀態

**NOT TESTED / REQUIRED** — 本輪環境沒有真實瀏覽器可供人工視覺驗收。
詳見 `R5.4-G1.4_MANUAL_QA_CHECKLIST.md`，全部項目標記為 NOT TESTED，
**不宣稱 Manual Visual PASS**。

## 14. 是否建議部署

**建議先完成 Manual Visual（真實瀏覽器）驗收後再部署。**

自動化驗證層面（Drawable State 分類、Permanent Label 建立/清除邏輯、
Honest Overlay 文案與觸發時機、Layer Lifecycle、CSS 規則本身、Scope
Guard 完整性）已透過真實 jsdom 執行、真實 sql.js DB fixture、22 組
Mutation Negative Test、三輪完整 Regression 多重驗證，功能正確性有高
信心；但 Permanent Label 在真實地圖上的視覺呈現（是否會跟 Marker 圖示
重疊、跟其他 UI 元件的圖層堆疊順序、Dark/Light Theme 下的實際對比度）、
Layer Toggle 按鈕的最終視覺效果，仍需要至少一次真實瀏覽器（建議 Chrome
+ 一個行動裝置瀏覽器）操作驗收，才能標記為可正式對外部署。
