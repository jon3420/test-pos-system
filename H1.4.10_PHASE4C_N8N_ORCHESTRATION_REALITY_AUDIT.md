# H1.4.10 Phase 4C — RECOVERY N8N ORCHESTRATION Reality Audit

**狀態：CODE COMPLETE ｜ AUTOMATED ACCEPTANCE PASS（152/152）｜ MANUAL N8N QA PENDING**

Backend／security/timezone/replay/SSRF 等邏輯已用 152 個真實 runtime assertions 驗證通過，但真實 n8n instance 上的 Import／Immediate ACK 執行順序／Code node 權限／Wait 時區行為／服務重啟後的 Wait 恢復／實際 has_more 迴圈，全部仍是 **PENDING MANUAL QA**（見 `H1.4.10_PHASE4C_N8N_ORCHESTRATION_MANUAL_QA.md`），沒有真實 n8n 環境前不得視為完整可上線。

## 核心原則

n8n 只負責「什麼時候叫醒 POS」（排程/等待）。POS 永遠是 authority，負責「現在到底能不能發」——n8n 絕對不自行判斷 consent／好友／購買／converted／max_attempts／token／LIFF／cart snapshot，即使 n8n 認為「應該提醒」，POS 收到 due request 後仍會重新 `evaluateLineRecoveryEligibility()`（Phase 4B 既有邏輯，本輪完全沒有重寫）。

## 1. 既有 `n8n_webhook_url` 是訂單 webhook

`routes/line-orders.js`／`routes/line-shipping.js`／`routes/orders.js` 都在讀這個 key，對應 `n8n-workflow.json`（name=「餐車 POS 訂單自動化」）。Phase 4C 新增完全獨立的 `cart_recovery_n8n_enabled`／`cart_recovery_n8n_webhook_url`／`cart_recovery_n8n_secret`，不共用任何 key。本輪用真實 HTTP 測試（設定寫入、Wake-Up 呼叫、Rotate、Due Endpoint 全部跑過一輪）加上 sha256 hash 比對，確認 `n8n_webhook_url` 的值與 `n8n-workflow.json` 檔案本身 byte-for-byte 完全沒被動過。

## 2. n8n 不持有顧客 PII

`sendWakeUp()` 的 payload 只有 `version`／`store_id`／`due_at`／`stage`／`request_id`／`sent_at`。`process-due` 的 request body 只有 `version`／`store_id`／`timestamp`／`request_id`，response 只有聚合數字（`processed`/`sent`/`skipped`/`failed`/`has_more`），完全沒有 `job_id`/`cart_id`/`order_id`/`line_user_id`/`token`/電話/地址。

## 3. Wake-Up 失敗不改 Recovery Job

`sendWakeUp()` 對任何失敗（network_error／timeout／http_error／invalid_or_unsafe_url／dns_resolved_to_private_ip）都只回傳失敗結果本身，完全不觸碰 `cart_recovery_jobs.status`——因為還沒有真的嘗試過 LINE Push，把它標記成 `failed` 會是錯誤的語意。

## 4. Old Schedule After Refresh 只會 No-op

`due_at` 被 refresh 後，n8n 手上舊的排程（如果已經在 Wait）到期呼叫 POS 時，POS 會用「當下最新」的 `due_at` 判斷，發現 job 還沒到期（`job_not_due`），直接 `sent=0` 正常結束，不視為 error。已用 RACE1-RACE5 完整驗證：舊時間點查詢查不到、真正到期後才送出、全程只有 1 筆 job、只送出 1 次提醒。

## 5. Conversion／Consent Revoke／Unfollow 都由 POS Due 時重新判斷

三個 race 情境（CV/CR/FR 系列）都驗證過：無論 n8n 何時真正呼叫 `process-due`，POS 都會用「呼叫當下」的真實資料庫狀態重新判斷，購買/取消同意/取消好友發生在 due 之前的話，一律 `sent=0`，且 job 狀態正確反映真實情況（`converted`/`not_contactable`）。

## 6. HMAC + Replay Protection

簽章公式：`v1\n<store_id>\n<timestamp>\n<request_id>\n<due_at>`（HMAC-SHA256），用 `crypto.timingSafeEqual` 比對（非 `===`）。Inbound（n8n→POS）用持久化在 `cart_recovery_orchestration_requests` 表的 replay protection（`UNIQUE(store_id, request_id, direction)`），process 重啟後仍然有效——這是本輪特別驗證的一點，不是 in-memory Map/Set。Header 與 body 的 `version`/`timestamp`/`request_id` 必須完全一致，不再 `header || body` fallback 混用（本輪收緊）。

## 7. SSRF 實際防護邊界（誠實記錄限制）

已阻擋：
- Literal hostname：`localhost`／`127.x`／`10.x`／`192.168.x`／`172.16-31.x`／`169.254.x`（含 cloud metadata IP）／IPv6 `::1`／`fc00::/7`／`fe80::/10`／IPv4-mapped 私有位址。
- 非 `https://` 協定一律拒絕。
- DNS 解析後的實際 IP：`resolveSafeWebhookTarget()` 用 `dns.promises.lookup` 解析 hostname，若解析出的任一 IP 落在私有範圍則拒絕（防禦「公開網域但 DNS 指向內網」的情境，已用 stub DNS 驗證）。
- `redirect:'error'`：不跟隨任何 HTTP redirect。

**尚未完全消除的已知限制**：`resolveSafeWebhookTarget()` 的 DNS 檢查與真正 `fetch()` 呼叫之間存在時間差（check-to-fetch window）。如果攻擊者在這個極短窗口內切換該 hostname 的 DNS 記錄（DNS rebinding），這裡的驗證可能被繞過——要完全消除需要在 fetch 階段用自訂的低階 Agent 把已驗證過的 IP「pin 住」，目前 Node 內建 `fetch`／`node-fetch` 沒有提供足夠低階、穩定的 API 能安全做到這件事而不大改整個 HTTP 層架構，這是 Phase 4C 明確、誠實記錄的限制，不宣稱「SSRF 已完全解決」。

## 8. `due_at` Timezone Contract

Audit 確認 `utils/cartRecovery.js` 的 `_nowIso()`／`_plusMinutesIso()` 都用 `Date.toISOString()`（永遠是 UTC）產生，只是把 `T`/`Z` 拿掉存成 `'YYYY-MM-DD HH:MM:SS'` 給 SQLite 文字比較用——DB 裡的 `due_at` **確定是 UTC**，不是 server local time。新增 `toOrchestrationDueAt()`，只用在 n8n payload／HMAC canonical `dueAt`，完全不改 DB schema 或既有 eligibility SQL。已用 T1-T5 驗證：轉換前後 epoch 完全相同（無偏移，特別驗證過不會有常見的 UTC/UTC+8 8 小時偏移錯誤），且簽章用的 `dueAt` 與實際送出的 `body.due_at` 完全是同一個值。

## 9. n8n Workflow 尚需真實 Import QA

本輪修正了兩個真實的 workflow 設計問題：

**(a) Immediate ACK 順序錯誤**：原本的 `Webhook → Verify → Wait → ... → Respond` 會讓 POS 端 `sendWakeUp()` 的 8 秒 timeout 必然在任何有意義的 Recovery 延遲（15～60 分鐘）下超時，導致每一次 wake-up 都被記成 `network_error/timeout`。已修正為 `Webhook → Verify → [If Valid] → Respond(202) → Wait → ...`，簽章驗證通過才 ACK，ACK 在 Wait 之前。

**(b) Loop Context 遺失**：呼叫 POS `process-due` 後，`HTTP Request` 節點的輸出會直接變成 POS 的回應本身，不會自動保留呼叫前的 `store_id`/`stage`/`_loopCount`。已修正為新增 `Merge：context + processor_result` 節點，明確把呼叫前保存的 context 與 POS 回應合併成一個 item，之後判斷一律讀 `processor_result.has_more`，`_loopCount` 用 `context._loopCount + 1` 真正累加（而非每圈重置回 0），並在同一個 if 條件式裡加上 `_loopCount < 10` 的硬性上限。

這些修正已用**真正解析 workflow JSON 的 connections 拓撲**驗證（不是字串搜尋 `.includes('wait')` 這種寬鬆檢查）——WF1-WF13 逐一驗證了節點間的實際連線關係、Merge 節點程式碼的合併邏輯、has_more 判斷讀的是哪個欄位、loop-back 目標節點正確。

**但這些都只是 workflow JSON 的靜態結構驗證**。真正需要以下才能確認可用，本輪完全沒有、也不可能宣稱已驗證：
- n8n 實際 Import 這份 JSON 是否成功
- `RespondToWebhook` 節點在該 n8n 版本下執行後，workflow 是否真的能繼續往下執行到 `Wait`
- `environment variable`（`POS_RECOVERY_SHARED_SECRET`／`POS_BASE_URL`）在真實 n8n 部署環境下的存取方式是否如預期
- `Wait` 節點在長時間（15～60 分鐘甚至更久）等待後，n8n instance 重啟／執行序列是否正確恢復
- `Code` 節點內 `crypto` 模組的實際執行權限

全部列在 Manual QA 文件，一律 `PENDING MANUAL QA`。

## 10. Phase 4C 沒有 Scheduler

`processDueLineRecoveryJobs()` 依然只是「可以被呼叫」的 service function，本輪新增的 `/process-due` 端點只是給 n8n（或未來任何有效的 orchestration 方式）呼叫的介面，POS 本身沒有新增任何 cron/setInterval/自動觸發機制。

## 11. Shared Secret 正式格式（本輪 Security Closeout）

`utils/cartRecoveryOrchestration.js` 新增唯一 SSOT `validateSharedSecret(value)`，正式格式固定為 **32 random bytes 的 base64url 編碼**（`crypto.randomBytes(32).toString('base64url')`）：

- 長度必須精確等於 **43 字元**（不是「至少 32 字元」這種寬鬆長度檢查——42 或 44 都拒絕）。
- charset 必須是 `^[A-Za-z0-9_-]{43}$`（base64url 字元集）。
- `Buffer.from(value, 'base64url').length` 必須精確等於 32 bytes。
- Defense-in-depth（實際 production 有三層獨立規則，逐一列出，不是單一 `size<=2` 判斷）：
  1. `repeated_single_char`：正則 `/^(.)\1+$/` 直接擋下單一字元重複到底（`'a'.repeat(43)`、`'b'.repeat(43)`）。
  2. `repeated_two_char_cycle`：逐字元比對是否為固定雙字元循環（例如 `'ab'` 重複填滿 43 字元）。
  3. `low_entropy`：unique 字元數門檻（`SHARED_SECRET_MIN_UNIQUE_CHARS = 12`）——字串裡不重複的字元種類數若少於 12，一律拒絕。這個門檻同時也會擋下前兩條規則沒有明確窮舉到的樣式，例如 4～8 字元的更長週期循環（一個 4 字元循環填滿 43 字元，unique 字元數只有 4，遠低於 12，會被這條規則擋下）。

這三層合起來是 **additional defense-in-depth heuristic**，不是完整的 entropy estimation（無法真正證明一個字串來自 CSPRNG）；正式安全來源仍是 `crypto.randomBytes(32)`。

`routes/settings.js`（手動輸入路徑）與 `routes/cart-recovery-orchestration.js` 的 `/secret/rotate`（自動產生路徑）都呼叫同一個 `validateSharedSecret()`，不各自重寫一套長度判斷。空字串代表「保留原值」，不是清空。`GET /api/settings` 一律不回傳明文，只回傳 `cart_recovery_n8n_secret_set` 布林值。詳細測試涵蓋見 Test Report。

## 12. Wake-Up Retry：retry-capable ≠ automatic retry guaranteed

`grep` 全專案確認：`sendWakeUp()` 只有一個呼叫入口——`utils/cartRecovery.js` 的 `_fireWakeUp()`，而 `_fireWakeUp()` 只在 `_scheduleStage()`（job 第一次建立）與 `refreshStageActivity()`（`due_at` 被新的 Recovery 活動刷新）這兩個路徑被呼叫。全專案的 `setInterval`（`server.js` 的 WebSocket ping／LINE 配額重置、`routes/line-checkout-handoff.js`／`routes/analytics.js`／`routes/line-member.js` 的 rate-limit bucket 清理）沒有一個與 Recovery Wake-Up 相關；沒有 cron、沒有 worker、沒有 startup hook 會呼叫 `sendWakeUp()` 或 `processDueLineRecoveryJobs()`。

**結論**：如果第一次 Wake-Up 失敗（timeout／DNS 解析失敗／HTTP 500），且**之後沒有任何新的 Recovery 活動**（沒有新的 `add_to_cart`／`checkout_click`／`payment_started` 事件去刷新同一筆 job 的 `due_at`），**沒有任何獨立的 production driver 會再次呼叫 `sendWakeUp()`**。`claimOutboundWakeUp()`/`releaseOutboundWakeUpAsFailed()` 提供的三態機制（`sending`/`processed`/`failed`）只是讓「如果被再次呼叫，允許重試」這件事在技術上可行，**不代表系統會主動去重試**。

**Retry-capable ≠ automatic retry guaranteed.** 這是本輪明確記錄的架構事實，不是待修的 bug——Phase 4C 從未宣稱有自動重試排程，這裡只是把這個既有事實寫清楚，避免未來誤以為「失敗後系統自己會處理」。若未來要補上真正的自動重試（例如：獨立的排程去掃描 `status='failed'` 的 outbound wake-up 記錄並重新嘗試），需要另外設計與實作，不在本輪範圍內。
