# R5.4-G1.5-B2 Implementation Reality Audit

fix18-10-hotfix30-B5-R5.4-G1.5-B2 — 逐項核對真實程式碼，非憑印象推測。
**本文件記錄兩個真實缺口，且已在本輪修正（不是先寫測試迎合現況）。**

## 1-3. Route Path

- GET：`routes/settings.js` → `router.get('/ga4-realtime', ...)` → 掛載於
  `/api/settings`（`server.js`）→ 完整路徑 `GET /api/settings/ga4-realtime`。
- PATCH：同一 router → `PATCH /api/settings/ga4-realtime`。
- Connection Test：`routes/geo-live.js` → `router.post('/ga4-realtime-test',
  ...)` → 掛載於 `/api/geo-live` → `POST /api/geo-live/ga4-realtime-test`。

## 4. Mount 層 requireStore

`server.js`：`app.use('/api/settings', requireStore, require('./routes/
settings'))`；geo-live 同理掛 `requireStore`。兩者都在 mount 層強制。

## 5. reports permission

Connection Test（`POST /ga4-realtime-test`）與 Status（`GET
/ga4-realtime-status`）都套用 `requireFeature('reports')`。**Settings GET/
PATCH 本身沒有額外套用 `requireFeature('reports')`**——這跟同一個檔案裡
既有的 `geo-map` GET/PATCH（`/api/settings/geo-map`）完全一致：整個
`routes/settings.js` 的慣例是「只要通過 `requireStore` 就能讀寫自己店的
設定」，沒有另外分層 feature gate。這不是本輪引入的落差，是沿用既有
架構的一致行為。

## 6. 管理員／後台權限

**真實查核結果：這個專案的 Store Settings 端點（包含既有 geo-map、本輪
新增的 ga4-realtime）沒有「管理員 vs 一般員工」這種角色分層**——
`middleware/adminGuard.js` 的 `requireAdminMode` 是完全不同概念（SaaS
平台層級的 License CRUD 開關，靠 `ADMIN_MODE` 環境變數，不是店內角色），
`routes/settings.js` 完全沒有引用它。凡是持有該店有效登入 token
（`requireStore` 能解析出 `req.storeId`）就能讀寫該店設定，這是整個
Store Settings 系統既有的一致行為，本輪沒有、也不應該為了 GA4 這一個
功能就另外發明一套角色系統（會違反「不得建立第二套設定系統」）。

## 7-9. req.storeId 使用

GET／PATCH／POST 三個 handler 都直接用 `req.storeId`，SQL 語句與
`getGa4RealtimeConfig(db, storeId)`／`invalidateGa4RealtimeCacheForStore
(storeId)`／`runGa4ConnectionTest(db, storeId)` 呼叫全部把這個值往下傳，
沒有任何一處改讀 `req.body.store_id` 或 query。

## 10. Body store_id 忽略/拒絕

`validateGa4RealtimeSettingsPatch()` 的 `forbiddenKeys` 清單明確包含
`store_id`／`storeId`，出現就直接 400 拒絕（不是靜默忽略，是明確拒絕並
回傳錯誤訊息）。

## 11. Unknown Keys 拒絕

同一函式：白名單外的任何 key 一律 400（`未知的設定欄位：${k}`）。

## 12-14. Transaction／Rollback／半套設定風險

**真實發現的第一個缺口（已修正）**：原本的 PATCH 實作直接用
`utils/db.js` 既有的 `db.transaction(fn)` 包住迴圈內的**包裝過**
`db.run()` 呼叫。實測發現：包裝過的 `db.run()` 每次呼叫都會執行
`save()`（= `sqlDb.export()` + 寫檔），而 `sqlDb.export()` 在 sql.js 底層
會**提早結束**目前開啟中的 SQL Transaction。結果：
1. 若迴圈中途失敗，呼叫 `ROLLBACK` 會直接丟出
   `cannot rollback - no transaction is active`。
2. 更嚴重的是，第一筆寫入其實已經因為 `export()` 被意外 autocommit，
   完全不是原子操作——ROLLBACK 就算沒有丟例外，也救不回已經提早
   commit 的資料。

已用一段獨立腳本重現並確認（見下方「重現記錄」），修正方式：改用跟
`utils/db.js` 內既有 hotfix7 categories 遷移完全同一種安全寫法——直接用
`db._db`（原始 sqlDb）跑 `BEGIN` → 一連串 `prepare/run/free`（不呼叫任何
包裝過的 `db.run()`）→ `COMMIT` → 只在 COMMIT 成功後呼叫一次
`db._save()`。失敗時 `catch` 呼叫 `db._db.run('ROLLBACK')`（外層再包一層
`try/catch`，因為在某些「BEGIN 本身就失敗」的邊界情況下 ROLLBACK 自己
也可能丟例外，安全忽略）。

**重現記錄**（節錄）：
```
BEGIN ok
UPDATE via wrapper ok
ERROR: cannot rollback - no transaction is active
```
修正後：
```
caught: simulated failure on 2nd key
rollback ok
final rows: [{"key":"ga4_realtime_property_id","value":"000111"}]   ← 維持原值，證明真的 rollback 了
```

## 15. Cache Invalidation 發生在 Commit 前或後

修正後：`invalidateGa4RealtimeCacheForStore(storeId)` 只在 `rawDb.run
('COMMIT')` 與 `db._save()` 都成功執行「之後」才被呼叫（寫在 try block
外、成功路徑的下一行）。寫入失敗時直接 `return`，不會執行到這一行。

## 16. Generation Token 阻止舊 Request 回寫

`utils/ga4Realtime/index.js`：`getGa4RealtimeData()` 在發起 fetch 前先讀
`generationAtStart = _getStoreGeneration(storeId)`，fetch 完成時比對
`_getStoreGeneration(storeId) === generationAtStart` 才寫入 `_cache`；
`invalidateGa4RealtimeCacheForStore()` 會遞增該 store 的 generation。
已用 Smoke（見下方 D 段）驗證：Store A 設定變更期間有一個舊 in-flight
request，完成後其結果不會覆蓋新 cache。

## 17. In-flight 是否真的按 Store 清理

`invalidateGa4RealtimeCacheForStore()` 目前**不會主動中止**已經送出的
Google API 呼叫（沒有可靠的取消機制，client 端沒有暴露 cancel token），
但透過 generation 比對，即使舊 request 完成，也不會寫回 cache／
last_success_at。這是誠實的設計限制，不是「假裝有中止」。

## 18-19. Connection Test Rate Limit／Single-flight 是否按 Store

`utils/ga4Realtime/connectionTest.js` 的 `_lastTestAt`／`_lastTestResult`／
`_inFlightTest` 三個 Map 都以 `storeId` 為 key，物理上不會跨店互相影響
（已用 Smoke 驗證 Store A 進入 cooldown 不影響 Store B 可以立即測試）。

## 20. Settings 頁載入是否完全不呼叫 Google

`geoGa4SettingsInit()` → `geoGa4SettingsLoad()` 只呼叫
`GET /api/settings/ga4-realtime`（純讀 DB／記憶體狀態，`routes/settings.js`
的這兩個 handler 完全沒有 import `ga4Client.runGa4RealtimeReport` 或
`connectionTest.js`），不會觸發任何 Google API 呼叫。

## 21. Test Button 是否只在按下時呼叫 Google

`geoGa4SettingsTestConnection()` 是唯一呼叫 `POST /ga4-realtime-test` 的
函式，只綁在按鈕 `onclick`，沒有任何自動觸發路徑（`geoGa4SettingsInit()`
不會呼叫它）。

## 22. Credential Status 是否只有 Boolean

`GET /api/settings/ga4-realtime` 回應只有 `credential_available`／
`sdk_available` 兩個 boolean，沒有任何字串/物件形式的憑證資訊。

## 23. DOM 中是否有 Credential 路徑

`public/js/geo-ga4-settings.js` 的 `geoGa4SettingsRenderServerStatus()`
只組「可用／不可用」「已設定／未設定」字串，沒有任何地方把
`GOOGLE_APPLICATION_CREDENTIALS`／JSON 內容／`client_email` 寫進
`innerHTML`。已用 Smoke 靜態掃描確認。

## 24-25. auto_refresh_enabled／auto_refresh_seconds 真實來源

`auto_refresh_enabled`：`geoGa4FetchAndRender()` 在每次 activate 時額外
呼叫一次 `GET /ga4-realtime-status`，讀 `data.auto_refresh_enabled` 寫入
`geoGa4State.configAutoRefreshEnabled`；`_geoGa4ScheduleAutoRefresh()`
排程前檢查這個 flag。
`auto_refresh_seconds`：**這個欄位本身在 status endpoint 回應中固定是
`null`**（`utils/ga4Realtime/index.js` 的 `getGa4RealtimeStatus()` 誠實
回 `null`，註解說明「由前端依 quota_status 動態決定」）。真正決定秒數的
是前端 `_geoGa4ScheduleAutoRefresh()` 自己的邏輯（60／120／不排程），
不是從 status endpoint 讀一個「伺服器決定的秒數」。這是刻意的設計（避免
兩套秒數來源互相打架），已在 B2a 文件記錄過，本輪沒有改變。

## 26. Cache Seconds 與 Refresh Seconds 是否分開

是。`ga4_realtime_cache_seconds` 只影響 `utils/ga4Realtime/index.js` 的
`_cache` TTL；前端 auto-refresh 的 60/120 秒是完全獨立的另一組邏輯
（依 `quota_status`），兩者程式碼路徑不相交。

## 27-29. Save 流程

`geoGa4SettingsSave()`：驗證失敗 → 顯示欄位錯誤，不呼叫 PATCH，保留輸入
（Smoke D41/D42 驗證）。驗證通過 → PATCH → 成功後再呼叫一次
`geoGa4SettingsLoad()`（GET）→ 呼叫 `geoGa4NotifySettingsChanged()`。

## 30. Test Cooldown 是否只有一個 Timer

`geoGa4SettingsUpdateCooldown()` 每次呼叫都先 `clearInterval(...
cooldownTimer)` 再建立新的，不會疊加多個 interval。

## 31-33. Disabled Layer／Single-store mode UI

`ga4RealtimeSinglePropertyMode` checkbox 的 `disabled` 屬性由
`server_single_store_mode_available` 決定（`geoGa4SettingsPopulateForm()`）；
前端驗證層 `geoGa4SettingsValidateForm()` 額外擋下「server 不允許時仍勾選
single mode」的情況（回傳 `errors.general`），防止使用者送出這個組合
（雖然 Server 端 `getGa4RealtimeConfig()` 本來就不會因為店家設定
`single_property_mode=true` 就自動繞過 `GA4_REALTIME_SINGLE_STORE_MODE`
的部署層級開關，屬於雙重保護）。

## 34. Error Code 中英文映射

`GA4_SETTINGS_TEST_ERROR_MESSAGES` 涵蓋 `credential_unavailable`／
`credential_invalid`／`permission_denied`(+`403`)／`property_not_found`
(+`404`)／`stream_filter_invalid`／`quota_limited`(+`429`)／`ga4_timeout`／
`ga4_unavailable`，共 7 種語意（8 個 key，`403`/`404`/`429` 是數字別名）。

## 35. Real GA4 Live Test

**NOT TESTED**。沙箱沒有真實 Google Cloud 憑證，也沒有出網 allowlist 對
`googleapis.com` 的存取。所有連線測試驗證都透過 fake GA4 client 完成。

## 本輪修正的兩個真實缺口總結

1. **PATCH 寫入不是真正原子操作**（見 §12-14）——已修正為使用 raw
   `db._db` transaction，避開包裝過 `db.run()` 的 `export()` 副作用。
2. **Rate-limited 回應缺少 `retry_after_seconds`**（需求文件五要求）——
   已在 `connectionTest.js` 補上，計算方式 `Math.max(0, Math.ceil((RATE_
   LIMIT_MS - elapsed)/1000))`，保證非負數；前端
   `geoGa4SettingsTestConnection()` 也已改為優先使用這個值決定 cooldown
   秒數，沒有值時才 fallback 30 秒。
