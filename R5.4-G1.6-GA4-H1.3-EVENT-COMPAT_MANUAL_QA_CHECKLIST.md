# R5.4-G1.6-GA4-H1.3-EVENT-COMPAT — Manual QA Checklist

fix18-10-hotfix30-B5-R5.4-G1.6-GA4-H1.3-EVENT-COMPAT

部署前狀態：**全部項目 NOT TESTED**（本文件只列出部署後需要人工用真實 GA4 Property 驗證的項目；自動化測試已在 Test Report 涵蓋，但自動化 Fake Client 測試 ≠ 真實 Google API 驗證）。

---

## 一、Realtime Event Metric（★ 本輪最關鍵，Variant B 是否真的解決正式問題）

| # | 項目 | 狀態 |
|---|---|---|
| 1 | Visitors 最近 5 分鐘 | NOT TESTED |
| 2 | Visitors 最近 30 分鐘 | NOT TESTED |
| 3 | 商品瀏覽（view_item）最近 5 分鐘 | NOT TESTED |
| 4 | 商品瀏覽（view_item）最近 30 分鐘 | NOT TESTED |
| 5 | 加入購物車（add_to_cart）最近 5 分鐘 | NOT TESTED |
| 6 | 加入購物車（add_to_cart）最近 30 分鐘 | NOT TESTED |
| 7 | 開始結帳（begin_checkout）最近 5 分鐘 | NOT TESTED |
| 8 | 開始結帳（begin_checkout）最近 30 分鐘 | NOT TESTED |
| 9 | 完成購買（purchase）最近 5 分鐘 | NOT TESTED |
| 10 | 完成購買（purchase）最近 30 分鐘 | NOT TESTED |
| 11 | 若某個 Metric 目前為 0 事件，畫面顯示「最近 N 分鐘此事件目前為 0」，**不出現紅色錯誤框** | NOT TESTED |
| 12 | Refresh 按鈕：重新整理後維持目前選定的 Metric／Window，不重設回 Visitors | NOT TESTED |
| 13 | 瀏覽器 Console 沒有出現新的 Error（尤其是 400/未預期例外） | NOT TESTED |

**若第 3～10 項中有任何一項仍然失敗**：呼叫 `POST /api/geo-live/ga4-realtime-test`，body 帶 `{ "event_compat": true }`（或 query `?event_compat=1`），檢查回應裡對應 Metric 的 `_ok`／`_stage`／`_code` 欄位，可以立即知道是哪個 Metric、哪個 Stage 失敗，不需要重新盲猜。若確認 Variant B 也失敗，考慮 Variant C（見 Reality Audit 三，需另開一輪）。

## 二、H1.2 既有功能不應被本輪影響

| # | 項目 | 狀態 |
|---|---|---|
| 14 | Pingzhen District Mapping 仍正確 → 桃園市 | NOT TESTED |

## 三、Historical

| # | 項目 | 狀態 |
|---|---|---|
| 15 | 近 7 天 Historical 仍有資料（確認本輪沒有破壞既有 PASS 狀態） | NOT TESTED |
| 16 | Today 手動同步 | NOT TESTED |
| 17 | Today 若 `rows_saved=0`：畫面顯示中性文案「同步成功，目前 GA4 報表尚無可用的區域資料」，**不是紅色錯誤**，且不清空已有的近 7 天資料 | NOT TESTED |
| 18 | 表格顯示「加購事件／人」（不是「加購率」） | NOT TESTED |
| 19 | 表格顯示「購買事件／人」（不是「購買率」） | NOT TESTED |
| 20 | 畫面完全不出現「500%」或「400%」等百分比字樣 | NOT TESTED |
| 21 | 龍潭區範例（若資料還在）：活躍使用者=2、加購事件=10、購買事件=8 時，畫面應顯示「5.0」與「4.0」 | NOT TESTED |

## 四、環境注意事項

- 部署後第一次驗證前，先確認店家設定裡 `ga4_realtime_enabled`／Property ID／Stream ID 都已正確設定（本輪沒有修改設定儲存方式）。
- Event Compatibility Probe（`event_compat=true`）預設不會自動執行，只有明確帶這個旗標才會多消耗 4 次 GA4 API Quota，日常操作不需要每次都帶。
- 若第 3～10 項全部失敗且第 1～2 項（Visitors）正常，代表問題確實侵限於 Event Metric 相容性，符合本輪修正的假設範圍；若連 Visitors 也失敗，代表是別的問題（Auth／Property／Stream 設定），不屬於本輪修正範圍，請先排查 H1.1/H1.2/G1.5 既有的診斷工具。
