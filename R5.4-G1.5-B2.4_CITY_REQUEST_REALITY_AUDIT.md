# R5.4-G1.5-B2.4 — City Request Reality Audit

fix18-10-hotfix30-B5-R5.4-G1.5-B2.4 — GA4 City Request Compatibility & Partial
Success Hotfix

本文件記錄本輪修正前的真實狀況（正式環境回報現象、程式碼實際邏輯），以及
修正後的行為。所有陳述以「已在本輪 Smoke／Regression 中實際驗證過」的內容
為限；沒有 Google Raw Error 佐證的部分，明確標示為未證實，不誇大結論。

---

## 1. 正式 Connection Test PASS

使用者回報的正式環境狀態（修正前）：

- GA4 Service Account Credential：PASS
- GA4 Property Permission：PASS
- Stream Filter Connection Test：PASS
- Settings 頁面「測試連線」顯示：「連線成功，目前最近30分鐘沒有即時資料。」

即：`POST /api/geo-live/ga4-realtime-test` 回應 `connected:true`。

## 2. 正式 Data Endpoint 502

同一時間，Chrome Console 實際請求：

```
GET /api/geo-live/ga4-realtime?window=5&metric=visitors&refresh=...
```

回應：`HTTP 502 Bad Gateway`。

已排除：舊 `/ga4-visitors` Endpoint、Browser Cache、Store Token、Service
Account 基本憑證、Property 權限。問題範圍縮小到正式 GA4 Data Orchestrator 的
Summary／City Request 流程。

## 3. Summary Request

`utils/ga4Realtime/requestBuilder.js` 的 `buildGa4RealtimeSummaryRequest()`：

- `dimensions: []`（無維度，只回官方加總的 `activeUsers`／`eventCount`／視
  metric 而定的 `screenPageViews`）。
- `minuteRanges` 依 `window` 為 5 或 30 分鐘。
- 依 `streamId`／`eventName` 組 `dimensionFilter`。

這是 `total_active_users_ga4` 唯一合法來源，不得從 City Request 的 rows 加總
得出（沿用 G1.5-A 既有規則）。

## 4. City Request

`buildGa4RealtimeCityRequest()`——本輪修正前後對比：

| | 修正前 | 修正後（B2.4） |
|---|---|---|
| dimensions | `city`／`cityId`／`country`／`countryId`（四維） | `city`／`countryId`（二維） |

## 5. Connection Test 原始假陽性

`utils/ga4Realtime/connectionTest.js` 的 `_runTestNow()` 修正前邏輯：

```js
const [summaryResult, cityResult] = await Promise.all([...]);
if (!summaryResult.ok) {
  return { connected: false, ... };
}
// 不論 cityResult.ok 是否為 false，都往下走到這裡：
return { connected: true, property_accessible: true, stream_filter_valid: true,
         realtime_request_ok: true, ... };
```

`rowsCount` 用 `cityResult.ok ? cityResult.rows.length : 0` 計算，但
`connected` 完全沒有檢查 `cityResult.ok`。結果：Summary 成功、City 失敗時，
Connection Test 仍回 `connected:true`。

## 6. Data Endpoint 原始處理

`utils/ga4Realtime/index.js` 的 `_fetchAndBuildPayload()` 修正前：

```js
if (!summaryResult.ok) throw new Ga4RealtimeError(summaryResult.code, summaryResult.retryable, 502);
if (!cityResult.ok) throw new Ga4RealtimeError(cityResult.code, cityResult.retryable, 502);
```

City 失敗與 Summary 失敗被同等對待，兩者都讓整個 request 502。

## 7. Summary／City 判斷差異（假陽性根因）

Connection Test 與正式 Data Endpoint 對「City 失敗」的判斷邏輯不對稱——
Connection Test 忽略 City 失敗，Data Endpoint 不忽略——造成「測試顯示成功、
地圖打不開」的矛盾現象。這正是使用者回報現象的直接技術根因。

## 8. 原四維 City Request

City Request 修正前送出四個維度：`city`、`cityId`、`country`、`countryId`。

## 9. 聚合器實際只需兩維

`_aggregateCityRows(rows, dimensionHeaders)`（`utils/ga4Realtime/index.js`）
逐行核對後確認：只讀取 `city`（縣市名稱正規化用）與 `countryId`（排除非
`TW` 資料用）。`cityId`／`country` 兩個維度自 G1.5-A 建立以來從未在聚合邏輯
中被讀取。

## 10. 新最小 Dimensions

```js
dimensions: [{ name: 'city' }, { name: 'countryId' }]
```

## 11. 重要限定：不得宣稱已證明四維就是 Google 失敗根因

**四維 Request 是目前最明顯的相容性風險，已依實際使用欄位最小化；目前沒有
取得 Google Raw Error 證明 cityId/country 為唯一根因。**

本輪修正的直接、可驗證的動機是「聚合器從未使用這兩個維度，移除它們沒有任何
功能損失，且降低了 Realtime Dimensions 相容性風險與回應大小」——這是可以
獨立成立的優化理由，不依賴於「證明了 Google 因為這兩個維度而拒絕請求」。
`client.js`／`errors.js` 的錯誤分類（`classifyGa4RealtimeError`）刻意不保留
Raw Google Error 內容，因此本專案（依現有安全紀錄原則）無法、也不應該在
文件中宣稱已經拿到 Google 端的原始錯誤訊息證明維度是唯一根因。真正的 Google
端 502／City Request 失敗原因，仍待正式環境重新部署後以「本輪已建立的
Safe Diagnostics Log」（見第 13 節）觀察 `stage=city code=... retryable=...`
分類代碼，才能進一步縮小範圍。

## 12. Request Pair

新增 `utils/ga4Realtime/requestPair.js`，提供 `runGa4RealtimeRequestPair()`：

- 輸入已組好的 `summaryRequest`／`cityRequest`（呼叫端組，本模組不知道
  Property／Stream 是否合法）與一個 `runFn(request)` 執行函式。
- 內部用 `Promise.all` 平行送出兩個 stage，各自計時（`elapsed_ms`）。
- 供 `connectionTest.js`（不重試）與 `index.js`（`_runWithRetry` 包裝、有
  重試）共用同一份「分階段執行＋安全 log」邏輯，避免兩處各自維護一份走樣。

## 13. Safe Logging

每個 stage 執行後印出一行：

```
[ga4-realtime] stage=summary code=OK retryable=false window=30 metric=visitors elapsed_ms=1
[ga4-realtime] stage=city code=503 retryable=true window=30 metric=visitors elapsed_ms=1002
```

只包含：`stage`／`code`（分類後的安全代碼，非原始例外訊息）／`retryable`／
`window`／`metric`／`elapsed_ms`。已在 B2.4 Smoke（B29-B32 及 J123/J124）
實際擷取 `console.log` 輸出驗證：不含 Property、Stream、Credential 關鍵字，
也不含原始例外訊息文字。

## 14. Partial Success

Summary 成功、City 失敗時，`_fetchAndBuildPayload()` 不再 throw，改為回傳：

```js
{
  status: 'partial',
  summary: { total_active_users_ga4, event_count, screen_page_views, mapped_counties: 0, unmapped_city_rows: 0, excluded_non_tw_rows: 0 },
  counties: [], unmapped: [],
  error_code: 'city_request_failed', error_stage: 'city',
  notices: ['GA4 即時總覽已取得，但城市區域資料暫時無法載入。', DISCLAIMER],
}
```

Route 對這個 payload 回 `HTTP 200 { success:true, data }`，不是 502。

## 15. Partial Cache Policy

`getGa4RealtimeData()` 的 cache 寫入條件由「generation 吻合」改為「generation
吻合 **且** `payload.status !== 'partial'`」：

```js
if (_getStoreGeneration(storeId) === generationAtStart && payload.status !== 'partial') {
  _cache.set(cacheKey, { data: payload, fetchedAt: ..., expiresAt: ..., lastSuccessfulAt: ... });
}
if (payload.status !== 'partial') {
  _storeLastSuccessAt.set(storeId, payload.fetched_at);
  _storeLastErrorCode.set(storeId, null);
}
```

Partial payload 本身固定 `is_cached:false`／`is_stale:false`，且從不寫入
`_cache`，因此不會在下一次 cache-hit 時被誤當成「這是使用者上次真正拿到的
完整城市資料」回傳。

## 16. Full Cache 保留

若呼叫 `getGa4RealtimeData()` 前已經存在一筆 Full Cache（Summary+City 都
成功寫入的），這次 City 失敗产生的 Partial 結果**完全不觸碰**該 cache
entry——已在獨立腳本驗證（見 Fix Report 第 12 節「Partial Cache 專項驗證」）：
Partial 發生前後，該 cache entry 的 `JSON.stringify(data)` 逐 byte 相同。

## 17. 5 分鐘 Range

```js
windowMinutes === 5 → { name: 'last_5_minutes', startMinutesAgo: 4, endMinutesAgo: 0 }
```

## 18. 30 分鐘 Range

```js
windowMinutes === 30 → { name: 'last_30_minutes', startMinutesAgo: 29, endMinutesAgo: 0 }
```

本輪未變更 Minute Range 邏輯，5／30 分鐘功能保留，未因 502 而移除或改動。

## 19. Manual Visual：NOT TESTED

本輪所有驗證皆透過自動化 Smoke／Regression（真實函式呼叫、jsdom、假
GA4 Client、真實 sql.js DB），**沒有**在瀏覽器裡人工操作過 Settings 頁面或
地圖圖層。Manual Visual QA 尚未執行。

## 20. Real GA4 Map Endpoint：待部署驗證

本輪修正基於使用者回報的正式環境現象與程式碼邏輯稽核，修正內容尚未在正式
GA4 Property／Stream 上重新驗證過。部署後仍需要：

1. 確認 Connection Test 在真實 City Request 失敗情境下正確回報
   `connected:false`／`error_stage:'city'`。
2. 確認 `GET /api/geo-live/ga4-realtime` 在同樣情境下回 `HTTP 200
   status:'partial'`，而不是 502。
3. 觀察 Server Log 的 `[ga4-realtime] stage=city code=...` 是否揭露更多
   Google 端實際錯誤分類，作為後續（若有必要的）G1.5-C 系列調查依據。

---

*本文件不含任何真實 Property ID、Stream ID、Service Account Email 或
private key 內容；所有範例值皆為程式碼中的佔位符或測試假資料。*
