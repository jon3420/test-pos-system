# H1.4.10 Phase 3 — CHECKOUT-SUBMIT-DIAGNOSTICS + PAYMENT-SUCCESS Reality Audit

## 1. Submit Button Reality

`public/line-order.html`：確認下單按鈕 `#subBtn`，`onclick="submitOrder(event)"`。
`public/line-shipping.html`：確認下單按鈕 `#submitBtn`，`onclick="submitOrder(event)"`。

實際 grep 確認兩頁的 `submitOrder(...)` 呼叫點各自只有一個（就是這個按鈕的 `onclick`），沒有 render／restore／auto-resume／LINE callback 會直接呼叫它。因此 Phase 3 可以安全建立 explicit submit-button causal contract，不會誤傷其他既有內部流程。

## 2. checkout_submit_click Reality

只有 `event.type==='click' && event.currentTarget===真正確認下單按鈕` 才送出 `checkout_submit_click`。直接呼叫 `submitOrder()`（無 event）或傳入其他元素的 click event，皆不產生此事件（已在 jsdom 中對兩頁分別驗證）。

與 H1.4.9 `checkout_click` 不同：`checkout_submit_click` **不**以 `cart_id` 永久去重——它代表「使用者每一次真實嘗試送單」，同一顧客兩次真實點擊會各自留下一筆。

## 3. Validation Reality（以 production 程式碼實際內容為準）

**line-order.html** 實際 `trackCheckoutValidationFailed()` 呼叫點與對應 reason：
`missing_name` / `missing_phone` / `empty_cart` / `missing_fulfillment` / `mode_unavailable` / `product_mode_conflict` / `missing_payment` / `missing_date` / `missing_time` / `missing_address` / `address_not_resolved` / `delivery_fee_pending` / `order_cutoff` / `line_member_required`。

**line-shipping.html** 實際呼叫點：
`empty_cart` / `minimum_order_not_met` / `missing_name` / `missing_phone`（原本 `if(!name||!phone)` 共用一個 branch，未拆分業務邏輯，只用三元運算式挑選正確 reason，toast 文案與判斷順序完全未變）/ `missing_address` / `missing_payment` / `line_member_required`。

`line_member_required` 只會在 `gate_mode==='checkout'`（強制登入 Gate）且驗證失敗時產生；`friend_checkout`（Phase 2 的非阻塞柔性引導）在程式碼結構上完全不會進入這個 `if` 分支，因此不可能產生 `line_member_required`（已用 jsdom 測試驗證）。

未新增 `out_of_range`／`inventory_unavailable`／`coupon_invalid`——這些若存在，只出現在後端 `/api/line-orders` 的回應（`orderRes.message`），不是 `submitOrder()` 送出網路請求前的 client-side 阻擋 validation，本階段刻意不納入，避免偷改語意。

## 4. Validation Event Contract

每一次真正 submit click：`checkout_submit_click=1`。若被擋下：`checkout_validation_failed` 最多 1 筆，只記第一個真正阻擋送單的 `reason_code`（例如同時缺 name/phone/payment，只記 `missing_name`；因為程式碼是一連串 `if(...){track(...);return;}`，一遇到第一個為真的條件就 `return`，後面的 `if` 根本不會執行到）。使用者修正後再按，才形成第二個 submit attempt，各自獨立記錄。

## 5. LINE Pay Authority Boundary

`routes/linepay.js` 的 `GET /confirm`：只有 `data.returnCode === '0000'`（LINE Pay 官方 Confirm API 回傳成功）才視為真正付款成功。`POST /request` 只代表「建立付款請求、取得 payment_url」，程式碼裡完全沒有 `payment_success` 寫入呼叫（已用靜態掃描 + 真實 HTTP 測試雙重確認）。

## 6. payment_success Authority

`payment_success` 列在 `routes/analytics.js` 的 `SERVER_ONLY_EVENTS`，前端 `POST /api/analytics/events` 送這個 `event_name` 會被拒絕（已用真實 HTTP 測試確認回傳 400/403、DB 沒有寫入任何列）。唯一的 server write 在 `routes/linepay.js` 的 Confirm 成功分支，`value` 來源是 DB `orders.total`（該 order 建立時後端算好的權威金額），不是前端傳入的任何 total／query total／request body total。

## 7. Idempotency

`payment_success` 沿用既有 `hasEventForOrder()`（`utils/analyticsLog.js` 的 `logServerEvent()`），與 `submit_order`／`purchase` 用同一套「同一 `store_id`+`order_id`+`event_name` 只能有一筆」機制，重複 Confirm 兩次（皆回 `0000`）驗證後仍只有 1 筆，`purchase` 原本的 dedupe 也維持不變。

## 8. GA4 / Meta Reality

`checkout_submit_click`／`checkout_validation_failed`／`payment_success` 三者皆**沒有**加入 `public/js/analytics-platforms.js` 的 `GA4_EVENT_MAP`／`META_EVENT_MAP`（已用靜態掃描確認）。因此不會造成重複 `begin_checkout`／`InitiateCheckout`／`Purchase`。既有 `checkout_click → GA4 begin_checkout`／`checkout_click → Meta InitiateCheckout`／`purchase → GA4/Meta Purchase` 契約維持原樣，一個字元未動。

## 9. Dashboard Scope Decision

Phase 3 沒有新增 Dashboard UI。核心目標是先建立可信事件（三個新診斷事件的正確性、安全性、去重與權威邊界），視覺化呈現（漏斗卡片、驗證失敗排行等）刻意 defer 到後續 Recovery / Dashboard Phase，避免在同一輪動到 H1 Analytics 既有大範圍 UI/canonical 架構。這是有意識的 scope control，不是遺漏。
