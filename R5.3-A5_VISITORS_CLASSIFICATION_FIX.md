# R5.3-A5 — Geo Visitors Classification Bug Fix

## 1. 回報症狀

使用者提供三張真實畫面截圖，確認已部署 `fix18-10-hotfix30-B5-R5.3-A4-QA-full.zip`
（含統一 Metric Bar，可正常切換 8 個指標），但點擊「訪客」指標後數字仍是
0，而同一時刻：

- 老闆儀表板「目前在線」顯示 1 人
- Analytics 轉換漏斗「商品瀏覽」顯示 1 人、100%

使用者明確要求：直接檢查 Geo Event Engine 與 Analytics 事件同步鏈，不要
再檢查 UI 部署狀態（UI 部署已確認正常，問題在資料層）。

## 2. 根因

用最小重現資料實際驗證（`utils/dashboardAnalytics.js` 的既有函式
`getRealtime()` vs `utils/geoEventEngine.js` 的 `getGeoEventFunnel()`）：

```
insertEvent(db, { store_id, visitor_id: 'v1', session_id: 's1',
                   event_name: 'view_product', product_id: 1, geo: null });

老闆儀表板 目前在線: 1
Geo Event Engine Visitors: 0   ← 修正前
```

`getRealtime()`（既有函式，本輪未修改）計算「目前在線」時，對
`analytics_events` 不篩選 `event_name`，任何事件都算在線。但
`utils/geoEventEngine.js` 的 `VISITOR_EVENT_NAMES` 只包含
`page_view`／`session_start` 兩種事件（沿用 R5.3-A2 需求文件當時的正式
分類定義）。

真實流量中，使用者經常透過商品連結（LINE 深連結／QR Code／分享連結）
直接落在商品頁，**第一個、也是唯一一個被追蹤到的事件是 `view_product`，
從未觸發過 `page_view`**。這類使用者：

- 老闆儀表板「目前在線」：✅ 算作 1 人（任何事件都算）
- Analytics 轉換漏斗「商品瀏覽」：✅ 算作 1 人（本來就是 view_product 的統計）
- **Geo Event Engine「訪客」：❌ 算作 0 人**（因為 `view_product` 不在
  `VISITOR_EVENT_NAMES` 裡）

這是分類邏輯本身的真正 Bug，不是 UI 沒同步呼叫、不是部署問題、不是
`geo_visit_log` 沒寫入（這筆事件確實有寫入 `geo_visit_log`，只是
「Visitors」這個 SQL 聚合條件把它排除在外）。

## 3. 修正

`utils/geoEventEngine.js` 新增 `ALL_FUNNEL_EVENT_NAMES`（涵蓋
`page_view`／`session_start`／`view_product`／`add_to_cart`／
`begin_checkout`／`purchase` 全部 6 種已分類事件），`getGeoEventFunnel()`
的 `visitors` 欄位改成統計這個更完整的集合，而不是只看
`VISITOR_EVENT_NAMES`。

「Visitors 是漏斗最上層，語意上必須涵蓋『有觸發任何一個漏斗事件』的
人」——這跟 `getRealtime()` 的「任何事件都算在線」語意一致，也符合漏斗
圖示本身的邏輯（能到 View Item／Add To Cart 等後續階段，前提就是這個人
已經「造訪」了）。

**下游各階段指標語意完全不變**：`view_item_visitors`／
`add_to_cart_visitors`／`begin_checkout_visitors`／`purchase_visitors`
仍然各自只統計對應的單一事件類型，沒有被這次修正影響（見
`smoke-hotfix30-b5-r5-3-a5-visitors-classification-fix.js` 的
A5-18～A5-20）。

## 4. 驗證

新增 `scripts/smoke-hotfix30-b5-r5-3-a5-visitors-classification-fix.js`
（20 項 assertion）：

- 精確重現並驗證修正原始回報症狀（A5-1～A5-4）。
- 額外驗證只有 `add_to_cart`／`begin_checkout`／`purchase`（皆無
  `page_view`）的 session 同樣正確計入 Visitors（A5-5～A5-8）。
- 既有 `page_view` 情境行為不變（A5-9）。
- 同一人觸發多種事件仍正確去重為 1 人，沒有因為擴大事件集合而重複計算
  （A5-10）。
- Unknown 訪客搭配 `view_product`-only 情境仍正確（A5-11）。
- 各階段指標語意未受影響（A5-18～A5-20）。

**完整 27 套既有 Regression 重新執行，全部 0 FAIL**（A1 128/128、A1.1
112/112、A1.2 188/188、A2 228/228、A4 110/110、B2 620/620、B3 157/157，
其餘全綠），`node --check` 全專案通過。

## 5. 影響範圍確認

- 未修改 Analytics Event Engine（`utils/analyticsLog.js` 的
  `INSERT INTO analytics_events` 本身、`EVENT_WHITELIST`）。
- 未修改老闆儀表板即時統計（`utils/dashboardAnalytics.js`，SHA-256 逐
  位元組相同）。
- 未修改 Order Heatmap Engine（`geo-heatmap.js`）。
- 只修改 `utils/geoEventEngine.js` 一處（`visitors` 欄位的 SQL 條件
  來源），影響範圍精準侷限在這次找到的真正根因。
