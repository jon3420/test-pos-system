# R5.4-G1.6-A1.2 — Implementation Report

fix18-10-hotfix30-B5-R5.4-G1.6-A1.2 — Authoritative Administrative
Representative Points & Region-only Marker Payload

## 1. Scope

本輪只解除：

- Authoritative Representative Point Source（官方 Catalog）
- Region-only Marker Payload（Dashboard／Visitor 兩端）
- Visitor Estimate Marker（district_centroid／county_centroid）
- Dashboard Estimate Marker

**不包含**（刻意排除，留給後續階段）：

- Visitor IP Geo Attribution（G1.6-A2 的職責）
- GA4 個別訪客回填（本專案任何階段都禁止）
- G1.6-A2 本身
- G2

## 2. Official Source Gate

- 兩個官方 Archive（Dataset 7442／7441）已驗證：完整 SHP 組成檔
  （.shp/.shx/.dbf/.prj，另含 .cpg/.sbn/.sbx/.shp.xml）、Archive
  SHA-256 已計算並記錄於 `SOURCE_MANIFEST.json`、DBF 欄位
  （COUNTYCODE/COUNTYNAME/COUNTYENG、TOWNCODE/TOWNNAME/TOWNENG/
  COUNTYCODE）核對通過、CRS（GCS_TWD97[2020] → EPSG:3824）已從 `.prj`
  讀出並記錄。
- 詳細稽核見 `R5.4-G1.6-A1.2_AUTHORITATIVE_SOURCE_REALITY_AUDIT.md`。

## 3. Correction Layer

`Town_Majia_Sanhe.shp`（隨鄉鎮市區 ZIP 附帶）**沒有**被隨附的
`修正清單_11403.xlsx` 提及（該清單記錄的是新竹縣尖石鄉／苗栗縣泰安鄉／
臺中市和平區／臺南市善化區的縣市界／村里界釐整，跟瑪家鄉無關）。

用 Shapely 分析幾何關係：不相等、不互相完整包含，
`symmetric_difference` 面積約占主檔面積的 8.6%。

**決策：`majia_correction_status = "reference_only"`**——Catalog 只用
主檔案（`TOWN_MOI_1140318.shp`）計算瑪家鄉的 Representative Point，
不 append 為第 369 筆、不整筆取代、不自行 union／difference。

## 4. Deterministic Catalog

- `scripts/build-authoritative-admin-points.js`：讀取 SHP（`shapefile`
  函式庫＋明確 `.cpg` 編碼）、CRS 轉換（`proj4`，EPSG:3824→EPSG:4326）、
  Representative Point（`@turf/turf` 的 `pointOnFeature()` ＋
  `booleanPointInPolygon()` 驗證）。
- `scripts/verify-authoritative-admin-points.js`：獨立驗證已產生的
  Catalog（不重新解析 SHP），57/57 通過。
- Catalog 內容：22 counties、368 districts、`point_method:
  "point_on_surface"`、`content_sha256`（排除 `generated_at` 後的核心
  內容雜湊，已實測兩次獨立 Build 結果相同）。
- **JavaScript 物件 key 列舉的真實行為（不得誤述為單純字典序排序）**：
  ECMAScript 規格規定「整數樣式 key」（不含前導零的數字字串，例如
  `"68000"`）一律強制數值遞增列舉，無論程式碼是否呼叫過
  `.sort()`；只有「非整數樣式 key」（例如帶前導零的
  `"09007"`／`"09020"`，金門縣／連江縣）才會照程式碼指定的順序排在
  整數樣式 key 之後。這代表 Catalog 的 `counties`／`districts` 物件
  key 順序**不是**單純字母排序，而是「整數樣式 key 數值遞增，其餘
  key 依插入順序接續」——但這個順序完全 deterministic、可重現（已用
  兩次獨立 Build 逐字比對驗證），只是措辭上不能說成
  「lexicographic sort」。已在 `build-authoritative-admin-points.js`
  的 `sortedObject()` 函式旁加上說明註解。

## 5. Runtime Resolver

`utils/authoritativeAdminPointCatalog.js`：
- `loadCatalog()`／`getCatalogStatus()`：惰性載入＋cache，失敗時回
  `available:false` 與對應 `error_code`，不 throw。
- `getCountyRepresentativePoint()`／`getDistrictRepresentativePoint()`：
  直接查表。
- `resolveAdministrativeRepresentativePoint(input)`：輸入優先順序
  district_code → county_code+district_name → county_code →
  county_name → null。**裸 `"Taoyuan District"` 沒有 county_code 時
  仍回 `null`**（已用 Smoke N213 實測驗證），不因為 Catalog 現在可用
  就放寬這個既有的模糊名稱保護。

## 6. Visitor Layer

`utils/geoVisitLog.js` 的 `getGeoVisitAreas()` 新增
`resolveAreaRepresentativeMarker()` enrichment，每個 area row 多一個
`marker: { available, lat, lng, accuracy, coordinate_source,
county_code, district_code }` 欄位。

`public/js/geo-visitor-layer.js` 的 `geoVisitorBuildMarkerPoints()`
**只信任**後端算好的 `area.marker`，且必須通過
`_geoVisitorIsTrustedMarker()`（`coordinate_source` 落在明確 allowlist
`['nlsc_official_boundary_representative_point']` 內，`accuracy` 合法，
lat/lng finite）才採用——不自己從 `city`／`district` 名稱猜座標。
Unknown 一律不建立 Marker。Ranking／Coverage 既有計算邏輯完全不變。

## 7. Dashboard

新增 `GET /api/geo-live/marker-model`（`getGeoLiveMarkerModel()`）。
`public/js/geo-live-layer.js` 的 `refresh()` 在 `markers`／`cluster`
模式下，同一次呼叫既有 `/markers`（Exact，`_renderMarkers()` 不變）之外
另外 fetch 這個新 Endpoint，把 `estimate_points` 餵給
`renderEstimateMarkers(buildEstimateMarkerPointsFromModel(...))`（A1.1
的 Shared Renderer）。既有 `requestSeq`／`AbortController`／
`isStaleResponse()` 機制原樣沿用，新 Endpoint 的請求同樣受保護。
Estimate Endpoint 失敗或 Catalog 不可用時，`renderEstimateMarkers([],
true)`——只清空 Estimate、標記 blocked notice，完全不影響 Exact
Renderer 或既有 Summary。

## 8. Dedupe

同一 entity 只能出現在 `exact_points`、`estimate_points`、或 unknown
count 三者之一：

- Entity Key：`store_id + visitor_key`（`VISITOR_KEY_SQL`，涵蓋
  `visitor_id`／`session_id` 既有 canonical 定義；訂單相關事件沿用
  同一套 key，`order_id` 目前作為既有 `analytics_events.order_id` 的
  同步欄位，不是本輪新增的獨立去重鍵）。
- `getGeoLiveMarkerModel()` 先建立 `exactVisitorKeys` 集合（來自既有
  Exact 查詢），Region Aggregation Query 明確排除這個集合內的
  `visitor_key`，確保不會同時出現在兩邊。
- District Marker：同 `accuracy='district_centroid'` + `district_code`
  聚合成一個 Marker；County Marker：同 `accuracy='county_centroid'` +
  `county_code` 聚合成一個 Marker。
- `count`／`unique_visitors` 是 unique entity 數（`Set.size`），
  `event_count` 另外累加——**不等於 Marker 數量**，已用真實 sqlite
  fixture 測試驗證（同一 visitor 的 `page_view`+`add_to_cart` 兩個
  事件，聚合結果 `unique_visitors=1`／`event_count=2`）。

## 9. Backward Compatibility

- `GET /api/geo-live/markers` 的路由定義與 Handler 呼叫
  （`_safeHandler(getGeoLiveMarkerPoints)`）逐字未變。
- `public/js/geo-heatmap.js`（Order Heatmap）本輪完全未修改，也沒有
  引用任何本輪新增的 Renderer／Endpoint。
- `public/js/geo-ga4-realtime-layer.js`（GA4 Layer）同樣完全未引用
  `window.GeoMarkerRenderer`／`marker-model`，維持自己獨立的 Choropleth
  呈現。
- A1.1 的 `geo-marker-renderer.js`／HTML Script Wiring／Browser
  Namespace／Lifecycle Cleanup 全數保留，未被回退。
- B2.5（行政區正規化）／B2.4（GA4 Partial Success）的程式與測試全數
  保留在本輪工作目錄與最終 ZIP 內。

## 10. Attribution／Privacy

Marker Legend／說明文字包含：「行政區推估標註資料來源：內政部國土測繪
中心行政區界線開放資料。」（`geoMarkerBuildLegendHtml()`／
Blocked Notice 文案，一次顯示，不隨每個 Marker 重複長篇顯示）。

`getGeoLiveMarkerModel()`／`resolveAreaRepresentativeMarker()` 回應
與內部函式本體皆已檢查：不含 raw IP、完整 `visitor_id`／`session_id`
欄位、地址、Token、Cookie、GA4 identity（見 Smoke section I／Static
Audit section H，逐項通過）。

## 11. Validation

- A1.2 Smoke：**250 PASS ／ 0 FAIL ／ 250 TOTAL**，連續三次一致。
- A1.2 Static Audit：**125 PASS ／ 0 FAIL ／ 125 TOTAL**，連續三次
  一致。
- Catalog Verify：**57 PASS ／ 0 FAIL**。
- Regression（三輪，約 30 支既有／新增 Suite）：全數 PASS，FAIL=0，
  exit=0，三輪數字完全一致。
- Security Scan：無真實 Credential，僅 denylist／文件說明字串命中。
- Manual QA：**NOT TESTED**（見
  `R5.4-G1.6-A1.2_MANUAL_QA_CHECKLIST.md`）。

詳細數字與三輪 Regression 清單見
`R5.4-G1.6-A1.2_TEST_REPORT.md`。

## 12. Gate

**Estimate Marker Gate**：待 ZIP／Clean Extraction 驗證完成後於
`R5.4-G1.6-A1.2_TEST_REPORT.md`／回報訊息中給出最終判定（本文件不
預先填入，避免 ZIP 內容與最終判定不同步）。

---

*本文件不含任何真實憑證、Property、Stream 或 Token 內容。*
