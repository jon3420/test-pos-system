# R5.4-G1.6-GA4-H1.1-AUTH — Implementation Report

## 範圍

只修改 `public/js/geo-ga4-h1-panel.js`（GA4-H1 前端面板）與新增測試/工具
腳本。**未修改**：`middleware/storeGuard.js`、`public/js/app.js`
（`apiFetch` 本身）、`routes/ga4-geo.js`、`routes/geo-live.js`、
`utils/ga4Realtime/*`、`public/js/geo-ga4-realtime-layer.js`。

## 1. `geoGa4H1ApiRequest(url, options)`

集中的 Auth Helper，取代散落的裸 `fetch()`。

```js
async function geoGa4H1ApiRequest(url, options = {}) {
  const winApiFetch = (typeof window !== 'undefined') ? window.apiFetch : undefined;
  const fetchFn = (typeof winApiFetch === 'function') ? winApiFetch
    : (typeof apiFetch === 'function' ? apiFetch : null);
  ...
}
```

- 優先 `window.apiFetch`，其次 bare `apiFetch`（跟 `geo-ga4-realtime-
  layer.js`／`geo-ga4-settings.js` 同一個判斷慣例，不是新發明的規則）。
- 不讀 `localStorage`、不組 `Authorization`／`x-store-id`、不解析 JWT——
  全部交給既有 `apiFetch()`。

## 2. 真實 `apiFetch()` Contract（讀 `public/js/app.js` 原始碼確認，非假設）

| 情況 | 回傳 |
|---|---|
| HTTP 200 / 400 / 429 / 502 / 其他非 401/403 | 原生 `fetch` `Response`（呼叫端自己 `res.json()`） |
| HTTP 401 | `{ ok:false, status:401, body }`，並在非 `/store-login` 時副作用呼叫 `clearToken()` + `showLoginOverlay()` |
| HTTP 403 | `{ ok:false, status:403, body }`，不登出，依 `body.error` 顯示對應 Toast |
| `fetch()` 本身 throw（含 AbortError） | 原樣往外拋，`apiFetch` 不吞 |
| Token 不存在 | `Authorization` header 省略，仍照送（可能仍靠 `x-store-id` 通過） |
| Store ID 來源 | `window.currentStore.store_id` 優先，其次 `localStorage.pos_store_info.store_id` |

`geoGa4H1ApiRequest()` 同時支援「原生 Response」與「401/403 特殊物件」
兩種形狀，不假設 `res.json` 一定存在（`typeof res.json !== 'function'`
guard），對 401/403 也不會誤呼叫 `.json()`。

## 3. Native Response／Auth Object 雙格式

```js
if (res.ok === false && (res.status === 401 || res.status === 403)) {
  return { success:false, code: res.status===401?'auth_required':'feature_disabled', http_status: res.status, body: res.body || {} };
}
if (typeof res.json !== 'function') return { success:false, code:'invalid_response', ... };
```

## 4. Sync Response Parsing + Success-only Refresh

`_geoGa4H1HandleSyncResult(result, onChange)`：

- 成功（`result.success===true`）：顯示 `rows_saved`（若有），呼叫
  `onChange()`（= `geoGa4H1Refresh`）刷新讀取端。
- 失敗：依 `GA4_H1_SYNC_ERROR_MESSAGES` 顯示對應訊息，**不呼叫
  `onChange()`**，不清空 `lastGoodPayload`／舊表格。

## 5. Cache Preservation

失敗路徑完全不觸碰 `geoGa4H1State.lastGoodPayload`／`markerGroup`；只有
下一次成功的 `geoGa4H1Refresh()` 才會更新它們。

## 6. Error Classification

`GA4_H1_STATUS_CODE_MAP` 涵蓋：`auth_required`／`feature_disabled`／
`property_not_bound`／`credential_unavailable`／`SDK_UNAVAILABLE`／
`permission_denied`／`invalid_argument`／`rate_limited`／`network_error`／
`ga4_backend_error`／`ga4_request_failed`／`ga4_realtime_disabled`／
`sync_in_progress`／`invalid_response`／`unexpected_error`，未知 code 一律
安全 fallback 成通用訊息，不顯示原始 code 或原始錯誤字串。

## 7. Abort-safe Wrapper

`geoGa4H1SafeRunFetch(fn)`：只吞 `error.name==='AbortError'`（回傳
`undefined`），其他錯誤原樣往上拋。`geoGa4H1Refresh()`／`syncHandler`／
`_geoGa4H1HandleSyncResult()` 的成功刷新都透過這個唯一入口。

## 8. Listener Promise Boundary

`modeHandler`／`metricHandler`：`Promise.resolve(onChange()).catch(_geoGa4H1SwallowAbort)`。
`geoGa4H1Init()` 的 fire-and-forget 初始刷新：`.catch(...)`。
`geoGa4H1Destroy()`：`try { currentAbort.abort() } catch(e) {}`。

## 9. Legacy Realtime Stress Test（未修改該檔案，只驗證）

`geo-ga4-realtime-layer.js` 完全未修改。本輪用 Browser Auth Runtime
#46/47 與 Mutation J 證明：(a) 它現有的 try/catch 真的會攔住
AbortError（拿掉就會產生 unhandledRejection，Mutation J 證實）；(b) 20 次
快速切換 Window/Metric 不會洩漏 unhandledRejection。

## 10. Browser Auth Runtime 使用真實 `app.js` Source

`scripts/run-g1-6-ga4-h1-1-browser-auth-runtime.js` 用「標記字串定位＋原始
子字串擷取」直接從 `public/js/app.js` 拿出 `TOKEN_KEY`／`getToken`／
`setToken`／`clearToken`／`parseJwtPayload`／`apiFetch`／
`showLoginOverlay` 的**真實原始碼**（有 8 條 `mustContain` 斷言，任何一條
消失就直接 throw），`eval()` 進 jsdom `window`，只有網路層 `fetch()` 是
可控假物件。

## 11. 未修改 Backend Store Guard

`middleware/storeGuard.js` 全程唯讀，僅用於閱讀真實 401/403 觸發條件；
沒有任何一行被修改。

## 12. 未建立第二套 Auth

- 沒有第二個 Token Reader（`localStorage.getItem('pos_store_token')`
  只出現在 `app.js`，`geo-ga4-h1-panel.js` 完全沒有）。
- 沒有第二個 JWT Parser（`atob(`／`parseJwtPayload` 都不出現在 panel）。
- 沒有第二套 Auth Header 組裝邏輯。

以上四點都有對應的 Static Audit check（H1-AUTH 3/4/5/6）與 Mutation
Negative（B）交叉驗證。

## 13. 未解決／未假裝解決 GA4 502

`utils/ga4Realtime/*`、`routes/geo-live.js` 全程唯讀（除了在 GA4
Diagnostic Contract Worker 裡用官方 `_setClientForTest()` 注入假 Client，
沒有修改任何一行 production 邏輯）。GA4 Diagnostic Contract 測試明確在
輸出裡加註：「This proves the Diagnostic Contract Gate only. It does NOT
prove the production GA4 Backend 502 is resolved.」

## 14. GA4 Diagnostic Contract — Worker Process 架構

初版用「手動清 `require.cache`」的方式在同一個 process 內切換 Fake
Client，結果發現 `connectionTest.js`／`utils/ga4Realtime/index.js`／
`routes/geo-live.js` 各自 `require('./client')` 捕捉到的是各自載入時的
module 實例，單純刷新 `client.js` 的 cache entry 不會回頭更新它們已經
捕捉好的參照，導致 permission_denied／summary-city 分辨等情境全部誤判成
`credential_unavailable`。改成「每個 Scenario 一個全新 child process」
（`scripts/helpers/ga4-h1-1-diagnostic-scenario-worker.js`，由
`spawnSync()` 啟動）後，這整類 stale reference 問題在結構上不可能發生：
每個 process 的 `require.cache` 從零開始，`client.js`／
`connectionTest.js`／`index.js`／`routes/geo-live.js`／
`middleware/storeGuard.js` 只 require 一次，彼此的內部參照天然一致。
Worker 本身不判斷 PASS/FAIL，只回報 HTTP 回應與 process 事件的原始事實；
所有 Assertion 都在 Parent Runner 做。
