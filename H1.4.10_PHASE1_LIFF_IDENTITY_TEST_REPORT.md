# H1.4.10 Phase 1 — LIFF 核心會員歸戶 Test Report

## 一、Phase 1 專用回歸測試（fresh-run）

```
node scripts/run-h1-4-10-phase1-liff-identity-runtime.js
TOTAL=68 PASS=68 FAIL=0
```

涵蓋範圍（對應本輪需求文件「十六、自動測試」全部 28 項可自動化項目，加上 Part 0 契約回歸）：

- **Part A（前端純模組，13 組情境／35 個斷言）**：`auto_identify_enabled` 開關、缺 `liff_id`、外部瀏覽器（`isInClient=false`）、未登入、Facebook／Instagram WebView、LIFF init 失敗、成功路徑（`gate_stage` 正確、`member_session` 正確保存、前端不重複送 success）、已有 session 不重複 verify、同頁不重複嘗試、ID Token 缺失／過期（皆確認 0 次 `liff.login()`）、後端 verify 失敗、network exception、metadata 白名單。
- **Part 0（新增，契約回歸）**：未傳 `opts` 時 `initLineMemberGate()` 行為與修改前逐字元相同（`gate_stage='callback'`、既有 hotfix25 流程照常）。
- **Part B（真實 HTTP + 暫存 sqlite，17 個斷言）**：`/verify`（`gate_stage=liff_auto_identify`）建立 `line_members`／`line_member_sessions`、`/link-context` 補綁成功、拒絕 raw UID、拒絕竄改 session、跨店隔離、idempotent 重送、verify 失敗不建立錯誤會員、事件 metadata 不含 Token／UID。
- **Part C（靜態檢查，10 個斷言）**：LIFF ID 分離與 fallback、`shouldInitLiff` 條件式、兩頁共用同一套邏輯（未複製第二套）、`skipLoginCallback` 只在正確情境傳遞、未 hardcode 本店實際 LIFF ID。

### 對應需求文件十六逐項狀態

| # | 項目 | 狀態 |
|---|---|---|
| 1 | gate off + auto identify on + LIFF client logged in → 自動 verify | PASS（T8） |
| 2 | Auto Identify 不呼叫 liff.login() | PASS（T3/T4/T8/T11/T12） |
| 3 | 成功後 member_session 保存正確 | PASS（T8） |
| 4 | 成功後 line_members 正確建立/更新 | PASS（Part B） |
| 5 | 成功後 line_member_sessions 正確 | PASS（Part B） |
| 6 | 已有有效 member_session → 不重複 verify | PASS（T9） |
| 7 | 同一頁 load → 不重複 Auto Identify | PASS（T10） |
| 8 | isInClient=false → skip | PASS（T3） |
| 9 | isLoggedIn=false → skip | PASS（T4） |
| 10 | Chrome 外部 → 不登入、不阻止下單 | PASS（T3 等價情境） |
| 11 | Facebook WebView → 不登入、不阻止下單 | PASS（T5） |
| 12 | Instagram WebView → 不登入、不阻止下單 | PASS（T6） |
| 13 | LIFF init fail → 點餐仍可操作 | PASS（T7） |
| 14 | ID Token verify fail → 不建立錯誤會員 | PASS（Part B） |
| 15 | verify fail → anonymous checkout 仍可進行 | PASS（T13） |
| 16 | Auto Identify 成功但 cart 尚不存在 | PASS（Part B T16） |
| 17 | 首次建立 cart 後 → /link-context 補綁成功 | PASS（Part B T17） |
| 18 | link-context 不接受 raw LINE UID | PASS（Part B T18） |
| 19 | 無效 member_session → link-context 拒絕 | PASS（Part B T19） |
| 20 | store A session → 無法綁 store B cart | PASS（Part B T20） |
| 21 | 相同 context 重送 → idempotent | PASS（Part B T21） |
| 22 | cart restore → context 補綁成功 | PASS（Part B T22） |
| 23 | line-order 使用正確 LIFF ID | PASS（Part C） |
| 24 | line-shipping 使用 shipping LIFF ID | PASS（Part C） |
| 25 | shipping ID 缺失 → fallback member LIFF ID | PASS（Part C） |
| 26 | LIFF member 完成訂單 → order 正確歸戶 | **MANUAL**（見下方說明） |
| 27 | order 後 LTV / order count 正確 | **MANUAL**（見下方說明） |
| 28 | 不得在 URL/log/localStorage 出現完整 Token/UID | PASS（T15、Part B） |

**#26／#27 說明**：訂單建立（`submitOrder()` → `member_session:_currentMemberSession()`）與後端 LTV／order_count 累計，是 H1.4.9 既有、未修改的既有機制（`routes/line-orders.js` 送單時重新 `verifyMemberSession()`）。Phase 1 沒有修改這條路徑本身，只確保 `member_session` 能在 LIFF 被動辨識後正確存在，因此本輪不重複造一份端對端下單測試（避免與既有下單回歸測試重疊、增加不必要的維護面），但這代表「LIFF member 完成訂單後 LTV 正確」仍需要一次跨 `line-order.html` 完整下單流程的真機/整合驗證，列入 Manual QA。

## 二、Frozen Contract Gate（H1.4.9）

修改前（基底）：
```
TOTAL=67 PASS=67 FAIL=0
```

Phase 1 完成後 fresh-run：
```
TOTAL=67 PASS=67 FAIL=0
```

**未修改、未弱化、未刪除任何一條既有 assertion。**

## 三、相關既有回歸套件（fresh-run，本輪執行）

| Script | Phase 1 後結果 | Pristine baseline 結果 | 差異 |
|---|---|---|---|
| `smoke-hotfix25.js` | PASS=26 FAIL=0 MANUAL=3 | 同左 | 無差異 |
| `smoke-hotfix26-a.js` | PASS=24 FAIL=0 MANUAL=1 | 同左 | 無差異 |
| `smoke-hotfix26-b.js` | PASS=30 FAIL=0 MANUAL=4 | PASS=29 FAIL=1 MANUAL=4 | **本輪修正**（見下方說明） |
| `smoke-hotfix26-c.js` | PASS=23 FAIL=1 MANUAL=2 | PASS=23 FAIL=1 MANUAL=2 | 無差異（pre-existing） |
| `smoke-hotfix26-d.js` | PASS=20 FAIL=0 MANUAL=3 | 同左 | 無差異 |
| `smoke-hotfix26-e.js` | PASS=36 FAIL=1 MANUAL=3 | 同左（cascading from -c/-verify-debug） | 無差異（pre-existing） |
| `smoke-hotfix26-f1.js` | PASS=11 FAIL=0 MANUAL=3 | 同左 | 無差異 |
| `smoke-hotfix26-f2.js` | PASS=22 FAIL=3 MANUAL=3 | 同左 | 無差異（pre-existing，`/timeslots` 日期判斷問題，與 LINE Member 無關） |
| `smoke-hotfix26-f3.js`〜`f8.js` | 皆與 baseline 相同 | 同左 | 無差異 |
| `smoke-hotfix26-i.js` | 相同 crash（`removeAttribute is not a function`） | 相同 crash | 無差異（pre-existing，測試 mock DOM 缺 API，與本輪程式碼無關） |
| `smoke-hotfix26-verify-debug.js` / `-verify-deep.js` | PASS=36 FAIL=1（cascading） | 同左 | 無差異（pre-existing，資料庫種子資料相關） |
| `smoke-hotfix31-r4-channel-visitor360.js` | PASS 118 FAIL 0 | 同左 | 無差異 |
| `smoke-hotfix31-r4-visitor360-ui.js` | PASS 73 FAIL 0 MANUAL 2 | 同左 | 無差異 |
| `smoke-hotfix30-b5-r5-1-d1-visitor-geo.js` | 相同 crash（`utils/cartSnapshot.js:593`） | 相同 crash | 無差異（pre-existing，與 cart geo attribution 有關，非 LINE Member） |
| `smoke-hotfix30-b5-r5-3-a1-2-visitor-geo-sync.js` | 全數 PASS | 同左 | 無差異 |
| `smoke-hotfix30-b5-r5-3-a5-visitors-classification-fix.js` | PASS 20 FAIL 0 | 同左 | 無差異 |

**所有 pristine baseline 既有的 FAIL／crash，逐一用「未修改的原始 zip 重新展開後獨立執行」的方式重新驗證過一次，確認與 Phase 1 修改後結果完全相同（同一項目、同一錯誤訊息）——不是本輪造成的 regression。**

### `smoke-hotfix26-b.js`：本輪修正的 1 個 FAIL

Baseline 該項目失敗訊息：`line-order.html／line-shipping.html 不應各自呼叫 liff.init(): order=true shipping=false`。

根因：**測試腳本本身**用「原始碼文字比對」偵測 `liff.init(` 字樣是否出現在頁面裡（用來確保兩頁沒有繞過共用模組直接呼叫）。本輪在 `line-order.html` 新增的**註解**裡不小心寫了「liff.init()」字面文字，被這個 grep 誤判成「頁面自己呼叫了 liff.init()」（實際上程式碼本身完全沒有直接呼叫，呼叫的是 `LineMemberGate.initLineMemberGate()`）。修正方式：把該行註解改寫為「初始化 LIFF SDK」，不再包含容易被誤判的字面字串。**修正的是註解文字，沒有改動任何可執行邏輯**，修正後這個既有測試恢復 PASS。

## 四、Baseline Pre-existing Failures 清單（誠實隔離，非本輪造成）

| Script | 失敗項目 | 根因（與本輪無關的既有問題） |
|---|---|---|
| `smoke-hotfix26-c.js` | `renderFriendStatus(null)` 文字判斷 | 既有函式回傳文字與測試期望的字串不一致（`尚未確認` vs `未知`），與 LIFF 被動辨識無關 |
| `smoke-hotfix26-e.js` / `verify-debug.js` / `verify-deep.js` | Cascading（呼叫上面兩個腳本失敗導致連鎖 FAIL）+ 資料庫種子資料 `line_member_login_channel_id` 內容比對 | 既有測試資料/環境相依問題 |
| `smoke-hotfix26-f2.js` | `/timeslots` 截止判斷未限定於今天 | 既有預購時段判斷邏輯的既有問題，與 LINE Member 無關 |
| `smoke-hotfix26-i.js` | `openOaBtn.removeAttribute is not a function` | 既有測試 mock DOM 元素缺少 `removeAttribute`，屬於測試 harness 本身的既有缺陷 |
| `smoke-hotfix30-b5-r5-1-d1-visitor-geo.js` | `utils/cartSnapshot.js:593` `Cannot read properties of undefined (reading 'has')` | 既有 cart geo attribution 程式碼的既有 bug，與本輪程式碼路徑無關 |

以上全部經「重新展開未修改的原始 zip、獨立執行」二次確認，錯誤訊息與行號完全相同。
