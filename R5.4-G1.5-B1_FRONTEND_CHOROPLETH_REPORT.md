# R5.4-G1.5-B1 Frontend Choropleth Report

fix18-10-hotfix30-B5-R5.4-G1.5-B1 — Frontend Contract Wiring & GA4 County Choropleth

## Contract Normalization

`geoGa4NormalizeResponse(json)` 是唯一的防禦性正規化入口，處理兩種頂層
形狀：`{success:true, data:{...}}`（正常/disabled/not_configured/fresh/
cached/stale_cache）與 `{success:false, code, message, retryable, status}`
（Route 層錯誤）。任何缺欄位／型別不符都 fallback 成安全預設值（0、null、
空陣列），**不 throw**，避免一個 malformed response 打壞整個 UI。

## HTML Script Wiring

`public/index.html` 內，`geo-ga4-realtime-layer.js` 排在
`geo-intelligence-map.js`／`geo-heatmap.js`／`geo-heatmap-ui.js` 之後、
只載入一次、只出現在 `index.html`（沒有載入到其他頁面）。對應 CSS
`geo-ga4-realtime-layer.css` 一併掛載。

## State

`window.geoGa4State`：`active/containerId/windowMinutes/metric/loading/
requestSeq/abortController/autoRefreshTimer/autoRefreshSeconds/
lastPayload/lastFetchedAt/layerGroup/countyIndex`。沒有任何欄位保存
Property ID／Stream ID／Credential／Access Token／Service Account。

## Fetch Lifecycle

`_geoGa4RunFetch()`：每次呼叫先 abort 前一個未完成的 `AbortController`，
遞增 `requestSeq`，fetch 完成後比對 `mySeq !== geoGa4State.requestSeq` 決定
要不要 render（防止舊回應覆蓋新狀態）。`AbortError` 安靜忽略，不視為錯誤。

## Abort／requestSeq

實測（Smoke K-C1）：切換 metric 時，前一個仍在等待的 fetch 真的收到
`abort` 事件；實測（Smoke E48/E49）：快速連續切換 metric，最終 render 的
資料對應最後一次呼叫，不是先發出但後完成的舊回應。

## County Index

`_geoGa4BuildCountyIndex(featureIndex)` 對 `featureIndex.byCountyDistrict`
遍歷一次，依 `county_code` 分組成 `Map<county_code, feature[]>`，只在第一
次 render 時建立並快取在 `geoGa4State.countyIndex`（不是每次 render 都
全掃描整張地圖，見需求文件八）。

## Polygon Styling

沿用本專案 Order Heatmap／Visitor Layer 既有慣例（見
`geo-visitor-layer.js` 的 `geoVisitorRenderChoropleth()`）：維護自己的
`L.layerGroup()`，每次 render 用 `clearLayers()` 清空後，對每個既有
feature 建立新的 `L.geoJSON(feature, {style})` clone 加進 group。

**設計決策（與需求文件字面描述的差異，已記錄並非疏漏）**：需求文件
§四／§八／§十五 提到 `previousStyles: new Map()`／
`geoGa4RestoreStyles()`「save/restore 原始樣式」的概念。實際採用的做法是
**不去動 featureIndex 原始 feature 的樣式**，只在自己的 `layerGroup` 裡放
一次性的 clone（跟 Order/Visitor 兩層完全一致的做法）。因此：
- 不需要 `previousStyles` Map，因為原始 feature 從來沒被改過。
- 「離開 GA4 Layer」＝清空 `layerGroup`（`geoGa4ClearLayer()`），效果上
  等同「還原」，因為地圖上除了這個空的 group 之外沒有留下任何 GA4 痕跡。
- 這個設計比「直接改寫共用 layer 的 style 再想辦法還原」更安全：不會有
  「還原漏了一個屬性」或「兩個 Layer 同時想改同一個共用物件」的風險。

固定樣式（不做 activeUsers 強度色階，見需求文件九）：
`{ color:'#0891b2', fillColor:'#06b6d4', weight:2, opacity:0.9,
fillOpacity:0.20, dashArray:'6,4' }`——青色虛線，與 Order Heatmap 的
綠→黃→紅色階、Visitor Layer 的藍色系都不同。同一 `county_code` 底下所有
行政區 feature 套用同一組樣式（不分行政區深淺）。

## Summary

`summary.total_active_users_ga4` 是卡片顯示的唯一總數來源；
`counties[].active_users` 只用在 Tooltip／逐縣市資料，程式碼裡沒有任何
`counties.reduce()` 或加總邏輯冒充總數（Smoke D1/D2、Static Audit #17/#18
交叉驗證）。

## Cache／Stale／Quota

`geoGa4StatusMessage()`：fresh→「剛剛更新」；cached→「目前顯示 N 秒前的
快取資料」；stale_cache→「Google Analytics 暫時無法連線，目前顯示 N 秒前
的舊資料」；disabled/not_configured 依 `error_code` 給出具體原因文案。
`geoGa4QuotaWarning()`：near_limit→「接近限制」提示；limited→「暫時受限」
提示；normal/unknown 不顯示警告。

## Timer

`_geoGa4ScheduleAutoRefresh()`：每次呼叫先 `geoGa4StopAutoRefresh()`
（`clearTimeout`）再決定要不要排程新的一個，保證同一時間只有一個 timer。
normal/unknown→60 秒；near_limit→120 秒；limited→提早 return，不排程
（只保留手動 `geoGa4Refresh()`）。`geoGa4Deactivate()` 呼叫
`geoGa4StopAutoRefresh()` 並 abort 進行中的 request。

## Layer Lifecycle

`geo-heatmap-ui.js` 的 `geoHeatUiSetLayer()` 在切離 `'ga4'`（無論切到
`'order'` 還是 `'visitor'`）時呼叫 `geoGa4Deactivate()`；切入 `'ga4'` 時
呼叫既有的 `geoGa4FetchAndRender()`。三層互斥仍沿用既有
`_geoHeatUiApplyLayerExclusivity()`（G1.5 階段就已經寫好的三層版本，本輪
沒有修改互斥邏輯本身，只加了 deactivate 呼叫）。

## Privacy

`GA4_REALTIME_DISCLAIMER`／`GA4_REALTIME_PRIVACY_NOTICE` 兩則固定文案在
`geoGa4RenderNoticesHtml()` 裡**無條件**加入輸出（不依賴後端 `notices`
陣列是否存在或內容是否重複），Smoke K-H1 驗證即使後端 `notices` 是
`undefined`，畫面仍然顯示這兩則聲明。

## Known Limitations

- Settings UI（Property/Stream/Cache 秒數設定畫面）完全沒有，本輪刻意
  不做（見需求文件排除清單）。
- `live_check=1` 管理者權限分支沒有實作（G1.5-A 階段就已經記錄延後）。
- 目前只用 fake fetch／fake Leaflet 驗證邏輯正確，沒有真實瀏覽器
  Manual Visual、沒有真實 GA4 憑證的 Live Test。
- Auto Refresh 的 120 秒／停止邏輯只驗證了「排程與否」，沒有驗證使用者
  長時間（例如數小時）停留在頁面上的記憶體/timer 累積行為（超出本輪
  jsdom 測試合理範圍）。

## Deferred Settings UI

Property／Stream／Cache 秒數／Auto Refresh 開關等設定畫面，以及對應的
`PATCH` 寫入端點，全部留給後續 Settings UI 階段（本輪只有讀取
`getGa4RealtimeConfig`，見 G1.5-A）。
