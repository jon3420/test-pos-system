# H1.4.5-PRODUCT-EXPOSURE-TO-CART-RATE — 正式交付報告

版本：`fix18-10-hotfix30-B5-R5.4-G1.6-GA4-H1.4.5-PRODUCT-EXPOSURE-TO-CART-RATE-QA-full`
基底：`fix18-10-hotfix30-B5-R5.4-G1.6-GA4-H1.4.4-GA4-VIEW-ITEM-SEMANTICS-FIX-QA-full`

---

## 1. Reality Audit

POS internal 的 `view_product` 事件實際語意是「商品卡進入顧客可見畫面的曝光」（由
`public/line-order.html` / `public/line-shipping.html` 的 IntersectionObserver 觸發），
不是「顧客主動點擊進入商品詳情頁」。H1.4.4 已修正 GA4 `view_item` 語意（`view_product`
不再映射成 GA4 `view_item`），但 POS internal 商品分析報表當時仍把這個曝光訊號的畫面文字
標成「瀏覽人數」／「瀏覽→加購」，會讓老闆誤以為顧客曾主動點擊查看商品。

H1.4.5 的範圍：**只修正老闆看得到的顯示語意與新增一個衍生指標**，不新增資料表、不新增
SQL 查詢、不修改事件定義、不重算另一套曝光/加購定義、不觸碰 GA4 `view_item` 語意。

## 2. H1.4.5 Root Cause（要解決的問題）

商品排行／商品漏斗畫面把「曝光」講成「瀏覽」，且沒有「曝光→加購率」這個指標可以直接
回答老闆最常問的問題：「看到這個商品的人，有多少人把它加入購物車？」。修正方式：

1. 後端 `getProductRanking()` 新增一個衍生欄位 `view_to_cart_rate`（不改變既有欄位）。
2. 前端三處畫面（主儀表板商品排行、Analytics V2 商品漏斗、AI Insights 商品證據文字）
   把「瀏覽」改成「曝光」，並在主儀表板新增「曝光→加購率」欄位直接顯示這個新欄位。

## 3. Production 修改檔案（正式產品程式碼，共 4 個檔案）

| 檔案 | 修改內容 |
|---|---|
| `utils/dashboardAnalytics.js` | `getProductRanking()` 新增 `view_to_cart_rate` 欄位 |
| `public/js/app.js` | 主儀表板商品排行表：「瀏覽人數」→「曝光人數」、新增「曝光→加購率」欄、「加入→成交率」→「加購→成交率」 |
| `public/js/analytics-v2.js` | Analytics V2 商品漏斗／Dashboard 總覽卡片：「瀏覽人數/View Users」→「曝光人數/Exposure Users」、「瀏覽→加購」→「曝光→加購」、空資料提示語意、附近註解 |
| `utils/analyticsV2.js` | AI Insights 商品洞察文字：「商品瀏覽高」→「商品曝光高」、「瀏覽 X 人」→「曝光 X 人」 |

以上 4 個檔案的完整 unified diff 見本報告附錄「Production Diff」。

## 4. 新增測試及 runner（H1.4.5 專用，共 3 個新檔案）

| 檔案 | 內容 |
|---|---|
| `scripts/run-g1-6-ga4-h1-4-5-exposure-runtime.js` | 直接呼叫真實 `getProductRanking()` + 真實 sql.js 檔案資料庫，涵蓋公式、邊界條件、店家隔離、日期區間、channel filter。**31/31 PASS** |
| `scripts/static-audit-g1-6-ga4-h1-4-5.js` | 字面字串／結構驗證：畫面標籤、API 欄位未破壞性改名、GA4/Meta 語意保護 Hard Gate。**40/40 PASS** |
| `scripts/run-regression-g1-6-ga4-h1-4-5.js` | 完整 regression runner：即時解析 H1.4.4 runner 原始碼取得 72 個 inherited suite，加上本輪 2 個新 suite，合計 74 個唯一 suite |

## 5. residue 根因、修正及驗證

**根因調查過程**：在一次完整 74-suite regression 執行中，`scripts/run-g1-6-ga4-h1-1-ga4-diagnostic-contract.js`
（H1.4.4 之前既有的 inherited suite，非 H1.4.5 新增）留下一個殘留暫存 DB 檔案
（`ga4-h1-1-diag-safe_output_scan-<pid>-<ts>-<rand>.db`），即使該 suite 自己的內部
check 29（「所有暫存 DB 已清除」）在執行當下驗證通過。獨立重跑該 suite 11 次
（直接 `node` 執行）與透過 `execFileSync` 重跑 5 次，皆為 0 殘留，無法穩定重現——
判斷是「74-suite 連續執行時的系統資源壓力下、檔案系統寫入/刪除時序邊界」造成的罕見
延遲殘留，而不是 `runScenario()` 的清除邏輯本身有誤（該邏輯單獨執行時 100% 可靠）。

**修正（測試基礎設施層，不改動任何 Production 程式碼）**：

1. `scripts/run-g1-6-ga4-h1-1-ga4-diagnostic-contract.js`：在 check 29 之後、Parent
   process 結束前，新增一道最終保險清除——逐一檢查 `tempDbFiles` 是否仍存在，若存在
   則強制刪除並明確記錄（不是靜默吞掉）。
2. `scripts/run-regression-g1-6-ga4-h1-4-5.js`：`detectResidue()` 改成「執行前後快照
   相減」，只計算「這一輪開始前沒有、這一輪結束時卻還存在」的新增殘留檔案，不會把
   執行前已經存在（不是這一輪造成）的檔案誤算成本輪 residue。

**驗證**：修正後，`run-g1-6-ga4-h1-1-ga4-diagnostic-contract.js` 單獨連跑 **5/5 PASS，
exit=0，每次執行前後皆無殘留**（第 5 次於最終檢查階段再單獨驗證一次，同樣無殘留）。

## 6. B4-2 調查過程、實際重現率、debug 結果（根因已確認）

`scripts/smoke-hotfix30-b5-r5-3-a7-geo-kpi-single-source.js` 的斷言 B4-2（`!html.includes('42')`）
是 H1.4.1 既有 inherited suite 的一部分（不是 H1.4.5 新增），偶發失敗。

**調查過程**：

1. 先追蹤程式碼路徑，逐一排除假設：`_geoKpiErrorCardsHtml()`（B4 情境的 KPI 錯誤卡片）
   本身不含 "42"；`computeGeoTopAreas()` 讀的是 `vm.funnel.areas`（此測試 fixture 為空
   陣列），不是含 "42" 的 `vm.overview.top_areas`；`setupDashboard()` 每次呼叫都建立
   全新 JSDOM，B1～B5 測試區塊之間不共用 `geoVisitorState`。
2. 靜態推論無法鎖定根因，改用實測：25 個平行 `node` 進程同時執行 → 0 次失敗；
   20 次連續（非平行）`node` 執行 → 1 次失敗（5%），證實這是此 suite 內生的
   time-dependent race，跟其他 suite 或 regression runner 的併發/資源壓力無關。
3. 建立一份帶偵測程式碼的暫時副本（**注意：第一次嘗試因為把副本複製到 `/tmp` 導致
   `__dirname`-relative `ROOT` 路徑解析錯誤，40 次執行全部在 `node --check` 階段就
   crash，從未真正跑到 B4 測試——這份錯誤資料已作廢，不採用**）。修正路徑問題後，
   改成把偵測程式碼直接寫在專案內的暫時副本 `scripts/_debug_a7_temp.js`（打包前已刪除），
   連續執行 60 次，第 55 次成功捕捉到失敗，並把觸發時的 HTML 上下文寫入
   `/tmp/a7_debug_hits.log`。

**捕捉到的實際 HTML 上下文**（逐字，未經改寫）：

```
"最後更新：09:18:42
    <button type=\"button\" onclick=\"refreshGeoDashboardKpiBlock('geo-a7-te"
```

**根因（已確認，非猜測）**：`public/js/geo-intelligence.js` 第 1772/1876 行，KPI 卡片
區塊下方會顯示一段真實、正常、且完全正確的 Production 行為——「最後更新：HH:MM:SS」
即時時鐘（`new Date(vm.updated_at).toLocaleTimeString('zh-TW', { hour12: false })`）。
B4-2 原本對**整個** `container.innerHTML`（含這段會隨秒數變動的時鐘文字）做全文
`includes('42')` 掃描；當目前時刻的秒數恰好是 42（例如 `09:18:42`），就會讓這個跟
「KPI 卡片不得 fallback 顯示舊 fixture 的 42」完全無關的字串命中，造成假性失敗。
重現率約 1/60（≈1.67%），與「每分鐘 60 秒中剛好命中第 42 秒」的機率吻合。

**修正（測試基礎設施層，不改動任何 Production 程式碼，也沒有放寬原本要驗證的業務邏輯）**：
把 B4-2 的檢查範圍從整個 `container.innerHTML` 縮小為 `html.split('最後更新')[0]`
（即排除已知、確定會隨時間變動的時鐘文字之後的內容，KPI 卡片本身與其餘畫面內容仍完整
檢查，沒有縮小對「KPI 卡片不得顯示 42」這個原本業務驗證的覆蓋範圍）。

**驗證**：修正後單獨連續執行 **150 次，0 次失敗**（遠超過原訂 5 次的驗證門檻，因為原始
重現率只有約 1/60，5 次不足以有統計意義，因此加碼到 150 次以確認修正有效）。

本項調查全程未修改：`getProductRanking()`、Geo KPI 計算公式、`_geoKpiErrorCardsHtml()`、
`loadGeoDashboardData()` 或任何其他 Production 業務邏輯。斷言本身（「KPI 卡片不得顯示
舊 fixture 的 42」）完全保留，未刪除、未改成 warning、未跳過、未從 74 個 suite 中排除、
未放寬驗證強度。

## 7. API 新增欄位

`GET /api/analytics/dashboard`（`routes/analytics.js` 呼叫 `getProductRanking()`，
原樣傳遞，未過濾欄位）回傳的 `products` 陣列中，每個商品物件新增：

```
view_to_cart_rate: number | null
```

既有欄位（`view_people`、`cart_people`、`cart_qty`、`purchase_people`、`purchase_qty`、
`not_purchased_people`、`cart_to_purchase_rate`、`product_id`、`product_name`、
`is_delisted`）全部保留、未更名、未刪除。

Analytics V2 的 `view_to_add_rate`（`utils/analyticsV2.js` 的 `getProductFunnel()`）
同樣保留，未更名。

## 8. 計算公式與所有邊界條件

```
view_to_cart_rate = viewCount > 0 ? round2(cartPeople / viewCount * 100) : null
```

| 邊界條件 | 結果 | 驗證方式 |
|---|---|---|
| 曝光 1、加購 1 | `100` | runtime Category A |
| 曝光 10、加購 3 | `30` | runtime Category A |
| 曝光 0（無論加購多少） | `null`（不是 `0`／`NaN`／`Infinity`） | runtime Category B |
| 曝光 1、加購 2 | `200`（不封頂，沒有 `Math.min(100,...)`） | runtime Category C |
| 已下架商品（products 表已無該筆資料） | 正常計算，`is_delisted:true`，不拋例外 | runtime Category D |
| 店家隔離 | 不同 store_id 的事件不互相污染 | runtime Category E |
| 日期區間 | 區間外事件不計入 | runtime Category F |
| channel filter | `pos`/`line_takeout` 各自正確篩選、各自算出正確的 `view_to_cart_rate` | runtime Category G |

前端 `_fmtPct()`（`public/js/app.js` 既有函式，未修改）把 `null` 顯示為 `—`，把數字
顯示為 `數字%`（包含 >100% 的情況，如實顯示 `200%`）。

## 9. GA4／Meta 語意保護

以下 Hard Gate 在 static/UI audit（40/40）與正式三輪 regression 中反覆驗證，本輪皆通過：

- `GA4_EVENT_MAP` 沒有恢復 `view_product: 'view_item'` 映射，也沒有任何 `view_item` key。
- `line-order.html` / `line-shipping.html` 完全沒有出現 `view_item` 字面字串（商品曝光
  沒有被改成送出 GA4 `view_item`）。
- `META_EVENT_MAP` 的 `view_product: 'ViewContent'` 對應仍存在（Meta Pixel 既有行為
  未被誤刪）。
- `line-order.html` 的 `_trackEvent` 仍會 POST `/api/analytics/events`（internal
  `view_product` 記錄未退化）。
- `trackGA4()` / `trackPlatformEvent()` 定義各剛好 1 個，沒有被重複定義或刪除。
- `geo-ga4-h1-panel.js`、`geo-ga4-realtime-layer.js` 存在且未被本輪誤動；
  `geo-ga4-realtime-layer.js` 的「商品瀏覽」仍對應 GA4 `view_item`（即時地圖語意
  保持不變，本輪明確排除、未誤改）。

沒有新增資料表、沒有資料庫 migration、沒有修改歷史資料格式——`view_to_cart_rate` 純粹是
既有 `view_people`／`cart_people` 兩個既有欄位的衍生計算，不涉及任何 schema 變更。

## 10. runtime 31/31（逐項）

```
H1.4.5 Product Exposure-to-Cart Rate Runtime — 31/31 passed
Category A（基本公式）: 8 項
Category B（曝光0→null）: 3 項
Category C（超額加購不封頂）: 5 項
Category D（已下架商品）: 5 項
Category E（店家隔離）: 1 項
Category F（日期區間）: 4 項
Category G（channel filter）: 5 項
PASS: 31 / FAIL: 0 / TOTAL: 31
```

## 11. static/UI 40/40（逐項）

```
H1.4.5 Static/UI Audit — 40/40 passed
Group 1（主儀表板 app.js）: 7 項
Group 2（後端 getProductRanking）: 7 項
Group 3（Analytics V2 商品漏斗）: 8 項
Group 4（AI Insights）: 6 項
Group 5（GA4/Meta Hard Gate）: 8 項
Group 6（GA4即時地圖等既有契約）: 3 項
PASS: 40 / FAIL: 0 / TOTAL: 40
```

## 12. 正式 regression 三輪精確結果

執行 `node scripts/run-regression-g1-6-ga4-h1-4-5.js 1`，三次獨立呼叫（非背景程序、
非拼接過去輪次），每次都等待程序真正結束並取得 exit code：

| Round | UNIQUE SUITES | PASS | FAIL | TOTAL | exit | residue |
|---|---|---|---|---|---|---|
| Round 1 | 74 | 74 | 0 | 74 | 0 | 0 |
| Round 2 | 74 | 74 | 0 | 74 | 0 | 0 |
| Round 3 | 74 | 74 | 0 | 74 | 0 | 0 |

三輪都額外確認：`node --check` 對本輪修改的 Production/Test 檔案全部 OK；
Round-to-round consistency: CONSISTENT；assertionMismatch: 0；exitMismatch: 0。
（此結果建立在 residue 快照修正與 B4-2 測試確定性修正皆已到位之後，這兩項修正的
獨立驗證見上方第 5、6 節。）

## 13. Manual QA Checklist

| 項目 | 狀態 |
|---|---|
| 商品排行顯示「曝光人數」 | 自動化驗證（static/UI audit）。**待人工瀏覽器實際檢視畫面** |
| 顯示「曝光→加購率」 | 自動化驗證。**待人工瀏覽器實際檢視畫面** |
| 1/1 顯示 100% | 自動化驗證（runtime + `_fmtPct` 邏輯確認）。**待人工瀏覽器實際檢視畫面** |
| 3/10 顯示 30% | 自動化驗證。**待人工瀏覽器實際檢視畫面** |
| 零曝光顯示「—」 | 自動化驗證（`_fmtPct(null)` 邏輯確認）。**待人工瀏覽器實際檢視畫面** |
| 超過 100% 如實顯示 | 自動化驗證（runtime Category C）。**待人工瀏覽器實際檢視畫面** |
| 已下架商品正常 | 自動化驗證（runtime Category D）。**待人工瀏覽器實際檢視畫面** |
| 手機橫向捲動正常 | **待人工實機／瀏覽器 DevTools 裝置模擬驗證**（未執行） |
| 日期區間正常 | 自動化驗證（runtime Category F） |
| 店家隔離正常 | 自動化驗證（runtime Category E） |
| channel filter 正常 | 自動化驗證（runtime Category G） |
| Analytics V2 曝光語意正常 | 自動化驗證（static/UI audit）。**待人工瀏覽器實際檢視畫面** |
| AI Insights 曝光文字正常 | 自動化驗證（static/UI audit）。**待人工在有足夠樣本的真實資料上觸發此規則檢視** |
| GA4 `view_item` 仍維持真正商品詳情語意 | 自動化 Hard Gate 驗證。**待人工在 GA4 DebugView 實際確認 view_item 事件數＝0** |
| Meta Pixel 不退化 | 自動化 Hard Gate 驗證（`META_EVENT_MAP` 字面檢查）。**待人工用 Meta Pixel Helper 實際確認** |

**沒有任何項目使用真實瀏覽器、實機手機或 GA4 DebugView 進行過操作驗證**——以上全部
「待人工驗證」項目均為誠實標示，本報告未虛構任何已通過的人工測試。

## 14. Production Diff

### 14.1 正式產品程式碼變更（4 檔）

```diff
--- utils/dashboardAnalytics.js
+++ utils/dashboardAnalytics.js
@@ -470,6 +470,12 @@
       purchase_qty: Number(purch.qty || 0),
       not_purchased_people: notPurchasedPeople,
       cart_to_purchase_rate: cartPeople > 0 ? round2(purchasePeople / cartPeople * 100) : null,
+      // H1.4.5-PRODUCT-EXPOSURE-TO-CART-RATE：view_people 語意其實是「商品卡曝光不重複人數」
+      // （來自 internal view_product，商品卡進入可視畫面時觸發），不是顧客點擊查看商品詳情。
+      // 這裡新增 view_to_cart_rate = 加購不重複人數 ÷ 曝光不重複人數 × 100%，
+      // 回答「看到這個商品的人，有多少人加入購物車？」。誠實顯示計算值，
+      // 不做 Math.min(100, ...) 上限篡改；曝光為 0 時回傳 null（避免 0/0 = NaN 或除以 0）。
+      view_to_cart_rate: viewCount > 0 ? round2(cartPeople / viewCount * 100) : null,
     };
   });
 }
```

`public/js/app.js`、`public/js/analytics-v2.js`、`utils/analyticsV2.js` 的完整 diff
內容較長（分別 28/69/25 行 unified diff），摘要如本報告第 3 節表格所述；完整逐字 diff
可用 `diff -u` 對照本 ZIP 與 H1.4.4 基底 ZIP 重現，內容與第 3 節描述的異動點完全一致
（表頭文字、KPI 卡片標籤、AI Insights 文字，無任何其他隱藏變更）。

### 14.2 H1.4.5 新增測試（2 檔，全新檔案，非修改既有檔案）

- `scripts/run-g1-6-ga4-h1-4-5-exposure-runtime.js`（新檔案）
- `scripts/static-audit-g1-6-ga4-h1-4-5.js`（新檔案）

### 14.3 H1.4.5 新 runner（1 檔，全新檔案）

- `scripts/run-regression-g1-6-ga4-h1-4-5.js`（新檔案）

### 14.4 既有 flaky／cleanup 測試修正（2 檔，測試基礎設施層，非 Production 功能變更）

```diff
--- scripts/run-g1-6-ga4-h1-1-ga4-diagnostic-contract.js
+++ scripts/run-g1-6-ga4-h1-1-ga4-diagnostic-contract.js
@@ (check 29 之後，process.exit 之前)
+  // 最終保險清除：check 29 執行當下曾經確認乾淨，但曾在一次完整 74-suite
+  // regression 中觀察到延遲出現的殘留檔案，因此在 Parent 結束前再做一次
+  // 明確記錄的強制清除（見交付報告第 5 節）。
+  const finalSweepForced = [];
+  tempDbFiles.forEach((p) => {
+    if (fs.existsSync(p)) {
+      finalSweepForced.push(p);
+      try { fs.unlinkSync(p); } catch (e) {}
+    }
+  });
+  if (finalSweepForced.length) {
+    console.log(`[TEST-INFRA] 最終保險清除觸發：${finalSweepForced.join(', ')}`);
+  }
```

```diff
--- scripts/smoke-hotfix30-b5-r5-3-a7-geo-kpi-single-source.js
+++ scripts/smoke-hotfix30-b5-r5-3-a7-geo-kpi-single-source.js
@@ B4.（情境E）區塊
-    assert(!html.includes('42') , 'B4-2（情境E）不得 fallback 顯示舊 getGeoFunnel() fixture 的數字（42）');
+    const htmlBeforeTimestamp = html.split('最後更新')[0];
+    assert(!htmlBeforeTimestamp.includes('42'), 'B4-2（情境E）不得 fallback 顯示舊 getGeoFunnel() fixture 的數字（42）（排除下方會隨時間變動、內容正常的「最後更新」時鐘文字）');
```

（`scripts/run-regression-g1-6-ga4-h1-4-5.js` 本身的 residue snapshot-diff 邏輯屬於
第 4.3 節「H1.4.5 新 runner」的一部分，因為這是全新檔案，不是修改既有 runner。）

## 15. 部署及資料安全說明

- 本輪沒有任何資料庫 schema 變更、沒有 migration script、沒有修改任何既有資料的
  格式或語意。`view_to_cart_rate` 是純計算欄位，部署後立即對既有歷史資料生效，
  不需要任何資料回填（backfill）。
- 沒有新增環境變數、沒有新增第三方套件依賴（`package-lock.json` 與 H1.4.4 基底逐字
  相同，只是 `npm install` 產生的檔案時間戳不同，內容沒有變更）。
- 部署此版本後，若同時有使用者正在檢視「商品轉換排行」，畫面上會立即從「瀏覽人數」
  變成「曝光人數」；建議在低流量時段部署，或準備讓老闆知道這是文字語意修正（背後
  資料來源沒有變）。
- ZIP 內不含 `node_modules`；部署前需在目標環境執行 `npm install`（使用 ZIP 內附的
  `package.json`/`package-lock.json`，版本鎖定不變）。
