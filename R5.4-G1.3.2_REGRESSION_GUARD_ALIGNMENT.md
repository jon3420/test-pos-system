# fix18-10-hotfix30-B5-R5.4-G1.3.2 — Regression Guard Alignment & Final QA Candidate

本文件記錄本輪如何把 A2（`scripts/smoke-hotfix30-b5-r5-3-a2-geo-event-engine.js`）
與 A1.2（`scripts/smoke-hotfix30-b5-r5-3-a1-2-visitor-geo-sync.js`）兩支過時的
「`public/js/geo-heatmap.js` 整檔 SHA-256 相等」防護測試，改成 Scope-aware
Invariant Guard，同時保留原本的保護目的。本輪**不修改任何產品邏輯**，只調整
測試 Guard 本身。

---

## 1. Root Cause

A2 的 `A14-1` 與 A1.2 的 `A16-1` 都對以下 4 個檔案做整檔 SHA-256 逐位元組
相等檢查：

```js
const ORDER_HEATMAP_BASELINE_SHA256 = {
  'public/js/geo-heatmap.js': '8f3ec8c0ae76f84825bc0e2e1a481002109244763741a16a2981d17d0cfc710d',
  'public/js/geo-intelligence-map.js': '05a38b4a...',
  'public/js/geo-map-settings.js': 'f7ab62d8...',
  'public/data/geo/taiwan/manifest.json': 'bdd969e0...',
};
```

G1.3.1 為了修正「有訂單但無 Geo」被誤顯示成「沒有訂單資料」的真實回報 Bug，
必須在 `public/js/geo-heatmap.js` 新增 Business Total additive plumbing
（`geoHeatState.businessTotals`、兩處對應的 reset 邏輯、
`geoHeatScheduleUpdate()` 對 `{ areas, businessTotals }` 回傳格式的向下
相容支援）——這是合法、必要、且經過完整 Regression／Static Audit 驗證過的
修改。從那之後，`geo-heatmap.js` 的整檔 hash 就永遠不可能再等於
`8f3ec8c0...`，A14-1／A16-1 從此必然 FAIL，不是因為程式碼壞了，而是因為
Guard 本身的檢查方式（整檔逐位元組相等）跟「允許 additive 修改」這個目標
互相矛盾。

## 2. Whole-file equality 的限制

整檔逐位元組相等這種檢查方式有一個結構性問題：它**只能保護「完全不修改」**，
一旦有任何合法、審慎的 additive 修改，整個防護就必須被停用、放寬、或直接
刪掉——而這正是最危險的處理方式，因為停用防護的同時，也一併喪失了它原本
真正要防的東西（有人在同一個檔案裡偷改 stale-request guard、偷建第二張
地圖、偷改既有 render 呼叫簽章…）。整檔相等混淆了兩件事：「這個檔案完全
沒被動過」跟「這個檔案沒有被不當修改」，本輪要做的是把 Guard 從檢查前者
換成真正檢查後者。

## 3. G1.3.1 合法 additive 修改

`public/js/geo-heatmap.js` 唯一被修改的 4 處，逐一列在下面的 Scope
Allowlist；除了這 4 處之外，檔案其餘每一個位元組都與 A2/A1.2 那一輪留下的
原始基線完全相同（見第 5 節 Reconstruction Check，這是可證明的，不是
「大致沒問題」）。

## 4. Scope Allowlist

`scripts/lib/geo-heatmap-g131-scope-guard.js` 匯出
`GEO_HEATMAP_G131_ALLOWED_ADDITIONS`，剛好 4 項，每一項都是**精確字串**
（不是寬鬆 regex、不是萬用比對）：

| # | id | 說明 |
|---|---|---|
| 1 | `businessTotals-state-field` | `geoHeatState` 新增 `businessTotals` additive 欄位（含固定說明註解） |
| 2 | `businessTotals-reset-test-helper` | `_geoHeatResetStateForTest()` 內新增 `businessTotals` 重置 |
| 3 | `businessTotals-reset-store-switch` | `geoHeatHandleStoreSwitch()` 內新增 `businessTotals` 重置（Store Isolation） |
| 4 | `scheduleUpdate-dual-format-support` | `geoHeatScheduleUpdate()` 新增對 `{ areas, businessTotals }` 回傳格式的向下相容支援 |

每一項在目前檔案中都**必須剛好出現一次**——出現 0 次（被刪掉）、出現 2 次
以上（被複製貼上）、或字串被改寫（連改一個字元、一個空白都算），都會讓
Reconstruction Check 判定失敗。這不是「整支檔案任意可變」，是「只有這 4 處
被明確授權可以跟基線不同，其餘一律不行」。

## 5. Reconstruction Check

```js
function computeScopedBaselineCheck(rootDir) {
  // 1. 把 4 個 allowlist 項目從目前檔案內容中「精確字串比對後移除」，
  //    每項必須剛好命中 1 次，否則整體判定失敗。
  // 2. 把還原後的內容算 SHA-256。
  // 3. 這個 hash 必須等於 PRISTINE_BASELINE_SHA256（原本的整檔基線）。
}
```

如果這個檢查通過，代表兩件事同時成立：
- 除了 allowlist 列出的這 4 段，檔案其餘每一個位元組都跟基線一模一樣
  （不是「大致沒問題」，是可證明的逐位元組相同——這比整檔相等的保護力
  更精確，因為整檔相等連合法的新增都會擋，這個只擋 allowlist 以外的
  變動）。
- allowlist 列出的新增內容剛好只出現一次、剛好就是聲明的那幾段，沒有被
  悄悄擴大範圍（例如藉機在 businessTotals 賦值旁邊夾帶其他改動）。

## 6. pristine baseline hash

`PRISTINE_BASELINE_SHA256 = '8f3ec8c0ae76f84825bc0e2e1a481002109244763741a16a2981d17d0cfc710d'`

這就是 A2/A1.2 原本 `ORDER_HEATMAP_BASELINE_SHA256['public/js/geo-heatmap.js']`
的值，直接沿用、沒有重新計算或竄改——新的 Guard 保護的目標基線，跟舊
Guard 是同一份，只是比對方式從「整檔」改成「扣掉授權範圍後的剩餘部分」。

## 7. Behavioral Invariant Check

整檔 hash 從來沒有真正驗證過「行為」，只驗證「位元組沒變」。
`runBehavioralInvariants()` 額外用 jsdom 實際載入並執行
`geo-heatmap.js`，驗證一批行為不變條件（詳見 G1.3.2 Smoke 逐項斷言），
涵蓋：`geoHeatState`／`areas`／`businessTotals` 存在性、
`geoHeatScheduleUpdate()` 新舊呼叫格式相容、`0` 值正確保存（不被 truthy
判斷誤判）、stale-request guard、duplicate-request guard、
`geoHeatRenderLayer()` 既有呼叫路徑、`geoHeatEnsureLayerGroup()` 不重建
既有 instance、不建立第二張 `L.map()`／第二個 tile layer、
`geoHeatBuildAreas()` 輸出結構與計算正確性（含 `conversion` 欄位的實際
計算值，不只檢查 key 存在）、reset 行為。

## 8. Mutation Negative Tests

本輪不只驗證「正常路徑會過」，也用一組 mutation（在記憶體中對來源字串做
字串替換，不寫壞磁碟上的真實檔案）驗證 Guard「真的會抓到」以下每一種
非法變動，全部在 G1.3.2 Smoke 中逐一斷言必須被偵測到：

- 移除 stale-request guard（兩個獨立案例）
- 注入第二張 `L.map()`（兩個獨立案例）
- 注入第二個 `L.tileLayer()`
- 改壞 `areas` schema（拿掉 `coordinate_source` 賦值 / 拿掉 `conversion`
  計算，兩個獨立案例）
- 刪除 plain-array backward compatibility（兩個獨立案例）
- 把 `businessTotals=0` 誤判成不存在（truthy 取代 typeof，兩個獨立案例）
- 刪除 `businessTotals` reset
- 更名既有 state（`areas` → `areaList`）
- 修改既有 render 呼叫路徑
- 注入額外隱藏 fetch
- 注入 `Math.random()`
- 注入 `console.log()`
- 注入硬編碼 `store_001`
- 注入假座標 fallback
- Reconstruction Check 邊界案例：缺少一個 allowed addition、allowed
  addition 出現兩次、allowed addition 被改寫、allowlist 外新增任意程式碼、
  comment 被改寫、多一個空白（whitespace 不被 normalize 掩蓋）

以上每一種 mutation，Scope Reconstruction Check 或 Behavioral Invariant
Check 至少一層必須判定失敗，兩層都通過才會被視為「合法」。

## 9. A2 更新方式

只替換 `A14-1` 這一個檢查項目，其餘 assertion 完全不動：
- `ORDER_HEATMAP_BASELINE_SHA256` 物件移除 `public/js/geo-heatmap.js` 這
  一個 entry，其餘 3 個檔案的整檔 hash 保持原樣。
- 新增 `A14-1a`（Reconstruction Check）與 `A14-1b`（Behavioral Invariant
  Check）兩項，都呼叫 `require('../scripts/lib/geo-heatmap-g131-scope-guard.js')`
  的匯出函式，不在 A2 內部重新實作一份 Guard 邏輯。

## 10. A1.2 更新方式

完全對稱：`A16-1` → `A16-1a` + `A16-1b`，同樣呼叫同一個 shared module，
其餘 3 個檔案的整檔 hash 保持原樣，其餘 assertion 不動。

## 11. Assertion count 變化

| Suite | 修改前 | 修改後 | 變化 |
|---|---|---|---|
| A2 | 227 PASS / 228 TOTAL（1 FAIL） | 229 PASS / 229 TOTAL | +2（新增 A14-1a/A14-1b），FAIL 歸零 |
| A1.2 | 187 PASS / 188 TOTAL（1 FAIL） | 189 PASS / 189 TOTAL | +2（新增 A16-1a/A16-1b），FAIL 歸零 |

**沒有任何既有 assertion 被刪除或弱化**，只是把 1 個過時項目換成 2 個更
精確的項目。

## 12. Backward Compatibility

本輪完全沒有修改任何產品程式碼（`public/js/geo-heatmap.js`／
`geo-heatmap-ui.js`／`utils/geoAnalyticsQueries.js`／
`public/css/geo-heatmap.css` 均與 G1.3.1 交付版本逐位元組相同）。只新增
了：
- `scripts/lib/geo-heatmap-g131-scope-guard.js`（新檔案，測試基礎設施）
- `scripts/smoke-hotfix30-b5-r5-4-g1-3-2-regression-guard.js`（新檔案）
- `scripts/static-audit-g1-3-2.js`（新檔案）

以及修改了 `scripts/smoke-hotfix30-b5-r5-3-a2-geo-event-engine.js` 與
`scripts/smoke-hotfix30-b5-r5-3-a1-2-visitor-geo-sync.js` 這兩支既有
smoke 檔案內、且只動了 `A14-1`/`A16-1` 那一小段。

## 13. 三次穩定性

A2／A1.2／G1.3.1 Smoke／G1.3.2 Smoke 均各自重跑 3 次，PASS/FAIL 數字
逐次完全一致（229/229、189/189、115/115、148/148），無 UNIQUE
constraint、無測試污染、exit code 每次都是 0。完整 Regression Batch
（G1/G1.1/G1.2/G1.3/G1.3.1/G1.3.2/A7/A5/A4/A2/A1.2）也整批重跑 3 輪，
每一輪每一支 suite 的 PASS/FAIL 數字完全相同。

## 14. Regression 結果

見 `R5.4-G1.3.2_COMPLETION_REPORT.md` 的完整表格；本輪達成 A2 全綠
（229/229）、A1.2 全綠（189/189），沒有再保留任何「geo-heatmap.js 未修改」
的預期失敗。

## 15. Known Limitations

- Reconstruction Check 是**精確字串比對**，不做任何語意層級的 diff／AST
  比對。這是刻意設計（避免比對邏輯本身太複雜、太模糊、反而降低可信度），
  代價是：如果未來要合法擴充 allowlist（例如 G1.4 又要新增別的 additive
  欄位），需要手動在 `GEO_HEATMAP_G131_ALLOWED_ADDITIONS` 新增對應項目，
  不會自動推導。
- Behavioral Invariant Check 涵蓋的是「目前已知、已列舉」的一批不變條件，
  不是窮舉所有可能的錯誤模式；新的破壞模式（例如未來才出現的新攻擊面）
  仍需要額外補上對應的 mutation negative test。
- 本輪 Guard 只保護 `public/js/geo-heatmap.js` 一個檔案；其餘 3 個檔案
  （`geo-intelligence-map.js`／`geo-map-settings.js`／`manifest.json`）
  仍是傳統整檔 hash，若未來這 3 個檔案也需要合法 additive 修改，屆時需要
  比照本輪做法建立各自的 Scope Allowlist。
