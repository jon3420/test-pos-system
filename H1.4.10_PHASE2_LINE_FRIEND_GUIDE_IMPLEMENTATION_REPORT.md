# H1.4.10 Phase 2 — friend_entry／friend_checkout Implementation Report

## 一、新增/修改檔案清單

| 檔案 | 類型 | 內容 |
|---|---|---|
| `public/js/line-member-gate.js` | Additive | `maybeShowFriendEntryGuide()`、`maybeShowFriendCheckoutGuide()`、`openFriendGuideLink()`、`knownFriendStatus()`、`hasSeenFriendGuide()`/`markFriendGuideSeen()`、`_renderFriendGuideModal()`/`_renderFriendGuideMissingUrlModal()`、metadata builder/emit helper |
| `routes/settings.js` | Additive | `gate_mode` 白名單擴充；`isFriendOnlyMode` 判斷放寬必填欄位規則 |
| `utils/analyticsLog.js` | Additive | `EVENT_WHITELIST` 新增 `line_friend_guide_view`／`line_friend_link_clicked`／`line_friend_guide_skipped` |
| `routes/analytics.js` | Additive | `sanitizeFriendGuideMetadata()` + `FRIEND_GUIDE_EVENTS`/`FRIEND_GUIDE_SKIP_REASONS`/`FRIEND_GUIDE_MODES` |
| `public/line-order.html` | Additive | `_initLineMemberGateFromShopData()` 呼叫 `maybeShowFriendEntryGuide`；`openCheckoutStep()` 呼叫 `maybeShowFriendCheckoutGuide`（在 `checkout_click` 送出之後） |
| `public/line-shipping.html` | Additive | 同上 |
| `public/index.html` | Additive | 入口模式下拉選單新增 2 個選項＋friend mode 說明區塊 |
| `public/js/app.js` | Additive | `updateLineMemberGateModeUI()` |
| `scripts/run-h1-4-10-phase2-line-friend-guide-runtime.js` | 新增 | Phase 2 專用回歸測試（83 項） |

沒有刪除或修改任何既有函式的既有呼叫簽章；`checkout`／`entry` 既有驗證規則與 UI 行為未被觸碰。

## 二、`gate_mode` 新增值

| value | 後台 label | 行為 |
|---|---|---|
| `disabled` | 不啟用 | 無 Gate、無引導（既有，label 文字更新） |
| `friend_entry` | 進站引導加入官方 LINE（免登入） | **新增**，進站顯示一次柔性引導 |
| `friend_checkout` | 結帳前引導加入官方 LINE（免登入） | **新增**，真實 `#goCheckoutBtn` 點擊後顯示一次柔性引導 |
| `checkout` | 結帳前要求 LINE 登入 | 既有（label 文字更新，行為不變） |
| `entry` | 進站要求 LINE 登入 | 既有（label 文字更新，行為不變） |

## 三、`friend_entry` 行為

- 觸發點：`_initLineMemberGateFromShopData()`，在 LIFF 初始化／Phase 1 被動辨識**之後**執行（讓 `knownFriendStatus()` 能拿到最新結果）。
- 即使 `line_member_liff_id` 是空的也會執行（friend mode 不需要 LIFF）。
- 條件：`gate_enabled && gate_mode==='friend_entry'` 且該 session 尚未顯示過（`sessionStorage: line_friend_guide_seen_{store_id}_friend_entry`）且**不是**已知好友（`knownFriendStatus()!=='friend'`）。
- UI：標題「加入官方 LINE」／說明文字／主按鈕「加入官方 LINE」／次按鈕「先逛逛」／右上角關閉。
- 三種關閉方式（加入好友／先逛逛／關閉）皆只關閉 Modal，不影響購物車、不影響頁面任何其他狀態。

## 四、`friend_checkout` 行為

- 觸發點：`openCheckoutStep(event)` 內，**在** H1.4.9 既有 `checkout_click` 送出邏輯（含 cart_id 去重）**之後**呼叫，且完全在同一個「真實 `#goCheckoutBtn` click」判斷分支內——非真實點擊（`event.type!=='click'`、`event.currentTarget!==goBtn`、`cart` 為空、按鈕 disabled 等既有 early-return 條件）一律不會執行到這裡，`friend_checkout` 引導與 `checkout_click` 共用同一個「這是不是真實點擊」的判斷，不會有 `friend_checkout` 顯示但 `checkout_click` 沒送出的情況。
- `maybeShowFriendCheckoutGuide()` 本身完全不送出 `checkout_click`，也不重新計算 `cart_id`、不影響既有去重 key（`line_cc_sent_{store_id}`）。
- UI 文案：主按鈕「加入官方 LINE」／次按鈕「繼續結帳」。按「繼續結帳」立即關閉 Modal，`checkoutStage` 早已在它之前就已經切換完成（`_enterCheckoutStage()` 在 `checkout_click` 送出之前就執行），因此不存在「Guide 擋住進入 checkout stage」的可能性——Guide 出現時使用者已經在結帳頁上了。

## 五、加好友按鈕（`openFriendGuideLink`）

- 唯一資料來源：`config.add_friend_url`，由頁面既有 `_buildLineMemberGateConfig()` 依 `add_friend_url || line_add_friend_url || line_member_add_friend_url` 既有優先順序解析（**沒有新增第三個 URL 設定 key**）。
- LIFF Client 內：`liff.openWindow({url, external:true})`。
- 非 LIFF Client：`window.open(url, '_blank')`。
- **不呼叫** `liff.requestFriendship()`（那是既有 `openFriendAddPage()` 專屬給 `checkout`/`entry` 強制 Gate 用的，需要 LIFF 登入狀態）。
- **不呼叫** `liff.login()`。
- 點擊後只送出 `line_friend_link_clicked`，**不寫入**任何 `is_friend`/`member_session` 相關資料——點擊前後 `getMemberSession()` 完全不變（見 Test Report T11）。

## 六、缺少 Add Friend URL 時的行為

`config.add_friend_url` 為空時，`maybeShowFriendEntryGuide`/`maybeShowFriendCheckoutGuide` 改呼叫 `_showMissingUrlFallback()`：只顯示「尚未設定官方 LINE 加好友網址，您仍可繼續下單。」+ 一顆「繼續」按鈕，不顯示綠色加入按鈕、不 throw、不阻擋 checkout。

## 七、已知好友狀態短路（Known Friend Short-Circuit）

`knownFriendStatus(storeId)`：

```
getMemberSession(storeId) 不存在      → 'unknown'
is_friend !== true（含 false/null）  → 'non_friend' / 'unknown'
is_friend === true（僅此一種情況）    → 'friend'
```

只有回傳 `'friend'` 才會短路（不顯示 guide，直接標記為已看過）。`undefined`／`null`／`false`／「只是點過加入官方 LINE 按鈕」都不構成短路條件——這些狀態下 guide 仍會顯示。

## 八、Admin Settings UI

- 下拉選單（`#set-line_member_gate_mode`）5 個選項，value 與後台顯示文字完全依需求文件指定。
- 選到 friend mode：顯示 `#lmgFriendModeHint` 說明「不要求 LINE Login、不阻止下單」；`#set-line_member_require_friend` checkbox 被 disable（不清空既有值），並顯示 `#lmgRequireFriendFriendModeNote` 語意區分說明。
- 若 `line_member_auto_identify_enabled==='1'`，額外顯示 `#lmgAutoIdentifyHint`（不論目前選的是哪個 mode，都會提示 Auto Identify 是獨立機制）。
- `updateLineMemberGateModeUI()` 純 UI 顯示邏輯，**不影響** `saveLineMemberGateSettings()` 送出的欄位或 `PUT /api/settings` 的驗證行為。

## 九、Analytics 事件與 GA4／Meta 隔離

新增事件：`line_friend_guide_view` / `line_friend_link_clicked` / `line_friend_guide_skipped`（全部前端可直接送出，非 SERVER_ONLY）。

metadata 白名單：`gate_mode`（僅 `friend_entry`/`friend_checkout`，否則存 `unknown`）／`page_type`／`checkout_stage`／`has_member_session`／`known_friend_status`（`friend`/`non_friend`/`unknown`）／`skip_reason`（`browse`/`continue_checkout`/`close`）。

`public/js/analytics-platforms.js` 的 `GA4_EVENT_MAP`／`META_EVENT_MAP` **未新增任何 `line_friend_*` 鍵值**——這代表 `_trackEvent()` 仍會把它們原樣（用事件名稱本身）送給 GA4/Meta 當自訂事件，但**絕不會**被映射成 `begin_checkout`／`InitiateCheckout`（那兩個映射鍵只對應 `checkout_click`，見 Test Report Part C）。

## 十、已知限制（誠實揭露）

1. `friend_checkout` 的「已知好友直接跳過」判斷完全依賴 Phase 1 Auto Identify 是否已經成功寫入 `is_friend===true` 的 `member_session`——如果店家只開 `friend_checkout` 而沒開 `line_member_auto_identify_enabled`，`knownFriendStatus()` 永遠是 `unknown`，guide 會對所有人顯示一次（這是預期行為，不是 bug：沒有 Auto Identify 就沒有可信好友狀態可供短路判斷）。
2. Admin UI 目前沒有 `line_member_auto_identify_enabled` 本身的可視化開關（Phase 1 已知限制，本輪只讀取該值來決定是否顯示提示文字，未新增開關本身，留給後續）。
