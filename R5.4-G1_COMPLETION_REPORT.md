# R5.4-G1 — Geo Intelligence V2：Live Geo Map + Heatmap Foundation
## Completion Report

fix18-10-hotfix30-B5-R5.4-G1

---

## 1. 新增/修改檔案清單

### 後端
- `utils/deviceParser.js`（新增）— User-Agent → 裝置別決定性分類
- `utils/geoLiveCoordinate.js`（新增）— 座標驗證、寫入、Consent Status 稽核
- `utils/geoVisitLog.js`（擴充，additive）— G1 查詢函式群 + `getGeoLiveMarkerPoints()`
- `utils/analyticsLog.js`（擴充，additive）— `insertEvent()` 新增選填 `user_agent`，重用 `classifySource()`/`classifyDeviceType()`
- `utils/db.js`（擴充，additive）— `geo_visit_log` 新欄位 + `geo_live_coordinates` + `geo_coordinate_status_log` 兩張全新表
- `routes/geo-live.js`（新增）— G1 API（10 個端點）
- `routes/analytics.js`（1 行擴充）— 傳入 `user_agent` 給 `insertEvent()`
- `server.js`（1 行擴充）— 掛載 `/api/geo-live`

### 前端
- `public/js/geo-live-coordinate.js`（新增）— 顧客端 Consent-driven Coordinate Acquisition
- `public/js/geo-live-layer.js`（新增）— GeoLiveLayer（Marker/Cluster/Heatmap/District/Postal/Timeline/Replay/Filters）
- `public/css/geo-live-layer.css`（新增）
- `public/js/vendor/leaflet.markercluster.js`、`public/js/vendor/leaflet-heat.js`（本地 vendor）
- `public/css/vendor/MarkerCluster.css`、`public/css/vendor/MarkerCluster.Default.css`（本地 vendor）
- `public/index.html`（additive script/link include，接在既有 geo-intelligence-map.js 之後）
- `public/line-order.html`（additive script include + consent 接線，外送地址 focus 觸發）

### 測試／文件／工具
- `scripts/smoke-hotfix30-b5-r5-4-g1-live-geo.js`（新增，212 assertions）
- `scripts/static-audit-g1.js`（新增，30 項稽核）
- `scripts/build-g1-qa-zip.js`（新增，打包工具）
- `R5.4-G1_GEO_LIVE_ARCHITECTURE.md` / `R5.4-G1_COMPLETION_REPORT.md` / `R5.4-G1_MANUAL_QA_CHECKLIST.md`（本文件組）

---

## 2. Coordinate Acquisition

唯一實際運作的真實座標來源是 **Browser Geolocation API**（`navigator.geolocation.getCurrentPosition`），需要使用者明確授權。Google Geolocation REST API 已預留白名單與驗證邏輯，但純網頁環境沒有 Wi-Fi/Cell 訊號輸入管道，**目前誠實標示為不可用**（不會靜默退化成 IP 估算）。GPS（手機）與 Browser Geolocation 在 Web 架構下是**同一條路徑**，不是兩套實作。

## 3. Consent Status

七種狀態（`granted/denied/timeout/unavailable/unsupported/error/unknown`）全部記錄於 `geo_coordinate_status_log`，供 `GET /api/geo-live/coverage` 統計。**拒絕後 7 天 cooldown 內不會再彈出**我們自己的定位說明 UI；已同意者背景更新最短間隔 5 分鐘。

## 4. DB Schema

`geo_visit_log` 新增 3 個 additive 欄位（`postal_code`/`channel`/`device_type`）；新增 2 張全新獨立表 `geo_live_coordinates`（真實座標）、`geo_coordinate_status_log`（同意稽核）。全部透過 `PRAGMA table_info` 檢查後才 `ALTER TABLE ADD COLUMN`，migration 已驗證 idempotent（重跑不重複建欄位）。

## 5. API

`/api/geo-live` 下 10 個端點（詳見架構文件 §4）。`POST /coordinate` 是唯一對顧客開放的公開端點，其餘全部受 `requireFeature('reports')` 保護。

## 6. Marker / 7. Cluster / 8. Heatmap

Marker 唯一資料來源是 `geo_visit_log` metadata JOIN `geo_live_coordinates` 最新真實座標；沒有真實座標的訪客**絕不**產生 Marker（已用真實 HTTP 驗證，見 §15）。Cluster（Leaflet.markercluster）與 Heatmap（Leaflet.heat）都重用同一份 Marker 陣列渲染，本地 vendor 檔案，非 CDN-only 依賴。

## 9. District / Postal

Ranking-only；**沒有正式 Polygon 資料時不畫任何地圖色塊**，只提供排行榜 + 「目前無可用地圖座標」提示。

## 10. Timeline / 11. Replay / 12. Filters

Timeline 有分頁/虛擬化；Replay 先一次性 fetch 全部時間桶，前端純函式切片播放，不逐幀重新呼叫 API；Filters（Channel/Device/時間範圍）共用單一狀態物件。

## 13. Store Isolation

所有查詢與寫入一律以 `req.storeId` 為準；已用真實 HTTP 驗證 Store A 無法看到 Store B 的 Marker/District/Timeline 資料（見 §15）。

## 14. Performance

查詢均有對應索引；Marker 查詢有筆數上限；前端輪詢下限 5 秒、背景分頁自動降頻 4 倍；Timeline 分頁；Replay 只 fetch 一次。

## 15. G1 Smoke（212 assertions，超過需求 180+）

```
PASS: 212 / FAIL: 0
```

涵蓋：DB Migration、Coordinate Validation（含 Null Island／非法 source／accuracy 上限）、Consent Status、Denied Cooldown、Timeout、Unsupported、Status-only POST、Coordinate Upsert（Better Accuracy Wins、No Duplicate Marker）、Coordinate-only Visitor Marker、geo_visit_log + coordinate join、Coverage Summary、Known/Unknown/Mappable、Store Isolation、Channel、Device Type、Marker/Cluster/Heat Query、District/Postal/Top5、Timeline、Replay、Filters、四態（Loading/Ready/Empty/Error）、Polling Throttle、Background Tab Guard、Request Guard/Abort/Stale Response Guard、Map/Tile/Layer Reuse、Single Active Mode、Tooltip Sanitization、Privacy、No Fake Marker/IP Coordinates/Store Fallback/District Centroid、No Math.random()/硬編碼/外部 GeoJSON、Accessibility/Responsive Hooks、Vendor Assets Local、Static Asset Load Order、line-order.html 結構完整、No Auto Prompt on Load。

## 16. 真實 HTTP Integration（情境 A–H，全部通過）

| 情境 | 結果 |
|---|---|
| A. granted | `POST /coordinate` 200、`geo_live_coordinates` 有資料、`coverage.granted+1`、`markers.length===1` |
| B. denied | `geo_coordinate_status_log` 有 denied、`markers.length===0`、`coverage.denied+1` |
| C. timeout | 同上，status=timeout |
| D. ip_lookup | `POST` 回 400、無寫入、Marker 不增加 |
| E. (0,0) | `POST` 回 400、無寫入 |
| F. 同一 visitor 更新更精確座標 | Marker 維持 1 筆、accuracy/lat/lng 更新為最新值（已用真實 HTTP + 單元測試雙重驗證） |
| G. Store Isolation | Store A 看不到 Store B 的 Marker（route-level 驗證，回應序列化字串不含對方 visitor_key） |
| H. Coordinate-only Visitor | 即使 `geo_visit_log` 尚無事件，仍安全顯示 Marker，metadata 全部使用安全預設值，序列化後不含 `undefined` |

## 17. Regression 與 Baseline 比較

重跑全部既有 65 套 Smoke／Verify + 全專案 `node --check`：

- **Pre-existing failures（G1 前後皆同樣狀態）**：`scripts/smoke-hotfix30-b5-r5-2-b1-3-geo-behavior-funnel-backend.js` 的 I6/I7/I8/I10/I11 有 pre-existing 的間歇性失敗（~40% 機率），**已用「重新抽取原始未修改 zip、套用同一測試」的方式證實此為專案既有的 flaky test，與 G1 完全無關**（詳見打包內 `regression_flaky_note.txt`）。
- **G1 New failures**：0（重跑多次，除上述已知 flaky 項目外，其餘 64 套全部與 baseline 一致）。
- **G1 Fixed failures**：0（本輪範圍不含修復既有 bug，未主動修改任何既有產品邏輯）。
- **全專案 `node --check`**：0 語法錯誤。

## 18. Static Audit（30/30 通過）

見 `scripts/static-audit-g1.js` 輸出，涵蓋 IP 推估／店家座標／行政區中心冒充、Unknown 不畫點、地圖/Tile/Layer 不重複、無 Memory Leak、無 console.log/Math.random()/硬編碼、Vendor 本地化、Tooltip/API 個資、Coordinate Validation/Store Isolation/Consent 稽核、A7 KPI/Order Heatmap/Dashboard 未退化、HTML 結構完整、Script Load Order、ZIP 排除規則。

## 19. 文件

三份文件皆已建立（本文件組）。

## 20. QA ZIP

`fix18-10-hotfix30-B5-R5.4-G1-QA-full.zip`（由 `scripts/build-g1-qa-zip.js` 產生，已驗證不含 `node_modules`/`.git`/`data/pos.db`）。

## 21. Manual Visual 狀態（誠實揭露）

**尚未執行**。本報告涵蓋的驗證全部是自動化（Node 環境單元測試 + 真實 sql.js DB + 真實 Route handler 呼叫 + 靜態原始碼稽核），**沒有**在真實瀏覽器（Chrome/Edge/Android Chrome/iPhone Safari）中做過人工視覺驗收，包括：Leaflet 地圖實際渲染、Marker/Cluster/Heatmap 視覺呈現、定位權限對話框的實際互動、跨裝置/斷點的版面。`R5.4-G1_MANUAL_QA_CHECKLIST.md` 已列出完整待辦清單。

## 22. 是否可以部署

**後端 API、資料驗證、Store Isolation、Consent 狀態機、既有回歸皆已通過自動化驗證，可以視為後端功能完整且安全。** 但由於 §21 所述人工視覺驗收尚未完成，**在完成 Manual QA Checklist 之前不建議宣告 Final Release／對真實使用者開放前端地圖功能**；建議先以 Feature Flag 或內部帳號驗證方式先行人工測試，確認 Leaflet 渲染、定位權限流程、跨裝置版面後再正式開放。
