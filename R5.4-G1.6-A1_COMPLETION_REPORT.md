# R5.4-G1.6-A1 — Geo Marker Rendering Unification — Completion Report

fix18-10-hotfix30-B5-R5.4-G1.6-A1

## 目標

在既有三個各自畫 Marker 的地方（`geo-live-layer.js` Dashboard 即時訪客
Marker、`geo-heatmap.js` Order Heatmap Marker/CircleMarker、（原本完全沒有
Marker 能力的）`geo-visitor-layer.js` Visitor Layer）之前，先抽出一個
單一、共用、資料格式明確的 Marker Renderer，統一四種精確度狀態
（`exact`／`district_centroid`／`county_centroid`／`unknown`）的畫法與
Tooltip 文案規則，作為後續 Visitor Geo Attribution Pipeline（G1.6-A2）
的渲染基礎設施。

## 已完成項目

1. **單一 Leaflet Map**：確認既有架構（`geo-intelligence-map.js` 的
   `geoInitMap()` / `window.geoMapState.instance`）已經是全站唯一
   Leaflet map 實例，`geo-live-layer.js`／`geo-visitor-layer.js`／
   GA4 Layer 全部透過 `attachToMap()` 或直接讀 `window.geoMapState.instance`
   重用，本輪新增的程式碼**沒有**、也**不需要**建立第二個
   `L.map()`（已用 Smoke B35-B40 實測確認：合併載入後 `mapCreateCount===0`）。
2. **Shared Marker Renderer**：新增 `public/js/geo-marker-renderer.js`——
   純粹的渲染層（不查表、不算座標、不打 API），提供
   `geoMarkerBuildPoints()`（過濾＋聚合）／`geoMarkerBuildTooltip()`／
   `geoMarkerRenderGroup()`／`geoMarkerClearGroup()`。
3. **四態支援**：`exact`（呼叫端已有真實 lat/lng，不聚合，tooltip 由呼叫端
   自訂）／`district_centroid`（依 `area_key` 聚合＋固定文案「行政區推估，
   非實際位置」）／`county_centroid`（同上，文案「縣市級推估，非實際
   位置」）／`unknown`（一律不建立 Marker，見 Smoke A7／B23-24）。
4. **Dashboard Marker 接線**：`geo-live-layer.js` 新增
   `renderEstimateMarkers(points)`／`clearEstimateMarkers()`，透過共用
   `geoMarkerRenderGroup()` 畫縣市／行政區估算 Marker，跟既有「真實座標」
   Marker Cluster（`_renderMarkers`，完全未修改）分開管理。
5. **Visitor Layer Marker 接線**：`geo-visitor-layer.js` 新增
   `geoVisitorRenderMarkers(mapInstance, points)`／
   `geoVisitorClearMarkers()`，同樣透過共用 Renderer。
6. **重要範圍限定（誠實記錄，不得省略）**：`geo-visitor-layer.js` 檔案
   開頭本來就有明確稽核結論——目前專案唯一的行政區 Polygon 資料集
   （`public/data/geo/taiwan/taoyuan-districts.geojson`）是軸對齊矩形
   測試 fixture，不是真實行政區邊界，因此**不能**從它算出「中心點」當
   Marker 座標（算出來的點沒有地理意義，等於假造座標）。本輪新增的
   `geoVisitorRenderMarkers()`／`renderEstimateMarkers()` 因此**目前沒有
   任何呼叫端會真的餵入座標**——這是刻意、誠實的設計：G1.6-A1 只負責把
   「渲染管線」接好、測試好，等 G1.6-A2（Visitor Geo Attribution
   Pipeline，若後續啟動）建立真正合法的 IP 推估 county／district 資料
   之後，直接呼叫這兩個既有函式即可，不需要再回頭修改渲染邏輯。目前這兩
   個函式在真實頁面中處於「已接好線、暫時沒有資料源」的狀態，跟既有
   Choropleth-only 策略完全一致，不是 Bug。
7. **Order Heatmap 不退化**：`geo-heatmap.js` 本輪**完全未修改**（逐字
   比對＋既有 Smoke `smoke-hotfix30-b5-r5-3-a1-geo-heatmap.js` 128/128
   維持不變，見 Smoke B41-45）。
8. **Unknown 不畫 Marker**：見第 3 點，`geoMarkerBuildPoints()` 過濾邏輯
   對 `unknown` 一律排除，且不用任何預設座標（店家座標／地圖中心／隨機
   座標）頂替。
9. **Layer Cleanup 完整**：`geoMarkerClearGroup()` 供兩個模組共用；
   `geo-live-layer.js` 的 `destroy()` 已同時呼叫 `clearEstimateMarkers()`
   （見 Smoke B29-30）。
10. **Tooltip 文案安全規則**：`district_centroid`／`county_centroid`
    tooltip 固定不含 `GPS`／`即時定位`／`精確位置`／`實際地址`（見 Smoke
    A10-17，並提供 `_geoMarkerTooltipHasForbiddenWords()` 供未來新增呼叫
    端自我檢查）。

## Smoke／Regression

- **G1.6-A1 Smoke**（`scripts/smoke-hotfix30-b5-r5-4-g1-6-a1-marker-unification.js`）：
  **50 PASS ／ 0 FAIL ／ 50 TOTAL**，連續三次一致。
- **Regression（`scripts/run-regression-g1-6-a1.js`，三輪）**：全部 PASS，
  FAIL=0，exit=0，三輪數字完全一致。涵蓋 G1.6-A1 Smoke、Order Heatmap
  既有 Smoke（128/128，確認不退化）、Dashboard Rewire 既有 Smoke
  （101/101）、`static-audit-g1.js`／`g1-2.js`／`g1-3.js`（30/37/32，
  確認舊 Scope Guard／Allowlist 不受影響），以及 B2.5／B2.4／B2.3／
  B2.2／B2.1／B2 Settings／B2 Static Audit／B2a／B1／G1.5-A／Geo Map
  Settings／Geo Settings UI／G1 geo-live／G1.4.1／G1.4／G1.3.2／G1.2／
  A2／A1.2／既有 Static Audits 全數維持既有 PASS 數，加上 `node --check`。

## 安全掃描

已掃描工作目錄（排除 `node_modules`／`.git`），無 `BEGIN PRIVATE KEY`／
`private_key_id`／真實 `.iam.gserviceaccount.com` 命中。

## 結論：G1.6-A1 Gate 條件確認

- [x] 單一 Leaflet Map
- [x] Shared Marker Renderer
- [x] Exact／District Centroid／County Centroid／Unknown 四態
- [x] Dashboard Marker 接線
- [x] Visitor Layer Marker 接線（渲染管線已接好；資料源留給 G1.6-A2，見上方第 6 點的誠實範圍限定）
- [x] Order Heatmap 不退化
- [x] Unknown 不畫 Marker
- [x] Layer Cleanup 完整
- [x] G1.6-A1 Smoke／Regression 全綠

本輪只完成 G1.6-A1 並打包 ZIP，依指示不進入 G1.6-A2。

---

## A1.1 Runtime Reality Addendum

**新增日期：fix18-10-hotfix30-B5-R5.4-G1.6-A1.1 輪次**

誠實記錄：本文件上方的 Gate PASS 判定**不完整**。實際稽核 ZIP 後確認：

1. 原 Gate PASS 判定不完整——「Shared Marker Renderer」與「Dashboard／
   Visitor Layer Marker 接線」兩項條件當時只是 API 層級的實作與匯出，
   不構成完整的 Runtime Wiring。
2. `public/js/geo-marker-renderer.js` 當時沒有被 `public/index.html`
   載入——正式瀏覽器環境中，`geo-visitor-layer.js`／`geo-live-layer.js`
   依賴的共用 Renderer 函式實際上不存在。
3. 沒有 Production Caller——`geoVisitorRenderMarkers()`／
   `GeoLiveLayer.renderEstimateMarkers()` 唯一的呼叫端是 A1 自己新增的
   Smoke Test，不是任何真實頁面流程（activate／refresh／metric
   switch／range switch）。
4. 原本的 50/50 Smoke 只證明「函式本身邏輯正確、手動呼叫會畫」，沒有
   證明「使用者操作後正式頁面會自己觸發」。
5. 沒有 Authoritative Centroid Source——Repo 內唯一的行政區 GeoJSON
   是矩形測試 fixture，`taiwan-administrative-areas.json` 完全沒有
   座標欄位。
6. Dashboard 的 `/api/geo-live/markers` API 目前會過濾掉所有沒有真實
   座標的列（`if (!coord) continue`），region-only 的資料本來就不會
   送到前端，這是第二個獨立的 Blocker。
7. A1.1（本輪）已完成 Runtime Wiring：HTML Script 載入、明確
   `window.GeoMarkerRenderer` Browser Namespace、Visitor／Dashboard
   兩端都已接上真實 fetch-and-render pipeline 的正式 Caller、Legend
   Runtime Wiring、Tooltip XSS Escape、四態視覺 CSS Class、完整
   Lifecycle Cleanup。詳見
   `R5.4-G1.6-A1.1_RUNTIME_WIRING_REALITY_AUDIT.md`／
   `R5.4-G1.6-A1.1_RUNTIME_WIRING_FIX.md`。
8. **Estimate Marker（District／County Centroid）仍因資料來源 BLOCKED**
   ——Runtime Wiring 已就緒，但因為上述第 5、6 點的兩個 Blocker，正式
   環境目前實際上不會畫出任何 District／County Estimate Marker（安全
   降級為不畫，不是 Bug）。
9. Manual Visual 未完成——見
   `R5.4-G1.6-A1.1_MANUAL_VISUAL_CHECKLIST.md`，全部項目 NOT TESTED。
10. **不允許進入 A2**——G1.6-A2（Visitor Geo Attribution Pipeline）的
    Gate 狀態為 **BLOCKED**，需要先取得 Authoritative Centroid Source
    及／或後端 region-only payload 支援，才能重新評估是否可以開始。

本 Addendum 不刪除、不覆寫上方原始結論，僅作誠實補充更正。


---

*本文件不含任何真實憑證、Property、Stream 或 Token 內容。*
