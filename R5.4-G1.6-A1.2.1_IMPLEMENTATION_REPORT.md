# R5.4-G1.6-A1.2.1_IMPLEMENTATION_REPORT.md
Geo Event Taiwan Time & Estimate Marker Verification Hotfix

---

## 1. Scope

本輪只完成：

- Geo Event（Recent Geo Events）台灣時間顯示修正
- Today 時間邊界修正（Asia/Taipei 日曆日，`[start, end)`）
- Known District Estimate Marker Runtime QA（真實 Production Helper 驗證）
- Known County Estimate Marker Runtime QA
- Unknown Policy Runtime QA（不畫 Marker、不誤判 Blocked）
- Exact／Estimate Dedupe Runtime QA
- 建立完全隔離的 QA Fixture／Harness

**本輪不包含：**

- IP Geo Attribution（G1.6-A2）
- GA4 Visitor 回填 POS 資料
- 任何往正式資料庫寫入假訪客資料
- 官方 Representative Point Catalog 的重建或修改
- 訂單／GA4／金流等其他模組的時區邏輯重構

## 2. Server Time Helper — `utils/dateTime.js`

新增的單一伺服器端時間 Helper，匯出：

| 函式 | 用途 |
|---|---|
| `parseStoredUtcTimestamp(value)` | 安全解析 DB 儲存值 → `Date \| null`，明確區分「SQLite naive UTC 字串」「已含 Z/offset 的 ISO」「epoch」，無法辨識時回 `null`（不猜時區） |
| `toUtcIsoString(value)` | → `"YYYY-MM-DDTHH:mm:ss.sssZ" \| null`，供 API 輸出標準欄位 |
| `formatTaipeiDateTime(value)` | → `"YYYY-MM-DD HH:mm:ss" \| "—"`，用 `Intl.DateTimeFormat('zh-TW', { timeZone: 'Asia/Taipei', hour12: false })` |
| `getTaipeiDayUtcRange(dateInput)` | → `{ startUtc, endUtcExclusive }`，台灣日曆日換算回 UTC 邊界，供 `[start, end)` 查詢 |
| `isValidTimestamp(value)` | → boolean |

**Contract：**
- DB 繼續儲存 UTC，不修改歷史資料。
- API 輸出 ISO UTC（帶 `Z`）。
- Browser 顯示 Asia/Taipei。
- 已含 Z／offset 的字串不再重複加 8 小時（`HAS_TZ_RE` 分支直接 `new Date(s)`）。

## 3. Browser Time Helper — `public/js/date-time-format.js`

獨立、瀏覽器端等價實作（不能 `require` Node 模組），匯出
`window.formatTaipeiDateTime()` / `window.formatTaipeiDate()` /
`window.parseStoredUtcTimestamp()`。規則與伺服器端一致：

- SQLite UTC 字串（無 Z、無 offset）→ 明確按 UTC 解析
- 已含 Z／offset 的 ISO → 直接解析，不重複加 8 小時
- epoch milliseconds → 直接解析
- 無法辨識 → `"—"`（不是 `Invalid Date` / `NaN` / `1970-01-01`）
- `Intl.DateTimeFormat('zh-TW', { timeZone: 'Asia/Taipei', hour12: false })`

## 4. HTML Wiring

`public/index.html` 新增：

```html
<script src="/js/date-time-format.js?v=fix18-10-hotfix30-B5-R5-4-G1-6-A1-2-1"></script>
```

載入順序：緊接在 `analytics-v2.js` 之後、**`geo-intelligence.js`／
`geo-visitor-layer.js`／`geo-live-layer.js` 之前**（已由 Static Audit #11-14
與 Smoke E-09~11 / H-17 逐一驗證載入順序）。

## 5. API Compatibility

`utils/geoVisitLog.js` 的 `getRecentGeoVisits()`：

- 原欄位 `event_time` 完全保留，格式不變。
- 新增欄位 `event_time_utc`（ISO UTC，帶 `Z`；無法解析時為 `null`）。
- 沒有修改任何 DB 歷史值（純讀取層新增，`SELECT` 語句不變，只有 JS 端 map
  多算一個欄位）。
- 本輪只在這一個函式加欄位，**沒有**大範圍改寫其他 Analytics／訂單時間 API
  （`routes/orders.js` 完全未被觸碰，稽核見 Static Audit #30/#43）。

## 6. Recent Geo Events Renderer

`public/js/geo-visitor-layer.js` 的 `geoVisitorRenderRecentDom()`：

- 優先使用 `r.event_time_utc`，缺失時 fallback 用 `r.event_time`。
- 一律透過 `formatTaipeiDateTime()` 轉換，並對該函式是否存在做防呆
  （未載入時不會整頁崩潰）。
- Tooltip/title 顯示「事件時間（台灣）」，不稱為「登入時間」。
- `visitor_mask` 遮罩邏輯、`event_name` 顯示邏輯完全未被修改。

## 7. Today Range

`utils/geoVisitLog.js` 的 `resolveTimeRangeSince('today')` 改用
`getTaipeiDayUtcRange(nowDate).startUtc`，使用 Asia/Taipei 日曆日邊界，取代
原本的 UTC 日曆日切片。查詢一律使用 `>=`（不使用 `BETWEEN`）。

`'24h'`／`'7d'`／`'30d'` 等 rolling window 分支維持既有精確計算，**不等同**
「今天」，兩者邏輯路徑完全獨立（已由 Smoke C-07~09 驗證兩者結果不同）。

## 8. QA Isolation — `scripts/lib/qa-temp-db.js`

- 只允許 `os.tmpdir()` 底下路徑，或純記憶體模式（`persist: false`，預設）。
- `assertPathIsSafe()` 對任何指向 `data/pos.db` 或非 `os.tmpdir()` 的路徑
  立即 `throw`。
- 完全不 `require('../../utils/db.js')`，不共用正式 DB 模組／singleton。
- DDL 逐字對照 `utils/db.js` 的 `geo_visit_log`／`geo_live_coordinates`，
  避免 Schema Drift。
- `cleanup()` 呼叫 `fs.unlinkSync()` 刪除 temp 檔案（persist 模式）。

## 9. QA Harness — `scripts/run-g1-6-a1-2-1-manual-qa.js`

Fixtures（呼叫真實 Production Helper `geoVisitLog.logGeoVisit()` /
`geoLiveCoordinate.recordLiveCoordinate()` 寫入隔離 temp DB）：

| Fixture | city | district | 座標 |
|---|---|---|---|
| Known District | 桃園市 | 中壢區 | 無 |
| Known County | 桃園市 | `null` | 無 |
| Unknown | `null` | `null` | 無 |
| Exact Control | 桃園市 | 中壢區 | 有（`geo_live_coordinates`） |

**結果：41 PASS / 0 FAIL，連續三次一致。**

`--serve` 模式：
- 只綁 `127.0.0.1`，隨機 Port（`server.listen(0, '127.0.0.1', ...)`）。
- 提供真實 `/api/geo-live/marker-model`／`/api/geo-analytics/visitor-log`
  端點，內部直接呼叫跟正式路由相同的 `utils/geoVisitLog.js` 函式。
- `SIGINT` 觸發 `server.close()` + `qaState.cleanup()`，已實測確認溫度 DB
  無殘留、process 正常結束。
- 不讀正式 `.env`、不連 GA4、不連外部 Geo Provider、不連正式 DB。

## 10. Marker Runtime Verification（真實結果，非預期臆測）

| 情境 | 結果 |
|---|---|
| Known District（桃園市／中壢區） | `accuracy=district_centroid`，1 個 Marker，`coordinate_source=nlsc_official_boundary_representative_point`，lat/lng finite |
| Known County（桃園市，無 district） | `accuracy=county_centroid`，1 個 Marker，沒有猜測 district |
| Unknown | 不產生任何 Marker；`unknown_count` 正確累加；`status='ok'`（不是 blocked/partial） |
| Exact Control | `exact_points=1`；同一 entity 不會同時出現在 `estimate_points`（去重） |
| Catalog Unavailable（模擬） | `exact_points` 不受影響；`estimate_points=[]`；`status='partial'`；`error_code='catalog_unavailable'` |
| Region Query 失敗（模擬） | `exact_points` 不受影響；`estimate_points=[]`；`error_code='region_query_failed'`（不是整頁 500） |
| All Unknown（對應正式畫面 Geo Visitors=2/Unknown=2） | `catalog_available=true`；`unknown_count=2`；`estimate_points=[]`；`status='ok'`；前端 `hadBlockedCandidates` 計算結果為 `false`，不會誤顯示 Blocked Notice |

以上全部由 `getGeoLiveMarkerModel()`／`getGeoVisitAreas()`／
`getRecentGeoVisits()` 這三個既有 A1.2 函式直接產出，本輪**沒有修改**
Marker 判斷邏輯本身（去重規則、accuracy 判斷、coordinate_source allowlist
均維持原樣，見 Static Audit #85-90）。

## 11. Validation

| 項目 | 結果 |
|---|---|
| A1.2.1 Smoke | 190 / 190 PASS，連續 3 次一致 |
| A1.2.1 Static Audit | 106 / 106 PASS，連續 3 次一致 |
| QA Harness | 41 / 41 PASS，連續 3 次一致 |
| Regression（3 輪，涵蓋 A1.2/A1.1/A1/B2.x/B1/G1.5-A/Geo Map Settings/G1 geo-live 等既有套件） | 全部 PASS，`FAIL=0`，`exit=0`，三輪數字一致 |
| Security Scan | 見 Test Report（無真實 Credential） |
| Clean Extraction | 見 Test Report |
| Manual Browser QA | **NOT TESTED**（沒有真實瀏覽器人工操作正式部署環境） |

## 12. Scope Boundary（重要）

**A1.2.1 不會讓正式環境現有的 Unknown 訪客自動變成 Known。**

本輪驗證的是：「假設某筆 `geo_visit_log` 資料已經有已知的 city/district
（不論是 A1.2 之前既有的 Geo Resolver 寫入的，還是未來某個尚未實作的
路徑寫入的），Estimate Marker 是否真的會正確顯示」——答案是**會**，且
去重／Unknown 政策都正確。

但正式環境目前顯示 `Geo Visitors:2 / Known District:0 / Unknown:2`，代表
**這兩位訪客的原始資料本身就是 Unknown**（city/district 都缺）。本輪的
QA Harness 用假 fixture 證明「Known 資料進來後系統能正確顯示」，但**沒有、
也不會**把正式環境現有的兩筆 Unknown 資料轉成 Known。

若要讓一般新訪客自動取得縣市／行政區（例如透過 IP Geo Attribution），
仍需要進行尚未開始的 **G1.6-A2**。
