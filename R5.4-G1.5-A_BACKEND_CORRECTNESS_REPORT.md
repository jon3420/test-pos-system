# R5.4-G1.5-A Backend Correctness Report

fix18-10-hotfix30-B5-R5.4-G1.5-A — GA4 Realtime Backend Correctness & Store Isolation

## 1. Scope

本階段只處理後端：Config Resolver、Request Builder、Client、Orchestrator
（Dedup/Cache/Single-flight/Retry/Stale/Quota）、Route。不含前端接線、
Choropleth 視覺、Settings UI、最終文件五份、最終 ZIP、完整 180+ 綜合
Smoke、G2、Browser GPS、單一使用者追蹤（全部延後）。

## 2. ActiveUsers 原始錯誤

G1.5 草稿：`totalActiveUsers += r.active_users`，對每一列 city×eventName
row 直接累加。同一位使用者若同時觸發多個事件或出現在多個 city row，會被
重複計入，導致回報的「總活躍使用者」高於實際去重後的數字。

## 3. 為何 city rows 不能直接 sum

GA4 Realtime Report 的 city 維度資料是「依維度分組」的列，同一位使用者
可能同時出現在多個 row（例如同時觸發 `view_item` 又觸發
`add_to_cart`，若 city request 也帶 eventName 維度就會拆成兩列）。Google
官方文件建議：需要「去重後總數」時應該使用不含該維度的獨立 Request，讓
Google 端做去重計算，而不是在應用層對已分組的列做加總。

## 4. Summary Request

```
dimensions: []
metrics: [activeUsers, eventCount, (screenPageViews 僅 visitors metric)]
dimensionFilter: 依 streamId/eventName（見 §6）
minuteRanges: 依 window（見 §8）
returnPropertyQuota: true
```
`summary.total_active_users_ga4` 只讀取這個 Request 的結果。

## 5. City Breakdown Request

```
dimensions: [city, cityId, country, countryId]
metrics: [activeUsers, eventCount]
dimensionFilter / minuteRanges / returnPropertyQuota：與 Summary 相同
```
用途：縣市聚合／Tooltip／mapping／unmapped 分析。其 `active_users` 只代表
該 county 聚合列自己的數值，**回應中沒有任何地方宣稱其加總等於
summary.total_active_users_ga4**。

## 6. 共同 Filter

`buildGa4DimensionFilter({streamId, eventName})`：
- 都沒有 → 不加 filter（`null`）
- 只有 streamId → `{filter:{fieldName:'streamId', stringFilter:{matchType:'EXACT', value}}}`
- 只有 eventName → 同上但 fieldName 為 `eventName`
- 兩者都有 → `{andGroup:{expressions:[stream filter, event filter]}}`

## 7. Metric Mapping

| metric | eventName filter |
|---|---|
| visitors | 無（不加 eventName filter，額外含 screenPageViews） |
| view_item | `view_item` |
| add_to_cart | `add_to_cart` |
| checkout | `begin_checkout`（不是自訂 `checkout_click`/`checkout_submit` 等事件） |
| purchase | `purchase` |

`revenue`／`conversion` 不支援，回 `unsupported_metric`。

## 8. Minute Ranges

- window=5 → `{name:'last_5_minutes', startMinutesAgo:4, endMinutesAgo:0}`
- window=30 → `{name:'last_30_minutes', startMinutesAgo:29, endMinutesAgo:0}`
- 其他值 → `invalid_window`

## 9. Property Normalization

`normalizeGa4PropertyId(raw)`：只接受純數字字串；拒絕已帶 `properties/`
前綴、URL、負數、空白、特殊符號。合法時用
`formatGa4PropertyPath()`／request builder 內的模板字串組成
`properties/{id}`。

## 10. Stream Validation

`validateGa4StreamId(raw)`：只接受純數字字串。不當作 secret（顯示層可以讓
店家管理員看到），但一律只從該店自己的設定讀取，不接受前端傳入。

## 11. Store Isolation

`getGa4RealtimeConfig(db, storeId)` 的 SQL 帶 `WHERE store_id=?`。Cache
Key／in-flight Key 都以 storeId 開頭。Smoke 已驗證 Store A/B 的
property/stream 互不相同、互不可讀取。

## 12. Single-store Fallback

需要「部署層級 `GA4_REALTIME_SINGLE_STORE_MODE=true`」**且**「該店設定
`ga4_realtime_single_property_mode=true`」同時成立。任一條件缺席都會回退
成該店自己的（可能缺失的）property/stream 設定，不會意外套用共用
Property。

## 13. Cache Key

`${storeId}::${propertyId}::${streamId||'nostream'}::${windowMinutes}::${metric}`。

## 14. TTL Clamp

`normalizeGa4CacheSeconds(raw, def)`：clamp 到 [30,300]；NaN/undefined 用
預設值（來源優先序：store 設定 → env `GA4_REALTIME_CACHE_SECONDS` →
硬編碼 60）。

## 15. Single-flight

模組層 `Map<cacheKey, Promise>`。同 key 併發共用同一個 Promise；
`.finally()` 保證清除，不論成功或失敗。10 個並行相同 key 呼叫只產生 2 次
Google API 呼叫（Smoke F1），不同 store/metric 各自獨立（F3/F4），reject
後可正常重新發起（F5/F6）。

## 16. Retry

可重試：429/500/502/503/504/TIMEOUT/ETIMEDOUT/ECONNRESET。不可重試：
400/401/403/404/INVALID_CREDENTIALS/INVALID_PROPERTY/INVALID_STREAM。最多
重試 2 次（共 3 次嘗試），退避約 250ms→750ms。

## 17. Timeout

預設 10000ms，可用 `GA4_REALTIME_TIMEOUT_MS` 調整；逾時分類為 `TIMEOUT`
（可重試）。

## 18. Stale Cache Fallback

只有在「沒有 fresh cache」+「有 expired cache」+「重新抓取失敗」三者同時
成立時才回退，回應標記 `is_stale:true`/`status:'stale_cache'`，並保留原
`fetched_at`（絕不假裝剛抓到新資料）。

## 19. Quota

`normalizeGa4QuotaStatus(summaryQuota, cityQuota)` 取兩者較嚴重狀態，只回傳
normal/near_limit/limited/unknown 四種；`client.js` 的 `_parseQuota()` 不
把完整 Google `propertyQuota` 物件往外傳。

## 20. Route Security

`GET /ga4-realtime` 只讀 `req.storeId` + query
（window/metric/refresh，皆有白名單驗證，不合法值 fallback 為安全預設
值）。完全不讀取 property_id/stream_id/credentials（query 或 body）。錯誤
回應只有 `{success:false, code, message, retryable, status}` 五個欄位。

## 21. Status Endpoint

`GET /ga4-realtime-status` 不呼叫任何 Google API（`live_check=1` 權限分支
本階段刻意不實作，延後到有可靠管理者判斷機制的階段）。

## 22. Credential Handling

載入順序：(1) `GOOGLE_APPLICATION_CREDENTIALS`（交給 SDK 解析檔案路徑）
(2) `GA4_SERVICE_ACCOUNT_JSON_BASE64`（base64 decode 後 JSON.parse）
(3) `GA4_SERVICE_ACCOUNT_JSON`（直接 JSON.parse）(4) 都沒有 → SDK 隱含
ADC 解析鏈。三者都只在 `utils/ga4Realtime/client.js` 讀取，`public/`
底下沒有任何檔案引用這些變數名稱（已用 Static Scan 確認）。

## 23. Error Sanitization

`classifyGa4RealtimeError(err)` 把任何例外轉成安全 code 字串，不含原始
error 物件／stack／請求細節。JSON parse 失敗一律分類為 `credential_invalid`，
不把原始字串內容外洩到訊息或 log。

## 24. Known Limitations

- 尚未接正式 HTML（`geo-ga4-realtime-layer.js` 沒有被任何 `<script>` 載入）
- 尚未完成 GA4 UI（5/30 分鐘、metric 切換、手動/自動 Refresh 皆不存在）
- 尚未完成 Choropleth 視覺接線（前端 shape 已過時）
- 尚未完成 Settings UI
- 尚未執行真實 GA4 API（沒有真實憑證，只用 fake client 驗證邏輯）
- 尚未做 Manual Visual（沒有真實瀏覽器環境）
- 尚未做最終全套 23+ Suite 三輪 Regression（本階段只跑了 §三 指定的 11
  項組合，三輪一致，見 Test Report）
- 前端草稿 contract 需要依 `R5.4-G1.5-A_API_CONTRACT.md` 重構才能對接新
  backend response

## 25. Deferred to G1.5-B

前端 script 接線、GA4 Layer UI（時間/指標切換、Refresh、Auto Refresh）、
Choropleth 視覺樣式套用、Settings UI 畫面、`live_check` 管理者權限分支、
Real GA4 Live Test、Manual Visual QA、最終完整 Regression、最終 ZIP 打包。
