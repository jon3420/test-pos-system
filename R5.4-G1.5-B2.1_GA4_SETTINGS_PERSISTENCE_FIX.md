# R5.4-G1.5-B2.1 GA4 Settings Persistence Hotfix

fix18-10-hotfix30-B5-R5.4-G1.5-B2.1 — GA4 Settings Persistence Hotfix

以 `fix18-10-hotfix30-B5-R5.4-G1.5-B2` 為基礎的緊急修正版。

> 本文件所有 Property ID／Stream ID 皆為明確假值（例如
> `stored_property_fixture`／`111111111`／`999999999`），不含任何真實
> GA4 Property／Stream ID。

---

## 一、正式環境重現流程

1. 使用者於 Settings UI 輸入 GA4 Property ID／Stream ID。
2. 按下「儲存設定」。
3. 前端顯示「設定已儲存」（PATCH 成功）。
4. 前端接著重新呼叫 `GET /api/settings/ga4-realtime`。
5. **Property／Stream 欄位立刻變空**，「啟用 GA4」Checkbox 也被取消勾選。
6. Server 狀態區塊顯示 Property／Stream「未設定」。

## 二、使用者看到的現象

- 剛剛才儲存成功的設定，重新整理／切換分頁再回來後「看起來像是沒有存到」。
- Checkbox 被系統自己取消勾選，使用者以為是自己按錯或系統有 bug 把資料清掉了。

## 三、DB 值是否真的被刪除

**沒有被刪除。** PATCH 的 Transaction（`BEGIN` → prepare/run/free →
`COMMIT` → `db._save()`）本身沒有問題，`settings` 表裡的
`ga4_realtime_property_id`／`ga4_realtime_stream_id`／`ga4_realtime_enabled`
等值在 DB 檔案裡一直都在。問題完全出在**讀出來之後怎麼組裝 API
Response**，不是寫入或儲存機制本身。

## 四、Root Cause

`GET`／`PATCH /api/settings/ga4-realtime` 修正前都呼叫：

```js
const config = getGa4RealtimeConfig(db, storeId);
```

`getGa4RealtimeConfig()`（`utils/ga4RealtimeConfig.js` 的
`parseGa4RealtimeSettingsRow()`）回傳的是 **Effective Runtime
Config**——也就是「全域 Feature Flag（`GA4_REALTIME_ENABLED`）＋店家開關＋
Property/Stream 格式＋（在某些路徑下）Credential 是否到位」全部組合後，
「目前系統是否真的能執行 GA4」的判斷結果。這支函式在設計上是給
Runtime Layer（`utils/ga4Realtime/`）與 Connection Test 用的，一旦
`globalEnabled=false`，它就會回傳：

```js
{
  enabled: false, configured: false, propertyId: null, streamId: null,
  singlePropertyMode: false, ...
}
```

而 Settings GET/PATCH Route 卻直接拿這個「Runtime 判斷結果」去填 Settings
Response：

```js
ga4_realtime_enabled: config.enabled,          // ❌ 這是 Effective，不是 Stored
ga4_realtime_property_id: config.propertyId || '', // ❌ 全域 disabled 時永遠是 ''
ga4_realtime_stream_id: config.streamId || '',      // ❌ 同上
```

只要 Server 尚未設定 `GA4_REALTIME_ENABLED=true`（部署環境變數，跟使用者
在 Settings UI 儲存的「店家啟用開關」是兩件不同的事），無論 DB 裡的
Property／Stream／店家 Checkbox 儲存了什麼，GET 一律回傳空字串／`false`。
前端 `geoGa4SettingsSave()` 儲存成功後會呼叫 `geoGa4SettingsLoad()` →
`geoGa4SettingsPopulateForm()`，於是把使用者剛剛存進去的值，用這組「假的
空值」覆蓋掉螢幕上的表單——DB 沒壞，是 API Response 把 Stored 資料跟
Runtime 資料混為一談。

## 五、Stored Settings

「Stored Settings」＝使用者實際儲存在 `settings` 表（`store_id`,`key`,
`value`）的原始值。用途：

- Settings UI 顯示／編輯
- 儲存後重新讀取（GET 確認）
- **不受**全域 Feature Flag 或 Credential 狀態影響

新增 `getGa4RealtimeStoredSettings(db, storeId)`（`utils/ga4RealtimeConfig.js`），
直接 `SELECT key, value FROM settings WHERE store_id=? AND key IN (...)`
讀六個白名單 key，只做「型別/格式」層級的正規化（boolean 解析、cache 秒數
`clamp(30,300)`），完全不套用 `globalEnabled` 或 `singleStoreMode` 判斷。

## 六、Effective Runtime Config

「Effective Runtime Config」＝系統目前**實際是否能執行** GA4（全域 Flag
＋店家開關＋Property/Stream 格式＋Credential 都到位才算）。用途：

- GA4 Layer 實際抓資料
- Connection Test
- Runtime 狀態顯示（可用／不可用）

沿用既有 `getGa4RealtimeConfig(db, storeId)`（`parseGa4RealtimeSettingsRow()`），
本次**沒有修改**其邏輯，G1.5-A 的 Runtime 行為完全不受影響。

## 七、原本 GET 錯誤

`GET /api/settings/ga4-realtime` 只呼叫 `getGa4RealtimeConfig()`，用
Effective Config 的 `enabled`／`propertyId`／`streamId` 直接填 Response 的
`ga4_realtime_enabled`／`ga4_realtime_property_id`／`ga4_realtime_stream_id`——
這三個欄位語意上應該是「使用者儲存了什麼」，卻被拿去回答「系統現在能不能
跑」，兩者被誤當成同一件事。

## 八、原本 PATCH Response 錯誤

`PATCH` 成功寫入 DB、`COMMIT`、`invalidateGa4RealtimeCacheForStore()`
之後，組裝 Response 用的也是同一段 `getGa4RealtimeConfig()` 邏輯，跟 GET
犯一樣的錯——即使剛剛才寫入成功，只要全域 Flag 沒開，PATCH 回應本身就已經
是「假的空值」，前端就算完全信任 PATCH Response 也沒用。

## 九、Stored Resolver

新增（`utils/ga4RealtimeConfig.js`）：

```js
function getGa4RealtimeStoredSettings(db, storeId) {
  // SELECT ... FROM settings WHERE store_id=? AND key IN (6 個白名單 key)
  // 只做型別/格式正規化，不套用 globalEnabled／singleStoreMode
  return {
    ga4_realtime_enabled, ga4_realtime_property_id, ga4_realtime_stream_id,
    ga4_realtime_single_property_mode, ga4_realtime_cache_seconds,
    ga4_realtime_auto_refresh_enabled,
  };
}
```

`getGa4RealtimeConfig()`（Effective）維持不動，兩者都 `module.exports`
出來供 Route／Smoke 各自使用。

## 十、Shared Response Builder

新增（`routes/settings.js`）：

```js
function buildGa4RealtimeSettingsResponse(db, storeId) {
  const stored = getGa4RealtimeStoredSettings(db, storeId);
  const effective = getGa4RealtimeConfig(db, storeId);
  const cred = ga4Client.credentialStatus();
  return {
    success: true,
    data: {
      // Stored（Settings Form 只信任這組）
      ga4_realtime_enabled: stored.ga4_realtime_enabled,
      ga4_realtime_property_id: stored.ga4_realtime_property_id,
      ga4_realtime_stream_id: stored.ga4_realtime_stream_id,
      ga4_realtime_single_property_mode: stored.ga4_realtime_single_property_mode,
      ga4_realtime_cache_seconds: stored.ga4_realtime_cache_seconds,
      ga4_realtime_auto_refresh_enabled: stored.ga4_realtime_auto_refresh_enabled,
      // Effective Runtime（只用於狀態顯示）
      global_enabled, effective_enabled, effective_configured, runtime_error_code,
      property_configured, stream_configured,
      server_single_store_mode_available, credential_available, sdk_available,
    },
  };
}
```

`GET` 與 `PATCH` 都改成呼叫這支函式，避免兩條 Route 各自組裝欄位再度
分歧。也透過 `router.__test.buildGa4RealtimeSettingsResponse` 暴露給
Smoke Test 直接呼叫驗證。

## 十一、GET Fix

```js
router.get('/ga4-realtime', (req, res) => {
  const db = getDb();
  const storeId = req.storeId;
  res.json(buildGa4RealtimeSettingsResponse(db, storeId));
});
```

`ga4_realtime_enabled`／`ga4_realtime_property_id`／`ga4_realtime_stream_id`
等六個欄位一律來自 `getGa4RealtimeStoredSettings()`，不再受
`GA4_REALTIME_ENABLED`／Credential 影響。

## 十二、PATCH Fix

`BEGIN` → prepare/run/free → `COMMIT` → `db._save()` →
`invalidateGa4RealtimeCacheForStore(storeId)` 的順序完全維持不變（本次
沒有修改交易邏輯本身），只有最後組裝 Response 的那一行改成：

```js
res.json(buildGa4RealtimeSettingsResponse(db, storeId));
```

跟 GET 共用同一份 Builder，`COMMIT`＋cache invalidate 都成功之後才重新
讀取 Stored／Effective，確保 PATCH Response 反映的是「剛剛真的寫進去的
值」。

## 十三、Frontend Preserve Form

`public/js/geo-ga4-settings.js`：

- `geoGa4SettingsPopulateForm()` 只使用 Stored 欄位（`ga4_realtime_*`），
  不再讀取／使用 `effective_enabled`／`effective_configured` 等 Runtime
  欄位。
- `geoGa4SettingsLoad()`：只有 `normalized.ok === true` 時才呼叫
  `geoGa4SettingsPopulateForm()`；`ok === false`（GET 失敗或 malformed
  response）時**不清空表單**，改呼叫 `geoGa4SettingsRenderLoadError()`
  顯示「重新讀取設定失敗，請稍後重新載入。」
- `geoGa4SettingsSave()`：PATCH 成功後先用 **PATCH Response** 立即
  `Populate` 表單，再呼叫 `geoGa4SettingsLoad()` 做確認；若確認用的 GET
  失敗，因為 `geoGa4SettingsLoad()` 本身不清空表單，PATCH 已經寫入的值
  會繼續留在畫面上，不會回退成空值。

## 十四、GET Failure 行為

GET 失敗或回傳格式不符預期（`success!==true` 或 `data` 缺失）時：

- 不呼叫 `geoGa4SettingsPopulateForm(emptyDefaults)`。
- 顯示「重新讀取設定失敗，請稍後重新載入。」（`#ga4RealtimeSettingsStatus`，
  套用 `.geo-ga4-settings-load-error` 樣式）。
- 保留使用者目前輸入／PATCH 已寫入的表單內容。
- 不影響先前「設定已儲存」的成功提示（`showToast`）。
- 使用者可再次觸發載入（例如切換分頁再切回來，或未來加上重新整理按鈕）。

## 十五、Runtime Status 分離

Server 狀態區塊（`geoGa4SettingsRenderServerStatus()`）現在顯示七＋二
個獨立欄位，互不覆蓋：

| 顯示項目 | 資料來源 |
|---|---|
| 店家設定 | `ga4_realtime_enabled`（Stored） |
| Server 全域功能 | `global_enabled` |
| 實際執行狀態 | `effective_enabled && effective_configured` |
| SDK | `sdk_available` |
| Server 憑證 | `credential_available` |
| Property | `property_configured`（Stored 是否為合法純數字，不受 Credential 影響） |
| Stream | `stream_configured`（同上） |
| Auto Refresh | `ga4_realtime_auto_refresh_enabled`（Stored） |
| Cache | `ga4_realtime_cache_seconds`（Stored） |

另外 `geoGa4SettingsRenderStatusMessage()` 依組合顯示對應文案：

- 店家已保存但全域未開：「店家設定已保存，但伺服器尚未開啟 GA4 即時功能。」
- 全域已開但憑證未設：「GA4 設定已保存，但伺服器憑證尚未設定。」
- 全部就緒：「GA4 即時功能已就緒。」

## 十六、Property／Stream 正確填法

- **GA4 Property ID（資源 ID）**：填 GA4 後台『資源詳細資料』頁顯示的
  資源 ID，純數字。
- **GA4 Web Stream ID（串流 ID）**：填 GA4 後台『網頁串流詳情』頁顯示的
  串流 ID，純數字。

## 十七、Property／Stream 不得填反

Settings UI 已加註：「Property ID 與 Stream ID 是不同欄位，請勿填反。」
且**不使用長度猜測**兩者是否填反（兩者都是數字、長度可能改變，長度判斷
不可靠）——僅做「是否為純數字」的格式驗證，實際對應關係由使用者依 GA4
後台頁面自行核對。

## 十八、G- 評估 ID 不使用

Stream ID 說明文字已加註：「不是 G- 開頭的評估 ID。」G- 開頭的是 GA4
「評估 ID」（Measurement ID，前端追蹤用），跟本頁需要的數字型 Web Stream
ID 是不同東西，不應填入 Property／Stream 欄位。

## 十九、B2.1 Smoke 最終結果

`scripts/smoke-hotfix30-b5-r5-4-g1-5-b2-1-ga4-settings-persistence.js`：

```
PASS:  85
FAIL:  0
TOTAL: 85
```

連續執行 3 次，結果完全一致（見下方 Regression 三輪紀錄）。涵蓋：

- A. Stored Resolver（A1–A10）
- B. GET Route（B11–B20）
- C. PATCH／Transaction／Rollback（C21–C30）
- D. Frontend jsdom（D31–D45）
- E. Labels（E46–E50）
- F. Mutation Testing（F51–F60，每項都是「若改回錯誤版本就會 FAIL」的斷言）
- G. 補充驗證（G61–G65：Stored/Effective 合法並存、Credential 缺失不清空、
  Mutation：load catch 分支未直接 Populate empty defaults）
- H. Stored Settings 專項場景 A–C／E（H66–H75：全域關閉／Credential 缺失／
  儲存後重讀一致／Store Isolation）

## 二十、Regression 三輪結果

使用 `scripts/run-regression-b2-1.sh 3` 執行，每支測試前清除
`data/pos.db` 殘留，避免跨腳本汙染：

| Suite | Round 1 | Round 2 | Round 3 |
|---|---|---|---|
| B2.1 Smoke | 85/85 | 85/85 | 85/85 |
| B2 Smoke | 181/181 | 181/181 | 181/181 |
| B2 Static Audit | 82/82 | 82/82 | 82/82 |
| B2a Smoke | 106/106 | 106/106 | 106/106 |
| B1 Smoke | 168/168 | 168/168 | 168/168 |
| B1 Static Audit | 71/71 | 71/71 | 71/71 |
| G1.5-A Smoke | 140/140 | 140/140 | 140/140 |
| G1.5-A Static Audit | 77/77 | 77/77 | 77/77 |
| Geo Map Settings Smoke | 620/620 | 620/620 | 620/620 |
| Geo Settings UI Smoke | 157/157 | 157/157 | 157/157 |
| G1 geo-live Smoke | 212/212 | 212/212 | 212/212 |
| G1.4.1 Smoke | 149/149 | 149/149 | 149/149 |
| G1.4 Smoke | 148/148 | 148/148 | 148/148 |
| G1.3.2 Smoke | 148/148 | 148/148 | 148/148 |
| G1.2 Smoke | 83/83 | 83/83 | 83/83 |
| A2 Smoke | 229/229 | 229/229 | 229/229 |
| A1.2 Smoke | 189/189 | 189/189 | 189/189 |
| Static Audit G1.4.1 | 56/56 | 56/56 | 56/56 |
| Static Audit G1.4 | 52/52 | 52/52 | 52/52 |
| Static Audit G1.3.2 | 48/48 | 48/48 | 48/48 |

三輪數字完全一致，`FAIL=0`，`exit=0`。

`node --check` 全部通過：`utils/ga4RealtimeConfig.js`／
`routes/settings.js`／`public/js/geo-ga4-settings.js`／
`public/js/geo-ga4-realtime-layer.js`／B2.1 Smoke／B2 Smoke／B2 Static
Audit。

**已知的既有 Housekeeping 事項（非本次修正引入）**：`static-audit-g1-4.js`
與 `static-audit-g1-3-2.js` 各自有一項「no test DB residue」檢查，若前一支
Smoke Test 留下 `data/pos.db` 未清除會誤判為 FAIL（52/52→51/52，48/48→
47/48）；`run-regression-b2-1.sh` 已在每支測試前清除 `data/pos.db` 解決，
本次 Regression 全數 52/52、48/48 通過。此為既有腳本間的殘留檔案依賴，
與本次 GA4 Settings Persistence 修正無關，也未修改該行為。

無 `data/pos.db` 殘留、無 Timer 殘留（jsdom cooldown timer 每次測試後
`clearInterval`）、無 fake fetch 殘留（jsdom `window` 於每個 IIFE 區塊內
建立、不跨區塊共用）、無 Store 汙染（Store A/B 用不同 `store_id`，每輪
GET/PATCH 皆帶對應 `x-test-store`）、無 credentials 殘留（Smoke/Manual
Walkthrough 全程未設定任何真實 Service Account／Credential 環境變數）。

## 二十一、Manual Visual：NOT TESTED

本次修正未在真實瀏覽器手動點擊驗證視覺呈現，僅透過 jsdom 驗證 DOM 行為與
資料綁定邏輯。

## 二十二、Real GA4 Live Test：NOT TESTED

本次修正未對真實 GA4 Property／Stream 或真實 Google Cloud Service
Account 憑證進行連線測試；所有驗證皆使用假的 GA4 Client
（`client._setClientForTest()`）與 sql.js 本地 DB。

## 二十三、Known Limitations

1. 本次修正只處理「Stored Settings 與 Effective Runtime Config 分離」與
   「前端不因 GET 失敗清空表單」；GA4 Realtime Layer 本身的 Runtime
   邏輯（`utils/ga4Realtime/`、G1.5-A）完全未變動。
2. Fake DB／Manual Walkthrough 驗證的是本地 sql.js DB 行為，未涵蓋
   Zeabur 實際部署環境下的環境變數注入時機、真實 Credential 載入路徑等
   基礎設施層面差異。
3. `static-audit-g1-4.js`／`static-audit-g1-3-2.js` 的「no test DB
   residue」檢查依賴呼叫順序（前一支測試需自行清理 `data/pos.db`），
   屬於既有 Regression Suite 的既知行為，本次沿用既有作法（Regression
   Runner 每支測試前清除）而未修改該 Static Audit 本身。
