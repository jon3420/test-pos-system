# R5.4-G1.5-B2.2 Auth Reality Audit

fix18-10-hotfix30-B5-R5.4-G1.5-B2.2 — GA4 Layer Store Authentication Hotfix

## 一、正式環境重現畫面

1. 使用者已用店家帳號登入（`store_001`），可正常操作其他功能。
2. Settings 頁的 GA4 GET／PATCH 正常，Property／Stream／Checkbox 都能
   正確保存與顯示（B2.1 Persistence Fix 已修正）。
3. 切換到「GA4 即時訪客」圖層後失敗。

## 二、錯誤訊息

畫面顯示兩則訊息：

```
GA4 即時圖層載入失敗，請重試
缺少店家登入 token，請重新登入
```

## 三、`/api/geo-live` mount middleware

`server.js` 掛載方式（沿用 `routes/analytics-geo.js` 既有慣例）：

```js
app.use('/api/geo-live', requireStore, require('./routes/geo-live'));
```

`requireStore` 在掛載時套用一次，`/api/geo-live` 底下所有 Route（包含
`/ga4-realtime`、`/ga4-realtime-status`、`/ga4-realtime-test`）都必須先
通過這一關才會進到各自的 handler。

## 四、requireStore 真實錯誤 Response

`middleware/storeGuard.js` `requireStore()`：解析順序 JWT → `x-store-id`
header → `query.store_id`，**沒有任何 fallback**。三者都缺席時：

```js
console.warn(`[storeGuard] 401 NO_STORE_TOKEN: ${req.method} ${req.path}`);
return res.status(401).json({
  success: false,
  error:   'NO_STORE_TOKEN',
  message: '缺少店家登入 token，請重新登入',
});
```

這與正式環境畫面上出現的第二則訊息**逐字相同**，證實請求根本沒有帶上
任何身分資訊（不是 token 過期，是請求本身沒有 `Authorization`／
`x-store-id`）。

## 五、GA4 Layer 原本兩個裸 fetch 位置

`public/js/geo-ga4-realtime-layer.js`（修正前）：

1. `geoGa4FetchData({ windowMinutes, metric, refresh, signal })`：

   ```js
   const resp = await fetch(url, { signal });
   ```

   對應 `GET /api/geo-live/ga4-realtime?window=...&metric=...&refresh=...`。

2. `geoGa4FetchAndRender(containerId)` 內的 inline status request：

   ```js
   const statusResp = await fetch('/api/geo-live/ga4-realtime-status');
   ```

兩者都是無認證的裸 `fetch()`，不帶 `Authorization`、不帶 `x-store-id`，
`requireStore` 因此每次都以 `401 NO_STORE_TOKEN` 擋下，不管使用者是否
真的已登入。

## 六、Settings UI 使用 apiFetch 的位置

`public/js/geo-ga4-settings.js`（修正前後皆同，未變動）：

```js
const res = await apiFetch('/api/settings/ga4-realtime', controller ? { signal: controller.signal } : {});
...
const res = await apiFetch('/api/settings/ga4-realtime', { method: 'PATCH', body: JSON.stringify(patch) });
...
const res = await apiFetch('/api/geo-live/ga4-realtime-test', { method: 'POST', body: JSON.stringify({}) });
```

`routes/settings.js` 的 GET/PATCH 也掛在有 `requireStore` 的路徑下
（`app.use('/api/settings', requireStore, ...)`，沿用既有 SaaS R1 慣例），
但因為都經過 `apiFetch()`，`Authorization`／`x-store-id` 一律自動帶上，
所以能正常通過驗證——這就是「Settings 正常、GA4 Layer 失敗」的完整落差
原因：兩邊呼叫的是同一種後端驗證機制，差別只在前端有沒有用
`apiFetch()`。

## 七、apiFetch 真實 Authentication 行為

`public/js/app.js` `apiFetch(url, options = {})`：

1. 從 `localStorage['pos_store_token']`（`TOKEN_KEY`）讀 token，若存在則加
   `Authorization: Bearer <token>`。
2. 從 `window.currentStore.store_id` 或
   `localStorage['pos_store_info'].store_id` 讀店家 ID，補上
   `x-store-id` header（JWT 失效時的第二條解析路徑）。
3. 呼叫底層 `fetch(url, { ...options, headers })`。
4. 依 `res.status` 分三種情況回傳（見下一節）。

`geoGa4FetchData()`／`geoGa4FetchStatus()` 一律呼叫 `apiFetch()`，
**不自行讀取 `localStorage`、不自行組 `Authorization` Header**，由
`apiFetch()` 統一負責（需求文件二、四）。

## 八、apiFetch Response Shape（實測三種情況）

| 情況 | 回傳值 |
|---|---|
| 200（或任何非 401/403 的 HTTP 狀態，如 400/500） | **原生 fetch Response**，呼叫端要自己 `await res.json()` |
| 401 | `{ ok: false, status: 401, body }`，`body` 是**已經解析過的 JSON**（`res.json().catch(() => ({}))`），**沒有 `.json()` 方法** |
| 403 | `{ ok: false, status: 403, body }`，同上 |
| Network Error（離線／DNS 失敗等） | 底層 `fetch()` reject，`apiFetch()` 本身沒有 try/catch 包起來，所以整個 `await apiFetch(...)` 呼叫直接 reject，呼叫端要自己 try/catch |
| AbortError（`controller.abort()`） | 同上，`fetch()` reject 一個 `name==='AbortError'` 的 Error，`apiFetch()` 原樣往外丟 |

也就是需求文件三列出的三選項中的 **C**：「Success 是原生 Response
（要自己 `.json()`），但 401/403 是 `{ok:false,status,body}`」——不是單一
固定形狀，呼叫端必須先判斷 `status` 再決定要不要呼叫 `.json()`，否則對
401/403 的回傳值呼叫 `.json()` 會直接噴 `TypeError: res.json is not a
function`。

`public/js/geo-visitor-layer.js` 的 `geoVisitorFetchAndRender()` 已經是
正確示範（`if (res && res.ok) { const json = await res.json(); ... }`），
`geoGa4FetchData()`／`geoGa4FetchStatus()` 現在採用同一種判斷順序。

## 九、Status Request 修正

`geoGa4FetchStatus()`（新增的具名函式，取代原本寫死在
`geoGa4FetchAndRender()` 裡的裸 fetch）：

```js
async function geoGa4FetchStatus() {
  const res = (typeof apiFetch === 'function')
    ? await apiFetch('/api/geo-live/ga4-realtime-status', { method: 'GET' })
    : null;
  if (!res) return null;
  if (res.status === 401 || res.status === 403) return null;
  if (typeof res.json !== 'function') return null;
  try {
    const json = await res.json();
    if (json && json.success === true && json.data && typeof json.data === 'object') return json.data;
  } catch (e) { /* malformed response，安全降級為 null */ }
  return null;
}
```

401/403 時回傳 `null`，讓呼叫端維持目前 `configAutoRefreshEnabled` 值，
不阻擋主要的 Data Request——真正的 Auth 錯誤由 Data Request 統一顯示，
避免同一個原因跳兩次錯誤訊息。

## 十、Data Request 修正

`geoGa4FetchData()`：

```js
async function geoGa4FetchData({ windowMinutes, metric, refresh, signal }) {
  const url = geoGa4BuildRequestUrl({ windowMinutes, metric, refresh });
  const res = (typeof apiFetch === 'function')
    ? await apiFetch(url, { method: 'GET', signal })
    : null;
  if (!res) return geoGa4NormalizeResponse(null);
  if (res.status === 401 || res.status === 403) {
    return geoGa4AuthErrorPayload(res.body);
  }
  if (typeof res.json !== 'function') return geoGa4NormalizeResponse(null);
  const json = await res.json();
  return geoGa4NormalizeResponse(json);
}
```

## 十一、AbortController 是否保留

保留。`_geoGa4RunFetch()` 仍在每次呼叫前 `abort()` 舊 controller、建立新
`AbortController`，把 `controller.signal` 傳進 `geoGa4FetchData()` 的
`{ signal }` 參數，`apiFetch(url, { method:'GET', signal })` 會把
`signal` 併入 `options` 一路傳到底層 `fetch()`。`geoGa4Deactivate()`
仍會 `abort()` 目前的 controller。

## 十二、requestSeq 是否保留

保留。`_geoGa4RunFetch()` 的 `mySeq = ++geoGa4State.requestSeq` 與
`if (mySeq !== geoGa4State.requestSeq) return;` 完全未變動，舊回應仍然
不會覆蓋新狀態。

## 十三、Auto Refresh 是否也走 apiFetch

是。`_geoGa4ScheduleAutoRefresh()` 排程的 timer 呼叫的是
`_geoGa4RunFetch(containerId, { refresh:false })`，跟手動 Refresh 呼叫
同一支函式、同一條 `geoGa4FetchData()` 路徑，沒有另外一套 fetch 邏輯。

## 十四、Manual Refresh 是否也走 apiFetch

是。`geoGa4Refresh(containerId)` 呼叫 `_geoGa4RunFetch(...,
{refresh:true})`，跟 Auto Refresh 走同一支函式。

## 十五、401／403 與 GA4 Backend Error 分離

新增 `status: 'auth_error'`（`geoGa4AuthErrorPayload()`），跟 GA4
Backend 回傳的 `status: 'error'`／`'disabled'`／`'not_configured'` 完全
是不同分支，`geoGa4StatusMessage()`／`geoGa4MapOverlayMessage()`／
notices renderer 都先判斷 `status==='auth_error'` 才判斷其他狀態，兩者
文字不共用、不會互相覆蓋。

## 十六、Global Disabled 正確文字

`status: 'disabled'`（`error_code: 'ga4_realtime_disabled'`）→

> 店家設定已保存，但伺服器尚未開啟 GA4 即時功能。

跟 Settings UI（B2.1）「Server 全域功能未啟用」訊息用語一致。

## 十七、Credential Missing 正確文字

`status: 'error'` 且 `error_code: 'credential_unavailable'`（或
`not_configured`／`SDK_UNAVAILABLE`）→ 沿用／新增
`GEO_GA4_ERROR_MESSAGES.credential_unavailable`：

> 伺服器尚未設定 GA4 憑證。

## 十八、Property／Stream Error 正確文字

- `not_configured` + `missing_property` → 「尚未設定 Property。請至 GA4
  設定完成 Property／Stream 設定。」
- `not_configured` + `invalid_property` → 「Property 格式錯誤。…」
- `error` + `property_not_found` → 「找不到此 GA4 Property。」
- `error` + `stream_filter_invalid` → 「Stream ID 無法套用於目前
  Property。」

## 十九、本階段未修改 Backend

`routes/geo-live.js`／`routes/settings.js`／`utils/ga4RealtimeConfig.js`／
`utils/ga4Realtime/*`／`middleware/storeGuard.js` 全數未變動。本輪只動
`public/js/geo-ga4-realtime-layer.js`（前端 Authentication 修正）與相關
Smoke Test 的 mock 機制。

## 二十、Manual Visual

NOT TESTED（僅透過 jsdom 驗證 DOM／狀態邏輯，未在真實瀏覽器手動操作）。

## 二十一、Real GA4 Live Test

NOT TESTED（未對真實 GA4 Property／Stream／Service Account 憑證做連線
測試，所有驗證皆用假的 GA4 Client 與假的 `apiFetch`／`fetch` mock）。
