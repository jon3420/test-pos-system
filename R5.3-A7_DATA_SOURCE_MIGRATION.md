# R5.3-A7 — Geo KPI Data Source Migration

## 1. Before（本輪之前）

```
analytics_events
  → _visitorGeoAttributionCTE()          (utils/geoAnalyticsQueries.js)
  → getGeoFunnel()                        (utils/geoAnalyticsQueries.js)
  → GET /api/analytics/geo/funnel         (routes/analytics-geo.js)
  → loadGeoDashboardData() → vm.funnel    (public/js/geo-intelligence.js)
  → computeGeoDashboardKpi(vm)            (public/js/geo-intelligence.js)
  → geoBuildKpiSummaryCards(...)
  → 「Geo 訪客／Geo 加購／Geo 結帳／Geo 訂單／Geo 成交率／
     Geo 辨識率／Unknown 比例」KPI 卡片
```

### 根因（R5.3-A6 診斷已證實，本輪未重新分析）

`_visitorGeoAttributionCTE()` 的 `visitor_geo_earliest` CTE：

```sql
WITH visitor_geo_earliest AS (
  SELECT identity_key, MIN(...) AS first_seen
  FROM analytics_events
  WHERE store_id = ? AND geo_context = 'visitor'
    AND (geo_city IS NOT NULL OR geo_district IS NOT NULL)   ← 排除點
  GROUP BY identity_key
)
```

任何訪客的 `geo_city`／`geo_district` 兩者皆 `NULL`（Unknown IP 地理），在
這裡就被整筆排除——不是分類成 Unknown，是這個人從未出現在
`visitor_geo_attributed` CTE 裡。`getGeoFunnel()` 的主查詢 `FROM
visitor_geo_attributed vga` 因此永遠看不到這個人，`areas` 陣列缺這一筆，
`computeGeoDashboardKpi()` 加總後，Unknown 訪客對應的「Geo 訪客／加購／
結帳／訂單」全部消失，顯示為 0，即使同一時刻老闆儀表板「目前在線」與
Analytics 轉換漏斗都正確顯示 1。

## 2. After（本輪之後）

```
geo_visit_log
  → getGeoEventFunnel()                        (utils/geoEventEngine.js — 未修改，A5 已含 Unknown 修正)
  → GET /api/analytics/geo/visitor-log          (routes/analytics-geo.js — 未修改，A1.2 既有端點)
  → geoVisitorFetchAndRender() → geoVisitorState.funnel + geoVisitorState.status
                                                  (public/js/geo-visitor-layer.js)
  → geoIntelligenceOnEventEngineUpdate() 更新 hook
  → geoAdaptEventEngineFunnelForKpi()            (public/js/geo-intelligence.js，新增，唯一 adapter)
  → geoBuildKpiSummaryCards(...)                 (未修改，純函式)
  → 「Geo 訪客／Geo 加購／Geo 結帳／Geo 訂單／Geo 成交率／
     Geo 辨識率／Unknown 比例」KPI 卡片
```

`getGeoEventFunnel()` 的 Visitors／Known／Unknown 統計本來就以
`COALESCE(visitor_id, session_id, event_id)` 為去重鍵、以
`is_unknown` 欄位分類（見 A1.2／A5 既有邏輯，本輪未修改一行），Unknown
訪客永遠計入分母，不會像舊路徑一樣在 CTE 階段整筆消失。

## 3. 為什麼不是單純移除 SQL WHERE 條件

本輪刻意**不**修改 `_visitorGeoAttributionCTE()` 的
`WHERE (geo_city IS NOT NULL OR geo_district IS NOT NULL)`，原因：

1. **影響面不可控**：這個 CTE 同時被 `getGeoFunnel()`、
   `getGeoDashboardSummary()`（`top_areas`）等多個既有查詢共用，貿然放寬
   WHERE 條件會讓 Unknown 訪客的 `(city, district)` = `(NULL, NULL)`
   一起混進 `GROUP BY vga.geo_city, vga.geo_district`，變成一列
   `city=NULL, district=NULL` 的「假行政區」，污染行政區排行榜／
   Choropleth／Top 3 等本來就正確、且本輪明確禁止修改的既有功能。
2. **系統內已經有一套正確處理 Unknown 的引擎**：`geo_visit_log` +
   `getGeoEventFunnel()`（A1.2／A5 完成）本來就把 Unknown 當一等公民
   處理（`is_unknown` 分類、永遠計入分母），沒有理由維護兩套「修 Unknown
   排除」的邏輯。
3. **需求文件明確決策**：本輪的正式決策是「KPI Single Source of Truth」
   ——換資料來源，而不是修好舊來源後继续维护两套。舊路徑保留給尚未遷移、
   且本輪禁止觸碰的模組（見下方）。

## 4. 仍保留 `getGeoFunnel()` / `vm.funnel` 的用途（legacy area aggregation only）

| 用途 | 呼叫位置 |
|---|---|
| 行政區 Top 3／高意願／低轉換排行 | `computeGeoTopAreas(vm)`、`refreshGeoDashboardKpiBlock()` 內的 `tops` |
| 排行榜（Ranking Table） | `computeGeoAreaRanking()`、`_renderGeoAreaRankingTable()` |
| Choropleth 地圖著色 | `geoInitMap(mapContainerId, vm.funnel.areas)` |
| isEmpty／Empty State 判斷 | `refreshGeoDashboardKpiBlock()` 的 `isEmpty`、`geoBuildEmptyStateMessage(vm)` |
| Decision Center／Recommended Actions | `geoRenderDecisionCenter(vm)`（讀 `vm.recommendation_view_models`，來自同一次 `/alerts`，不是本輪範圍） |
| `GET /api/analytics/geo/funnel` 路由本身 | `routes/analytics-geo.js`，未刪除、未停用 |
| `getGeoDashboardSummary()`（`@deprecated` 但保留） | `routes/analytics.js` 的 `GET /api/analytics/dashboard` 仍回傳 `geo_summary`，未動 |

以上全部標記為 **legacy area aggregation only**（原始碼中 `computeGeoDashboardKpi()`
與 `_geoRenderKpiLiveHtml()` 附近皆有對應註解），不再驅動本輪遷移的那組
KPI 卡片。
