# G1.6-A2-P1 — Production IP Geo Provider 官方來源比較

依據：各 Provider 官方網站（`members.ip-api.com`、`ip-api.com/docs/legal`、
`ipinfo.io/pricing`、`ipdata.co` 官方文件、`maxmind.com`/`dev.maxmind.com`/
`support.maxmind.com`）。不使用部落格或比較網站的價格作為依據；第三方
估價僅在 Provider 官方頁面本身不公開報價時，標註為「非官方估計」。

**特別注意（適用所有方案）**：本 POS 是**多租戶 SaaS**，同一份程式碼服務
多個獨立店家（`store_id`）。這件事本身會影響授權判定——尤其 MaxMind
免費 GeoLite2 的線上 EULA 明確排除「用於支援任何 B2B 產品/服務/平台」，
這正是本專案的實際型態，見下方 D 方案。

## 逐項比較

| 項目 | A. ip-api Pro | B. IPinfo | C. IPdata | D. MaxMind GeoIP2/GeoLite2 |
|---|---|---|---|---|
| 1. 商用授權 | Pro 方案明確允許商用 | Core/Max/Enterprise 皆含 Commercial License | 需查其官方條款（付費方案通常允許商用） | **免費 GeoLite2 線上 EULA 明確禁止用於 B2B 產品/平台**（本專案的實際型態）；需付費 Commercial License 或 GeoIP2（仍需 Enterprise 議價，非公開定價） |
| 2. HTTPS | Pro 支援（`pro.ip-api.com`） | 支援 | 支援 | 本地資料庫方案不涉及網路傳輸；GeoIP2 Web Service 支援 HTTPS |
| 3. API Key | 需要 | 需要 | 需要 | Web Service 需要；本地資料庫檔案不需要 Key，但需授權/授權碼下載 |
| 4. 台灣縣市（county）命中率 | 官方未公開台灣專屬數字；一般城市層級資料庫對台灣六都命中率通常較好 | 同上，Core 起支援 city-level | 未查得公開數字 | 同上，GeoIP2 City 資料庫是業界常用基準之一，但仍是「城市」而非台灣行政層級的分類 |
| 5. 台灣行政區（district，如中壢區）命中率 | **既有 adapter 註解已誠實記載：district 欄位覆蓋率有限，常為空字串** | 官方回應欄位是 `city`，沒有「行政區」這一層，需要額外對照才可能命中到市轄區等級（如台北市大安區），縮小到「區」通常仍不可靠 | 同上，沒有 Provider 公開宣稱可到台灣「區」等級 | 同上，GeoIP2 City 資料庫官方文件本身警告「位置通常接近人口中心點，不應用於識別特定地址/戶」——不承諾區級精度 |
| 6. district/city 欄位語意 | `district` 欄位存在但官方未強調覆蓋率 | 無獨立 district 欄位，只有 `city` | 需查閱其 schema | 無獨立 district 欄位，只有 `city` |
| 7. 每月查詢額度 | **無限**（Pro/Enterprise 皆為 unlimited requests） | Core 150k/mo，Max 250k/mo（超額 $0.54~$1.03/千次） | 依方案，需查官網 | 依方案；Web Service 按查詢計費，本地資料庫是「檔案授權」不是「額度」 |
| 8. Rate Limit | 官方未特別列出額外速率限制（unlimited requests 條款下） | 依 API Key 監控用量，非硬性速率限制 | 需查 | Web Service 依方案；本地資料庫無網路速率限制（本地查詢） |
| 9. 價格（官方公開報價） | **€13.3/月**（年繳，Pro），Enterprise 洽詢 | Lite 免費（僅國家層級）／**Core $41/mo**（150k）／Max $130/mo（250k）／Enterprise 洽詢 | 未在本輪取得官方公開報價頁 | 免費 GeoLite2（僅限非 B2B 內部用途，本專案不適用）；GeoIP2 資料庫官方不公開報價，需聯繫 `enterprise@maxmind.com`（第三方估計約 US$200–400/月起，僅供參考，非官方數字） |
| 10. SLA | 99.9%（未達標自動延長訂閱 10 天） | 99.99% uptime（官方頁面宣稱） | 未查得 | Web Service 99.99%（官方宣稱）；本地資料庫無 SLA 概念（無網路依賴） |
| 11. Timeout | 由呼叫端（既有 adapter）自行設定，不受 Provider 限制 | 同上 | 同上 | 本地資料庫查詢是同步記憶體操作，理論上無網路 timeout 問題，但需另外處理資料庫檔案讀取/更新流程 |
| 12. Privacy | 免費端點官方隱私政策寫明：**在記憶體中最多保留客戶端 IP 1 分鐘用於 rate-limit，不記錄請求**（Pro 條款另有規範，但同屬同一公司政策精神） | 官方未在本輪查得逐字保留政策細節，需查其 Privacy Policy | 需查 | 本地資料庫方案：**IP 完全不離開自己的伺服器**，沒有第三方看到任何訪客 IP——隱私上最乾淨，但仍需符合 EULA 商用限制 |
| 13. 查詢 IP 是否被 Provider 留存 | 依其官方隱私政策，短暫記憶體保留（≤1 分鐘，用於 rate-limit），不做請求記錄 | 未在本輪確認逐字條款 | 未查 | **本地資料庫方案不適用**——IP 從未送出到任何第三方 |
| 14. 資料更新頻率 | 「Frequently updated」（Pro 頁面用詞，未給出精確頻率） | 官方頁面用詞「daily data updates」（部分競品比較頁引用） | 未查 | GeoLite2/GeoIP2 官方固定每週更新（業界通用認知，MaxMind 文件描述為定期更新，需要維運者自行下載新檔案套用) |
| 15. IPv4 | 支援 | 支援 | 支援 | 支援 |
| 16. IPv6 | **Provider 官方支援**（Pro 頁面列「IPv4, IPv6 and domain queries」）；**但既有 `utils/geoProviders/ipapi.js` adapter 目前刻意回傳 `IPV6_UNSUPPORTED`，這是 adapter 本身尚未實作，不是 Provider 限制** | 支援 | 支援 | 支援（GeoIP2 資料庫本身即含 IPv6） |
| 17. Node.js 整合方式 | 純 HTTP GET + JSON，現有 adapter 已用 `node-fetch` 實作 | 同樣是 HTTP GET + JSON，或官方 Node SDK（`node-ipinfo`） | HTTP GET + JSON | 需要 `maxmind` npm package 讀取本地 `.mmdb` 檔案，或呼叫其 Web Service HTTP API |
| 18. 是否能沿用既有 adapter contract | **可以，且幾乎零改動**——現有 adapter 已支援 Pro endpoint、API Key、timeout、429/5xx/invalid JSON，缺口只有 IPv6 | 需新增一個 adapter 檔案（`ipinfo.js`），介面照抄 `ipapi.js` 的 shape 即可 | 同上，需新增 adapter | 需新增 adapter，但**查詢邏輯不同**（讀本地檔案而非發 HTTP 請求）——仍可包成同一個 `lookupVisitorGeo(ip, options)` 介面，對 `geoResolver.js`/`geoProviders/index.js` 完全透明 |
| 19. 是否需要新增 dependency | 不需要（`node-fetch` 已存在） | 不需要（同樣可用現有 `node-fetch`），或選用官方 SDK 則需新增一個小型 npm 套件 | 不需要（`node-fetch`） | **需要**新增 `maxmind` npm package |
| 20. 是否需要本地資料庫檔 | 不需要 | 不需要 | 不需要 | **需要**，且需要建立「定期更新 `.mmdb` 檔案」的維運流程（Zeabur 上要處理檔案下載/掛載） |
| 21. Zeabur 部署負擔 | 最低（只是多一個環境變數＋API Key） | 低（同上） | 低（同上） | **較高**——需要把資料庫檔案放進部署（image 內建或啟動時下載），並規劃更新機制，避免資料庫過期 |
| 22. 維護成本 | 低（Provider 端維護資料，本專案只需續約） | 低 | 低 | 中～高（除了續約/授權，還要自己維護資料庫檔案更新排程） |

## 三種明確建議

### 1. 最少程式改動方案 — **ip-api Pro**
- 優點：現有 `utils/geoProviders/ipapi.js` 幾乎完整支援 Pro endpoint（HTTPS、
  API Key、timeout、429/5xx/invalid JSON 全部已實作），只需要：
  (a) 補上 IPv6 支援（目前刻意 short-circuit 回 `IPV6_UNSUPPORTED`，
  這是本專案自己選擇先不做，不是 Provider 做不到），
  (b) 設定 `GEO_VISITOR_IP_PROVIDER=ipapi` + `GEO_VISITOR_IP_API_KEY`。
- 缺點：district 覆蓋率官方未強調，且目前 adapter 對 IPv6 有已知缺口
  （需要小幅修改才算完整支援 Provider 本身的能力）。
- 預估費用：€13.3/月（年繳），約合 NT$450~470/月（依匯率浮動）。
- 是否需要新增 adapter：不需要（沿用既有檔案，只補 IPv6 分支）。
- 是否需要 API Key：需要。
- 是否可辨識到中壢區：**不保證**，見下方風險。
- 可能只到桃園市的比例風險：**偏高**——adapter 註解本身已誠實記載
  district 欄位「常為空字串」，多數結果預期落在 county（桃園市）層級。
- 隱私影響：單次查詢時，ip-api.com 會接收到要解析的訪客 IP；依其官方
  隱私政策短暫記憶體保留（≤1 分鐘）用於 rate-limit，不做請求記錄
  （Pro 條款精神相同，但建議簽約前再次確認 Pro 版隱私政策全文）。
- Zeabur 設定需求：`GEO_VISITOR_IP_PROVIDER=ipapi`、
  `GEO_VISITOR_IP_API_KEY=<Pro Key>`。

### 2. 最低長期成本方案 — **ip-api Pro**（與方案 1 相同）
- 在本次比較的四個路線中，ip-api Pro 的官方公開報價（€13.3/月，年繳）
  是唯一一個「查得到公開價格且明顯最低」的正式商用選項。IPinfo 最低商用
  方案（Core，$41/mo）約為 ip-api Pro 的 3 倍價格；IPdata 官方定價本輪
  未查得；MaxMind 免費層不適用本專案（B2B EULA 限制），付費層需另外議價
  （非官方估計約 US$200+/月，明顯高於前者）。
- 因此最低改動方案與最低成本方案在本輪比較下**是同一個選擇**
  （ip-api Pro），差異只在於「是否接受 district 命中率不保證」這件事。

### 3. 台灣行政區命中率最佳方案 — **沒有任何候選能保證**
- 誠實結論：**A/B/C/D 四個路線的官方文件都沒有任何一個承諾「台灣區級
  （如中壢區）行政區命中率」**。這不是本專案挑選能力不足，而是 IP 
  geolocation 這個技術本身的限制——多數商業資料庫的最小粒度是「城市」，
  台灣的鄉鎮市區這一層在大多數國際 Provider 眼中通常會被併入城市或
  完全省略，只有極少數 IP（例如已知固定屬於某個機房/ISP 節點的位址）才
  可能被標到區級。
- 若真的要提高「區」等級命中率，比較現實的做法（本輪只列出，不建議
  立即採用，因為都超出「換一個 Provider」的範圍）：
  - 接受「多數落在 county 等級」是常態，UI/Dashboard 上誠實顯示
    County Marker 而非強求 District Marker（A1.2 既有架構本來就支援
    county/district 兩級 Marker，這件事不需要新程式碼）。
  - 若未來有預算，可評估 MaxMind GeoIP2 Insights 等更高階（更貴）方案，
    但即便如此，官方文件也**不承諾**行政區級精度，只承諾「更準的城市
    定位」。
- 因此本輪建議：**維持 ip-api Pro 作為起步方案**，並在
  `normalizeTaiwanGeo()` 既有邏輯下，接受「多數新事件會落在 county
  等級」是可預期、非異常的結果，不承諾中壢區級精度。

## Privacy Comparison 摘要

| | ip-api Pro | IPinfo | IPdata | MaxMind 本地資料庫 |
|---|---|---|---|---|
| 查詢時 IP 是否送到第三方 | **是** | **是** | **是** | **否**（本地查詢） |
| 第三方留存政策 | 短暫記憶體，≤1分鐘，不記錄請求（免費端點官方政策，Pro 精神相同，建議簽約前覆核） | 未在本輪確認逐字條款 | 未查 | 不適用 |
| 適合本專案商用型態（多租戶 SaaS） | 是（Pro 方案允許商用） | 是 | 需查 | **免費層不適用**（EULA 排除 B2B 平台）；付費層需議價 |

**誠實聲明（依需求文件九）**：若最終選用任何外部 HTTP Provider
（ip-api Pro／IPinfo／IPdata），**單次查詢時，該 Provider 會接收到要
解析的訪客 IP**，這是 IP geolocation 服務本質上無法避免的事，不得宣稱
外部 Provider 看不到 IP。唯一能避免「IP 離開本專案伺服器」的路線是
MaxMind 本地資料庫方案，但該路線在部署維運成本與商用授權門檻都更高。
