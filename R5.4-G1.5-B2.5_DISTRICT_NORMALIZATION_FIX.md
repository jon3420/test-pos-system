# R5.4-G1.5-B2.5 — District Normalization Fix Report

fix18-10-hotfix30-B5-R5.4-G1.5-B2.5 — Taiwan District-to-County
Normalization Hotfix

## 1. 正式 API 回傳的兩個 unmapped 名稱

正式環境 GA4 Realtime Data Endpoint 已成功回傳 `HTTP 200`，真實 payload
的 `unmapped` 陣列含兩筆：

```
1. city: "Longtan District"   active_users: 1   event_count: 13
2. city: "Taoyuan District"   active_users: 1   event_count: 11
```

`summary.mapped_counties: 0`／`summary.unmapped_city_rows: 2`。

## 2. Longtan District

`龍潭區`（桃園市）的英文名稱。全國資料集中唯一對應此區，無歧義。

## 3. Taoyuan District

`桃園區`（桃園市）的英文名稱。**但這個英文字串在全國資料集中同時也對應
到高雄市桃源區**——詳見第 5 節。

## 4. Parent City：桃園市

兩者的上層縣市皆為桃園市（`county_code: 68000`）。

## 5. 為何不能直接 strip District 猜測

如果做 `city.replace(/ District$/, '')` 這種通用字串處理，剩下的
`Longtan`／`Taoyuan` 仍然只是字串，不保證能安全對應到縣市——而且完全繞過
了既有資料集裡「這個名稱在全國範圍內是否唯一」的稽核。本輪稽核
（見 `R5.4-G1.5-B2.5_DISTRICT_NORMALIZATION_AUDIT.md` 第 7 節）發現一個
必須誠實揭露的事實：

> **`Taoyuan District` 在全國行政區資料集中同時對應到「桃園市桃園區」與
> 「高雄市桃源區」**——桃園與桃源中文不同字、臺灣華語發音相同
> （Táoyuán），英文皆音譯為 `Taoyuan`／`Taoyuan District`，是資料集本身
> 既有的全國唯一性衝突，不是本輪新增的錯誤。

因此本輪的做法**不是**「反正 District 後面接的字串通常安全，所以全部
strip 掉猜測」，而是：

1. 建立**明確、可稽核的白名單**（`DISTRICT_PARENT_ALIASES`），只認得列在
   表裡的精確字串（正規化大小寫／空白後比對），不接受任何其他
   `"X District"` 字串。
2. 對 `Longtan District`：全國唯一，加入白名單是單純正確的判斷，沒有
   取捨。
3. 對 `Taoyuan District`：**明知它有全國唯一性衝突**，仍選擇加入白名單
   對應到桃園市，是一個**有意識、有記錄的業務決策**——理由：
   - 本白名單刻意只服務 GA4 Visitor Geo Layer 這一個「IP 推估、僅供區域
     趨勢分析、非精確定位」的功能（既有 `DISCLAIMER` 已明確告知使用者
     這個限制），不影響任何「錯誤分類會造成實際業務後果」的流程（訂單
     履行地址、外送地址解析等，見第 6 節範圍限定）。
   - 高雄市桃源區是人口約 4,000 人的山地原住民鄉，這家店的 GA4 即時流量
     來自桃源區的機率遠低於來自桃園市桃園區（市中心、人口 20 萬以上）；
     即便真的錯誤分類，影響範圍也僅限於地圖上一個縣市區塊的訪客計數
     被輕微高估，不影響任何金流、訂單、或使用者個資。
   - 如果未來 Google Realtime API 提供更細的地區欄位（例如
     `region`／`subcontinent` 或行政區代碼）可以拿來消歧，應該優先改用
     那個訊號，而不是繼續依賴這個字串層級的取捨——本文件記錄這一點，
     作為後續改進的提示。

## 6. Explicit Alias Map

新增於 `utils/taiwanGeoNormalize.js`：

```js
const DISTRICT_PARENT_ALIASES = Object.freeze({
  '桃園市': [
    'Longtan District', 'Longtan Dist.', 'Longtan', '龍潭區',
    'Taoyuan District', 'Taoyuan Dist.', '桃園區',
  ],
});
```

配合 `normalizeDistrictToParentCounty(value)`：trim 前後空白、大小寫不
敏感、連續空白（含 tab／換行）正規化成單一空白後查表；查不到就回
`null`，不猜測。

**範圍限定**（刻意的設計決策，見 Audit 文件第 8 節）：

- 這個白名單與函式是**新增、獨立**的，完全沒有修改既有
  `COUNTY_ALIASES`／`normalizeCounty()`／`resolveTaiwanAdministrativeArea()`／
  `normalizeSubdivision()`——這些既有函式的行為（含 Hsinchu／Chiayi 保護、
  雙層縣市＋行政區解析引擎）完全不受影響。
- 只有 `utils/ga4Realtime/index.js` 的 `_aggregateCityRows()` 會在
  `normalizeCounty()` 找不到時，用它當 fallback：

```js
const county = normalizedCity
  ? (normalizeCounty(normalizedCity) || normalizeDistrictToParentCounty(normalizedCity))
  : null;
```

- 沒有接入訂單履行地址解析、外送地址解析等其他呼叫
  `normalizeCounty()`／`resolveTaiwanAdministrativeArea()` 的流程，把
  「Taoyuan District 取捨」的影響範圍限定在地圖圖層。

## 7. 同縣市聚合

`_aggregateCityRows()` 既有的聚合邏輯（用 `county.county_code` 當 Map
key）不需要修改就能正確把 `Longtan District`／`Taoyuan District` 兩筆
row 聚合成同一筆桃園市 entry——只要 `normalizeCounty()` 或新的
`normalizeDistrictToParentCounty()` fallback 回傳的是同一個
`county_code`，既有的 `countyMap.has(key)` 判斷就會自動合併。已用
B2.5 Smoke C15-C22／H61-H70 實測確認：

- `active_users`：1+1=2（對應正式回報的 `total_active_users_ga4: 2`）。
- `event_count`：13+11=24。
- `counties.length === 1`（不是兩筆獨立的「桃園區」「龍潭區」縣市級
  entry——地圖層級是縣市層級，不是行政區層級）。
- `unmapped.length === 0`。
- row 出現順序（先 Longtan 或先 Taoyuan）不影響聚合結果。
- 縣市層級 row（如 `Taoyuan City`）與新行政區 row 混合出現時正確合併
  進同一筆，不產生重複 entry。

## 8. 模糊名稱保護

`Hsinchu`／`Chiayi` 裸名稱的既有保護完全不受影響——新的
`DISTRICT_PARENT_ALIASES` 不含任何 Hsinchu／Chiayi 相關字串，
`normalizeCounty('Hsinchu')`／`normalizeCounty('Chiayi')` 仍回 `null`，
`normalizeDistrictToParentCounty('Hsinchu')`／`('Chiayi')` 也回 `null`
（已用 B2.5 Smoke D23-D24b／G50-G51 實測確認）。

## 9. Smoke

`scripts/smoke-hotfix30-b5-r5-4-g1-5-b2-5-district-normalization.js`：
**76 PASS ／ 0 FAIL ／ 76 TOTAL**（要求 ≥70），連續三次一致。涵蓋
Exact Aliases、Whitespace、Aggregation、Safety、Existing Compatibility、
Frontend（真實 jsdom + 假 Leaflet）、Mutation Negative、Real-World
Payload Reproduction 八個區塊。

## 10. Regression

使用更新後的 `scripts/run-regression-b2-5.js`（在 B2.4 版本基礎上新增
B2.5 Smoke 與 `taiwanGeoNormalize.js` 的 `node --check`），依序執行 24
支既有 Smoke／Static Audit。**三輪結果完全一致，全部 PASS，FAIL=0，
exit=0**：B2.5 Smoke 76/76、B2.4 Smoke 139/139、B2.3 75/75、B2.2 95/95、
B2.1 85/85、B2 Settings 187/187、B2 Static Audit 82/82、B2a 106/106、
B1 168/168、B1 Static Audit 71/71、G1.5-A 140/140、G1.5-A Static Audit
77/77、Geo Map Settings 620/620、Geo Settings UI 157/157、G1 geo-live
212/212、G1.4.1 149/149、G1.4 148/148、G1.3.2 148/148、G1.2 83/83、A2
229/229、A1.2 189/189、Static Audit G1.4.1 56/56、G1.4 52/52、G1.3.2
48/48。額外抽查了 4 支未在正式清單但也 `require('../utils/
taiwanGeoNormalize')` 的既有 Smoke（`R5.1-D1`／`R5.2-A Integrated`／
`R5.2-A Order Geo Write`／`R5.2-A Stage 7`），確認新增的 additive-only
匯出不影響任何既有行為，全部維持原有 PASS 數。每輪前清除
`data/pos.db`；輪次結束後確認無殘留 DB。

## 11. Manual Visual：NOT TESTED

本輪修正尚未在瀏覽器中人工開啟 Settings／地圖頁面驗證視覺呈現（縣市著色、
tooltip 顯示、Summary 卡片數字），僅透過自動化 Smoke（真實函式呼叫 +
jsdom + 假 Leaflet）驗證邏輯層與 DOM 渲染函式輸出正確。

## 12. Real GA4

- Endpoint：**PASS**（使用者回報，正式環境已確認 `HTTP 200`）。
- Summary：**PASS**（`total_active_users_ga4: 2`／`event_count: 24`／
  `screen_page_views: 2` 已在正式環境取得，本輪未變更 Summary 邏輯）。
- City：**PASS**（City Request 本身成功回傳兩筆行政區 row，問題出在
  正規化，不是 City Request 失敗）。
- District Mapping：**待部署驗證**——本輪修正基於使用者提供的真實
  payload 逐字重現驗證（見 Smoke H61-H64，數字與正式回報完全吻合），
  但尚未在正式環境重新請求確認 `mapped_counties` 真的從 0 變成 1、
  `unmapped_city_rows` 真的從 2 變成 0。

---

*本文件不含任何真實憑證、Property、Stream 或 Token 內容。*
