# R5.4-G1.6-GA4-H1.4.4 — VIEW-ITEM-SEMANTICS Manual QA

前提：Production Modified = 1 file（`public/js/analytics-platforms.js`）。
`line-order.html` / `line-shipping.html` 未變更。

決策（已確認）：目前系統沒有合法的 Product Detail View Interaction，因此
GA4 `view_item` **預期為 0**，直到未來版本新增真正的商品查看互動為止。這不是
tracking 缺漏，是本輪刻意的正確行為。

## TRACK-01 — Homepage Load

1. Fresh Incognito，開啟 LINE 點餐頁面
2. 什麼都不點，等待商品卡進入視窗

**預期：** GA4 Realtime／DebugView `view_item` delta = 0

## TRACK-02 — Scroll Exposure

1. 承 TRACK-01，往下滾動頁面，讓更多商品卡進入視窗

**預期：** `view_item` 仍 = 0（不論曝光幾張卡片）

## TRACK-03 — Category Switch

1. 切換分類（全部 → 熱門 → 其他分類）
2. 不點任何商品

**預期：** `view_item` 不因換分類 rerender 而增加，仍 = 0

## TRACK-04 — Card Click

1. 點擊商品卡本體（圖片／名稱／描述任何地方）

**預期：** `view_item` 不增加，仍 = 0
（因為目前沒有 Product Detail View，這是預期行為，不是 bug）

## TRACK-05 — Add to Cart

1. 加入購物車一次

**預期：**
- `add_to_cart` +1
- `view_item` 仍 = 0

## TRACK-06 — Second Product

1. 承 TRACK-05，再加入另一個商品一次

**預期：**
- `add_to_cart` 依使用者操作次數增加
- `view_item` 仍 = 0

## TRACK-07 — 原始症狀復現檢查（Regression）

1. Fresh Incognito，新 GA4 Property
2. 只點開第一個商品一次（或完全不點，只讓畫面自然顯示多張商品卡）

**預期：** `view_item` **不會**變成 9（或任何等於畫面商品卡數量的數字）。
`view_item` 應維持 0，因為現在沒有任何 code path 會送出它。

## DebugView 優先

若 GA4 DebugView 可用，優先用它觀察 event timeline，逐一確認：
- 頁面載入、換分類、加入購物車、點擊商品卡等操作，**timeline 上完全不出現
  `view_item`**
- `add_to_cart` 事件正常出現，次數對應使用者實際點擊次數
- Realtime 作為第二層確認（Realtime 有聚合延遲，DebugView 更即時）

## 對報表的影響（務必先讀）

部署後，GA4 `view_item` 會變成 0（或維持 0）。**這是預期結果**，因為系統目前
沒有真正的商品詳情查看互動可以合法對應到這個事件。如果任何 Dashboard／報表
畫面有欄位叫「GA4 商品瀏覽」且底層抓的是 `view_item`，那個欄位會顯示 0——
**不要因為不想看到 0 而重新製造一個假的 view_item 觸發**。如果商業上需要「商品
被看到多少次」的數據，正確做法是新增 `view_item_list`（清單曝光），這是下一輪
才處理的 Future Finding，不在本輪範圍。

## 內部曝光資料（不要混淆）

Internal `view_product`（`/api/analytics/events` 記錄）仍然正常運作，記錄商品卡
進入視窗的曝光次數。這跟 GA4 `view_item` 是兩件事：`view_product` 是內部曝光統計，
`view_item` 是 GA4 標準的「使用者查看單一商品」事件。UI／報表文字如果把兩者混用
（例如把 internal 曝光數字標成「GA4 商品瀏覽」），是一個需要另外核對的 Follow-up
Finding，本輪不處理。
