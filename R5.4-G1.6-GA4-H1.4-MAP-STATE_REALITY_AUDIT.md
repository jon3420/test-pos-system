# R5.4-G1.6-GA4-H1.4-MAP-STATE — Reality Audit

Baseline: `fix18-10-hotfix30-B5-R5.4-G1.6-GA4-H1.3-EVENT-COMPAT-QA-full.zip`
方法：直接讀原始碼（`public/js/geo-intelligence.js`、`geo-intelligence-map.js`、
`geo-heatmap-ui.js`、`geo-ga4-h1-panel.js`、`geo-ga4-realtime-layer.js`、
`services/ga4GeoSyncService.js`、`routes/ga4-geo.js`、`utils/dateTime.js`），
未依賴推測。

---

## 重要前提：目前架構跟需求文件的假設不完全一致

需求文件五描述「Geo Intelligence：[儀表板][Heatmap] 兩個獨立分頁」。
實際程式碼確實有兩個分頁（`geoHeatUiRenderTabBar` / `geoHeatUiSwitchTab`,
`public/js/geo-heatmap-ui.js:85-131`），**但兩個分頁的 DOM 面板同時存在於
同一個容器內**（`#{containerId}-panel-dashboard` 與 `#{containerId}-panel-heatmap`
只是用 `hidden` 屬性互相切換顯示／隱藏，不是各自獨立掛載/卸載），而且兩者
**共用同一個 Leaflet map instance**。這個共用是刻意設計（見
`geo-heatmap-ui.js` 檔頭註解「重用 geo-intelligence-map.js 既有的同一個
Leaflet map instance，不建立第二個地圖」），不是 bug。

這代表本輪如果要做「Dashboard／Heatmap 各自 State」與「Layer Ownership
分離」，正確做法是在**同一個地圖上**做 LayerGroup 級的所有權切分，而不是
兩個地圖實例的切分（Section 二十四／二十六本身也是這樣要求的，這裡先
確認現況與需求文件的假設一致）。

---

## A. Dashboard map instance

單一全域 `geoMapState`（`geo-intelligence-map.js:350`），`window.geoMapState`
（line 377）。`geoInitMap(containerId, rows)`（line 396-441）建立
`geoMapState.instance = L.map(container)`（line 413）。

Dashboard 面板渲染時（`geo-intelligence.js` 內 Dashboard render 函式）呼叫
`geoInitMap(mapContainerId, mapRows)`，其中 `mapRows = vm.funnel.areas`
（**訂單/session 轉換率資料，不是 GA4 Historical、也不是 POS Visitor
Geo**）。這是 choropleth（`geoMapState.geoJsonLayer`），不是 marker。

## B. Heatmap map instance

**沒有獨立的 Heatmap map instance。** `_geoHeatUiEnsureMapReuse()`
（`geo-heatmap-ui.js` 約 line 133 起）直接讀 `geoMapState.instance` 並指定給
`geoHeatState.instance`，明確「重用」而非新建。

## C. 是否共用 Leaflet instance

**共用，同一個 `geoMapState.instance`。** 這是本輪 Layer Ownership／Cleanup
設計必須尊重的既有事實，不能假設兩個獨立地圖。

## D／E. Layer owner／Marker owner

同一張地圖上疊了 4 組獨立 LayerGroup，各自有自己的 owner 物件：

| Layer | Owner state | 建立位置 |
|---|---|---|
| Dashboard Choropleth | `geoMapState.geoJsonLayer` | `geo-intelligence-map.js` |
| Heatmap Order | `geoHeatState.layerGroup` | Engine（`geo-heatmap.js`，本輪不動） |
| Heatmap Visitor | `geoVisitorState.choroplethLayerGroup` | `geo-visitor-layer.js` |
| Heatmap GA4 Realtime | `geoGa4State.layerGroup` | `geo-ga4-realtime-layer.js` |
| Heatmap GA4 Historical (H1) | `geoGa4H1State.markerGroup` | `geo-ga4-h1-panel.js:181` |

三個 Heatmap 子 Layer（order/visitor/ga4-realtime）之間已經有正確的互斥
addLayer/removeLayer 邏輯：`_geoHeatUiApplyLayerExclusivity()`
（`geo-heatmap-ui.js` 約 line 528-548）。**但這個互斥函式完全不知道
H1 的 `markerGroup` 存在**，也完全不會在「離開 Heatmap 分頁」時被呼叫。

## F／G. Cleanup owner／Tab cleanup — **這就是 Root Cause**

`geoHeatUiSwitchTab(containerId, tab)`（`geo-heatmap-ui.js:96-131`）：

- 切到 `'heatmap'`：呼叫 `_geoHeatUiEnsureMapReuse()`、重新 fetch/render。
- 切到 `'dashboard'`（else 分支，line 122-123）：**只呼叫
  `_geoHeatUiRestoreChoropleth()`**。

`_geoHeatUiRestoreChoropleth()`（line 144-149）內容：

```js
function _geoHeatUiRestoreChoropleth() {
  if (typeof geoMapState === 'undefined' || !geoMapState || !geoMapState.geoJsonLayer) return;
  if (typeof geoUpdateMapData === 'function') {
    try { geoUpdateMapData(geoMapState.rows, geoMapState.metric); } catch (e) {}
  }
}
```

它只重新畫 choropleth 的顏色/資料，**完全沒有對
`geoHeatState.layerGroup` / `geoVisitorState.choroplethLayerGroup` /
`geoGa4State.layerGroup` / `geoGa4H1State.markerGroup` 呼叫
`map.removeLayer()`**。這 4 組 LayerGroup 一旦在 Heatmap 分頁被
`map.addLayer()` 加上去，切回 Dashboard 分頁後仍留在同一張地圖上。

`geoGa4H1Panel.destroy()`（`geo-ga4-h1-panel.js` 約 line 707 起）確實會清
`pollTimer` 與 `currentAbort`，**但只在「切換 Heatmap 內部的 order/
visitor/ga4 layer」時被呼叫**（`geo-heatmap-ui.js` 的
`geoHeatUiSetLayer()`），不會在「切到 Dashboard 分頁」時被呼叫。也就是
說：從 GA4 Layer 切到 Order Layer（都還在 Heatmap 分頁內）會正確清掉 H1
markerGroup；但從 Heatmap 分頁整體切到 Dashboard 分頁，不會觸發任何
Heatmap 內部 layer 的 cleanup，因為 `geoHeatUiSwitchTab()` 的
`'dashboard'` 分支根本沒有呼叫 `geoHeatUiSetLayer()` 或任何等效清理。

## H. Marker leakage root cause

**`geoHeatUiSwitchTab()` 切到 `'dashboard'` 時，不移除 Heatmap 端已經
`addLayer` 上地圖的任何 LayerGroup（order／visitor／ga4-realtime／
H1 GA4 historical marker group）。** 這 4 組都是 `L.layerGroup()` 附掛在
共用的 `geoMapState.instance` 上，DOM 面板隱藏不影響 Leaflet layer 是否
還在地圖上——`hidden` 只作用在 HTML panel（非地圖容器本身），Leaflet
overlay 不會因為外層容器被隱藏而自動移除。這就是使用者回報「紫色 GA4
Marker 仍留在 Dashboard Map」的完整成因。

## I. F5 後 marker 消失 root cause

F5／Direct Load 是整頁重新載入，所有前端 state（`geoMapState`、
`geoHeatUiState`、`geoGa4State`、`geoGa4H1State`）與 Leaflet map instance
本身全部重新建立（`geoInitMap()` 會重新 `L.map(container)`），
`geoHeatUiState.activeTab` 預設值是 `'dashboard'`（line 44），Heatmap 分頁
從未被啟用過，所以 GA4 Layer 從未 `addLayer`。這不是「修好了」，只是
「重新開始，還沒有機會犯同一個錯」。這也解釋了使用者回報「不是 DB 資料
遺失／GA4 Sync 失敗」的判斷是對的：資料庫、GA4 同步、Marker Catalog 都
沒有涉入，純粹是前端 Leaflet layer 生命週期管理缺口。

## J. 現有 Historical 支援哪些 Range

`routes/ga4-geo.js`：`ALLOWED_RANGES = new Set(['today', 'yesterday', '7d', '30d', 'custom'])`。
`GET /api/analytics/ga4-geo/history?range=...&start_date=&end_date=`。
目前**沒有** `90d`／`180d`／`this_year`／`last_year`／`single` 這些 preset。

## K. 現有 API 是否已支援 start_date/end_date

**支援。** `range=custom` 時，`svc.getRangeGeoStats(storeId, range, start_date, end_date)`
→ `resolveRangeWindow('custom', customStart, customEnd)`
（`services/ga4GeoSyncService.js:98-111`）直接驗證並使用
`customStart`/`customEnd` 兩個 `YYYY-MM-DD` 字串，格式驗證用
`/^\d{4}-\d{2}-\d{2}$/`。這與需求文件十二／十三的假設一致：新 preset
可以只在前端 resolve 成 `start_date`/`end_date`，再打既有的
`range=custom` 路徑，不需要新 endpoint。

## L. 現有 Custom Range 是否已能完成長區間 Query — **發現一個真實限制，需求文件沒有預期到**

`services/ga4GeoSyncService.js:26`：

```js
const CUSTOM_RANGE_MAX_DAYS = 92; // 需求文件十二：自訂日期最大範圍限制
```

`resolveRangeWindow()` line 108-110：`span > CUSTOM_RANGE_MAX_DAYS` 時回傳
`{ ok: false, code: 'range_too_large' }`。

**這會直接擋掉 `180d`（180 天）、`今年`（最多約 365 天）、`去年`（365 天）
這三個新 preset**，因為它們 resolve 出來的 `start_date`~`end_date` 天數
（inclusive）遠超過 92 天上限，會被現有 API 直接拒絕，回應
`range_too_large`。`90d`（90 天）本身在 92 天上限之內，不受影響；
`single`／`today`／`yesterday`／`7d`／`30d` 也都不受影響。

這與需求文件四十「若現有 Backend Range Resolver 需要增加 preset，只能做
最小擴充」的允許範圍相容：**把 `CUSTOM_RANGE_MAX_DAYS` 從 92 調整為至少
366（涵蓋閏年）是本輪唯一必要的 backend 數值調整**，不需要新增
endpoint、不需要重寫 Historical Query Architecture、不需要新的 GA4 Query
family，純粹是一個常數邊界值的調整。若不調整，180d／今年／去年 三個
UI 選項會呈現的每一次查詢都失敗，屬於功能等於沒做。

（此發現需要在 Implementation Report 中明確記錄，並在 Static Audit /
Range Runtime 測試中加入「90d 通過、180d／this_year／last_year 在調整前
會被 `range_too_large` 擋下、調整後應通過」的對照測試，避免下一輪誤判為
「既有 API bug」。）

---

## 結論：本輪必須做的變更範圍（供 Implementation Report 使用，本文件本身不做修改）

1. **前端 Tab Cleanup（Section 二十五／二十六的核心）**：`geoHeatUiSwitchTab()`
   切到 `'dashboard'` 時，需要在 `_geoHeatUiRestoreChoropleth()` 之前（或
   同一個函式內）明確 `map.removeLayer()` 這 4 組 LayerGroup：
   `geoHeatState.layerGroup`、`geoVisitorState.choroplethLayerGroup`、
   `geoGa4State.layerGroup`、`geoGa4H1State.markerGroup`，並呼叫既有
   `geoGa4Deactivate()` / `GeoGa4H1Panel.destroy()` 停 timer／abort 掛起的
   request（這兩個函式已存在、已被 Heatmap 內部 layer 切換使用，只是
   沒有被「切到 Dashboard 分頁」這條路徑呼叫）。
2. **新增 Dashboard 自己的 GA4 Historical Layer/LayerGroup**：目前 Dashboard
   完全沒有 GA4 marker，只有 order-funnel choropleth。需要新增
   `dashboardGa4LayerGroup`（獨立於上面 4 組），由 Dashboard 自己的
   state／range selector 驅動，讀 `GET /api/analytics/ga4-geo/history`。
3. **Backend 唯一必要調整**：`CUSTOM_RANGE_MAX_DAYS: 92 → 366`（含註解更新），
   不新增 endpoint、不動 `resolveRangeWindow()` 其他邏輯。
4. **Range Resolver（前端新檔案）**：把 `single`／`90d`／`180d`／
   `this_year`／`last_year` 全部 resolve 成 `{ startDate, endDate }`，
   全部走 `range=custom`；`today`/`yesterday`/`7d`/`30d` 維持走既有的
   對應 preset 值（不必也轉成 custom，除非為了一致性，但非必要不動）。
   一律重用 `utils/dateTime.js` 的 `getTaipeiCalendarDateString()`
   （已存在、已支援 `offsetDays`，見該檔案 line ~140 起），不建立第二套
   timezone 邏輯——前端若需要同等函式，需確認 `public/js/date-time-format.js`
   （檔頭註解提到的「瀏覽器端獨立但等價實作」）是否已有對應函式可重用，
   避免前端另外寫一套。
5. **Dashboard／Heatmap Range State 分離**：新增 `dashboardGeoState`
   （獨立物件），不得與 `geoHeatUiState.visitorRange` 或
   `geoGa4State`/`geoGa4H1State` 內部欄位共用同一個變數。

本文件到此為止，僅記錄現況，不包含任何程式碼修改。

---

# 追加：Final Implementation Findings（Stage 1～7C 完成後）

以下內容是後續多輪 Stage 1～7C 實作完成後的追加記錄。**上面的原始
Root Cause Audit（章節一～五）保留原樣，沒有被覆寫或刪除。**

## Implementation Findings 摘要

- **Map Instance 共用是刻意設計**：Dashboard／Heatmap 全程共用同一個
  `geoMapState.instance`，本輪沒有建立第二張地圖。
- **原始 Bug 是 Layer Cleanup 缺失**：`geoHeatUiSwitchTab(..., 'dashboard')`
  過去只 `_geoHeatUiRestoreChoropleth()`，從未移除 Heatmap 端的 4 組
  LayerGroup。已新增 `_geoHeatUiCleanupForDashboard()` 集中處理。
- **F5 marker 消失只是 state reset**，不是修復——資料庫／GA4 同步／Marker
  Catalog 全程沒有涉入。
- **Dashboard 原本是 `vm.funnel.areas` choropleth**，完全沒有 GA4 marker；
  GA4 Historical Overlay 是本輪新增的 New View Capability，choropleth 保留
  不變。
- **CUSTOM_RANGE_MAX_DAYS：92 → 365 span**，語意是「最多 366 個 inclusive
  calendar days」，不是字面對稱的 366。
- **90d/180d/single/custom/this_year/last_year 的 Persistence Identity 用
  真正的 `range_start_date`/`range_end_date`**（Stage 4.2 用真實 SQLite 驗證，
  沒有新增 `range_key` 這種粗粒度標籤，沒有 DB Schema 變更）。
- **Manual Sync Late Completion Leakage**（Stage 6 發現）：POST /sync 沒有
  AbortController，`destroy()` 無法取消它，晚完成的 Sync 會 resurrect
  `markerGroup` 到共用地圖。修法：`destroyed` 旗標（第一層）+
  `lifecycleGeneration`（第二層，解決 destroy→init 的 ABA 風險）。
- **Browser Entry Wiring Gap**（Production Diff review 發現，詳見下方
  獨立一節）：三個新檔案原本沒有被 `public/index.html` 載入，已修正。

## Browser Entry Wiring Finding（獨立記錄）

**分類：REAL PRODUCTION INTEGRATION GAP。**

Pre-Browser-Entry 階段（Stage 1～7A）：JS Module Runtime、Map State
Runtime、Mutation、Static Audit（當時 216/216）全部可以 PASS，因為這些
測試全部是用 jsdom `eval()` 手動組裝依賴順序，或 Node `require()`
fallback 驗證 Production JS 本身，從未真正解析 `public/index.html`。

Production Diff review 第一次直接對 `public/index.html` 做 `diff -rq`
時發現：`public/js/geo-range-resolver.js`、`public/js/geo-range-control.js`、
`public/js/geo-ga4-dashboard-layer.js` 三個 H1.4 新檔案，從未被加進正式
頁面的 `<script>` 載入清單。這三個檔案的頂層程式碼會在解析當下用
`typeof resolveGeoHistoricalRange === 'function'` 這類檢查決定要不要走
Node `require` fallback——這個 fallback 只在 Node 測試環境有效，真實
瀏覽器沒有 `require`，也沒有這三支腳本。**結論：在修正之前，真實部署頁面
上 H1.4 的 Range Resolver／Range Control／Dashboard GA4 三項功能完全不會
被啟用。**

修正：
1. `public/index.html` 插入正確順序的三個 `<script>` 標籤。
2. 新增 `scripts/run-g1-6-ga4-h1-4-browser-entry-runtime.js`（24/24 PASS
   ×3）：直接解析真實 `<script src>` 順序，依序 eval 進 jsdom window（不
   提供 Node require），驗證 4 個全域符號存在可用，含 4 個 Negative
   Control。
3. `scripts/static-audit-g1-6-ga4-h1-4.js` 新增 Q1-Q11，直接讀
   `public/index.html` 字串位置比對（216/216 → **227/227**）。
4. Full Regression 從 56 suites → **57 suites**。

歷史數字如實保留，沒有覆寫：

| 階段 | Static | Full Regression |
|---|---|---|
| Pre-Browser-Entry | 216/216 | 56 suites ×3 |
| Post-Browser-Entry（Final） | 227/227 | 57 suites ×3 |

## Final Automated Gates

| Gate | 結果 |
|---|---|
| Dashboard Direct Load | PASS |
| Heatmap → Dashboard Cleanup | PASS |
| Dashboard → Heatmap Cleanup | PASS |
| Reload Determinism | PASS |
| Extended Range | PASS |
| Single Day | PASS |
| Persisted Range Isolation | PASS |
| Manual Sync Lifecycle ABA | PASS |
| Browser Entry Wiring | PASS |
| Mutation | PASS |
| Static | PASS |
| Full Regression | PASS |

## Production Gate（部署前語言，不寫 Production PASS）

- Dashboard GA4 Production：**READY FOR MANUAL DEPLOYMENT VERIFICATION**
- Map State Production：**READY FOR MANUAL DEPLOYMENT VERIFICATION**
- Browser Entry Production：**READY FOR MANUAL DEPLOYMENT VERIFICATION**
- Extended Range Production：**READY FOR MANUAL DEPLOYMENT VERIFICATION**
- Single Day Production：**READY FOR MANUAL DEPLOYMENT VERIFICATION**
- Lifecycle ABA Production：**READY FOR MANUAL DEPLOYMENT VERIFICATION**

以上全部要等實際部署到 Zeabur 並完成 Manual QA Checklist 後，才可以升級
成「Production PASS」。本文件與 Automated Gate 只代表自動化測試綠燈，不
代表 Production 已驗證。
