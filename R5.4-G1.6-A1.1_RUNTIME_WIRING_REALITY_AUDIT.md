# R5.4-G1.6-A1.1 — Runtime Wiring Reality Audit

fix18-10-hotfix30-B5-R5.4-G1.6-A1.1 — Geo Marker Runtime Wiring & Safe
Centroid Source Closure

## 1. 原 A1 Gate 為何誤判

`R5.4-G1.6-A1_COMPLETION_REPORT.md` 宣稱 Gate PASS，但實際稽核 ZIP 後發現
三個關鍵缺口全部成立：

1. `public/index.html` 從未載入 `public/js/geo-marker-renderer.js`。
2. 正式產品程式碼裡沒有任何地方呼叫 `geoVisitorRenderMarkers()` 或
   `GeoLiveLayer.renderEstimateMarkers()`——唯一的呼叫端是 A1 自己新增的
   Smoke Test。
3. A1 Smoke 的 50 個斷言全部是「直接呼叫函式驗證行為」，沒有一個是「模擬
   使用者操作，確認正式頁面會自己觸發呼叫」。

換句話說，A1 交付的是一個**設計良好、單元測試完整、但完全沒有接上正式
頁面的 Renderer API**——這是 Prototype，不是 Runtime Integration。

## 2. Renderer 原本未載入 HTML

已用文字搜尋確認（修正前）：`grep geo-marker-renderer public/index.html`
無結果。`geo-visitor-layer.js`／`geo-live-layer.js` 依賴的
`geoMarkerRenderGroup`／`geoMarkerClearGroup` 在瀏覽器裡因為載入順序
不存在，兩個檔案的 `typeof geoMarkerRenderGroup === 'function'` 防禦判斷
會是 `false`，`renderEstimateMarkers()`／`geoVisitorRenderMarkers()` 一律
走 no-op fallback（`{ drawn: 0, skipped: N }`），不會拋錯，但也永遠不會
畫出任何 Marker——這是「安靜失敗」，比報錯更難被發現。

## 3. 原本無 Production Caller

`grep -n "geoVisitorRenderMarkers\|renderEstimateMarkers"
public/js/*.js scripts/*.js`（修正前）只在
`geo-visitor-layer.js`／`geo-live-layer.js` 的函式**定義**本身，以及
`scripts/smoke-hotfix30-b5-r5-4-g1-6-a1-marker-unification.js` 出現。
`geoVisitorFetchAndRender()`（Visitor Layer 真正的 fetch-and-render
pipeline）與 `refresh()`（Dashboard 真正的 fetch-and-render pipeline）
都完全沒有呼叫這兩個新函式。

## 4. Smoke 只驗證直接呼叫

原 A1 Smoke 的 Section B（B19-B30）全部是這種模式：

```js
const result = window.geoVisitorRenderMarkers(fakeMapInstance, [...]);
assert(result.drawn === 1, ...);
```

這證明「函式本身邏輯正確」，但完全沒有證明「使用者切換 Tab／點擊 Metric
按鈕／頁面第一次載入資料後，這個函式真的會被呼叫」。

## 5. 目前 HTML／Namespace／Caller 修正

本輪（A1.1）已完成：

- `public/index.html` 新增 `<script src="/js/geo-marker-renderer.js?v=
  fix18-10-hotfix30-B5-R5-4-G1-6-A1-1"></script>`，位置在
  `geo-intelligence-map.js` 之後、`geo-visitor-layer.js`／
  `geo-live-layer.js` 之前，只出現一次。
- `geo-marker-renderer.js` 新增明確 `window.GeoMarkerRenderer = {...}`
  namespace；`geo-visitor-layer.js`／`geo-live-layer.js` 都改成優先透過
  `window.GeoMarkerRenderer.renderGroup()`／`.clearGroup()` 呼叫，只有
  Node/無 `window` 環境才 fallback 用裸函式全域名稱。
- `geoVisitorFetchAndRender()` 內，在 Choropleth 渲染之後，新增呼叫
  `geoVisitorRenderMarkers(window.geoMapState.instance,
  geoVisitorBuildMarkerPoints(geoVisitorState.areas))`——這是每次 Visitor
  Layer 資料成功取得、真正 render 到畫面時都會執行的路徑（activate／
  refresh／metric switch／range switch 都會呼叫到
  `geoVisitorFetchAndRender()`）。
- `refresh()`（Dashboard）在 `markers`／`cluster` 模式的資料成功取得後，
  新增呼叫 `renderEstimateMarkers(deriveEstimateMarkerPoints(points))`，
  跟既有 `_renderMarkers(points, ...)` 同一次 `refresh()` 呼叫、同一批
  資料。
- Lifecycle：`_clearActiveLayers()`（mode switch／destroy 都會呼叫）新增
  呼叫 `clearEstimateMarkers()`；`geoVisitorHandleStoreSwitch()`／
  `_geoVisitorResetStateForTest()` 新增清空 `markerLayerGroup`。

## 6. Centroid Source Blocker

見第 7-8 節的具體證據。結論：**Repo 內沒有任何 Authoritative Centroid
Source**，`_geoVisitorResolveAreaCentroid()`／
`_resolvePointCentroid()`（Dashboard 端）明確設計為一律回傳 `null`
（`AUTHORITATIVE_CENTROID_SOURCE = null` / `DASHBOARD_
AUTHORITATIVE_CENTROID_SOURCE = null`），並在程式碼註解中記錄原因與後續
擴充方式。

## 7. 矩形 Fixture 證據

實際解析 GeoJSON 檔案內容（Python `json.load`）確認：

- `public/data/geo/taiwan/taoyuan-districts.geojson`
- `public/data/geo/taiwan-districts.geojson`

兩者皆為 13 筆 feature，每筆 geometry 座標為
`[[121.2,24.93],[121.25,24.93],[121.25,24.98],[121.2,24.98],[121.2,24.93]]`
這種**軸對齊矩形**（四個角點兩兩共用經緯度，不是任何真實行政區的不規則
邊界形狀），且不同行政區彼此座標範圍疑似只是平移，屬於測試用 fixture，
不是官方測繪資料。

## 8. taiwan-administrative-areas.json 無座標

實際檢查資料集結構（Python）確認：`d['counties'][0]` 與
`d['subdivisions'][0]` 皆只有 `county_code`／`county_name`／
`county_name_en`／`subdivision_code`／`subdivision_name`／
`subdivision_name_en`／`subdivision_type`／`aliases` 欄位，**完全沒有
`lat`／`lng`／`centroid` 任何座標相關欄位**。這是純粹的代碼／名稱對照表，
從資料集本身無法算出任何座標。

## 9. Backend marker API 過濾無座標 rows

`utils/geoVisitLog.js` 的 `getGeoLiveMarkerPoints()`：

```js
const coord = (r.visitor_id && byVisitor.get(r.visitor_id))
  || (r.session_id && bySession.get(r.session_id))
  || null;
if (!coord) continue; // 沒有真實座標 → 不畫 Marker，直接跳過
```

任何沒有真實座標（`geo_live_coordinates` 表沒有對應記錄）的訪客事件，在
SQL／JS 過濾階段就被排除，連 `city`／`district` 欄位都不會送到前端。這是
**獨立於 Centroid Source Blocker 之外的第二個 Blocker**——即使前端有安全
的 centroid 資料源，Dashboard 目前的 API 回應本身也不包含「只有
district、沒有座標」的列可以拿來畫 Estimate Marker。

## 10. 為何不能直接解除過濾

`if (!coord) continue` 這行過濾對 Exact Marker 是正確、必要的行為（本來就
不該為沒有真實座標的訪客畫出精確位置 Marker）。但即使移除這個過濾，讓
`city`／`district`-only 的列也送到前端，前端仍然沒有辦法把
`city`／`district` 換算成合法座標（見第 6-8 節），所以移除過濾本身不會讓
Estimate Marker 真的能畫出來，只會讓 API 回應多出一堆「有 city／district
但畫不出來」的資料，徒增前端處理複雜度，卻不解決根本問題。因此本輪刻意
不修改這個過濾邏輯（也符合本輪範圍限定：不修改後端資料查詢邏輯）。

## 11. Runtime 安全降級

已確認的降級鏈：

1. 有真實 `lat`／`lng`（Exact）→ 正常畫 Exact Marker（既有邏輯，未受影響）。
2. 只有 `county`／`district`，呼叫 centroid resolver → 目前一律回傳
   `null`（Blocked）→ `geoMarkerBuildPoints()` 安全過濾掉，不畫、不猜、
   不報錯。
3. Summary／Ranking／Coverage 卡片繼續正常顯示 Known／Unknown 數字
   （不受 Marker Blocked 影響）。
4. UI 顯示安全說明文案（`geoMarkerBuildBlockedNoticeHtml()`）：「已取得
   區域統計，但目前缺少可驗證的區域中心資料，因此未顯示推估標註。」——
   不使用「沒有地區資料」這種會誤導使用者以為完全沒有統計資料的說法。

## 12. Gate 判定

- **A1.1 Runtime Wiring**：PASS（見 `R5.4-G1.6-A1.1_RUNTIME_WIRING_FIX.md`
  完整清單與 Smoke／Regression 證據）。
- **Estimate Marker Data Source**：**BLOCKED — Missing Authoritative
  Centroid Source and Dashboard Region-only Payload**。
- **G1.6-A2 Gate**：**BLOCKED**（在取得合法 Centroid Source 或後端提供
  region-only payload 之前，不允許開始 Visitor Geo Attribution Pipeline
  的下一階段工作）。

---

*本文件不含任何真實憑證、Property、Stream 或 Token 內容。*
