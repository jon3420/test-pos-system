# H1.4.10 Phase 1 — LIFF 核心會員歸戶 Manual QA

以下項目需要真實 LINE App / 真實 LIFF App / 真機瀏覽器才能驗證，無法在 sandbox 內自動化。

## 前置設定

1. 後台（或直接 `PUT /api/settings`）設定：
   - `line_member_gate_enabled = 0`
   - `line_member_auto_identify_enabled = 1`
   - `line_member_gate_mode = disabled`
   - `line_member_liff_id = 2010718887-jtVUEHJZ`（線上點餐，本店實際值，僅供人工設定參考，程式碼未寫死）
   - `line_shipping_liff_id = 2010718887-K37rXUUs`（冷藏宅配，本店實際值）
   - `line_member_login_channel_id`：對應的 LINE Login Channel ID

## A. LINE 官方帳號圖文選單進入（LIFF Client 內、已登入）

**操作**：從 LINE 官方帳號圖文選單點擊連結，開啟 `https://liff.line.me/2010718887-jtVUEHJZ`

**預期**：
- [ ] 不跳出登入 Modal
- [ ] 不要求輸入 LINE 帳號密碼
- [ ] 頁面在背景自動完成會員辨識（無感）
- [ ] 可以正常瀏覽商品、加入購物車
- [ ] 加入第一項商品後（cart_id 建立），稍後在後台會員詳情頁可看到該會員已與這次的 visitor/cart 關聯（`line_member_sessions` 有對應紀錄）
- [ ] 完成下單後，該 LINE 會員的訂單數 / 累積消費 / LTV / 首次購買（若是第一筆）正確增加
- [ ] 瀏覽器開發者工具 Network 分頁確認：`POST /api/line-member/verify` 的 request/response 均無完整 Access Token／ID Token 外洩到別處（僅這一次呼叫本身帶 Token 是預期行為，重點是不落地到 localStorage／URL／其他 log）

## B. 一般瀏覽器（Chrome）直接開啟網址

**操作**：Chrome 開啟 `https://<你的網域>/line-order.html?store_id=store_001`（非 LIFF URL）

**預期**：
- [ ] 不會取得任何 LINE 身分（`_currentMemberSession()` 全程為 `null`）
- [ ] 完全匿名（`visitor_id`／`session_id`／`cart_id` 正常運作）
- [ ] 可以正常加入購物車、按「前往結帳」（`checkout_click` 正常送出）、完成下單
- [ ] Network 分頁確認全程沒有呼叫 `/api/line-member/verify` 或 `/api/line-member/link-context`（因為 `isInClient()` 一定是 false，`tryPassiveLiffIdentification` 應在條件判斷就 return，不送任何 HTTP request）

## C. Facebook / Instagram 應用內建瀏覽器入口

**操作**：從 Facebook／Instagram App 內建瀏覽器點擊廣告或貼文連結進入點餐頁

**預期**：
- [ ] 不強迫 LINE Login（沒有跳出任何要求登入的畫面）
- [ ] 維持匿名，可正常加購、`checkout_click`、下單
- [ ] 若同時有 `line_member_gate_enabled=1`（非本輪預設建議設定，但需確認舊有行為不受影響）：既有的「外部瀏覽器登入引導」（hotfix26-I）行為應完全不變

## D. `line_member_auto_identify_enabled = 0`（關閉狀態）

**操作**：後台關閉 Auto Identify，其餘設定不變，從 LIFF URL 進入

**預期**：
- [ ] 完全不做被動辨識（`tryPassiveLiffIdentification` 因 `auto_identify_disabled` 直接 return，不呼叫 LIFF SDK 相關 API、不打 `/verify`）
- [ ] 若 `gate_enabled` 也是關閉狀態，則此時應完全不初始化 LIFF SDK（`shouldInitLiff` 三個條件皆為 false）

## E.（新增）冷藏宅配頁 LIFF ID 分離

**操作**：
1. 只設定 `line_shipping_liff_id`，確認 `line-shipping.html` 使用這個 ID 初始化 LIFF（非 `line_member_liff_id`）
2. 清空 `line_shipping_liff_id`，確認自動 fallback 回 `line_member_liff_id`

**預期**：
- [ ] 兩種設定情境下，`line-shipping.html` 的 LIFF 初始化都使用正確的 LIFF ID（可透過瀏覽器開發者工具檢查 `liff.init({liffId: ...})` 呼叫參數，或觀察 LINE Developers Console 的 LIFF App 存取記錄）

## F.（新增）`skipLoginCallback` 契約不影響既有 Gate

**操作**：
1. 設定 `line_member_gate_enabled=1, gate_mode=checkout`（不啟用 auto_identify），從「加入好友引導頁」實際按下「使用 LINE 登入」完成一次真實 LIFF OAuth 導頁流程並返回
2. 設定 `gate_mode=entry`，重複一次

**預期**：
- [ ] 兩種情境下，返回頁面後應自動完成驗證並打開對應畫面（hotfix25 既有「登入返回自動恢復」體驗），**與 Phase 1 修改前完全一致**
- [ ] Network 分頁確認 `/verify` 呼叫的 `analytics.gate_stage` 為 `callback`（不是 `liff_auto_identify`）——這是既有行為，Phase 1 刻意保留不變

---

## 尚待項目（Phase 2 之後）

- 後台「由 LINE LIFF 進入時自動辨識會員」可視化開關 UI（目前僅資料層支援）
- `friend_entry` / `friend_checkout` 加好友引導 UI
- Cart / Checkout / Payment 未完成找回（cart recovery jobs、n8n、LINE Push）
- CRM 找回同意（`cart_recovery_consents`）與 Dashboard 找回區塊
