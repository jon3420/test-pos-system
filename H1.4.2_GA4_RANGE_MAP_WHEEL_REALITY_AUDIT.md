# H1.4.2_GA4_RANGE_MAP_WHEEL_REALITY_AUDIT.md

## 問題 1：Dashboard GA4 Range → Map

### 結論（先講答案，細節見下）

**Root Cause 分類：[B] Dashboard Persisted Data UX Gap。不是 [A] identity mismatch，不是 Range wiring bug。**

依照第二輪指示的六個判定問題逐一回答：

| # | 問題 | 答案 | 證據 |
|---|------|------|------|
| A | 按 Range 是否真的 onChange？ | 是 | `run-g1-6-ga4-h1-4-2-range-map-wheel-runtime.js` Category A：10 種 mode 逐一點擊，`dashboardGa4State.rangeState.mode`／`lastResolved.mode`／按鈕 `aria-pressed`／label 五者同步更新，全部 PASS。 |
| B | onChange 是否真的呼叫 `geoDashboardGa4Refresh`？ | 是 | 同上；Category B 額外證明每次呼叫後 marker 集合確實替換（today→7d→30d 逐步清空舊 marker）。 |
| C | Dashboard GET 是否真的帶正確 range/start/end？ | 是 | `run-g1-6-ga4-h1-4-2-persisted-identity-runtime.js`：對全部 10 種 mode，把 `resolveGeoHistoricalRange()` 的輸出直接餵給真正的 `_geoDashboardGa4Fetch()` 呼叫路徑，query string 與 Manual Sync 送出的 body 欄位（`apiRange`/`startDate`/`endDate`）逐一核對一致。 |
| D | 正式 persisted store 是否存在該 identity？ | **視情況——這正是 Gap 的核心**：如果使用者從未對該 range 執行過 Manual Sync，答案是「不存在」，這是預期行為（GET-only Contract），不是 bug。 |
| E | 如果不存在，原因是「沒有同步過」還是「identity mismatch」？ | **沒有同步過**，不是 mismatch。 | 見下方 Persisted Identity Round-Trip 結果：44/44 PASS，10 種 mode 的 Sync 寫入 identity 與 Dashboard GET 讀取 identity 逐一比對完全一致，包含跨 mode 交叉驗證（today 不會讀到 yesterday 的資料）與「從未同步過的 range 讀回合法空陣列」。 |
| F | Dashboard 沒 marker 究竟是 render bug 還是 persisted data missing？ | **persisted data missing**，不是 render bug。 | Category B/C 已證明「有資料時 render 正確、清除正確」；`GAP-1` 額外證明「沒資料時讀回的是合法空陣列，不是 render 出錯」。 |

### Persisted Identity Round-Trip（用真正的 service + 真正 SQLite temp DB，不是 fixture mock）

`scripts/run-g1-6-ga4-h1-4-2-persisted-identity-runtime.js`：

- 對 `today`／`yesterday`／`7d`／`30d`／`90d`／`180d`／`this_year`／`last_year`／`single`／`custom` 十種 mode，各自：
  1. 用前端真正的 `resolveGeoHistoricalRange(mode)` 算出 `{ apiRange, startDate, endDate }`。
  2. 用這組值呼叫真正的 `svc.syncGeoRangeStats()`（跟 `geo-ga4-h1-panel.js` 既有 Manual Sync handler、以及本輪新增的 `geoDashboardGa4SyncNow()` 送出的 body 欄位完全一致）寫入真正的 SQLite temp DB。
  3. 用**同一組** `{ apiRange, startDate, endDate }` 呼叫真正的 `svc.getRangeGeoStats()`（跟 `routes/ga4-geo.js` 的 `GET /history` 呼叫方式一致）讀回。
  4. 斷言讀到的正是剛剛寫入的那一筆（不是別的 mode 的殘留、不是空的）。
- 額外驗證：today／yesterday 的 identity 彼此不同、不會互相覆蓋；`custom` 一個從未同步過的日期區間讀回合法空陣列（不是 error、不是 stale rows）。
- 結果：**44/44 PASS**，重複執行 3 輪皆為 44/44（無 flaky）。

關鍵背景（`resolveRangeWindow()`, `services/ga4GeoSyncService.js`）：

- `today`／`yesterday`／`7d`／`30d` 四個 preset：Backend 完全不信任前端傳來的 `start_date`/`end_date`，用自己的 server clock 重新計算 window（`_todayDateString()`），Sync 與 GET 呼叫的是**同一個** `resolveRangeWindow()`，天生一致。
- `90d`／`180d`／`this_year`／`last_year`／`single`／`custom`：Backend 統一走 `apiRange='custom'`，直接信任前端傳來的 `start_date`/`end_date` 作為 identity 的一部分（`WHERE range_start_date=? AND range_end_date=?`）。這組 Contract 是 H1.4-MAP-STATE 階段已經證明過的（`R5.4-G1.6-GA4-H1.4-MAP-STATE_REALITY_AUDIT.md`，本輪額外對 `today`/`yesterday`/`30d`/`this_year`/`last_year` 補了明確覆蓋，之前只驗證過 `7d`/`90d`/`180d`/單一 custom）。

### 為什麼正式部署會看到「按日期地圖沒有反應」

不是 wiring 斷掉、不是 identity 算錯，而是：

1. Dashboard 是 **GET-only** Contract（H1.4 就已經定案，本輪不改）：點 range button 只讀「已經同步過」的 persisted 資料，不會自動打 Google。
2. 使用者從未對「這個特定的日期區間」執行過 Manual Sync（例如只同步過 7d，沒同步過 30d/90d）。
3. H1.4.1 的行為是：清掉舊 marker（正確），但只顯示一句「請至 Heatmap → GA4 區域分析執行手動同步」純文字——**從老闆／使用者的視角看起來像「按了沒反應」**，因為畫面確實變了（標題換了、marker 消失了），但變化很不明顯，而且要求的後續動作（切 Heatmap 找同步按鈕）太繞，很容易被解讀成「壞了」而不是「還沒同步」。

### 本輪修法：Dashboard Sync CTA（見下方 Implementation）

不動 Range wiring（已證明是對的），只補 UX Gap：Dashboard 自己提供「立即同步並顯示」按鈕，內部呼叫既有 Manual Sync pipeline，成功後自動重新 GET/render，使用者完全不需要離開 Dashboard。

### Sync Pending / Concurrency Finding（第三輪 Audit 要求）

**既有 Backend Contract（`routes/ga4-geo.js`）：**

```js
const _lastManualSync = new Map();          // key = req.storeId（不分 range）
const MANUAL_SYNC_MIN_INTERVAL_MS = 5000;   // 同一店 5 秒內只能成功送出一次 Manual Sync
```

這是 **per-store 的節流窗**，不是 per-identity 的 mutex，也不是「不同 range 可以安全並行 sync」的 Contract。同一家店在 5 秒節流窗內，不論送的是 `today`／`7d`／`30d` 哪一個 range，第二支請求一律被 Backend 擋下（`429 rate_limited`）。這是既有 H1 架構就有的行為，本輪完全沒有碰 Backend。

**H1.4.2 前端因應此既有 Contract，採用的最終行為：**

1. **UI Pending 顯示（identity-keyed）**：`dashboardGa4State.syncPendingKey` 記錄「目前是哪一個 range identity 在飛行」。使用者在 7d 同步中切到 30d，30d 畫面顯示正常的「尚未同步＋可點擊 CTA」，不會被誤標成「同步中…」（那是 7d 自己的狀態，不該外溢到 30d）。
2. **實際送出 Request（single-flight，非 identity-scoped）**：同一時間全域最多一個 Manual Sync request 在飛行，跟既有 Backend 5 秒節流窗的精神一致。如果使用者在 7d 還在飛行時真的按下 30d 的 CTA，前端不送出第二支 request（送了也大機率被 429 擋掉），直接顯示「另一個區間正在同步中，請稍候再試」，等 7d 完成（成功或失敗）釋放 pending slot 後，30d 才能自己送出。
3. **Backend 429 `rate_limited` 的正確呈現**：萬一前端的 single-flight 判斷因為某種競速仍讓兩支 request 幾乎同時送出、其中一支被 Backend 429 擋下，這個 code 會被對應到跟「本地就攔下來」相同的合法 busy 文案，不會被誤判為一般網路錯誤或帳號問題。

**為什麼這樣不會造成 stale UI 或 API storm：**

- 不會 stale：pending 完成時一律先比對「目前選的 range identity 是否還等於這次同步送出時的 identity」（`_geoDashboardGa4RangesEqual`），不相等就靜默結束，不會把畫面／標題／marker 切回使用者已經離開的舊 range。
- 不會 API storm：全域 single-flight 保證同一時間最多一個 sync 在飛行；同一 identity 重複點擊被 dedupe；不同 identity 在別人飛行中點擊不送出新 request，只顯示 busy 訊息。三層防護對應到 `run-g1-6-ga4-h1-4-2-range-map-wheel-runtime.js` Category Q（UI 顯示 identity-keyed）與 Category R（Backend 真的 429 時的呈現）。

---

## 問題 2：Heatmap Wheel UX（延續前一輪，方向已確認）

H1.4.1 的 `geoHeatUiSwitchTab()` 切到 `heatmap` 分支呼叫 `geoDashboardMapDeactivateForHeatmap()`，該函式會強制 `scrollWheelZoom.enable()`。這不是 bug，是 H1.4.1 當時的 Intentional Contract（Heatmap 分析頁滾輪永遠可用）。本輪使用後確認需要改成跟 Dashboard 完全一致的 click-to-activate。

修法：`geoHeatUiSwitchTab()` 的 `heatmap` 分支不再呼叫 `geoDashboardMapDeactivateForHeatmap()`，改成呼叫既有的 `geoDashboardMapActivate(mapContainerId)`——因為 Dashboard／Heatmap 共用同一個 Leaflet map instance、同一個 `mapContainerId`、同一個 `-wheel-hint` DOM 節點，重用同一套 bind/enable/disable lifecycle 即可，不需要另建一套 Heatmap 專屬 state。`geoDashboardMapDeactivateForHeatmap()` 函式本體保留（未刪除，避免任何外部直接 import 它時整個炸掉），但已無任何 production call site。

Issue 2 分類：**POST-DEPLOYMENT UX CONTRACT CHANGE**，不是 H1.4.1 defect——H1.4.1 當時的決定是「Heatmap 分析頁滾輪永遠可用」，本輪使用後確認需要跟 Dashboard 統一。

## Production Bug #2：Map Wheel Lifecycle Listener DOM Element Identity（真實 Production Bug，不是 test migration）

**分類：REAL PRODUCTION BUG.**

### Root Cause

`geoDashboardMapBindWheelLifecycle()` 原本的 idempotent 判斷只依賴 `dashboardMapInteractionState.mapContainerId`（一個字串），從未追蹤實際綁定的 DOM element 本身。

`refreshGeoDashboardKpiBlock()`（既有、每次 Dashboard 重新整理都會呼叫的函式）會用同一個 `${containerId}-map` id 整段 `innerHTML` 重建 map container——**id 相同，但是全新的 DOM 節點物件**。

H1.4.1 時這個裂縫被「Heatmap 每次 activate 都會先 unbind、Dashboard 每次重新 activate 都會重新 bind」的既有流程意外蓋住：只要使用者切一次 Heatmap 再切回來，就會強制重新查一次 `document.getElementById()`，重新綁到當下真正存在的節點上，裂縫從未被踩到。

H1.4.2 讓 Heatmap 改成跟 Dashboard 一樣的 idempotent activate（不再每次都 unbind），這個「靠切分頁意外修復」的機制消失了，裂縫第一次被真正踩到：`geoDashboardMapActivate()` 被連續呼叫多次（Direct Load → 切 Heatmap → 切回 Dashboard → Range 切換觸發的 refresh…）時，只要中間有一次 `refreshGeoDashboardKpiBlock()` 重建了 map container，舊的 click listener 就會停留在已經被丟棄、detached 的舊節點上，新節點上完全沒有 listener——**Click-to-Activate 在 refresh 之後會靜默失效**，使用者點地圖沒有任何反應。

這是在撰寫 H1.4.1 Target Runtime 的 migration 測試時發現的：D14 之外，D3/D4/D5/D6/D7/D10/IDEMP-3/IDEMP-4/REFRESH-0 這 9 條原本以為「應該要因為 Contract Change 而 FAIL」的斷言，實際上是被這個既有裂縫拖累失敗，跟 Heatmap Wheel Contract Change 本身無關。

### Fix

`dashboardMapInteractionState` 新增 `boundCanvasEl` 欄位，記住「當初實際 `addEventListener` 的那個 DOM 節點參考」。`geoDashboardMapBindWheelLifecycle()` 每次呼叫都先用 `document.getElementById(mapContainerId)` 查詢當下「活著」的節點，跟 `boundCanvasEl` 比較：

- `containerId` 變了，或
- `id` 沒變但活著的節點跟 `boundCanvasEl` 不是同一個物件（`domNodeReplaced`）

任一為真，都先 `_geoDashboardMapUnbindWheelLifecycle()`（用 `boundCanvasEl` 這個記住的舊節點參考去 `removeEventListener`，不是重新查一次「現在的」節點——現在的節點可能已經是別的，對它 remove 沒有意義），再重新綁到活著的節點上，並更新 `boundCanvasEl`。

### Regression Evidence

`run-g1-6-ga4-h1-4-2-range-map-wheel-runtime.js` Category S（8 個新斷言）正式覆蓋這個情境：首次 render 點擊生效 → 觸發 `refreshGeoDashboardKpiBlock()` 產生新的 map container element（同 id、不同物件）→ 新節點點擊生效（證明 rebind 成功）→ 舊的、已 detached 的節點點擊完全不再影響目前 wheel 狀態（證明 unbind 成功，不是留著兩份 listener）→ 連續 refresh 三次後仍然只有一份 click lifecycle，不會 listener 累積。`static-audit-g1-6-ga4-h1-4-2.js` Category G（G8–G15）另外從原始碼層級確認 `boundCanvasEl` 的存在與正確使用。

## Test Migration Finding

本輪過程中，以下 5 支 inherited suite 的舊斷言假設了已經被 H1.4.2 Intentional Contract Change 推翻的舊 Contract，全部已改寫成符合 H1.4.2 新 Contract 的新斷言，並確認 **FAIL=0**（不是被 override 接受的已知 FAIL）：

1. `run-g1-6-ga4-h1-4-dashboard-source-runtime.js`（59/59）——舊：純文字 empty message 斷言。新：Sync CTA 文案／DOM 存在性 + Range-switch GET-only／POST 只在 CTA handler 內的語意檢查。
2. `run-g1-6-ga4-h1-4-map-state-runtime.js`（100/100）——同上（Empty-2）。
3. `static-audit-g1-6-ga4-h1-4.js`（230/230）——舊：literal `return;` regex（E52/E56）+「Dashboard 完全不 POST」假設（M143/M144）。新：generation guard 條件語意檢查 + Range-switch GET-only／POST-only-in-CTA／no-dual-render-source 的完整語意驗證。
4. `run-g1-6-ga4-h1-4-1-geo-dashboard-cleanup-runtime.js`（112/112）——舊：Heatmap auto-enable（D14/D15）。新：Heatmap click-to-activate。（這支 migration 過程中也是發現 Production Bug #2 的地方。）
5. `static-audit-g1-6-ga4-h1-4-1.js`（106/106）——舊：E10 assert `geoDashboardMapDeactivateForHeatmap()` 被呼叫。新：assert `geoDashboardMapActivate()` 被呼叫、且 `geoDashboardMapDeactivateForHeatmap()` 不再被呼叫。

## Regression Runner History（誠實記錄，不作為 Final 證據）

開發中期，`run-regression-g1-6-ga4-h1-4-2.js` 曾短暫允許「documented partial FAIL」（`OVERRIDES` 內同時記錄 expected pass 與 expected total，容許 `fail = total - pass > 0` 也算 PASS），用來在完成 5 支 migration 之前先確認整體 pipeline 沒有其他問題。**這個方法已經在 Final Gate 之前撤銷**：5 支 migration 全部完成、Production Bug #2 修好之後，`classify()` 改回嚴格版本——只有 `exit===0 && fail===0 && pass===expected && total===expected` 才算 PASS，`OVERRIDES` 現在只能調整 expected COUNT，不能也不會包含任何 expected FAIL。這段期間的 partial-fail 結果不構成 Final 證據，Final 證據是本文件與 Test Report 記錄的 67/67 ×3、每個 child suite `FAIL=0` 的 Strict Regression 結果。

## JSDOM Test Harness Finding（Test-only，不是 Production bug）

H1.4.2 新增的兩支 Runtime（`run-g1-6-ga4-h1-4-2-range-map-wheel-runtime.js`、以及間接受影響的既有 runtime）會用 `dom.window.eval()` 載入完整的 `public/js/app.js`，其中包含一段 WebSocket 重連的 `setTimeout` 鏈。每個測試區塊呼叫 `freshEnv()` 都會建立一個新的 jsdom `window` 並完整 eval 這份程式碼，但從不 `close()` 這個 window——累積約 30 個測試區塊後，就有 30 條永不清除的重連計時器持續佔用 Node event loop，即使所有斷言與 `printSummary()` 都已經完成，process 也不會自然結束（需要外部 `timeout` 強制殺掉）。修法：在 `printSummary()` 之後、確認所有 async 斷言與 summary 都已經完成印出後，才呼叫 `process.exit(process.exitCode)`——跟既有 `run-g1-6-ga4-h1-4-1-geo-dashboard-cleanup-runtime.js` 已經在用的慣例完全一致。分類：**TEST HARNESS FIX，不是 Production bug**（正式部署的瀏覽器環境本來就會自然存在、也應該保持這條重連邏輯）。

