# R5.4-G1.2 — 訂單熱區／訪客熱區 Layer Switch Hotfix
## Completion Report

fix18-10-hotfix30-B5-R5.4-G1.2

**版本狀態：QA BUILD**
**Automated Verification：PASS**
**Manual Visual：REQUIRED / NOT TESTED**
**Final Completion：NOT YET APPROVED**

---

## 1. Scope

修正真實瀏覽器回報的「訂單熱區／訪客熱區」模式切換 Bug：按鈕 active、面板內容、Leaflet Layer 三者與 `geoHeatUiState.layer` 不同步。**不新增功能、不修改 Backend/DB/Coordinate API/A7 KPI/既有統計口徑、不開始 R5.4-G2**。

## 2. Root Cause

三個獨立缺陷（詳見 `R5.4-G1.2_LAYER_SWITCH_FIX.md`）：(1) 按鈕 active/aria 更新迴圈是空的 `forEach`；(2) Order／Visitor 的 Leaflet `layerGroup` 從未互斥顯示；(3) Visitor Layer 無可畫資料時地圖沒有任何說明文字。確認**沒有**第二套競爭 state（`orderHeatmapMode`/`geoVisitorMode`/`geoLiveState`/`activeLayer` 均不存在），唯一權威狀態始終是 `geoHeatUiState.layer`。

## 3. 修改檔案

- `public/js/geo-heatmap-ui.js`（核心修正：`_geoHeatUiApplyLayerExclusivity`、`_geoHeatUiRerenderLayerToggle`、`_geoHeatUiRenderVisitorMapOverlay`、`_geoHeatUiVisitorMapOverlayMessage`）
- `public/css/geo-heatmap.css`（新增 `.geo-heat-visitor-map-overlay` 樣式）
- `scripts/smoke-hotfix30-b5-r5-4-g1-2-layer-switch.js`（新增，83 assertions）
- `scripts/static-audit-g1-2.js`（新增，25 項稽核）
- `R5.4-G1.2_LAYER_SWITCH_FIX.md` / `R5.4-G1.2_COMPLETION_REPORT.md` / `R5.4-G1.2_MANUAL_QA_CHECKLIST.md`

**未修改**：`public/js/geo-heatmap.js`（Order Engine）、`public/js/geo-visitor-layer.js`（Visitor Engine）、`public/js/geo-live-layer.js`（G1 GeoLiveLayer）、任何 `utils/`、`routes/`、DB migration。

## 4. Active / Aria 修正

`geoHeatUiSetLayer()` 切換後呼叫 `_geoHeatUiRerenderLayerToggle()`，重新產生整段按鈕 HTML，`is-active`/`aria-pressed`/`aria-selected` 三者正確互斥（已驗證：兩個按鈕的 `aria-pressed`/`aria-selected` 永遠一真一假，不會同時為 true 或同時為 false）。

## 5. Panel 切換

沿用既有 `orderEl.hidden`/`visitorEl.hidden` 切換邏輯（本來就正確運作），與按鈕/地圖更新在同一次 `geoHeatUiSetLayer()` 呼叫內完成，不分散在不同時間點。

## 6. Leaflet Layer Exclusivity

`_geoHeatUiApplyLayerExclusivity(layer)` 只用 `map.addLayer()`/`removeLayer()` 切換既有兩個 `layerGroup`（不 `clearLayers`、不重建、不建立第二張地圖或 Tile Layer）。已驗證：快速切換 20 次後地圖上最多只有一個 Layer，`orderGroup`/`visitorGroup` 物件參考全程不變。

## 7. Unknown Overlay

`_geoHeatUiRenderVisitorMapOverlay()` 依 `loading`/`error`/`empty`/`no-drawable-coordinate` 四態顯示不同文案，已重現並驗證真實回報案例（Geo Visitors=1, Known District=0, Exact Coordinate=0, Unknown=1, Coverage=0%）會顯示「目前有 1 位訪客，但尚未取得可繪製的真實座標」+ 完整四個數字；有已知行政區 Polygon 或真實座標可畫時不顯示覆蓋文字（已用有資料情境驗證）。

## 8. G1.2 Smoke

```
scripts/smoke-hotfix30-b5-r5-4-g1-2-layer-switch.js
PASS: 83 / FAIL: 0 / TOTAL: 83
```

透過真實 jsdom 執行 `geo-heatmap.js` + `geo-visitor-layer.js` + `geo-heatmap-ui.js` 三支檔案的實際程式碼（非字串掃描），涵蓋按鈕/aria/Panel/Layer 互斥/Unknown Overlay/四態/Store Switch/idempotent 重複點擊/合法資料時 Overlay 自動隱藏等情境。

## 9. G1 / G1.1 Regression

```
G1 Smoke:          212 / 212 PASS
G1.1 Visual Smoke:  91 /  91 PASS
```

均未受本輪修改影響。

## 10. 完整 Regression

重跑 G1/G1.1/G1.2 三支 Smoke、A7/A5/A4/A2/A1.2/B2/B3 指定腳本、其餘完整 65 套既有 Smoke/Verify、全專案 `node --check`（0 語法錯誤）。

**Baseline Comparison：**
- **Pre-existing failures（本輪前後皆同樣狀態，與 G1.2 無關）**：`smoke-cart-delivery-live-refresh.js`、`smoke-hotfix26-c.js`、`smoke-hotfix26-f2.js`、`smoke-hotfix26-f7.js`、`smoke-hotfix26-f8-b.js`、`smoke-hotfix27-cd.js`、`smoke-hotfix27.js`、`smoke-hotfix28.js`、`smoke-hotfix29.js`、`smoke-hotfix30-b5-r5-2-b1-3-geo-behavior-funnel-backend.js`——全部已在先前回合透過「重新解壓原始未修改 zip、同條件重跑比對」證實為既有問題（詳見 `regression_flaky_note.txt`），本輪重跑再次確認同樣的間歇模式（`smoke-hotfix27.js`/`smoke-hotfix28.js` 失敗筆數在 2–3 之間波動，屬已知範圍）。
- **G1.2 New failures**：0。
- **G1.2 Fixed failures**：0（本輪範圍是修正 Layer 切換 Bug，未涉及上述既有失敗項目的產品邏輯，未為了全綠修改無關程式碼）。

## 11. Static Audit

`scripts/static-audit-g1-2.js`：**25/25 通過**（無第二張地圖/Tile Layer、無重複 Order/Visitor Layer、Order/Visitor 不會同時 active、active class/aria/Panel/Leaflet Layer 皆與 state 一致、Refresh 不重設模式、無 duplicate polling、無 Memory Leak、無 console.log/Math.random()、無 Fake Marker/IP 座標/店家座標冒充/行政區中心冒充、Unknown 不畫點、A7 KPI/Order Heatmap Engine/G1 GeoLiveLayer 均未退化、Script Load Order 正確、無硬編碼 store_001/桃園中壢）。既有 `scripts/static-audit-g1.js`（G1/G1.1 範圍 30 項）重跑亦全數維持通過。

## 12. Known Limitations

見 `R5.4-G1.2_LAYER_SWITCH_FIX.md` §7：Choropleth 只畫合法 Polygon 對應的已知行政區；覆蓋文字的四個數字完全來自既有、未修改的 `geoVisitorComputeCoverage()`；`with_coordinate` 目前系統性設計下恆為 0（既有行為，非本輪引入）。

## 13. Manual Visual 狀態

**NOT TESTED。** 本輪工作在無瀏覽器的伺服器端 Linux 沙箱完成，無法執行真實瀏覽器點擊/視覺驗收。`R5.4-G1.2_MANUAL_QA_CHECKLIST.md` 所有項目皆明確標記 NOT TESTED，未寫 PASS。

## 14. 是否可部署

**程式碼層面（自動化驗證）已完整**：G1.2 Smoke 83/83、G1 Smoke 212/212、G1.1 Visual Smoke 91/91、Static Audit 25/25（+ 既有 30/30）、完整 Regression 零新增失敗、全專案 `node --check` 零錯誤。

**因 Manual Visual 尚未執行，不得宣告 Final Release。** 建議先在真實瀏覽器（Chrome/Edge/Android Chrome/iPhone Safari）由人工完成 `R5.4-G1.2_MANUAL_QA_CHECKLIST.md` 全部項目，確認實際點擊行為與視覺效果後，再將版本狀態提升為 Final Release。
