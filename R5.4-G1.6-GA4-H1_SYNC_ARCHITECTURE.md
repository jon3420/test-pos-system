# R5.4-G1.6-GA4-H1 — Sync Architecture

## 集中 Sync Service

`services/ga4GeoSyncService.js` 是所有 GA4 城市同步邏輯的唯一入口，Route
與 Frontend 都不直接呼叫 GA4 API。

```
routes/ga4-geo.js
  → services/ga4GeoSyncService.js
      → utils/ga4Geo/productionAdapter.js（正式）
        或 utils/ga4Geo/mockAdapter.js（僅測試腳本注入）
          → utils/ga4Realtime/client.js（唯一 GA4 Client Singleton）
```

對外函式：`syncRealtimeGeoSnapshot` / `syncGeoRangeStats` /
`syncTodayGeoStats` / `syncYesterdayGeoStats` / `backfillGeoStats` /
`getGa4GeoSyncStatus` / `getRealtimeGeoSummary` / `getRangeGeoStats`。

## Property Binding

`getPropertyBinding(db, storeId)` 包一層 `getGa4RealtimeConfig()`
（既有 G1.5-A 模組）。任何 sync/read 函式第一件事就是呼叫這個函式；
`configured:false` 時一律回傳 `property_not_bound`，**在呼叫 Adapter 之前
就返回**（QA Harness Scenario L 與 Mutation Negative #8/#9 驗證過這個
順序）。

## Mutex

模組層 `Map`（`_inFlight`），Key 為 `${storeId}::${syncType}`。同一店同一
sync type 同時只允許一個進行中，第二個立即回 `sync_in_progress`（不排隊、
不靜默丟棄）。

## Retry / Timeout / Backoff

`_fetchWithRetry()`：每支 Query 最多重試 2 次，只在 Adapter 回傳
`retryable:true` 時才重試，間隔遞增（200ms/400ms，上限 500ms）。
`timeoutMs` 由呼叫端傳入 Adapter。

## Partial / Fail-open / Stale Fallback

- 三支 Historical Query 中只要有一支成功，就用成功的部分建立/更新資料，
  標示 `partial=1`；全部失敗才整體回失敗。
- Realtime 與 Range 同步失敗時，**完全不觸碰既有 DB 資料**（不 DELETE、
  不清空），Read Path 靠 `ga4_geo_sync_runs.status` 判斷是否要標示
  `stale`。
- 所有失敗路徑都在 `try/catch` 內，最終回傳結構化 `{success:false, code}`，
  不 throw 給 Route，也不洩漏原始 Error/Stack。

## Merge-by-location

Server-side 用 `raw_location_key`（country+region+city 正規化字串，不是
只用 city）當 Merge Key，三支 Query 的結果彙整進同一個 `Map`。

## Upsert

三張表都用 SQLite `INSERT ... ON CONFLICT(...) DO UPDATE SET ...`，Unique
Key 見 Data Model 文件。QA Harness Scenario B/G 與 Smoke J 驗證重跑不會
產生重複列。

## Credential Guard（本輪新增的安全修正）

`utils/ga4Realtime/client.js` 的 `_getClient()` 在「需要建立新 Client」且
`credentialStatus().available === false` 時直接回 `null`（觸發既有
`SDK_UNAVAILABLE` fail-open 路徑），不嘗試呼叫 SDK 建構子，避免
google-auth-library 隱含 ADC 解析造成的 `unhandledRejection`／process
crash。已注入的測試假 Client（`_setClientForTest`）完全不受這個 Guard
影響。
