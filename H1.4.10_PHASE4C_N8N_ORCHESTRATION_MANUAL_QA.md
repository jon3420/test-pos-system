# H1.4.10 Phase 4C — RECOVERY N8N ORCHESTRATION Manual QA

**狀態：CODE COMPLETE ｜ AUTOMATED ACCEPTANCE PASS（152/152）｜ MANUAL N8N QA PENDING**

沒有真實的 n8n instance，以下全部標記 **PENDING MANUAL QA**，不得標成 PASS。Sandbox 自動化測試（152/152，見 Test Report）已涵蓋所有可以在沒有真實 n8n 的情況下驗證的邏輯路徑（HMAC、Replay、SSRF、Timezone、Workflow 靜態拓撲、Shared Secret 格式驗證）；以下是 n8n 實際執行環境才能驗證的部分。

## 部署前準備

1. **匯入 workflow**：`n8n-cart-recovery-workflow.json`。
2. **設定環境變數**：
   - `POS_RECOVERY_SHARED_SECRET`：與 POS 後台「n8n 自動排程」設定卡中的 Secret 完全一致（建議用「重新產生 Secret」按鈕產生的高熵值）。
   - `POS_BASE_URL`：POS 服務對外可存取的網址（例如 `https://your-pos.example.com`）。
3. **POS 後台**：開啟「啟用 n8n 自動排程」、填入 n8n 的 Webhook URL（`https://<n8n-domain>/webhook/pos-cart-recovery-wakeup`）、設定 Secret。

## A. n8n Import Workflow

**操作**：在 n8n 介面匯入 `n8n-cart-recovery-workflow.json`。

**預期**：所有節點正確顯示，無解析錯誤，`environment variable` 引用（`$env.POS_BASE_URL`／`process.env.POS_RECOVERY_SHARED_SECRET`）在該 n8n 版本下能正確存取。

狀態：**PENDING MANUAL QA**

## B. Webhook Early ACK

**操作**：POS 觸發一次 Wake-Up（例如：開啟 Recovery 功能後，讓一個顧客加入購物車但不結帳，等待 delay 時間內觀察）。

**預期**：n8n 的 Webhook 節點收到請求後，**在進入 Wait 節點之前**就先回應 202（不是等 Wait 結束才回應）。POS 端 `sendWakeUp()` 應在 8 秒內收到 2xx 回應，不會 timeout。

狀態：**PENDING MANUAL QA**

## C. Credential / Environment Variable Access

**操作**：確認 workflow 內的 Code 節點能正確讀到 `POS_RECOVERY_SHARED_SECRET` 與 `POS_BASE_URL`。

**預期**：簽章計算成功，不因讀不到環境變數而簽出錯誤的值。

狀態：**PENDING MANUAL QA**

## D. Code Node Crypto 權限

**操作**：確認 n8n 的 Code 節點執行環境允許 `require('crypto')`。

**預期**：`crypto.createHmac`／`crypto.timingSafeEqual`／`crypto.randomUUID` 皆可正常呼叫，不因 sandbox 限制被擋。

狀態：**PENDING MANUAL QA**

## E. Wait Node Timezone 行為

**操作**：觀察 `Wait Until due_at` 節點實際等待的時間點，與 POS 傳來的 `due_at`（明確帶 `Z` 的 UTC ISO8601）比對。

**預期**：n8n 正確依 UTC 時間等待，不因 n8n instance 自身的 timezone 設定產生偏移（這是本輪已經在 POS 端修正的「明確 timezone」contract，但 n8n 那端如何解讀 `dateTime` 參數仍需真實驗證）。

狀態：**PENDING MANUAL QA**

## F. Loop Execution（has_more Loop）

**操作**：讓某家店累積超過 50 筆同時到期的 Recovery Job（可用測試環境批次建立），觸發一次 Wake-Up。

**預期**：第一次呼叫 `process-due` 回傳 `has_more=true`，workflow 正確迴圈用新的 `request_id` 再呼叫一次，直到 `has_more=false` 或達到 10 次上限才停止；每一圈的 `store_id`/`stage`/`_loopCount` 都正確保留（不因 HTTP Response 覆蓋而遺失）。

狀態：**PENDING MANUAL QA**

## G. 實際 POS Callback

**操作**：完整跑一次「加購物車 → 不結帳 → 等待 delay → n8n 呼叫 POS → POS 真的發送 LINE 提醒」。

**預期**：顧客的 LINE 收到一次提醒訊息，內容符合 Phase 4B 既定文案（不含姓名/電話/地址），Recovery Job 狀態變成 `sent`。

狀態：**PENDING MANUAL QA**

## H. n8n Restart / Persisted Wait

**操作**：在某個 Job 還在 `Wait` 節點等待期間，重啟 n8n instance。

**預期**：n8n 是否能正確恢復該筆等待中的 workflow 執行（這取決於 n8n 版本與部署方式的 workflow persistence 能力，不是 POS 端能控制的）。若 n8n 無法恢復，POS 端因為完全不依賴 n8n 作為 Recovery Job 的 SSOT（`cart_recovery_jobs` 才是），資料不會遺失，但這次排程會遺失，需要有其他機制（例如定期補發 Wake-Up，留待未來階段）重新觸發。

狀態：**PENDING MANUAL QA**

## 建議測試順序

1. 先在 n8n 測試環境跑 A-D（匯入與基本設定），確認沒有解析/存取錯誤。
2. 用一個測試店家的低流量環境跑 E-G（真實一次完整流程），確認顧客真的收到提醒且內容正確。
3. 最後在準生產環境驗證 F/H（多筆 Job / 服務重啟的邊界情況）。

## I. hotfix30-FRIEND-SECRET-UX：One-Time Secret Modal 真機／真瀏覽器 QA

**操作**：在後台按「重新產生 Secret」，確認 Modal 內明文可肉眼辨識（非圓點），按「複製 Secret」後貼到別處確認內容正確，關閉 Modal 後重新整理頁面，確認無法再看到剛才那組明文（欄位變回空白，只顯示「已設定」狀態）。額外在不支援 `navigator.clipboard`（例如非 HTTPS 的內網測試環境）或使用者拒絕剪貼簿權限的瀏覽器上測試，確認 fallback（`input.select()` + 手動 Ctrl/Cmd+C）仍可用。

**預期**：明文只在 Modal 開啟期間可見一次；複製功能（或其 fallback）正常運作；關閉後、reload 後皆無法再取得同一組明文。

狀態：**PENDING MANUAL QA**（自動化測試見 `scripts/run-h1-4-10-secret-ui-runtime.js`，48/48 PASS，涵蓋所有非真機依賴的行為）。
