# R5.4-G1.5-B2.3 GA4 Endpoint Unification Fix

fix18-10-hotfix30-B5-R5.4-G1.5-B2.3 — GA4 Legacy Endpoint Removal & Runtime
Request Unification

以 `fix18-10-hotfix30-B5-R5.4-G1.5-B2.2` 為基礎的緊急修正版。詳細搜尋方法
與逐項稽核結果見同份交付內的 `R5.4-G1.5-B2.3_RUNTIME_ENDPOINT_AUDIT.md`；
本文件是給複核者看的修正摘要。

## 一、Connection Test PASS

正式環境已確認：

1. GA4 Service Account Credential：PASS
2. GA4 Property Permission：PASS
3. Stream Filter：PASS
4. Settings Connection Test：PASS，顯示「連線成功，最近30分鐘暫無即時
   資料。」

## 二、正式地圖仍失敗

Connection Test 全綠，但切到「GA4 即時訪客」地圖圖層時仍失敗。

## 三、Console Network 證據

Chrome DevTools 實際抓到的請求：

```
GET /api/geo-live/ga4-visitors?refresh=0
→ HTTP 502
```

這個路徑**沒有 `window`、沒有 `metric`**，跟正式 Contract（見下）明顯不同。

## 四、Legacy Endpoint

`/api/geo-live/ga4-visitors`——**這個路徑在本專案目前的原始碼裡完全不
存在**（`routes/geo-live.js` 沒有對應 handler，全 repo 搜尋
`ga4-visitors`／`ga4_visitors` 零命中，包含所有既有文件）。詳見
Runtime Endpoint Audit 文件第一至四節的完整搜尋記錄。

## 五、New Endpoint

正式 Contract（`R5.4-G1.5-A_API_CONTRACT.md`，G1.5-A/B1/B2 沿用至今）：

```
GET /api/geo-live/ga4-realtime-status
GET /api/geo-live/ga4-realtime?window=5|30&metric=visitors|view_item|add_to_cart|checkout|purchase&refresh=0|1
```

`public/js/geo-ga4-realtime-layer.js` 的 `geoGa4FetchData()`／
`geoGa4FetchStatus()`（B2.2 已改用 `apiFetch()`）就是打這兩個端點，
沒有第二套實作。

## 六、Root Cause

由於 `ga4-visitors` 這個字串在整個 codebase（含所有歷史文件）裡查無
任何蹤跡，**排除「程式碼裡新舊兩套 Endpoint 並存或接錯線」的假設**。
最可能的解釋：使用者瀏覽器當下實際執行的 JS，跟這份原始碼裡的
`geo-ga4-realtime-layer.js` 不是同一份檔案——可能是：

1. Zeabur 部署平台尚未真正把最新 build 推上線（正式環境還在跑更早期、
   在 Contract 定案為 `ga4-realtime` 之前的某個原型版本）；或
2. CDN／反向代理層快取住了更舊的靜態資源；或
3. 使用者瀏覽器本身的 HTTP Cache 保留了舊版 JS。

本專案確認**沒有 Service Worker**（詳見 Audit 文件第八節），所以不是
應用層快取造成，可能性集中在（1）（2）（3）三者，這些都是部署/傳輸層
面，無法單靠修改這份原始碼本身解決，只能透過確保 cache-buster 版本號
正確反映「這是全新一次修改」來降低使用者側殘留舊快取的風險，並要求
部署後 Hard Refresh 驗證。

## 七、呼叫來源

排查 `public/index.html`／`geo-heatmap-ui.js`／`geo-ga4-settings.js`：
Layer Switch、Settings 存檔成功 Hook、Auto Refresh、Manual Refresh
全部只呼叫 `geoGa4FetchAndRender()`／`geoGa4Refresh()`／
`geoGa4NotifySettingsChanged()`（都定義在 `geo-ga4-realtime-layer.js`），
沒有找到任何呼叫 `ga4-visitors` 或其他 legacy 名稱函式的程式碼路徑
（詳見 Audit 文件第五節逐項核對表）。

## 八、Layer Switch 修正

**本輪未發現需要修正之處**——`geo-heatmap-ui.js` 的 `geoHeatUiSetLayer()`
在切到 `ga4` 分支時只呼叫 `geoGa4FetchAndRender(containerId)`
（一套 lifecycle），切離開時只呼叫 `geoGa4Deactivate()`；四種切換方向
（Visitor→GA4、Order→GA4、GA4→Visitor、GA4→Order）都經過同一組
互斥分支，沒有第二套 GA4 loader 可以移除。B2.3 Smoke 的 C26／F56／F69
以真實 jsdom 執行驗證：`activate` 只會觸發一次 status 呼叫、一次 data
呼叫，不會有第二條 fetch chain。

## 九、Manual Refresh 修正

**本輪未發現需要修正之處**——`geoGa4Refresh(containerId)` 呼叫
`_geoGa4RunFetch(containerId, {refresh:true})`，組出的 URL 帶
`refresh=1`，沒有任何路徑會打到 `ga4-visitors?refresh=0`（那個模式本身
在程式碼裡不存在）。B2.3 Smoke B19／C29 驗證。

## 十、Auto Refresh 修正

**本輪未發現需要修正之處**——`_geoGa4ScheduleAutoRefresh()` 排程呼叫
`_geoGa4RunFetch(containerId, {refresh:false})`，跟 Manual Refresh 共用
同一支函式，組出的 URL 帶 `refresh=0`。B2.3 Smoke C30 驗證。

## 十一、Window／Metric 修正

**本輪未發現需要修正之處**——`geoGa4BuildRequestUrl()` 一律組出
`window=5|30`、`metric=visitors|view_item|add_to_cart|checkout|purchase`，
不存在「缺少 window/metric」的請求路徑（Console 觀察到的舊請求缺少這兩
個參數，正好證明它不是這份程式碼產生的）。B2.3 Smoke B11-B17／F53-F54
驗證。

## 十二、Cache-buster

發現並修正了一處版本標籤不一致：`geo-ga4-realtime-layer.css` 停留在
舊版 `fix18-10-hotfix30-B5-R5-4-G1-5-B1`，但配套的
`geo-ga4-realtime-layer.js` 已經是
`fix18-10-hotfix30-B5-R5-4-G1-5-B2-3`——同一功能模組的 JS／CSS 版本
沒有同步。本輪已將 CSS 的版本字串補齊為跟 JS 一致的
`fix18-10-hotfix30-B5-R5-4-G1-5-B2-3`。

`app.js?v=fix18-10-hotfix22` **不是快取問題**：這個檔案是全站共用的
Store JWT／`apiFetch()` 基礎設施，本輪與過去所有 GA4 相關 hotfix
都沒有修改過它的內容，版本號停留在最後一次真正修改的版本是正確、
符合預期的行為，跟正式環境的整體版本無關。

## 十三、Service Worker Audit

專案裡**沒有 Service Worker**——沒有 `service-worker.js`／`sw.js`，
沒有 Workbox，`public/` 底下沒有任何 `navigator.serviceWorker` 或
`caches.open()` 呼叫。唯一命中的 `manifest.json` 是台灣行政區 GeoJSON
資料索引檔，跟 PWA／離線快取無關。詳見 Runtime Endpoint Audit 文件
第八節完整搜尋記錄。

## 十四、Smoke

`scripts/smoke-hotfix30-b5-r5-4-g1-5-b2-3-ga4-endpoint-unification.js`：

```
PASS:  75
FAIL:  0
TOTAL: 75
```

連續執行 3 次結果一致。涵蓋：Source Audit（A1-A8b，全 repo 掃描，排除
本測試與 Audit 文件自身的說明性文字）、Request（B9-B25，真實 jsdom 執行
驗證 window/metric/refresh/apiFetch/signal/no-property-stream-store）、
Lifecycle（C26-C34，單一 fetch chain／不重複 timer／abort／deactivate）、
Cache Busting（D35-D40，版本字串一致性＋ Service Worker 分類）、Error
（E41-E50，200 無資料／400／401／403／500／502 不可能／malformed／
AbortError／Network Error 全部分開驗證）、Mutation（F51-F70，全部是
「若改回錯誤版本就會 FAIL」的真實可執行斷言）。

## 十五、Regression

`scripts/run-regression-b2-2.sh`（沿用 B2.2 的 runner，加入 B2.3 Smoke）
3 輪：

| Suite | Round 1 | Round 2 | Round 3 |
|---|---|---|---|
| B2.3 Smoke | 75/75 | 75/75 | 75/75 |
| B2.2 Smoke | 95/95 | 95/95 | 95/95 |
| B2.1 Smoke | 85/85 | 85/85 | 85/85 |
| B2 Smoke | 181/181 | 181/181 | 181/181 |
| B2 Static Audit | 82/82 | 82/82 | 82/82 |
| B2a Smoke | 106/106 | 106/106 | 106/106 |
| B1 Smoke | 168/168 | 168/168 | 168/168 |
| B1 Static Audit | 71/71 | 71/71 | 71/71 |
| G1.5-A Smoke | 140/140 | 140/140 | 140/140 |
| G1.5-A Static Audit | 77/77 | 77/77 | 77/77 |
| Geo Map Settings Smoke | 620/620 | 620/620 | 620/620 |
| Geo Settings UI Smoke | 157/157 | 157/157 | 157/157 |
| G1 geo-live Smoke | 212/212 | 212/212 | 212/212 |
| G1.4.1 Smoke | 149/149 | 149/149 | 149/149 |
| G1.4 Smoke | 148/148 | 148/148 | 148/148 |
| G1.3.2 Smoke | 148/148 | 148/148 | 148/148 |
| G1.2 Smoke | 83/83 | 83/83 | 83/83 |
| A2 Smoke | 229/229 | 229/229 | 229/229 |
| A1.2 Smoke | 189/189 | 189/189 | 189/189 |
| Static Audit G1.4.1 | 56/56 | 56/56 | 56/56 |
| Static Audit G1.4 | 52/52 | 52/52 | 52/52 |
| Static Audit G1.3.2 | 48/48 | 48/48 | 48/48 |

三輪數字完全一致，`FAIL=0`，`exit=0`。每支測試前清除 `data/pos.db`，
確認無 DB residue／timer residue／duplicate request／credentials
residue。`node --check` 全部通過：`geo-ga4-realtime-layer.js`／
`geo-heatmap-ui.js`／`geo-ga4-settings.js`／以及所有新增/修改的 Smoke
腳本。

## 十六、Manual Visual

NOT TESTED（僅透過 jsdom 驗證 DOM／請求邏輯，未在真實瀏覽器手動操作，
也未實際部署到 Zeabur 驗證 Hard Refresh 後的行為）。

## 十七、Real GA4 Data

Connection PASS（Service Account／Property Permission／Stream Filter／
Settings Connection Test 皆為正式環境已驗證通過的既有事實，本輪未重新
測試也未變更）。**Map Endpoint（`GET /api/geo-live/ga4-realtime` 在正式
環境的實際回應）尚待部署驗證**——本輪的修正（CSS cache-buster 補齊、
確認原始碼零 legacy 殘留）需要重新部署並搭配 Hard Refresh 才能確認
正式環境是否真正改善，不宣稱本輪修正已在正式 GA4 Live 環境驗證通過。
