# R5.3-A4 — Geo Metric Switcher 對照表（整合前）

## 一、DOM 對照

| 項目 | 舊切換器（geo-intelligence-map.js） | 新切換器（geo-visitor-layer.js） |
|---|---|---|
| 容器 class | `.geo-map-metrics` | `.geo-heat-controlbar`（`#-metric-bar`） |
| 按鈕 class | `.geo-map-metric-btn` | `.geo-heat-ctl-btn` |
| 按鈕屬性 | `data-geo-map-metric` / `aria-pressed` | `aria-pressed`（onclick 內帶 metric 字串） |
| 渲染函式 | `geoRenderMapBlock()` 內聯產生 | `geoVisitorMetricBarHtml(containerId)` |
| 可見時機 | **永遠可見**（在 Dashboard／Heatmap 兩個分頁之上，`mapHtml` 一部分） | 僅在 Heatmap 分頁 → Visitor Layer 才可見（整合前） |
| Metric 清單 | Visitors / Orders / Revenue / Conversion / Cart Abandonment / Recommendation Risk（6 個） | 訪客 / 加入購物車 / 開始結帳 / 完成訂單 / 營收 / 成交率 / 購物車放棄 / 建議風險（8 個） |

## 二、State 對照

| 項目 | 舊切換器 | 新切換器 |
|---|---|---|
| State 物件 | `geoMapState`（`geo-intelligence-map.js`） | `geoVisitorState`（`geo-visitor-layer.js`） |
| Metric 欄位 | `geoMapState.metric`（預設 `'visitors'`） | `geoVisitorState.metric`（預設 `'visitors'`） |
| 資料欄位 | `geoMapState.rows`（= `vm.funnel.areas`，來自 `getGeoFunnel()`） | `geoVisitorState.funnel`（= `getGeoEventFunnel()` 回應） |
| Store 隔離 | 隨 Dashboard 整體重新整理（`refreshGeoDashboardKpiBlock`） | `geoVisitorHandleStoreSwitch()` |

**結論：兩個 state 完全獨立，切換其中一個不會影響另一個——這正是「兩套
按鈕分別控制不同 state」的問題本體。**

## 三、Handler 對照

| 項目 | 舊切換器 | 新切換器 |
|---|---|---|
| 切換函式 | `geoSetMapMetric(metric)` | `geoVisitorSetMetric(containerId, metric)` |
| 內部動作 | 設定 `geoMapState.metric` → 呼叫 `geoUpdateMapData()`（重新著色 Choropleth＋Legend＋Summary＋Tooltip＋Area List） | 設定 `geoVisitorState.metric` → 重新渲染 Metric Summary＋Ranking |
| 是否更新地圖著色 | ✅ 是（這是它唯一的核心職責） | ❌ 否（整合前完全不影響地圖，因為 Choropleth 目前允許清單為空，本來就不畫色，但如果以後開放合法 Polygon，兩者也不會同步） |

## 四、API／資料來源對照

| 項目 | 舊切換器 | 新切換器 |
|---|---|---|
| API | 無獨立 API，資料來自 Dashboard 既有的 `loadGeoDashboardData()`（`/api/analytics/geo/overview`／`/funnel`／`/county-summary`／`/alerts`） | `GET /api/analytics/geo/visitor-log`（回應含 `funnel`，來自 `getGeoEventFunnel()`） |
| 底層資料表 | `analytics_events`（透過 `utils/geoAnalyticsQueries.js` 的 `getGeoFunnel()`） | `geo_visit_log`（透過 `utils/geoEventEngine.js`） |
| 兩者是否同步 | 是（見 `R5.3-A3_ANALYTICS_DATA_FLOW_AUDIT.md`：`insertEvent()` 是唯一寫入點，兩張表同步寫入） | 同上 |

## 五、Render Function 對照

| 項目 | 舊切換器 | 新切換器 |
|---|---|---|
| Map Overlay | `geoUpdateMapData()` → 重新計算 `geoBuildMetricScale()`／逐一 `layer.setStyle()` | 無（`geoVisitorRenderChoropleth()` 是獨立的、只在合法 Polygon 清單非空時才畫，目前恆為 0 layer） |
| Legend | `_geoRenderMapLegendAndSummaryDom(scale)` | 無獨立 Legend render function |
| Summary | 同上（同一函式負責 Legend＋Summary） | `geoVisitorRenderSummaryDom()`／`geoVisitorRenderMetricSummaryDom()` |
| Ranking | `_geoRenderAreaListDom()`（鍵盤可操作清單，不是排行榜） | `geoVisitorRenderRankingDom()`（依 Metric 排序的排行榜） |
| Tooltip | `_geoBuildTooltipContent(areaId)`（綁在 `layer.bindTooltip()`） | `geoVisitorBuildTooltipContent(area)` |
| Empty State | `geoBuildMapEmptyHtml()` | 各 Metric 各自的文字（`目前沒有可用訂單識別資料」等） |

## 六、整合決策（依使用者指示）

1. **保留一套主切換器**：新的 8 個 Metric Tab（`geoVisitorState.metric`）
   成為正式主切換器。
2. **舊切換器按鈕移除**：`geoRenderMapBlock()` 不再輸出
   `.geo-map-metrics` 按鈕列；`geoMapState.metric` 保留為內部狀態（既有
   `geoUpdateMapData()`／`geoGetFeatureStyle()`／`geoBuildMetricScale()`
   等既有函式簽名不變，只是不再由獨立按鈕驅動）。
3. **統一切換入口**：`geoVisitorSetMetric()` 新增一步——透過新舊 Metric
   名稱對照表，呼叫既有 `geoSetMapMetric(mappedMetric)`，讓同一次點擊
   同時驅動 Map Overlay／Legend／Summary／Tooltip（沿用既有函式，不重寫
   Choropleth 邏輯）。
4. **可見位置調整**：Metric Tab 從「只在 Heatmap 分頁的 Visitor Layer
   才看得到」搬到「跟共用地圖同一層級，Dashboard／Heatmap 兩個分頁都看
   得到」，因為共用地圖本身也是兩個分頁都可見。
5. **初始自動 fetch**：`geoHeatUiRegisterContext()` 不再限定「只有
   Heatmap 分頁啟用時才抓 Geo Event Engine 資料」，只要頁面掛載就抓一次
   （預設 Metric 為「訪客」）。

## 七、Metric 名稱對照表（新 → 舊，供 `geoSetMapMetric()` 使用）

| 新（`GEO_EVENT_METRICS`） | 舊（`GEO_MAP_METRICS`） | 說明 |
|---|---|---|
| `visitors` | `visitors` | 直接對應 |
| `add_to_cart` | `visitors`（fallback） | 舊系統沒有這個維度，地圖暫時退回顯示 Visitors 分布，不影響新系統本身的 Summary/Ranking 數字 |
| `checkout` | `visitors`（fallback） | 同上 |
| `orders` | `orders` | 直接對應 |
| `revenue` | `revenue` | 直接對應 |
| `conversion` | `conversion_rate` | 直接對應 |
| `cart_abandonment` | `cart_abandonment_rate` | 直接對應 |
| `recommendation_risk` | `risk` | 直接對應 |
