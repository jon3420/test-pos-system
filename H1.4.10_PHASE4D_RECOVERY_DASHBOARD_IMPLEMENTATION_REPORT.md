# H1.4.10 Phase 4D — RECOVERY ANALYTICS DASHBOARD Implementation Report

**狀態：WIP ANALYTICS DASHBOARD ｜ AUTOMATED CORE ACCEPTANCE PASS（59/59）｜ MANUAL QA PENDING**

## 新增檔案

- **`routes/cart-recovery-dashboard.js`**：`GET /api/cart-recovery-dashboard/overview?days=<1-90>`，`requireStaffJwt` 保護，`req.storeId` 隔離。回傳：
  - `funnel`：三個 stage（`cart_abandoned`/`checkout_abandoned`/`payment_abandoned`）× 八種 status 的真實筆數。
  - `reminder_stats`：每個 stage 的 `reminder_sent`（真的送過提醒的筆數）、`reminder_then_converted`（送過提醒後來也成交的筆數）、`conversion_rate_of_resolved`（只算已有最終結果的 job，百分比，無資料時 `null`）、`resolved_count`、`avg_minutes_to_recovery`（精確的 `julianday()` 時間差計算，無資料時 `null`，不是誤導性的 `0`）、`time_to_recovery_sample_size`。
  - `recovered_revenue`：`payment_abandoned` 用真實 JOIN `orders` 表計算（只算有 `order_id` 且真的送過提醒又轉換的），`cart_abandoned`/`checkout_abandoned` 明確回傳 `total: null, reason: 'no_reliable_order_linkage'`（見 Reality Audit 的技術原因）。
  - 全部欄位皆為聚合數字，response 不含任何 `cart_id`/`order_id`/`line_user_id`/顧客資料。

## 修改檔案

- **`server.js`**：掛載 `/api/cart-recovery-dashboard`（`requireStore` + `requireFeature('line_order')`）。
- **`public/index.html`**：「購物車找回」卡片新增「購物車找回成效」小節——一個「重新整理成效資料」按鈕與一個結果表格容器（`#cartRecoveryDashboardPanel`），插在 consent 設定與 n8n 設定小節之間。
- **`public/js/app.js`**：新增 `loadCartRecoveryDashboard()`，呼叫既有 `apiFetch()`（自動帶 `Authorization: Bearer` token，與 `requireStaffJwt` 的期待完全匹配，呼叫前已確認過這個既有慣例，不是假設）取得資料後直接渲染成表格，**前端不重新計算任何數字**——`revenue: null` 就直接顯示「無法計算」文字，不用前端公式去填補。
- **`scripts/run-h1-4-10-phase4b-recovery-full-runtime.js`**：新增 `extractBalancedDiv()` 輔助函式（真正的 HTML tag-balance 掃描，取代原本脆弱的非貪婪正則），套用在 Part A／Part M 兩處卡片內容擷取。這是本輪過程中發現並修正的測試工具問題（見 Reality Audit 第 6 點），不影響 production 程式碼行為。

## 新增測試

- **`scripts/run-h1-4-10-phase4d-recovery-dashboard-runtime.js`**（59 assertions，多輪擴充自最初的 20）：涵蓋 JWT 保護、真實漏斗計數、`reminder_then_converted` 正確排除未真正送出的 job、`recovered_revenue` 正確排除無 `order_id` 連結的訂單、`cart_abandoned`/`checkout_abandoned` 明確回傳 `null`（非估算值）、`days` 參數 clamp、`conversion_rate_of_resolved` 正確排除進行中狀態並曝光 `resolved_count`/`converted_count`、`avg_minutes_to_recovery` 精確計算（10 分鐘控制組驗證）且要求 `converted_at>=sent_at`（異常時序資料整筆排除，不拉低平均）、response 不含任何原始識別字串（PII-free 聚合檢查）、真實 `requireStaffJwt` 簽發的 JWT 做跨店隔離（非 app-level stub）、`?store_id=` query 覆蓋被完全忽略、order-based revenue dedup（同一訂單兩筆 job 只算一次；兩張不同訂單同金額分別計入，證明沒有誤用 `SUM(DISTINCT total)`）、canonical `order_id`（`orders.id`/`orders.uuid` 恆等）的 schema-level 安全性、以及 Admin UI 的真實 jsdom runtime 驗證（見下方）。

## Admin UI Runtime（本輪新增）

- **`public/js/app.js`**：`loadCartRecoveryDashboard()` 修正成交率顯示格式，改為同時呈現百分比與樣本數（例如 `100%（1/1）`），不再只顯示孤立的百分比。
- 新增 `runAdminUiRuntimeTests()`（jsdom，reuse Phase 4B Full Suite 的 `extractBalancedDiv()` 手法擷取真實 production HTML 片段，執行真實 `app.js` 原始碼，不重寫假邏輯）：驗證面板與按鈕存在、`loadCartRecoveryDashboard()` 真的呼叫既有 `apiFetch()`（不自建 fetch 邏輯）、原始碼對 backend 給的數值沒有做任何算術重新推導（只做顯示格式化）、`revenue:null`→「無法計算」、成交率與樣本數一併呈現、`avg_minutes_to_recovery:null`→「—」（不是誤導性的「0 分鐘」）、真實數值（10）→「10 分鐘」、API 例外與 `success:false` 回應皆安全顯示錯誤且不 crash 頁面、換一組不同的 backend 數字畫面會跟著變（證明是真的渲染，不是寫死樣板）。

## Order-based Revenue Dedup（本輪修正的真實 bug）

`routes/cart-recovery-dashboard.js` 的 `recovered_revenue` 查詢原本是「job JOIN orders 直接 SUM」，若同一 `order_id` 出現在兩筆 job row 會被重複加總。修正為子查詢先 `DISTINCT j.order_id` 再 JOIN `orders`，`COUNT`/`SUM` 都是對「不重複的訂單」做。曾一度誤判為 sql.js 層的不穩定聚合結果而改寫成 JS-side Set 去重版本，後續定位到真正根因其實是測試本身的 JWT `store_id` 與預期不符（`requireStaffJwt` 用 JWT payload 覆蓋了測試想用 stub 設定的 `req.storeId`），與 SQL 完全無關；修正測試後已改回 DB-side 版本（用獨立 debug script 驗證過這支 SQL 本身正確，不需要為不存在的疑慮保留更複雜的 JS-side 寫法）。

## Frozen Contracts Preserved

H1.4.9（67/67）、Phase 1（68/68）、Phase 2（83/83）、Phase 3（111/111）、Phase 4A（89/89）、Backend Recovery Targeted（92/92）、Frontend Recovery Targeted（50/50）、Phase 4B Full Suite（168/168）、Phase 4C（152/152）全部 fresh-run 確認未下降。既有 `n8n_webhook_url`／`n8n-workflow.json`（Phase 4C 已驗證過）與本輪 Dashboard 完全獨立，未再次觸碰。
