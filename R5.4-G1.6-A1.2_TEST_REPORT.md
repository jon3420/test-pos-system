# R5.4-G1.6-A1.2 — Test Report

fix18-10-hotfix30-B5-R5.4-G1.6-A1.2

## 1. A1.2 Smoke

`scripts/smoke-hotfix30-b5-r5-4-g1-6-a1-2-authoritative-points.js`

**251 PASS ／ 0 FAIL ／ 251 TOTAL**，連續三次一致（含最終在乾淨解壓
環境下重新確認一致，見第 7 節）。

過程中曾出現、已修正的兩個 FAIL：

1. **Manifest wording／fixture 期望值不一致**：測試斷言用了過於狹隘的
   regex（要求緊接著「無法連線」後面literal「data.gov.tw」），但
   Manifest 實際文字用「無法連線該平台頁面」的說法，語義相同、措辭不同。
   Production 文字本身正確、誠實，修正的是測試 regex 過嚴，不是放寬
   驗證強度。
2. **content_sha256 self-reference 測試邏輯問題**：測試recompute
   content hash 時，從完整 Catalog 物件只排除了 `generated_at`，但
   `content_sha256` 欄位本身也已經寫進 Catalog——導致 recompute 的
   input 包含了 `content_sha256` 自己，跟原始計算時（`content_sha256`
   欄位還不存在時）的 input 不同，造成雜湊值不一致。**Production Hash
   邏輯本身正確**（Build Tool 是先算 hash、再把結果指派給
   `catalog.content_sha256`，沒有循環引用問題）；修正的是測試／驗證
   端也要排除 `content_sha256` 自己，不是 Production Bug。

## 2. A1.2 Static Audit

`scripts/static-audit-g1-6-a1-2.js`

**125 PASS ／ 0 FAIL ／ 125 TOTAL**，連續三次一致。

過程中出現的 4 個 False Positive 皆為 Production 程式碼裡的**說明性
註解**被 regex 誤判命中（例如註解裡提到「不使用 taoyuan-districts.geojson
這種矩形 fixture」本身包含了被檢查的關鍵字），已改成排除註解行
（`codeOnly()`）後再檢查——調整的是「檢查對象範圍」（只看真正的程式
邏輯，不看說明文字），不是放寬安全規則本身。

## 3. Catalog Verify

`scripts/verify-authoritative-admin-points.js`

**57 PASS ／ 0 FAIL**，涵蓋：Manifest schema／Archive hash 交叉核對／
22 counties／368 districts／duplicate code 檢查／parent link／座標
finite 與合理 bounds／`content_sha256` 可重算／Representative Point
metadata（`point_method`／`source`）欄位一致性。

## 4. A1.1 Compatibility

- G1.6-A1.1 Smoke：**160/160**
- G1.6-A1.1 Static Audit：**90/90**
- G1.6-A1 Smoke：**50/50**

原本測試 A1.1 BLOCKED 佔位行為的斷言（例如「Dashboard 端 centroid
resolver 明確回傳 null」）已更新為驗證 A1.2 的官方 Catalog Contract
（例如「Dashboard 端改用後端 marker-model，前端不再有 BLOCKED 常數」）
——這是 Category B 的刻意契約演進，因為 Estimate Marker 的資料來源
BLOCKED 狀態已在本輪解除，舊斷言假設的前提本身已經改變，不是降低
檢查強度。

## 5. Full Regression（三輪）

Regression Runner：`scripts/run-regression-g1-6-a1-2.js`。

**三輪結果完全一致，全部 PASS，FAIL=0，exit=0：**

| Suite | Round 1/2/3 |
|---|---|
| A1.2 Smoke | 251/251/251 |
| A1.2 Static Audit | 125/125/125 |
| Catalog Verify | 57/57/57 |
| G1.6-A1.1 Smoke | 160/160/160 |
| G1.6-A1.1 Static Audit | 90/90/90 |
| G1.6-A1 Smoke | 50/50/50 |
| Order Heatmap (A1) | 128/128/128 |
| Dashboard Rewire | 101/101/101 |
| Static Audit G1 | 30/30/30 |
| Static Audit G1.2 | 37/37/37 |
| Static Audit G1.3 | 32/32/32 |
| B2.5 Smoke | 76/76/76 |
| B2.4 Smoke | 139/139/139 |
| B2.3 Smoke | 75/75/75 |
| B2.2 Smoke | 95/95/95 |
| B2.1 Smoke | 85/85/85 |
| B2 Settings Smoke | 187/187/187 |
| B2 Static Audit | 82/82/82 |
| B2a Smoke | 106/106/106 |
| B1 Smoke | 168/168/168 |
| B1 Static Audit | 71/71/71 |
| G1.5-A Smoke | 140/140/140 |
| G1.5-A Static Audit | 77/77/77 |
| Geo Map Settings | 620/620/620 |
| Geo Settings UI | 157/157/157 |
| G1 geo-live | 212/212/212 |
| G1.4.1 | 149/149/149 |
| G1.4 | 148/148/148 |
| G1.3.2 | 148/148/148 |
| G1.2 | 83/83/83 |
| A2 | 229/229/229 |
| A1.2（legacy Visitor Geo Sync） | 189/189/189 |
| Static Audit G1.4.1 | 56/56/56 |
| Static Audit G1.4 | 52/52/52 |
| Static Audit G1.3.2 | 48/48/48 |
| node --check（本輪修改 JS） | OK |

過程中發現並修正一個真實 Category B 回歸：`scripts/static-audit-g1.js`
check #3 原本假設「centroid 這個詞只能出現在寫死回傳 null 的 BLOCKED
resolver 裡」——A1.2 合法解除了這個 BLOCKED 狀態後，這個假設本身已經
過時。已更新為「centroid 若出現，必須確認來自有稽核紀錄的官方 Catalog
Resolver（`authoritativeAdminPointCatalog`／
`resolveAreaRepresentativeMarker`），且沒有 bbox center／店家座標／
random offset 這類冒充來源」——保留原本的保護目的，反映新的合法實作。

## 6. Security Scan

- 工作目錄（排除 `node_modules`／`.git`）：掃描
  `BEGIN PRIVATE KEY`／`private_key_id`／`client_email`／
  `iam.gserviceaccount.com`／`access_token`／`refresh_token`／
  `Authorization:`／`Bearer`／`Cookie:` 等關鍵字。
- `data/geo/authoritative/`（含 extracted 官方 SHP／DBF／XML）：
  獨立掃描，0 筆真實 Credential 命中。
- 所有命中皆為：(a) 本專案自己的 denylist 陣列項目／安全掃描規則
  程式碼、(b) 文件說明文字（描述掃描結果或政策），逐一人工核對確認
  沒有一筆是真實憑證內容。
- ZIP Scan：見第 7 節（打包後執行）。

## 7. Clean Extraction

解壓至全新目錄（`/tmp/g1-6-a1-2-clean2`，非原工作目錄，不重用
`node_modules`／DB／cache）。

- **`npm ci`**：exit=0，`added 476 packages`。
- **A1.2 Smoke**：251 PASS ／ 0 FAIL ／ 251 TOTAL。
- **A1.2 Static Audit**：125 / 125 OK。
- **Catalog Verify**：57 / 57 OK。
- **G1.6-A1.1 Smoke**：160/160。
- **G1.6-A1.1 Static Audit**：90/90。
- **G1.6-A1 Smoke**：50/50。
- **Order Heatmap**：128/128。
- **Dashboard Rewire**：101/101。
- **B2.5**：76/76。**B2.4**：139/139。**B2.3**：75/75。**B2.2**：95/95。
  **B2.1**：85/85。**B2 Settings**：187/187。**B2 Static Audit**：
  82/82。**B2a**：106/106。**B1**：168/168。**G1.5-A**：140/140。
  **Geo Settings UI**：157/157。
- **`node --check`**（11 個核心修改檔案）：全部 OK。
- 殘留檢查：`data/pos.db` 不存在、無 `*.shp`/`*.dbf`/`*.prj` 殘留、
  `data/geo/authoritative/` 內只有 `SOURCE_MANIFEST.json`／
  `taiwan-admin-representative-points.v1.json` 兩個檔案（無 Raw SHP／
  官方 ZIP）、無真實 `.env`（只有安全的 `.env.example` 範本）。

**過程中發現並修正一個真實打包缺陷**：ZIP 排除規則原本用
`.env.*` 這個 glob pattern 排除環境變數檔案，但這個 pattern 意外連帶
排除了 `.env.example`（安全的範本檔案，不含真實密鑰，且被既有
`smoke-hotfix30-b5-r5-4-g1-5-a-ga4-backend-correctness.js` 讀取）——
導致該支既有測試在第一次乾淨解壓驗證時因為 `ENOENT` 而整支中斷。已
修正排除規則為只排除真實的 `.env`（精確比對，不用萬用字元），重新
打包後 `.env.example` 正確保留，該測試恢復正常，其餘先前已驗證通過
的項目未受影響。

**Clean Extraction：PASS。**

---

*本文件不含任何真實憑證、Property、Stream 或 Token 內容。*
