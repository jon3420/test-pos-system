# H1.4.10 Phase 4A — CART RECOVERY STATE ENGINE Implementation Report

## 修改/新增檔案清單

1. `utils/db.js` — 新增 `cart_recovery_jobs` 表 + 7 個索引（`CREATE TABLE/INDEX IF NOT EXISTS`，未 `DROP` 任何既有表）
2. `routes/settings.js` — 新增 `CART_RECOVERY_KEYS`（5 個設定）
3. `routes/analytics.js` — 新增 `sanitizePaymentStartedMetadata()`（`payment_method` 白名單）
4. `utils/analyticsLog.js` — `insertEvent()` 尾端新增 Recovery Engine hook（fail-open）
5. `public/line-order.html` / `public/line-shipping.html` — `payment_started` additive 補上 `metadata.payment_method`
6. `utils/cartRecovery.js`（新增）— Recovery State Engine 本體
7. `scripts/run-h1-4-10-phase4a-cart-recovery-state-runtime.js`（新增）— Phase 4A 專用測試（89 項）

## cart_recovery_jobs Schema

欄位：`id`／`store_id`／`cart_id`／`visitor_id`／`session_id`／`order_id`／`line_user_id`／`stage`／`status`／`trigger_event`／`last_event`／`recoverable_value`／`due_at`／`last_event_at`／`attempt_count`／`max_attempts`／`channel`／`idempotency_key`／`cancel_reason`／`sent_at`／`converted_at`／`cancelled_at`／`created_at`／`updated_at`。

索引：`store_id`／`(store_id,cart_id)`／`(store_id,order_id)`／`(store_id,due_at)`／`(store_id,status)`／`(store_id,stage)`／`(store_id,idempotency_key)`。

`status` 白名單全部保留（`pending`/`waiting`/`processing`/`sent`/`converted`/`cancelled`/`failed`/`not_contactable`/`not_configured`），但 Phase 4A 實際只會產生 `pending`/`cancelled`/`converted`（沒有 Push，不會有 `sent`/`failed`/`not_contactable`/`not_configured`，只是預留欄位不必之後再 `ALTER TABLE`）。

## Settings

| Key | 預設 |
|---|---|
| `cart_recovery_enabled` | `'0'` |
| `cart_recovery_cart_delay_minutes` | `60` |
| `cart_recovery_checkout_delay_minutes` | `30` |
| `cart_recovery_payment_delay_minutes` | `15` |
| `cart_recovery_max_attempts` | `1` |

**UI deferred to Phase 4B**：目前只有 backend settings contract（透過既有 `PUT /api/settings` 白名單機制），後台尚無「Recovery / CRM 設定區」的可視化欄位。

## 狀態機（utils/cartRecovery.js）

### 三個 canonical stage
`cart_abandoned` / `checkout_abandoned` / `payment_abandoned`。

### 事件驅動 Dispatch（onAnalyticsEvent，單一集中入口）

| event_name | 行為 |
|---|---|
| `add_to_cart` | `scheduleCartRecovery`（refresh due_at，不重複建立） |
| `checkout_click` | 取消 `cart_abandoned` → 建立/refresh `checkout_abandoned` |
| `checkout_submit_click` / `checkout_validation_failed` | 只 refresh 既有 `checkout_abandoned` 的 `last_event`/`due_at`，不新建 |
| `submit_order` | 取消 `cart_abandoned` + `checkout_abandoned`（**絕不**取消 `payment_abandoned`） |
| `payment_started` | 只在 `metadata.payment_method==='linepay'` 時，透過 `resolveAuthoritativeOrderForPaymentStart()` 解析出可信 order_id + 權威金額後建立/refresh `payment_abandoned` |
| `payment_success` / `purchase` | 該 cart/order 底下所有 stage 的 pending/waiting job 一律 `converted` |

### Idempotency
`cart_abandoned`/`checkout_abandoned` 用 `cart:{cart_id}:{stage}`；`payment_abandoned` 用 `order:{order_id}:{stage}`（透過 cart→submit_order 解析出的**真實** order_id，非 client 提供值）。同 idempotency_key 且狀態為 pending/waiting 時只 refresh，不新建。

### Fail-open
`_safe()` 包裹所有函式；`insertEvent()` 呼叫 hook 本身也包 try/catch。已用「讓 DB 操作全部 throw」與「讓 `onAnalyticsEvent` 本身 throw」兩種情境測試，確認 `analytics_events` 主表寫入完全不受影響。

## 安全邊界（本輪最重要的修正，詳見 Reality Audit）

- **`payment_started` 不信任 client `order_id`**：`onAnalyticsEvent()` 完全不讀取 `fields.order_id` 來建立 `payment_abandoned`，改用 `resolveAuthoritativeOrderForPaymentStart(db, storeId, cartId)` 從 `store_id+cart_id` 回查 server-authoritative 的 `submit_order`，再二次確認 `orders` 表存在該筆訂單。
- **`recoverable_value` 不信任 client metadata**：payment stage 的金額一律來自 `orders.total`（上一步驟查到的訂單）；`payment_started` 的 sanitizer 本身也只允許 `payment_method` 一個欄位，`value`/`cart_value` 等欄位在到達 Recovery Engine 之前就已被濾除。
- **`line_user_id` 只信任既有 `verifyMemberSession()` 解析結果**：`routes/analytics.js` 既有機制，未新增替代欄位。
- **Store isolation**：`resolveAuthoritativeOrderForPaymentStart()` 的 `submit_order` 查詢與 `orders` 二次確認都以 `store_id` 為必要條件，同一 `cart_id` 字串在不同店家下完全獨立解析。
- **`payment_success` client 偽造仍被拒**（Phase 3 既有契約）：即使偽造請求帶著真實存在的 `order_id`，也不會讓對應的 `payment_abandoned` job 被 converted（只有 `logServerEvent()` 真正寫入成功才會觸發 `markRecoveryConverted`）。

## Compatibility

- H1.4.9 `checkout_click` canonical contract 未變（67/67 PASS）。
- Phase 1 LIFF Auto Identify 未變（68/68 PASS）。
- Phase 2 Friend Guide 未變（83/83 PASS）。
- Phase 3 Checkout Submit Diagnostics / `payment_success` server authority 未變（111/111 PASS）。
- `utils/cartRecovery.js` 完全不呼叫任何第三方 Analytics 平台函式（`trackPlatformEvent`/`fbq`/`gtag`），Recovery 純粹是 backend state，不會產生額外的 GA4/Meta 事件。
