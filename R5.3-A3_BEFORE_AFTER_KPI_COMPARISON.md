# R5.3-A3 — Before / After KPI Comparison

以下數字皆來自本輪實際執行的真實 sql.js DB 測試（非估算），對應
`R5.3-A3_GEO_SYNC_VERIFICATION.md` 的情境 5（歷史資料落差，本輪最具代表
性的修正案例）。

## 情境：一個已經有 Analytics 歷史資料、但 `geo_visit_log` 同步 hook
是「之後」才部署的商店（Before = 修正前的實際行為；After = 本輪修正後）

| KPI | Before（修正前） | After（本輪修正後） | 說明 |
|---|---|---|---|
| Dashboard「進站訪客」（Funnel） | 2 | 2 | Dashboard 一直讀 `analytics_events`，不受本輪修正影響，Before/After 皆正確 |
| **Geo Intelligence「Geo 訪客」** | **0** ❌ | **2** ✅ | 修正後與 Dashboard 一致 |
| Geo Intelligence「Geo 加購」 | 0 ❌ | 1 ✅ | 同上 |
| Geo Unknown 比例 | 顯示不出來（因為 Geo Visitors 本身是 0，無從計算比例） | 50%（1 位 Unknown / 2 位總訪客） | Unknown 比例現在能正確反映真實狀態，不再被「查不到資料」掩蓋 |

## 情境：正常運作中的商店（新事件即時同步，Before 與 After 皆正確，用於
確認本輪修正沒有讓「本來就正常」的情境變差）

| KPI | Before | After | 說明 |
|---|---|---|---|
| Dashboard 訪客 | 1 | 1 | 不變 |
| Geo 訪客 | 1（前提：使用者手動切到 Visitor Layer 才看得到） | 1（切到 Heatmap 分頁後自動可見，不需要額外手動切換） | 數值相同，但 After 的可見性更好（見下方 UX 落差說明） |

## UX 落差對照（本輪找到的落差 A）

| 項目 | Before | After |
|---|---|---|
| 使用者進入 Heatmap 分頁後，Geo Event Engine 何時抓資料 | 只有手動切到「訪客熱區 Visitor Layer」才抓 | 進入 Heatmap 分頁即自動抓（不論目前顯示 Order 或 Visitor Layer） |
| 使用者只看預設畫面（Order Layer）時，能否看到 Geo 訪客數字 | 否（資料從未被抓取，數字停留在初始空狀態） | 是（資料已在背景抓好，切到 Visitor Layer 立即有數字，無需等待） |

## Regression 確認：本輪修正沒有改變任何既有 Order Heatmap 數字

| 項目 | Before | After |
|---|---|---|
| Order Heatmap 既有 Metric（Visitors/Orders/Revenue... 依 `getGeoFunnel()`） | 不變 | 不變（`geo-heatmap.js` 逐位元組未修改，見 Regression Report） |
| A1/A1.1/A1.2/A2 既有 Smoke 數量 | 128/112/188/228 | 128/112/188/228（完全相同，見 Regression Report） |
