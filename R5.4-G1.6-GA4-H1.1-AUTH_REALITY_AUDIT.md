# R5.4-G1.6-GA4-H1.1-AUTH — Reality Audit

本文件誠實記錄三個獨立問題各自的真實狀態。三者互相獨立，**不得混成同一個
問題**，也不得因為其中一個已修好就推論另一個也已解決。

---

## 一、H1 Frontend Auth Root Cause：**CONFIRMED**

### 症狀（使用者回報）
```
GET /api/analytics/ga4-geo/realtime  → 401
```
同時 GA4 官方 Realtime 畫面顯示同時間有活躍使用者，因此不是「GA4 沒有資料」。

### 根本原因（讀取 production 原始碼確認，非推測）

`public/js/geo-ga4-h1-panel.js` 的 `geoGa4H1Fetch()`（修正前）：

```js
const fetchOpts = { credentials: 'include' };
...
const res = await fetch('/api/analytics/ga4-geo/realtime', fetchOpts);
```

這是一個**裸 `fetch()`**，只帶 `credentials:'include'`（也就是只會帶
Cookie），完全沒有帶：

- `Authorization: Bearer <JWT>`（JWT 存在 `localStorage.pos_store_token`，
  不是 Cookie）
- `x-store-id`

而 `middleware/storeGuard.js` 的 `requireStore` 解析 store_id 的優先順序是：

1. `Authorization: Bearer <JWT>` payload 裡的 `store_id`
2. `x-store-id` header
3. `req.query.store_id`
4. 都沒有 → `401 NO_STORE_TOKEN`

裸 `fetch()` 三個來源都沒帶到，所以正式環境必然落在第 4 種情況，回
`401 NO_STORE_TOKEN`。這跟「GA4 沒資料」完全無關，是純前端的 Auth Header
缺失問題。

同一份程式碼裡的 `geo-ga4-realtime-layer.js`（G1.5 既有的 GA4 即時圖層）
本輪之前已經改用 `apiFetch()`，所以它的 502 是**另一個獨立問題**（見下）。

### H1 Frontend Auth Code Fix：**IMPLEMENTED AND AUTOMATED-TESTED**

修正內容（只動 `public/js/geo-ga4-h1-panel.js`，未動
`middleware/storeGuard.js`、未新增第二套 Auth）：

- 新增 `geoGa4H1ApiRequest()`：唯一入口，優先用 `window.apiFetch`，其次
  `apiFetch`（bare identifier），跟 `geo-ga4-realtime-layer.js` 同一個判斷
  慣例。
- 三支 API（`realtime` GET／`history` GET／`sync` POST）全部改走這個
  helper，程式碼裡不再出現任何 `fetch('/api/analytics/ga4-geo...` 的裸呼叫
  （由 Static Audit + Browser Auth Runtime 兩層驗證）。
- 不重新讀取 `localStorage.pos_store_token`、不自己組
  `Authorization`／`x-store-id`、不解析 JWT payload（沿用既有 `apiFetch()`
  Contract，見下方「未建立第二套 Auth」）。

### 正式 Browser 401：**READY FOR MANUAL DEPLOYMENT VERIFICATION**

以上修正已經用「載入真實 `app.js` 原始碼（byte-exact 擷取，不是重寫）＋
真實 `geo-ga4-h1-panel.js`」的方式在自動化測試裡驗證過（見 Test Report）。
但正式瀏覽器（Chrome／Edge／Android／iOS／LINE In-App Browser）尚未實際
部署驗證，狀態維持 **READY FOR MANUAL DEPLOYMENT VERIFICATION**，不寫
PASS。

---

## 二、GA4 Backend 502 Root Cause：**NOT YET DETERMINED**

### 症狀
```
GET /api/geo-live/ga4-realtime → 502
```
這是完全獨立於上面 401 問題的 Backend Gate，本輪**沒有修正、也沒有假裝
修正**這個問題。

### 已確認的事實（讀取 production 原始碼）

- `routes/geo-live.js` 的 `/ga4-realtime` 端點呼叫
  `utils/ga4Realtime/index.js` 的 `getGa4RealtimeData()`，任何
  `Ga4RealtimeError` 都會被轉成安全的 `{success:false, code, stage,
  message, retryable, status}`，HTTP status 依 `e.httpStatus` 決定（可能是
  502）。
- 既有的安全診斷端點（本輪**沒有新增**，已經存在）：
  - `GET /api/geo-live/ga4-realtime-status`（不呼叫 Google，回安全欄位）
  - `POST /api/geo-live/ga4-realtime-test`（呼叫一次 Google，回安全分類
    結果：`credential_available`／`sdk_available`／`property_accessible`／
    `summary_request_ok`／`city_request_ok`／`error_stage`／`error_code`
    等，見 `utils/ga4Realtime/connectionTest.js`）。
  - 這兩個端點已經在既有的 `public/js/geo-ga4-settings.js` 的 GA4 設定頁
    裡完整串接（連線測試按鈕、Cooldown、錯誤碼對照表），本輪確認過這套
    UI 已經滿足「顯示 credential_available／sdk_available／
    property_accessible／summary_request_ok／city_request_ok／error_stage／
    error_code」的需求，**沒有重複建立第二個診斷 UI**。
- 本輪新增的 `scripts/run-g1-6-ga4-h1-1-ga4-diagnostic-contract.js` **只**
  驗證這套診斷 Contract 本身的安全性與正確分類邏輯（用 Fake GA4 Client
  模擬 permission_denied／SDK unavailable／summary 成功 city 失敗等情境），
  **完全沒有連到真實 Google API**，因此無法、也不試圖判斷正式環境的 502
  實際原因是哪一種（憑證未設定？Property 權限？Property 未綁定？）。

### 不得做的推論

- 不得因為「GA4 官方網站看得到活躍使用者」就推論 Server 端 API 也應該
  連得上（兩者是不同的驗證路徑：官網用使用者的 Google 帳號登入權限，
  Server API 用 Service Account 憑證）。
- 不得因為 H1 Auth 修好了就宣稱 GA4 502 也解決——這是本文件刻意分成兩節
  的原因。

### GA4 Backend Production Gate：**READY FOR MANUAL DIAGNOSTIC VERIFICATION**

正式判定 502 的根本原因，必須實際在部署環境按「GA4 設定」頁的「連線測試」
按鈕，記錄真實的 `credential_available`／`sdk_available`／
`property_accessible`／`summary_request_ok`／`city_request_ok`／
`error_code` 組合（見 Manual QA Checklist 第 15–23 項），才能分類到
Case A/B/C/D 其中之一。本文件與本輪自動化測試都無法、也不宣稱已經完成
這個判定。

---

## 三、AbortError Root Cause：依實際追蹤結果

### 追蹤範圍
`public/js/geo-ga4-h1-panel.js`、`public/js/geo-ga4-realtime-layer.js`、
`public/js/geo-heatmap-ui.js`、inline onclick、event listener、fire-and-
forget 呼叫、AbortController、`apiFetch()`、Response body parsing——完整
Promise Chain：

```
button click / addEventListener callback
  → geoGa4SetWindow / geoGa4SetMetric / geoGa4Refresh（layer）
    或 modeHandler / metricHandler / syncHandler（H1 panel）
  → _geoGa4RunFetch / geoGa4H1Refresh
  → geoGa4FetchData / geoGa4H1Fetch → geoGa4H1ApiRequest
  → apiFetch
  → fetch
  → Response parsing (res.json())
```

### 發現

1. **`geo-ga4-h1-panel.js`（本輪修正的檔案）**：修正前的裸 `fetch()` 沒有
   任何 AbortError 邊界問題（因為它也沒有真正的 Auth，401 立即回來，不會
   卡在 in-flight 狀態太久），但仍缺乏集中化的 Abort-safe wrapper。修正
   後新增 `geoGa4H1SafeRunFetch()` 作為唯一的 AbortError 吞噴點：只吞
   `error.name==='AbortError'`，其他錯誤原樣往上拋，不得把所有錯誤都
   `return null`（見 Mutation H 的攔截驗證）。`geoGa4H1ApiRequest()` 對
   401/403 特殊物件與原生 Response 兩種 Contract 都不假設 `res.json`
   一定存在。事件監聽器（`modeHandler`／`metricHandler`）與 Init 的
   fire-and-forget 呼叫都額外加了 `.catch(...)` 邊界（雙重防禦，即使
   `geoGa4H1Refresh()` 內部已經不會對 AbortError 往外 reject）。

2. **`geo-ga4-realtime-layer.js`（既有的 G1.5 GA4 圖層，本輪未修改）**：
   讀取原始碼確認 `_geoGa4RunFetch()` 的 `try/catch` 已經把 AbortError
   與其他錯誤都轉成安全的 UI Error Payload，不會往外 reject——**本輪
   刻意沒有修改這個檔案**（不重做 GA4-H1 架構，不建立第二套 GA4
   Client）。但這個 catch 目前是「所有錯誤都吞成同一種 UI 狀態」，比
   AbortError 特判更寬鬆；本輪用 Mutation J 證明「如果拿掉這層
   try/catch，AbortError 會真的變成 unhandledRejection」，藉此確認這層
   保護是真的在生效、不是巧合。

3. 正式 Console 回報的 `Uncaught (in promise) AbortError` 目前**主要對應
   的是 H1 Panel 修正前的行為**（快速切換 Mode/Metric 時，舊版
   `geoGa4H1Fetch()` 會 abort 前一個 in-flight request，但當時的呼叫鏈
   缺乏集中化保護——本輪已經補上，見 Test Report 的 Browser Auth Runtime
   #39–42、#46–47）。無法百分之百排除正式環境還有其他未追蹤到的呼叫
   路徑（例如尚未列出的第三方 SDK／瀏覽器擴充功能），因此下方 Gate 維持
   「READY FOR MANUAL DEPLOYMENT VERIFICATION」，不寫 PASS。

### Abort Safety Automated Gate：**PASS**
（見 Test Report；47/47 Browser Auth Runtime + 24/24 Mutation Suite，
連續三次一致）

### Abort Safety Production Gate：**READY FOR MANUAL DEPLOYMENT VERIFICATION**

---

## 四、Store Isolation

`SINGLE_STORE_ONLY`——本輪沿用既有規則，未變更。GA4 Diagnostic Contract
測試額外驗證了 query string／POST body 都不能覆寫 JWT 決定的
`req.storeId`（見 Test Report Scenario 5/6）。

## 五、POS Individual Visitor Mapping

`NOT IMPLEMENTED` / `OUT OF SCOPE`——本輪未觸碰。
