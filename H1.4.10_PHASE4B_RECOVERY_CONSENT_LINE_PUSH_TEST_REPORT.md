# H1.4.10 Phase 4B — RECOVERY CONSENT × LINE PUSH DELIVERY Test Report

## 八層 Final Gate（fresh-run）

```
Gate 1 Phase 4B Full Suite      : TOTAL=168 PASS=168 FAIL=0
Gate 2 Backend Recovery Targeted: TOTAL=92  PASS=92  FAIL=0
Gate 3 Frontend Recovery Targeted:TOTAL=50  PASS=50  FAIL=0
Gate 4 Phase 3                  : TOTAL=111 PASS=111 FAIL=0
Gate 5 Phase 4A                 : TOTAL=89  PASS=89  FAIL=0
Gate 6 Phase 2                  : TOTAL=83  PASS=83  FAIL=0
Gate 7 Phase 1                  : TOTAL=68  PASS=68  FAIL=0
Gate 8 H1.4.9                   : TOTAL=67  PASS=67  FAIL=0
```

## Full Phase 4B Suite 組成（168 assertions，達成 ≥100 正式 acceptance 門檻）

本輪擴充自 42 個 assertions 到 168 個，全部是真正 feature-level integration checks（不同 contract／不同 state／不同 security boundary／不同 E2E transition），沒有為了湊數字機械拆分既有斷言。Targeted 套件（Backend 92／Frontend 50）不計入這 168，只作為獨立的 Composition Gate（2 個 assertion，驗證兩支套件本身仍 FAIL=0）——三者在報告中明確分開列示，不合併宣稱總數。

### Part A：Admin Settings UI（21 assertions，沿用原有）
逐字擷取 production HTML/JS 片段執行，驗證 card 存在、default 全關、load/save/clamp、token readiness 布林化。

### Part B：兩個完整 End-to-End Scenario（19 assertions，沿用原有）
購物車提醒後成交、LINE Pay 付款提醒後成交，各自完整跑過 Push→Restore→Checkout→Purchase／Push→Resume→Confirm→Purchase 全鏈路。

### Part D：Consent Full Integration（12 assertions，新增）
真實 HTTP `routes/cart-recovery.js` `/consent`：無 member_session 拒絕、raw UID 不可信、valid session grant 成功且欄位正確（`consent_text_version`/`status`/`granted_at`）、同 cart 重複 grant 不 duplicate、revoke 保留 row 且 `revoked_at` 存在、revoke 後 job 仍存在但 eligibility false、跨店 session 無法操作他店 consent。

### Part E：Recovery Settings Backend Integration（10 assertions，新增）
新店 default 全部驗證（master/LINE/三個 delay/max_attempts），真實 `PUT /api/settings` 驗證 master/LINE/custom delay 真的生效，完全沒有任何 key 的舊店仍安全 fallback OFF。

### Part F：Eligibility Full Integration Matrix（14 assertions，新增）
14 個獨立條件（master/LINE/due/converted/cancelled/consent missing/consent revoked/member missing/friend false/friend unknown/token missing/LIFF missing/attempt max/全部符合）逐一單獨驗證，每條都檢查對應的 canonical reason_code，不只是 boolean。

### Part G：Push Processor Full Integration（15 assertions，新增）
成功送出（sent_at/attempt_count/channel/status 四個欄位）、同 processor 重跑不重送、consent revoke 後 0 push、friend false 後 0 push、token missing → not_configured、transport failure → failed 且 max_attempts=1 不重送、Push message 內容不含姓名/電話/地址/raw UID。

### Part H：Recovery URL Full Integration（14 assertions，新增）
line_order/line_shipping 各自對應正確 LIFF、shipping LIFF 缺失時 fallback member LIFF、全部 LIFF 缺失時無 URL、URL 內容不含 cart_id/order_id/line_user_id 只有 opaque token、token page_type 正確記錄、wrong page/wrong member/expired/converted job 皆安全拒絕（含真實 HTTP 驗證 `already_completed` defense-in-depth）。

### Part I：Cart/Checkout E2E 再補（8 assertions，新增）
checkout_click 前 job 已 sent、checkout_click 不產生第二筆 cart job、submit_order 取消 checkout-stage recovery、purchase 後相關 job converted 且 token invalidated、`sent_at` 保留／`converted_at` 有值、第二次 purchase 狀態仍是 converted（idempotent）。

### Part J：Payment E2E 安全性再補（18 assertions，新增）
client 偽造 total/order_id 完全被忽略（DB request amount 仍是真實 `order.total`）、response 不回 order_id/total、LINE Pay request 失敗不寫 payment_started／成功才 +1、`payment_started` 的 cart_id/order_id 皆為 token 綁定的真實值、並發 2 個 request 只 1 次 LINE API call、`payment_requested` 不可直接再 claim、cancel-return reset 後才能重試、payment_success/purchase 後 job converted、old token 不可再 resume（0 API call）、purchase 恰好 1 筆、orders 筆數不變。

### Part K：External Analytics Contract（8 assertions，新增，靜態）
`checkout_click→GA4 begin_checkout`／`→Meta InitiateCheckout` 映射鎖定；`cart_updated`/`cart_restored`/`payment_success`/`checkout_submit_click`/`checkout_validation_failed` 確認未被加入 GA4/Meta 映射表（`payment_started` 本身是既有合法的 e-commerce 漏斗事件映射，Recovery 沿用同一個 event_name，不是新事件，故不檢查其排除）；`purchase→GA4 purchase` 映射存在；`utils/cartRecovery.js`／`utils/cartRecoveryDelivery.js` 完全不呼叫任何第三方 Analytics 平台函式；frontend Recovery 區塊（排除說明性註解）不呼叫 `_trackEvent('payment_started')`。

### Part L：Security Full Acceptance（15 assertions，新增）
`signLinePayPost()`/`signLinePayGet()` 回傳值不含簽章 preimage；`routes/linepay.js` 程式碼（排除說明性註解）不再有 `messagePreview:` 欄位賦值；三個呼叫端皆改用 `linePayClient` SSOT；兩頁 production 原始碼皆無 test-only hook 殘留；client 偽造 `payment_success` 真實 HTTP 驗證仍 400/403 且未寫入。

### Part M：Admin UI Master/Line Semantics（8 assertions，新增）
master OFF/ON 對 LINE checkbox 的 disable/enable 聯動、master OFF 不清除既有 LINE 設定值、hint 正確顯示、settings save payload 不含 `line_channel_token`、token readiness 只顯示布林狀態（不同 token 值皆同樣顯示）、卡片文案包含關鍵說明字句。

### Part C：Composition Gate（2 assertions，獨立列示，不計入上述 168 的 feature-level 部分但屬於 Full Suite 總數）
真實 `child_process.execFileSync` spawn 兩支既有 Targeted 檔案，解析輸出並要求 `FAIL=0`。

## 本輪擴充中發現的問題分類（誠實記錄）

第一輪執行 168 assertions 時出現 4 個 FAIL，逐一分類：

1. **AN5/AN6-partial（`payment_started` 誤判未加入 GA4/Meta 映射）**：**測試斷言錯誤**，非 production bug。`payment_started` 本身早在 Phase 3 之前就合法對應 GA4 `AddPaymentInfo`／Meta `add_payment_info`（標準 e-commerce 漏斗事件），Recovery backend authority 沿用同一個 event_name 寫入，不是發明新事件。已修正斷言，不檢查 `payment_started` 是否被排除在映射表外。
2. **AN3b（誤判 frontend 呼叫 `_trackEvent(payment_started)`）**：**測試斷言錯誤**。原始碼裡確實有字串 `_trackEvent('payment_started'` 出現在 Recovery 區塊，但那是一行**說明性註解**（解釋「這裡故意不呼叫」），不是真的函式呼叫。已修正斷言為排除註解行後再檢查。
3. **SEC12（誤判 `messagePreview` 殘留）**：**測試斷言錯誤**，同樣是說明性註解文字（記錄上一輪修復內容）被誤判成程式碼殘留。已修正斷言為排除註解行、只檢查真正的欄位賦值語法。

四個問題全數是測試本身對「原始碼字串比對」過於天真（把出現在註解裡的說明文字誤判成真實程式碼），**沒有一個是 production 邏輯錯誤**，因此本輪未修改任何 production 檔案，只修正了 `scripts/run-h1-4-10-phase4b-recovery-full-runtime.js` 本身的斷言邏輯。

## Backend Recovery Targeted（92/92，涵蓋範圍摘要）

Cart Snapshot Restore Authority（含 variant fail-closed）、Recovery Target Resolver（line_order/line_shipping）、LIFF-only Recovery URL（無 LIFF 直接拒絕，不 fallback 一般網址）、Token 安全拒絕（uid_mismatch/wrong_store/expired/wrong_page）、Payment Token resume_type 區分（不回 cart）、Existing-Order Payment Resume（server-authoritative amount/order，client 假值完全無效，無 duplicate order）、Payment Resume Concurrency（真正 CASE A 延遲並發 + CASE B 快速成功後緊接第二次點擊，皆驗證只有 1 次 LINE API call）、Cancel-Return Retry（獨立 reset endpoint，7 個安全條件全驗）、Token Horizon（不超過 Phase 4A 24h horizon）、Race Cancellation（token 安全作廢）、sent→converted、Recovery Token Invalidation after conversion（cart 與 payment 兩種 token 皆驗，checkout purpose token 不受影響）、Recovery payment_started Backend Authority（cart_id/order_id 來自 token 而非 client，8 個情境）、Restore Job Authoritative Recheck（job 必須是 `sent` 才允許 restore，defense-in-depth）。

## Frontend Recovery Targeted（50/50，涵蓋範圍摘要）

line-order/line-shipping Cart Restore（真實 HTTP request body 驗證 `page_type` 正確、不含 line_user_id/cart_id/order_id/total）、Checkout Restore（checkout stage visible，`checkout_click` 真實 HTTP analytics event delta=0，`#goCheckoutBtn` 去重旗標未被 fake 觸發）、Wrong-Page Fail-Closed（cart 不變）、No-Member 行為（0 次 restore call、0 次 `liff.login`、一般頁面功能不受影響）、Payment Panel（page load 0 次 resume-payment、真實點擊才 1 次、request body 只有 member_session+recovery_token、成功導頁、frontend 不重複送 payment_started、真正的雙擊防護——第二次點擊在第一次回應回來前送出仍只有 1 次 fetch）、Cancel-Return（payment-cancelled 1 次、resume-payment 0 次、面板顯示、使用者重新點擊才變 1 次）、Security（token 不進 HTTP analytics event、不進 localStorage、URL 已清除、backend reason 被 mapping 成安全文案而非直接顯示）、Ordering（Recovery 最終覆蓋舊本地購物車，非被覆蓋）、Bootstrap Architecture（不用 timer，真實 await Promise completion）、Static Safety（Recovery 區塊無 console.log token、無寫入 localStorage/sessionStorage token）。

## Related Smoke Regression（實際執行）

| Script | 結果 |
|---|---|
| `run-h1-4-7-analytics-contract-runtime.js` | 61/61 |
| `run-g1-6-ga4-h1-4-7-two-stage-checkout-runtime.js` | 113/113 |
| `smoke-hotfix29-c.js` | 54/54，MANUAL=2（既有） |
| `smoke-hotfix30-b5-r5-2-a-order-geo-write.js` | 90/90 |
| `smoke-hotfix30-b5-r5-debounce.js` | 32/32 |
| `smoke-hotfix30-c1-rollback.js` | 22/22 |

## Baseline Known Failures（誠實記錄，非本輪 regression）

`smoke-hotfix30-b5-r5-2-a-integrated.js` 的 **M3**（`/cart-attribution`）：`Cannot read properties of undefined (reading 'district_ranking')`，`utils/cartSnapshot.js`／`utils/cartGeoAttribution.js` 既有 bug。本輪重新確認錯誤訊息、行號與過去所有 Phase（1/2/3/4A）記錄的完全相同，與本輪 Recovery/Consent/LINE Push/Admin UI 程式碼路徑無關，未 opportunistic 修復。

其他已知 baseline（未在本輪觸碰，沿用既有記錄）：`smoke-hotfix26-c`（`renderFriendStatus(null)` 文字不符）、`smoke-hotfix26-e/-verify-debug/-verify-deep`（cascading + seed data）、`smoke-hotfix26-f2`（`/timeslots` 日期邏輯）、`smoke-hotfix26-i`（test mock DOM 缺 `removeAttribute`）。

## Residue Audit

搜尋 `DEBUG_RESUME_PAYMENT`、`console.log`/`warn`/`error` 印出 recovery_token/member_session/line_channel_token/channelSecret 的模式、`__RECOVERY_NAVIGATION_HOOK__`/`__RECOVERY_TEST__`/`TEST_ONLY_RECOVERY`、`TODO Phase4B`/`FIXME Phase4B`——全部**沒有找到**任何殘留。`routes/line-webhook.js` 唯一一處印出 secret 相關字樣的 log 本身就是既有、正確的 masked 版本（`_maskSecret()`），非本輪引入。

## Schema Audit

`cart_recovery_jobs`（Phase 4A）、`cart_recovery_consents`（本輪新增）、`line_cart_handoff_tokens` 的 4 個 additive 欄位（`purpose`/`recovery_cart_id`/`resume_type`/`recovery_page_type`）全部使用 `CREATE TABLE/INDEX IF NOT EXISTS` 或 `ALTER TABLE ADD COLUMN ... DEFAULT`，無任何 `DROP TABLE`/`DROP COLUMN`/destructive rebuild。

## Settings Audit（Release Blocker 檢查）

`cart_recovery_enabled` 預設 `'0'`、`cart_recovery_line_enabled` 預設 `'0'`、delay 預設 60/30/15、`max_attempts` 預設 `1`——確認未升級的既有店家不會因為這次部署突然開始對顧客發送任何提醒。此為 release 前必須確認的硬性條件，已用 Admin UI Part A 的 default-settings 測試（A2-A5）與 backend `utils/cartRecovery.js`/`utils/cartRecoveryDelivery.js` 的 fallback 邏輯雙重驗證。

## Security Boundary 總覽（對應 Full Suite 涵蓋範圍）

client 偽造 `payment_success` 仍 400/403 且不 convert recovery（Phase 3 既有契約延續）；raw client `line_user_id`/`order_id`/`amount` 全部不可信（backend 結構上不讀取這些欄位當 authority）；`recovery_token` 不進 analytics HTTP event、不進 `localStorage`、從 URL 立即清除；LINE Channel Token／LINE Pay Secret 不出現在任何 log 或 UI（含本輪修復的一處既有洩漏）；簽章 preimage 不再從 `signLinePayPost()`/`signLinePayGet()` 回傳；production 原始碼確認無任何 test-only hook 殘留。
