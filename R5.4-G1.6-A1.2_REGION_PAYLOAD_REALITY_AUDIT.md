# R5.4-G1.6-A1.2 — Region Payload Reality Audit

fix18-10-hotfix30-B5-R5.4-G1.6-A1.2

## 舊 marker endpoint

`GET /api/geo-live/markers`（`utils/geoVisitLog.js` 的
`getGeoLiveMarkerPoints()`）——本輪**完全未修改**。查詢 JOIN
`utils/geoLiveCoordinate.js` 提供的真實座標（使用者裝置回報的
Browser Geolocation 等），只回傳「有真實座標」的列：

```js
const coord = (r.visitor_id && byVisitor.get(r.visitor_id))
  || (r.session_id && bySession.get(r.session_id))
  || null;
if (!coord) continue;
```

## 為何過濾對 Exact Marker 是正確的

這個過濾防止「沒有真實座標的訪客被畫成看起來很精確的 Marker」——完全
正確，本輪刻意保留，不刪除。

## 造成的限制

過濾也意味著：只有 `city`／`district`（沒有真實座標）的訪客事件，過去
完全不會出現在 `/markers` 回應裡，Dashboard 端因此永遠沒有機會為這些
訪客畫出任何 Marker（即使是推估的也沒有）。

## 為何不能只刪除過濾

即使移除 `if (!coord) continue`，讓這些列也送到前端，前端仍然沒有辦法
把 `city`／`district` 換算成合法座標——移除過濾本身不解決根本問題，只會
讓 API 回應混入一堆「有名字沒座標」的假 Exact 資料，讓前端誤判成精確
定位。因此本輪**不修改**這個過濾邏輯，改成新增獨立 Endpoint。

## 新 marker-model

`GET /api/geo-live/marker-model`（`getGeoLiveMarkerModel()`）：

1. **Exact Query**：直接呼叫既有 `getGeoLiveMarkerPoints()`（不重複
   查詢邏輯，同一份程式碼）。
2. **Region Aggregation Query**：另外查詢同一個 `store_id`／時間窗內
   的所有事件列（`city`／`district`／`is_unknown`／`event_name`），
   排除已經在 Exact 集合裡的 `visitor_key`。
3. **Representative Point Enrichment**：對每個候選列呼叫
   `resolveAreaRepresentativeMarker()`——先用既有
   `resolveTaiwanAdministrativeArea()`（B2.5／R5.2 引擎）把
   `city`／`district` 安全轉成 `county_code`／`subdivision_code`
   （無法唯一辨識就回 unknown，不猜），再用
   `authoritativeAdminPointCatalog.resolveAdministrativeRepresentativePoint()`
   查表拿真實座標。
4. **Exact Entity Exclusion**：已在步驟 2 排除，確保同一個
   `visitor_key` 不會同時出現在 exact 與 estimate。
5. **Estimate Aggregation**：同 `accuracy`＋`district_code`／
   `county_code` 聚合成一個 Marker，`count`＝unique visitor 數，
   `event_count`＝事件總次數（分開計算，不用事件次數畫多個 Marker）。
6. **Unknown Count**：`is_unknown=1` 或查無 Representative Point 的
   unique visitor 數（後者計為「已知但無法顯示」，不誤標成完全未知，
   但目前實作為求簡單，仍歸入同一個 unknown 統計桶——見 Test Report
   已知限制）。

## Dedupe

Entity Key：`store_id + visitor_key`（`VISITOR_KEY_SQL`，既有
canonical 定義，同時涵蓋 `visitor_id`／`session_id` 語意）。同一個
entity 只能出現在 `exact_points`、`estimate_points`、或 unknown
count 三者之一，已用真實 sqlite fixture 測試驗證（見
`scripts/smoke-hotfix30-b5-r5-4-g1-6-a1-2-authoritative-points.js`
section O）。

## Partial

Catalog 不可用：`status: 'partial'`，`exact_points` 保留，
`estimate_points: []`，`capabilities.catalog_available: false`，
`error_code: 'catalog_unavailable'`（或更精確的
`catalog_missing`／`catalog_invalid` 等，視實際失敗原因）。

Region Query 本身失敗（例如 SQL 錯誤）：同樣 `status: 'partial'`，
`error_code: 'region_query_failed'`，Exact 完全不受影響（`try/catch`
包住 Region 查詢，不包 Exact 查詢，兩者失效互不影響）。

## Privacy

`estimate_points` 的 `id` 欄位格式為 `district:<district_code>` 或
`county:<county_code>`——不可逆、非個人識別，不含 `visitor_id`／
`session_id` 字面值。回應完全不含 raw IP、完整 visitor_id、完整
session_id、地址、Token、Cookie、GA4 identity。

## Backward Compatibility

`/api/geo-live/markers` 舊 Endpoint 的路由定義、Handler 函式呼叫
（`_safeHandler(getGeoLiveMarkerPoints)`）逐字保留，未被任何本輪修改
觸碰。`/api/geo-live/marker-model` 是全新、獨立的路由，兩者互不影響。

---

*本文件不含任何真實憑證、Property、Stream 或 Token 內容。*
