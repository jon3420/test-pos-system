# R5.4-G1 — Geo Intelligence V2：Live Geo Map + Heatmap Foundation
## Architecture Document

fix18-10-hotfix30-B5-R5.4-G1

---

## 1. 設計原則

1. **絕不假造座標。** Live Marker 唯一合法座標來源是使用者裝置自己回報的真實座標（Browser Geolocation API）。IP 推估座標、店家座標、行政區中心點座標一律不得畫成訪客 Marker。
2. **Unknown ≠ Empty。** Unknown 是「有訪客但沒有真實座標／無法辨識地理位置」；Empty 是「這個篩選條件下沒有符合的事件」。兩者的空狀態文案與統計口徑不同。
3. **重用既有基礎設施，不建立平行系統。** GeoLiveLayer 重用既有 Leaflet map 實例（`window.geoMapState.instance`）、既有 `classifySource()`、既有 `geo_visit_log` 表與既有 Store Isolation 慣例，不新建第二套 Analytics、第二張地圖、或第二套 channel 分類邏輯。
4. **Additive only。** 所有 DB schema 變更一律是 `ALTER TABLE ADD COLUMN` 或全新獨立表，絕不 `DROP`／重建既有表。所有新路由掛在獨立的 `/api/geo-live` 前綴下，不修改既有 `/api/analytics/*` 端點的行為。

---

## 2. Coordinate Acquisition（真實座標取得）

### 2.1 允許的座標來源

| 來源 | 說明 | 目前狀態 |
|---|---|---|
| `browser_geolocation` | `navigator.geolocation.getCurrentPosition()`（W3C Geolocation API）。桌面瀏覽器多半用 Wi-Fi/IP 訊號估算；手機瀏覽器在使用者同意後，作業系統會視裝置能力直接動用 GPS 晶片——**這正是需求文件「GPS（手機）」在 Web 架構下的實際路徑，不是另一支獨立 API**。 | ✅ 唯一實際運作的來源 |
| `google_geolocation_api` | Google Geolocation REST API（`https://www.googleapis.com/geolocation/v1/geolocate`），設計上只有在呼叫端真的握有 Wi-Fi AP／基地台訊號時才允許使用；若沒有真實訊號輸入，Google 端會自動退回 IP 估算——這正是使用者明確禁止的行為。 | ⚠️ 白名單已預留，但純網頁環境沒有讀取 Wi-Fi/Cell 訊號的權限，**目前不可用**（`GET /api/geo-live/coordinate-sources` 誠實回報 `google_geolocation_api_available:false`） |
| IP 推估 / 地址反解析中心點 / 行政區中心點 | — | ❌ 明確禁止，任何一處都不會被 `validateCoordinate()` 接受 |

### 2.2 Consent Flow（顧客頁面端）

`public/js/geo-live-coordinate.js`（`GeoLiveCoordinate`）：

```
GeoLiveCoordinate.init({ storeId, getVisitorId, getSessionId })   // 不會呼叫定位 API
  │
  ├─ requestNow()                 // 使用者主動按「使用目前位置」按鈕才呼叫
  ├─ requestOnDeliveryFlow()       // 使用者聚焦外送地址欄位時呼叫（見 line-order.html）
  │     └─ shouldAttemptGeolocation(record, now) 判斷是否該問：
  │           - 從未問過        → 可以問
  │           - 曾經 denied     → 7 天 cooldown 內完全不問
  │           - 曾經 granted    → 距離上次成功 < 5 分鐘不重問（背景更新節流）
  │           - timeout/其他    → 允許使用者主動再試
  └─ backgroundUpdateIfGranted()   // 只有「已同意過」才會嘗試背景靜默更新
```

`init()` 本身**不會**呼叫 `getCurrentPosition()`；頁面載入時不會有任何定位提示彈出。

### 2.3 Status Flow

`navigator.geolocation.getCurrentPosition(onSuccess, onError, { enableHighAccuracy:true, timeout:8000, maximumAge:60000 })`

```
onSuccess → status='granted' → POST /api/geo-live/coordinate { lat,lng,accuracy_m,source:'browser_geolocation',status:'granted' }
onError   → PositionError.code:
              1 (PERMISSION_DENIED)  → status='denied'
              2 (POSITION_UNAVAILABLE) → status='unavailable'
              3 (TIMEOUT)            → status='timeout'
              其他                    → status='error'
無 navigator.geolocation           → status='unsupported'
```

七種狀態列舉（`GEO_COORD_STATUSES`）：`granted / denied / timeout / unavailable / unsupported / error / unknown`。

---

## 3. DB Schema（Additive）

### 3.1 `geo_visit_log` 新增欄位（既有表，`ALTER TABLE ADD COLUMN`）

| 欄位 | 型別 | 說明 |
|---|---|---|
| `postal_code` | TEXT | 沿用既有 `analytics_events.geo_postal_code`（Geo Resolver 目前預設關閉，實務上多為 NULL） |
| `channel` | TEXT | 重用既有 `utils/analyticsV2.js` 的 `classifySource()`，不重寫第二套分類 |
| `device_type` | TEXT | 新增 `utils/deviceParser.js` 對呼叫端提供的 User-Agent 做決定性分類 |

### 3.2 `geo_live_coordinates`（全新獨立表 — 唯一真實座標來源）

```sql
CREATE TABLE geo_live_coordinates (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  store_id TEXT NOT NULL, visitor_id TEXT NOT NULL, session_id TEXT NOT NULL,
  lat REAL NOT NULL, lng REAL NOT NULL, accuracy_m REAL,
  source TEXT NOT NULL, captured_at TEXT NOT NULL DEFAULT (datetime('now')),
  created_at TEXT DEFAULT (datetime('now'))
)
```

只在 `status='granted'` 且 `validateCoordinate()` 驗證通過時寫入一列。**每一次成功回報都新增一列**（不是 UPDATE），Marker 查詢一律取「每位訪客最新一列」（`getLatestCoordinatesByVisitor()`），因此同一訪客更新更精確座標時，Marker 永遠只顯示最新一筆，不會重複。

### 3.3 `geo_coordinate_status_log`（全新獨立表 — 同意狀態稽核軌跡）

```sql
CREATE TABLE geo_coordinate_status_log (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  store_id TEXT NOT NULL, visitor_id TEXT NOT NULL, session_id TEXT NOT NULL,
  status TEXT NOT NULL, source TEXT, accuracy_m REAL,
  captured_at TEXT NOT NULL DEFAULT (datetime('now')),
  created_at TEXT DEFAULT (datetime('now'))
)
```

記錄**每一次**定位嘗試的結果（granted 也會記一筆），供 Coverage KPI 使用；`geo_live_coordinates` 只保留「granted 且驗證通過」的列，兩表職責明確分離。

---

## 4. API（`/api/geo-live`，掛載於 `requireStore` 之下）

| Method | Path | 保護層級 | 說明 |
|---|---|---|---|
| GET | `/markers` | `requireFeature('reports')` | 真實座標 Marker（見 §5） |
| GET | `/unknown-pool` | 同上 | Known/Unknown/Mappable 統計 |
| GET | `/districts` | 同上 | District 聚合排行 |
| GET | `/postal` | 同上 | Postal 聚合排行 |
| GET | `/heat-summary` | 同上 | Top5 熱區 |
| GET | `/replay` | 同上 | 時間桶（Replay 用） |
| GET | `/visitor/:key` | 同上 | 單一訪客 Timeline + 漏斗（遮罩） |
| GET | `/filters` | `requireFeature('reports')` | Channel/Device/時間範圍列舉 |
| GET | `/coverage` | 同上 | 定位同意 Coverage KPI |
| GET | `/coordinate-sources` | 同上 | 誠實揭露目前可用座標來源 |
| POST | `/coordinate` | **無 feature gate**（僅 `requireStore`） | 顧客頁面公開回報座標／同意狀態 |

`POST /coordinate` 是唯一對顧客開放、不需要後台權限的端點；其餘全部只給有 `reports` 授權的後台使用者。

---

## 5. Marker Query（`getGeoLiveMarkerPoints`）

```
geo_visit_log（事件 metadata：city/district/postal/channel/device/最後事件）
        │  依 visitor_id 優先、session_id 其次 JOIN
        ▼
geo_live_coordinates（每位訪客最新一筆真實座標）
        │
        ▼
只有「兩邊都能對得上」或「至少有真實座標」的訪客才輸出一個 Marker：
  - 有 geo_visit_log 事件 + 有真實座標 → 完整 Marker（含 city/district/channel/device）
  - 只有真實座標、還沒有任何 geo_visit_log 事件（consent-only）→ 仍輸出 Marker，
    metadata 一律用安全預設值（city:'Unknown', channel:'Direct', device:'unknown'），
    不使用 undefined/null 裸值
  - 只有 geo_visit_log 事件、沒有真實座標 → 不輸出（沒有真實座標 = 不畫）
```

同一 visitor_key 只會出現一次（去重），且一定是「最新座標」（見 §3.2 upsert-by-latest 語意）。

---

## 6. Cluster（Leaflet.markercluster）

前端 `GeoLiveLayer` 在 `mode='cluster'` 時，把 §5 的同一份 Marker 陣列丟進 `L.markerClusterGroup()`（本地 vendor 檔案，非 CDN-only），而不是另外呼叫一支「Cluster API」。聚合、Zoom-to-bounds、Spiderfy 全部是 Leaflet.markercluster 原生行為。

---

## 7. Heatmap（Leaflet.heat）

同樣重用 §5 的 Marker 陣列（`[lat, lng, weight]`），交給 `L.heatLayer()`（本地 vendor 檔案）畫熱區。目前每個真實座標點的權重固定為 1（Marker 層級沒有「同一格多人」的聚合資訊可用來加權 Add to Cart/Checkout/Revenue），這是誠實的現況限制，不是臆測出一套虛構的加權公式。

---

## 8. District / Postal（Ranking-only，非地圖色塊）

- 資料來源是 `geo_visit_log.district` / `geo_visit_log.postal_code`，**完全不需要真實座標**即可顯示排行榜（訪客數/加購/結帳/訂單/轉換率），因為這兩欄位在 Geo Resolver 啟用時就有值。
- 專案目前**沒有**任何合法的行政區/郵遞區號 Polygon 資料（`data/taiwan-administrative-areas.json` 只是縣市/區名稱與代碼對照表，不含幾何邊界），因此本輪**不畫任何地圖色塊**，只提供 Ranking + 「目前無可用地圖座標」提示。點擊有真實座標的區域可以 `panTo`；沒有座標的區域只顯示提示文字。

---

## 9. Timeline / Replay / Filters / Polling

- **Timeline**：`getGeoLiveVisitorTimeline()` 依時間排序回傳單一訪客的事件序列（含 channel/device/coordinate 狀態），前端 `paginateTimeline()` 提供分頁/虛擬化，避免一次渲染整份清單。
- **Replay**：`getGeoLiveReplayBuckets()` 一次把整個時間範圍算成固定分鐘桶（預設 10 分鐘），前端 `replayFrameAt(buckets, frameIndex)` 純粹在記憶體中切片播放，**不會**每一幀重新呼叫 API。
- **Filters**：Channel（All/LINE/Facebook/Google/Organic/Direct/Other）、Device（Android/iPhone/Tablet/Desktop/Unknown）、時間範圍共用同一組 `_parseCommonQuery()`，不建立第二套篩選狀態。
- **Polling**：`resolvePollIntervalMs(requestedMs, isTabVisible)` 強制下限 5 秒；背景分頁自動降頻為 4 倍。`isStaleResponse(seq, latestSeq)` 提供 Stale Response Guard，配合 `AbortController` 避免過期回應覆蓋畫面。

---

## 10. Store Isolation

所有查詢一律以 `req.storeId`（由 `requireStore` middleware 解析）為篩選條件，顧客端 `POST /coordinate` 也透過同一套機制（`query.store_id` 相容 LINE 點餐頁面既有慣例）解析，不接受任何其他來源決定資料範圍。

---

## 11. Privacy

- Marker／Timeline 一律只回傳 `visitor_key`（`COALESCE(visitor_id, session_id, 'event_'+id)`）或遮罩後的 `vis_***xxx`，絕不回傳完整 `visitor_id`/`session_id`，更不回傳姓名/電話/地址。
- `geo_coordinate_status_log` 只記錄 store/visitor/session/status/source/accuracy/時間，不記錄任何額外敏感資訊。

---

## 12. Performance

- 所有查詢一律搭配既有 `(store_id, event_time)` 系列索引 + 本輪新增的 `(store_id, postal_code)` / `(store_id, channel)` / `(store_id, device_type)` 索引。
- Marker 查詢有 `limit`（預設 5000，上限 20000）避免無界回應。
- 前端輪詢有 5 秒下限 + 背景降頻；Timeline 有分頁；Replay 只 fetch 一次。

---

## 13. Known Limitations（誠實揭露）

1. Browser Geolocation 需要使用者明確授權；拒絕/逾時/不支援時該訪客**沒有** Marker，只會計入 Coverage 統計的對應狀態。
2. Google Geolocation REST API 的白名單已預留，但純網頁環境沒有 Wi-Fi/Cell 訊號輸入來源，**目前不可用**（`google_geolocation_api_available:false`）。
3. GPS（手機）與 Browser Geolocation 在本專案的 Web 架構下是**同一條路徑**（`navigator.geolocation` + `enableHighAccuracy:true`），不是兩套獨立實作。
4. District/Postal 只能做聚合排行，**不能**代替真實 Marker；沒有正式 Polygon 資料時不畫任何色塊。
5. Heatmap 目前權重固定為 1（訪客數），尚未支援依 Add to Cart/Checkout/Revenue 加權（Marker 層級資料不足以支撐）。
6. 本文件涵蓋的功能已通過自動化 Smoke（180+ 斷言）與真實 HTTP／Route-level 整合測試；**跨瀏覽器/裝置的人工視覺驗收（Manual QA）尚未執行**，正式上線前必須完成 `R5.4-G1_MANUAL_QA_CHECKLIST.md` 所列項目。
