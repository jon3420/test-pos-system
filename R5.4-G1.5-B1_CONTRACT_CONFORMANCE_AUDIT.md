# R5.4-G1.5-B1 Contract Conformance Audit

fix18-10-hotfix30-B5-R5.4-G1.5-B1 — 在改前端之前，先用真實程式碼執行結果
核對 `R5.4-G1.5-A_API_CONTRACT.md`，不是憑印象或憑文件描述猜測。

## 方法

直接 `require('./utils/ga4Realtime')` 並呼叫 `getGa4RealtimeData({db, storeId,
window, metric, forceRefresh})`，用假 `db`／假 GA4 client 觸發每一種狀態，
印出完整 JSON。以下是實際輸出（已格式化，欄位順序照原始輸出）。

## 1. disabled 真實 payload

```json
{
  "source": "ga4_realtime", "accuracy": "ip_city_county_estimate",
  "window_minutes": 5, "metric": "visitors",
  "fetched_at": null, "cache_age_seconds": null,
  "is_cached": false, "is_stale": false,
  "status": "disabled", "quota_status": "unknown",
  "summary": { "total_active_users_ga4": 0, "event_count": 0, "screen_page_views": null, "mapped_counties": 0, "unmapped_city_rows": 0, "excluded_non_tw_rows": 0 },
  "counties": [], "unmapped": [],
  "notices": ["GA4 位置由 IP 推估，僅供區域趨勢分析，非精確定位。"],
  "error_code": "ga4_realtime_disabled"
}
```

## 2. not_configured 真實 payload

```json
{
  "...": "同上其餘欄位形狀相同",
  "status": "not_configured",
  "error_code": "missing_property"
}
```
（`error_code` 依實際缺什麼設定而不同：`missing_property`／
`stream_not_configured`／`invalid_property`／`invalid_stream`／
`SDK_UNAVAILABLE`。）

## 3. fresh 真實 payload

```json
{
  "source": "ga4_realtime", "accuracy": "ip_city_county_estimate",
  "window_minutes": 5, "metric": "visitors",
  "fetched_at": "2026-08-03T08:42:34.286Z", "cache_age_seconds": 0,
  "is_cached": false, "is_stale": false,
  "status": "fresh", "quota_status": "unknown",
  "summary": { "total_active_users_ga4": 4, "event_count": 10, "screen_page_views": null, "mapped_counties": 1, "unmapped_city_rows": 0, "excluded_non_tw_rows": 0 },
  "counties": [{ "county_code": "68000", "county_name": "桃園市", "active_users": 2, "event_count": 5, "source": "ga4_city", "accuracy": "ip_city_county_estimate" }],
  "unmapped": [],
  "notices": ["GA4 位置由 IP 推估，僅供區域趨勢分析，非精確定位。", "Google Analytics 可能基於隱私保護省略部分低量資料。"],
  "error_code": null
}
```

**注意這個實測數字本身就是 §一 關鍵規則的活教材**：
`summary.total_active_users_ga4 = 4`，但 `counties[0].active_users = 2`（縣市
自己的加總）——兩者本來就不相等（4 是 Summary Request 的去重總數，2 是
City Request 裡桃園市這一列自己的數字），這正是前端絕對不能拿
`counties.reduce()` 去湊出「總訪客」的原因：就算湊出來的數字看起來合理，
語意上也是錯的。

## 4. cached 真實 payload

與 fresh 幾乎相同，差異：`is_cached: true`, `status: 'cached'`，
`fetched_at` 保持原本抓取時間不變。

## 5. stale_cache 真實 payload

與 cached 幾乎相同，差異：`is_stale: true`, `status: 'stale_cache'`，
`fetched_at` 仍是**原本第一次成功抓取**的時間（不會更新成現在時間）。

## 6. error 真實 payload（Route 層）

`utils/ga4Realtime/index.js` 的 `getGa4RealtimeData()` 在沒有 stale cache
可回退時會 `throw new Ga4RealtimeError(code, retryable, httpStatus)`。
`routes/geo-live.js` 的 `/ga4-realtime` handler catch 到之後回傳：

```json
{ "success": false, "code": "GA4_API_ERROR", "message": "GA4 Realtime API 發生錯誤，請稍後再試", "retryable": false, "status": "error" }
```

這個 response **沒有** `data` 欄位（跟 disabled/not_configured/fresh 等
`{success:true, data:{...}}` 的形狀完全不同），前端 `geoGa4NormalizeResponse()`
必須同時處理這兩種頂層形狀（`success:true` 走 `data.*`，`success:false`
走頂層 `code`/`message`/`retryable`）。

## 7. enabled 位於哪一層

**不在** `GET /ga4-realtime` 的 response 裡（無論哪個 status）。只出現在
`GET /ga4-realtime-status` 的獨立 response（`data.enabled`）。

## 8. configured 位於哪一層

同上，只在 `/ga4-realtime-status` 的 `data.configured`。

## 9. error_code 位於哪一層

`GET /ga4-realtime` 成功 response（`success:true`）：`data.error_code`
（disabled/not_configured 時非 null，其餘為 null）。
失敗 response（`success:false`）：頂層 `code`（不是 `error_code`，兩者是
不同 endpoint/不同失敗模式下的不同欄位名稱，前端要分開處理）。

## 10. status 位於哪一層

`data.status`（`/ga4-realtime` 成功 response）。失敗 response 的頂層也有
一個 `status` 欄位，但值固定是字串 `'error'`（不是同一組 enum，只是剛好
同名，語意不同——成功 response 的 `status` 是資料新鮮度狀態，失敗
response 的 `status` 只是一個常數標記）。

## 11. counties 真實欄位

`{ county_code, county_name, active_users, event_count, source:'ga4_city', accuracy:'ip_city_county_estimate' }`。
**沒有** `source_cities`（G1.5 草稿版本才有這個欄位，G1.5-A 已經拿掉）。

## 12. summary 真實欄位

`{ total_active_users_ga4, event_count, screen_page_views, mapped_counties, unmapped_city_rows, excluded_non_tw_rows }`。
`screen_page_views` 只有 `metric='visitors'` 時是數字，其餘 metric 為
`null`。

## 附錄：H5（精確位置字樣）誤判排查記錄

第一輪 Smoke 執行時，H5（「不得冒充精確位置文案」檢查）誤報 FAIL。追查
結果：

- **實際命中字串**：`精確位置`（來自 `public/js/geo-ga4-realtime-layer.js`
  檔案頂端設計原則註解「不冒充 GPS／精確位置：只用既有...」）
- **前後 100 字元 context**：`...ga4-realtime 讀取資料，不持有／不讀取任何 // Google 憑證、Property ID、Stream ID...// 2. 不冒充 GPS／精確位置：只用既有 window.geoMapState.featureIndex 的 // 官方行政區 GeoJSON 做「整個縣市同色」的 Polygon 著色，不建立 // Mark...`
- **檔案**：`public/js/geo-ga4-realtime-layer.js`
- **行號**：檔案頂端邊界說明註解（第 2 條）
- **分類**：**C（Comment）**——這是設計原則註解，用「不冒充」這個否定
  動詞描述邊界，不是產品正向宣稱。

**判定**：這是測試 helper 過於天真（原本用單一 regex `(?!，非)` 檢查
「後面接非」，方向反了）造成的誤判，**不是產品文案問題**。修正方式：
只修測試判斷（見需求文件五「若實際命中只是合法否定式聲明：只修測試判斷，
不改產品」），建立 `containsForbiddenExactLocationClaim(text)`：
1. 先用 `codeOnly()` 濾掉 `//` 開頭整行（排除 Comment）。
2. 再用 `stripAllowedPrivacyDisclaimers()` 遮罩掉「非精確位置」「不是精確
   位置」「不冒充 GPS／精確位置」等一組合法否定式片語。
3. 剩餘內容才拿去比對正向宣稱清單（「客人目前在這裡」「精確位置」「GPS
   定位」等）。

修正後同時新增 9 個 Mutation Test（`H5-mut1`~`H5-mut9`，見
`scripts/smoke-hotfix30-b5-r5-4-g1-5-b1-ga4-frontend-choropleth.js`），
證明這個 helper 本身：對合法否定式片語一律 PASS、對正向宣稱一律 FAIL、
對「把『非』拿掉」的變異一律 FAIL、對 Comment 內的禁止詞不誤判、且真實
Tooltip 程式碼裡的隱私聲明確實還在。**沒有修改任何產品文案**——
`geoGa4BuildTooltipContent()` 的輸出從頭到尾都是
「精度：IP 城市／縣市級推估，非精確位置」，這句話本身完全合規。

## 附錄：G1.5-B1 Smoke／Static Audit 最終結果

- Smoke（`scripts/smoke-hotfix30-b5-r5-4-g1-5-b1-ga4-frontend-choropleth.js`）：
  **168 PASS / 0 FAIL / 168 TOTAL**，三輪一致。
- Static Audit（`scripts/static-audit-g1-5-b1.js`）：**71 / 71 OK**，三輪
  一致。
- G1.5-A Backend Regression（確認前端改動沒有反向修改後端語意）：Smoke
  140/140、Static Audit 77/77，與 G1.5-A 完成時的基準數字完全相同。

## 與已發布 API Contract 的落差與修正


實測發現 `R5.4-G1.5-A_API_CONTRACT.md` 的「前端草稿差異」表格有一處錯誤
描述：原文誤寫「`enabled`/`configured` 沒變（仍在頂層）」，但實測顯示
`/ga4-realtime` 的成功 response **本來就沒有**這兩個欄位（`/ga4-realtime`
本輪就是新增的 endpoint，沒有「以前有現在還有」這件事，是文件描述本身的
筆誤，不是程式行為改變）。已修正 `R5.4-G1.5-A_API_CONTRACT.md` 對應段落，
文件與程式現在一致。**沒有修改**去重方式、Store Isolation、Cache Key 或
Retry 語意（本輪唯一的程式碼變更是文件本身的措辭修正，不涉及
`utils/ga4Realtime/` 或 `utils/ga4RealtimeConfig.js` 任何行為）。
