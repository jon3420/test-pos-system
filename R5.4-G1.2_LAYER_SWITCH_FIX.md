# R5.4-G1.2 — 訂單熱區／訪客熱區 Layer Switch Hotfix

fix18-10-hotfix30-B5-R5.4-G1.2

本文件說明 Bug 的真實 Root Cause 與修正方式。**不涉及後端、DB、Coordinate API、A7 KPI 或既有統計口徑**，全部改動集中在既有的 `public/js/geo-heatmap-ui.js`（R5.3-A1.2 引入的 Layer 切換列）與 `public/css/geo-heatmap.css`。

---

## 1. Root Cause

真實瀏覽器重現的現象——按鈕沒切換、地圖仍像 Order 模式、Visitor Layer 沒有明確空狀態——來自**三個獨立、具體的程式缺陷**，唯一權威狀態鍵 `geoHeatUiState.layer` 本身是正確的，問題是這個 state 沒有被完整套用到 UI 的三個地方：

### 1.1 原本空的 active `forEach`

```js
// 修正前：
const btns = document.querySelectorAll(`#${containerId}-panel-heatmap .geo-heat-layer-btn`);
btns.forEach((b) => { /* aria-pressed 由重新渲染的 HTML 負責，這裡只切換顯示 */ });
```

註解說「由重新渲染的 HTML 負責」，但**從來沒有任何程式碼真的重新渲染這段 HTML**——`forEach` 迴圈本體是空的。按鈕的 `is-active` class 與 `aria-pressed`/`aria-selected` 屬性永遠停留在頁面第一次渲染時的值。

### 1.2 Order／Visitor `layerGroup` 從未互斥

```js
// geo-heatmap.js（Order Engine）
geoHeatState.layerGroup.addTo(leafletMapInstance);   // 只在建立時呼叫一次

// geo-visitor-layer.js（Visitor Layer）
geoVisitorState.choroplethLayerGroup.addTo(mapInstance); // 也只在建立時呼叫一次
```

兩個 `layerGroup` 一旦建立，就會各自透過自己的 `.addTo(map)` 掛在同一張共用地圖上，**但沒有任何程式碼在切換 Layer 時把「目前不該顯示的那一個」從地圖上移除**。實際結果：使用者切到 Visitor Layer 後，Order Heatmap 的 `layerGroup`（通常內容較密集、視覺較強）仍然掛在地圖上，蓋過稀疏的 Visitor Layer 內容，看起來就像「完全沒有切換」。

### 1.3 Visitor Layer 沒有可畫資料時，地圖完全空白

`geoVisitorRenderChoropleth()` 只畫「已知行政區且能匹配到合法 Polygon」的資料；若 `known_area_only === 0`（如同真實回報案例：Known District=0），這個函式本來就正確地什麼都不畫（不使用假座標/中心點），但**沒有任何地方在地圖上顯示文字說明「為什麼是空的」**，使用者無從分辨「Layer 沒切換成功」跟「Layer 切換成功但剛好沒有可畫資料」。

### 1.4 排除的假設

依需求文件追查，確認**不存在**任何第二套競爭狀態：

| 可疑名稱 | 是否存在 |
|---|---|
| `orderHeatmapMode` | 不存在 |
| `geoVisitorMode` | 不存在 |
| `geoLiveState` | 不存在（那是 G1 GeoLiveLayer 自己的內部 state，跟這支 Layer 切換完全無關的獨立模組） |
| `activeLayer` | 不存在 |

唯一狀態鍵始終是 `geoHeatUiState.layer`（`'order' | 'visitor'`），這不是「雙重狀態競爭」問題，而是「render 沒有完整跟著 state 走」的問題。

---

## 2. 對照表（Root Cause 追查產出）

| 項目 | 內容 |
|---|---|
| DOM ID | `#{containerId}-layer-toggle` 內的 `.geo-heat-layer-btn`（新增 `data-layer` 屬性） |
| state key | `geoHeatUiState.layer`（`'order'｜'visitor'`，唯一正式狀態） |
| click handler | `geoHeatUiSetLayer(containerId, layer)` |
| render function | `_geoHeatUiRerenderLayerToggle()`（按鈕）／`geoVisitorFetchAndRender()`（Visitor 面板內容）／`_geoHeatUiRenderVisitorMapOverlay()`（地圖覆蓋文字） |
| map layer | `window.geoHeatState.layerGroup`（Order）／`window.geoVisitorState.choroplethLayerGroup`（Visitor） |
| active class/aria | `.is-active` / `aria-pressed` / `aria-selected`，由 `_geoHeatUiRerenderLayerToggle()` 重新渲染整段 HTML 決定 |
| API source | Order：既有 Heatmap Engine API；Visitor：`/api/analytics/geo/visitor-log`（本輪未修改任一個 API） |

---

## 3. 修正內容

### 3.1 `_geoHeatUiApplyLayerExclusivity(layer)`（新增）

只用 `map.addLayer()` / `map.removeLayer()` 切換既有兩個 `layerGroup` 物件的顯示狀態：

```js
if (layer === 'order') {
  if (visitorGroup && map.hasLayer(visitorGroup)) map.removeLayer(visitorGroup);
  if (orderGroup && !map.hasLayer(orderGroup)) map.addLayer(orderGroup);
} else if (layer === 'visitor') {
  if (orderGroup && map.hasLayer(orderGroup)) map.removeLayer(orderGroup);
  if (visitorGroup && !map.hasLayer(visitorGroup)) map.addLayer(visitorGroup);
}
```

不 `clearLayers()`（資料內容不受影響）、不重建 `layerGroup` 物件、不重建地圖或 Tile Layer。`map.hasLayer()` 判斷是冪等的，快速連點多次同一個 Layer 不會產生 duplicate add。

### 3.2 `_geoHeatUiRerenderLayerToggle(containerId)`（新增）

切換後直接把 `#{containerId}-layer-toggle` 的 `outerHTML` 換成 `geoHeatUiLayerToggleHtml(containerId)` 重新產生的內容——這段函式本來就會依 `geoHeatUiState.layer` 正確算出每個按鈕的 `active`/`aria-pressed`/`aria-selected`，只是修正前從來沒有人呼叫它。

### 3.3 `_geoHeatUiRenderVisitorMapOverlay()` + `_geoHeatUiVisitorMapOverlayMessage()`（新增）

依 `geoVisitorState.status`（既有欄位）與 `geoVisitorComputeCoverage()`（既有函式，回傳值完全沒有變動）決定四種文案之一：

| 狀態 | 文案 |
|---|---|
| `loading` | 訪客熱區載入中… |
| `error` | 訪客熱區載入失敗，請重試 |
| `ready` 且 `total===0` | 目前沒有符合條件的訪客事件 |
| `ready` 且 `known_area_only===0` 且 `with_coordinate===0` | 目前有 N 位訪客，但尚未取得可繪製的真實座標＋Known District/Exact Coordinate/Unknown/Coverage 四個數字 |
| 其餘（有 Polygon 或有真實座標可畫） | 不顯示覆蓋文字 |

覆蓋文字是**純 DOM 文字**（`overlay.textContent = message`），不是 Leaflet Layer、不是 Marker，`pointer-events:none` 不攔截地圖操作，`z-index:500` 低於 Leaflet 內建控制項（1000）。

---

## 4. Layer Lifecycle 保證

- 全程沒有任何路徑呼叫 `L.map()` 或 `L.tileLayer()`（已用假 Leaflet 環境驗證呼叫次數為 0）。
- `orderGroup`／`visitorGroup` 物件參考從頭到尾是同一個，只被 `addLayer`/`removeLayer`，不重建（已驗證快速切換 20 次後物件參考不變）。
- 切換到某個 Layer 時，若該 Layer 的 `layerGroup` 尚未建立（Visitor 首次使用），交由既有 `geoVisitorRenderChoropleth()`（未修改）在資料回來後自然建立＋掛載，`_geoHeatUiApplyLayerExclusivity()` 事後再次確認狀態正確。

---

## 5. Refresh／Rerender 保證

`geoHeatUiState.layer` 只在兩處被賦值：初始化（`'order'` 預設值）與 `geoHeatUiSetLayer()`。Date／Channel／Metric 切換觸發的 `geoHeatUiFetchAndRender()`／`geoVisitorFetchAndRender()` 都只重新產生「該 Layer 自己的資料內容」，不會、也從來沒有任何程式碼路徑會把 `geoHeatUiState.layer` 重設回 `'order'`。

---

## 6. LocalStorage State

`geoHeatUiState.layer` 是純記憶體變數，**本輪未新增任何 LocalStorage 持久化邏輯**（跟 R5.4-G1.1 的 `GeoLiveLayer.saveLastDisplayMode()`／`getLastDisplayMode()` 是完全不同的模組、不同的 LocalStorage key，兩者互不影響、不會衝突）。

---

## 7. Known Limitations

1. Visitor Layer 的 Choropleth 只畫「有合法 Polygon 對應」的已知行政區；本輪沒有新增任何座標推估或中心點假資料。
2. 覆蓋文字的四個數字（Known District/Exact Coordinate/Unknown/Coverage）完全來自既有 `geoVisitorComputeCoverage()` 的既有計算邏輯，本輪未修改其計算方式（`with_coordinate` 目前系統性設計下恆為 0，是既有、預期的行為，不是本輪引入的限制）。
3. 本文件涵蓋的驗證是自動化（jsdom 執行真實程式碼 + 全專案 `node --check` + 既有 Regression 重跑）；跨瀏覽器/裝置的人工視覺驗收狀態見 `R5.4-G1.2_MANUAL_QA_CHECKLIST.md`。
