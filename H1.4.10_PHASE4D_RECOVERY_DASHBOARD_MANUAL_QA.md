# H1.4.10 Phase 4D — RECOVERY ANALYTICS DASHBOARD Manual QA

**狀態：WIP ANALYTICS DASHBOARD ｜ AUTOMATED CORE ACCEPTANCE PASS（59/59）｜ MANUAL QA PENDING**

Sandbox 自動化測試（59/59，見 Test Report）已涵蓋所有可以在沒有真實生產資料的情況下驗證的邏輯正確性（計算公式、邊界條件、權限保護、Admin UI 渲染）。以下是需要真實環境/真實資料才能驗證的部分，一律標記 **PENDING MANUAL QA**。

## A. 真實資料量下的效能

**操作**：在有數千筆歷史 `cart_recovery_jobs` 的真實店家帳號執行 `GET /overview?days=90`。

**預期**：回應時間在可接受範圍內（後端目前用單次 SQL 查詢加總，理論上應該可以應付一般規模，但實際大量資料下的效能尚未實測）。

狀態：**PENDING MANUAL QA**

## B. 後台實際顯示

**操作**：登入後台，點擊「重新整理成效資料」。

**預期**：表格正確顯示三個 stage 的真實數字，`revenue` 為 `null` 的階段正確顯示「無法計算」文字（不是空白、不是 `null` 字樣、不是報錯）。

狀態：**PENDING MANUAL QA**

## C. 多店家資料隔離

**操作**：在有多家店的環境下，分別用不同店家帳號登入查看。

**預期**：每家店只看到自己的數據，不會看到其他店家的 job/訂單資訊混入。

狀態：**PENDING MANUAL QA**（後端已用 `req.storeId` 隔離查詢，邏輯上應該正確，但需要真實多租戶環境確認）

## D. 權限邊界

**操作**：用非管理者角色（如果系統有分角色）嘗試存取這個 API。

**預期**：`requireStaffJwt` 正確擋下非管理者存取。

狀態：**PENDING MANUAL QA**

## E. 數字對帳

**操作**：抽樣挑幾筆真實的 `payment_abandoned` converted job，人工核對 Dashboard 顯示的「追回營收」是否與該筆訂單在後台訂單管理頁看到的金額一致。

**預期**：完全一致（因為是直接 JOIN `orders.total`，理論上不會有差異，但建議正式上線前人工核對至少幾筆）。

狀態：**PENDING MANUAL QA**

---

## 尚未實作項目（不在本輪範圍，未來若要做需要另外規劃）

- Cohort/Segment 分析（例如依顧客年齡層、消費金額區間分群比較成交率）
- CSV／Excel 匯出
- 時間序列趨勢圖（例如過去 30 天每日成交率變化）
- 更細緻的漏斗視覺化（目前只有表格，沒有圖表）
- A/B 測試框架（用來真正驗證「提醒是否真的提升了轉換」而不只是狀態關聯）
