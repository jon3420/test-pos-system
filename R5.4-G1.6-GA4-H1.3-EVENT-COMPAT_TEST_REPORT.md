# R5.4-G1.6-GA4-H1.3-EVENT-COMPAT — Test Report

fix18-10-hotfix30-B5-R5.4-G1.6-GA4-H1.3-EVENT-COMPAT

---

## 一、新增測試腳本一覽

| Script | 數字 | 一致性 |
|---|---|---|
| `scripts/run-g1-6-ga4-h1-3-request-builder-contract.js` | 68/68 PASS | ×3 一致 |
| `scripts/run-g1-6-ga4-h1-3-event-compat-connection-test.js` | 48/48 PASS | ×3 一致 |
| `scripts/run-g1-6-ga4-h1-3-realtime-event-runtime.js` | 65/65 PASS | ×3 一致 |
| `scripts/run-g1-6-ga4-h1-3-historical-runtime.js` | 44/44 PASS | ×3 一致 |
| `scripts/run-g1-6-ga4-h1-3-mutations.js` | 37/37 PASS | ×3 一致 |
| `scripts/static-audit-g1-6-ga4-h1-3.js` | 63/63 OK | ×3 一致 |
| `scripts/run-regression-g1-6-ga4-h1-3.js`（43 Suites） | 全綠 | ×3 一致，0 Residue |

全部腳本用真實 Production 函式呼叫（`requestBuilder`／`requestPair`／`connectionTest`／`index.js`／真實 Express Router／真實 HTTP Server／真實 jsdom + 真實前端檔案），Fake Google Client 一律只從既有 `client._setClientForTest()` 這個正式注入點進入，沒有在 route／aggregator／frontend 中途插入假資料。

---

## 二、Request Builder Contract（68/68）涵蓋範圍

- `GA4_REQUEST_VARIANT === 'B'`、`buildRealtimeDimensions` 存在且為純函式（不 mutate 輸入陣列）。
- Visitors baseline（有無 Stream 兩種情況）：Summary `dimensions:[]`、City `dimensions:[city,countryId]`，完全不變。
- 4 個 Event Metric（view_item/add_to_cart/checkout/purchase）× 2 種 Stream 情況（有/無）：Summary／City 的 `dimensions` 是否正確包含 `eventName`（＋`streamId`），`dimensionFilter` 是否正確引用對應的真實 GA4 事件名稱（`begin_checkout` 而非字面 `checkout`）。

## 三、Event Compat Connection Test（48/48）涵蓋範圍

- Basic Mode：預設 `includeEventCompatibility=false`，完全不呼叫 Event Probe，Google Call 數與 H1.2 baseline 一致（2 次）。
- Event Mode：`includeEventCompatibility=true` 時，額外呼叫 4 個 Event Metric 的 Summary-only Probe（Google Call 數 = 6）。
- 安全性：不洩漏 Property／Stream／Credential／Raw Google Error。
- 隔離性：Basic／Event 兩個方向（先 Basic 後 Event／先 Event 後 Basic）互不 Rate-limit、互不污染 Cache。
- 失敗／逾時隔離：單一 Event Metric 失敗或逾時，不影響其他 Metric 與 Visitors 本身的結果。

## 四、Realtime Event Runtime（65/65）涵蓋範圍

- 成功矩陣：5 個 Metric × 2 個 Window（5m/30m）= 10 案例，逐一驗證正確 `eventName`、正確 `minuteRanges`、Summary+City 成功、HTTP 200、非錯誤狀態。
- Zero Event Contract：4 個 Event Metric 各自的 0 rows 案例都回傳合法空結果（HTTP 200，`status:'fresh'`），並反向驗證「Zero≠Error」與「Error≠Zero」不會互相誤判。
- Google Error Family Runtime：400/403/429/5xx/Timeout/Network Reject，逐一驗證安全 code/stage/retryable、無 Raw Error 洩漏、前端有對應安全文案。
- Partial Result Contract：City 單獨 400 → `status:'partial'`（HTTP 200），與既有 Production Contract（B2.4）一致，沒有為了測試方便改語意。
- 行政區 Regression 穿過 Event Metric：Pingzhen District（view_item）／Yangmei District（add_to_cart）→ 桃園市；Banqiao District（purchase）→ 新北市；US+Pingzhen → 排除。
- Dimension／Metric Header 順序穩健性：2 種不同 `dimensionHeaders` 排列＋ Summary 的 `metricHeaders` 交換順序，驗證 Header-based Parsing 不依賴固定 index（City Metric 本身的 Positional 特性見 Reality Audit 五，未強行套用交換測試）。
- Frontend Interaction Runtime：透過真實 HTTP Server + 真實 `geo-ga4-realtime-layer.js`，涵蓋 Metric/Window 切換、Refresh 保留狀態、20 次快速切換、Activate/Deactivate、Abort 中途請求，全程 0 個 `unhandledRejection`。
- `event_compat` Boolean Contract（透過真實 HTTP POST）：`{}`／`false`／`0`／`"false"` → Basic（2 calls）；`true`／`1` → Event Mode（6 calls）。

## 五、Historical Runtime（44/44）涵蓋範圍

- Asia/Taipei Calendar Boundary：00:01/07:59/08:00/23:59:59/午夜翻頁前後一秒，全部用注入的固定絕對時刻驗證。
- Range Contract：today/yesterday/7d/30d，並額外驗證跨月、跨年、閏年、Custom Range 不套用 offset。
- Sync UX：`rows_saved=0` → 中性成功文案（非「已更新 0 筆」）且仍觸發 Refresh；`rows_saved>0` → 正常成功文案；失敗 → 不觸發 Refresh。
- Metric Semantics：`10/2=5.0`、`8/2=4.0`、分母 0 → `—`；**真實 DOM 渲染**（不只是 Helper 單元測試）確認表格與 Tooltip 都出現 `5.0`/`4.0`/`加購事件／人`/`購買事件／人`，不出現 `500%`/`400%`/`加購率`/`購買率`；Sort（含 NaN 防禦）；DB／API 欄位名稱字樣未變。

## 六、Mutation Suite（37/37）— 18 個 Scenario 清單

| ID | Mutation | 驗證方式 |
|---|---|---|
| A | Event Metric 移除 eventName dimension（保留 filter） | Request Builder 層直接斷言 |
| B | 保留 streamId filter，移除 streamId dimension | 同上 |
| C | view_item 對應改成錯誤事件名 | 同上 |
| D | checkout 對應改回字面 `"checkout"` | 同上 |
| E | Event Probe 變成無條件執行（Basic Mode 也跑） | 真實 `runGa4ConnectionTest()` + Google Call 計數 |
| F | Basic／Event Cache 完全共用（雙向） | 真實 Connection Test，兩個方向都驗證 |
| G | `_parseGa4EventCompatFlag` 改回 `Boolean(value)` | 直接呼叫解析函式 |
| H | 合法 0 事件被誤判成 API Error | 真實 `getGa4RealtimeData()` |
| I | 真實 API 400 被吞成合法 0 事件（`summaryResult` 覆寫成合法空成功） | 真實 `getGa4RealtimeData()` |
| J | Taipei Helper 改回 naive UTC Calendar Day | 直接呼叫 `getTaipeiCalendarDateString()` |
| K | 7d 起始日 off-by-one | 真實 `resolveRangeWindow()` |
| L | `rows_saved=0` 改顯示紅色 Error | 真實 `_geoGa4H1HandleSyncResult()` |
| M/N | Per-user 又乘 100（×100 regression） | 直接呼叫 `_geoGa4H1PerUser()` |
| O/P | 表頭改回「加購率」／「購買率」 | 原始碼字面比對（已排除註解） |
| Q | Legacy `_geoGa4H1Rate()` 重新接回正式 Render Path | 針對 `_geoGa4H1BuildRowHtml()` 函式本體字面比對 |
| R | `classifyGa4RealtimeError()` fallback 洩漏 raw error message | 真實 `client.js.runGa4RealtimeReport()` |

每個 Scenario 都同時驗證「Mutated 版本真的 FAIL（防護被移除後行為確實變壞）」與「原始正式碼真的 PASS」，並用 `mutateOnce()` 先驗證命中次數恰好 1 次才進行替換，避免誤判或誤傷其他程式碼。

## 七、H1.3 Static Audit（63/63）涵蓋範圍

7 大類（Realtime Request／Event Compatibility／Error Safety／Taiwan Calendar／Historical Sync UX／Metric Semantics／Scope Freeze），細項見 `scripts/static-audit-g1-6-ga4-h1-3.js` 原始碼註解。所有「不得出現舊字樣」類檢查（例如 `加購率`／`購買率`／`_geoGa4H1Rate` 呼叫）都先用 `codeOnly()` 去除註解，並針對特定函式本體（`extractFnBody()`）判斷，避免被說明性註解或 Legacy Export 名稱誤判成正式 Render Path 呼叫（見 Implementation Report 三之 6）。

## 八、Full H1.3 Regression（43 Suites）

涵蓋本輪新增的 6 個 H1.3 Suite ＋ H1.2／H1.1／H1／G1.5-B2／G1.5-A／G1 geo-live／B2.5～B2.1／A1.2／A1.2.1／A1.1／A1／Geo Map／Geo Settings UI／Order Heatmap／Dashboard Rewire／A2-T1，共 43 個獨立 Suite。每個 Suite 都在獨立 child process 執行（沿用 H1.2 既有的 Process Isolation 設計），Mutation Test 的任何 in-memory monkey-patch 天然無法跨 Suite 殘留。3 輪執行結果**完全一致**，`FAIL=0`，`exit=0`，**0 個 KNOWN_BASELINE_MISMATCH**（本輪沒有發現任何新的 Stale Expectation）。

Residue 檢查（每輪）：`data/pos.db` 不存在、無殘留 temp DB、無殘留 `unhandledRejection` listener、無殘留 jsdom global。
