# H1.4.10 Phase 2 — friend_entry／friend_checkout Manual QA

以下項目需要真實 LINE App / 真實瀏覽器 / 真機才能驗證，無法在 sandbox 內自動化。

## 前置設定（本店推薦設定）

```
line_member_auto_identify_enabled = 1
line_member_gate_enabled = 1
line_member_gate_mode = friend_checkout
line_member_add_friend_url = https://lin.ee/<本店實際加好友網址>
```

效果：
- 從官方 LINE LIFF 進入 → 自動辨識會員 → 已是好友直接使用、非好友柔性引導加入
- Facebook / Chrome → 匿名點餐 → 前往結帳時柔性引導加入 LINE → 可略過 → 不強制 LINE Login

## A. friend_entry

**操作**：設定 `gate_mode=friend_entry`，Chrome 開啟點餐頁

**預期**：
- [ ] 進站顯示「加入官方 LINE」引導
- [ ] 按「先逛逛」→ Modal 關閉，正常瀏覽商品
- [ ] 可以正常加入購物車、前往結帳、完成下單（全程不受影響）
- [ ] 重新整理頁面（同一瀏覽器 session）→ 不會再次顯示引導

## B. friend_checkout

**操作**：設定 `gate_mode=friend_checkout`，從 Facebook 廣告連結進入（模擬外部瀏覽器匿名訪客）

**預期**：
- [ ] 進站時**不**顯示任何引導
- [ ] 選商品、加入購物車 → 不顯示引導
- [ ] 按下「前往結帳」→ 確認畫面已切到結帳頁（金額、商品摘要正常）→ 才顯示「加入官方 LINE」引導
- [ ] 開發者工具 Network 分頁確認：`checkout_click` 事件只送出一次
- [ ] 按「繼續結帳」→ Modal 關閉，可以正常填寫資料並送出訂單
- [ ] 返回購物車再次按「前往結帳」→ 不會重複顯示引導

## C. 點加入官方 LINE

**操作**：在 A 或 B 情境中，點擊「加入官方 LINE」按鈕

**預期**：
- [ ] 正確開啟官方 LINE 帳號加入頁面（LINE App 內用內建瀏覽器開啟；一般瀏覽器開新分頁）
- [ ] 返回原本的點餐頁後，購物車內容、已填寫的資料、結帳畫面狀態都還在（沒有被清空或重置）
- [ ] 這個當下**不會**立即顯示「已是好友」的任何視覺變化（因為按下按鈕不代表已驗證加入，需要之後 Webhook 或重新 Auto Identify 才會更新）

## D. LINE LIFF 已是好友

**操作**：使用已加入本店官方帳號的 LINE 帳號，從 LIFF URL 進入（`auto_identify_enabled=1` 且 `gate_mode=friend_checkout`）

**預期**：
- [ ] 背景自動辨識會員（無感，不跳登入 Modal）
- [ ] 按「前往結帳」→ **不會**再顯示加好友引導（因為系統已可信確認是好友）
- [ ] 直接可以看到結帳頁，正常送單

## E. 沒有 Add Friend URL

**操作**：`gate_mode=friend_entry`（或 `friend_checkout`）但 `line_member_add_friend_url` 留空

**預期**：
- [ ] 顯示「尚未設定官方 LINE 加好友網址，您仍可繼續下單。」
- [ ] 只有一顆「繼續」按鈕（沒有綠色的「加入官方 LINE」按鈕）
- [ ] 點「繼續」後可以正常瀏覽/結帳，沒有任何 JS 錯誤（可開發者工具 Console 確認）

## F. Admin 後台設定頁

**操作**：登入店家後台 → LINE 會員登入設定

**預期**：
- [ ] 「入口模式」下拉選單可以看到 5 個選項，文字分別為：不啟用／進站引導加入官方 LINE（免登入）／結帳前引導加入官方 LINE（免登入）／結帳前要求 LINE 登入／進站要求 LINE 登入
- [ ] 選擇「進站引導加入官方 LINE（免登入）」或「結帳前引導加入官方 LINE（免登入）」後，畫面上出現說明文字：「此模式只引導顧客加入官方 LINE，不會要求 LINE Login，也不會阻止顧客下單。」
- [ ] 此時「要求加入官方帳號」checkbox 變成灰色不可勾選，旁邊出現「免登入好友引導模式不使用此設定」的說明
- [ ] 切換回「結帳前要求 LINE 登入」或「進站要求 LINE 登入」→ 上述說明消失，「要求加入官方帳號」checkbox 恢復可勾選，且原本的勾選狀態沒有被清空
- [ ] 選擇 friend mode 且 `LIFF ID`／`LINE Login Channel ID` 欄位空白時，仍可正常按下「儲存」（不會跳出「必填」錯誤）
- [ ] 選擇「結帳前要求 LINE 登入」或「進站要求 LINE 登入」但 `LIFF ID`／`LINE Login Channel ID` 空白時，儲存應該被拒絕（既有行為，確認未被本輪破壞）

## G. line-shipping.html（冷藏宅配頁）

重複 A～E 的操作於 `line-shipping.html`，確認行為與 `line-order.html` 一致（共用同一套邏輯）。

---

## 尚待項目（Phase 3 之後）

- `checkout_submit_click` / `checkout_validation_failed` / `payment_success` 深度診斷事件
- Cart / Checkout / Payment 未完成找回（cart recovery jobs、n8n、LINE Push）
- CRM 找回同意（`cart_recovery_consents`）與 Dashboard 找回區塊
- Admin UI 增加 `line_member_auto_identify_enabled` 本身的可視化開關（目前只讀取顯示提示，尚無開關 UI）
