# R5.4-G1.6-GA4-H1.1-AUTH — Manual QA Checklist

所有項目預設 **NOT TESTED**。本文件只提供步驟，實際結果由部署後的人工
測試填入（`PASS` / `FAIL` / `NOT TESTED`），不得由這份文件自己宣稱通過。

## 部署前

1. 確認正式環境已部署本輪 `public/js/geo-ga4-h1-panel.js` 新版本 — NOT TESTED
2. 強制重新整理（清除瀏覽器快取，`Ctrl+Shift+R` 或等效操作） — NOT TESTED
3. 重新登入 POS（確保 `localStorage.pos_store_token` 是最新的） — NOT TESTED

## H1 Auth（401 修正驗證）

4. Network 面板開啟 Preserve Log，切換到 GA4 H1 圖層 — NOT TESTED
5. `GET /api/analytics/ga4-geo/realtime` 不再回 401 — NOT TESTED
6. `GET /api/analytics/ga4-geo/history?...` 不再回 401 — NOT TESTED
7. `POST /api/analytics/ga4-geo/sync` 不再回 401 — NOT TESTED
8. Request Headers 確認存在 `Authorization: Bearer ...` — NOT TESTED
9. Request Headers 確認存在 `x-store-id` — NOT TESTED
10. **確認 Token 值本身沒有出現在 URL／Query String 裡**（只看 Header 是否
    存在，不截圖或記錄 Token 實際值） — NOT TESTED

## 手動同步

11. 按「手動同步」按鈕一次 — NOT TESTED
12. 成功時畫面顯示 `rows_saved` 數字，且畫面資料有刷新 — NOT TESTED
13. 故意在網路不穩／連點兩次的情況下觸發一次失敗，確認畫面**不會清空**
    舊資料、不會誤顯示成功 — NOT TESTED

## 錯誤分類文案

14. 手動讓 Token 過期（或清掉 `localStorage.pos_store_token`）後操作，
    確認畫面顯示「登入已失效，請重新登入」，且**自動跳出登入視窗**
    （不是顯示一片空白或原始錯誤碼） — NOT TESTED
15. 若店家方案未開放報表功能，確認顯示「此方案未開放報表分析功能」
    （403，非登出） — NOT TESTED
16. 若 GA4 手動同步遇到 429（連點觸發 Rate Limit），確認顯示「操作過於
    頻繁，請稍候再試」 — NOT TESTED
17. 若 GA4 手動同步遇到 502，確認顯示「GA4 連線發生錯誤，請稍後再試」
    （不是原始 Google 錯誤訊息或 Stack） — NOT TESTED

## GA4 Backend 診斷（獨立於上面的 Auth 問題）

18. 前往「GA4 設定」頁，查看目前狀態摘要（`GET
    /api/geo-live/ga4-realtime-status`） — NOT TESTED
19. 按「連線測試」按鈕（`POST /api/geo-live/ga4-realtime-test`） — NOT TESTED
20. 記錄真實回傳的 `credential_available` — NOT TESTED
21. 記錄真實回傳的 `sdk_available` — NOT TESTED
22. 記錄真實回傳的 `property_accessible` — NOT TESTED
23. 記錄真實回傳的 `summary_request_ok` — NOT TESTED
24. 記錄真實回傳的 `city_request_ok` — NOT TESTED
25. 記錄真實回傳的 `error_stage` — NOT TESTED
26. 記錄真實回傳的 `error_code` — NOT TESTED
27. **只記錄上述安全欄位，不記錄／不截圖 Property ID、Service Account
    Email、任何憑證內容** — NOT TESTED

依上面 20–26 的組合，對照下表分類（僅供部署後人工判斷用，本文件不預先
下結論）：

| credential_available | sdk_available | property_accessible | summary_request_ok | city_request_ok | 判定 |
|---|---|---|---|---|---|
| false | — | — | — | — | Server 尚未設定 GA4 Service Account 憑證 |
| true | true | false | false | — | Service Account 無法讀取此 GA4 Property |
| true | true | true | true | false | GA4 基本連線成功，但城市區域查詢失敗 |
| true | true | true | true | true | GA4 Backend Gate PASS |

## AbortError / Console 檢查

28. 快速連續切換 Mode（今天／昨天／近 7 天／近 30 天／即時），Console
    不得出現 `Uncaught (in promise) AbortError` — NOT TESTED
29. 快速連續切換 Metric（活躍使用者／商品瀏覽／加入購物車…），Console
    同上 — NOT TESTED
30. 快速切換 Layer（Order → Visitor → GA4 → Order），Console 同上，且
    Loading 狀態能正確恢復（不會卡在「載入中…」） — NOT TESTED

## 跨瀏覽器／裝置矩陣

31. Windows Chrome — NOT TESTED
32. Windows/Mac Edge — NOT TESTED
33. Android Chrome — NOT TESTED
34. LINE Android（LINE 內嵌瀏覽器） — NOT TESTED
35. iPhone Safari — NOT TESTED
36. LINE iPhone（LINE 內嵌瀏覽器） — NOT TESTED

## 最終 Gate（部署驗證完成後才能填寫，本文件交付時全部留白）

- H1 Frontend Auth Production Gate：____________________
- GA4 Backend Production Gate：____________________
- Abort Safety Production Gate：____________________

**注意**：Diagnostic Contract 自動化測試的 PASS（40/40）只證明診斷端點
本身的安全 Contract；上表的 GA4 Backend 判定必須依賴本頁第 18–26 項
**實際部署環境**的真實回應填寫，不得用自動化測試的 Fake Client 結果代替。
