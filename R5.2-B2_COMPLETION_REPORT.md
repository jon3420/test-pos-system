# R5.2-B2 Leaflet Geo Intelligence Map — COMPLETION REPORT

## 版本名稱
- 工作目錄：`fix18-10-hotfix30-B5-R5.2-B2`
- 基準版本：`fix18-10-hotfix30-B5-R5.2-B1-6`（B1-6A：UNCHANGED）

## 修改檔案
| 檔案 | 說明 |
|---|---|
| `public/js/geo-intelligence.js` | additive：Map 區塊插入 Dashboard render 流程、ranking row 加上 `data-geo-area-key`、`geoOpenAreaExplorer()` 額外呼叫 `geoSelectArea()`（`typeof` 防呆，地圖模組未載入時零影響） |
| `public/css/geo-intelligence.css` | 新增 `.geo-map-` namespace 樣式，含三個 breakpoint |
| `public/index.html` | 新增 Leaflet CDN（含 `integrity`）與 `geo-intelligence-map.js` 的 `<script>`/`<link>` |
| `scripts/smoke-hotfix30-b5-r5-2-b1-5-geo-dashboard-ui.js` | 修正一個過嚴的 regex（`<tr class="db-v3-hover">` 精確比對），因 B1-6 合法新增 `data-geo-area-key` 屬性而失效，屬測試脆弱性修正，非功能性回歸 |

## 新增檔案
| 檔案 | 說明 |
|---|---|
| `public/js/geo-intelligence-map.js` | 主要程式：地圖生命週期、行政區比對、metric scale/style、Legend/Summary、Ranking↔Map↔Explorer 三方同步、集中式訊息系統 |
| `public/data/geo/taiwan-districts.geojson` | 桃園市 13 個行政區的精簡邊界資料 |
| `scripts/smoke-hotfix30-b5-r5-2-b2-geo-map.js` | B2 專用 Smoke Test（489 assertions） |

## 本輪修正的 production bugs
1. `Number(null) === 0` 導致 `geoGetMetricValue()` 把缺失資料誤判成真實的 0（違反「無資料不當成 0」核心要求）。
2. `geoHandleMapError()` 忽略傳入的 message 參數，Loading 與 Error 兩種語意共用同一句文案。
3. `geoLoadBoundaryData()` 完全沒有 request sequence 防護，較晚發出但較快完成的請求會被較早但較慢的請求事後覆蓋。
4. `geoBuildAreaFeatureIndex()` 的 `features` 提取用 `||` 而非 `Array.isArray()`，非陣列的 truthy 值會讓 `.forEach()` 拋錯。
5. `geoInitMap()` 的 `tileLayer.addTo()` 沒有 try/catch，Tile 供應商失敗會讓整個地圖初始化 crash（違反「Tile 失敗不得讓分析失效」）。
6. `geoUpdateMapData()`／`geoBuildMapSummary()` 同樣的 `rows||[]` 非陣列防護缺口。
7. `geoRetryMap()` 呼叫 `geoInitMap()` 時走了「已初始化只更新資料」快速路徑，但 `geoHandleMapError()` 已經把容器 DOM 整個覆蓋成錯誤訊息，導致 Retry 後錯誤畫面永久卡住——修正為 Retry 一律先 `geoDestroyMap()` 再重新 `geoInitMap()`。
8. 新增原本缺少的 `geoInvalidateMapSize()`（容器尺寸變化時的正確處理，此前完全沒有這個函式）。
9. `geo-intelligence-map.js` 內部原本有多處中文文案直接寫死在 render 函式裡（Loading/Error/No-Data），已全部改為透過集中的 `GEO_MAP_MESSAGES` 常數表 + `geoMapStatusText()`/`geoBuildMapStatusHtml()` 取用。

## 本輪修正的 test bugs（誠實列出）
- 一開始兩次分開的 `dom.window.eval()`（av2Src / geoSrc）造成頂層 `let` 綁定跨檔案不共享，已改回沿用 R5.1-C 既有的單次合併 eval 慣例。
- 若干 `window.geoDashboardFilters`/`window.geoMapState` 在 `_geoExposeWindowState()`/首次曝露前就被測試直接寫入，已修正呼叫順序，並讓 `geoMapState` 維持穩定物件參考（`_geoResetMapStateForTest()` 改為就地清空，不重新賦值變數）。
- 一個 XSS 測試誤用「序列化 HTML 屬性值字串比對」當作安全判準，造成誤判——已改為用真實 DOM 查詢（`querySelectorAll('script')` 等）驗證是否產生真正可執行節點。
- 一個 Retry 相關測試的失敗，追查後發現是 Leaflet mock 沒有模擬「`L.map()` 會清空容器內容」這個真實 Leaflet 行為，修正 mock 後測試才能正確驗證 production 邏輯（而不是掩蓋掉它）。
- `AQ-PIPELINE-11`：原本斷言方向寫反（誤以為缺字尾應該找不到，但去字尾 fallback 存在的目的正是要能找到），已修正為驗證正確的預期行為。
- 多處 CSS/HTML 靜態檢查 regex 誤配對到自己的說明註解文字，已改為先剝除註解再檢查。

## Smoke 最終結果
```
總計：489 項，PASS 489，FAIL 0
exit code: 0（自然結束，無 timeout kill）
```

## 兩次執行結果
| 執行 | 總數 | PASS | FAIL | exit code |
|---|---|---|---|---|
| 第一次 | 489 | 489 | 0 | 0 |
| 第二次 | 489 | 489 | 0 | 0 |

PASS 順序逐字元 diff 完全相同。

## Regression Gate 結果
| 套件 | 結果 | 要求 |
|---|---|---|
| B1-6A | 376/376 PASS | ≥376 ✅ |
| B1-5 | 313/313 PASS（修正後） | ≥313 ✅ |
| B1-4.6 | 867/867 PASS | ≥867 ✅ |
| B1-4.5 | 819/819 PASS | ≥819 ✅ |
| B1-4 | 132/132 PASS | ≥132 ✅ |
| B1-3 backend | 76/76 PASS | ≥76 ✅ |
| B1-2 | 150/150 PASS | ≥150 ✅ |
| B1-1 | 100/100 PASS | ≥100 ✅ |
| R5.1-C | 196/196 PASS | ≥196 ✅ |

**新增 regression：0**（B1-5 一度出現 312/313，追查後確認是 B1-6 合法新增的 DOM 屬性讓測試自身的 regex 過嚴失效，修正測試 regex 後恢復 313/313；不是功能性回歸）

## Static Audit
- `node --check`：`geo-intelligence.js`／`geo-intelligence-map.js`／smoke test 全部通過
- 重複 function／const/let 宣告：兩個檔案皆無
- 重複 HTML id：無
- CSS/JS 只引用一次：`geo-intelligence.css`／`geo-intelligence.js`／`geo-intelligence-map.js`／`leaflet.js`／`leaflet.css` 各恰好 1 次
- CSS namespace：全部新樣式在 `.geo-map-`，Leaflet 覆寫包在 `.geo-map-root` 底下，無全域 `.card{}`/`table{}`/`button{}` 污染
- Script 載入順序：`app.js → analytics-v2.js → geo-intelligence.js → Leaflet CSS/JS → geo-intelligence-map.js`（正確）
- Forbidden scope audit：無第二套 filter state、無第二套 recommendation engine（已用測試逐項確認）
- console 敏感資料：無

## Diff Audit（相對 B1-6）
```
Modified:
  public/css/geo-intelligence.css
  public/index.html
  public/js/geo-intelligence.js
  scripts/smoke-hotfix30-b5-r5-2-b1-5-geo-dashboard-ui.js
New:
  public/js/geo-intelligence-map.js
  public/data/geo/taiwan-districts.geojson
  scripts/smoke-hotfix30-b5-r5-2-b2-geo-map.js
```
無 `node_modules`、無 `.git`、無資料庫檔、無 log、無暫存測試輸出、無 debug script。

## Manual Visual
**REQUIRED** — 本環境為 CLI/jsdom，無真實瀏覽器，未實際看過 Leaflet 畫面呈現。人工仍需在以下尺寸檢查：
- 1440px：Tile 是否正常、Polygon 是否對齊、Legend 是否遮擋
- 1024px：Metric Switcher 換行、Summary 兩欄
- 390px：Metric Switcher 橫向捲動、Legend 折疊、Tooltip 是否超出 viewport、Drawer 與地圖 touch 衝突

## 已知限制
1. GeoJSON 邊界為精簡近似多邊形（矩形），非精確地理邊界——僅供功能驗證，未來若要精確呈現需替換為真實測繪資料（架構上已支援替換，`geoBuildAreaFeatureIndex()` 不假設任何硬編碼座標）。
2. 只涵蓋桃園市 13 個行政區（目前商家實際營運範圍），跨縣市需擴充 GeoJSON 檔案，載入邏輯本身已與縣市無關。
3. Revenue metric 在「行政區」粒度目前資料來源有限（county-summary 是縣市層級），Legend/Summary 遇到無資料時會誠實顯示灰色/暫無資料，不會假裝有數字。
4. `L.map()`/`L.geoJSON()` 本身若拋出例外（跟已修正的 `tileLayer.addTo()` 不同層級），目前尚未包 try/catch——已在 Smoke 測試中誠實記錄此行為（`AV-ERRORRECOVERY` 系列），非隱藏風險。

## 明確未包含
- Heatmap
- ROI Map
- 額外 Marker 系統（Order Marker／Customer Marker）
- B1-6B Geo Explorer Data APIs（Customer Explorer／Order Explorer／Device Breakdown／行政區級外送分析，維持 B1-6A 定義的 UNAVAILABLE 狀態）
- 自動投放廣告／自動發優惠券
- ZIP 之外的任何新商業功能

## 最終狀態

**R5.2-B2：COMPLETE**

完成條件逐項確認：
- [x] Smoke ≥400（實際 489）
- [x] Smoke 連跑兩次穩定（PASS 數/順序/exit code 完全一致）
- [x] Regression Gate 全綠（0 新增 regression）
- [x] Static Audit PASS
- [x] Diff Audit PASS
- [x] Manual Visual 誠實標記 REQUIRED（未謊稱已完成人工瀏覽器驗證）
