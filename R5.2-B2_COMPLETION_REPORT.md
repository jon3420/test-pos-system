# R5.2-B2 Leaflet Geo Intelligence Map — COMPLETION REPORT
(Revision 3 — supersedes Revision 2. Revision 2 fixed a Manual-Visual production bug；
Revision 3 converts the Geo Intelligence Map from a **hardcoded-Taoyuan** SaaS liability
into a **商家可設定地圖聚焦範圍** architecture. Revision 2's content is preserved below
under "Revision 2 History" for traceability.)

## 版本名稱
- 工作目錄：`fix18-10-hotfix30-B5-R5.2-B2`
- 基準版本：`fix18-10-hotfix30-B5-R5.2-B2`（Revision 2，同一版本號，不建立新版本）

---

## Revision 3：商家可設定地圖聚焦範圍（SaaS 架構）

### 一、Audit 結論

| # | 位置 | 分類 | 處置 |
|---|---|---|---|
| 1 | `geoLoadBoundaryData()` 原本無條件 `fetch('/data/geo/taiwan-districts.geojson')` | production hardcode | 改為 `geoResolveBoundarySource(settings, manifest)` 依商家 `city_code` 查表，查不到就不載入邊界 |
| 2 | `geoInitMap()` 原本無條件 `fitBounds(geoJsonLayer.getBounds())`（=桃園） | production hardcode | 改為 `geoResolveInitialViewport()` 依 `scope_mode` 決定 viewport，無效資料落到台灣全域 fallback |
| 3 | `taiwan-districts.geojson`（檔名誤導，內容只有桃園13區） | 檔名誤導 + test fixture | 保留原檔（fixture 相容），另建 manifest 驅動的 `taiwan/taoyuan-districts.geojson` |
| 4 | `store_001` 特例 | 無發現 | 不需修正 |
| 5 | 前端 fallback center/zoom | production hardcode（新規則） | 唯一 fallback 為台灣全域，非任何商家座標 |
| 6 | Smoke fixture 內桃園座標/GeoJSON | test fixture | 保留，改為透過商家設定顯式驅動 |

A（production hardcode）：項目 1、2、5 已全部移除。B（test fixture）：項目 6 保留但顯式化。

### 二、聚焦模式設計

新增 `utils/geoMapScope.js`（單一 pure function 集合，前後端共用同一份規則）：
- `GEO_MAP_SCOPE_MODES=['store_location','districts','custom_bounds','data_bounds']`
- `geoResolveInitialViewport()`：優先順序完全依 scope_mode 決定資料來源，無效一律落到台灣 fallback（`[23.7,121.0]`/zoom 7），不混用其他模式資料，不 fallback 桃園
- `geoResolveBoundarySource()`：依 city_code 在 manifest 白名單查表，來源必須通過安全檢查（拒絕 http(s)/javascript:/data:/protocol-relative/path traversal/非`/`開頭路徑）

前端 `geo-intelligence-map.js` 內建功能相同的一份（瀏覽器 classic script 與後端 CommonJS 無法共用 require，此為既有慣例）；smoke test 內建 `SaaS-C/D-consistency-*` 逐項比對兩份實作結果一致。

### 三、設定欄位

未建第二套設定框架，沿用既有 `settings(store_id,key,value)` key-value 表，additive 新增 7 個 key：
`geo_map_scope_mode`／`geo_map_default_zoom`／`geo_map_city_code`／`geo_map_district_codes`／`geo_map_bounds`／`geo_map_geojson_source`／`geo_map_auto_fit_bounds`。
`store_lat`/`store_lng`（既有欄位）直接沿用，未重複存座標。

### 四、後端 API

新增 `GET /api/settings/geo-map`、`PATCH /api/settings/geo-map`（沿用既有 `PATCH /store-location`/`PATCH /pickup-location` 框架與 `requireStore`/`req.storeId` 隔離機制）；一般 `GET/PUT /api/settings` 也把新 key 併入 `ALL_ALLOWED`。驗證涵蓋：scope enum、zoom 1~20、lat/lng range、bounds 順序、district_codes 型別、geojson_source 安全 URL。

### 五、GeoJSON 載入架構

新增 `public/data/geo/taiwan/manifest.json` + `public/data/geo/taiwan/taoyuan-districts.geojson`（含新增的 `district_code` 屬性）。manifest 目前只登記 `TAO`，架構支援後續擴充其他縣市，無需改動任何 production 判斷邏輯（無 `if city==='桃園'`）。舊檔 `public/data/geo/taiwan-districts.geojson` 保留不刪（fixture 相容），production 已完全不再直接引用該路徑。

### 六、行政區選取過濾

`scope_mode='districts'` 時只 render `district_codes` 白名單內的 feature（依新增 `district_code` 屬性比對）。缺此屬性的舊版 fixture 視為無法辨識、不過濾（向後相容）。

### 七、Store Isolation／安全

讀寫皆透過既有 `req.storeId`，與其餘既有 settings 端點同一套隔離機制。`geojson_source` 一律拒絕任意外部/危險 URL，只允許 manifest 白名單內站內相對路徑。

### 八、Smoke 結果

```
scripts/smoke-hotfix30-b5-r5-2-b2-geo-map.js
總計：621 項，PASS 621，FAIL 0
```
新增 **117 項** SaaS 架構專屬 assertion（超過需求 80~100 項）：

| 區段 | 內容 | 項數 |
|---|---|---|
| SaaS-A | validateGeoMapSettingsPatch 後端驗證 | 36 |
| SaaS-B | parseGeoMapSettingsRow 預設值與防呆 | 15 |
| SaaS-C | geoResolveBoundarySource（manifest／安全／前後端一致性） | 13 |
| SaaS-D | geoResolveInitialViewport（優先順序／不 fallback 桃園／一致性） | 17 |
| SaaS-E | District Filtering（真實 GeoJSON 資料） | 9 |
| SaaS-F | Manifest／靜態檔案完整性 | 7 |
| SaaS-G | Store Isolation／Cross-Store（sql.js 模擬真實 SQL 寫入路徑） | 10 |
| SaaS-H | Memory／狀態不累積 | 6 |

舊版 9 個假設「`geoLoadBoundaryData()` 無參數＝無條件載入桃園」的 fixture 呼叫點，已改為透過 `primeGeoMapScopeForLegacyFixture()`／新增的 `/api/settings/geo-map`、`/data/geo/taiwan/manifest.json` fetch mock 攔截規則，顯式指定商家設定後再呼叫——**只修正 fixture，未修改任何 production 程式碼**。

過程中額外發現並修正一個真實 production 缺陷（非 fixture 問題）：`_geoApplyInitialViewport()` 原本不論 `scope_mode` 為何都無條件計算 `dataBounds`（內部會多呼叫一次 `L.geoJSON()` 建立臨時圖層），已修正為只在該模式實際需要時才計算對應 bounds，避免每次地圖初始化/更新都產生用不到的臨時 Leaflet 圖層。

### 九、Regression Gate 結果

| 套件 | 結果 |
|---|---|
| B2（自身，含新 SaaS 區段） | 621/621 PASS |
| B1-6A | 376/376 PASS |
| B1-5 | 313/313 PASS |
| B1-4.6 | 867/867 PASS |
| B1-4.5 | 819/819 PASS |
| B1-4 | 132/132 PASS |
| B1-3 backend | 76/76 PASS |
| B1-2 | 150/150 PASS |
| B1-1 | 100/100 PASS |
| R5.1-C | 196/196 PASS |

**全部 PASS，0 新增 regression。**

### 十、Static Audit

- `node --check`：`utils/geoMapScope.js`／`routes/settings.js`／`public/js/geo-intelligence-map.js`／`public/js/geo-intelligence.js`／smoke test 全部通過
- 搜尋 production 檔案內固定桃園座標、固定引用 `taiwan-districts.geojson`、`store_001` 特例、`if city==='桃園'`：**全部找不到**（唯一「桃園」字樣出現在註解，說明「不得 fallback 桃園」）
- production 內所有 `fetch()` 皆由 settings/manifest 動態決定 URL

**Static Audit：PASS**

### 十一、Diff Audit（相對 Revision 2）

```
New:
  utils/geoMapScope.js
  public/data/geo/taiwan/manifest.json
  public/data/geo/taiwan/taoyuan-districts.geojson

Modified:
  routes/settings.js
  public/js/geo-intelligence-map.js
  public/js/geo-intelligence.js
  scripts/smoke-hotfix30-b5-r5-2-b2-geo-map.js
  R5.2-B2_COMPLETION_REPORT.md
```
無 `node_modules`、無 `.git`、無資料庫檔、無 log、無暫存測試輸出。舊版 `public/data/geo/taiwan-districts.geojson` 保留未刪。

### 十二、Manual Visual

**REQUIRED**——尚未在瀏覽器實機驗證。至少須測試：
1. `store_001`（桃園，districts 模式）顯示正確
2. 模擬台北商家（store_location，台北座標）不顯示桃園範圍
3. 模擬無座標商家 → 台灣全域，不是桃園
4. 自訂 bounds 商家
5. 空資料情境
6. 手機 390px／平板 1024px／桌面 1440px

**不宣稱已人工通過。**

### 十三、已知限制（誠實揭露）

1. manifest 目前只登記桃園市；其餘縣市 GeoJSON 待後續分批補齊，架構已支援。
2. GeoJSON 邊界仍是精簡近似多邊形（沿用 Revision 2 既有限制）。
3. district_codes 過濾依賴 `district_code` 屬性；舊版精簡 fixture 沒有此屬性時不套用過濾（向後相容選擇）。
4. **後台設定 UI 尚未開始**——本輪僅完成後端 API／資料模型／前端 viewport/boundary 引擎／Smoke／Regression。依指示，待本輪穩定後才進入 UI 階段。
5. `L.map()`/`L.geoJSON()` 本身拋出例外仍未包 try/catch（沿用 Revision 2 既有限制）。

---

## 最終狀態（Revision 3）

**R5.2-B2（SaaS 聚焦範圍架構部分）：COMPLETE**

- [x] production 不再固定桃園
- [x] 商家可設定聚焦模式（4 種 scope_mode）
- [x] store_lat/store_lng 沿用既有欄位
- [x] 行政區來源由 manifest 決定
- [x] 不同商家不互相污染（SaaS-G 10 項全綠）
- [x] 無設定時 fallback 台灣，不 fallback 桃園（SaaS-D 全綠）
- [x] B2 Smoke 全綠（621/621）
- [x] Regression Gate 全綠（0 新增 regression）
- [x] Static Audit PASS
- [x] Diff Audit PASS
- [ ] Manual Visual：**REQUIRED**（尚未執行）
- [ ] 後台設定 UI：**尚未開始**（下一階段）

因 Manual Visual 與後台 UI 尚未完成，商家目前尚無 UI 可設定聚焦範圍；但本輪要求的後端／資料模型／前端引擎／Regression／Smoke／Static Audit 範圍已全數完成並穩定通過。

---

## Revision 2 History（保留舊報告全文，供追溯）

**Manual Visual 發現的真實 Production Bug：** `renderDashboardGeoIntelligence()` 原本 `if (!summary) return ''`，只要伺服器回應沒帶舊版 `geo_summary` 欄位就整段跳過渲染，導致地圖容器永遠不存在。修正為缺欄位時安全代打 `{}`，不再提早結束；`disabled`（功能停用）與 summary 缺失是兩個不同語意，不共用早期 return。

Smoke 最終結果（Revision 2）：總計 504 項，PASS 504，FAIL 0。Regression Gate 全部 PASS。

已知限制（Revision 2）：(1) GeoJSON 邊界為精簡近似多邊形；(2) 只涵蓋桃園市 13 個行政區；(3) Revenue metric 於行政區粒度資料有限；(4) `L.map()`/`L.geoJSON()` 例外未包 try/catch。

Manual Visual（Revision 2）：REQUIRED——使用者已完成第一輪實機驗證並回報上述 bug；修正後尚未再次於瀏覽器確認畫面。
