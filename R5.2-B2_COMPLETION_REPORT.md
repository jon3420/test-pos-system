# R5.2-B2 Leaflet Geo Intelligence Map — COMPLETION REPORT
(Revision 2 — supersedes the previous report, which was invalidated by a real bug found via Manual Visual browser testing)

## 版本名稱
- 工作目錄：`fix18-10-hotfix30-B5-R5.2-B2`
- 基準版本：`fix18-10-hotfix30-B5-R5.2-B1-6`（B1-6A：UNCHANGED）

## ⚠️ Manual Visual 發現的真實 Production Bug（本次修正的核心內容）

**症狀（使用者實機瀏覽器驗證）：**
```
typeof L                => object     （Leaflet 已載入）
typeof geoInitMap       => function   （模組已載入）
typeof geoUpdateMapData => function
geoMapState             => instance:null, tileLayer:null, geoJsonLayer:null, rows:Array(0)
document.querySelector('.geo-map-root') => null
```
Leaflet 與 `geo-intelligence-map.js` 都成功載入，但地圖容器從未被建立。

**真正根因：** 不在 `refreshGeoDashboardKpiBlock()`（那裡的地圖 wiring 本身沒問題），而是在**更上層**的進入點 `renderDashboardGeoIntelligence(data)`：

```js
// 修正前
const summary = data && data.geo_summary;
if (!summary) return '';   // ← 這裡
```

只要伺服器這次回應剛好沒有帶舊版 `geo_summary` 欄位，整個函式就回傳空字串——導致下面 `<div id="${kpiContainerId}">` 容器與其中 `setTimeout(() => refreshGeoDashboardKpiBlock(...))` 的排程**完全沒有機會被建立/執行**。`refreshGeoDashboardKpiBlock()`／`geoInitMap()` 本身完全正常，只是永遠沒被呼叫到，因為連呼叫它的容器都不存在。

**修正：**
```js
// 修正後
const summary = (data && data.geo_summary) || {};   // 缺欄位安全代打，不再整段跳過
const disabled = summary.data_quality && summary.data_quality.status === 'disabled';
window.__geoDashboardLegacyDisabled = !!disabled;   // 純粹設旗標，不在這裡 return
_geoExposeWindowState();
// ...一律繼續往下建立 KPI／Decision Center／地圖容器／排程 refreshGeoDashboardKpiBlock()
```
`summary` 缺失（資料還沒來）跟 `disabled`（功能被明確停用）是兩個不同語意，不得共用同一個「提早結束」判斷；`disabled` 一律只是設旗標，由稍後排程執行的 `refreshGeoDashboardKpiBlock()` 自己的快捷路徑決定要不要顯示「Geo Analytics 未啟用」——這是修正前就存在的既有設計，第一版修正時誤把 `disabled` 也一起提早 return，造成 B1-1／R5.1-C 兩個既有測試的「停用快捷路徑」情境失敗，已改回正確語意並重新驗證全綠。

## 修改檔案
| 檔案 | 說明 |
|---|---|
| `public/js/geo-intelligence.js` | **本次修正核心**：`renderDashboardGeoIntelligence()` 不再因 `summary` 缺失就整段回傳空字串；additive：Map 區塊插入 Dashboard render 流程、ranking row 加上 `data-geo-area-key`、`geoOpenAreaExplorer()` 額外呼叫 `geoSelectArea()` |
| `public/css/geo-intelligence.css` | `.geo-map-` namespace 樣式，含三個 breakpoint |
| `public/index.html` | Leaflet CDN（含 `integrity`）與 `geo-intelligence-map.js` 的 `<script>`/`<link>` |
| `scripts/smoke-hotfix30-b5-r5-2-b1-5-geo-dashboard-ui.js` | 修正過嚴 regex（`<tr class="db-v3-hover">`），因合法新增 `data-geo-area-key` 屬性而失效 |
| `scripts/smoke-hotfix30-b5-r5-2-b1-1-dashboard-rewire.js` | 更新一個逐字比對原始碼行的斷言，配合上方 bug 修正後的正確程式碼行 |

## 新增檔案
| 檔案 | 說明 |
|---|---|
| `public/js/geo-intelligence-map.js` | 地圖生命週期、行政區比對、metric scale/style、Legend/Summary、Ranking↔Map↔Explorer 三方同步、集中式訊息系統 |
| `public/data/geo/taiwan-districts.geojson` | 桃園市 13 個行政區的精簡邊界資料 |
| `scripts/smoke-hotfix30-b5-r5-2-b2-geo-map.js` | B2 專用 Smoke Test（504 assertions，含本次 Manual Visual bug 的 14 項專屬 regression test） |

## 本輪修正的 production bugs（完整列表）
1. **`renderDashboardGeoIntelligence()` 的 `if (!summary) return ''`**（Manual Visual 發現）——整段跳過渲染，導致地圖容器永遠不存在。已修正為 summary 缺失時安全代打 `{}`，不再提早結束。
2. `Number(null) === 0` 導致 `geoGetMetricValue()` 把缺失資料誤判成真實的 0。
3. `geoHandleMapError()` 原本忽略傳入的 message 參數。
4. `geoLoadBoundaryData()` 完全沒有 request sequence 防護。
5. `geoBuildAreaFeatureIndex()`／`geoUpdateMapData()`／`geoBuildMapSummary()` 的 `features`/`rows` 提取用 `||` 而非 `Array.isArray()`。
6. `geoInitMap()` 的 `tileLayer.addTo()` 沒有 try/catch，Tile 供應商失敗會讓整個地圖初始化 crash。
7. `geoRetryMap()` 原本走快速路徑，但 `geoHandleMapError()` 已把容器 DOM 覆蓋成錯誤訊息，導致 Retry 後畫面永久卡死——修正為 Retry 一律先 destroy 再重新 init。
8. 新增原本缺少的 `geoInvalidateMapSize()`。
9. 中文文案全部改為透過集中的 `GEO_MAP_MESSAGES` 常數表取用。

## 本輪修正的 test bugs（誠實列出）
- 兩次分開的 `dom.window.eval()` 造成頂層 `let` 綁定跨檔案不共享，已改回單次合併 eval。
- 若干測試在首次曝露前就直接寫入 `window.geoDashboardFilters`/`window.geoMapState`，已修正順序，並讓 `geoMapState` 維持穩定物件參考。
- 一個 XSS 測試誤用序列化屬性值字串比對當安全判準，改為真實 DOM 查詢。
- Leaflet mock 沒有模擬「`L.map()` 會清空容器內容」，修正 mock 後才能正確驗證 Retry 邏輯。
- `AQ-PIPELINE-11` 斷言方向寫反。
- **本次新發現**：第一版 bug 修正把 `disabled` 也一起提早 return，破壞既有「停用快捷路徑」設計，造成 B1-1／R5.1-C 各一/兩項測試失敗；已修正回正確語意並恢復綠燈，同時更新 B1-1 測試中一個逐字比對原始碼行的斷言。

## Smoke 最終結果
```
總計：504 項，PASS 504，FAIL 0
exit code: 0（自然結束，無 timeout kill）
```
含 14 項專門針對本次 Manual Visual 真實 bug 的 regression test（模擬 `data.geo_summary` 缺失＋`rows=[]`，逐項驗證 `.geo-map-root` 存在、`geoInitMap()` 被呼叫、GeoJSON 發出、13 個 feature 建立、instance 非 null、no-data legend、summary valid count=0、灰色無資料樣式、不誤當成 0、tab 切換後 re-init）。

## 兩次執行結果
| 執行 | 總數 | PASS | FAIL | exit code |
|---|---|---|---|---|
| 第一次 | 504 | 504 | 0 | 0 |
| 第二次 | 504 | 504 | 0 | 0 |

PASS 順序逐字元 diff 完全相同。

## Regression Gate 結果
| 套件 | 結果 | 要求 |
|---|---|---|
| B2（自身） | 504/504 PASS | ≥400 ✅ |
| B1-6A | 376/376 PASS | ≥376 ✅ |
| B1-5 | 313/313 PASS（修正後） | ≥313 ✅ |
| B1-4.6 | 867/867 PASS | ≥867 ✅ |
| B1-4.5 | 819/819 PASS | ≥819 ✅ |
| B1-4 | 132/132 PASS | ≥132 ✅ |
| B1-3 backend | 76/76 PASS（重跑後穩定） | ≥76 ✅ |
| B1-2 | 150/150 PASS | ≥150 ✅ |
| B1-1 | 100/100 PASS（修正後） | ≥100 ✅ |
| R5.1-C | 196/196 PASS（修正後） | ≥196 ✅ |

**新增 regression：0**

**過程中如實記錄的插曲：**
1. B1-5 一度 312/313——合法新增的 `data-geo-area-key` 屬性讓其測試 regex 過嚴失效（測試脆弱性，非功能回歸），修正後恢復 313/313。
2. 第一版 bug 修正跑出 B1-1 兩項＋R5.1-C 一項失敗——誤把 `disabled` 也納入提早 return；修正回正確語意後全部恢復綠燈。
3. B1-3 backend 多次出現不穩定 pass 數——這是**每一輪（B1-3/B1-4.6/B1-5/B1-6A/B1-6）都重複確認過**的既有環境性 flake，與本輪或任何一輪程式異動無關（`utils/geoAnalyticsQueries.js` 與該測試腳本逐字元跟 B1-6 基準版本相同），重跑後穩定回到 76/76。

## Static Audit
- `node --check`：全部通過
- 重複 function／const/let／HTML id：無
- CSS/JS 只引用一次
- CSS namespace 正確、Leaflet 覆寫包在 `.geo-map-root` 底下
- Script 載入順序正確
- 無第二套 filter state、無第二套 recommendation engine
- console 敏感資料：無

## Diff Audit（相對 B1-6）
```
Modified:
  public/css/geo-intelligence.css
  public/index.html
  public/js/geo-intelligence.js
  scripts/smoke-hotfix30-b5-r5-2-b1-1-dashboard-rewire.js
  scripts/smoke-hotfix30-b5-r5-2-b1-5-geo-dashboard-ui.js
New:
  public/js/geo-intelligence-map.js
  public/data/geo/taiwan-districts.geojson
  scripts/smoke-hotfix30-b5-r5-2-b2-geo-map.js
  R5.2-B2_COMPLETION_REPORT.md
```
無 `node_modules`、無 `.git`、無資料庫檔、無 log、無暫存測試輸出、無 debug script。

## Manual Visual
**REQUIRED**——使用者已完成第一輪實機驗證並回報上述真實 bug；本次修正後尚未再次於瀏覽器中確認畫面。仍需人工在 1440px／1024px／390px 複查地圖底圖、13 個行政區灰色邊界、Legend、Summary、Metric Switcher 是否正確顯示。

## 已知限制
1. GeoJSON 邊界為精簡近似多邊形，非精確地理邊界。
2. 只涵蓋桃園市 13 個行政區。
3. Revenue metric 在行政區粒度目前資料有限。
4. `L.map()`/`L.geoJSON()` 本身拋出例外的情況尚未包 try/catch，已在測試中誠實記錄。

## 明確未包含
- Heatmap／ROI Map／額外 Marker 系統／Customer API／B1-6B／自動投放廣告／自動發優惠券

## 最終狀態

**R5.2-B2：COMPLETE**

- [x] Manual Visual 發現的真實 bug 已修正並有專屬 regression test
- [x] Smoke ≥400（實際 504），連跑兩次穩定
- [x] Regression Gate 全綠（0 新增 regression，既有 pre-existing flake 如實記錄）
- [x] Static Audit PASS／Diff Audit PASS
- [x] Manual Visual 誠實標記 REQUIRED（修正後尚待人工於瀏覽器複查）
