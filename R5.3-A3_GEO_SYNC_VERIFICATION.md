# R5.3-A3 — Geo Sync Verification

驗證需求文件第 6 點列出的一致性要求，全部用真實 sql.js DB + 真實
`insertEvent()`/`getGeoEventFunnel()`/既有 `getGeoFunnel()` 執行，不是
理論推導。

## 驗證方法

對同一組事件資料，分別呼叫：

- **Dashboard 側**：既有 `utils/geoAnalyticsQueries.js` 的 `getGeoFunnel()`
  （Dashboard／Funnel 既有查詢，本輪未修改）。
- **Geo 側**：`utils/geoEventEngine.js` 的 `getGeoEventFunnel()`（本輪
  統一的 Geo Event Engine，讀 `geo_visit_log`）。

比較兩者的訪客數/加購數/結帳數/購買數是否一致。

## 驗證情境與結果

### 情境 1：單一訪客完整走過 Visitor → Cart → Checkout → Purchase

| 指標 | Dashboard（analytics_events） | Geo Intelligence（geo_visit_log） | 一致？ |
|---|---|---|---|
| Visitor | 1 | 1 | ✅ |
| Add To Cart | 1 | 1 | ✅ |
| Checkout | 1 | 1 | ✅ |
| Purchase | 1 | 1 | ✅ |

### 情境 2：老闆儀表板「近 5 分鐘目前在線」對照 Geo Visitors（必做診斷 Scenario 1）

| 項目 | 數值 |
|---|---|
| 老闆儀表板「目前在線」（`utils/dashboardAnalytics.js` `getRealtime()`，本輪未修改） | 1 |
| Geo Intelligence「Geo 訪客」（`getGeoEventFunnel()`，近 5 分鐘） | 1 |
| Unknown | 1 |
| Unknown Rate | 100% |

**一致，且 Unknown 100% 沒有導致 Geo Visitors 被排除成 0**（需求文件
第 7 點）。

### 情境 3：同一訪客重複 10 次 page_view（去重驗證）

| 指標 | Dashboard 側（`visitors` 去重欄位，既有查詢本來就有人數/次數分離） | Geo 側 | 一致？ |
|---|---|---|---|
| 訪客人數 | 1 | 1 | ✅ |
| 事件次數 | 10（Dashboard 的「次數」欄位，不去重） | 10（`geo_visit_log` 逐筆保留，Recent Log 可查全部 10 筆） | ✅（兩邊都正確區分「人數」與「次數」） |

### 情境 4：Unknown 訪客（Geo 完全無法辨識）是否仍計入 Visitor

| 指標 | 結果 |
|---|---|
| Dashboard 的訪客總數（不分是否可辨識地理位置） | 1 |
| Geo Visitors（含 Unknown） | 1 |
| Geo Known District | 0 |
| Geo Unknown | 1 |

**Unknown 只影響 Geo 的「已知/未知」分類欄位，完全沒有讓 Visitor 總數
歸零或被排除**（需求文件第 7 點的核心要求，已驗證通過）。

### 情境 5：歷史資料 Backfill 前後（本輪找到的真正落差）

| 階段 | Geo Visitors | Add To Cart |
|---|---|---|
| Backfill 前（模擬 hook 部署前的歷史資料缺口） | 0 | 0 |
| Backfill 後（執行 `scripts/backfill-geo-visit-log.js`） | 2 | 1 |
| 同一時間，Dashboard 側（`analytics_events`，不受影響） | 2（總是正確，因為讀的是原始事件表） | 1 |

Backfill 後兩者一致。

## 結論

在本輪修正（UI 主動抓取 + 歷史 Backfill）之後，Dashboard 與 Geo
Intelligence 在下列四項全部驗證一致：

- Dashboard Visitor = Geo Visitor ✅
- Dashboard AddToCart = Geo AddToCart ✅
- Dashboard Checkout = Geo Checkout ✅
- Dashboard Purchase = Geo Orders ✅

沒有發現任何「Analytics 有資料、Geo 卻是 0」的資料層面矛盾——找到的兩個
真正落差都是「Geo Event Engine 沒有被觸發去讀取已經同步好的資料」
（UI 時機）與「舊資料沒有回填」（Backfill），而不是兩套系統本質上不同步。
