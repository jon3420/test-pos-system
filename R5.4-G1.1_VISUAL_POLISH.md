# R5.4-G1.1 — Geo Intelligence Visual Polish

fix18-10-hotfix30-B5-R5.4-G1.1

本輪不新增功能，只讓 R5.4-G1 已完成的 GeoLiveLayer 真正做到「老闆一眼就能看懂」。**沒有修改任何後端 API、DB Schema、Store Isolation 或 Consent 邏輯**——全部改動集中在 `public/js/geo-live-layer.js`（純函式擴充）與 `public/css/geo-live-layer.css`。

---

## 1. Heatmap 視覺強化

`buildHeatOptions(pointCount, theme)` 依資料筆數動態決定參數，不再是固定一組偏淡的設定：

| 筆數 | radius | blur | minOpacity |
|---|---|---|---|
| ≤3 筆 | 45 | 35 | 0.6 |
| 4~10 筆 | 35 | 25 | 0.5 |
| ＞10 筆 | 25 | 20 | 0.35 |

即使只有 1~3 筆真實座標，也會用夠大的半徑與夠高的不透明度呈現，肉眼可辨識。漸層色階固定為 **綠 → 黃 → 橘 → 紅 → 深紅**（`HEAT_GRADIENT_LIGHT`），Dark Theme 下改用對比更高的組合（`HEAT_GRADIENT_DARK`，見 §7）。`HEAT_LEGEND_STOPS` 提供固定的文字圖例（🟢低／🟡中／🟠高／🔴最高），不只靠顏色本身傳達資訊。

## 2. Circle Mode

`buildCircleStyle(metric, value, minValue, maxValue)`：顏色依 Metric 決定（Visitors 藍／Orders 橘／Revenue 金／Conversion 紫，`METRIC_COLORS`），半徑依數值在該 Metric 範圍內的比例，在 `CIRCLE_MIN_RADIUS_PX`(6px) 到 `CIRCLE_MAX_RADIUS_PX`(24px) 之間縮放，**不是固定大小**。只有單一數值時（min===max）視為最大值，仍給予可辨識的大小。

## 3. Marker Mode

`resolveMarkerStage(eventName)` 把最後事件分類成四個階段（visitor/checkout/order/revenue），`markerColorForStage()` 對應不同顏色的 Marker Icon（藍/橘/綠/金）。Tooltip（`buildMarkerTooltipFields`）維持既有欄位（匿名 Visitor Key／行政區／Channel／Device／最後事件／最後時間／Accuracy／Geo Quality），已驗證序列化後不含 undefined/NaN/Infinity，且不含姓名/電話/地址等個資。

## 4. Auto Fit Bounds

`shouldAutoFitBounds(hasData, alreadyDone)`：只有「這是本次 session 第一次有資料」時才會 `fitBounds()`；`state.autoFitDone` 一旦設成 `true` 就不會再自動跳視角，切換 Filter／Metric 不會強制重新縮放。沒有資料時維持地圖目前預設視角。

## 5. Summary Card

`buildSummaryCard({ districtRows, unknownPool })` 輸出：最高訪客區域、最高成交（訂單）區域、平均轉換率、GPS Coverage %、Unknown %。**誠實揭露**：最高營收區域與平均外送距離目前 G1 既有 API 沒有提供 revenue/distance 欄位，一律回傳 `null` + `revenue_available:false` / `distance_available:false`，不臆測假數字；若未來資料列中出現這些欄位，函式會自動正確帶出（forward-compatible，已測試）。

## 6. Ranking

`buildRankingTable(rows, metric)` 把既有 District/Postal 資料整理成「排名／區域／訪客／加購／結帳／訂單／成交率」欄位齊全的表格，依目前選擇的 Metric 動態排序。`resolveRankingClickAction(row)`：有座標回傳 `{action:'panTo', lat, lng}`，沒有座標（目前 District/Postal 聚合本來就沒有座標）回傳 `{action:'no_coordinate', message:'目前無地圖座標'}`，不假裝有座標可以跳轉。

## 7. Coverage Panel

`buildCoveragePanel(unknownPool)` 把既有 Coverage 數字轉成 0–100 clamp 過的百分比，供 CSS Progress Bar（`.geo-live-progress-track` / `.geo-live-progress-fill`）渲染視覺化長條，不再只是純數字。

## 8. Business Opportunity（Rule-based，非 AI）

`buildBusinessOpportunities(districtRows)`：
- 規則一：某區 `conversion_rate_pct > 80` 且 `order_count >= 1` → 建議增加該區廣告曝光。
- 規則二：某區訪客數高於平均且 `conversion_rate_pct < 5` → 建議檢查價格／優惠券／結帳流程。

完全是固定規則比對（`if`/比較運算），**不是 AI／機器學習**，沒有資料時不產生任何建議。

## 9. Recommended Actions

`buildRecommendedActions(districtRows, coverage)` 依 Geo Summary 輸出最多 3 個建議：成交率最高的區域建議增加曝光、GPS 覆蓋率偏低（<50%）建議提高定位同意率、有訪客無結帳的區域數建議改善結帳流程。

## 10. 區域優惠建議

`buildRegionDiscountSuggestions(rankedRows)` 依 Ranking 前三名，依序給予「95 折／免外送費／滿額送好禮」三種**不同**的建議樣板；區域名稱完全來自動態資料（真實 district，不是硬編碼特定縣市），**只是建議文字，不會呼叫或修改任何優惠系統**。

## 11. 外送最佳化

`buildDeliveryOptimization(rows)`：只有在資料列真的帶有 `distance_km` 欄位時才計算平均/最遠/最近距離與平均外送費，超過 `DELIVERY_DISTANCE_ALERT_KM`（5 公里）時建議檢視運費；**沒有 distance 資料時誠實回傳 `{available:false}`**，不產生虛構的距離數字（目前 G1 既有 District/Postal API 未提供此欄位，此函式是為未來資料可用時準備的計算邏輯）。

## 12. Heatmap Legend

`HEAT_LEGEND_STOPS`（🟢低／🟡中／🟠高／🔴最高）+ CSS `.geo-live-heat-legend`，固定顯示在 Heatmap 模式旁，避免使用者只能靠顏色深淺猜測數值高低。

## 13. Display UX（Segmented Control）

CSS 新增 `.geo-live-segmented` / `.geo-live-segmented-btn[aria-pressed="true"]`：目前模式高亮顯示，取代原本平鋪的按鈕列；按鈕具備 `:focus-visible` 樣式供鍵盤操作辨識焦點。

## 14. Heatmap Toggle（Switch + LocalStorage）

CSS 新增 `.geo-live-switch`（真正的 Switch 樣式，取代純文字 On/Off）。`getLastDisplayMode()`/`saveLastDisplayMode()` 把使用者最後選擇的顯示模式存進 `localStorage`（key: `geo_live_layer_last_mode`），`init()` 在沒有明確指定模式時會自動還原；不合法的模式值會被拒絕寫入，不影響先前已儲存的合法值。

## 15. 動畫

`ANIMATION_DURATION_MS = 250`（落在 200~300ms 區間，`isAcceptableAnimationDuration()` 可驗證）。CSS `@keyframes geoLiveFadeIn` / `geoLiveZoomIn` 分別用於 Marker Fade In、Circle Zoom In、Heat Fade In；並尊重 `prefers-reduced-motion: reduce` 使用者偏好，自動關閉動畫。

## 16. Responsive

CSS 新增 390px（手機）與 1024px（桌面/平板橫向）響應式斷點，確保 Summary／Ranking／Coverage／地圖在各尺寸下不重疊（既有 768px 斷點沿用 R5.4-G1）。

## 17. Dark Theme

`[data-theme="dark"]` / `.geo-live-theme-dark` 樣式覆寫：Ranking Panel、Insight Card、Progress Bar、Legend 文字顏色在深色背景下重新校正對比；Heatmap 改用 `HEAT_GRADIENT_DARK`（更亮的紅/橘），避免深色背景下純紅色看不清楚。

## 18. Accessibility

`ARIA_LABELS`（modeSwitcher／heatToggle／legend／rankingRow）提供固定文案供對應元件掛 `aria-label`；CSS 對 Segmented Control 按鈕與 Switch 均提供 `:focus-visible` 樣式，支援鍵盤 Tab 操作辨識焦點。

## 19. Performance

本輪沒有新增任何 API 呼叫、沒有重新建立 Leaflet Map／Tile Layer（沿用 R5.4-G1 的 `attachToMap()` 機制），`setMode()` 切換模式時仍是「先清除既有 Layer 再畫新的」（`_clearActiveLayers()`），資料不會因為切換顯示模式而重新 Fetch（同一份 Marker 陣列供 Marker/Cluster/Heatmap 三種模式共用渲染）。

## 20. 已知限制（誠實揭露）

1. 最高營收區域、平均外送距離目前無真實資料來源（G1 既有 API 未提供 revenue/distance 欄位），UI 會顯示「資料不足」而非假數字。
2. Business Opportunity／Recommended Actions／區域優惠建議皆為**規則引擎**（if/比較運算），不是 AI／機器學習模型。
3. Ranking 點擊 Pan To 只在該列真的有座標時才可用；District/Postal 聚合目前沒有座標，一律顯示「目前無地圖座標」。
4. 本文件涵蓋的驗證是自動化單元測試（91 assertions）+ 全專案 `node --check`；**跨瀏覽器/裝置的實際視覺效果尚未人工驗收**，需完成 `R5.4-G1_MANUAL_QA_CHECKLIST.md` 對應項目後才能視為正式可上線。
