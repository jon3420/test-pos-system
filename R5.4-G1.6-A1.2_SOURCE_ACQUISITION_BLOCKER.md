# R5.4-G1.6-A1.2 — Source Acquisition Blocker

fix18-10-hotfix30-B5-R5.4-G1.6-A1.2 — Authoritative Administrative
Representative Points & Region-only Marker Payload

## 結論（先講重點）

本階段在第一步——取得官方行政區界線來源——就無法進行，且是**沙箱環境層級
的、可驗證的、無法繞過的限制**，不是資料本身不存在或懶得找。依需求文件
第三節的明確指示，本輪**不建立**任何 Catalog、Resolver、Payload 修改，
也**不建立**可部署 ZIP，只建立這一份文件。

## 實際驗證過程（可重現）

在本輪工作環境中，直接測試對兩個官方來源網域發出 HTTPS 請求：

```
$ curl -sS --max-time 8 -D - -o /dev/null "https://data.gov.tw"
HTTP/2 403
x-deny-reason: host_not_allowed

$ curl -sS --max-time 8 -D - -o /dev/null "https://www.nlsc.gov.tw"
HTTP/2 403
x-deny-reason: host_not_allowed
```

對照組（確認 curl／網路機制本身正常運作，問題確實是網域白名單，不是連線
本身壞掉）：

```
$ curl -sS --max-time 8 -D - -o /dev/null "https://github.com"
HTTP/2 200
```

本工作環境的網路 egress 是白名單制（`network_configuration`），目前允許
的網域只有套件登錄／原始碼平台（`npmjs.com`、`pypi.org`、`github.com`
等），**不包含**：

- `data.gov.tw`（政府資料開放平臺）
- `www.nlsc.gov.tw`（內政部國土測繪中心）
- 國土測繪圖資服務雲（`data.nlsc.gov.tw` 或其正式網域）

這代表：即使本輪原本規劃的下載／解析／Checksum 驗證流程完全正確，也
**無法**在本沙箱環境中執行第一步（連線官方網站、下載 SHP 壓縮檔）。

## 缺少哪些官方檔案

依需求文件第二節列出的官方來源，目前完全沒有取得：

1. **Dataset 7442 — 直轄市、縣市界線（TWD97經緯度）**
   - 提供單位：內政部國土測繪中心
   - 平台：政府資料開放平臺（data.gov.tw）
2. **Dataset 7441 — 鄉鎮市區界線（TWD97經緯度）**
   - 提供單位：內政部國土測繪中心
   - 平台：政府資料開放平臺（data.gov.tw）

兩者預期都是 Shapefile 格式，需要完整的組成檔案（依 Shapefile 規格，至少
需要）：

- `.shp`（幾何本體）
- `.shx`（索引）
- `.dbf`（屬性表，含縣市／鄉鎮市區代碼與中英文名稱）
- `.prj`（座標系統定義，用於確認 CRS／EPSG，不得只看檔名猜測）

以及資料集頁面本身的：

- Metadata（資料更新日期、提供單位、資料集說明）
- 授權條款（是否需要標示來源、允許的使用方式）
- 建議引用／標示文字（若平台要求）

## 使用者應從哪個官方 Dataset 下載

1. 前往政府資料開放平臺（`https://data.gov.tw`），搜尋資料集編號
   `7442`（直轄市、縣市界線）與 `7441`（鄉鎮市區界線），提供單位篩選
   「內政部國土測繪中心」。
2. 或直接前往國土測繪圖資服務雲官方下載專區，尋找「最新直轄市／縣市
   界線」與「最新鄉鎮市區界線」的正式下載連結。
3. 下載完整壓縮檔（通常是 `.zip`，內含上述 `.shp/.shx/.dbf/.prj` 等組成
   檔）。
4. **不要**解壓縮後只挑 `.shp` 上傳——建置工具需要完整的組成檔案集合
   才能正確讀取幾何與屬性。

## 預期檔名（供對照，實際檔名以官方壓縮檔內容為準）

國土測繪中心的公開資料集在不同時間點發布的檔名可能變動（例如附帶版本
日期），本文件不假設固定檔名，避免使用者誤以為檔名必須完全一致。實際
建置工具（`scripts/build-authoritative-admin-points.js`，本輪尚未建立）
應該要能接受使用者提供的實際檔名，而不是硬編碼特定檔名字串。

## SHA-256 記錄方式

一旦取得官方壓縮檔，建議由使用者（或下一輪有網路存取權限的環境）執行：

```
sha256sum <下載的壓縮檔案名稱>
```

並把結果連同下載日期、資料集 ID、來源頁面 URL 一起提供給下一輪，記錄進
`data/geo/authoritative/SOURCE_MANIFEST.json`（本輪尚未建立此檔案，因為
還沒有真實可記錄的雜湊值與檔案）。**不得**在沒有真實下載檔案的情況下，
編造一個看起來合理的 SHA-256 字串填進去——那樣的雜湊值無法對應任何真實
檔案，會讓 Manifest 變成謊報來源，比完全沒有 Manifest 更危險。

## 下一回合如何繼續

有兩種可行路徑，由使用者選擇：

**路徑 A（建議）**：使用者在自己有網路存取權限的環境（例如本機電腦）
下載上述兩個官方 Dataset 的完整壓縮檔，取得 SHA-256，並將檔案（或至少
其 `.dbf`／`.prj`／屬性欄位說明與雜湊值）提供給下一輪對話。下一輪可以
在有這些真實檔案的情況下，實際執行 Shapefile 解析、CRS 轉換、
Point-on-surface 運算、Catalog Build 與驗證，完整跑完本階段規劃的全部
工作項目。

**路徑 B**：若沙箱環境的網路白名單在未來被調整為允許
`data.gov.tw`／`nlsc.gov.tw` 等網域，下一輪可以重新嘗試直接下載，不需要
使用者手動提供檔案。

在取得上述任一路徑的真實官方檔案之前，本專案**不會**：

- 使用矩形測試 fixture 頂替
- 使用任何未記錄來源的 centroid JSON
- 從 Google 搜尋結果或第三方 Blog 抄座標
- Hardcode 桃園／中壢等座標
- 宣稱 Catalog／Resolver／Region-only Payload 已完成

## Gate 判定

- **Official Source Gate**：**BLOCKED**——沙箱網路白名單不允許連線
  `data.gov.tw`／`nlsc.gov.tw`，無法下載 Dataset 7442／7441 官方壓縮檔。
- **Source Manifest**：未建立（沒有真實檔案可記錄）。
- **Catalog Build**：未開始。
- **Region-only Payload Integration**：未開始（依需求文件明確指示，
  Official Source Gate 未通過前不得進入這一步）。
- **Estimate Marker Gate**：**BLOCKED**（沿用 A1.1 狀態，未改變）。
- **G1.6-A2 Gate**：**BLOCKED**（沿用 A1.1 狀態，未改變）。

**最終 Gate：BLOCKED — Waiting for official NLSC boundary files.**

本輪完成後停止，不建立 ZIP，不建立除本文件外的任何其他文件，不修改任何
現有程式碼。

---

*本文件不含任何真實憑證、Property、Stream、Token 或私人下載網址內容。*
