# R5.2-B3 — Geo Intelligence Settings UI — Manual QA Checklist

給人工在**真實瀏覽器**執行，逐項打勾。看到問題直接記在對應項目旁邊，不用等全部做完才寫。

---

## Hotfix 1 驗證（優先執行——先確認 401 問題已解決，再繼續下面完整清單）

背景：舊版 QA build 因為 `geo-map-settings.js` 用裸 `fetch()` 呼叫受保護的 `/api/settings/geo-map`，沒有帶授權資訊，導致 `requireStore` 回 401 NO_STORE_TOKEN。本版已改用既有的 `apiFetch()`（跟「店家座標設定」同一套授權流程）。請先確認這個問題真的解決了：

- [ ] **1.** 登入商家帳號，開啟「設定 → 🗺️ Geo Intelligence 地圖設定」分頁
- [ ] **2.** Network 面板確認 `GET /api/settings/geo-map` 回應狀態碼是 **200**，不再是 401
- [ ] **3.** 修改「預設縮放層級」後點擊「💾 儲存設定」
- [ ] **4.** Network 面板確認 `PATCH /api/settings/geo-map` 回應狀態碼是 **200**
- [ ] **5.** F5 整頁重新整理，重新進入分頁，確認剛剛的修改仍然保留
- [ ] **6.** Console 確認**不再**出現 `GET /api/settings/geo-map 401` 這類錯誤
- [ ] **7.** Network 面板點開 `GET /api/settings/geo-map` 的 Request Headers，確認有 `Authorization` 或 `x-store-id` 其中之一（跟其他既有 API 呼叫，例如店家座標設定，帶的資訊一致即可，**不需要**、也**不要**在回報時把完整 token 貼出來）
- [ ] **8.** 回報時請只寫「有帶授權資訊」或「沒有帶」，不要複製貼上完整 Authorization header 的值

Hotfix 1 結果：PASS / FAIL（若 FAIL 請附 Console/Network 截圖或錯誤訊息文字）

---

## 0. 事前準備

- [ ] 啟動後端（`npm start` 或既有啟動方式），登入商家帳號
- [ ] 開啟瀏覽器 DevTools，切到 **Console** 分頁，保持開著
- [ ] 開啟 **Network** 分頁，保持開著

---

## 1. 進入 Geo Intelligence 地圖設定

- [ ] 進入「設定」頁
- [ ] 點擊「🗺️ Geo Intelligence 地圖設定」分頁按鈕
- [ ] Panel 正確顯示，沒有跑版
- [ ] 切到其他設定分頁再切回來，反覆 3～5 次
  - [ ] 沒有重複建立地圖（畫面沒有閃爍重建）
  - [ ] Console 沒有新增 Error
  - [ ] 沒有出現重複的 DOM 元素（可用 Console 執行 `document.querySelectorAll('#geo-settings-preview-map').length` 應該永遠是 1）

---

## 2. 四種模式切換

依序點選以下四個 Radio，每次都確認欄位顯示/隱藏正確：

- [ ] **店家位置**：顯示 緯度/經度/預設縮放/取得目前定位；隱藏 行政區、自訂範圍欄位
- [ ] **行政區**：顯示 縣市/行政區多選/自動依邊界縮放；隱藏 店家定位、自訂範圍欄位
- [ ] **自訂範圍**：顯示 south/west/north/east/套用目前預覽範圍；隱藏 其他模式欄位
- [ ] **依分析資料**：顯示 模式說明文字；隱藏 其他模式欄位

- [ ] 快速連續切換 4 種模式 10 次，沒有欄位殘留或顯示錯亂

---

## 3. Preview 地圖

- [ ] 第一次進入分頁時，地圖只出現一次（不是先出現一個又被換掉）
- [ ] 切換模式時，地圖**不會**整個重新出現/閃爍（只有畫面內容變化）
- [ ] 拖曳 Zoom 滑桿，地圖平滑更新，**不會**每拖一格就重新建立地圖
- [ ] 自訂範圍模式下修改 south/west/north/east，地圖正確 fitBounds
- [ ] 行政區模式下勾選/取消勾選行政區，地圖 layer 正確替換（不會疊加出重複的行政區色塊）

### 世界地圖重複／灰色區塊（本輪重點）

- [ ] 分頁切走再切回來，地圖沒有出現大片灰色未渲染區
- [ ] 瀏覽器視窗從很窄拉到很寬（或用 DevTools 裝置模擬切換 390px → 1440px → 390px），地圖沒有出現世界地圖水平重複平鋪
- [ ] 地圖下方沒有出現空白/tile 只載入一半
- [ ] Loading 提示文字不會卡住不消失（正常情況下應該很快消失）

---

## 4. Save（儲存設定）

- [ ] 修改任一設定（例如切換模式、改 zoom）
- [ ] 點擊「💾 儲存設定」
- [ ] 出現 Toast「Geo 地圖設定已儲存」
- [ ] 停留在原本的 Geo Intelligence 分頁（沒有跳走）
- [ ] Preview 畫面沒有消失或重置
- [ ] **F5 整頁重新整理**，重新進入 Geo Intelligence 分頁，確認剛剛的修改還在

### Save 失敗情境（可選，若能模擬網路離線）

- [ ] 離線或擋掉 API 後點儲存，出現失敗 Toast，欄位內容不被清空

---

## 5. 重新載入已儲存設定

- [ ] 修改設定但**不要**按儲存
- [ ] 點擊「🔄 重新載入已儲存設定」
- [ ] 確認畫面恢復成上次**已儲存**的值（剛剛沒存的修改消失）
- [ ] 確認畫面沒有整頁重新整理（網址列沒有轉圈重新載入）

---

## 6. 還原系統預設

- [ ] 點擊「↺ 還原系統預設」
- [ ] 確認畫面變成預設值（店家位置模式、zoom 12）
- [ ] 出現提示：尚未儲存，需要再按「儲存設定」
- [ ] **不要按儲存**，重新整理頁面，確認資料庫裡的設定沒有被改變（因為只是本地重設，不是立即 PATCH）

---

## 7. 定位（Geolocation）

- [ ] 切到「店家位置」模式
- [ ] 點擊「📡 取得目前定位」
- [ ] 瀏覽器跳出定位權限詢問
  - [ ] 選「允許」→ 緯度/經度欄位自動帶入、出現成功訊息
  - [ ] 重新整理頁面，改成選「拒絕」→ 出現友善的拒絕提示，不會讓頁面崩潰
- [ ] 沒有在**還沒點擊按鈕之前**就自動跳出定位詢問（必須是使用者主動點擊才觸發）
- [ ] 按鈕點擊後沒有永遠卡在 loading 狀態

---

## 8. Responsive（用 DevTools 裝置模擬或實機）

- [ ] **390px**：單欄排列、自訂範圍四個欄位不會擠成一行、按鈕會換行、Preview 高度約 300px、沒有水平捲軸
- [ ] **768px**：欄位排列自然、Preview 沒有被裁切
- [ ] **1024px**：排版合理、Preview 正常顯示
- [ ] **1440px**：沒有過度拉寬或大片空白、深色主題視覺一致

---

## 9. 鍵盤操作（滑鼠先放旁邊，全程只用鍵盤）

- [ ] `Tab` / `Shift+Tab` 可以依序聚焦每一個欄位/按鈕，順序合理
- [ ] Focus 樣式清楚可見（看得出目前聚焦在哪個元素）
- [ ] Radio 群組可以用方向鍵切換
- [ ] Checkbox 可以用 `Space` 切換
- [ ] Button 可以用 `Enter` 或 `Space` 觸發
- [ ] 沒有被卡住出不來的 focus trap

---

## 10. Console 檢查

- [ ] 從進入分頁到操作完所有項目，Console **沒有**任何未預期的紅字 Error
- [ ] 沒有出現 `Cannot read properties of null/undefined`
- [ ] 沒有出現 SyntaxError

---

## 11. Network 檢查

- [ ] `/api/settings/geo-map`（GET/PATCH）狀態碼正常（200），沒有重複無限發送
- [ ] `/data/geo/taiwan/manifest.json` 載入正常，沒有 404
- [ ] 對應的 GeoJSON 檔案載入正常，沒有 404
- [ ] 沒有看到任何指向外部網域的 GeoJSON 請求（不應該有 `http(s)://其他網域/...`）

---

## 回報格式（請填寫後回傳）

```
Manual Visual Result：

Browser：
OS：
Viewport：
Geo Tab：
Mode Switching：
Preview：
Save：
Reload：
Defaults：
Geolocation：
Responsive：
Keyboard：
Console：
Network：
Issues Found：
Final Result：PASS / FAIL
```
