# R5.3-A1.2 — Analytics Visitor Geo Sync — Manual QA Checklist

狀態：R5.3-A1.2：QA BUILD。Automated Verification（Visitor Geo Sync Smoke
185/185、全部 Regression 0 FAIL）：PASS。Manual Visual：**REQUIRED（本清單
尚未執行）**。Final Completion：NOT YET APPROVED。

---

## 一、Visitor Layer 切換

- [ ] 進入 Geo Intelligence → Heatmap 分頁，看得到「訂單熱區 Order
      Heatmap」／「訪客熱區 Visitor Layer」兩個 Layer 切換按鈕。
- [ ] 預設停在「訂單熱區」，畫面跟切換功能上線前完全一樣（Circle/Marker、
      Control Bar、Ranking 皆不變）。
- [ ] 切到「訪客熱區」：Control Bar 換成時間範圍（近5分鐘/30分鐘/今日/7天/
      30天），下方出現 Geo Visitor Summary、Visitor Coverage、Visitor
      Ranking、Recent Visitor Log。
- [ ] 來回切換兩個 Layer 數次：地圖不重建、不閃爍，Order Heatmap 原本的
      Circle/Marker 沒有消失或跑位。

## 二、無訂單也能看到訪客

- [ ] 找一個**尚未有任何訂單**的測試店家，瀏覽幾個頁面（page_view）。
- [ ] 切到「訪客熱區」：Geo Visitor 數字 > 0（不再是 0），Ranking/Recent
      Visitor Log 看得到剛剛的瀏覽紀錄。
- [ ] 同一分頁重新整理（同一個 session）多次：Geo Visitor 數字**不會**
      跟著暴增（同一人只算一次）。

## 三、Choropleth（目前預期不上色）

- [ ] 確認地圖上**沒有**因為訪客資料而新增任何色塊（目前所有城市的
      Polygon 都是測試用矩形 fixture，尚未有官方合法邊界，這是預期行為，
      不是 bug）。
- [ ] 確認地圖上**沒有**任何訪客的 CircleMarker/圓點（IP 定位到行政區
      層級，沒有精確座標，本輪刻意不畫點）。
- [ ] 即使地圖上看不到訪客標記，Ranking／Summary／Coverage／Recent
      Visitor Log 仍要完整顯示資料，不會因為「沒有 Polygon 可畫」就消失。

## 四、Geo Visitor / AddToCart / Checkout / Orders

- [ ] 走一次完整流程（瀏覽 → 加入購物車 → 進入結帳 → 完成訂單），確認
      Geo AddToCart／Geo Checkout／Geo Orders 都會各自 +1（不是都變成
      Geo Visitor 的數字）。
- [ ] 同一個人（同一 visitor）重複加入購物車 3 次：Geo AddToCart 只增加
      1，不是 3。

## 五、Unknown 處理

- [ ] 用無法辨識地區的環境（例如關閉/擋掉 IP Geo Provider）瀏覽：
      Recent Visitor Log 看得到一筆行政區顯示「Unknown」的紀錄，不是
      整筆消失。
- [ ] Geo Visitor 統計把這筆 Unknown 計入總數（分子+分母都算），畫面上
      不會出現「Geo Visitor：0，但 Unknown：100%」這種矛盾組合。
- [ ] Visitor Coverage 卡片清楚顯示 Known/Unknown 各自的數字與 Coverage
      百分比。

## 六、Tooltip

- [ ] 若之後有城市開放 Choropleth 上色，滑鼠移到色塊上，Tooltip 明確
      顯示三行：「定位層級：行政區推定」「來源：IP Geo / Analytics
      Sync」「精確座標：未取得」，不會讓人誤以為是精確 GPS 定位。

## 七、Recent Visitor Log

- [ ] 面板顯示時間／行政區／事件／來源四欄，最新的排在最上面。
- [ ] 同一人連續瀏覽 5 個頁面：Recent Visitor Log 顯示全部 5 筆紀錄（保留
      完整足跡），但上方 Summary 的 Geo Visitor 數字仍是 1（不是 5）。
- [ ] 沒有任何紀錄時，顯示「目前沒有訪客紀錄」，不是空白一片。

## 八、時間範圍切換

- [ ] 切換近5分鐘/30分鐘/今日/7天/30天，Summary/Ranking/Recent Visitor
      Log 都會跟著重新整理成對應範圍的資料。
- [ ] 切到「近5分鐘」時，確認幾分鐘前的舊資料正確被排除。

## 九、Store Isolation

- [ ] 切換到另一家店家：訪客熱區的所有數字/清單都重新整理成新店家的資料，
      不會殘留前一家店的訪客足跡。
- [ ] 兩家不同店家即使剛好有相同的 visitor_id（理論上不太會發生，但邏輯
      上要正確隔離），彼此的統計數字不得互相影響。

## 十、Loading / Error

- [ ] 切換時間範圍或 Layer 時，短暫看得到 Loading，資料回來後自動消失。
- [ ] 手動模擬 API 失敗：畫面顯示安全降級狀態，不是白畫面，Console 沒有
      未捕捉的例外。

## 十一、Responsive / Dark Theme

- [ ] 手機窄版下，Recent Visitor Log 的四欄資訊排版正常，不會被截斷或
      重疊。
- [ ] Layer 切換按鈕、時間範圍按鈕在深色主題下文字對比清楚可讀。

## 十二、目前已可部署？

- [ ] 以上全部項目皆已人工在真實瀏覽器驗證通過後，才可以把本清單狀態
      改成「Manual Visual：PASS」，並回頭更新
      `R5.3-A1.2_COMPLETION_REPORT.md` 開頭的 Final Completion 狀態。
- [ ] 在完成以上手動驗證之前，本輪結論僅代表**自動化驗證（185 項
      Visitor Geo Sync Smoke + 全部 Regression）通過**，不等於「已完成
      正式視覺驗收」。
