# R5.4-G1.3 — Geo Metric Sync & Coverage Explanation

fix18-10-hotfix30-B5-R5.4-G1.3

---

## 1. Root Cause

真實瀏覽器回報：上方全域 Metric 已選「營收」，下方 Order Heatmap 的 Metric 卻仍高亮「Orders」。追查後確認專案裡實際存在**三個** Metric state，不是兩個：

| State | 檔案 | 用途 |
|---|---|---|
| `geoVisitorState.metric` | `geo-visitor-layer.js` | 「上方全域 Metric」——8-Tab Metric Bar，`GEO_EVENT_METRICS`（8 種：visitors/add_to_cart/checkout/orders/revenue/conversion/cart_abandonment/recommendation_risk） |
| `geoMapState.metric` | `geo-intelligence-map.js` | 共用 Choropleth 地圖自己的 metric，`GEO_MAP_METRICS`（6 種：visitors/orders/revenue/conversion_rate/cart_abandonment_rate/risk） |
| `geoHeatState.metric` | `geo-heatmap.js` | Order Heatmap 分頁自己的 metric，`GEO_HEAT_METRICS`（6 種：visitors/add_to_cart/begin_checkout/orders/revenue/conversion） |

R5.3-A4「Metric Switcher Consolidation」**已經**在 `geoVisitorSetMetric()` 裡把 `geoVisitorState.metric` 的變化透過 `GEO_EVENT_TO_MAP_METRIC` 對照表同步到 `geoMapState.metric`（驅動共用 Choropleth 地圖），這部分本來就正確運作。**但從來沒有同步到 `geoHeatState.metric`**——Order Heatmap 分頁的按鈕/Summary/Ranking/Legend 全部只認自己的 `geoHeatState.metric`，跟上方全域 Metric 完全獨立。這正是「上方變了、下方沒變」的真正原因。

## 2. Global Metric State

`geoVisitorState.metric`，唯一設值函式 `geoVisitorSetMetric(containerId, metric)`（`geo-visitor-layer.js`）。本輪**未修改**它原本同步 `geoMapState.metric` 的邏輯，只在函式最後新增一行呼叫。

## 3. Heatmap Metric State

`geoHeatState.metric`，唯一設值函式 `geoHeatUiSetMetric(metric)`（`geo-heatmap-ui.js`）。本輪擴充此函式：套用新值後，若該值在全域指標中有對應（`GEO_HEATMAP_TO_EVENT_METRIC`），同步呼叫 `geoVisitorSetMetric()` 回推全域 Metric（雙向同步，見 §6）。

## 4. Mapping Table（集中管理，唯一一份）

```js
const GEO_EVENT_TO_HEATMAP_METRIC = {
  visitors: 'visitors', add_to_cart: 'add_to_cart', checkout: 'begin_checkout',
  orders: 'orders', revenue: 'revenue', conversion: 'conversion',
  cart_abandonment: null, recommendation_risk: null,
};
```

反向表 `GEO_HEATMAP_TO_EVENT_METRIC` 由上表自動反轉產生（`Object.keys().reduce()`），不手動維護第二份可能兜不起來的對照表。`cart_abandonment`／`recommendation_risk` 明確映射為 `null`——這兩個全域指標目前沒有對應的 Order Heatmap Metric（Order Heatmap 沒有畫這兩種地理熱區的能力），選到它們時 Order Heatmap **保留原本的 Metric 選擇**，並顯示「此指標目前沒有對應的地理熱區 Metric」，**不得靜默改成 Orders**。

## 5. 單一同步入口

`geoHeatUiSyncMetricFromGlobal(globalMetric)`（`geo-heatmap-ui.js`）是全域→Heatmap 方向**唯一**的同步邏輯所在，由 `geoVisitorSetMetric()` 在完成自己的狀態更新與既有的 Choropleth 同步後，於函式最後呼叫。同步內容：

1. 更新 `geoHeatState.metric`（若有對應）
2. 重新渲染 Order Heatmap 圖層（`geoHeatRenderLayer`）
3. 重新渲染 Ranking／Summary／Coverage Card／Legend（既有函式，未修改邏輯）
4. 重新渲染 Metric 按鈕列（`_geoHeatUiRerenderControlBar`，active/aria-pressed 正確互斥）
5. 重新渲染 Coverage Explanation（`_geoHeatUiRenderCoverageExplanation`，見 §8）

## 6. 雙向同步決策

依 A4 既有設計原則（Single Switcher Invariant：所有模式共用單一 state，不留兩套可能矛盾的控制）採用**雙向同步**：Order Heatmap 的 6 個 Metric 都有對應的全域指標，因此點擊 Order Heatmap 自己的按鈕時，`geoHeatUiSetMetric()` 也會呼叫 `geoVisitorSetMetric()` 把全域 Metric 同步過去。**Reentrancy Guard**（`_geoMetricSyncInProgress`，模組層級布林旗標）防止「上方觸發下方、下方又觸發上方」無限互相呼叫：任一方向的同步函式一開始就檢查旗標，若已在同步中就立即返回，不重複 fetch、不重複 render、不造成按鈕抖動。已用連續 10 次快速雙向觸發驗證不會遞迴或拋例外。

## 7. Refresh 保證

`geoHeatState.metric` 的賦值只發生在兩處：`geoHeatUiSetMetric()`（使用者點擊 Heatmap 按鈕）與 `geoHeatUiSyncMetricFromGlobal()`（全域同步過來）。Date／Channel／Display／Layer Switch／Store Switch／頁面局部 Rerender 觸發的所有既有函式（`geoHeatUiSetChannel`／`geoHeatUiSetDisplay`／`geoHeatUiSetLayer`／`geoHeatHandleStoreSwitch`）本身完全不包含對 `geoHeatState.metric` 的賦值語句，已用原始碼稽核（`scripts/static-audit-g1-3.js` 第 8–12 項）與 Smoke 情境測試（第 28–33 項）雙重驗證。

## 8. Coverage Explanation

新函式 `_geoHeatBuildCoverageExplanationText(metric, total, drawn)`，完全讀取既有、未修改的 `geoHeatBuildSummary()`/`geoHeatState.areas` 既有欄位（`submitted_orders`／`coordinate_count`／`revenue`），區分四種資料狀態：

| 狀態 | 條件 | 範例文字 |
|---|---|---|
| `no_business_data` | total ≤ 0 | 目前沒有符合條件的訂單資料 |
| `no_geo_data` | total > 0 且 drawn ≤ 0 | 今日已有 1 筆訂單，但目前沒有訂單包含可用的地理資料，因此無法顯示地圖熱區。 |
| `partial_coverage` | drawn > 0 且 < total | 10 筆Orders中有 4 筆可顯示於地圖，Coverage 40% |
| API Error | 由既有 `errorEl` 機制處理 | 目前無法載入 Heatmap 資料，請稍後重試 |

六種 Metric（Visitors/Add to Cart/Checkout/Orders/Revenue/Conversion）各自有專屬文案（`GEO_HEAT_COVERAGE_NO_GEO_TEXT`），不共用同一句模糊的「資料不足」。若目前全域 Metric 沒有對應的 Heatmap Metric，額外附加一句提示，明確告知使用者「維持顯示哪一個 Metric」。

## 9. 外帶／外送差異

`GEO_HEAT_TAKEOUT_DELIVERY_EXPLANATION` 是**固定的教育性靜態文字**，說明「為什麼外帶訂單通常沒有可繪製地理資料」，**不是**逐筆真實統計數字（本輪沒有新增任何「外帶/外送筆數」的資料來源或 API，因此不冒充精確統計）。外送最佳化空狀態依既有 `submitted_orders`/`coordinate_count` 語意，區分「今日沒有外送訂單」與「有外送訂單但缺少座標」兩種文案。

## 10. Known Limitations

1. `_geoHeatMetricTotals()` 對 visitors/add_to_cart/begin_checkout/conversion 四個 metric，`drawn` 目前恆為 0（系統性沒有座標來源，見 R5.4-G1 架構文件），Coverage Explanation 誠實反映此現況。
2. Revenue 的「可歸屬地理區域營收」沿用既有 `coordinate_count>0` 判斷慣例（跟 orders 用同一套語意），不是獨立的新統計口徑。
3. 外帶/外送的具體逐筆數字目前沒有資料來源，只提供靜態教育性說明文字。
4. 本文件涵蓋的驗證是自動化（jsdom 執行真實程式碼 + 全專案 `node --check` + 既有 Regression 重跑）；跨瀏覽器/裝置的人工視覺驗收狀態見 `R5.4-G1.3_MANUAL_QA_CHECKLIST.md`。
