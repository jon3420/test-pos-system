# H1.4.4 — GA4 view_item Duplicate Reality Audit

Baseline: `fix18-10-hotfix30-B5-R5.4-G1.6-GA4-H1.4.3-...-full.zip`（byte-identical，見附註）

## 一、Production 症狀

Fresh incognito，新 GA4 Property：

- activeUsers = 1, page_view = 1, session_start = 1, first_visit = 1
- 使用者只點開第一個商品一次
- GA4 Realtime 顯示 view_item = 9

## 二、Call Chain（實際追出來的，不是假設）

```
renderMenu()
  └─ buildCard(p)  ×N（產生商品卡 HTML，每張卡 <div class="prod-card" id="pc-${p.id}">）
  └─ _setupViewProductObserver()
        └─ new IntersectionObserver(threshold: 0.5)
              └─ 對每張 .prod-card[id^="pc-"] 呼叫 observer.observe(card)
              └─ 任何一張卡片進入視窗（isIntersecting）→
                    _trackEvent('view_product', {product_id})
                        ├─ POST /api/analytics/events（內部記錄，不受本輪影響）
                        └─ AnalyticsPlatforms.trackPlatformEvent('view_product', pp)
                              └─ GA4_EVENT_MAP['view_product'] === 'view_item'  ← 根因
                              └─ trackGA4('view_product', ...) → gtag('event', 'view_item', ...)
```

`_setupViewProductObserver()` 在 `renderMenu()` 每次執行時都會重新對「目前畫面上所有商品卡」
呼叫一次 `observe()`。IntersectionObserver 對「呼叫 observe() 當下已經在視窗內」的元素會立刻
觸發一次 callback。手機版面通常一次可看到多張商品卡，所以：

- **首頁載入本身**（不需要任何點擊）就會讓所有一開始就在視窗內的商品卡各自送一次 `view_product`
- 每次 `view_product` 都被錯誤對應成 GA4 `view_item`
- 使用者實際點擊的那一次操作，跟這 9 次 `view_item` **完全沒有關係**——9 次是 9 張卡片各自曝光各送一次，不是同一個點擊被送了 9 次

## 三、Root Cause Classification

**RENDER LOOP FIRING + WRONG EVENT SEMANTICS**（候選 D 不成立，實際是候選組合）：
`view_product`（IntersectionObserver 曝光訊號，語意上等同「清單曝光」）被錯誤對應成
GA4 `view_item`（GA4 官方語意：使用者查看單一商品內容）。曝光訊號本質上會隨畫面上可見
商品數量變動，跟「使用者查看了幾次商品」在語意上是兩件不同的事，錨在同一個 GA4 event
name 上必然造成計數失真。

`view_item` 本身不存在第二個平行 sender、不存在 render loop 直接呼叫、不存在
all-item payload（`items` 陣列本身在修正前甚至沒有被組出來——見下方「順便發現的缺陷」）。

## 四、Fix Applied（Final — Production Modified = 1）

`public/js/analytics-platforms.js` 的 `GA4_EVENT_MAP` 移除 `view_product: 'view_item'`
這一行對應。`view_product` 保留給：
- 內部 `/api/analytics/events` 記錄（未受影響）
- 既有 Meta `ViewContent`（`META_EVENT_MAP` 未變更，語意不動）

不再流向 GA4 `view_item`。

`line-order.html`、`line-shipping.html` 現在與 H1.4.3 baseline **byte-identical**
（用 `diff -q` 驗證）。第一版曾在這兩個檔案加上一個商品卡點擊事件與 GA4 payload 完整度
改善，兩者都不是修正這個 duplicate bug 的必要條件，已全部撤回，理由見第六節與第十節。

## 五、驗證

- `scripts/run-g1-6-ga4-h1-4-4-view-item-runtime.js`：真實載入 production
  `line-order.html` / `line-shipping.html`（含未被 mock 的真實 `analytics-platforms.js`），
  用忠實轉發的 fake `IntersectionObserver` 模擬「9 張卡片依序進入 viewport」，並對真實
  DOM 節點 dispatch click 事件、呼叫真實的 `_trackAddToCart()`/`_trackRemoveFromCart()`。
  **24/24 PASS，重複 3 輪結果一致（24/24, exit=0 ×3）**。
- `scripts/static-audit-g1-6-ga4-h1-4-4.js`：原始碼層級 Hard Gate（A–F）。
  **23/23 PASS，重複 3 輪結果一致（23/23, exit=0 ×3）**。
- H1.4.3 既有 Regression（`run-regression-*.js` 等既有 suite）：**本輪尚未重新執行**，
  沿用第六節「未執行事項」的誠實聲明——沒有獨立重新跑過就不宣稱具體通過數字。
- 兩個頁面用 JSDOM 完整載入（含真實 `analytics-platforms.js`，未 mock 掉），確認頁面
  初始化不 throw；inline `<script>` 全部用 DOM parser（非 regex）逐一 `new Function()`
  驗證可解析：line-order.html 3/3、line-shipping.html 1/1。

## 六、重要 Semantic Finding — 目前沒有合法的「查看單一商品」互動

**這是本輪最重要的發現，必須先回報，不能自行決定。**

檢查 `public/line-order.html` 與 `public/line-shipping.html` 的完整商品卡渲染邏輯
（`buildCard()` / `renderProductGrid()`）後確認：

- 商品卡本身**沒有** `onclick` 或任何點擊反應
- 沒有商品詳情 modal、drawer，也沒有導向商品詳情頁的邏輯
- 專案裡唯一的 `detailSheet` / `fetchAndShowDetail()` 是**訂單查詢**用的（查詢歷史訂單
  明細），跟商品完全無關
- 商品卡上唯一可互動的元素是 `[+]` / `[-]` / 加入購物車按鈕，這些已經對應
  `add_to_cart` / `remove_from_cart`，語意上跟「查看商品」是兩件事

**結論：目前系統不存在明確的 product-detail view action。**
點擊商品卡（卡片本身、圖片、名稱、描述任何地方）目前**沒有任何 UI 反應**。

上一輪我曾經加上一個 `handleProductCardView()` / `bindProductViewDelegation()`，
在商品卡上綁一個只會呼叫 tracking、使用者看不到任何反應的隱形 `onclick`，讓
`view_item` 在「點擊」時送出。**這個做法已經被撤回，且 line-order.html /
line-shipping.html 已恢復成跟 H1.4.3 baseline byte-identical**——這本質上是為了讓
Analytics 數字符合預期，而自己發明一個看不見的互動語意，跟 Section 十三禁止的
「掩蓋根因」手法性質相同，只是換了一種形式（造事件 vs. 灌水測試）。撤回後，
GA4_EVENT_MAP 裡目前**沒有任何 key 對應到 `view_item`**，原始碼裡也沒有任何
字面字串 `'view_item'`（已用 static audit 驗證，涵蓋全部 3 個檔案）。

同一輪也曾在 `_trackEvent()` payload 加上 GA4 `items` 陣列（含 `item_name`）的完整度
改善，這是真實存在但獨立於本次 duplicate bug 的次要缺陷（GA4 payload 本來就沒有帶
`item_id`/`item_name`）。因為它不是修正 duplicate 根因的必要條件，且會讓
Production diff 超出「只修 duplicate view_item」的範圍，本輪也一併撤回，列入第八節
Future Finding，等日後有明確的 `view_item`（或 `view_item_list`）送出點時再一起做。

## 七、目前狀態與需要的決定

- **Duplicate 根因**：已修正並驗證。`view_product`（清單曝光）不會再流向 GA4 `view_item`，
  無論首頁載入、換分類、搜尋、重新 render 多少次，都不會產生任何 `view_item`。
- **`view_item` 本身**：目前完全不會被送出（因為沒有合法的觸發來源），這是刻意的
  空狀態，不是遺漏。

需要 Production 決定其中一項，才能讓 `view_item` 重新有數據：

1. **新增真正的商品查看互動**（例如商品詳情彈窗、規格展開、獨立商品頁），
   `view_item` 綁在那個互動上——這是額外的產品功能開發，不是本輪 tracking bug fix
   的範圍，需要另外立案。
2. **暫時不送 `view_item`**，直到 (1) 做完為止；如果商業上需要「商品清單被看到多少次」
   這種數據，正確的 GA4 事件應該是 `view_item_list`（Section 六已提過，本輪刻意不新增，
   列為 Future Finding）。

## 八、Future Finding（不在本輪範圍內，僅記錄）

- 如果需要商品清單曝光統計，正式作法是把 `_setupViewProductObserver()` 的
  `view_product` 額外（不是取代）對應到 GA4 `view_item_list`，payload 用
  `items: [目前一次曝光的所有商品]`。這跟本輪修正的 `view_item` 是兩個獨立事件，
  不會互相污染彼此的計數。
- `_trackEvent()` 送到 GA4 的 payload 目前不含 `items` 陣列（只有 Meta 用的
  `content_ids`/`content_type`），也就是說 `add_to_cart` 目前送到 GA4 的事件本身沒有
  `item_id`/`item_name`。這是獨立於本輪 duplicate bug 的既有缺陷，本輪為了保持
  Production diff 最小（只修 duplicate view_item）而不處理，留給下一輪一併處理
  `add_to_cart` 的 payload 完整度、以及（如果決定要做）新的 `view_item`/`view_item_list`
  送出點。
- 如果任何 Dashboard／報表畫面把 internal `view_product`（清單曝光）標示成
  「GA4 商品瀏覽」或跟 GA4 `view_item` 混用文字，需要另開 Follow-up Finding 核對
  UI 文字是否誤導；本輪未發現這類畫面，但也沒有做全面 UI 文字審查（範圍外）。

## 九、Production Diff（本輪最終，Production Modified = 1）

| 檔案 | 變更內容 |
|---|---|
| `public/js/analytics-platforms.js` | 移除 `GA4_EVENT_MAP` 的 `view_product → view_item` 對應，補充根因說明註解 |
| `public/line-order.html` | **無變更，byte-identical to H1.4.3 baseline** |
| `public/line-shipping.html` | **無變更，byte-identical to H1.4.3 baseline** |

其餘（`services/`、`routes/`、`utils/`、`middleware/`、DB schema、
`public/js/geo-ga4-h1-panel.js`）：0 diff，已用 `diff -rq` 驗證。

## 十、Rejected Attempt（本輪開發過程中，已否決並撤回）

開發過程中，曾短暫 prototype 一個「點擊商品卡 → 送 GA4 view_item」的追蹤點
（`handleProductCardView()` / `bindProductViewDelegation()`），讓 `view_item` 有數據可看。

Reality Audit 證明商品卡本身沒有任何 detail-view 互動（見第六節）。這個只為了送
tracking event、使用者看不到任何反應的隱形點擊掛鉤，因此被判定為不合法的語意發明，
在正式 Gate 之前**已完整撤回**——這是一次好的自我修正（good correction），
不是一個 Production feature，最終 `line-order.html`／`line-shipping.html`
都恢復成跟 baseline byte-identical。
