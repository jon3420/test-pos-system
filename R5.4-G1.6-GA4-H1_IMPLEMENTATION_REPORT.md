# R5.4-G1.6-GA4-H1 — Implementation Report

## 交付範圍

GA4 城市歷史統計、即時快照與行政區轉換地圖：Migration、Normalizer、
Sync Service、GA4 Client Adapter、API Routes、Frontend Panel（含搜尋／
排序）、五層測試（QA Harness / Credential Guard / Frontend Runtime /
Targeted Smoke / Static Audit）、正式 Regression Runner、七份文件、
Security Scan、最終 ZIP。

## 本輪發現並修正的真實問題

### 1. Route Validation／Rate Limit Ordering Bug

**原問題**：`POST /api/analytics/ga4-geo/sync` 先扣 Rate Limit 額度，
再驗證 `sync_type`/`range`。同一店在節流視窗內先送一次合法同步，緊接著
送一次打錯 range 的請求，第二個請求會被誤判為 429 `rate_limited`，而不
是正確的 400 `invalid_range`——使用者看到誤導性的錯誤訊息。

**修正**：`routes/ga4-geo.js` 新增 `_validateSyncBody()`，在扣 Rate Limit
之前先做完整輸入驗證（`sync_type` 白名單、`range` 白名單、`custom` 日期
格式）。只有通過驗證的請求才會消耗 Rate Limit 額度；驗證失敗完全不呼叫
Sync Service、不寫 DB、不建立 Cache。

**發現方式**：Targeted Smoke 的真實 HTTP 整合測試（非 mock router）。

### 2. Missing GA4 Credential → Process Crash

**原問題**：完全沒有設定 `GOOGLE_APPLICATION_CREDENTIALS` /
`GA4_SERVICE_ACCOUNT_JSON(_BASE64)` 時，`_getClient()` 仍嘗試
`new BetaAnalyticsDataClient()`，google-auth-library 內部嘗試隱含 ADC
解析，產生一個跟呼叫端 await 鏈脫勾的背景 Promise。該 Promise reject 後
變成 Node 的 `unhandledRejection`，實測會讓整個 process crash（不是被
`try/catch`接住的一般錯誤）。本專案部署在 Zeabur（非 GCP），永遠不會有
隱含 ADC 可用，因此這個路徑在正式環境是必然會踩到的地雷，不是邊界情況。

**修正**：`utils/ga4Realtime/client.js` 的 `_getClient()` 在「真的要建立
新 Client」（`_client` 尚未被 `_setClientForTest()` 注入）且
`credentialStatus().available === false` 時直接回 `null`，觸發既有的
`SDK_UNAVAILABLE` fail-open 路徑，完全不呼叫 SDK 建構子。

**保留**：`_setClientForTest()`/`_resetForTest()` 測試注入路徑完全不受
影響——Guard 放在「已注入判斷」之後才生效。Credential Guard Suite 的
Scenario B/C/D（17/17）與既有 G1.5-A 測試套件（140/140、77/77）逐一驗證
過這個保留是真的，不是假設。

### 3. G1.5-A Outdated Static Assertion（Category B）

**原 Contract**：`scripts/static-audit-g1-5-a.js` check #2 斷言
`client.js` 全檔完全不得出現 `.runReport(` 呼叫（防止 Realtime 路徑被
誤植換成 Historical API）。

**新 Contract**：GA4-H1 新增 `runGa4Report()`，刻意呼叫
`client.runReport(request)` 以支援 Historical 查詢，跟 Realtime 共用同一
個 Singleton Client（避免建立第二個 GA4 Client）。

**為何屬於合法變更**：原始斷言真正要保護的是「Realtime 路徑本身不能被
靜靜換成 Historical API」，不是「整份檔案永遠不能出現 `runReport` 字面」。
修正後的斷言改成只檢查 `runGa4RealtimeReport()` 這一個函式自己的函式體，
精準對應原始意圖，同時允許新增的獨立函式使用 `runReport`。

### 4. Server-side marker_point Gap

**原問題**：初版 API 只回傳 `district_code`/`county_code`，Frontend 沒有
合法管道查詢 A1.2 Authoritative Catalog（那是 Server-only 檔案）。

**修正**：`getRealtimeGeoSummary()`/`getRangeGeoStats()` 在 Server 端用
`resolveMarkerPoint()` 附加 `marker_point: {lat,lng}|null` 與
`marker_accuracy`。Frontend（`geo-ga4-h1-panel.js`）只讀
`row.marker_point`，從未自己查表、猜座標、或使用地圖中心。

### 5. Frontend Search／Sort

補齊 UI 規格缺項：
- 搜尋：`county_name`/`district_name`/`city_raw`/`region_raw`/
  `country_raw`，trim、case-insensitive、中英文皆支援；只重繪
  `<tbody>`（不重建搜尋框本身），保留使用者輸入焦點/游標位置。
- 排序：13 個欄位，第一次點擊 descending，第二次 ascending；中文欄位用
  `localeCompare('zh-Hant')`；`null`/`undefined`/`'—'`/`NaN` 一律穩定排
  在最後（不論方向）。
- Mode 切換重設搜尋／排序；Metric 切換保留——兩者皆有對應測試固定行為。
- 搜尋／排序純粹是前端記憶體操作：不呼叫 fetch、不寫 DB、不動 Marker
  Layer、不修改原始 cached rows（回傳全新陣列）。

### 6. Frontend Runtime Export

新增的正式 Public Function（`public/js/geo-ga4-h1-panel.js` 的
`module.exports`）：

- `geoGa4H1RenderToolbar` — 渲染 Mode/Metric/Sync 控制列，正式 UI 邏輯
  的一部分，不是測試專用。
- `geoGa4H1RenderMarkers` — 渲染 GA4 Aggregate Marker，正式渲染邏輯。
- `geoGa4H1Fetch` — 統一的 fetch 包裝（AbortController + URL 組裝），
  正式資料存取邏輯。

**確認**：三者都是頁面正常運作時會被 `geoGa4H1Refresh()`/`geoGa4H1Init()`
實際呼叫的正式函式，只是額外 export 出來方便測試直接呼叫；沒有
`testOnly` 命名、沒有 `NODE_ENV` 分支、不暴露任何 Credential 或 GA4
Client 內部物件（`geoGa4H1Fetch` 只回傳 parsed JSON，`geoGa4H1RenderMarkers`
只接受已經由 Server 算好的 `marker_point`）。

## 已知範圍限制（誠實記錄，非隱瞞）

- `currency` 欄位存在但恆為 NULL（GA4 Commerce Query 沒有查 currency
  dimension）。
- `view_product_count` 恆為 0（沒有獨立可信事件來源，見 Reality Audit）。
- 沒有自動排程（Cache-first + 手動同步，見 Reality Audit 第八節）。
- Realtime Retention Cleanup 尚未實作（快照會持續累積，本輪範圍不含
  自動清除舊快取）。
- Production GA4 Query Compatibility 未對真實 Property 驗證。
