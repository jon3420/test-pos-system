# R5.4-G1.5-A Test Report

fix18-10-hotfix30-B5-R5.4-G1.5-A — GA4 Realtime Backend Correctness & Store Isolation

## 1. 測試環境

容器內 Linux 沙箱，無真實 Google Cloud 憑證、無真實網路存取 GA4 API
（network allowlist 不含 googleapis.com）。所有 GA4 SDK 呼叫皆透過
`client.js` 的 `_setClientForTest()` 注入假的 `runRealtimeReport()` 實作。

## 2. Node version

`v22.22.2`（`node --version`）。

## 3. 套件依賴狀態

- `@google-analytics/data@6.1.0` 已安裝並可 `require()`（`isSdkAvailable()`
  回傳 `true`）。
- `package.json` 宣告 `^6.1.0`，`package-lock.json` resolved `6.1.0`，兩者
  一致（已用 `node -e` 直接讀取兩份 JSON 比對，見 Reality Audit Addendum
  §14）。
- `express`/`body-parser`/`cors`/`jsonwebtoken` 等既有相依套件另行以
  `npm install` 補齊（本沙箱先前沒有 `node_modules`），僅為讓
  `routes/geo-live.js` 能被 `require()` 驗證語法/依賴完整性，不影響交付
  內容（`node_modules` 不納入 ZIP）。

## 4. Fake GA4 Client 架構

`utils/ga4Realtime/client.js` 的 `_setClientForTest(fakeClient)` 直接替換
內部 lazy singleton。假 client 只需實作
`async runRealtimeReport(request) → [response]`（符合官方 SDK 回傳陣列的
慣例）。測試依 `request.dimensions.length === 0` 判斷是 Summary 還是 City
Request，回傳對應假資料。

## 5. Store A／Store B fixture

```js
{
  store_a: { ga4_realtime_enabled:'1', ga4_realtime_property_id:'111111', ga4_realtime_stream_id:'9001' },
  store_b: { ga4_realtime_enabled:'1', ga4_realtime_property_id:'222222', ga4_realtime_stream_id:'9002' },
}
```
`makeFakeDb(rowsByStore)` 模擬 `db.all(sql, [storeId, ...keys])` 只回傳該
storeId 底下存在的 key，物理上不可能跨店讀取（測試層面模擬 SQL
`WHERE store_id=?` 的效果）。

## 6. Summary／City mock

Summary mock：`dimensionValues:[]`、`metricValues:[{value:'4'},{value:'10'}]`
（activeUsers=4, eventCount=10）。
City mock：兩列 `[Taoyuan City, TW, 2, 5]`、`[台北市, TW, 2, 5]`，總計
巧合等於 4，另有專門測試案例（C-agg 系列）用不相等的數字驗證
「summary 總數 ≠ county 加總」不是巧合依賴。

## 7. 140 個 Smoke 覆蓋分類

| 分類 | 項目數 | 內容 |
|---|---|---|
| 0-parse | 6 | node --check 所有本階段檔案 |
| A. Store Config | 10 | 隔離／disabled／缺 stream／single-store fallback／invalid property/stream |
| B. Request Builder | 25 | property 格式、summary/city 維度、minuteRanges、filter、metric mapping、quota flag |
| C. Aggregation/Dedup | 5 | 縣市聚合、非 TW 排除、unmapped、sanity cross-check |
| D. Mapping | 21 | Taoyuan/New Taipei/Taipei/Hsinchu/Chiayi 等 15 個别名 + 6 個 ambiguous/unmapped 邊界 |
| E. Cache | 19 | 命中/未命中、跨 store/metric/window key、TTL clamp 8 項、refresh bypass |
| F. Single-flight | 6 | 10 併發只 2 次呼叫、跨 store/metric 隔離、reject 後重試 |
| G. Retry/Error | 13 | 可重試/不可重試分類、退避排程、非重試立即失敗 |
| H. Route | 13 | req.storeId、query allowlist、middleware 保留、disabled/not_configured/status payload |
| I. Regression/Security | 9 | 無憑證外洩、無第二地圖、Scope Guard 存活、程式衛生 |
| J. Mutation Negative | 9 | 錯誤去重邏輯偵測、禁用欄位掃描、cache key 缺 store 偵測、stale 不可偽裝 fresh |
| **總計** | **140** | |

執行方式：直接 `require()` 並呼叫模組匯出的函式（`_aggregateCityRowsForTest`
`_runWithRetryForTest` 等），並注入假 client／假 db，不是字串比對冒充。

## 8. 77 個 Static Audit 覆蓋分類

SDK/Request 欄位（13）、Metric Mapping（8）、ActiveUsers 去重（8）、Store
Isolation（8）、Cache/Single-flight/Retry/Stale/Quota（15）、安全/憑證
（10）、Route/Status/依賴（6）、程式衛生/Regression 邊界（9）。全部用
`fs.readFileSync` 讀原始碼 + 正規表達式比對「程式碼行」（先用
`codeOnly()` 濾掉 `//` 開頭的註解行，避免說明性註解裡提到的禁止字串造成
誤判——這個誤判在本階段開發過程中真的發生過三次，見下方 §15）。

## 9. 三輪結果表

| Suite | Round1 | Round2 | Round3 | Exit |
|---|---|---|---|---|
| G1.5-A Smoke | 140/140 | 140/140 | 140/140 | 0/0/0 |
| G1.5-A Static Audit | 77/77 | 77/77 | 77/77 | 0/0/0 |
| G1.4.1 Smoke | 149/149 | 149/149 | 149/149 | 0/0/0 |
| G1.4 Smoke | 148/148 | 148/148 | 148/148 | 0/0/0 |
| G1.3.2 Smoke | 148/148 | 148/148 | 148/148 | 0/0/0 |
| A2 Smoke | 229/229 | 229/229 | 229/229 | 0/0/0 |
| A1.2 Smoke | 189/189 | 189/189 | 189/189 | 0/0/0 |
| G1(geo-live) Smoke | 212/212 | 212/212 | 212/212 | 0/0/0 |
| Static Audit G1.4.1 | 56/56 | 56/56 | 56/56 | 0/0/0 |
| Static Audit G1.4 | 52/52 | 52/52 | 52/52 | 0/0/0 |
| Static Audit G1.3.2 | 48/48 | 48/48 | 48/48 | 0/0/0 |

三輪數字完全一致，FAIL=0，exit=0。單輪耗時約 14-15 秒（含 node 啟動與
sql.js 初始化開銷）。

**重要修正記錄（誠實記錄，過程中真的抓到的問題，不是一次就對）**：

1. 第一次執行時 `audit14`／`audit132` 各短少 1 項 OK（51/52、47/48）——
   原因不是程式碼問題，而是我的 Regression 執行順序把「會建立真實
   `data/pos.db`」的 Smoke 排在 Static Audit 之後，導致 Static Audit 的
   「no test DB」檢查看到殘留檔案。修正：改成「先跑完所有會建 DB 的
   Smoke → 清除 `data/pos.db` → 才跑 Static Audit」，這正是 G1.4.1
   Completion Report 已經記載過的既有協議，本階段只是重新對齊執行順序。
2. Smoke 腳本內部一次 `resetForTest()` 之後忘記重新注入 fake client，
   造成一個未注入真實 SDK 憑證環境下嘗試呼叫真實 Google Auth 而丟出未捕捉
   例外（`MetadataLookupWarning`／`Could not load the default
   credentials`），整個 process 崩潰。修正：每次 `resetForTest()` 後立即
   重新 `_setClientForTest()`。
3. 三個 Static Audit 檢查項（buildGa4DimensionFilter 相關兩項、
   credentialStatus 相關一項）最初用過於寬鬆的正規表達式，被檔案內自己的
   說明性註解（逐字寫出禁止出現的模式作為文件說明）誤判為「真的違反」。
   修正：對這些檢查改用 `codeOnly()`（濾掉註解行）或改用更精確比對實際
   呼叫語法的 regex。

## 10. 現有 Regression 結果

見 §9 表格；數字與 `R5.4-G1.4.1_COMPLETION_REPORT.md` 記載的既有基準完全
相同（149/148/148/229/189/212/56/52/48），確認 G1.5-A 沒有造成任何既有
G1.x／A1.2／A2 功能退化。

## 11. Test Isolation

- 每輪開始前：`orch.resetForTest()`（清 `_cache`／`_inFlight`／
  `client._resetForTest()`），確認 `cache_entries=0`／`inflight=0`。
- 每輪結束後：再次確認 `cache_entries=0`／`inflight=0`（Smoke/Static Audit
  執行過程中產生的 cache 會在下一輪開始前被清掉；G1.5-A Smoke 內部各區塊
  本身也在每個子場景前呼叫 `resetForTest()`，避免跨場景污染）。
- 三輪之間沒有共用任何 in-memory 狀態或 fake client 實例（每輪都是獨立
  的 `node` process）。

## 12. data/pos.db 清理

三輪皆確認：Phase 1（Smoke）結束後清除、Phase 2（Static Audit）前確認
已清除、整輪結束後再次確認「no data/pos.db residue」。

## 13. credential scan

`public/js/*.js` 掃描 `GA4_SERVICE_ACCOUNT|GOOGLE_APPLICATION_CREDENTIALS|
private_key|GA4_PROPERTY_ID|GA4_STREAM_ID` 無結果。額外掃描到
`public/js/app.js`／`public/js/line-member-gate.js` 含 `access_token`
字樣，人工確認後是既有 LINE LIFF OAuth token 處理（跟 GA4/Google Cloud
憑證無關），非本階段引入、非洩漏。

## 14. package-lock 檢查

`@google-analytics/data` 在 `package.json`（`^6.1.0`）與
`package-lock.json`（resolved `6.1.0`）中版本相容一致。

## 15. 未執行項目

- Real GA4 Live Test：**NOT TESTED**（無真實憑證/Property，也無出網
  allowlist）。
- Manual Visual：**NOT TESTED**（無真實瀏覽器環境，且本階段規格禁止動
  前端）。
- Frontend Integration：**NOT TESTED**（前端檔案本階段未修改，contract
  已知不相容，見 API Contract 文件）。
- 完整 23+ Suite 全量三輪 Regression：本階段只執行 §三 指定的 11 項組合，
  未執行 G1/G1.1/G1.2/G1.3/G1.3.1/A7/A5/A5-live-e2e/A4 等其餘既有 suite
  （非本階段規格要求範圍，留給最終 G1.5 QA 階段）。
