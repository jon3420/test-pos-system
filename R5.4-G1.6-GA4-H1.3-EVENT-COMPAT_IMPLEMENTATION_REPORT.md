# R5.4-G1.6-GA4-H1.3-EVENT-COMPAT — Implementation Report

fix18-10-hotfix30-B5-R5.4-G1.6-GA4-H1.3-EVENT-COMPAT

---

## 一、Production Diff（與 H1.2 baseline byte-diff 確認）

實際修改的 8 個檔案（`diff -rq` 對照 H1.2 baseline zip 確認，無其他檔案被觸碰）：

| 檔案 | 修改內容 |
|---|---|
| `utils/ga4Realtime/requestBuilder.js` | 新增 `GA4_REQUEST_VARIANT`、`buildRealtimeDimensions()`；Summary/City Request 在建構後（保留原本 `dimensions: []` / `[{city},{countryId}]` 字面literal）多一步 `request.dimensions = buildRealtimeDimensions(...)`，只在 event metric（`eventName` 有值）時才加入 `eventName`（＋`streamId`）維度。Visitors 完全不受影響。 |
| `utils/ga4Realtime/requestPair.js` | 抽出 `runGa4RealtimeSingleRequest(stage, request, windowMinutes, metric, runFn)`，供 `runGa4RealtimeRequestPair()` 與 `connectionTest.js` 的 Event Compatibility Probe 共用同一套安全 Log／錯誤處理，不新增第二套。 |
| `utils/ga4Realtime/connectionTest.js` | 新增 Opt-in Event Compatibility Probe（`includeEventCompatibility`，預設 `false`）。拆成 `_runBasicConnectionTest()`（逐行等同 H1.2 baseline，沿用原本的 `_lastTestAt`／`_lastTestResult`／`_inFlightTest`）與 `_runEventCompatConnectionTest()`（獨立的 `_lastTestAtEventCompat`／`_lastTestResultEventCompat`／`_inFlightTestEventCompat`），兩者物理上不共用任何 Map。 |
| `routes/geo-live.js` | 新增 `_parseGa4EventCompatFlag()`（白名單布林解析：`true`/`1`/`'1'`/`'true'` → true，其餘含 `"false"` 字串 → false），`POST /ga4-realtime-test` 從 body／query 讀取 `event_compat` 旗標並轉成 `includeEventCompatibility`。 |
| `services/ga4GeoSyncService.js` | `_todayDateString()` 改為委派 `utils/dateTime.js` 的 `getTaipeiCalendarDateString()`；新增 test-only clock override（`_setClockForTest`/`_resetClockForTest`），不影響任何既有呼叫點的字面文字。 |
| `utils/dateTime.js` | 新增純函式 `getTaipeiCalendarDateString(dateInput, offsetDays)`，重用既有 `TAIPEI_FORMATTER`，不建立第二套時區解析。 |
| `public/js/geo-ga4-realtime-layer.js` | 新增 `_geoGa4ClassifyErrorFamilyMessage(code)`，作為既有 `GEO_GA4_ERROR_MESSAGES` 字典的 fallback，把 400/401/403/429/TIMEOUT/5xx 等「安全分類後的 code 家族」對應到使用者可讀文案。 |
| `public/js/geo-ga4-h1-panel.js` | 新增 `_geoGa4H1PerUser()`（不乘 100 的 event/user 平均值）與 `_geoGa4H1FormatPerUser()`（固定一位小數字串化，分母 0 → `—`）；`_geoGa4H1Rate` 保留為 legacy alias。Sort Key／表頭／Tooltip 全部改用新名稱與新格式；`_geoGa4H1HandleSyncResult()` 對 `rows_saved===0` 改用中性成功文案。 |

**未修改（byte-identical，已用 `diff -q` 確認）**：`utils/ga4Realtime/index.js`、`utils/taiwanGeoNormalize.js`、`middleware/storeGuard.js`、`middleware/featureGate.js`、`utils/ga4RealtimeConfig.js`、`utils/db.js`，以及所有 Delivery／Fulfillment／DB Schema 相關檔案。

---

## 二、關鍵設計決策

### 2.1 Variant B（見 Reality Audit 三）
Event Metric 的 Summary／City Request，在 filter 引用 `eventName`（必要時 `streamId`）的同時，把它們也列進 `dimensions`。Visitors 的 Request Contract（`dimensions: []`／`[city, countryId]`）完全保留，靠 `buildRealtimeDimensions()` 的 `hasEvent` 判斷把兩條路徑分開。

### 2.2 Event Compatibility Probe 是 Opt-in，不是自動
第一次實作時曾經讓 Connection Test 無條件多打 4 次 Google API，破壞了既有 Single-flight Call Count Contract（`F102`/`F103`）。修正為明確 `includeEventCompatibility` 旗標，並把 Basic／Event 兩條路徑徹底物理隔離成不同的 Map 實例，而不是同一組 Map 加字尾區分 key（更保守，不可能有 key 命名衝突）。

### 2.3 Boolean 白名單解析
`event_compat` 旗標不用 `Boolean(value)`（會把非空字串 `"false"` 誤判成 `true`），改用明確白名單：只有 `true`／`1`／`'1'`／`'true'`（大小寫不拘）視為 true，其餘一律 false。

### 2.4 Taipei Calendar 重用既有 Helper
沒有在 `ga4GeoSyncService.js` 重新發明時區換算，而是在 A1.2.1 已建立的集中 `utils/dateTime.js` 裡新增一個純函式，繼續用同一個 `Intl.DateTimeFormat` 實例。

### 2.5 Per-user 語意修正，不新增 Query
`event_count / active_users` 的顯示語意從「率（%）」改成「平均事件數／人」，純粹是 Presentation 層的重新命名與格式修正，沒有新增或修改任何 GA4 Query、DB Schema、Historical API 欄位。

---

## 三、遇到並修正的實作問題（誠實記錄，不隱藏）

1. **Event Compatibility Probe 一開始無條件執行**，導致 `F102`/`F103`（Connection Test Single-flight Call Count Contract）FAIL。修正：改為 Opt-in ＋ 物理隔離的 Map。
2. **`static-audit-g1-5-b2.js` check 75（無 console.log）FAIL**：新增的 Probe 曾直接在 `connectionTest.js` 呼叫 `console.log`。修正：改為重用 `requestPair.js` 既有的安全 Log 機制（抽出 `runGa4RealtimeSingleRequest`），不在 `connectionTest.js` 新增 Log。
3. **`F101`（rate limit 30 秒後允許）與 static audit check 32（`_lastTestAt.get(storeId)` 字面比對）FAIL**：因為把 Basic／Event 的 Map 用同一個 Map 加 key 字尾區分，導致既有測試直接操作 `_lastTestAtForTest` 這個 test hook 時對不到新的 key。修正：改成完全獨立的兩組 Map（見 2.2）。
4. **Request Builder 的 Variant B 若直接改寫 `dimensions: []` 字面literal**，會打壞 `static-audit-g1-5-a.js` check 7/8（regex 直接比對這個字面文字）。修正：保留原本的 literal 賦值，在後面多一行 `request.dimensions = buildRealtimeDimensions(...)` 才修改實際內容，兩邊都滿足。
5. **City Response 的 metricHeaders 交換順序測試一開始寫錯**：以為 City Metric Parsing 也是 Header-based，結果撞到既有的 Positional 假設（見 Reality Audit 五），修正測試本身而不是誤改 Production 碼。
6. **Mutation F 第一版只 Mutate 了 Rate-limit 檢查的讀取，沒有 Mutate `.then()`/`.catch()` 裡的寫回**，導致 Mutation 沒有真的讓兩個模式共用 Cache。修正：整個函式本體一起 Mutate。
7. **Mutation I 第一版只是移除 `throw`**，結果下游 `summaryResult.metricHeaders.indexOf(...)` 因為 `summaryResult` 沒有 `metricHeaders` 而丟出另一個 TypeError，沒有真正示範「API Error 被吞成 Zero」。修正：Mutate 成明確把 `summaryResult` 覆寫成一個合法的空成功結果。
8. **Mutation R 第一版 Mutate route 層的 `message` 欄位**，證明「不會洩漏」——因為 `Ga4RealtimeError` 的 `message` 早就是分類後的安全 code，不是原始錯誤（見 Reality Audit 四：Defense in Depth）。修正：改成 Mutate `classifyGa4RealtimeError()` 本身的 fallback。
9. **Historical Runtime 的 zero-active-users 渲染測試**，一開始沒注意到 `geoGa4H1RenderTable()` 預設會過濾掉 `active_users===0` 的正常 row，導致假 FAIL。修正：改用既有支援的 `{ showZeroRows: true }` 選項。

---

## 四、最終測試數字（全部 3 輪一致，見 Test Report 細節）

| Suite | 數字 |
|---|---|
| H1.3 Request Builder Contract | 68/68 |
| H1.3 Event Compat Connection Test | 48/48 |
| H1.3 Realtime Event Runtime | 65/65 |
| H1.3 Historical Runtime | 44/44 |
| H1.3 Mutation Suite | 37/37 |
| H1.3 Static Audit | 63/63 |
| Full H1.3 Regression（43 Suites） | 全綠，3 輪一致，0 Residue |
