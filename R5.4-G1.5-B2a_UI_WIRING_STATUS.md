# R5.4-G1.5-B2a UI Wiring Status

fix18-10-hotfix30-B5-R5.4-G1.5-B2a — Settings UI Wiring & Save/Test Workflow

**狀態：Targeted Gate 通過（106/106 Smoke，三輪一致），尚未進行完整 B2 驗收。**

## 1. 實際 Settings tab 架構

沿用既有 `.settings-tab-btn` / `.settings-tab-panel` / `switchSettingsTab(tab)`
（`public/js/app.js`）機制，跟 `geo_map` tab 完全一樣的切法：按鈕
`data-stab="ga4_realtime"`，panel `id="stab-ga4_realtime"`，`switchSettingsTab()`
內新增一行 `if (tab === 'ga4_realtime') { if (typeof geoGa4SettingsInit ===
'function') geoGa4SettingsInit(); }`（緊接在既有 `geo_map` 那行之後）。沒有
建立第二套 tab 系統、沒有建立第二個 settings bootstrap、沒有使用
`alert()`。

## 2. HTML ids

Tab 按鈕：`tab-btn-ga4_realtime`（`data-stab="ga4_realtime"`）。
Panel：`stab-ga4_realtime`。
欄位：`ga4RealtimeEnabled`／`ga4RealtimeAutoRefresh`／
`ga4RealtimePropertyId`／`ga4RealtimeStreamId`／
`ga4RealtimeSinglePropertyMode`（+ 提示文字 `ga4RealtimeSinglePropertyModeHint`）／
`ga4RealtimeCacheSeconds`。
按鈕：`ga4RealtimeSaveBtn`／`ga4RealtimeTestBtn`。
狀態/錯誤元素：`ga4RealtimeServerState`／`ga4RealtimeSettingsStatus`／
`ga4RealtimeTestResult`／`ga4RealtimePropertyError`／
`ga4RealtimeStreamError`／`ga4RealtimeCacheError`／`ga4RealtimeGeneralError`。

Script 順序（`public/index.html`）：Leaflet → geo-intelligence-map.js →
geo-heatmap.js → geo-heatmap-ui.js → geo-ga4-realtime-layer.js →
**geo-ga4-settings.js** → coupons.js（頁面 bootstrap 相關腳本）。只載入
一次，沒有載入到其他頁面。

## 3. Read endpoint

`GET /api/settings/ga4-realtime`（已在上一輪完成，本輪未重做）。
`geoGa4SettingsLoad()` 用 `apiFetch()` + `AbortController` + `requestSeq`
呼叫，`geoGa4SettingsInit()` 只在 `!loaded && !loadError` 時才觸發（切
Tab 不會每次重抓），完全不會觸發 Google API 呼叫。

## 4. Write endpoint

`PATCH /api/settings/ga4-realtime`（已在上一輪完成）。前端
`geoGa4SettingsBuildPatch()` 只組出 6 個白名單欄位，Smoke D34 驗證送出
的 body key 集合跟白名單完全相等（不多不少）。

## 5. Test endpoint

`POST /api/geo-live/ga4-realtime-test`（已在上一輪完成）。前端
`geoGa4SettingsTestConnection()` 送空物件 `{}`，不含
property/stream/credential。

## 6. Validation

`geoGa4SettingsValidateForm()`：Property/Stream 純數字驗證（拒絕
`properties/` 前綴、URL、負數）、enabled 且非 single-property 時兩者
必填、Cache 30~300（含邊界）、single-property mode 在
`server_single_store_mode_available=false` 時一律視為錯誤。前端驗證只是
UX，Server 端 `validateGa4RealtimeSettingsPatch()` 才是權威。

## 7. Save UX

驗證 → 清空舊 field error → 按鈕 disabled/loading → PATCH → 成功
toast + 重新 GET 一次 → 呼叫 `geoGa4NotifySettingsChanged()`（清舊
Polygon/Summary/Timer 並在目前於 GA4 Layer 時強制重新 fetch）→ 恢復按鈕。
失敗時保留使用者輸入、欄位下方顯示錯誤、不顯示 raw server error、不用
`alert()`。

## 8. Connection Test UX

按下立即 disabled + 顯示「測試中…」→ 呼又 API → 依 `connected`／
`rate_limited`／`error_code` 顯示對應中文文案（7 種 error_code 對照表）→
啟動 30 秒倒數 cooldown（`geoGa4SettingsUpdateCooldown()`，用單一
`setInterval`，按鈕文字顯示剩餘秒數，倒數結束自動恢復）。

## 9. Cooldown

前端：呼叫成功後鎖定 30 秒（`testCooldownUntil`），倒數期間再次呼叫直接
被擋下（`if (Date.now() < testCooldownUntil) return;`），不重打 API。同時
`testing` flag 防止同一時間點兩個併發呼叫。伺服器端 rate limit（30 秒/店）
與 single-flight 已在上一輪的 `connectionTest.js` 完成，本輪前端只是消費
`rate_limited` 欄位並顯示文案，沒有重做後端邏輯。

## 10. Auto Refresh integration

`geoGa4FetchAndRender()` 在每次「activate（非 reactivate）」時額外呼叫一次
`GET /ga4-realtime-status` 取得 `auto_refresh_enabled`，寫入
`geoGa4State.configAutoRefreshEnabled`；`_geoGa4ScheduleAutoRefresh()`
在排程前檢查這個 flag，`false` 時完全不排程（手動 `geoGa4Refresh()`
仍可用）。Settings 儲存成功後呼叫 `geoGa4NotifySettingsChanged(newFlag)`
立即更新這個 flag 並在目前於 GA4 Layer 時強制重新 fetch（重新走一次
`_geoGa4ScheduleAutoRefresh()`，不會有兩個 timer 疊加，因為排程函式一律
先 `geoGa4StopAutoRefresh()` 才決定要不要建立新的）。

**Cache 秒數與 Auto Refresh 秒數是兩個獨立概念，沒有混用**：`cache_seconds`
只影響後端 `_cache` 的 TTL；前端 auto-refresh 的 60/120 秒完全是另一組
獨立邏輯（依 `quota_status`），本輪沒有讓兩者互相覆蓋。

## 11. Targeted Smoke

`scripts/smoke-hotfix30-b5-r5-4-g1-5-b2a-ga4-settings-ui.js` →
**106 PASS / 0 FAIL / 106 TOTAL**（規格要求本回合 ≥100），三輪一致。
過程中發現並修正一個真實跨檔案 regression：新增的
`public/js/geo-ga4-settings.js` 說明性註解裡逐字提到「private_key」等
禁止欄位名稱，導致 G1.5-A smoke 的 credential-leak 掃描（I1）誤判——已修正
**測試本身**（改用去除註解後的原始碼掃描），沒有修改任何產品程式碼或
G1.5-A 的去重/隔離/Cache Key/Retry 邏輯。

## 12. Targeted Regression（三輪）

| Suite | Round1 | Round2 | Round3 |
|---|---|---|---|
| B2a Smoke | 106/106 | 106/106 | 106/106 |
| B1 Smoke | 168/168 | 168/168 | 168/168 |
| B1 Static Audit | 71/71 | 71/71 | 71/71 |
| G1.5-A Smoke | 140/140 | 140/140 | 140/140 |
| G1.5-A Static Audit | 77/77 | 77/77 | 77/77 |
| G1(geo-live) Smoke | 212/212 | 212/212 | 212/212 |

`node --check` 全數通過：`geo-ga4-settings.js`／`geo-ga4-realtime-layer.js`／
`routes/settings.js`／`routes/geo-live.js`／`ga4RealtimeConfig.js`／
`ga4Realtime/index.js`／`connectionTest.js`。每輪之間清除 `data/pos.db`，
無殘留；jsdom 測試每個區塊使用獨立 window，process 結束前強制
`process.exit()`，無 timer/open handle 殘留。

## 13. 尚未完成（留給 B2b）

- 完整 B2 Smoke ≥170（本輪只做了 106 項 targeted gate，不是最終驗收）
- B2 Static Audit ≥70
- 正式三輪完整 Regression（B2b 需要涵蓋 G1.4.1/G1.4/G1.3.2/G1.2/A2/A1.2
  等本回合未重跑的既有 suite）
- 四份 B2 正式文件（`SETTINGS_ARCHITECTURE`／`CONNECTION_TEST`／
  `TEST_REPORT`／`MANUAL_QA_CHECKLIST`）
- B2 QA ZIP 與驗證
