# fix18-10-hotfix30-B5-R5.4-G1.3.1 — Completion Report

## 範圍確認

本輪只修正兩個真實瀏覽器人工驗收問題：
1. Coverage Explanation 誤將「有訂單但無 Geo」顯示成「沒有訂單資料」。
2. Coverage Explanation 在 Dark Theme 下白底／白字。

未重做 G1／G1.1／G1.2／G1.3，未開始 G2，未新增功能，未修改 DB Schema、
Backend 訂單／營收定義、Geo Engine、Coordinate API、A7 KPI、訂單統計口徑、
Metric Sync Mapping。

## 誠實記錄

- **確實修改了 Backend additive response**：`utils/geoAnalyticsQueries.js`
  的 `getGeoFulfillment()` 新增 `business_total_orders` /
  `business_total_revenue` 兩個欄位。
- **沒有改變既有欄位語意**：`areas`／`page`／`limit`／
  `takeout_no_fulfillment_address` 完全不動；`coordinate_count`／
  `coordinate_source`／`submitted_orders` 語意不變，仍是「Geo Drawable」。
- **沒有改變訂單／營收定義**：新查詢逐字元沿用既有 `ORDERS_BASE_WHERE`／
  `ORDERS_PAID_EXPR` 常數，未新建第二套訂單有效性或營收判斷邏輯。
- **確實修改了 Frontend Engine**（`public/js/geo-heatmap.js`）：
  `geoHeatState` 新增 `businessTotals` 欄位，`geoHeatScheduleUpdate()`
  擴充支援 `{areas, businessTotals}` 回傳格式（向下相容純陣列格式）。這是
  本輪唯一觸碰到「Engine」檔案的地方，範圍限定在新增 additive 欄位與其
  重置邏輯，未修改任何既有渲染/請求時機邏輯。

## 兩個初始 Smoke Failure 的最終原因

上一輪回報「尚有兩個測試 Harness 問題」：

1. **percent clamp regex**：Harness 問題。原本用 `/[0-9]{3,}%/` 判斷
   「沒有超過三位數的百分比」，但 `100%` 本身就是三位數字＋`%`，正常的
   100% Full Coverage 案例反而被誤判成失敗。修法：改成擷取 `(\d+)%` 的
   實際數字，斷言 `<= 100`，而不是用字元數量判斷。已在
   `scripts/smoke-hotfix30-b5-r5-4-g1-3-1-coverage-total-dark-theme.js`
   的「PCT-a ~ PCT-g3」區塊完整覆蓋 total=0／drawn=0／40%／100%／
   drawn>total／drawn<0／NaN／Infinity 全部七種案例，並額外用 sql.js 真實
   DB fixture 交叉驗證。

2. **jsdom const/window scope**：Harness 問題。`dom.window.eval(...)`
   是 indirect eval，依 JS 語言規格，頂層 `const`/`let` 宣告不會變成
   `window` 的自身屬性（只有 `var`／function 宣告會），所以先前用
   `dom.window.GEO_HEAT_TAKEOUT_DELIVERY_EXPLANATION` 這類方式直接讀
   `const` 常數一定拿到 `undefined`——這跟產品程式碼本身的正確性無關（
   產品程式碼內部所有函式仍在同一個 lexical scope 下彼此正常讀到這些
   常數，執行結果本來就是對的）。修法：在同一個 `eval()` 字串裡追加一段
   `window.__G131_BRIDGE__ = { ... }` bridge，把這些 const 額外指派成
   `window` 的屬性，讓測試能從外部讀到——這仍然是「真的執行產品程式碼」，
   不是字串掃描冒充。

另外在本輪修正過程中發現並修掉一個額外的 Harness 問題：真實 sql.js DB
測試（`0.5` 區塊）沒有在測試開始前清空上一次留下的固定 test id
（`g131-a1` 等），重複執行會撞到 `UNIQUE constraint failed: orders.id`。
已在測試開頭加上
`db.run('DELETE FROM orders WHERE store_id IN (...)')`
清理，確認可重複執行兩次以上皆全綠（見下方 Smoke 結果）。

以上三個問題全部確認是 **Test Harness 問題**，沒有為了讓測試通過而改動
任何正確的產品程式碼。

## G1.3.1 Smoke 結果

`scripts/smoke-hotfix30-b5-r5-4-g1-3-1-coverage-total-dark-theme.js`

```
PASS: 115
FAIL: 0
TOTAL: 115
```

（要求至少 110，實際 115；已驗證可重複執行兩次以上維持全綠，非一次性
僥倖通過。）涵蓋：node --check、真實 sql.js DB fixture（外帶無 Geo／
外送+外帶混合／取消訂單排除／Store Isolation／0 vs undefined 型別判斷／
Backward Compatibility own-property 檢查）、`_geoHeatMetricTotals()`／
`_geoHeatBuildCoverageExplanationText()` 全狀態與 percent clamp 全案例、
Coverage 四態文案（含真實案例逐字比對）、`geoHeatScheduleUpdate()` stale
response／duplicate request guard、新舊呼叫格式向下相容、Dark/Light
Theme CSS、DOM escaping／no duplicate element／no undefined-null-NaN-
Infinity、不冒充座標來源、外帶外送說明、重複操作穩定性、G1.2/G1.3 介面
未退化。

## Regression 結果

| Suite | 結果 | 備註 |
|---|---|---|
| G1 Smoke (`smoke-hotfix30-b5-r5-4-g1-live-geo.js`) | 212 / 212 PASS | 無退化 |
| G1.1 Visual Smoke | 91 / 91 PASS | 無退化 |
| G1.2 Layer Switch Smoke | 83 / 83 PASS | 無退化 |
| G1.3 Metric Sync Smoke | 117 / 117 PASS | 無退化 |
| G1.3.1 Coverage Smoke（本輪） | 115 / 115 PASS | 見上 |
| R5.3-A7（Geo KPI Single Source） | 87 / 87 PASS | 無退化 |
| R5.3-A5（Visitors Classification） | 20 / 20 PASS | 無退化 |
| R5.3-A4（Metric Switcher Consolidation） | 110 / 110 PASS | 無退化 |
| R5.3-A2（Geo Event Engine） | 227 / 228 PASS，1 FAIL | 見下方 Baseline Comparison |
| R5.3-A1.2（Visitor Geo Sync） | 187 / 188 PASS，1 FAIL | 見下方 Baseline Comparison |
| R5.2-B2（Geo Map） | 620 / 620 PASS | 無退化 |
| R5.2-B3（Geo Settings UI） | 157 / 157 PASS | 無退化 |
| static-audit-g1-2.js（既有） | 25 / 25 OK | 無退化 |
| static-audit-g1-3.js（既有） | 32 / 32 OK | 無退化 |
| 全專案 `node --check`（208 個 .js 檔） | 208 / 208 PASS | 無語法錯誤 |

### Baseline Comparison

- **Pre-existing failures**：0（regression 執行前，`geo-heatmap.js` 未修改
  版本下，全部 suite 皆為全綠 baseline）。
- **G1.3.1 new failures**：2 項，皆為同一個已知、預期內原因——
  `smoke-hotfix30-b5-r5-3-a2-geo-event-engine.js` 的 `A14-1` 與
  `smoke-hotfix30-b5-r5-3-a1-2-visitor-geo-sync.js` 的 `A16-1`，兩者都是
  「`public/js/geo-heatmap.js` 與（各自那一輪留下的）基線逐位元組相同」
  的**檔案未變更防護測試**。本輪為了新增 `businessTotals` additive
  plumbing 與 `geoHeatScheduleUpdate()` 向下相容擴充，**必須**修改
  `geo-heatmap.js`（需求文件本身第四／五節也要求把 businessTotals 傳入
  `geoHeatState`／`geoHeatScheduleUpdate()`）。這兩個防護測試的設計初衷
  是攔截「非預期的跨模組修改」，而本輪的修改是審慎、最小化、且完全向下
  相容的 additive 擴充（見上方 Backward Compatibility 說明），不是非預期
  的破壞——`geo-heatmap.js` 的所有既有行為都已被 G1/G1.1/G1.2/G1.3 既有
  Smoke（212+91+83+117 項）與本輪 G1.3.1 Smoke 的 stale-guard／duplicate-
  guard／backward-compat 測試（第 34–37 項）完整覆蓋且全數通過。
  這兩項 diff-guard failure 如實記錄於此，不隱藏、不視為全綠、也不修改
  這兩支既有 smoke 的邏輯去讓它們通過（那屬於「為了全綠修改無關產品
  邏輯」，本輪明確禁止）。
- **G1.3.1 fixed failures**：2 項（見上方「兩個初始 Smoke Failure 的最終
  原因」）。

無 flaky 隱藏——上述兩個 diff-guard failure 每次執行都穩定重現（非
間歇性），已如實列出，不算作「全綠」。

## Static Audit 結果

`scripts/static-audit-g1-3-1.js`：**40 / 40 OK**（要求至少 32 項，實際
40 項，含 2 項 EXTRA 跨模組隔離檢查）。涵蓋 Business Total／Geo Drawable
分離、Filter 一致性、Order Status／Revenue 定義一致性、API additive／
Backward Compatibility、Dark/Light Theme、DOM 安全性、不冒充資料來源、
既有功能未退化、無 debug 殘留、無硬編碼 store_001。

## Manual Visual

**NOT TESTED** — 本輪環境沒有真實瀏覽器可供人工視覺驗收（Dark/Light
Theme 實際渲染截圖、Chrome/Edge/Android Chrome/iPhone Safari 跨瀏覽器
檢查）。詳見 `R5.4-G1.3.1_MANUAL_QA_CHECKLIST.md`，全部項目標記為
「NOT TESTED」，不得視為 Final Release。

## 是否可部署

**建議先完成 Manual Visual（真實瀏覽器）驗收後再部署。**

程式邏輯層面（Backend SQL、Frontend 資料流、Coverage 四態文案、Dark
Theme CSS 規則本身）已透過真實 sql.js DB fixture、jsdom 實際執行產品
函式、完整 Regression、Static Audit 多重驗證，功能正確性有高信心；但
Dark Theme 的「視覺對比是否人眼可讀」、「是否被其他全域 CSS specificity
覆蓋回白色」這類最終呈現效果，在沒有真實瀏覽器渲染的環境下無法百分之百
排除意外，需要至少一次真實瀏覽器（建議 Chrome + 一個行動裝置瀏覽器）
Dark Theme 開關切換的人工目視確認，再標記為可正式對外部署。
