# R5.4-G1.6-GA4-H1.3-EVENT-COMPAT — Reality Audit

fix18-10-hotfix30-B5-R5.4-G1.6-GA4-H1.3-EVENT-COMPAT
GA4 Realtime Event Metric 相容性 ＋ Historical Today 時區 ＋ Historical 指標語意修正

---

## 一、本輪要解決的三個獨立問題

1. **GA4 Realtime Event Metric（view_item / add_to_cart / begin_checkout / purchase）正式環境全部失敗**，只有 Visitors 正常。
2. **Historical Today 日期邊界用 UTC 計算**，在 Asia/Taipei 00:00–07:59 之間會把「今天」誤判成 UTC 的「昨天」。
3. **Historical 表格把「事件數／活躍使用者」的平均值誤標成「加購率／購買率」並乘以 100**，龍潭區範例顯示「加購率 500%／購買率 400%」，但這不是 conversion rate。

---

## 二、正式環境已確認 PASS（本輪不得再當 Bug）

| 項目 | 狀態 |
|---|---|
| GA4 Auth Production | PASS |
| Realtime Visitors Production | PASS |
| H1.2 Unique Admin Production（Pingzhen District → 桃園市等） | PASS |
| Historical 近 7 天（手動同步） | PASS |
| Historical Query／Merge／Save／Read／Marker／Table Gate | PASS（維持不動） |

Realtime Event Metrics（H1.3 之前）：**BLOCKED**——切換到 view_item/add_to_cart/checkout/purchase 時，按鈕確實變成 Active（Click Handler 正常），但隨後畫面顯示「GA4 連線發生錯誤，請稍後再試。」

---

## 三、Realtime Event Metric 根因分析（Variant B 選擇依據）

### 3.1 假設與限制

本環境沒有真實 Google Analytics Data API 憑證與網路存取，**無法對正式 GA4 Property 重放請求來實測哪個 Request Variant 真的可行**。這是一個誠實的限制，不假裝已經用真實 API 驗證過。

### 3.2 已知事實

- 目前正式（Variant A）的 Request 對 `eventName` 下了 `dimensionFilter`，但 `dimensions` 陣列裡完全沒有 `eventName`。
- Visitors（沒有 eventName filter）一直正常。
- 正式環境的失敗模式（Click Handler 正常、Request 送出、隨後回錯誤）與「filter 引用了未輸出維度」這類 Google Data API 相容性問題的外部症狀一致。

### 3.3 本輪決定

改用 **Variant B**：filter 用到的維度（`eventName`／`streamId`）一律同時列進 `dimensions`（見 `utils/ga4Realtime/requestBuilder.js` 的 `buildRealtimeDimensions()`）。這是根據「已知會失敗的 Variant A」與「架構上最保守、風險最低的下一步」做出的**有根據的工程判斷**，不是盲猜，但也**不等於已經拿正式 Google API 驗證過**。

若部署後 Variant B 仍然失敗，`connectionTest.js` 新增的 `view_item_ok`／`add_to_cart_ok`／`checkout_ok`／`purchase_ok`（＋各自 `stage`／`code`）診斷欄位，可以立即告訴維運人員是哪個 Metric、哪個 Stage 失敗，不需要重新盲猜；届時再視情況導入 Variant C（診斷用 fallback：dimensions 加 eventName 但不下 filter，後端只挑選對應 row），不在本輪範圍內預先實作。

**Visitors 完全不受影響**：`GA4_REALTIME_METRICS.visitors.eventName === null`，`buildRealtimeDimensions()` 在沒有 eventName 時原樣回傳，不論 Stream 是否設定都不會新增任何維度——這點已由 G1.5-A Request Builder Contract（B13/B14）與新增的 H1.3 Request Builder Contract 逐一驗證。

---

## 四、Defense-in-Depth 的意外發現（誠實記錄）

在設計 Mutation R（模擬「Raw Google Error 洩漏進 DOM」）時，**第一次嘗試失敗**：直接讓 `routes/geo-live.js` 的 catch 區塊把 `e.message` 塞進 JSON response，結果證明「不會洩漏」——因為 `Ga4RealtimeError` 的 constructor 是 `super(code)`，此時 `code` 早已是 `utils/ga4Realtime/errors.js` 的 `classifyGa4RealtimeError()` 分類過的安全代碼字串。Raw Google Error 在 `client.js`／`errors.js` 這一層就已經被丟棄，根本不會留到 route 層。

這是一個**額外一層防護縱深（Defense in Depth），本輪之前就存在，不是 H1.3 新增的**，值得記錄下來。Mutation R 因此改為直接對 `classifyGa4RealtimeError()` 本身做 Mutation（讓它的 fallback 回傳 `err.message` 而不是固定的 `'GA4_API_ERROR'`），才能真正示範「如果分類邊界被破壞，raw 內容確實會一路洩漏到 `client.js` 回傳值的 `code` 欄位」，並證明目前的安全測試會攔截這個 Mutation。

---

## 五、City Metric Parser 的真實 Contract（不誇大）

- **City Dimension Parsing：Header-based。** `_aggregateCityRows()` 用 `dimensionHeaders.indexOf('city')`／`dimensionHeaders.indexOf('countryId')` 找位置，不管 Google 回傳的維度順序如何排列都正確（H1.3 Realtime Event Runtime #28b／#29 已用兩種不同順序的 `dimensionHeaders` 驗證過）。
- **City Metric Parsing：Positional（固定位置），不是 Header-based。** 目前程式碼用固定的 `{ activeUsers: 0, eventCount: 1 }`。原因：`requestBuilder.js` 的 City Request 一律固定送出 `metrics: [{ name: 'activeUsers' }, { name: 'eventCount' }]` 這個順序，而 Google Data API 的回應 `metricValues` 順序對應請求的 `metrics` 順序，所以這個固定位置的假設在目前的呼叫方式下是成立的。

**這是 PRE-EXISTING DESIGN CHARACTERISTIC（在 H1.2/G1.5-B2.4 就已經是這樣），不是 H1.3 造成的 Regression，本輪沒有修改它。** 如果未來要把 City Metric Parsing 也改成完全 Header-based（不依賴我們自己固定送出的 metrics 順序），需要另開一輪獨立的 hotfix，不在本輪範圍內。

Summary（Visitors／Event 共用同一支）的 Metric Parsing 則一直是 Header-based（`summaryResult.metricHeaders.indexOf('activeUsers')` 等），這部分沒有变化，也已用 H1.3 Realtime Event Runtime #28 驗證「metricHeaders 順序交換後仍正確解析」。

---

## 六、Asia/Taipei Calendar Boundary

修正前，`services/ga4GeoSyncService.js` 的 `_todayDateString()` 用 `new Date(); d.setUTCDate(...); toISOString().slice(0,10)`——純 UTC Calendar Day。在 Asia/Taipei 00:00～07:59 之間（此時 UTC 還是前一天），會把「今天」誤判成 UTC 的「昨天」。

修正方式：**重用本專案既有的集中 Timezone Helper `utils/dateTime.js`**（A1.2.1 建立、已驗證），新增一個純函式 `getTaipeiCalendarDateString(dateInput, offsetDays)`，用同一個 `Intl.DateTimeFormat({ timeZone: 'Asia/Taipei' })` 實例取出日曆日年/月/日，再用 `Date.UTC()` 做 ±N 天的日期算術（避免任何隱性時區假設）。`_todayDateString()` 改為委派給它，**沒有在 `ga4GeoSyncService.js` 內另外建立第二套時區實作**。

驗證案例（全部用注入的固定時刻，不依賴機器時區）：
- 2026-08-06T16:01:00Z（台灣 00:01）→ `2026-08-07` ✅
- 2026-08-06T23:59:00Z（台灣 07:59）→ `2026-08-07` ✅
- 2026-08-07T00:00:00Z（台灣 08:00）→ `2026-08-07` ✅
- 2026-08-07T15:59:59Z（台灣 23:59:59）→ `2026-08-07` ✅
- 2026-08-07T16:00:00Z（下一秒，台灣 2026-08-08 00:00）→ `2026-08-08` ✅
- 跨月（2026-03-02 台灣，7d）→ `2026-02-24 ~ 2026-03-02` ✅
- 跨年（2027-01-01 台灣，7d）→ `2026-12-26 ~ 2027-01-01` ✅
- 閏年（2028-03-01 台灣，7d，跨過 2028-02-29）→ `2028-02-24 ~ 2028-03-01` ✅
- Custom Range：完全不套用 offset，維持使用者指定日期 ✅

本輪只修改「今天／昨天／近 7 天／近 30 天」這四個 Historical Range 代表的 Calendar Date，**沒有**修改：Realtime 5/30 分鐘 window、GA4 fetched_at、DB UTC timestamp、API ISO Z timestamp、瀏覽器顯示層（A1.2.1 的 UTC→Taipei 顯示 Contract 完全不受影響）。

---

## 七、Historical 0 rows 的定性

正式環境已證明近 7 天同步成功；Today 曾出現 `success=true, rows_saved=0`。**這是合法的空結果（DATA FRESHNESS / LEGAL EMPTY），不是架構失敗**——GA4 標準報表本身就有處理延遲，不代表 Query／Merge／DB 出錯。本輪只修改 Sync 成功後的提示文案（`同步成功，目前 GA4 報表尚無可用的區域資料。即時資料與標準報表的處理時間不同，稍後再同步即可。`），沒有動 Query／Merge／DB Schema／Marker／Table API。

**誠實補充**：`geoGa4H1RenderTable()` 預設會過濾掉 `active_users === 0` 且 `normalization_status === 'ok'` 的正常 rows（既有行為，本輪未修改）。H1.3 Historical Runtime 裡驗證「denominator=0 顯示 `—`」的測項，用的是既有支援的 `{ showZeroRows: true }` 選項，**只是用來驗證 Formatter 本身對 0 分母安全**，不代表正式預設 UI 一定會顯示 zero-user 的 row——這兩件事分開陳述，不得混為一談。

---

## 八、Historical Metric Semantic Bug（已確認、已在程式碼修正）

正式近 7 天畫面（龍潭區範例）：`active_users=2, add_to_cart_count=10, purchase_count=8`，畫面卻顯示「加購率 500%／購買率 400%」。實際公式是 `event_count / active_users`（事件數／活躍使用者的平均值），**不是 conversion rate**（真正的 conversion rate 需要「觸發過該事件的 Unique Users」當分母，本輪沒有新增這個 Query，見需求文件二十三，未來若要做需另開一輪）。

修正：改稱「加購事件／人」／「購買事件／人」，公式不變但**不再乘以 100**，顯示固定一位小數（`5.0`／`4.0`），分母為 0 顯示 `—`（不是 Infinity/NaN/0%/100%）。Sort Key 同步改名（`add_to_cart_per_user`／`purchase_per_user`），只影響 Frontend 呈現層，**沒有修改 DB Schema、Historical API 欄位名稱、或新增任何 GA4 Query**。

舊名稱 `_geoGa4H1Rate` 保留成一個 alias（委派給新的 `_geoGa4H1PerUser`），只為了不破壞既有測試對這個函式名稱的呼叫，**已用 H1.3 Static Audit #55 與 Mutation Q 驗證：所有正式 Render 路徑（Row／Tooltip／Sort）只呼叫 `_geoGa4H1PerUser`，不再呼叫 `_geoGa4H1Rate`**。

---

## 九、最終 Gate 狀態

| Gate | 狀態 |
|---|---|
| Realtime Visitors Production | PASS |
| Realtime Event Metrics（H1.3 之前） | BLOCKED |
| Realtime Event Automated Gate | PASS（Fake Client，65/65 ×3，見 Realtime Event Runtime） |
| **Realtime Event Production Gate** | **READY FOR MANUAL DEPLOYMENT VERIFICATION**（未部署到真實 Google API 前，不得寫 PASS） |
| Historical 7d Production | PASS |
| Historical Today 0 rows | LEGAL EMPTY / DATA FRESHNESS（不是 architecture failure） |
| Taipei Calendar Automated | PASS（44/44 ×3，見 Historical Runtime） |
| Historical Semantic Bug | CONFIRMED / FIXED IN CODE |
| **Historical Semantic Production Gate** | **READY FOR MANUAL DEPLOYMENT VERIFICATION** |
| H1.2 Unique Admin Production | PASS（未修改，byte-identical） |
| GA4 Auth Production | PASS（未修改） |
| Store Isolation | SINGLE_STORE_ONLY |
| Visitor IP Provider | disabled（未開始） |
| POS Individual Visitor Mapping | NOT IMPLEMENTED / OUT OF SCOPE |

即使 Request Builder（68/68）、Event Compat Connection Test（48/48）、Realtime Event Runtime（65/65）全部用 Fake Client 通過，**在真實 Zeabur 部署＋真實 Google API 對 view_item/add_to_cart/begin_checkout/purchase 實測出 HTTP 200 之前，Realtime Event Production Gate 一律只能寫 READY FOR MANUAL DEPLOYMENT VERIFICATION，不得寫 PASS。**
