# H1.4.7-TWO-STAGE-CHECKOUT 交付報告

版本：`fix18-10-hotfix30-B5-R5.4-G1.6-GA4-H1.4.7-TWO-STAGE-CHECKOUT-QA-full`

---

## 1. 交付摘要與問題背景

H1.4.6 之前，`public/line-order.html`／`public/line-shipping.html` 的 `cartBar`（商品頁固定購物車列）點擊後直接呼叫 `openCartSheet()`，該函式：

1. 一開啟就同時顯示商品摘要**與**顧客資料表單（姓名、電話、地址、付款方式、送單按鈕），沒有真正的「先看購物車、再決定要不要結帳」兩階段流程。
2. 誤觸發 `begin_checkout` 事件——即使使用者只是想看一下購物車內容，也會被記成「已開始結帳」。
3. H1.4.6 的既有測試把「`cartBar` 點擊進入 `openCartSheet()`」直接當成「前往結帳＝既有結帳流程」，這個假設本身就是本輪要修正的錯誤前提。

H1.4.7 把 `openCartSheet()` 拆成兩個明確階段：

- **第一階段（`#cartStage`／購物車摘要）**：商品、數量、優惠券、金額明細、「前往結帳」按鈕。不顯示姓名、電話、地址、付款方式、送單按鈕。
- **第二階段（`#checkoutStage`／結帳表單）**：只有按下「前往結帳」才會顯示，包含原有的顧客資料欄位與送單按鈕。

## 2. `line-order.html`／`line-shipping.html` 修改前後流程

**修改前：**
```
點擊 cartBar → openCartSheet() → 同時顯示商品摘要＋顧客表單（含送單按鈕）→ 使用者直接看到並可送單
                                → 誤觸發 begin_checkout
```

**修改後（兩頁一致）：**
```
點擊 cartBar → openCartSheet() → 只顯示 #cartStage（商品摘要）→ view_cart
              （checkoutStage 維持 hidden，不可見不可操作）

點擊「前往結帳｜NT$金額」→ openCheckoutStep() → _enterCheckoutStage()
              → 切換到 #checkoutStage（顧客資料表單）→ checkout_click
              （cartStage 隱藏；不呼叫 submitOrder()）

點擊「確認下單｜NT$金額」→ submitOrder()（既有正式送單/付款路徑，未重做）
              → 成功後 → submit_order → purchase（既有流程）→ 清空購物車、stage 重設為 cart

「返回購物車」→ backToCartStep() → 回到 #cartStage，資料與 cart_id 不變、不清除、不重觸發事件
```

LIFF／LINE Gate 導回（`gate_mode='checkout'`）：`openCartSheet({step:'checkout'})` 直接恢復到 `#checkoutStage`，視為 silent（不補送 `view_cart`、不補送 `checkout_click`），表單資料與優惠券、`cart_id` 保留。

## 3. 事件契約表

| 動作 | 事件 |
|---|---|
| 開啟購物車（真實點擊「查看購物車」，每次都記一筆，不是同一 cart_id 永久去重） | `view_cart` |
| 點「前往結帳」並成功切換到第二階段（同一 cart_id 去重一次） | `checkout_click` |
| 送出訂單（既有流程，未重做） | `submit_order` |
| 付款完成（既有流程，未重做） | `purchase` |
| — | `begin_checkout`：僅保留在 GA4/Meta 平台映射與 Geo 相容欄位供歷史相容，**不由正式兩階段流程送出**（`line-order.html`／`line-shipping.html` 的 production code 已無任何 `_trackEvent('begin_checkout')` 呼叫，static audit 已驗證） |

LIFF/Gate 導回、`updateBar()`（render/金額重算）、`backToCartStep()`（返回購物車）皆不觸發 `view_cart` 或 `checkout_click`。

## 4. Funnel 精確順序

```
page_view → view_product → add_to_cart → view_cart → checkout_click → submit_order → purchase
```

`utils/dashboardAnalytics.js` 的 `getFunnel()` 回傳陣列固定精確為這 7 個元素，`begin_checkout` **不在**這個陣列裡（曾經有一版把它 push 到陣列尾端當「相容別名」，經完整 inventory 確認沒有任何真實消費端依賴它，且會讓 `funnel[funnel.length-1]`（多處消費端用來取得 `purchase` 的位置假設）以及 Dashboard 的 `renderDashboardFunnelV2()`（會把陣列每個元素都畫成一條可視 bar）壞掉，故最終版本移除，不是刪除既有契約）。

## 5. Production／測試修改檔案（僅列出實際修改，未修改但檢查過的檔案不列入）

**Production：**
- `public/line-order.html` — 兩階段結帳結構（`#cartStage`/`#checkoutStage`）、`openCartSheet()`/`openCheckoutStep()`/`backToCartStep()`/`_enterCartStage()`/`_enterCheckoutStage()`、`view_cart`/`checkout_click` 事件、LIFF 導回改為恢復第二階段、`clearCartByUser()`/下單成功流程加入 stage 重設、修正一個真實 race-condition 缺陷（`refreshCartProductsFromLatestMenu()`）。
- `public/line-shipping.html` — 同上（宅配版本，對應函式/DOM 結構）。
- `public/js/analytics-platforms.js` — `GA4_EVENT_MAP`/`META_EVENT_MAP` 新增 `view_cart`/`checkout_click`；新增 `META_CUSTOM_EVENTS` 使 `trackMeta()` 能依事件分流 `fbq('trackCustom',...)` 或 `fbq('track',...)`。
- `utils/analyticsLog.js` — `EVENT_WHITELIST` 新增 `view_cart`、`checkout_click`。
- `utils/dashboardAnalytics.js` — `getFunnel()` 改為七階段 canonical 順序（移除 `begin_checkout` 假別名）；`getRecommendations()` 改讀 `checkout_click`；即時線上人數分類新增 `view_cart`/`checkout_click`；`getIncomplete()` 權威來源改為 `checkout_click`，並修正排除已 `purchase` 的購物車（真實缺陷，非本輪引入但本輪測試發現並修正）；廣告歸因表新增獨立 `checkout_click` 欄位。
- `utils/analyticsV2.js` — `getProductFunnel()` 的 per-product `checkout` 欄位權威來源改為 `checkout_click`。
- `utils/geoAnalyticsQueries.js` — `GEO_FUNNEL_EVENTS.checkout` 單一選擇點改為 `'checkout_click'`；新增 `checkout_click_visitors`/`checkout_click_events` 欄位（與既有 `begin_checkout_visitors`/`begin_checkout_events` 相容別名並存，同一次計算結果）。
- `routes/analytics.js` — **未修改**（已確認是通用事件處理端點，白名單已足夠，未加空殼程式碼）。

**測試／腳本：**
- `scripts/run-g1-6-ga4-h1-4-7-two-stage-checkout-runtime.js`（新建）— line-order.html + line-shipping.html 合併 canonical runtime，113 項。
- `scripts/run-h1-4-7-analytics-contract-runtime.js`（新建）— 真實 sql.js schema + fixture + 真實 HTTP 路由 + 真實 jsdom 平台派送，59/59（PASS=59 FAIL=0 TOTAL=59 exit=0）。
- `scripts/static-audit-g1-6-ga4-h1-4-7.js`（新建）— 39 項。
- `scripts/static-audit-g1-6-ga4-h1-4-6.js`（修改）— 5 項過期 checkout 斷言 supersede，66→72。
- `scripts/run-regression-g1-6-ga4-h1-4-6.js`（修改）— 更新 static-audit-h1-4-6 期望值 66→72（誠實更新，非竄改歷史）。
- `scripts/run-regression-g1-6-ga4-h1-4-7.js`（新建）— canonical regression runner，76 個 H1.4.6 leaf suite 明確凍結 + 3 個 H1.4.7 新 suite，`--dry-run` 模式，`path.resolve(__dirname,'..')` 動態 root。

## 6. H1.4.4～H1.4.6 supersession inventory 與保留契約

| 檔案 | 過期斷言 | 處理 |
|---|---|---|
| static-audit-h1-4-6.js | cartBar→openCartSheet()＝前往結帳 | 保留結構事實（onclick 不變）+ 新增反向語意斷言（checkoutStage 仍 hidden） |
| static-audit-h1-4-6.js | openCartSheet() 空購物車 return（零參數簽名） | 改比對新簽名 `openCartSheet(opts)` |
| static-audit-h1-4-6.js | begin_checkout 於 openCartSheet() 內觸發（兩頁） | 改為「不含 begin_checkout」+「改含 view_cart」+「checkout_click 由獨立 openCheckoutStep() 觸發」 |

H1.4.4（23/23、25/25）、H1.4.5（40/40、31/31）**完全未修改**，其中 H1.4.4 runtime 的 `begin_checkout` 映射測試（直接呼叫 `trackPlatformEvent('begin_checkout',{})` 驗證映射本身）判定為**仍有效**（測的是相容映射保留，不是兩階段流程送出它），未修改。

## 7. Analytics 時間 fixture 算法

`scripts/run-h1-4-7-analytics-contract-runtime.js` 不假設任何固定時鐘時刻，改為：從真實呼叫的 `resolveDateRange('today')` 取得 `startLocal`/`endLocal`（Asia/Taipei 本地時間字串），換算成 UTC ms 後取**區間中點**作為所有 fixture 事件共用的安全時間戳。加入自我診斷 T1（min 時間戳嚴格 > `range.startLocal`）、T2（max 時間戳嚴格 < `range.endLocal`），皆真實通過。

**Analytics Contract Runtime 最終結果：PASS=59 FAIL=0 TOTAL=59 exit=0**（不是舊的 57/57——59 已包含 T1/T2 時間自我診斷這兩項）。

## 8. H1.4.7 Static Audit

`node scripts/static-audit-g1-6-ga4-h1-4-7.js` → **PASS=39 FAIL=0 TOTAL=39 exit=0**。涵蓋兩頁兩階段結構、平台映射方式、Funnel 七階段精確順序與位置、Geo 相容別名——使用括號配對精準擷取函式體（不是固定字數 char window）、負向檢查一律先 `stripComments()` 去除註解（避免 supersession 註解裡提到的事件名稱造成誤判）。

## 9. Regression Runner 組成

- `H146_INHERITED_SUITES`：**76** 個 H1.4.6 canonical leaf suite，明確逐一凍結（`Object.freeze`），一次性從 `run-regression-g1-6-ga4-h1-4-6.js` 解析後人工轉錄，**不是每次執行時動態解析**。
- `H147_NEW_SUITES`：**3** 個新 suite，各恰好一次。
- `EXPECTED_TOTAL = 79`，`UNIQUE SUITES = 79`（三次驗證：dedup、存在性、repo-root 內）。
- 原本的即時解析函式改為 `parseH146FinalSuiteForDiagnosticsOnly()`，只做「額外對照診斷」（執行時印出 `[DIAG]` 比對即時解析與凍結清單是否一致），**不參與**組成執行清單，不影響 exit code。
- Dry inventory：`missing=0`、`duplicate=0`、`self-recursion=0`、`nested regression runners=0`。

## 10. 三輪完整 Regression（獨立前景 process）

修正循環驗證問題後，重新以固定 allowlist 版本執行三輪，每輪皆為獨立前景 Node process：

```
Run 1: UNIQUE_SUITES=79 PASS=7919 FAIL=0 TOTAL=7919 exit=0 residue=0
Run 2: UNIQUE_SUITES=79 PASS=7919 FAIL=0 TOTAL=7919 exit=0 residue=0
Run 3: UNIQUE_SUITES=79 PASS=7919 FAIL=0 TOTAL=7919 exit=0 residue=0
```

三輪數字完全一致。每輪皆有 `[DIAG]` 確認即時解析與凍結清單 path 集合一致（76=76）。曾有一次嘗試用背景 process（`nohup`）執行完整三輪，該 process 在工具呼叫之間被環境終止（sandbox 不支援跨工具呼叫的背景任務），**該次嘗試不計入以上三輪**，以上三輪皆為獨立前景 process 且確實取得 `exit=0`。

## 11. 相容性說明

- `view_product` 維持既有 response key；raw event 來源仍為 `view_product`（H1.4.4 Reality Audit 已確認專案內沒有 `view_item` 作為清單曝光事件，`view_item` 是另一個獨立事件，用於 H1.4.6 商品詳情 Modal，兩者語意刻意不合併）。
- Geo `begin_checkout_visitors`/`events` 來源為 `checkout_click`（`GEO_FUNNEL_EVENTS.checkout` 單一選擇點），compatibility alias，同一次計算結果，不重新查詢 legacy raw `begin_checkout`。
- legacy `begin_checkout` 不在正式 `getFunnel()` 回傳陣列。
- `purchase` 仍為 `getFunnel()` 陣列最後一項。
- 已購買（有 `purchase` 事件）的 cart_id 不會列入 `getIncomplete()` 的 `cart_not_checked_out`/`checkout_not_submitted`。

## 12. 已知限制

- `view_cart`/`checkout_click` 只從 H1.4.7 事件契約上線後才開始有資料；沒有可靠的部署時間分界可回溯合併，本輪 Funnel/Geo 查詢不嘗試「補齊」上線前的數字，也不會跟 `begin_checkout` 相加或取代。
- Geo Analytics 目前只在既有的 `checkout` 步驟換了資料來源；尚未新增獨立的 `view_cart` 地理維度（需要在 `utils/geoAnalyticsQueries.js` 的多個既有多階段 SQL CTE 中新增一個對應步驟，屬於未來擴充範圍，本輪未做）。
- `_buildCartAnalyticsPayload()` 送到後端的 `items`/`subtotal`/`discount`/`delivery_fee`/`total`/`value`/`currency`/`item_count` 等欄位，目前 `routes/analytics.js` 只保留 `cart_id`/`order_mode` 等既定欄位，其餘未落地到 `metadata_json`（沿用既有通用事件路由設計，未擴充 metadata 白名單）。

## 13. 打包驗證方法（本節結果以實際執行為準，尚未完成的步驟不寫成 PASS）

見對話後續紀錄；本報告產出時，乾淨打包、解壓版驗證、`npm ci`、ZIP checksum 尚未執行，將在下一階段完成後另行回報真實結果。
