# R5.2-B3.1 — GeoJSON Boundary Fixture Audit

範圍：只做資料盤點，**沒有修改任何程式碼**（B3.1 的程式修改已全數回復到
本輪解壓縮時的原始狀態，`geo-intelligence-map.js`／`geo-map-settings.js`／
`manifest.json` 與原始 ZIP 逐位元組相同，已用 `diff` 確認）。

---

## 1. 專案內目前所有 GeoJSON 檔案

| 路徑 | Feature 數 | Geometry 型態 |
|---|---|---|
| `public/data/geo/taiwan/taoyuan-districts.geojson` | 13 | Polygon（實測：全部 13 個都是 5 點封閉、僅 2 個獨立經度值＋2 個獨立緯度值的**軸對齊矩形**） |
| `public/data/geo/taiwan-districts.geojson`（舊版，不在 `taiwan/` 子目錄下） | 13 | Polygon（實測：同樣全部 13 個都是軸對齊矩形，座標與新版逐筆相同） |

專案內**只有這兩個 GeoJSON 檔案**，沒有其他行政區資料檔案。

驗證方式：程式化檢查每個 feature 的 `geometry.coordinates[0]`（外環），確認
點數是否為 5、以及 x／y 座標是否都只有 2 種相異值（= 矩形四個角）。兩個檔案
的 13 個 feature 全數符合，無一例外。

Properties 欄位：
- `public/data/geo/taiwan/taoyuan-districts.geojson`：`county`, `district`, `district_code`, `area_id`
- `public/data/geo/taiwan-districts.geojson`（舊版）：`county`, `district`, `area_id`（沒有 `district_code`）

兩個檔案都沒有 `district_name` / `name` / `TOWNNAME` / `town_name` /
`area_name` 這類正式行政區資料常見欄位，也沒有任何座標點數 >5 或非矩形的
feature。

---

## 2. Production Ready？還是 Fixture？

**結論：兩個都不是 Production Ready 的正式行政區資料，兩個都是 Fixture。**

判斷依據：
- 真正的行政區邊界（例如中壢區）是不規則多邊形，通常由數十到數百個頂點組成
  （沿著實際行政區界線走）。
- 這兩個檔案裡，13 個行政區（含面積差異很大的復興區、龍潭區 vs. 中壢區、
  桃園區）全部都只用 5 個點畫出矩形，且矩形彼此之間有明顯重疊／間隙
  （例如 bounding box 互相咬合的網格排列），這是典型的「用經緯度範圍隨手
  畫框」的測試資料特徵，不是任何正式測繪或政府資料來源的產出。
- 兩份檔案座標完全相同（逐筆比對一致），差別只在 `taiwan/` 版多了
  `district_code` 欄位、少了 `_comment` 之外的其他中繼資料——這代表新版是
  在舊版 fixture 基礎上加欄位，而不是換成正式資料重新製作。

兩個檔案的角色差異：
- `public/data/geo/taiwan/taoyuan-districts.geojson`：**目前 manifest 指向、
  Dashboard／Preview 實際會 fetch 到的那一份**（見下方第 3 節）。
- `public/data/geo/taiwan-districts.geojson`（舊版）：**目前沒有任何產品程式碼
  路徑會讀取它**。只在既有 smoke test（`smoke-hotfix30-b5-r5-2-b2-geo-map.js`
  的 `SaaS-F-7`）裡被斷言「仍保留、供舊版 fixture 相容」，屬於測試用途的
  遺留檔案，不是執行期資料來源。

---

## 3. Dashboard／Preview 實際引用了哪些檔案

執行期資料流（兩者都走同一條路徑，來源相同）：

```
Dashboard / Settings Preview
  → fetch('/data/geo/taiwan/manifest.json')
      manifest.cities.TAO.source = "/data/geo/taiwan/taoyuan-districts.geojson"
  → geoResolveBoundarySource(settings, manifest) 查表得到上面這個路徑
  → fetch(該路徑) 取得 GeoJSON
      Dashboard：geoLoadBoundaryData() → geoMapState.featureIndex
      Preview  ：geoMapSettingsApplyPreviewBoundary() → p.boundaryLayer
```

- **Dashboard** 與 **Settings Preview** 都只會載入 `manifest.json` 指到的
  `public/data/geo/taiwan/taoyuan-districts.geojson`，兩者是同一份資料、
  同一份 fixture、同一個 Root Cause（不是兩個獨立問題）。
- `public/data/geo/taiwan-districts.geojson`（舊版，無 `taiwan/`）**兩者都不會
  在執行期引用**——manifest 裡沒有任何 city 指向這個路徑，程式碼裡也沒有
  任何裸 `fetch()` 指向它。它只活在測試檔案裡。

---

## 4. 真正缺的是什麼

專案內**沒有任何一份正式行政區（政府測繪等級）GeoJSON**。目前只有前述兩份
矩形佔位 fixture。若要讓 Boundary 顯示真正的行政區輪廓，需要補上：

- **標的**：桃園市 13 個行政區（中壢、平鎮、八德、桃園、楊梅、龍潭、大溪、
  龜山、蘆竹、觀音、新屋、大園、復興）的正式行政區界線 Polygon/MultiPolygon。
- **建議 Schema**（供之後有正式資料時比對／轉換用，本輪不下載、不假設任何
  外部資料來源）：
  ```json
  {
    "type": "FeatureCollection",
    "features": [
      {
        "type": "Feature",
        "properties": {
          "county": "桃園市",
          "district": "中壢區",
          "district_code": "ZHONGLI",
          "district_name": "中壢區"
        },
        "geometry": {
          "type": "Polygon | MultiPolygon",
          "coordinates": [ /* 真實行政區界線的多點座標，不是 5 點矩形 */ ]
        }
      }
    ]
  }
  ```
  - `district_code` 需與 `manifest.json` 的 `district_property`
    （目前設定為 `"district_code"`）對得上，且與 Settings UI 既有的
    行政區代碼（`ZHONGLI`／`PINGZHEN`／`BADE`／`TAOYUAN`／`YANGMEI`／
    `LONGTAN`／`DASI`／`GUISHAN`／`LUZHU`／`GUANYIN`／`XINWU`／`DAYUAN`／
    `FUXING`）一致，不能重新編一套代碼。
  - `geometry.type` 允許 `Polygon` 或 `MultiPolygon`（部分行政區可能有離島
    或飛地需要 MultiPolygon）。
  - 座標點數應反映實際地形轉折（通常遠多於 5 點），不是矩形框。

- **來源建議（僅列出方向，本輪不代為下載/嵌入任何外部資料）**：政府開放資料
  平台（如內政部國土測繪中心）或其他已授權、可合法內嵌於專案的行政區界線
  資料集，需由專案方確認授權條款後再行取得。

---

## 5. 結論與本輪停止點

- Root Cause 確認為 **A：GeoJSON 本身就是格網（矩形 bounding box）資料**，
  且專案內找不到第二份可替代的正式資料——兩個既有檔案都是同一等級的
  fixture，沒有一個是 Production Ready。
- 依指示：**沒有正式 Polygon 資料時，不繼續修 Grid、不新增技術債**（不新增
  verified flag／boundaryVerified／boundary_notice／大量 fallback 邏輯／
  Shared Style Helper／新 Smoke），因為這些都只是在同一份錯誤 fixture 上
  疊加更多邏輯，無法解決「資料本身不是行政區」這個根本問題。
- 本輪 B3.1 的所有程式修改已回復原狀（`geo-intelligence-map.js`／
  `geo-map-settings.js`／`manifest.json` 與原始 ZIP 一致），**沒有留下任何
  程式碼變更**。
- 本輪只交付這份 Audit，完成後停止。不開始 Heatmap，不建立更多 Boundary
  Runtime 邏輯。真正的下一步是先取得或製作正式行政區 GeoJSON（見第 4 節
  schema），有了正式資料之後才適合回頭處理 Style／Tooltip／Layer
  Lifecycle 等顯示層議題。
