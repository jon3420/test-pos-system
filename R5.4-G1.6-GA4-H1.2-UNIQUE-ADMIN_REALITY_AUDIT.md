# R5.4-G1.6-GA4-H1.2-UNIQUE-ADMIN — Reality Audit

fix18-10-hotfix30-B5-R5.4-G1.6-GA4-H1.2-UNIQUE-ADMIN
GA4 Realtime 全台唯一鄉鎮市區安全映射修正

---

## 一、正式環境真實證據（本輪修正對象）

正式 API：`GET /api/geo-live/ga4-realtime`

實測回應：

```
HTTP 200
success = true
source = ga4_realtime
status = fresh
quota_status = normal
error_code = null
```

正式 Raw Payload：

```
summary.total_active_users_ga4 = 1
summary.event_count            = 2
summary.mapped_counties        = 0     ← 修正前
summary.unmapped_city_rows     = 1     ← 修正前
summary.excluded_non_tw_rows   = 0

unmapped: [
  { city: "Pingzhen District", active_users: 1, event_count: 2 }
]
```

**Before（本輪修正前）**：

```
mapped_counties    = 0
unmapped_city_rows = 1
```

**Automated Expected After（本輪修正後，已由 Aggregator 實際執行驗證，見下）**：

```
mapped_counties    = 1
unmapped_city_rows = 0
county_code        = 68000
county_name        = 桃園市
active_users       = 1
event_count        = 2
```

### 1.1 用 Production Aggregator 實際重現（非模擬）

用 `utils/ga4Realtime/index.js` 的 `_aggregateCityRowsForTest()`（正式聚合函式本體，非測試替身）直接餵入正式回報的原始列：

輸入：`countryId=TW, city="Pingzhen District", activeUsers=1, eventCount=2`

實際輸出：

```json
{
  "counties": [
    { "county_code": "68000", "county_name": "桃園市", "active_users": 1, "event_count": 2,
      "source": "ga4_city", "accuracy": "ip_city_county_estimate" }
  ],
  "unmapped": [],
  "excludedNonTw": 0
}
```

與正式回報案例逐欄位核對：`mapped_counties=1`／`unmapped_city_rows=0`／`excluded_non_tw_rows=0`／`county_code=68000`／`county_name=桃園市`／`active_users=1`／`event_count=2` — **全部相符**。

同時確認：`summary.total_active_users_ga4` 的計算路徑（`utils/ga4Realtime/index.js` 內 `totalActiveUsers = summaryRow ? Number((summaryRow.metricValues[...active_users...]) ...)`）**完全未被本輪改動**，仍只讀取 Summary Request 的官方 `activeUsers`，本輪沒有新增、也沒有修改任何 `counties.reduce(...)` 或 `sum(counties.active_users)` 這類加總邏輯。

---

## 二、既有架構稽核

已讀取並確認以下模組現況：

| 模組 | 現況 |
|---|---|
| `utils/ga4Realtime/index.js` | `_aggregateCityRows()` 是唯一的 GA4 City 維度聚合入口，本輪只在其中新增一個 fallback 分支 |
| `utils/taiwanGeoNormalize.js` | 全台 22 縣市／368 鄉鎮市區權威資料集的唯一存取層（`_buildIndexes()` 建立一次性索引） |
| `data/taiwan-administrative-areas.json` | 22 counties + 368 subdivisions，每筆 subdivision 已內建 `aliases[]`（中文全名／英文全名／中英文無後綴簡稱） |
| `data/taiwan-administrative-areas.manifest.json` | 資料來源／版本／抓取日期／checksum 記錄，本輪未變更 |
| `DISTRICT_PARENT_ALIASES`（Legacy） | 只手動列出 `Longtan District`／`Taoyuan District` 兩組別名，全部指向桃園市 |

### 2.1 現有 Catalog／Resolver 可直接共用

`_buildIndexes()` 建立的 `_subdivisionNameIndex`（`Map<正規化別名, subdivision[]>`）**已經天然具備「全國唯一性」判斷所需的全部資訊**：同一個別名字串若對應到多個不同 `subdivision_code`，這個 Map 的 value 就會有多筆——這正是 `resolveTaiwanAdministrativeArea()` 既有的「裸鄉鎮市區名稱＋無縣市提示」分支（見該函式第 3 段）已經在用的機制。本輪新增的 `resolveUniqueSubdivisionParentCounty()` 直接重用同一份索引，不重讀 JSON、不建立第二份資料結構。

### 2.2 現有手動 Alias 數量

`DISTRICT_PARENT_ALIASES`：僅 1 個縣市（桃園市）× 7 個別名字串（`Longtan District` / `Longtan Dist.` / `Longtan` / `龍潭區` / `Taoyuan District` / `Taoyuan Dist.` / `桃園區`），對應 2 個實際行政區（龍潭區、桃園區）。

### 2.3 全台 Subdivision 數量（由 `scripts/audit-taiwan-unique-subdivision-aliases.js` 真實掃描得出）

```
subdivision_total            = 368
county_total                 = 22
canonical_chinese_alias_count = 710
canonical_english_alias_count = 693
unique_alias_count           = 1365
ambiguous_alias_count        = 47
no_parent_county_count       = 0
duplicate_subdivision_code_count = 0
```

### 2.4 Pingzhen／Yangmei／Banqiao 實際候選數（真實掃描）

| 名稱 | 候選數 | 狀態 | 對應縣市 | subdivision_code |
|---|---|---|---|---|
| Pingzhen District | 1 | unique | 桃園市 | 68000100 |
| Yangmei District | 1 | unique | 桃園市 | 68000040 |
| Banqiao District | 1 | unique | 新北市 | 65000010 |

### 2.5 舊 Realtime Layer 仍為縣市級聚合

`_aggregateCityRows()` 的輸出結構（`counties[]`，每筆只有 `county_code`/`county_name`/`active_users`/`event_count`）**本輪完全未改變**。新增的 fallback 只是「多一種方式把 GA4 回傳的 city 字串正確歸戶到 county」，最終輸出仍然是**縣市層級**的聚合列，不是行政區層級的精確點位。

**明確聲明（需求文件九）**：

- 平鎮區 → 桃園市（縣市聚合，非行政區 Marker）
- 楊梅區 → 桃園市（縣市聚合，非行政區 Marker）
- 板橋區 → 新北市（縣市聚合，非行政區 Marker）
- **不宣稱**：平鎮區/楊梅區/板橋區精確 GPS、個別訪客精準位置。GA4 位置本身就是 IP 推估，僅供區域趨勢分析（見 `DISCLAIMER` 常數，本輪未改動其文字）。

### 2.6 H1 Aggregate Marker Layer 不在本輪修改範圍

`public/js/geo-ga4-h1-panel.js`、H1 的行政區精確點位圖層（A1.2 Authoritative Admin Point Catalog）**本輪完全未觸碰**，也未新增任何 lat/lng 產生邏輯（見 Static Audit check 15）。

---

## 三、Legacy Override vs. Unique Resolver — 明確分類

呼叫順序（`utils/ga4Realtime/index.js` `_aggregateCityRows()`，順序不可調換）：

```
1. normalizeCounty(city)                          — 縣市層級別名
2. normalizeDistrictToParentCounty(city)          — LEGACY_EXPLICIT_OVERRIDE
3. resolveUniqueSubdivisionParentCounty(city)     — AUTHORITATIVE_UNIQUE_RESOLUTION
4. 以上皆未命中 → unmapped
```

### A. LEGACY_EXPLICIT_OVERRIDE（`DISTRICT_PARENT_ALIASES`，本輪未修改內容）

| 別名 | 對應縣市 | Catalog 真實唯一性 | 分類理由 |
|---|---|---|---|
| Longtan District / Longtan Dist. / Longtan / 龍潭區 | 桃園市 | **本身即唯一**（全國僅一個龍潭區） | 明確、無取捨，單純正確 |
| Taoyuan District / Taoyuan Dist. / 桃園區 | 桃園市 | **Catalog 層級 ambiguous**（同時對應 68000010 桃園市桃園區、64000370 高雄市桃源區） | **有意識的既有業務決策**：桃園市桃園區人口 20 萬以上，高雄市桃源區為原住民鄉、人口約 4000 人，GA4 於此店家情境下產生桃源區流量機率極低。此判斷在 R5.4-G1.5-B2.5 已作成並記錄，**本輪不重新定義、不移除、不擴散這個取捨**。 |

**驗證**：`resolveUniqueSubdivisionParentCounty('Taoyuan District')` 若被直接呼叫（略過 Legacy 分支），Catalog 真實回傳 `status: 'ambiguous'`，候選為：

```json
[
  { "county_name": "高雄市", "subdivision_name": "桃源區", "subdivision_code": "64000370" },
  { "county_name": "桃園市", "subdivision_name": "桃園區", "subdivision_code": "68000010" }
]
```

因為 Legacy Override 在呼叫順序上優先於 Unique Resolver，`_aggregateCityRows()` 對 `"Taoyuan District"` 實際仍會先命中 Legacy Override（桃園市），**行為與修正前完全一致，不受本輪影響**。

### B. AUTHORITATIVE_UNIQUE_RESOLUTION（本輪新增 `resolveUniqueSubdivisionParentCounty()`）

只處理 Legacy Override 沒有涵蓋、且 Catalog 證明全國唯一的名稱。實際命中案例：

| 名稱 | 對應縣市 | 分類 |
|---|---|---|
| Pingzhen District / Pingzhen Dist. / Pingzhen / 平鎮區 | 桃園市 | AUTHORITATIVE_UNIQUE_RESOLUTION |
| Yangmei District | 桃園市 | AUTHORITATIVE_UNIQUE_RESOLUTION |
| Banqiao District | 新北市 | AUTHORITATIVE_UNIQUE_RESOLUTION |
| Zhongli District | 桃園市 | AUTHORITATIVE_UNIQUE_RESOLUTION |
| Bade District | 桃園市 | AUTHORITATIVE_UNIQUE_RESOLUTION |
| Luzhu District | 桃園市 | AUTHORITATIVE_UNIQUE_RESOLUTION |
| Xindian District | 新北市 | AUTHORITATIVE_UNIQUE_RESOLUTION |
| Sanchong District | 新北市 | AUTHORITATIVE_UNIQUE_RESOLUTION |

**兩者不可混為一談**：A 類是「即使 Catalog 顯示歧義、仍由人為業務判斷明確指定」的既有決策；B 類是「Catalog 本身證明全國唯一」才自動映射，完全不涉及業務判斷或猜測。B 類邏輯**不會**、也**不可能**覆蓋、移除 A 類設定（A 類在呼叫順序上更早被檢查，一旦命中就直接回傳，不會再往下檢查 B 類）。

---

## 四、Ambiguity Gate — 完整候選資料（僅使用公開行政區資料）

Ambiguous aliases count（真實掃描）：**47**

以下為實際 Runtime Test 驗證的樣本（見 `scripts/smoke-hotfix30-b5-r5-4-g1-5-b2-6-unique-subdivision-normalization.js` 第 26、33、34 項與 Mutation A/B）：

### Taoyuan District（需求文件九明確要求列出真實候選）

```json
{
  "status": "ambiguous",
  "candidates": [
    { "county_name": "高雄市", "subdivision_name": "桃源區", "subdivision_code": "64000370" },
    { "county_name": "桃園市", "subdivision_name": "桃園區", "subdivision_code": "68000010" }
  ]
}
```

### Dongshan（宜蘭冬山鄉 vs 台南東山區）

```json
{
  "status": "ambiguous",
  "candidates": [
    { "county_name": "宜蘭縣", "subdivision_name": "冬山鄉", "subdivision_code": "10002080" },
    { "county_name": "臺南市", "subdivision_name": "東山區", "subdivision_code": "67000060" }
  ]
}
```

### Fuxing（彰化福興鄉 vs 桃園復興區）

```json
{
  "county_name": "彰化縣", "subdivision_name": "福興鄉", "subdivision_code": "10007050"
}
```
```json
{
  "county_name": "桃園市", "subdivision_name": "復興區", "subdivision_code": "68000130"
}
```

以上三組全部驗證 `resolveUniqueSubdivisionParentCounty()` 回傳 `status: 'ambiguous'`，**不自動映射，county/subdivision 皆不回傳有效值**，一律進入 `unmapped`。完整 47 筆清單見 `scripts/audit-taiwan-unique-subdivision-aliases.js` 執行輸出（`ambiguous_alias_detail`），該輸出即本文件資料的唯一真實來源，本文件僅摘錄代表性樣本。

---

## 五、Non-TW 排除語意（未受影響）

`_aggregateCityRows()` 對 `countryId !== 'TW'` 的排除檢查在原始碼順序上位於**任何**縣市/行政區解析（含新的 Unique Resolver）**之前**：

```js
if (countryId && countryId !== 'TW') { excludedNonTw += 1; continue; }
```

因此即使 `city` 名稱本身在 Catalog 中全國唯一（例如 `US + "Pingzhen District"`），仍會在到達 Unique Resolver 之前就被 `continue` 排除，`excludedNonTw += 1`，**不會**進入 `mapped_counties`。已有 Runtime Test（Smoke 第 32 項）與 Mutation Test（Mutation C）雙重驗證。

---

## 六、Production Diff（真實檔案比較結果）

以 `diff -rq` 比較 H1.1-AUTH baseline 與本輪 H1.2 工作目錄（排除 node_modules/.git/data/pos.db）：

```
Files .../H1.1-AUTH/utils/ga4Realtime/index.js and .../H1.2-UNIQUE-ADMIN/utils/ga4Realtime/index.js differ
Files .../H1.1-AUTH/utils/taiwanGeoNormalize.js and .../H1.2-UNIQUE-ADMIN/utils/taiwanGeoNormalize.js differ
Only in .../H1.2-UNIQUE-ADMIN/scripts: audit-taiwan-unique-subdivision-aliases.js
Only in .../H1.2-UNIQUE-ADMIN/scripts: run-regression-g1-6-ga4-h1-2.js
Only in .../H1.2-UNIQUE-ADMIN/scripts: smoke-hotfix30-b5-r5-4-g1-5-b2-6-unique-subdivision-normalization.js
Only in .../H1.2-UNIQUE-ADMIN/scripts: static-audit-g1-6-ga4-h1-2.js
```

**確認：Production 檔案異動只有 2 個**（`utils/taiwanGeoNormalize.js`、`utils/ga4Realtime/index.js`），其餘全部是新增的 scripts（測試／稽核／回歸），完全符合本輪凍結範圍。（`scripts/run-regression-g1-6-ga4-h1.js` 內容也有異動——修正兩個 stale expected 數字，屬於既有 script 的 bug fix，不屬於 Production 邏輯，見 Test Report 第五節。）

逐行 diff 全文見 Implementation Report 附錄。

---

## 七、聲明彙總（不得混淆的邊界）

1. 只有全國唯一名稱會自動映射（AUTHORITATIVE_UNIQUE_RESOLUTION）。
2. 歧義名稱（47 筆）維持 unmapped，不猜測、不取第一筆。
3. 非台灣資料（`countryId !== TW`）一律先排除，不論名稱是否唯一。
4. 舊即時圖層（`/api/geo-live/ga4-realtime`）仍是**縣市級聚合層**（County Aggregation Layer），不是行政區精確點位圖層（District Marker Layer）。
5. 不代表個別訪客精確位置、不代表 GPS。
6. 完全未修改 H1 新 Aggregate Marker Layer（A1.2 Authoritative Admin Point Catalog／`geo-ga4-h1-panel.js`）。
