# R5.3-A3 — Regression Report

## 1. 範圍

本輪修改：`utils/db.js`（additive 新增 `geo_visit_log.source_event_id` 欄位
+ 索引）、`utils/geoVisitLog.js`（`logGeoVisit()` 支援 `source_event_id`）、
`utils/analyticsLog.js`（`insertEvent()` 傳遞新寫入列的 id 給
`logGeoVisit()`）、`public/js/geo-heatmap-ui.js`（Heatmap 分頁啟用時主動
抓取 Geo Event Engine 資料，不再只綁在 Visitor Layer 手動切換之後）、新增
`scripts/backfill-geo-visit-log.js`（一次性、冪等）。

**未修改**：Analytics Event Engine（`utils/analyticsLog.js` 的
`INSERT INTO analytics_events` 陳述式本身、事件白名單、Identity/Channel/
Page Type Resolver）、Order Heatmap Engine（`geo-heatmap.js`）、既有
Dashboard 查詢（`geo-intelligence.js`／`utils/geoAnalyticsQueries.js`）、
老闆儀表板即時統計（`utils/dashboardAnalytics.js`）。

## 2. Static 驗證

```
node --check（全專案，排除 node_modules）→ 全部 OK
```

## 3. 檔案不變性確認（SHA-256）

| 檔案 | 結果 |
|---|---|
| `public/js/geo-heatmap.js`（Order Heatmap Engine） | 與 R5.3-A2 基線逐位元組相同 |
| `public/js/geo-intelligence-map.js` | 與基線逐位元組相同 |
| `public/js/geo-map-settings.js` | 與基線逐位元組相同 |
| `public/data/geo/taiwan/manifest.json` | 與基線逐位元組相同 |
| `public/js/geo-intelligence.js`（Dashboard／Funnel） | 與基線逐位元組相同 |
| `utils/dashboardAnalytics.js`（老闆儀表板即時統計） | 與基線逐位元組相同 |

## 4. 既有 Smoke／Regression（全部 26 套件，0 FAIL）

| 套件 | 結果 |
|---|---|
| A2 Geo Event Engine Smoke | 228/228 PASS |
| A1.2 Visitor Geo Sync | 188/188 PASS |
| A1.1 Integration | 112/112 PASS |
| A1 Heatmap Engine | 128/128 PASS |
| B2 Geo Map | 630/630 PASS |
| B3 Geo Settings UI | 157/157 PASS |
| B1-1 Dashboard Rewire | 100/100 PASS |
| B1-2 Area Ranking | 150/150 PASS |
| B1-3 Geo Behavior Funnel Backend | 76/76 PASS（見下方第 5 節既有 flakiness 說明） |
| B1-4 Geo Rule Engine | 132/132 PASS |
| B1-4.5 Geo Explainability | 819/819 PASS |
| B1-4.6 API Stabilization | 867/867 PASS |
| B1-5 Geo Dashboard UI | 313/313 PASS |
| B1-6 Geo Explorer | 376/376 PASS |
| R5.1-A Geo Foundation | 76/76 PASS |
| R5.1-B Geo API | 111/111 PASS |
| R5.1-C Geo UI | 196/196 PASS |
| R5.1-D1 Visitor Geo | 164/164 PASS |
| R5.2-A Business Area Reserved | 62/62 PASS |
| R5.2-A Integrated E2E | 232/232 PASS |
| R5.2-A Order Geo Write | 90/90 PASS |
| R5.2-A Stage 7 | 140/140 PASS |
| Cart/Order Hours（既有，跟 Geo 無關） | 55/55 PASS，MANUAL 4 |
| Dashboard UI（既有） | 20/20 PASS |
| Debounce（既有） | 32/32 PASS |
| Hotfix30-C1 Rollback | 22/22 PASS |

沒有降低任何既有 assertion 數量。

## 5. 已知既有 Flakiness（沿用前輪報告，本輪未新增，未修正也不需要修正）

`smoke-hotfix30-b5-r5-2-b1-3-geo-behavior-funnel-backend.js` 與
`smoke-hotfix30-b5-r5-1-c-geo-ui.js` 已在前一輪（R5.3-A2）確認為既有
時序脆弱測試，且已用暫時停用 hook + 對照未修改的 R5.3-A1.2-QA 原始 ZIP
兩種方式證實與 Geo Event Engine／`insertEvent()` 的任何改動無關。本輪
新增的 `source_event_id` 欄位是額外的 additive column，不影響既有查詢
欄位集合，兩個測試在本輪的乾淨執行下皆為 76/76 與 196/196 全綠（見上表）。

## 6. 新增驗證項目（本輪特有）

- `scripts/backfill-geo-visit-log.js` 冪等性：對同一批歷史資料執行兩次，
  第二次找到 0 筆待補資料，不重複寫入（已用真實 sql.js DB 驗證，見
  `R5.3-A3_GEO_SYNC_VERIFICATION.md`）。
- `geoHeatUiRegisterContext()`／`geoHeatUiSwitchTab()` 修正後，Order
  Heatmap 既有行為（Metric/Display/Channel 切換、Ranking、Legend）完全
  沒有改變——這兩個函式修正的是「額外多呼叫一次 `geoVisitorFetchAndRender()`」，
  沒有動到既有呼叫 `geoHeatUiFetchAndRender()`（Order Heatmap 的既有
  抓取函式）的任何一行。

## 7. 結論

本輪修正不影響 Analytics Event Engine、Order Heatmap Engine、既有
Dashboard／Funnel 查詢，全部既有 Regression 維持 0 FAIL，新增的
Backfill 腳本與 UI 抓取時機修正皆已通過真實資料驗證。
