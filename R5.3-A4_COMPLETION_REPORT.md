# R5.3-A4 — Geo Metric Switcher Consolidation — Completion Report

狀態：**QA BUILD**。Automated Verification：PASS。Manual Visual：REQUIRED。
Final Completion：NOT YET APPROVED（本文件本身不宣告正式 COMPLETE）。

---

## 1. Scope

把「舊版 Geo Intelligence Map 獨立 Metric 切換器」（Visitors/Orders/
Revenue/Conversion/Cart Abandonment/Recommendation Risk，共用地圖旁的
`.geo-map-metrics` 按鈕列）與「新版 Geo Event Engine 8 個 Metric Tab」
（`geoVisitorState.metric`）整合成**單一正式主切換器**，消除兩套按鈕
分別控制不同 state 的問題。

## 2. 修改檔案

| 檔案 | 動作 |
|---|---|
| `public/js/geo-intelligence-map.js` | 移除 `geoRenderMapBlock()` 內建的獨立 `.geo-map-metrics` 按鈕列；`geoSetMapMetric()`/`geoUpdateMapData()`/`GEO_MAP_METRICS` 等既有函式與常數簽名完全不變，只是不再自己產生按鈕 |
| `public/js/geo-visitor-layer.js` | 新增 `GEO_EVENT_TO_MAP_METRIC` 對照表；`geoVisitorSetMetric()` 切換時同步呼叫既有 `geoSetMapMetric()`，一次驅動 Map Overlay／Legend／Summary／Tooltip |
| `public/js/geo-heatmap-ui.js` | 新增 `geoHeatUiRenderSharedMetricBar()`；Metric Bar 從「只在 Heatmap 分頁的 Visitor Layer 內」移到「跟共用地圖同一層級」；`geoHeatUiRegisterContext()`／`geoHeatUiSwitchTab()` 的 Geo Event Engine 抓取改為不論目前分頁一律主動執行（Eager Fetch） |
| `public/js/geo-intelligence.js` | 在既有 `mapHtml` 之後插入 `sharedMetricBarHtml`（單一渲染點，Dashboard／Heatmap 兩分頁皆可見） |
| `scripts/smoke-hotfix30-b5-r5-2-b2-geo-map.js` | 更新 5 項因架構調整而過時的斷言（J-RENDER-7/8/11、AA-A11Y-6、AW-A11Y2-3），改為正向驗證新架構（舊按鈕確實移除、`geoSetMapMetric()` 外部呼叫仍正確） |
| `scripts/smoke-hotfix30-b5-r5-3-a1-1/a1-2/a2-*.js` | 更新 SHA-256 基線鎖為 `geo-intelligence-map.js` 本輪合法變更後的新雜湊值；A2 測試補上組裝 DOM 缺漏（含入 Metric Bar） |
| `scripts/smoke-hotfix30-b5-r5-3-a4-metric-switcher-consolidation.js` | **新增**，110 項 assertion |

**沒有修改**：`utils/analyticsLog.js`、`utils/geoVisitLog.js`、
`utils/geoEventEngine.js`、`routes/analytics-geo.js`、
`public/js/geo-heatmap.js`（Order Heatmap Engine）、
`utils/dashboardAnalytics.js`（老闆儀表板即時統計）。

## 3. 舊／新切換器比較結論

見 `R5.3-A4_METRIC_SWITCHER_COMPARISON.md`（DOM／State／Handler／API／
Render Function 完整對照）。結論：兩者原本各自維護獨立 state
（`geoMapState.metric` vs `geoVisitorState.metric`），互不影響——這正是
問題本體。整合後只保留 `geoVisitorState.metric` 作為單一事實來源，
`geoMapState.metric` 改為被動接受更新的內部欄位。

## 4. Single Switcher Architecture

- 唯一的按鈕產生函式：`geoVisitorMetricBarHtml()`。
- 唯一的切換 handler：`geoVisitorSetMetric()`。
- 唯一渲染插入點：`geo-intelligence.js` 的 `sharedMetricBarHtml`，跟
  `mapHtml` 同一層級，不在任何分頁的隱藏 panel 裡面。
- `geoRenderMapBlock()` 不再輸出任何按鈕、不再有第二個 click 入口。

## 5. Metric Mapping

| 新（8 個） | 舊（對應） |
|---|---|
| `visitors` | `visitors` |
| `add_to_cart` | `visitors`（舊系統無此維度，fallback，不影響新系統自己的 Summary/Ranking 數字） |
| `checkout` | `visitors`（同上） |
| `orders` | `orders` |
| `revenue` | `revenue` |
| `conversion` | `conversion_rate` |
| `cart_abandonment` | `cart_abandonment_rate` |
| `recommendation_risk` | `risk` |

不合法的 metric 一律安全拒絕（`geoVisitorSetMetric()` 回傳 `false`），
不會污染 `geoVisitorState.metric`（A4 Smoke A4-1/A4-2 已驗證）。

## 6. Shared State Sync

單次切換驗證同步更新（A4 Smoke B3 系列）：`geoVisitorState.metric` →
`geoMapState.metric` → active button `aria-pressed` → Map Overlay
（`layer.setStyle()`）→ Tooltip（`layer.bindTooltip()`）→ Legend → Summary，
全部在同一次 `geoVisitorSetMetric()` 呼叫內完成，不需要使用者做第二個
動作。

## 7. Eager Fetch

`geoHeatUiRegisterContext()` 不論目前 `activeTab` 是 `dashboard` 還是
`heatmap`，掛載時一律呼叫一次 `geoVisitorFetchAndRender()`（預設 metric
為「訪客」）。重複掛載（模擬重新整理）不會造成 request 疊加暴增——沿用
既有 `requestSeq` + `AbortController` 機制（同一個既有防護，本輪未重寫）。

## 8. Dashboard／Heatmap 共用行為

Metric Bar 在切換分頁前後是**同一個 DOM 節點**（jsdom 實測用物件參考
相等比對，B6-1/B6-3），metric 選擇不會因為切分頁被重置（B6-2/B6-4），
按鈕的 `onclick` 屬性前後一致，都指向同一個 `geoVisitorSetMetric()`
（B6-5，沒有第二套 handler）。

## 9. Map／Tile／Layer Reuse

- 快速切換全部 8 個 Metric，`L.map()` 從未被呼叫（No Duplicate Map，
  B7-1）、`tileLayer` 從未被建立（Tile Reuse，B7-2）。
- Choropleth 的 `layerGroup` 只建立一次，重複 render 呼叫
  `clearLayers()` 重畫，不重新 `L.layerGroup()`（Layer Reuse，B8-1/B8-2）。

## 10. Store Isolation

切店（`geoHeatUiRegisterContext()` 重新掛載）清空舊 `areas`/`funnel`，
但**保留目前選中的 metric**（B11-2，例如選了「加入購物車」切店後仍是
「加入購物車」，不會被重置回「訪客」），重新抓取新店家資料後正確恢復
（B11-3）。

## 11. A4 Smoke（110/110）

`scripts/smoke-hotfix30-b5-r5-3-a4-metric-switcher-consolidation.js`：
Part A（22 項，Static/Source Audit）＋Part B（88 項，jsdom 實測），涵蓋
需求文件列出的全部 12 個類別：Single Switcher Invariant、8 指標完整性、
狀態同步、Metric Mapping、Eager Fetch、Dashboard/Heatmap 共用、Map
Reuse、Layer Reuse、Empty State、Accessibility、Store Isolation、
Date/Channel 沿用既有狀態。

過程中發現兩處**測試腳本本身**的設定順序問題（不是產品程式碼問題，已在
撰寫過程中修正，未列入最終 110 項的 FAIL）：`geo-intelligence-map.js`
內部自己宣告 `let geoMapState = {...}`，若在 `eval` 之前就設定
`window.geoMapState`，會被這個宣告覆蓋——改成先 `eval` 再用
`Object.assign()` 原地覆寫欄位即修正。

## 12. 完整 Regression（27 套件，全部 0 FAIL）

| 套件 | 結果 |
|---|---|
| **A4 Metric Switcher Consolidation Smoke（新增）** | **110/110 PASS** |
| A2 Geo Event Engine | 228/228 PASS |
| A1.2 Visitor Geo Sync | 188/188 PASS |
| A1.1 Integration | 112/112 PASS |
| A1 Heatmap Engine | 128/128 PASS |
| B2 Geo Map（含本輪更新的 5 項斷言） | 620/620 PASS |
| B3 Geo Settings UI | 157/157 PASS |
| B1-1～B1-6 | 100/150/76/132/819/867/313/376，全部 0 FAIL |
| R5.1-A／B／C／D1 | 76/111/196/164，全部 0 FAIL |
| R5.2-A 全系列（Business Area／Integrated／Order Geo Write／Stage7） | 62/232/90/140，全部 0 FAIL |
| Cart/Order Hours（既有，跟 Geo 無關） | 55/55 PASS，MANUAL 4 |
| Dashboard UI（既有） | 20/20 PASS |
| Debounce（既有） | 32/32 PASS |
| Hotfix30-C1 Rollback（Privacy 相關既有測試） | 22/22 PASS |

沒有降低任何既有 assertion 數量（B2 從 630 降到 620 屬於本輪合法架構
變更：移除的 10 項舊按鈕相關斷言，已用等量或更多的正向驗證取代，見第 2
節）。

`node --check`（全專案，排除 `node_modules`）：全部 OK。

## 13. Static Audit

- **舊 Metric Switcher DOM 已完全移除**：`geoRenderMapBlock()` 輸出的
  HTML 逐一驗證不含 `.geo-map-metrics`／`.geo-map-metric-btn`／
  `data-geo-map-metric`（A4 Smoke A1-1～A1-5）。
- **唯一正式 Metric Switcher 只有一套**：`geoVisitorMetricBarHtml()`／
  `geoVisitorSetMetric()`／`geoHeatUiRenderSharedMetricBar()` 皆只定義
  一次，且插入點只有一處（A1-6～A1-9）。
- **不存在雙 state 競爭**：`geoMapState.metric` 現在只被動接受
  `geoSetMapMetric()` 更新，沒有獨立按鈕再寫入它。
- **不存在雙 click handler**：`geoRenderMapBlock()` 輸出的 HTML 沒有任何
  `onclick="geoSetMapMetric(...)"`（A1-5）。
- **不存在第二個 Leaflet map／tile layer**：B7-1/B7-2 已驗證。
- **不存在第二套 Channel/Date state**：`geo-visitor-layer.js` 原始碼掃描
  確認沒有宣告任何第二個 `dashboardDateState`/`geoChannelState` 類變數
  （A12-1～A12-3），Channel 切換一律透過既有 `av2SetChannel()`。
- **不存在重複 API**：本輪未新增或修改任何路由（`routes/analytics-geo.js`
  未變動）。
- **不存在 debug log／`Math.random()`**：`geo-visitor-layer.js`／
  `geo-heatmap-ui.js`／`geo-intelligence-map.js` 掃描確認皆無。
- **不存在假座標**：本輪沒有新增任何座標運算邏輯，沿用 A1.2/A2 已確認
  的「Visitor Geo 無精確座標時 lat/lng 一律 NULL」規則。
- **不存在 fixture dependency**：Choropleth 合法 Polygon 允許清單沿用
  A1.2 稽核結論，維持空集合，未啟用矩形 fixture。
- **不存在 `store_001` 或桃園硬編碼作為邏輯來源**：本輪修改的三個檔案
  掃描確認無任何店家 ID 或縣市名稱寫死在判斷邏輯中（測試 fixture 資料中
  出現「桃園市」僅為範例資料，不影響產品邏輯）。
- **Eager fetch 有 request guard**：沿用既有 `requestSeq` +
  `AbortController`（B5-5 已驗證重複 mount 不會疊加多次 request）。
- **快速切換有 debounce／abort／stale response guard**：`geoVisitorFetchAndRender()`
  的 `seq !== geoVisitorState.requestSeq` 檢查沿用不變，本輪未修改這段
  邏輯。
- **Order Heatmap 與 Visitor Heatmap 既有功能未退化**：`geo-heatmap.js`
  SHA-256 逐位元組與基線相同；A1/A1.1/A1.2/A2 既有 Smoke 全數 0 FAIL。

## 14. Known Limitations

- Revenue／Conversion／Cart Abandonment／Recommendation Risk 四個
  Metric 在舊地圖著色系統沒有獨立的行政區級數字來源，Choropleth 著色
  沿用對照表 fallback（Add To Cart／Checkout 兩者 fallback 顯示
  Visitors 分布），不影響這四個 Metric 自己在 Summary/Ranking 面板顯示
  的精確數字。
- Choropleth 目前仍不會在任何城市顯示顏色（沿用 A1.2 稽核結論：矩形
  fixture 未啟用），Metric 切換對地圖的可見影響目前主要是 Legend／
  Summary 數字，尚無法在地圖上直接看到色塊變化。
- Channel 篩選尚未在 Geo Event Engine 的查詢層生效（見
  `R5.3-A2_DATA_DECISION.md` 已記載的 Pending 項目，本輪未變更）。

## 15. Manual QA 狀態

見 `R5.3-A4_MANUAL_QA_CHECKLIST.md`——尚未執行。涵蓋八個指標逐一點擊、
Dashboard/Heatmap 分頁切換、頁面載入即有資料、Unknown/無資料狀態、
Store/Date/Channel 切換、快速連點、Responsive（390/768/1024/1440）、
Chrome/Edge/Android、Console 錯誤檢查共 9 大類。

## 16. 是否可部署

自動化驗證（110 項 A4 Smoke + 完整 27 套 Regression + `node --check`）
全數 PASS，可視為 QA BUILD 完成。**Manual Visual 尚未執行，依使用者
本輪明確指示，在完成真實瀏覽器驗證前不得宣告 Final Release。**
