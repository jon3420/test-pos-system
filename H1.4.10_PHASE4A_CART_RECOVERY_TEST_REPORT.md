# H1.4.10 Phase 4A — CART RECOVERY STATE ENGINE Test Report

## 五層 Regression Gate（fresh-run）

```
Gate 1 Phase 4A： TOTAL=89  PASS=89  FAIL=0
Gate 2 Phase 3 ： TOTAL=111 PASS=111 FAIL=0
Gate 3 Phase 2 ： TOTAL=83  PASS=83  FAIL=0
Gate 4 Phase 1 ： TOTAL=68  PASS=68  FAIL=0
Gate 5 H1.4.9  ： TOTAL=67  PASS=67  FAIL=0
```

## 第一輪測試發現的真實 Bug（誠實記錄，非測試撰寫錯誤）

第一輪執行時，「payment_started metadata Integration（真實 HTTP）」測試失敗：`payment_abandoned found 0`，且後續查詢 `analytics_events` 時因誤用 `order_id` 當查詢條件（client payment_started 本來就沒有 order_id）導致 `storedRow` 為 `undefined`、测试崩潰。

判斷為 **Production bug（架構缺陷）**，非 test harness 誤用：`onAnalyticsEvent()` 原本直接信任 `fields.order_id` 來建立 `payment_abandoned`，但這個欄位對 client `payment_started` 事件從未有值。修正方式見 Reality Audit：新增 `resolveAuthoritativeOrderForPaymentStart()`，透過 `store_id+cart_id → submit_order → orders` 這條 server-authoritative 路徑解析真實 order_id 與權威金額，完全不信任任何 client 提供的 `order_id`/`value`/`cart_value`。修正後重新執行，89/89 全數 PASS。

## Phase 4A Test Coverage（依需求文件章節）

### 一、DB Migration
`cart_recovery_jobs` 欄位齊全（含 Phase 4B 預留欄位）；7 個索引皆存在；`CREATE TABLE/INDEX IF NOT EXISTS`；未 `DROP` 任何既有表。

### 二、Settings Defaults
`cart_recovery_enabled` 預設 `0`；`cart`/`checkout`/`payment` delay 預設 `60`/`30`/`15`；`max_attempts` 預設 `1`。`enabled=0` 時 `add_to_cart` 不建立 job；`enabled=1` 後正常建立。

### 三、Cart Stage（C1-C4）
add_to_cart 建立 1 個 job，`due_at≈+60分`；同 cart 連續 5 次 add_to_cart 仍只有 1 個有效 job，`due_at` 隨最新活動 refresh；`checkout_click` 取消 `cart_abandoned`、建立 `checkout_abandoned`。

### 四、Checkout Stage
`checkout_click`→`checkout_submit_click`→`checkout_validation_failed` 全部作用在同一筆 job（id 相同），`last_event` 正確更新為最新活動，未新建第二筆。

### 五、submit_order Cancellation Semantics
`submit_order` 正確取消 `cart_abandoned`／`checkout_abandoned`，且不留下任何「永久禁止 payment recovery」的旗標（下一節即證明）。

### 六、LINE Pay 真實順序（核心規則）★
`submit_order` 已存在的情況下，`payment_started(linepay)` 仍能正確建立 `payment_abandoned` —— 這是 Phase 4A 最重要的單一斷言，PASS。

### 七、payment_started metadata Integration（真實 HTTP，含本輪修正後的安全驗證）
- linepay：真實 HTTP 呼叫成功；`analytics_events` 用 `cart_id`（非 `order_id`）查得到這筆事件；`metadata_json` 只含 `payment_method`（`value`/`cart_value` 已被 sanitizer 濾除）；`payment_abandoned` job 正確建立。
- **安全驗證**：即使 client 夾帶 `order_id='FAKE-CLIENT-ORDER'` 與 `metadata.value=1`/`cart_value=999999`，Recovery job 的 `order_id` 仍是 cart→submit_order 解析出的真實值，**絕不是**偽造值；`recoverable_value` 正確等於 DB `orders.total`（=1234），不是任何 client 傳入的數字。
- cash／transfer：即使已有 `submit_order` 存在，仍**不建立** `payment_abandoned`（只有 `linepay` 才建立）。
- Store Isolation：store A／B 對同一字串 `cart_id` 各自解析出自己店内的 `submit_order`，`store_id` 是查詢的強制條件。

### 八、Payment Idempotency
同 cart 三次 `payment_started(linepay)`（皆解析到同一個 order）→ 有效 job 仍只有 1 筆。

### 九、Payment Conversion（P1/P2/P3）
- P1：`payment_success` → job converted，`getRecoverableJobs()` 查不到。
- P2：`purchase` → job converted。
- P3：只有 `payment_started`、無後續事件（模擬放棄）→ 仍 current recoverable。

### 十、Full Convergence
`add_to_cart→checkout_click→submit_order→payment_started→payment_success` 全流程跑完後，cart/checkout/payment 三個 stage 的 job 全部是 `cancelled` 或 `converted`，沒有任何一筆仍是 pending/waiting。

### 十一、Empty Cart（等價語意）不誤取消 payment_abandoned
`submit_order` 取消 cart/checkout job 的同時，已建立的 `payment_abandoned` 完全不受影響，仍是 pending。

### 十二、Anonymous Cart
無 `line_user_id` 的顧客仍可正常建立 `cart_abandoned`/`checkout_abandoned`，`line_user_id` 欄位為空，不因匿名判定不可 recovery。

### 十三、Trusted LINE Identity
真實 HTTP 帶 `line_user_id='U_FAKE'` 但無有效 `member_session` → job 的 `line_user_id` 絕不是這個偽造值（`routes/analytics.js` 既有的 `verifyMemberSession()` 機制持續生效）。

### 十四、Store Isolation
店 A/B 對同一字串 `cart_id` 建立完全獨立的 job；店 A 的 `checkout_click` 不會 cancel/convert/refresh 店 B 的 job。

### 十五、Idempotency Key
`cart:{cart_id}:{stage}` / `order:{order_id}:{stage}`（payment 有 order_id 時優先）/ 兩者皆無時回傳 `null`（安全跳過）。

### 十六、Delay Settings Override
預設值與 override 後的值（90/45/20）皆驗證正確，`due_at` 使用最新設定值計算，非 hardcode。

### 十七、getRecoverableJobs()
只回 `pending`/`waiting`；`converted`/`cancelled`/`not_configured` 皆不回。

### 十八、24h Horizon
24h 內到期的 pending job 算 current recoverable；超過 24h 的舊 job 不算，但資料列本身不被刪除（歷史資料保留）。

### 十九、Recovery Amount（Authoritative）
`schedulePaymentRecovery()` 呼叫端傳入的 `recoverableValue` 正確存入（單元層級）；真實 HTTP integration（第七節）進一步證明整條路徑最終落地的金額是 DB `orders.total`，不是任何 client 數字。Cart stage 沒有 authoritative snapshot 來源時，`recoverable_value` 保持 `NULL`，不假造。

### 二十、Fail-open
DB 操作全部 throw 時 `onAnalyticsEvent()` 不拋出；Recovery hook 本身 throw 時，`logServerEvent()` 呼叫端不受影響，`analytics_events` 主表確實正常寫入該筆事件。

### 二十一、payment_success Security Regression
client 偽造 `payment_success` 依然被拒（HTTP 400/403，DB count=0），且偽造請求不會讓已存在的 `payment_abandoned` job 被誤 converted，仍是 pending。

### 二十二、Canonical Mapping 靜態檢查
`checkout_click→GA4 begin_checkout`／`checkout_click→Meta InitiateCheckout` 映射契約未變；`utils/cartRecovery.js` 完全不呼叫任何第三方 Analytics 平台函式。

## 相關既有回歸套件（fresh-run）

| Script | 結果 |
|---|---|
| `run-h1-4-7-analytics-contract-runtime.js` | 61/61 |
| `run-g1-6-ga4-h1-4-7-two-stage-checkout-runtime.js` | 113/113 |
| `smoke-hotfix29-c.js` | 54/54，MANUAL=2（既有） |
| `smoke-hotfix30-b5-r5-2-a-integrated.js` | M3 `/cart-attribution` 失敗（詳下） |

## Pre-existing Failure（誠實隔離，非本輪 regression）

`smoke-hotfix30-b5-r5-2-a-integrated.js` 的 M3：`Cannot read properties of undefined (reading 'district_ranking')`，`utils/cartSnapshot.js`／`utils/cartGeoAttribution.js` 既有 bug，與本輪 checkout submit/payment/recovery 程式碼路徑完全無關。此問題在 Phase 1/2/3 的 Test Report 中皆已記錄並用 pristine baseline 驗證過，本輪重新確認錯誤訊息與行號完全相同，非 Phase 4A 造成。
