# R5.4-G1.3 — Geo Metric Sync & Coverage Explanation
## Completion Report

fix18-10-hotfix30-B5-R5.4-G1.3

**版本狀態：QA BUILD**
**Automated Verification：PASS**
**Manual Visual：REQUIRED / NOT TESTED**
**Final Completion：NOT YET APPROVED**

---

## 1. 三個既有 Metric State

`geoVisitorState.metric`（全域 8-Tab）、`geoMapState.metric`（共用 Choropleth 地圖）、`geoHeatState.metric`（Order Heatmap 分頁）。詳見 `R5.4-G1.3_METRIC_SYNC_FIX.md` §1。

## 2. Root Cause

R5.3-A4 已完成 `geoVisitorState.metric → geoMapState.metric` 同步，**從未**完成 `geoVisitorState.metric → geoHeatState.metric` 同步，兩邊各自維護獨立按鈕/state。

## 3. Mapping

`GEO_EVENT_TO_HEATMAP_METRIC`（8 個全域指標 → 6 個有對應 + 2 個 null）+ 自動反轉的 `GEO_HEATMAP_TO_EVENT_METRIC`。`cart_abandonment`／`recommendation_risk` 確認不會靜默降級為 Orders。

## 4. 單一同步入口

`geoHeatUiSyncMetricFromGlobal(globalMetric)`，由 `geoVisitorSetMetric()` 唯一呼叫點觸發。

## 5. 雙向同步 Guard

`_geoMetricSyncInProgress` 模組層級布林旗標，兩個方向的同步函式開頭都檢查並提前返回，已用連續 10 次快速雙向觸發驗證無遞迴。

## 6. 兩個初始 Test Failure 的最終原因

**兩者皆為 Test Harness 問題（Category A），不是產品程式碼 Bug：**

- **#38（API Error display）／#39（Loading state cleared）**：追查後發現是我方 Smoke 測試自己的 `buildDom()` 輔助函式，建立測試用 DOM 骨架時**漏掉了** `#{containerId}-heat-loading` 與 `#{containerId}-heat-error` 兩個元素本身。`geoHeatUiFetchAndRender()` 產品程式碼本身完全正確——用手動建立完整 DOM 骨架的方式直接呼叫同一段程式碼重現，`errorEl.hidden` 與 `loadingEl.hidden` 的行為與預期完全一致。修正方式：只補上測試用 DOM 骨架缺少的兩個元素，**未修改任何產品程式碼**。

## 7. Coverage Explanation

已用真實回報案例逐一驗證：

| 情境 | 輸出 |
|---|---|
| Orders 模式，訂單=1，Geo=0 | 今日已有 1 筆訂單，但目前沒有訂單包含可用的地理資料，因此無法顯示地圖熱區。 |
| Revenue 模式，營收=NT$150，Geo=0 | 目前已有營收 NT$150，但目前沒有任何營收可歸屬到地理區域。 |
| 外送最佳化，外送=0 | 今日沒有外送訂單，因此目前無法計算平均距離、外送費與配送最佳化建議。 |
| Business Opportunity，訂單=1／營收=150／Geo=0 | 目前已有 1 筆Orders資料，但尚無可歸屬地理區域的資料，因此暫時無法產生區域商機建議。（Revenue 版本另有專屬 NT$ 金額文案） |

不再出現「資料不足」/「沒有資料」/裸 0 這類模糊文案。

## 8. G1.3 Smoke

```
scripts/smoke-hotfix30-b5-r5-4-g1-3-metric-sync-coverage.js
PASS: 117 / FAIL: 0 / TOTAL: 117（超過需求 110+）
```

## 9. 完整 Regression

```
G1 Smoke:                212 / 212 PASS
G1.1 Visual Smoke:        91 /  91 PASS
G1.2 Layer Switch Smoke:  83 /  83 PASS
G1.3 Metric Sync Smoke:  117 / 117 PASS
A7/A5/A4/A2/A1.2/B2/B3：全部 PASS（A4 即既有 Metric Switcher Consolidation 測試本身，本輪重跑確認未被破壞）
其餘既有 65 套 Smoke/Verify：全部重跑
全專案 node --check：204 個檔案，0 語法錯誤
```

**Baseline Comparison：**
- **Pre-existing failures（本輪前後皆同樣狀態，與 G1.3 無關）**：`smoke-cart-delivery-live-refresh.js`(3)、`smoke-hotfix26-c.js`(1)、`smoke-hotfix26-f2.js`(3)、`smoke-hotfix26-f7.js`(1)、`smoke-hotfix26-f8-b.js`(2)、`smoke-hotfix27-cd.js`(3)、`smoke-hotfix27.js`(2)、`smoke-hotfix28.js`(2)、`smoke-hotfix29.js`(3)——全部是先前回合已確認、與 Geo Live Layer/Metric Sync 完全無關的既有問題（詳見 `regression_flaky_note.txt`）。
- **G1.3 New failures**：0。
- **G1.3 Fixed failures**：0（本輪範圍是 Metric 同步與說明文字，未修改上述既有失敗涉及的產品邏輯）。

## 10. Static Audit

`scripts/static-audit-g1-3.js`：**32/32 通過**（含 2 項額外稽核）。既有 `scripts/static-audit-g1.js`（30/30）與 `scripts/static-audit-g1-2.js`（25/25）重跑均維持全數通過。

## 11. Known Limitations

見 `R5.4-G1.3_METRIC_SYNC_FIX.md` §10：非座標型 Metric 的 `drawn` 恆為 0（系統性現況，非本輪引入）；外帶/外送逐筆數字目前無資料來源，只提供靜態說明。

## 12. Manual Visual 狀態

**NOT TESTED。** 本輪工作在無瀏覽器的伺服器端 Linux 沙箱完成，無法執行真實瀏覽器點擊/視覺驗收。`R5.4-G1.3_MANUAL_QA_CHECKLIST.md` 所有項目皆明確標記 NOT TESTED，未寫 PASS。

## 13. 是否可部署

**程式碼層面（自動化驗證）已完整**：G1.3 Smoke 117/117、G1/G1.1/G1.2 既有 Smoke 全數維持通過、Static Audit 32+30+25 全通過、完整 Regression 零新增失敗、全專案 `node --check` 零錯誤。

**因 Manual Visual 尚未執行，不得宣告 Final Release。** 建議先在真實瀏覽器由人工完成 `R5.4-G1.3_MANUAL_QA_CHECKLIST.md` 全部項目（尤其是上下 Metric 實際點擊互動與 Coverage Explanation 的實際排版），確認後再提升版本狀態。
