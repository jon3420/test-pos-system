# R5.4-G1.6-A1.2.1_TIMEZONE_REALITY_AUDIT.md
Geo Event Taiwan Time & Estimate Marker Verification Hotfix

---

## 1. 現場症狀

真實部署畫面：

- 右上角系統時間：`2026-08-05 09:53`
- Recent Geo Events：`2026-08-05 01:52:36`／`vis_***b95`／`view_product`

兩者相差整整 8 小時。

`view_product` 是**商品瀏覽事件時間**，不是訪客登入時間。本輪修正過程中，
UI 文案與內部命名一律沿用「事件時間」，不使用「登入時間」。

## 2. DB Storage Reality

`geo_visit_log.event_time` 欄位定義（`utils/db.js`）：

```sql
event_time  TEXT NOT NULL DEFAULT (datetime('now'))
```

`geo_live_coordinates.captured_at` 欄位定義同樣是：

```sql
captured_at  TEXT NOT NULL DEFAULT (datetime('now'))
```

SQLite 的 `datetime('now')` 回傳**UTC**時間字串，不含時區標記（無 `Z`、無 offset）。
這是本輪稽核唯一需要確認的儲存事實，且與專案既有的 `utils/dashboardDate.js`
（`analytics_events.created_at` 稽核註解）記錄的結論一致。

**本輪明確不修改：**
- DB Schema（沒有新增/刪除/改型任何欄位）
- 歷史資料（沒有任何 `UPDATE ... SET event_time`）
- 既有 `event_time` / `captured_at` 原始字串內容
- `geo_visit_log` / `geo_live_coordinates` 的 DDL 或索引

## 3. API Reality

**修正前**（`utils/geoVisitLog.js` 的 `getRecentGeoVisits()`）：

```js
return rows.map((r) => ({
  event_time: r.event_time, city: r.city, district: r.district, ...
}));
```

只回傳 `event_time: "YYYY-MM-DD HH:mm:ss"`，沒有時區標記，前端無從得知這是 UTC
還是 Local。

**修正後：**

```js
return rows.map((r) => ({
  event_time: r.event_time,                  // 舊欄位，向後相容，原樣保留
  event_time_utc: toUtcIsoString(r.event_time), // 新欄位，標準 ISO 8601 UTC
  ...
}));
```

- 舊欄位 `event_time` **完全保留**，格式不變。
- 新欄位 `event_time_utc`：合法時間 → `"YYYY-MM-DDTHH:mm:ss.sssZ"`；
  無法解析（null／空字串／格式不明）→ 安全回傳 `null`，不 throw、不產生
  `1970-01-01` 或 `Invalid Date`。
- 沒有修改 `event_time` 本身或任何 DB 值。

## 4. Frontend Reality

**修正前**（`public/js/geo-visitor-layer.js`）：

```js
`<span class="geo-visitor-recent-time">${_geoVisitorEsc(r.event_time)}</span>`
```

直接把資料庫原始 UTC 字串塞進 DOM，沒有任何 timezone parsing 或格式化。

**修正後：**

```js
const taipeiTime = (typeof formatTaipeiDateTime === 'function')
  ? formatTaipeiDateTime(r.event_time_utc || r.event_time)
  : (r.event_time || '—');
`<span class="geo-visitor-recent-time" title="事件時間（台灣）">${_geoVisitorEsc(taipeiTime)}</span>`
```

- 優先使用標準欄位 `event_time_utc`，舊欄位僅作 fallback。
- 透過唯一的瀏覽器端 Helper `public/js/date-time-format.js` 的
  `formatTaipeiDateTime()` 轉換，該函式已載入 `index.html` 中
  `geo-visitor-layer.js` 之前。
- Tooltip／title 顯示「事件時間（台灣）」。

**驗收範例（已由 Smoke／QA Harness 實測確認）：**

| 輸入（DB / API `event_time_utc`） | 畫面顯示 |
|---|---|
| `2026-08-05 01:52:36`（DB 原始）| — |
| `2026-08-05T01:52:36.000Z`（API）| `2026-08-05 09:52:36` |

不會顯示 `01:52:36`（未轉換），也不會顯示 `17:52:36`（雙重轉換）。

## 5. Today Boundary Bug

**修正前**（`utils/geoVisitLog.js` 的 `resolveTimeRangeSince('today')`）：

```js
return `${nowDate.toISOString().slice(0, 10)} 00:00:00`;
```

`toISOString().slice(0,10)` 取出的是**UTC 日曆日**，不是 Asia/Taipei 日曆日。
在 UTC 08:00–16:00 這段時間（對應台灣午夜到早上 8 點），UTC 日曆日仍是前一天，
會導致「今天」篩選少算或多算一整天的資料。

**修正後：**

```js
return getTaipeiDayUtcRange(nowDate).startUtc;
```

`getTaipeiDayUtcRange()`（`utils/dateTime.js`）用 `Intl.DateTimeFormat` 先取出
輸入時間對應的 Asia/Taipei 日曆日（年/月/日），再用固定 UTC+8 offset
（台灣不實行夏令時間，全年固定）換算回 UTC 邊界：

```
2026-08-05 00:00:00 Asia/Taipei → 2026-08-04T16:00:00.000Z （startUtc，inclusive）
2026-08-06 00:00:00 Asia/Taipei → 2026-08-05T16:00:00.000Z （endUtcExclusive）
```

查詢邊界規則固定為 `event_time >= start`（inclusive）且理論上
`event_time < end`（exclusive）——`resolveTimeRangeSince()` 目前只回傳
下限 `since`（沒有上限，因為既有查詢慣例是「since 到現在」，本輪未新增上限
參數），但下限的計算已改為台灣日曆日換算，不再是 UTC 日曆日。

`'24h'`／`'7d'`／`'30d'` 等 rolling window 分支完全未受影響，仍是精確的
「now - N 小時/天」計算，不會被台灣今日邏輯覆蓋（見 A1.2.1 Smoke C 類別）。

## 6. 未受影響範圍

本輪**明確不修改**：

- `routes/orders.js` 的訂單日期篩選（`buildDateWhere()` 仍用既有
  `Date.now() + 8*60*60*1000` 寫法，屬於 pre-existing、out-of-scope）
- GA4 Realtime 自己的時間窗 Contract（`utils/ga4Realtime/*`）
- 金流／付款相關時間欄位
- 其他非 Geo 模組既有時區邏輯（`utils/dashboardAnalytics.js`、
  `utils/visitorAudience.js`、`utils/analyticsHealth.js` 等既有的
  `replace(' ','T')+'Z'` UTC 解析慣例，本輪確認其做法與新 Helper 一致，
  沒有衝突，故不強制重構）
- DB 歷史資料（沒有任何 migration 批次修改時間欄位）
- G1.6-A2（IP Geo Attribution／自動地區回填）——完全未開始

## 7. Root Cause（正式結論）

**DB 儲存的 UTC 時間本身是正確的。**

問題出在讀取／顯示層，共兩個獨立根因：

1. **API 缺少標準 UTC ISO 欄位**：`getRecentGeoVisits()` 只回傳無時區標記的
   原始字串，前端無法安全判斷它是 UTC 還是 Local。
2. **前端直接顯示無 Offset 的字串**：`geo-visitor-layer.js` 沒有做任何時區
   轉換，直接把 DB 原始字串塞進 DOM。
3.（附帶發現）**Today 查詢誤用 UTC 日曆日**：`resolveTimeRangeSince('today')`
   用 `toISOString().slice(0,10)`，在 UTC 08:00–16:00 這段時間會誤判「今天」
   的範圍。

三者都已修正，且修正方式集中在單一 Helper（`utils/dateTime.js` /
`public/js/date-time-format.js`），沒有在各處分散手動加 8 小時。
