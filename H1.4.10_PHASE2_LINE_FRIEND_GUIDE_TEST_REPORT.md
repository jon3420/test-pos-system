# H1.4.10 Phase 2 — friend_entry／friend_checkout Test Report

## 一、Phase 2 專用回歸測試（fresh-run）

```
node scripts/run-h1-4-10-phase2-line-friend-guide-runtime.js
TOTAL=83 PASS=83 FAIL=0
```

- Part A（前端純模組，~40 個斷言）：friend_entry 顯示/略過/不呼叫 login/不阻擋、friend_checkout 顯示時機、guide view 恰好一次、加好友連結開啟（LIFF Client 用 `openWindow`／外部瀏覽器用 `window.open`）、加好友不寫 `is_friend`、缺 URL fallback、同 session 去重（entry/checkout 各自獨立）、已知好友三態判斷、外部瀏覽器/Facebook 情境不呼叫 `liff.login()`、metadata 白名單。
- Part B（真實 HTTP，`routes/settings.js`）：`friend_entry`/`friend_checkout` 可儲存且不強制 `liff_id`/`login_channel_id`；既有 `checkout`/`entry` 驗證規則不受影響（缺欄位仍拒絕）；不合法 `gate_mode` 仍拒絕。
- Part C（靜態檢查）：兩頁共用同一套邏輯；`friend_checkout` 呼叫點確認在 `checkout_click` 送出程式碼「之後」；`friend_entry` 呼叫點確認不在 `openCheckoutStep()` 內；`GA4_EVENT_MAP`/`META_EVENT_MAP` 未含任何 `line_friend_*` 鍵；`checkout_click` 映射契約未變；未 hardcode 加好友網址。
- Part D（Admin Settings UI 靜態檢查）：下拉選單 5 個 value/label 正確；friend mode 說明存在且預設隱藏；無「必填」錯誤文字誤植；`require_friend` 語意區分說明存在；既有 `checkout`/`entry` 選項仍存在；新增 id 皆唯一；`updateLineMemberGateModeUI()` 存在且被正確呼叫。
- Part E（Multi-store 隔離）：store A/B 的 guide-seen 狀態互不影響；sessionStorage key 格式為 `line_friend_guide_seen_{store_id}_{mode}`，不含 LINE UID。

### 對應需求文件十一逐項狀態

| # | 項目 | 狀態 |
|---|---|---|
| 1 | friend_entry 進站顯示 guide | PASS（Part A T1） |
| 2 | 可略過 | PASS（T2） |
| 3 | 不呼叫 liff.login | PASS（T3） |
| 4 | 不呼叫 requestFriendship | PASS（結構性：`openFriendGuideLink()` 原始碼內完全沒有 `requestFriendship` 呼叫，見 Implementation Report 第五節） |
| 5 | 不阻止頁面使用 | PASS（T4：函式同步返回，非 Promise，不會被誤 await） |
| 6 | session 內只顯示一次 | PASS（T13） |
| 7 | friend_checkout 進站時不顯示 | PASS（T5） |
| 8 | 非 goCheckoutBtn click 不顯示 | PASS（Part C：呼叫點位於 `openCheckoutStep()` 內、`checkout_click` 送出之後，該函式本身的 explicit-button-only 判斷是 H1.4.9 frozen 部分，不受本輪影響） |
| 9 | 真實 goCheckoutBtn click 顯示 | PASS（T6/T8） |
| 10 | checkout_click 恰好一次 | PASS（H1.4.9 Gate 3 仍 67/67，且 Part C 確認 Guide 呼叫點不影響其送出邏輯） |
| 11 | Friend Guide view 恰好一次 | PASS（T8） |
| 12 | continue checkout 正常 | PASS（T9） |
| 13 | 再次返回 cart → checkout 不再重複 guide | PASS（T14，同一組 T6-T8 測試內含） |
| 14 | 不產生 internal begin_checkout | PASS（H1.4.9 frozen contract 未修改；Part C 確認 GA4/Meta 映射表未變） |
| 15 | 使用既有 add friend URL | PASS（T10） |
| 16 | 點擊只送 line_friend_link_clicked | PASS（T10） |
| 17 | 不直接改 is_friend=true | PASS（T11） |
| 18 | 缺 URL 仍可正常 checkout | PASS（T12） |
| 19 | friend=true short-circuit | PASS（T15） |
| 20 | friend=false 顯示 | PASS（T16） |
| 21 | unknown 顯示 | PASS（T17） |
| 22 | view 正確 | PASS |
| 23 | click 正確 | PASS |
| 24 | skipped 正確 | PASS |
| 25 | metadata 無 PII | PASS（T23） |
| 26 | 不包含 Token/UID/地址/電話 | PASS（T23，全部白名單欄位皆為布林/enum，不可能夾帶自由格式 PII） |
| 27 | line_friend_* 不映射 Meta InitiateCheckout | PASS（Part C T25） |
| 28 | line_friend_* 不映射 GA4 begin_checkout | PASS（Part C T24） |
| 29 | checkout_click 仍唯一映射 GA4/Meta | PASS（Part C） |
| 30 | friend_entry 可儲存 | PASS（Part B） |
| 31 | friend_checkout 可儲存 | PASS（Part B） |
| 32 | friend mode 無 LIFF ID 可儲存 | PASS（Part B） |
| 33 | friend mode 無 Login Channel ID 可儲存 | PASS（Part B，同一請求已驗證） |
| 34 | checkout/entry 缺必要設定仍照舊拒絕 | PASS（Part B） |
| 35 | invalid gate_mode 仍拒絕 | PASS（Part B） |
| 36 | store A/B guide-seen 互不影響 | PASS（Part E） |
| 37 | session key 不含 LINE UID | PASS（Part E） |

**line-order 與 line-shipping 都有實際 coverage**：Part A 的 `loadGateModule()` 直接測試共用模組本身（兩頁共用同一套邏輯），Part C 對兩個 HTML 檔案分別做靜態整合檢查，確認呼叫點與時序在兩頁都正確。

## 二、三層 Regression Gate（fresh-run）

```
Gate 1（Phase 2）：TOTAL=83 PASS=83 FAIL=0
Gate 2（Phase 1）：TOTAL=68 PASS=68 FAIL=0
Gate 3（H1.4.9） ：TOTAL=67 PASS=67 FAIL=0
```

三層皆為本次回覆內重新 fresh-run 的結果，非沿用舊紀錄。

## 三、相關既有回歸套件（fresh-run）

| Script | 本輪結果 | 與 Phase 1 階段記錄比對 |
|---|---|---|
| `smoke-hotfix25.js` | PASS=26 FAIL=0 MANUAL=3 | 相同 |
| `smoke-hotfix26-a.js` | PASS=24 FAIL=0 MANUAL=1 | 相同 |
| `smoke-hotfix26-b.js` | PASS=30 FAIL=0 MANUAL=4 | 相同 |
| `smoke-hotfix26-c.js` | PASS=23 FAIL=1 MANUAL=2 | 相同（pre-existing） |
| `smoke-hotfix26-d.js` | PASS=20 FAIL=0 MANUAL=3 | 相同 |
| `smoke-hotfix26-e.js` | PASS=36 FAIL=1 MANUAL=3 | 相同（pre-existing，cascading） |
| `smoke-hotfix26-f1.js` | PASS=11 FAIL=0 MANUAL=3 | 相同 |
| `smoke-hotfix26-f2.js` | PASS=22 FAIL=3 MANUAL=3 | 相同（pre-existing，`/timeslots` 日期判斷，與本輪無關） |
| `smoke-hotfix26-f3.js` | PASS=35 FAIL=0 MANUAL=3 | 相同 |
| `smoke-hotfix26-f8.js` | PASS=21 FAIL=0 MANUAL=3 | 相同 |
| `smoke-hotfix26-verify-debug.js` | PASS=36 FAIL=1 MANUAL=3 | 相同（pre-existing，DB 種子資料） |
| `smoke-hotfix26-verify-deep.js` | FAIL=2（cascading，計數方式本身在多次執行間有微幅浮動，34~36 PASS 之間） | 根因與 Phase 1 記錄相同（cascading from `-c`／`-verify-debug`），已用未修改的原始 zip 重新展開獨立執行二次確認，同樣的兩個根因、同樣的錯誤訊息 |

**沒有新增任何一項本輪造成的 FAIL。**

## 四、Baseline Pre-existing Failures（沿用 Phase 1 已確認清單，本輪未變動）

| Script | 失敗項目 | 根因 |
|---|---|---|
| `smoke-hotfix26-c.js` | `renderFriendStatus(null)` 文字判斷 | 既有函式回傳文字與測試期望字串不一致，與本輪無關 |
| `smoke-hotfix26-e.js` / `verify-debug.js` / `verify-deep.js` | Cascading + 種子資料 `line_member_login_channel_id` 比對 | 既有測試環境相依問題 |
| `smoke-hotfix26-f2.js` | `/timeslots` 截止判斷未限定於今天 | 既有預購時段判斷邏輯，與 LINE Member 無關 |

本輪未修改上述任何一個既有問題（依指示不順手修）。
