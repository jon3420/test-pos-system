# H1.4.10 Phase 4C — RECOVERY N8N ORCHESTRATION Test Report

**狀態：CODE COMPLETE ｜ AUTOMATED ACCEPTANCE PASS ｜ MANUAL N8N QA PENDING**

## 九層 Fresh Regression Gate（fresh-run）

```
Gate 1 Phase 4C                  : TOTAL=152 PASS=152 FAIL=0
Gate 2 Phase 4B Full Suite       : TOTAL=168 PASS=168 FAIL=0
Gate 3 Backend Recovery Targeted : TOTAL=92  PASS=92  FAIL=0
Gate 4 Frontend Recovery Targeted: TOTAL=50  PASS=50  FAIL=0
Gate 5 Phase 3                   : TOTAL=111 PASS=111 FAIL=0
Gate 6 Phase 4A                  : TOTAL=89  PASS=89  FAIL=0
Gate 7 Phase 2                   : TOTAL=83  PASS=83  FAIL=0
Gate 8 Phase 1                   : TOTAL=68  PASS=68  FAIL=0
Gate 9 H1.4.9                    : TOTAL=67  PASS=67  FAIL=0
```

Phase 4C 達成 152/152（本輪 Secret Security Closeout 後從 123 擴充），超過正式 acceptance 門檻（≥135），全部是真正 feature-level 斷言，無任何 `|| true` 或字串搜尋式 placeholder（本輪明確做了一次自我掃描，`Self-check` 測項本身就是這個保證）。

## Shared Secret Security（本輪擴充，123→152，+29 assertions）

- **純函式（`validateSharedSecret()`）**：`123456`／`password`／`'a'.repeat(43)`／`'b'.repeat(43)`／兩字元循環／長度 42／長度 44／含 `+`／含 `/` 全部驗證為 invalid；兩次獨立 `crypto.randomBytes(32)` 產生的真實值都驗證為 valid 且彼此不同（非單一樣本巧合）。
- **真實 HTTP（`PUT /api/settings`）**：同一組攻擊樣式全部真實送出，全部 400，且確認被拒絕的值完全沒有寫進 DB；真正高熵值 200 成功寫入。
- **`/secret/rotate` 自我一致性**：連續 5 次 rotate，每次都自我驗證通過 `validateSharedSecret()`，且 5 個值彼此互不相同。
- **GET redaction**：`GET /api/settings` 真實回應確認不含明文 `cart_recovery_n8n_secret` 欄位，只有 `cart_recovery_n8n_secret_set` 布林值。

## 第一輪執行找到的真實 Bug（本輪修正過程）

1. **Router factory export bug**：`routes/cart-recovery-orchestration.js` 結尾誤寫 `module.exports = router;`（在已經是 factory function 內部再賦值，等於 no-op），導致 `require(...)(requireStore)` 恆回傳 `undefined`，`app.use()` 掛載直接 crash。用 `node -e` 直接診斷（非猜測）確認後修正為 `return router;`。
2. **Wake-Up 2xx-only 語意缺失**：原本任何 HTTP response（含 4xx/5xx）都被當成 `sent:true`，已修正為只有 2xx 才算成功。
3. **Outbound Dedupe 會永久吃掉 Retry**：原本 `record→fetch` 順序讓一次暫時性失敗永久阻擋同 `due_at` 的後續嘗試，已改用專屬的 `claimOutboundWakeUp`/`releaseOutboundWakeUpAsFailed` 三態機制。
4. **`_safe(async()=>{})` 無法真正 catch async 例外**：已改成真正的 `async`/`try`/`catch`。
5. **Batch 全域排序遺漏**：三個 stage 各自排序後直接串接，`limit` 前沒有跨 stage 重新排序，可能讓真正最早到期的 job 被誤切掉。
6. **`has_more` 用 `processed>=limit` 猜測**：已改成直接反映真正候選總數。
7. **Workflow Immediate ACK 順序錯誤**：`Wait` 在 `Respond` 之前，導致 POS 端 8 秒 timeout 必然在任何有意義延遲下逾時。
8. **Workflow Loop Context 遺失**：HTTP Request 節點輸出直接覆蓋掉 `store_id`/`stage`/`_loopCount`，已用 Merge 節點修正。
9. **IPv6 URL hostname 判斷失效**：Node `URL.hostname` 對 IPv6 literal 保留中括號（`[::1]`），原本的正則沒有處理，已修正。

九個都是本輪測試撰寫過程中真正發現、真正修的 production/workflow 問題，不是為了湊斷言數字而製造的。

## Phase 4C Test Coverage（123 assertions，依主題摘要）

### Router Mount（3）
factory export 型別、缺 `requireStore` 明確 throw、正確 factory 回傳真正可 `.use()` 的 router。

### Settings + Non-Interference（N1-N4，含 sha256 hash 比對）
三個新 key 的預設值；真正的 non-interference 測試（seed 既有 `n8n_webhook_url`，跑過完整 Phase 4C 流程後，值與檔案 hash 皆 byte-for-byte 不變）。

### HMAC（N30-N38，純函式）
簽章 deterministic、不同輸入產生不同簽章、canonical string 不含 secret、正確/錯誤簽章驗證、timestamp window（過期/未來太遠皆拒絕）、`timingSafeEqual` 使用確認。

### SSRF（hostname literal + DNS resolution，含 IPv4/IPv6）
localhost/127.x/10.x/192.168.x/172.16-31.x/169.254.x（含 cloud metadata）、IPv6 `::1`/`fc00::/7`/`fe80::/10` 全部拒絕；DNS 解析（stub，非真實網路）驗證「公開 hostname 但 DNS 指向私有 IP」被拒絕、指向公開 IP 才放行。

### Replay Protection（N39-N43，純函式 + 真實 HTTP）
first request 接受、同 request_id 二次呼叫偵測為 replay、不同 request_id 各自獨立、跨店 request_id 不互相阻擋；真實 HTTP `/process-due` 驗證 replay 回應正確（`replay:true`，不重複執行）。

### `/process-due` 真實 HTTP（N44-N53，含本輪收緊的 strict HMAC）
合法請求成功呼叫 `processDueLineRecoveryJobs`、response 不含任何 PII/job 細節；n8n 停用時安全 no-op；**header/body timestamp 不一致時拒絕**（本輪新增的 strict equality 檢查）；錯誤簽章/過期 timestamp 皆 401，拒絕理由統一不洩漏細節；不存在的 `store_id` 簽章即使正確也 401，且不建立任何資料列。

### Wake-Up 200/500/Retry/Timeout/Dedupe（N-wake-200, W1-W5）
HTTP 200 → `sent:true`；HTTP 500 → `sent:false, reason:http_error`，audit 記 `failed`；**同 `due_at` 因暫時性失敗後允許 retry**，mock 改回成功後真的重新呼叫一次 fetch；已 `processed` 的 `due_at` 再次呼叫 → 0 新 HTTP call（dedupe 生效）；`network_error` 不影響 `cart_recovery_jobs.status`（仍是 `pending`，不是 `failed`）。

### Timezone Contract（T1-T5）
確認 DB `due_at` 無 timezone 標記（純文字格式）；`toOrchestrationDueAt()` 輸出帶 `Z`；轉換前後 epoch 完全相同（無偏移，特別驗證不會有常見的 UTC±8 錯誤）；HMAC canonical 用的 `dueAt` 與實際送出的 `body.due_at` 完全是同一個值（不是簽一個值卻傳另一個）。

### Workflow Static Topology（Workflow-0, WF1-WF13，真正解析 connections）
不是字串搜尋——用 `workflow.connections` 真正解析節點間的拓撲關係：Webhook→Verify→[If Valid]→Respond→Wait→Build Due Request→...→Merge→If has_more（loop back 或安全結束）；Merge 節點程式碼確認真的從 context 取回 `store_id`（不被 HTTP 回應覆蓋）、`_loopCount` 真的累加（不是每圈重置）；has_more 判斷讀 `processor_result.has_more`（合併後的明確欄位）且同時檢查 `_loopCount<10`；每圈用 `crypto.randomUUID()` 產生全新 `request_id`；靜態安全掃描（無 PII、secret 只走 `process.env`、無 hardcoded secret、無 LINE Push/DB 節點）——這些掃描明確排除了 `meta.description` 說明文字本身（避免說明文字裡提到「不得包含姓名/電話/地址」被誤判成違規）。

### Settings HTTP（S1-S5, GET-redact）
真實 `PUT`/`GET /api/settings`：不相關設定儲存不影響已存 secret；空字串 secret 不清空；合法新 secret（≥32 字元）成功更新；弱 secret（如 `"123456"`）400 拒絕且不影響原有值；GET 真實回應確認不含明文，只有 `cart_recovery_n8n_secret_set` 布林值。

### Rotate（ROT1-ROT6）
未經 `requireStore` 明確拒絕；合法 Admin 呼叫成功；新 secret ≥32 bytes 等價強度；DB 真的換新值；**舊 secret 簽的 due signature 在 rotate 後立即失效**；新 secret 簽的 due signature 成功。

### Refresh Race（RACE1-5）
舊排程時間點（T1）查詢查不到已 refresh 到 T2 的 job；真正到期（T2）才處理；全程只有 1 筆 job、只送出 1 次提醒（不因 refresh 產生重複 job 或重複發送）。

### Conversion / Consent / Friendship Race（CV1-8, CR1-6, FR1-5）
購買提前發生 → job `converted`，n8n 稍後呼叫 `process-due` 時 `sent=0`，`converted` 狀態維持，Recovery token 已失效；consent 提前被撤回 → `sent=0`，job 狀態正確反映為 `not_contactable`；好友狀態提前變為非好友 → `sent=0`。

### Batch 49/50/51（B1-B5）
49 筆剛好在 limit 內 → `has_more=false`；剛好等於 limit（50）→ `has_more=false`；超過 limit（51）→ 只處理 50、`has_more=true`；第二次呼叫正確處理剩餘 1 筆（需要 job 真正被移出待處理佇列，本輪測試已修正為完整開啟 LINE recovery 讓 job 走到終態，不是卡在 store 層級開關而永遠重複出現）；100 筆未到期 + 1 筆到期 → 到期的 1 筆仍被正確選出（不被大量未到期的 job 餓死）。

## Related Smoke Regression（實際執行，fresh-run）

| Script | 結果 |
|---|---|
| `run-h1-4-7-analytics-contract-runtime.js` | 61/61 |
| `run-g1-6-ga4-h1-4-7-two-stage-checkout-runtime.js` | 113/113 |
| `smoke-hotfix29-c.js` | 54/54，MANUAL=2（既有） |
| `smoke-hotfix30-b5-r5-2-a-order-geo-write.js` | 90/90 |
| `smoke-hotfix30-b5-r5-debounce.js` | 32/32 |
| `smoke-hotfix30-c1-rollback.js` | 22/22 |

## Baseline Known Failure（誠實記錄，非本輪 regression）

`smoke-hotfix30-b5-r5-2-a-integrated.js` 的 **M3**：`Cannot read properties of undefined (reading 'district_ranking')`，`utils/cartSnapshot.js`／`utils/cartGeoAttribution.js` 既有 bug，錯誤訊息與行號與歷次 Phase 記錄完全相同，與 n8n orchestration 程式碼路徑無關，Phase 4C 未觸碰。
