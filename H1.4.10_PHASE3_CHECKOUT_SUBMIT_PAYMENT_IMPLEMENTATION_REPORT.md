# H1.4.10 Phase 3 — CHECKOUT-SUBMIT-DIAGNOSTICS + PAYMENT-SUCCESS Implementation Report

## Modified Production Files

1. `public/line-order.html`
2. `public/line-shipping.html`
3. `routes/analytics.js`
4. `routes/linepay.js`
5. `utils/analyticsLog.js`

## New Test File

6. `scripts/run-h1-4-10-phase3-checkout-submit-payment-runtime.js`

共 5 個 production 修改檔案 + 1 個新增 test runner（實際工作目錄確認結果）。

---

## checkout_submit_click

- `line-order.html`：`#subBtn`／`line-shipping.html`：`#submitBtn`，皆改為 `onclick="submitOrder(event)"`。
- `submitOrder(event)` 開頭計算 `_isRealSubmitAttempt = !!(event && event.type==='click' && _submitBtnEl && event.currentTarget===_submitBtnEl)`，只有為 true 才送出 `checkout_submit_click`。
- 不做 `checkout_click` 那種永久 `cart_id` 去重——每次真實點擊都各自送出一筆，用來分析真實 retry 次數。
- metadata：`checkout_stage`／`submit_trigger`／`order_mode`／`item_count`／`cart_value`（皆為既有安全 cart analytics payload 的欄位，非新建資料來源）。

## checkout_validation_failed

- 沿用原本每一支 `if(!欄位){toast(...);return;}` 的既有驗證順序、既有 toast 文案，只在每個既有分支「新增一行」`trackCheckoutValidationFailed(reasonCode)` 呼叫，**沒有**重寫或新建第二套 validation engine。
- `trackCheckoutValidationFailed()` 內部用 `_validationFailedSent` 旗標保證每次 submit attempt 最多送出一筆（第一次呼叫後即鎖住，後續呼叫直接 no-op）。
- metadata 白名單：`reason_code`／`order_mode`／`checkout_stage`／`has_cart`／`item_count`／`payment_method_type`（皆在 `routes/analytics.js` 的 `sanitizeCheckoutSubmitMetadata()` 強制過濾，非白名單欄位一律捨棄）。
- 不含 PII：已用 jsdom 測試注入真實姓名/電話/地址/假 LINE UID/假 Token，確認 metadata 皆不含。

## payment_success

- `utils/analyticsLog.js` 的 `EVENT_WHITELIST` 新增 `payment_success`；`routes/analytics.js` 的 `SERVER_ONLY_EVENTS` 新增 `payment_success`（client `POST /api/analytics/events` 直接拒絕）。
- 唯一寫入點：`routes/linepay.js` 的 `GET /confirm`，`data.returnCode==='0000'` 分支內，緊接在既有 `purchase` 事件寫入之後。
- `value` 使用 DB `order.total`（後端權威金額），不採信任何前端/query/body 傳入值。
- `logServerEvent()` 內部把 `payment_success` 納入既有 `hasEventForOrder()` 查重（與 `submit_order`／`purchase` 同一套機制），達成 idempotent。

## Security

以下欄位皆確認不會出現在 `checkout_submit_click`／`checkout_validation_failed`／`payment_success` 的 metadata 中：姓名、電話、地址、經緯度座標、LINE UID、`member_session`、Token、優惠券代碼、備註（note）、LINE Pay channel secret / transaction 簽章相關資料。`payment_success` metadata 只允許 `order_number`／`payment_provider`／`payment_method`／`currency`／`value` 五個欄位，皆由後端自行組成，不採信任何前端輸入。

## Compatibility

- H1.4.9 `checkout_click` canonical contract（`view_cart`／`checkout_click`／explicit-button-only／`#goCheckoutBtn`／`openCheckoutStep(event)`／`_enterCheckoutStage()`／order summary／CTA 金額同步／GA4 `begin_checkout`／Meta `InitiateCheckout`）**未變**，`67/67 PASS`。
- Phase 1 LIFF Auto Identify（`member_session`／`link-context`／`skipLoginCallback`／匿名外部瀏覽器）**未變**，`68/68 PASS`。
- Phase 2 `friend_entry`／`friend_checkout`／Friend Guide session dedupe（**未呼叫** `liff.login()`／不阻止下單）／`line_friend_*` 事件 **未變**，`83/83 PASS`。
