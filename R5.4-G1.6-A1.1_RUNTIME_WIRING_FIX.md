# R5.4-G1.6-A1.1 — Runtime Wiring Fix Report

fix18-10-hotfix30-B5-R5.4-G1.6-A1.1 — Geo Marker Runtime Wiring & Safe
Centroid Source Closure

## 1. Script Wiring

`public/index.html` 新增：

```html
<script src="/js/geo-marker-renderer.js?v=fix18-10-hotfix30-B5-R5-4-G1-6-A1-1"></script>
```

位置：`geo-intelligence-map.js` 之後、`geo-map-settings.js`／
`geo-heatmap.js`／`geo-visitor-layer.js`／`geo-live-layer.js` 之前。只出現
一次，cache-buster 為新版本字串，無舊版本引用殘留。

## 2. Browser Namespace

`geo-marker-renderer.js` 新增：

```js
if (typeof window !== 'undefined') {
  window.GeoMarkerRenderer = {
    ACCURACY_STATES, FORBIDDEN_CENTROID_WORDS, isValidAccuracy,
    buildPoints, buildTooltip, escapeHtml, iconClassFor, defaultIcon,
    buildLegendHtml, buildBlockedNoticeHtml, renderGroup, clearGroup,
  };
}
```

Node `module.exports` 維持不變（供既有測試繼續 `require()`）。

## 3. Visitor Caller

`geoVisitorFetchAndRender()`（Visitor Layer 唯一的 fetch-and-render
pipeline，activate／refresh／metric switch／range switch 都會呼叫到）在
Choropleth 渲染之後新增：

```js
geoVisitorRenderMarkers(window.geoMapState.instance,
  geoVisitorBuildMarkerPoints(geoVisitorState.areas));
```

新增純函式 `geoVisitorBuildMarkerPoints(areas)`：只把「已知」
（`!is_unknown`）的行政區聚合列轉成 Marker 點，Unknown 一律不嘗試查
centroid（不建立任何 Estimate Model）。`_geoVisitorResolveAreaCentroid()`
是唯一的 centroid 查詢入口，目前因為 `AUTHORITATIVE_CENTROID_SOURCE =
null` 明確 Blocked，一律回傳 `null`。

## 4. Dashboard Caller

`refresh()`（`markers`／`cluster` 模式）在既有 `_renderMarkers(points,
...)` 之後新增：

```js
renderEstimateMarkers(deriveEstimateMarkerPoints(points));
```

新增純函式 `deriveEstimateMarkerPoints(points)`：跳過已有真實座標的點
（避免 Exact／Estimate 重複畫同一筆），對沒有座標的點呼叫
`_resolvePointCentroid()`——同樣因為
`DASHBOARD_AUTHORITATIVE_CENTROID_SOURCE = null` 明確 Blocked，一律回傳
`null`，因此目前 `deriveEstimateMarkerPoints()` 對任何真實 API 回應都會
回傳空陣列（見 Reality Audit 第 9 節：後端本身也已經濾掉了 region-only
的列）。

## 5. Legend

新增 `geoMarkerBuildLegendHtml()`／`geoMarkerBuildBlockedNoticeHtml()`
（`geo-marker-renderer.js`），並接上真實 Runtime：

- **Visitor Layer**：`geoVisitorRenderCoverageDom()`（每次真實資料
  render 都會呼叫，跟 Coverage 卡片同一個 DOM 元素、同一次渲染）在既有
  Coverage 文字之後附加 Legend HTML；若「有已知 county／district 統計但
  目前沒有任何 Marker 被畫出來」，額外附加 Blocked 說明。
- **Dashboard**：`attachToMap(mapInstance)` 透過 Leaflet 既有的
  `mapInstance.getContainer()` 找到地圖容器 DOM 元素，若
  `#geo-live-marker-legend` 容器不存在就動態建立為地圖容器的下一個
  sibling；`renderEstimateMarkers()` 每次真正執行（即每次 `refresh()`
  成功）都會把 Legend／Blocked 說明寫進這個容器。
- **GA4 Choropleth Layer**：`geo-ga4-realtime-layer.js` 完全沒有引用
  `window.GeoMarkerRenderer`，繼續使用自己既有的縣市色階說明，不共用這個
  Marker Legend。
- **Order Heatmap**：`geo-heatmap.js` 完全未修改，既有 Legend／Circle／
  Marker 控制不受影響。
- Legend 所有文字都是固定常數，但仍走 `geoMarkerEscapeHtml()` 一次
  （防禦性一致性，不依賴「反正是常數就不用 escape」的假設）。

## 6. XSS

`geoMarkerEscapeHtml(value)` 新增於 `geo-marker-renderer.js`，處理 `&`／
`<`／`>`／`"`／`'` 五個字元。`geoMarkerBuildTooltip()`／
`geoMarkerBuildLegendHtml()` 的所有動態值（`label`／`count`／`tooltip`）
一律先經過這個函式，不再要求呼叫端自己 escape。

## 7. Visual Classes

`geoMarkerIconClassFor(accuracy)` 產生
`geo-marker-accuracy-{exact|district_centroid|county_centroid}` 三種互斥
class；`geoMarkerDefaultIcon(point)` 用 `L.divIcon()` 掛上對應 class 與
符號（●／◌／▢）。新增 `public/css/geo-marker-renderer.css`
（已在 HTML 引入，cache-buster 與 JS 一致）：

- `district_centroid`：虛線外框、圓形、透明度 0.75。
- `county_centroid`：虛線外框、方形（`border-radius: 3px`）、透明度 0.5
  （比 district 更低）。
- `unknown`：完全沒有對應 CSS class（因為根本不會建立 Marker）。

## 8. Cleanup

- `_clearActiveLayers()`（Dashboard mode switch／destroy 都會呼叫）新增
  呼叫 `clearEstimateMarkers()`。
- `destroy()` 同時呼叫 `_clearActiveLayers()` 與
  `clearEstimateMarkers()`（後者其實已經被前者間接呼叫，這裡保留顯式呼叫
  只是為了程式碼可讀性上更清楚兩者都會被清）。
- `geoVisitorHandleStoreSwitch()`／`_geoVisitorResetStateForTest()`
  新增清空 `geoVisitorState.markerLayerGroup`。
- Marker Group 沿用單一 group、`clearLayers()` 後重繪的既有慣例（跟
  Choropleth／既有 Marker Cluster 同一套模式），不是每次都
  `addTo()`／建立新 group（`geoMarkerRenderGroup()` 內部判斷
  `existingGroup` 是否已存在）。

## 9. Exact Marker 支援

完全未修改既有 `_renderMarkers()`（Dashboard 真實座標 Marker）／
`resolveMarkerStage()`／`markerColorForStage()`／
`buildMarkerTooltipFields()`。`deriveEstimateMarkerPoints()` 明確跳過
已有 `lat`／`lng` 的點，避免同一筆資料被畫成 Exact＋Estimate 兩個 Marker。

## 10. Estimate Blocker

見 Reality Audit 第 6-10 節。摘要：

- **Blocker A（Centroid Source）**：Repo 內沒有任何 Authoritative
  Centroid Source（矩形 fixture GeoJSON、無座標的行政區代碼對照表）。
- **Blocker B（Backend Payload）**：`/api/geo-live/markers` 目前只回傳
  已有真實座標的列，region-only 的列在後端就被濾掉，不在本輪修改範圍。

兩個 Blocker 皆已誠實記錄，且降級行為安全（不報錯、不中斷 Summary／
Ranking／Order Heatmap、不使用任何假座標）。

## 11. Smoke

`scripts/smoke-hotfix30-b5-r5-4-g1-6-a1-1-runtime-wiring.js`：目標
≥150 PASS，見 Test Report 記錄實際數字與三輪一致性結果。

## 12. Static Audit

`scripts/static-audit-g1-6-a1-1.js`：目標 ≥80 PASS，見 Test Report。

## 13. Regression

見 Test Report 完整三輪結果表。

## 14. 後續需求

在允許進入 G1.6-A2（Visitor Geo Attribution Pipeline）之前，需要先解決：

1. 取得或建立經審核的 Authoritative County／District Centroid Catalog
   （官方 GeoJSON 算出的真實 centroid，或有審核紀錄的對照表）。
2. 後端 `/api/geo-live/markers`（或新增等效 API）需要決定是否要、以及
   如何安全地回傳「只有 region、沒有真實座標」的列，供 Dashboard Estimate
   Marker 使用（這牽涉到資料表 Schema／查詢邏輯變更，屬於後續資料階段的
   工作，不屬於本輪前端 Renderer Closure 範圍）。
3. 只有具備上述兩項之一（或兩者）之後，`district_centroid`／
   `county_centroid` 才可能在正式環境真的畫出 Marker——目前的 Runtime
   Wiring 已經就緒，屆時不需要再修改 `geoVisitorRenderMarkers()`／
   `renderEstimateMarkers()`／`geoMarkerRenderGroup()` 任何一行，只需要把
   資料源接進 `_geoVisitorResolveAreaCentroid()`／
   `_resolvePointCentroid()` 這兩個明確的擴充點。

---

*本文件不含任何真實憑證、Property、Stream 或 Token 內容。*
