# R5.3-A1 — Geo Intelligence Heatmap Foundation — Manual QA Checklist

狀態：R5.3-A1：QA BUILD。Automated Verification：PASS。
Manual Visual：**REQUIRED（本清單尚未執行）**。Final Completion：NOT YET APPROVED。

> ⚠️ 重要前提：本輪 `geo-heatmap.js` 是 Engine 層（helper + Leaflet 繪製 +
> DOM 渲染函式），**尚未接上 Dashboard 頁面的 Heatmap Mode 分頁 UI**（見
> Completion Report 第 13 節「已知限制」最後一項）。以下項目中，凡標示
> 「⏳ 需先完成 UI 接線」的項目，必須等 Heatmap Mode 分頁實際掛上
> `public/index.html`／`geo-intelligence.js` 之後才能測。

---

## 一、資料正確性（可先測，不需 UI 接線）

- [ ] 用真實/測試店家資料呼叫 `/api/analytics/geo/fulfillment`，確認回應內
      多出 `coordinate_count`／`lat`／`lng`／`coordinate_source`／
      `coordinate_confidence` 四個欄位，且數值與資料庫內真實的
      `delivery_lat`/`delivery_lng` 吻合（可用 A1-98~103 的手法自行核對）。
- [ ] 確認完全沒有外送訂單座標的行政區，`lat`/`lng` 是 `null`，不是 `0` 或
      任何看起來像座標的數字。

## 二、Heatmap Mode UI（⏳ 需先完成 UI 接線）

- [ ] Dashboard 可一鍵切換 Visitors / Add to Cart / Checkout / Orders /
      Revenue / Conversion / **Heatmap** 七個分頁。
- [ ] Metric／Display（Circle/Marker/Ranking only）／Channel／Heatmap 開關
      控制列排版正常，不會過度複雜。
- [ ] 切換 Metric／Channel／Date range 時：地圖不重建（tile 不閃爍）、Ranking
      與 Summary 同步更新。

## 三、Circle/Marker 呈現（⏳ 需先完成 UI 接線）

- [ ] 只有真的有外送訂單座標的行政區才出現地圖圓點/marker，其餘行政區地圖
      上完全沒有點（不是透明點、不是 0 半徑點）。
- [ ] 圓形大小隨 metric 數值變化，且即使某一區數值遠高於其他區（outlier），
      也不會整張圖被一個圓蓋住（sqrt 正規化效果目視確認）。
- [ ] 顏色四級（綠/黃/橘/紅）在 Dark Theme 背景下清楚可辨識，且有文字
      Legend（不是只靠顏色）。
- [ ] Hover／Tooltip：顯示行政區名稱、六個 metric 數值、Channel、座標來源、
      資料準確度、Coverage，畫面上完全沒有 `undefined`/`null`/`NaN`/
      `Infinity` 字樣。
- [ ] 選中（Selected）狀態的圓點邊框明顯不同於未選中／hover 狀態。

## 四、Ranking 同步（⏳ 需先完成 UI 接線）

- [ ] Ranking 顯示**所有**行政區（含沒有座標的），沒有座標的那些顯示
      「目前尚無可用座標」，不是消失也不是空白。
- [ ] 點 Ranking 項目 → 地圖 `panTo` 到對應熱點（若有座標）／若無座標，
      顯示提示文字，不跳出任何錯誤訊息或 console error。
- [ ] 點地圖熱點 → 對應 Ranking 項目高亮（`aria-selected="true"`）。
- [ ] 再次點擊同一個熱點/排名可取消選取。

## 五、Summary／Coverage Card（⏳ 需先完成 UI 接線）

- [ ] Heatmap 上方 Summary 顯示：熱度最高／最高成交／最高營收／最高轉換／
      有座標行政區數／無座標行政區數，數字與 Ranking 一致。
- [ ] Coverage Card 六張卡片（Visitors/Add to Cart/Checkout/Orders/Revenue/
      Conversion），Visitors/Add to Cart/Checkout 明確顯示「尚未收集訪客
      座標」，Orders/Revenue/Conversion 顯示實際百分比與「依外送訂單位置
      聚合」說明文字。確認這些說明文字清楚易懂，老闆不需要額外解釋就能看懂
      「這份地圖哪些是真的、哪些還沒收集」。

## 六、Store Isolation（⏳ 需先完成 UI 接線）

- [ ] 切換店家時：舊店的熱點、選取狀態、待處理的 API request 全部清除，不會
      短暫看到前一家店的熱點殘留。
- [ ] 切換店家後，地圖與 tile layer 沒有被整個銷毀重建（沒有明顯閃爍/重載）。

## 七、Accessibility

- [ ] Metric／Channel／Display 控制項可用鍵盤（Tab／Enter／Space）操作。
- [ ] Ranking 項目可 Tab 聚焦，Enter/Space 可選取，選中狀態有
      `aria-selected`（自動化測試已驗證 DOM 屬性存在，這裡驗證螢幕閱讀器
      實際朗讀是否合理）。
- [ ] Summary 區塊變動時，螢幕閱讀器會透過 `aria-live="polite"` 播報
      （自動化已驗證屬性存在，這裡驗證實際朗讀時機是否恰當、不會太頻繁）。
- [ ] Legend 有文字說明，不只靠顏色。

## 八、Responsive（⏳ 需先完成 UI 接線）

- [ ] 390px：控制列換行、地圖高度約 320px、Ranking 移到地圖下方、Tooltip
      不超出畫面、無水平 overflow。
- [ ] 768px／1024px／1440px：版面正常，控制列與地圖比例合理。

## 九、既有功能不退化

- [ ] Dashboard 既有 Visitors/Add to Cart/Checkout/Orders/Revenue/Conversion
      六個既有分頁（choropleth 顯示）行為與本輪之前完全一致。
- [ ] Settings Preview 行為與本輪之前完全一致（本輪未修改該檔案）。
- [ ] Geo Settings API（Hotfix 1）與 Dashboard GeoJSON 例外處理（Hotfix 2）
      行為不變。

---

Manual Visual 尚未完成前，狀態維持：

R5.3-A1：QA BUILD
Automated Verification：PASS
Manual Visual：REQUIRED
Final Completion：NOT YET APPROVED
