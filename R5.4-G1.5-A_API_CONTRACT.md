# R5.4-G1.5-A API Contract

fix18-10-hotfix30-B5-R5.4-G1.5-A — GA4 Realtime Backend Contract Snapshot

本文件是 G1.5-B 前端接線唯一依據。**不要**依目前 `public/js/
geo-ga4-realtime-layer.js` 草稿猜測回應形狀，該草稿是對舊（G1.5 草稿）
backend 寫的，已經不相容（見 §5）。

## 1. `GET /api/geo-live/ga4-realtime`

保護層級：`requireStore`（掛載層）+ `requireFeature('reports')` +
`requireGeoAnalyticsEnabled`。

### Query 參數

| 參數 | 允許值 | 預設 | 說明 |
|---|---|---|---|
| `window` | `'5'` \| `'30'` | `'5'` | 不合法值 fallback 為 `'5'`，不是回錯誤 |
| `metric` | `'visitors'`\|`'view_item'`\|`'add_to_cart'`\|`'checkout'`\|`'purchase'` | `'visitors'` | 不合法值 fallback 為 `'visitors'` |
| `refresh` | `'0'` \| `'1'` | `'0'` | `'1'` 時繞過 cache 強制重新抓取 |

**不接受**：`property_id`／`stream_id`／`credentials`／`private_key`（無論
放 query 或 body，一律忽略）。

### 成功 Response（`{success:true, data:{...}}`）

| 欄位 | 型別 | Nullable | 說明 |
|---|---|---|---|
| `source` | string | 否 | 固定 `'ga4_realtime'` |
| `accuracy` | string | 否 | 固定 `'ip_city_county_estimate'` |
| `window_minutes` | number | 否 | `5` 或 `30` |
| `metric` | string | 否 | 見上方允許值 |
| `fetched_at` | string(ISO) | **是**（disabled/not_configured 時為 `null`） | 這批資料真正抓取的時間（stale 時仍是原始抓取時間，不會更新） |
| `cache_age_seconds` | number | **是** | disabled/not_configured 時為 `null` |
| `is_cached` | boolean | 否 | 是否命中 cache（不含首次 fresh fetch） |
| `is_stale` | boolean | 否 | 是否為過期後回退使用的 cache |
| `status` | string | 否 | `'disabled'`\|`'not_configured'`\|`'fresh'`\|`'cached'`\|`'stale_cache'` |
| `quota_status` | string | 否 | `'normal'`\|`'near_limit'`\|`'limited'`\|`'unknown'` |
| `summary.total_active_users_ga4` | number | 否 | **唯一**去重後總活躍使用者來源（Summary Request） |
| `summary.event_count` | number | 否 | |
| `summary.screen_page_views` | number | **是**（非 visitors metric 時為 `null`） | |
| `summary.mapped_counties` | number | 否 | |
| `summary.unmapped_city_rows` | number | 否 | |
| `summary.excluded_non_tw_rows` | number | 否 | |
| `counties` | array | 否（可為空陣列） | 見下方 county row 形狀 |
| `unmapped` | array | 否（可為空陣列） | `[{city, active_users, event_count}]` |
| `notices` | array&lt;string&gt; | 否 | 人類可讀提示文字（含固定 disclaimer） |
| `error_code` | string | **是** | 只有 disabled/not_configured 時非 null |

`counties[]` row 形狀：
```
{ county_code, county_name, active_users, event_count, source:'ga4_city', accuracy:'ip_city_county_estimate' }
```

**關鍵規則（前端不得違反）**：`counties[].active_users` 的加總**不等於**
`summary.total_active_users_ga4`（兩者是不同去重口徑，見架構文件與
Backend Correctness Report §2-3）。前端不得自行加總 `counties` 來顯示
「總訪客」，也不得把這個值跟系統既有 Visitor Layer 的訪客數相加顯示。

### 錯誤 Response（`{success:false, ...}`，非 200 狀態碼時）

```json
{ "success": false, "code": "...", "message": "...", "retryable": true|false, "status": "error" }
```
不含 `stack`／`rawError`／完整 Google Error 物件／任何憑證內容。

### Error Codes（目前已定義／已在程式碼中出現的）

| code | 觸發情境 |
|---|---|
| `ga4_realtime_disabled` | 全域或店家層級未啟用（`status:'disabled'`，不是 HTTP 錯誤，是正常 200 payload） |
| `stream_not_configured` | 非 single-property 店家缺少 streamId（`status:'not_configured'`） |
| `invalid_property` | property id 格式不合法（非純數字） |
| `invalid_stream` | stream id 格式不合法 |
| `missing_property` | 完全沒有設定 property id |
| `SDK_UNAVAILABLE` | `@google-analytics/data` 無法載入 |
| `invalid_window` | window 不是 5 或 30（HTTP 400） |
| `unsupported_metric` | metric 不在支援清單（HTTP 400） |
| `credential_invalid` | Service Account JSON/Base64 parse 失敗 |
| `429`／`500`／`502`／`503`／`504`／`TIMEOUT`／`ETIMEDOUT`／`ECONNRESET` | 可重試的 Google API 錯誤（`retryable:true`） |
| `401`／`403`／`404`／`INVALID_CREDENTIALS`／`INVALID_PROPERTY`／`INVALID_STREAM` | 不可重試的 Google API 錯誤 |
| `GA4_API_ERROR` | 其他未分類 Google API 錯誤 |

**尚未實作**（需求文件列出但本階段程式碼沒有對應分支，G1.5-B 若要用需要
先補上）：`ga4_realtime_not_configured`（目前用 `status:'not_configured'`
+ 各種細分 `error_code` 表達，沒有單一同名 code）、`property_not_configured`
（目前叫 `missing_property`）、`credential_unavailable`、
`permission_denied`（目前落在通用 `403`）、`property_not_found`（目前落在
通用 `404`）、`stream_filter_invalid`（目前沒有這個細分，錯誤的 stream 在
建立 request 前就已經被 `validateGa4StreamId` 擋掉，不會走到 Google
API）、`quota_limited`（目前用 `quota_status:'limited'` 表達，不是
error_code）、`ga4_timeout`（目前叫 `TIMEOUT`）、`ga4_unavailable`（目前
用 `GA4_API_ERROR` 概括）。

## 2. `GET /api/geo-live/ga4-realtime-status`

保護層級：`requireFeature('reports')`。**不呼叫任何 Google API**。

### Response

```
{
  enabled: boolean,
  configured: boolean,
  credential_available: boolean,
  property_configured: boolean,
  stream_configured: boolean,
  single_store_mode: boolean,
  sdk_available: boolean,
  last_success_at: string(ISO) | null,   // 本階段誠實回 null（跨 fetch 保存
                                           // per-store 成功時間戳留給 G1.5-B）
  last_error_code: string | null,
  cache_entries: number,                  // 該 store 目前的 cache entry 數
  in_flight_requests: number,             // 該 store 目前的 in-flight 數
  cache_seconds: number,
}
```

## 3. 尚未實作（G1.5-B 需求文件提到但本階段沒做）

- `live_check=1` 權限保護分支（需要可靠的管理者判斷機制）
- Settings UI 對應的寫入端點（本階段只有讀取 `getGa4RealtimeConfig`，沒有
  `PATCH /api/settings/ga4-realtime` 之類的寫入 route）
- Auto Refresh 秒數依 quota 狀態自動延長的邏輯（`quota_status` 已回傳，但
  前端節流行為本階段未實作）

## 4. 前端草稿差異（Read-only Audit，本階段未修改任何前端檔案）

`public/js/geo-ga4-realtime-layer.js` 目前程式碼（G1.5 草稿）與本文件描述
的新 contract之間的具體差異：

| 項目 | 舊草稿（現況） | 新 Backend Contract |
|---|---|---|
| Endpoint 呼叫 | `fetch('/api/geo-live/ga4-realtime')`（無任何 query 參數） | 需要帶 `?window=5\|30&metric=visitors\|...&refresh=0\|1` |
| 縣市陣列欄位名稱 | `payload.by_county` | `data.counties` |
| 總數欄位位置 | `payload.total_active_users_ga4`（頂層） | `data.summary.total_active_users_ga4`（巢狀在 `summary` 下） |
| county row 欄位 | `{county_code, county_name, active_users, source_cities}` | `{county_code, county_name, active_users, event_count, source, accuracy}`（沒有 `source_cities`，但多了 `event_count`/`source`/`accuracy`） |
| 未對應城市 | 舊欄位 `unmatched_cities` | 新欄位 `unmapped`（欄位名稱不同，且形狀從 `{city, active_users}` 變成 `{city, active_users, event_count}`） |
| disclaimer 位置 | `payload.disclaimer`（單一字串） | `data.notices`（字串陣列，可能包含多則提示） |
| enabled/configured | `payload.enabled` / `payload.configured` | **不存在**於 `GET /ga4-realtime` 的成功 response（已用實測確認，見 `R5.4-G1.5-B1_CONTRACT_CONFORMANCE_AUDIT.md`）。這兩個欄位只出現在 `GET /ga4-realtime-status` 的獨立 response 裡；`/ga4-realtime` 一律用 `status`（`'disabled'`/`'not_configured'`/...）+ `error_code` 表達同樣的意思 |
| Metric/時間切換 | UI 完全不存在 | Route 已支援 `window`/`metric`，前端需要新增對應控制項才能用到 |
| 快取/新鮮度資訊 | 完全沒有 | 新增 `is_cached`/`is_stale`/`cache_age_seconds`/`quota_status`，前端可以（也應該）顯示「N 秒前的快取資料」 |

**結論**：`geo-ga4-realtime-layer.js` 目前的 `geoGa4FetchAndRender()` /
`geoGa4RenderChoropleth()` / `geoGa4MapOverlayMessage()` 三個函式都直接讀
`payload.by_county`／`payload.total_active_users_ga4`，套用新 contract 後
會全部讀到 `undefined`，圖層會呈現「沒有資料」而不是報錯——這是 G1.5-B
最優先要處理的重構項目，本階段依規格未動它。

**（以下為 G1.5-B2 新增章節，原本的 GA4 Realtime Data API Contract 內容
完整保留、未刪除。）**

---

## GA4 Realtime Settings API

以下三個 endpoint 是 G1.5-B2 新增，供 G1.5-B2 前端唯一依據。

### 1. `GET /api/settings/ga4-realtime`

- **Mount／權限**：掛載於 `/api/settings`，套用 `requireStore`（`server.js`）。
  **沒有**額外的 `requireFeature('reports')` 或任何管理員角色檢查——這跟
  同一個檔案裡既有的 `geo-map` GET/PATCH 完全一致（誠實記錄：這個專案的
  Store Settings 端點目前沒有分層角色系統，凡持有該店有效登入即可讀寫該店
  設定；若未來需要「店員 vs 店主」分級，需要另外設計
  `settings-admin` 權限，不在本輪範圍）。
- **Query**：無。
- **Body**：無（GET）。
- **成功 Response**：
  ```json
  {
    "success": true,
    "data": {
      "ga4_realtime_enabled": false,
      "ga4_realtime_property_id": "",
      "ga4_realtime_stream_id": "",
      "ga4_realtime_single_property_mode": false,
      "ga4_realtime_cache_seconds": 60,
      "ga4_realtime_auto_refresh_enabled": true,
      "server_single_store_mode_available": false,
      "credential_available": false,
      "sdk_available": true
    }
  }
  ```
- **Nullable**：無欄位為 null；`ga4_realtime_property_id`／
  `ga4_realtime_stream_id` 未設定時是空字串 `""`，不是 `null`。
- **Store Isolation**：SQL 帶 `WHERE store_id=?`（`req.storeId`），物理上
  不可能跨店讀取。
- **Credential Handling**：只回兩個 boolean（`credential_available`／
  `sdk_available`），不回任何憑證內容、路徑或 Property/Stream 以外的敏感
  資訊。

### 2. `PATCH /api/settings/ga4-realtime`

- **Mount／權限**：同上。
- **Query**：無。
- **Body**（只接受以下 6 個白名單欄位，其餘一律 400）：
  `ga4_realtime_enabled`／`ga4_realtime_property_id`／
  `ga4_realtime_stream_id`／`ga4_realtime_single_property_mode`／
  `ga4_realtime_cache_seconds`／`ga4_realtime_auto_refresh_enabled`。
  **明確拒絕**：`store_id`／`storeId`／`credentials`／`private_key`／
  `client_email`／`service_account_json`／`access_token`／
  `refresh_token`／`property_path`／`property_id`（非白名單命名）／
  `stream_id`（非白名單命名）。
- **成功 Response**：跟 GET 同一種形狀（回傳寫入後的最新設定）。
- **錯誤 Response**：
  - 驗證失敗：`{ "success": false, "message": "..." }`，HTTP 400。
  - 寫入失敗（Transaction 內任何一步拋出例外）：
    `{ "success": false, "message": "設定儲存失敗，請稍後再試" }`，
    HTTP 500，**不含** stack／raw DB error。
- **Validation**：
  - Property/Stream：空字串或純數字；`properties/123`、URL、負數、帶符號
    一律拒絕。
  - Cache：30～300（含邊界）。
  - Boolean：JS boolean 或字串 `"true"`/`"false"`。
  - `enabled=true` 且非 single-property mode 時，Property 與 Stream 皆
    必填。
  - `single_property_mode=true` 但部署層級
    `GA4_REALTIME_SINGLE_STORE_MODE` 未開啟時拒絕。
- **Transaction 行為**：見 `R5.4-G1.5-B2_SETTINGS_ARCHITECTURE.md` 詳述。
  摘要：使用 raw `db._db`（不是共用 wrapper 的 `db.transaction()`）做
  `BEGIN` → 逐一 `prepare/run/free` → `COMMIT` → 單次 `db._save()`；任何
  一步失敗 → `ROLLBACK` → 回 500，不留半套設定。
- **Cache Invalidation**：只在 `COMMIT` 成功後才呼叫
  `invalidateGa4RealtimeCacheForStore(storeId)`；失敗路徑完全不觸碰
  cache／generation。
- **Generation Guard**：`invalidateGa4RealtimeCacheForStore()` 會遞增該
  store 的 generation token；任何在設定變更前就已經送出、尚未完成的
  GA4 Realtime fetch，完成時會比對 generation，不相符就不寫回 cache、
  不更新 `last_success_at`/`last_error_code`。

### 3. `POST /api/geo-live/ga4-realtime-test`

- **Mount／權限**：掛載於 `/api/geo-live`，套用 `requireStore` +
  `requireFeature('reports')`。
- **Query**：無。
- **Body**：忽略（不接受 property/stream/credential 覆寫，一律只讀
  `req.storeId` 對應的已儲存設定）。
- **成功 Response**：
  ```json
  {
    "success": true,
    "data": {
      "connected": true,
      "sdk_available": true,
      "credential_available": true,
      "property_accessible": true,
      "stream_filter_valid": true,
      "realtime_request_ok": true,
      "has_recent_data": false,
      "rows_count": 0,
      "tested_at": "...",
      "message": "連線成功，目前最近30分鐘沒有即時資料。",
      "error_code": null
    }
  }
  ```
  Rate limit 命中時額外多出：
  ```json
  { "rate_limited": true, "retry_after_seconds": 17 }
  ```
  （合併在同一個 `data` 物件裡，其餘欄位延用上一次真正測試過的結果）。
- **Nullable**：`error_code` 只有失敗時非 null；`rate_limited`／
  `retry_after_seconds` 只有觸發 rate limit 時才出現。
- **Connection Test No-data 語意**：`connected:true` 但
  `has_recent_data:false`／`rows_count:0` 是**成功**，不是錯誤（GA4
  Realtime 本來就常常「連得上但剛好沒有即時流量」）。
- **Validation／Store Isolation**：完全不接受 body 傳入的
  property/stream/credential，一律用 `getGa4RealtimeConfig(db,
  req.storeId)` 已儲存的設定建構 Request；Rate Limit／Single-flight（見
  下）都以 `storeId` 為 key，物理上不會跨店互相影響。
- **Rate Limit**：同店 30 秒內最多一次真正呼叫 Google 的測試；超過限制
  直接回上一次結果並標記 `rate_limited:true`，附上
  `retry_after_seconds`（非負整數，`Math.max(0, Math.ceil((30000 -
  elapsedMs)/1000))`）。
- **Sanitization**：不回傳城市名稱列表／逐城市 activeUsers／raw Google
  rows／raw `propertyQuota`／Property ID／Stream ID／任何憑證資訊。
- **Error Codes**：`credential_unavailable`／`credential_invalid`／
  `permission_denied`（含 `401`/`403`）／`property_not_found`（含
  `404`）／`stream_filter_invalid`／`quota_limited`（含 `429`）／
  `ga4_timeout`／`ga4_unavailable`。

### GA4_REALTIME_STATUS（狀態端點擴充）

`GET /api/geo-live/ga4-realtime-status` 本輪新增欄位：
`last_test_at`／`last_test_status`（`'connected'`/`'failed'`/`null`）／
`auto_refresh_enabled`／`auto_refresh_seconds`（固定 `null`，秒數決定權
在前端，見 `R5.4-G1.5-B2_SETTINGS_ARCHITECTURE.md`）。**不呼叫任何 Google
API**（`live_check` 權限分支仍未實作，延後）。

