# R5.4-G1.6-GA4-H1.1-AUTH — Test Report

所有數字都是**實際執行**取得的真實結果（見下方各腳本的最後一行輸出），
不是推算或摘要。每一層都連續執行 3 次，數字完全一致。

## 1. Frontend Runtime

```
node scripts/run-g1-6-ga4-h1-frontend-runtime.js
=== GA4-H1 Frontend Runtime QA: 81/81 PASS, 0 FAIL ===
```
3 次一致。涵蓋：init／destroy／mode switch／metric switch／search／
sort／marker render／table render／stale cache／partial／error／
AbortController／generation token／POS Marker 不被清除／XSS safety／
Sync button lifecycle（含新版 success-refresh／429/502/401 failure 不
清快取的行為，checks 19c–19j）。

## 2. Browser Auth Runtime（真實 `app.js` apiFetch Contract）

```
node scripts/run-g1-6-ga4-h1-1-browser-auth-runtime.js
=== GA4-H1.1 Browser Auth Runtime: 47/47 PASS, 0 FAIL ===
```
3 次一致。45 項需求文件列出的驗證 + 2 項額外驗證（legacy
`geo-ga4-realtime-layer.js` 快速切換）。特別記錄一個過程中發現並修正的
**測試環境 Bug**（非 Production Bug）：checks 46/47 一開始沒有設定
`global.apiFetch`，導致 legacy layer 內部 `typeof apiFetch === 'function'`
判斷為 false，整條 fetch 路徑從未真正被觸發——測試「通過」但沒有測到
任何東西。修正後（設定 `global.apiFetch = win.apiFetch`）重新驗證，用
獨立腳本確認 `fetch` 真的被呼叫了 7 次（見下方除錯記錄），47/47 才是
真正有意義的通過。

## 3. Auth Mutation Suite

```
node scripts/smoke-hotfix30-b5-r5-4-g1-6-ga4-h1-1-auth-mutations.js
=== GA4-H1.1 Auth Mutation Suite: 24/24 PASS, 0 FAIL ===
```
3 次一致。A–L 十二項 Mutation，每項都先斷言「命中 production 原始碼裡
剛好 1 個（或 Mutation I 的 2 個）byte-exact occurrence」，再證明對應的
Static Audit check 或 Runtime 行為真的轉為 FAIL，同時證明真實未修改的
原始碼仍然 PASS。過程中發現並修正兩個測試環境 Bug：(1) Node 模組作用域
下 Mutation 注入的程式碼引用裸 `localStorage` 時，需要額外設定
`global.localStorage`；(2) 共用的 Fake Network Fetch 一開始沒有實作
`delayMs`／AbortSignal 模擬，導致 Mutation J 的競態測試永遠測不到真正的
Abort（兩個 fetch 都在下一個 tick 就完成，永遠沒有機會互相 abort）。

| Mutation | 內容 | 攔截機制 |
|---|---|---|
| A | apiFetch 改回裸 fetch | Runtime：apiFetch 從未被呼叫，Authorization 缺失 |
| B | 直接讀 `pos_store_token` | Static：No-Second-Token-Reader |
| C | Token 放進 URL | Runtime：URL 含 secretToken |
| D | 401→ga4_backend_error | Runtime：401 分類錯誤 |
| E | 403→auth_required | Runtime：403 分類錯誤 |
| F | Sync 無條件 refresh | Runtime：失敗仍呼叫 onChange |
| G | Sync 不解析 response | Runtime：成功也不再次讀取 |
| H | AbortError 重新 throw | Runtime：SafeRunFetch 直接 throw |
| I | Listener Promise 無 catch | Static：mode/metric handler 缺 `.catch(` |
| J | Legacy Layer try/catch 拿掉 | Runtime：真的產生 unhandledRejection |
| K | Auth Object 無條件 res.json() | Runtime：401 分類壞掉 |
| L | Raw Error 顯示到 DOM | Runtime：DOM textContent 含機密字串 |

## 4. GA4 Diagnostic Contract（Child Process Worker 架構）

```
node scripts/run-g1-6-ga4-h1-1-ga4-diagnostic-contract.js
=== GA4-H1.1 GA4 Diagnostic Contract: 40/40 PASS, 0 FAIL ===
NOTE: This proves the Diagnostic Contract Gate only. It does NOT prove the production GA4 Backend 502 is resolved.
```
3 次一致。11 個 Scenario，每個都在自己的全新 `child_process.spawnSync()`
process 裡執行（`scripts/helpers/ga4-h1-1-diagnostic-scenario-worker.js`），
require 真實 `middleware/storeGuard.js`／`routes/geo-live.js`／
`utils/ga4Realtime/{client,connectionTest,index}.js`，用官方
`_setClientForTest()` 注入 Fake Google Client，真實 JWT 簽發，真實 HTTP
請求。

**除錯記錄（誠實揭露）**：第一版把所有 Scenario 放在同一個 process 內，
手動 `delete require.cache`／重新 `require()` 來切換 client.js 狀態，
結果 20/29 PASS，其中 Scenario 7–16（permission_denied／
summary-city 分辨等）全部錯誤落到 `credential_unavailable`——追查後發現
是 `connectionTest.js`／`index.js`／`routes/geo-live.js` 各自
`require('./client')` 捕捉到的是各自載入時的舊 module 實例，單純刷新
`client.js` 的 cache entry 不會回頭更新它們的參照。改成 Child Process
Worker 架構後（每個 Scenario 一個全新 process，`require.cache` 從零
開始），這個問題在結構上不可能再發生，重新執行後 40/40 全部通過。

驗證項目對照（29 項需求 + 額外 11 項 Worker 存活性檢查）：

| # | 項目 | 結果 |
|---|---|---|
| 1–3 | Status/Test 端點未登入或無效 JWT → 401 | PASS |
| 4 | 合法 JWT 進入 Route | PASS |
| 5–6 | Query／Body store_id 不覆寫 JWT Store | PASS |
| 7 | 無 Credential → `credential_available:false` | PASS |
| 8 | SDK unavailable → `sdk_available:false`／`error_code=sdk_unavailable` | PASS |
| 9 | Property 未設定 → `property_configured:false` | PASS |
| 10 | Permission denied (403) → `error_code=permission_denied` | PASS |
| 11 | Invalid-argument 類錯誤 → 安全短碼（誠實揭露：production 無逐字 `invalid_argument` code，見下） | PASS |
| 12 | Network failure (ECONNRESET) → 安全碼，非原始文字 | PASS |
| 13 | Rejected Promise 不產生 unhandledRejection | PASS |
| 14 | Summary ok + City fail 可區分（`error_stage='city'`） | PASS |
| 15 | Summary ok + City ok → `connected:true` | PASS |
| 16 | `property_accessible:true`（Case A/C 皆然） | PASS |
| 17 | `error_stage` 區分 summary/city 失敗 | PASS |
| 18–27 | Property ID／Stream ID／Email／Private Key／Access/Refresh Token／Authorization Header／Raw Google Error／Stack／Fake Client 內部標記，全部掃描不存在 | PASS |
| 28 | 不造成 Process Crash | PASS |
| 29 | 無 Listener／Temp DB 殘留 | PASS |

**誠實揭露**：`utils/ga4Realtime/connectionTest.js` 的
`_classifyTestFailure()` 對照表裡沒有逐字命名為 `'invalid_argument'`
的 key；未知的 Google 錯誤一律 fallback 成 `'ga4_unavailable'`。第 11 項
驗證的是「安全分類、絕不洩漏原始訊息」這個性質本身（`error_code` 是
一個短、有限、安全的字串），不是斷言一個不存在的逐字 Contract。

## 5. Static Audit

```
node scripts/static-audit-g1-6-ga4-h1.js
=== GA4-H1 Static Audit: 190/190 PASS, 0 FAIL ===
```
166 項既有 + 24 項本輪新增（`H1-AUTH 1`–`H1-AUTH 24`，見 Implementation
Report）。過程中修正一個測試環境 Bug：`window.apiFetch === 'function'`
的寫法被「Panel 只允許一個 `window.X =` 全域賦值」的既有 check 誤判成賦值
（`===` 開頭的 `=` 被 regex 誤配對），改寫成先取局部變數
`winApiFetch` 後即消除。

## 6. Regression（H1.1 Runner，34 Suites × 3 輪）

```
node scripts/run-regression-g1-6-ga4-h1-1-auth.js
  Suites per round: 34
  node --check: OK
  3 rounds all green: YES
  3 rounds consistent: YES
  data/pos.db residue: no
```
34 個 Suite 全部三輪一致、全綠，涵蓋本輪新增 5 層 + 既有 GA4-H1／G1.5／
G1／A1.2.1／A1.2／A1.1／A1／B1/B2 系列／Geo Map／Geo Settings 全部既有
Suite。執行期間監控：無 `data/pos.db` 殘留、無 Temp DB 殘留、無
`unhandledRejection` listener 殘留（`detectResidue()`）。

## 7. Packaging Verification（真實執行結果）

| 項目 | 結果 |
|---|---|
| Workdir Security Scan（3 輪） | **PASS**（Real-credential SUSPECT hits: 0，3 輪一致） |
| Staging Security Scan（3 輪） | **PASS**（0 hits，3 輪一致） |
| ZIP Extraction Security Scan（全新暫存目錄，3 輪） | **PASS**（0 hits，3 輪一致） |
| ZIP Verification（`unzip -t`） | **PASS**（No errors detected；單一頂層 `fix18-10-hotfix30-B5-R5.4-G1.6-GA4-H1.1-AUTH/`；無 traversal／絕對路徑／staging 路徑／node_modules／.git／.env／DB／舊 ZIP） |
| `npm ci`（全新解壓目錄，使用 ZIP 內 `package-lock.json`） | **PASS**（exit=0，added 476 packages，無沿用 Workdir `node_modules`） |
| Clean Extraction Core Tests | **PASS**（Frontend Runtime 81/81、Browser Auth Runtime 47/47、Auth Mutation 24/24、Diagnostic Contract 40/40、Static Audit 190/190、H1 QA 22/22、H1 Credential Guard 17/17、H1 Targeted Smoke 108/108、G1.5-A Smoke 140/140、G1.5-A Static Audit 77/77、G1 geo-live 212/212、A1.2.1 Smoke 190/190、A1.2.1 Static Audit 106/106、A1.2.1 QA 41/41、B2.4 139/139、B2.5 76/76、A1.1 Smoke 160/160、A1.1 Static Audit 90/90、A2-T1 12/12，全部 FAIL=0） |
| Clean Extraction H1.1 Regression Runner | **PASS**（34/34 Suites，執行 3 輪，全綠、一致，`node --check` OK） |
| Clean Extraction Residue | **PASS**（無 `data/pos.db`、無 Temp DB、無 `.sqlite`、無殘留 process） |

## 8. Gate 判定

| Gate | 狀態 |
|---|---|
| H1 Frontend Auth Automated Gate | **PASS** |
| H1 Frontend Auth Production Gate | READY FOR MANUAL DEPLOYMENT VERIFICATION |
| GA4 Diagnostic Contract Gate | **PASS**（見上方 40/40，僅證明 Contract 本身） |
| GA4 Backend Production Gate | READY FOR MANUAL DIAGNOSTIC VERIFICATION（**不得**寫 PASS） |
| Abort Safety Automated Gate | **PASS** |
| Abort Safety Production Gate | READY FOR MANUAL DEPLOYMENT VERIFICATION |
| Static Audit Gate | **PASS** |
| Regression Gate | **PASS** |
| Security Gate | **PASS** |
| ZIP Verification Gate | **PASS** |
| Clean Extraction Gate | **PASS** |
| Store Isolation | SINGLE_STORE_ONLY |
| Production GA4 | 不得寫 PASS |
| POS Individual Visitor Mapping | NOT IMPLEMENTED / OUT OF SCOPE |

**注意**：ZIP 檔案本身的 Size／SHA-256 只在交付訊息裡外部回報，不寫回這份
文件後再重新打包（避免自我參照造成 ZIP 內容改變後 SHA 又要重算的循環）。

