# R5.4-G1.6-A1.2.1_TEST_REPORT.md
Geo Event Taiwan Time & Estimate Marker Verification Hotfix

---

## 1. Dependency Verification

| 檢查 | 結果 |
|---|---|
| `package.json` vs A1.2 baseline | **byte-identical**（SHA-256 一致） |
| `package-lock.json` vs A1.2 baseline | **byte-identical**（SHA-256 一致） |
| 新增 dependency | **無** |
| Lockfile drift | **無** |
| `sql.js`（QA Harness 使用） | 已是專案原有 pinned production dependency（`^1.14.1`），非新增 |

`npm ci` 只安裝既有 lockfile 鎖定的套件，可在乾淨環境重現。

## 2. QA Harness

腳本：`scripts/run-g1-6-a1-2-1-manual-qa.js`

| 執行次數 | PASS | FAIL | TOTAL |
|---|---|---|---|
| Run 1 | 41 | 0 | 41 |
| Run 2 | 41 | 0 | 41 |
| Run 3 | 41 | 0 | 41 |

**Serve Mode**（`--serve`）：

| 檢查 | 結果 |
|---|---|
| 綁定位址 | `127.0.0.1`（實測 curl 可連線） |
| Port | 隨機（`listen(0, ...)`），本次實測為 `39367` |
| `/api/geo-live/marker-model` | 回傳真實 marker-model JSON（含 exact/estimate/unknown 正確計數） |
| `/api/geo-analytics/visitor-log` | 回傳真實 summary/areas/recent JSON |
| SIGINT | `server.close()` + `qaState.cleanup()`，process 正常結束（exit，非殘留） |
| Temp DB cleanup | 確認無殘留檔案（`find /tmp -iname "*qa-geo-store*"` 為空） |
| Process cleanup | `ps -p <pid>` 確認程序已結束 |
| 正式 `.env`／GA4／外部 Geo Provider | 原始碼掃描確認無耦合 |

## 3. Targeted Smoke

腳本：`scripts/smoke-hotfix30-b5-r5-4-g1-6-a1-2-1-time-and-marker-qa.js`

| 執行次數 | PASS | FAIL | TOTAL |
|---|---|---|---|
| Run 1 | 190 | 0 | 190 |
| Run 2 | 190 | 0 | 190 |
| Run 3 | 190 | 0 | 190 |

主要涵蓋區段：

- **A. UTC Parsing**（23 項）：SQLite UTC／ISO Z／ISO offset／epoch／null／invalid／
  no-double-conversion／leap day／year rollover
- **B. Taipei Display**（13 項）：`01:52:36Z → 09:52:36`、日/月/年 rollover、
  24 小時制、invalid fallback（`—`，非 `Invalid Date`/`NaN`）
- **C. Today Range**（12 項）：`[start,end)`、inclusive start／exclusive end、
  today 與 24h/7d 邊界相異、全年固定 UTC+8
- **D. API Contract**（12 項）：legacy `event_time` 保留、`event_time_utc` 帶 Z、
  invalid→null、DB 原值不變、visitor masking、`event_name` 不變
- **E/E2. Runtime Renderer**（26 項，含真實 jsdom Pipeline）：載入真實
  `date-time-format.js` + `geo-heatmap.js` + `geo-heatmap-ui.js` +
  `geo-visitor-layer.js`，透過 `geoVisitorFetchAndRender()` 觸發，斷言 DOM
  實際顯示 `09:52:36`、不顯示 `01:52:36`/`17:52:36`/`Invalid Date`；
  Refresh／Metric Switch／Range Switch 後顯示一致
- **F. QA Fixture Safety**（15 項）：`os.tmpdir()` 限制、正式 DB 拒絕、
  `127.0.0.1`-only、無 `0.0.0.0`、cleanup 驗證
- **G. Known District/County/Unknown/Exact**（22 項）：Marker accuracy／
  coordinate_source／dedupe／summary 對應
- **H/H2. Mutation Negative**（29 項）：19 組負向測試證明每個防護真的會抓到
  對應的錯誤手法（SQLite-當-Local、雙重加 8、UTC-Calendar-Today、
  `BETWEEN`、DB migration 改歷史值、`0.0.0.0` 綁定、all-Unknown 誤判
  blocked 等）
- **K. Partial Failure Contract**（13 項）：Catalog unavailable／Region query
  失敗／All-Unknown 三種情境的 `status`/`error_code`/`capabilities` 正確性
- **M/N. 額外邊界情境**（25 項）：多訪客聚合、同訪客多事件去重、跨 district
  聚合、`custom` range、`limit` 邊界、型別防呆、事件排序

## 4. Static Audit

腳本：`scripts/static-audit-g1-6-a1-2-1.js`

| 執行次數 | PASS | FAIL | TOTAL |
|---|---|---|---|
| Run 1 | 106 | 0 | 106 |
| Run 2 | 106 | 0 | 106 |
| Run 3 | 106 | 0 | 106 |

稽核範圍精確限制在：`utils/dateTime.js`、`utils/geoVisitLog.js`、
`public/js/date-time-format.js`、`public/js/geo-visitor-layer.js`、
`public/index.html`（載入順序）、A1.2.1 QA scripts；並確認 A1.2 Catalog／
Marker Contract、`routes/orders.js` 既有 `+8` 寫法（pre-existing、
明確排除在本輪範圍外）未被誤動。

## 5. Regression（3 輪）

腳本：`scripts/run-regression-g1-6-a1-2-1.js`

| Round | 結果 |
|---|---|
| Round 1 | 全部 PASS，FAIL=0 |
| Round 2 | 全部 PASS，FAIL=0 |
| Round 3 | 全部 PASS，FAIL=0 |

`node --check`：**OK**（全部 A1.2.1 新增/修改檔案）
三輪數字：**完全一致**
`exit code`：**0**

主要 Suite 結果（三輪皆相同）：

| Suite | 結果 |
|---|---|
| A1.2.1 Smoke | 190/190 |
| A1.2.1 Static Audit | 106/106 |
| A1.2.1 QA Harness | 41/41 |
| A1.2 Smoke | 251/251 |
| A1.2 Static Audit | 125/125 |
| Catalog Verify | 57/57 |
| A1.1 Smoke | 160/160 |
| A1.1 Static Audit | 90/90 |
| A1 Smoke | 50/50 |
| Order Heatmap | 128/128 |
| Dashboard Rewire | 101/101 |
| B2.5 | 76/76 |
| B2.4 | 139/139 |
| B2.3 | 75/75 |
| B2.2 | 95/95 |
| B2.1 | 85/85 |
| B2 Settings | 187/187 |
| B2 Static Audit | 82/82 |
| B2a | 106/106 |
| B1 | 168/168 |
| B1 Static Audit | 71/71 |
| G1.5-A | 140/140 |
| G1.5-A Static Audit | 77/77 |
| Geo Map Settings | 620/620 |
| Geo Settings UI | 157/157 |
| G1 geo-live | 212/212 |

**Residue after each round**：無 `data/pos.db`、無 QA temp DB、無 server
process、無 timer residue。

## 6. Security Scan

**No real credentials found.**

掃描範圍：工作目錄（`utils/`／`public/`／`scripts/`／四份文件／staging）＋
最終 ZIP 解壓後內容。

搜尋關鍵字：`BEGIN PRIVATE KEY`／`END PRIVATE KEY`／`private_key`／
`private_key_id`／`client_email`／`iam.gserviceaccount.com`／
`GA4_SERVICE_ACCOUNT_JSON`／`access_token`／`refresh_token`／
`Authorization:`／`Bearer`／`Cookie:`。

| 分類 | 結果 |
|---|---|
| A. 真實 Credential（`BEGIN PRIVATE KEY-----` 實際區塊、真實 GCP `private_key_id` 值等） | **0 筆命中** |
| B. Denylist Literal（安全掃描程式本身的關鍵字清單、文件中說明「不得含有 X」的敘述、既有 `forbiddenKeys` 白名單陣列） | 多筆命中，皆為程式碼中主動**拒絕**憑證欄位的邏輯或文件說明文字，非真實憑證值（例如 `utils/ga4RealtimeConfig.js` 的 `forbiddenKeys` 陣列、既有 `R5.4-G1.5-B2_SETTINGS_ARCHITECTURE.md` 說明文字） |
| C. 安全假資料（QA fixture） | `scripts/run-g1-6-a1-2-1-manual-qa.js` 的 `qa_v_district`／`qa_v_county`／`qa_v_unknown`／`qa_v_exact`／`qa_geo_store` 等，全部是明顯的假 ID，不含真實 Email／真實 visitor_id／raw IP（僅有 `127.0.0.1` loopback，屬必要的伺服器綁定位址，非使用者資料）／地址／LIFF ID／Token／正式 `store_id` |

`.env` / `.env.local` / `.env.production` 檔案：**不存在**於工作目錄與 ZIP 中。
`.env.example` 內僅有安全 placeholder（例如
`JWT_SECRET=replace-with-a-random-secret`），無真實值。

ZIP 解壓後額外掃描 `BEGIN PRIVATE KEY-----` 完整區塊與
`"private_key_id": "<20+ hex>"` 模式：**0 筆命中**。

## 7. Clean Extraction

解壓目錄：`/tmp/g1-6-a1-2-1-clean/fix18-10-hotfix30-B5-R5.4-G1.6-A1.2.1/`
（全新目錄，未重用任何既有 `node_modules`／DB／cache／temp／env）

| 步驟 | 結果 |
|---|---|
| `npm ci` | **exit code 0**（476 packages installed，與原環境一致） |
| `node --check`（9 個本輪新增/修改檔案） | 全部 **OK** |
| A1.2.1 Smoke | **190 / 190 PASS** |
| A1.2.1 Static Audit | **106 / 106 PASS** |
| A1.2.1 QA Harness | **41 / 41 PASS**（連續 3 次） |
| A1.2 Smoke | 251 / 251 |
| A1.2 Static Audit | 125 / 125 |
| Catalog Verify | 57 / 57 |
| A1.1 Smoke | 160 / 160 |
| A1.1 Static Audit | 90 / 90 |
| A1 Smoke | 50 / 50 |
| Order Heatmap | 128 / 128 |
| Dashboard Rewire | 101 / 101 |
| B2.5 | 76 / 76 |
| B2.4 | 139 / 139 |
| B2.3 | 75 / 75 |
| B2.2 | 95 / 95 |
| B2.1 | 85 / 85 |
| B2 Settings | 187 / 187 |
| B2 Static Audit | 82 / 82 |
| B1 | 168 / 168 |
| G1.5-A | 140 / 140 |
| Geo Settings UI | 157 / 157 |

殘留檢查：無 `data/pos.db`、無 QA temp DB、無 server process、無 timer
residue。

**重要修正記錄（本節誠實揭露）：** 第一次乾淨解壓時，QA Harness 出現
`40/41`（`QA-26` 失敗）。根因是 `scripts/lib/qa-temp-db.js` 的
`assertPathIsSafe()` 原本用 `normalized.startsWith(os.tmpdir())` 判斷路徑
合法性——當專案本身被解壓在 `/tmp/xxx-clean/...` 這種路徑下時，專案原始碼
目錄本身的任何路徑字串上都會「碰巧」以 `os.tmpdir()` 開頭，導致這個防呆
判斷失效。已修正為要求路徑的 `dirname` 必須**直接等於** `os.tmpdir()`
本身（符合 `createTempQaDb()` 實際產生檔案的方式：
`path.join(os.tmpdir(), 'qa-geo-store-<random>.sqlite')`，不會有巢狀子
目錄），修正後在原開發環境與乾淨解壓（`/tmp` 路徑）環境下都穩定回到
`41/41`。此修正已同步套入最終 ZIP，前述「1. 目前已完成且不得回退的真實
結果」章節中的 A1.2.1 Smoke（190/190，其中 F/H 類別涵蓋這個 Helper 的路徑
判斷邏輯）與 Static Audit（106/106）在修正後重新驗證，數字不變。
