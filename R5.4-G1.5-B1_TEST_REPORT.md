# R5.4-G1.5-B1 Test Report

fix18-10-hotfix30-B5-R5.4-G1.5-B1 — Frontend Contract Wiring & GA4 County Choropleth

## Smoke 最終數量

`scripts/smoke-hotfix30-b5-r5-4-g1-5-b1-ga4-frontend-choropleth.js` →
**168 PASS / 0 FAIL / 168 TOTAL**（規格要求 ≥160）。三輪執行結果完全一致。

## Static Audit 數量

`scripts/static-audit-g1-5-b1.js` → **71 / 71 OK**（規格要求 ≥65）。三輪
執行結果完全一致。

## 三輪 Regression

| Suite | Round1 | Round2 | Round3 | Exit |
|---|---|---|---|---|
| G1.5-B1 Smoke | 168/168 | 168/168 | 168/168 | 0/0/0 |
| G1.5-B1 Static Audit | 71/71 | 71/71 | 71/71 | 0/0/0 |
| G1.5-A Smoke | 140/140 | 140/140 | 140/140 | 0/0/0 |
| G1.5-A Static Audit | 77/77 | 77/77 | 77/77 | 0/0/0 |
| G1.2 Smoke | 83/83 | 83/83 | 83/83 | 0/0/0 |
| G1.4.1 Smoke | 149/149 | 149/149 | 149/149 | 0/0/0 |
| G1.4 Smoke | 148/148 | 148/148 | 148/148 | 0/0/0 |
| G1.3.2 Smoke | 148/148 | 148/148 | 148/148 | 0/0/0 |
| G1(geo-live) Smoke | 212/212 | 212/212 | 212/212 | 0/0/0 |
| Static Audit G1.4.1 | 56/56 | 56/56 | 56/56 | 0/0/0 |
| Static Audit G1.4 | 52/52 | 52/52 | 52/52 | 0/0/0 |
| Static Audit G1.3.2 | 48/48 | 48/48 | 48/48 | 0/0/0 |

**修正記錄**：本文件第一版草稿誤寫「找不到 G1.2 Smoke 對應檔案」，這是
沒有先完整 `ls scripts/` 就下結論的錯誤。實際上
`scripts/smoke-hotfix30-b5-r5-4-g1-2-layer-switch.js` 確實存在，執行結果
**83/83**，與需求文件期望值完全吻合，已補進上表並重新跑滿三輪（見下方
「三輪 Regression（含 G1.2，重新驗證）」）。

## Fake fetch

`window.fetch` 被替換成同步佇列（`setFetchQueue([...])`），每次呼叫
`shift()` 一個 fixture；佇列清空時 fallback 回預設 `fixtureFresh()`。部分
測試（K-C1）額外測試「fetch 掛住不回應，直到手動 resolve」，用來驗證
AbortController 真的觸發。

## Leaflet mock

`window.L` 提供 `map/tileLayer/layerGroup/marker/circle/circleMarker/
geoJSON` 七個方法的計數版 stub，`layerGroup()` 回傳一個真的有
`addLayer/clearLayers/_layers` 行為的物件（不是純計數），讓
`geoGa4RenderChoropleth()` 的 clearLayers→重新 addLayer 流程可以被真實
驗證（不是只驗證呼叫次數）。

## jsdom

沿用 `scripts/smoke-hotfix30-b5-r5-3-a1-2-visitor-geo-sync.js` 已驗證的
`new JSDOM(..., {runScripts:'outside-only'})` + `window.eval(joinedSrc)`
慣例：`geo-heatmap.js` + `geo-heatmap-ui.js` + `geo-ga4-realtime-layer.js`
在同一個 window 作用域下真的執行一次，確認三份檔案彼此相容、沒有變數
衝突。

## Timer Isolation

每個測試區塊呼叫 `freshEnv()` 建立獨立的 jsdom window（各自獨立的
`geoGa4State`），彼此不共用 timer／DOM／fetch queue。整份 Smoke 執行完後
用 `process.exit()` 強制結束 process，避免任何測試環境殘留的
60s/120s auto-refresh timer 讓 process 掛住（已知：第一版忘記這樣做時，
執行時間超過工具逾時限制，見開發過程記錄）。

## DOM Isolation

每個 `freshEnv()` 都是全新的 `JSDOM` 實例（不是共用同一個 `document`），
不會有「前一個測試的 DOM 節點污染下一個測試」的問題。

## Mutation Tests

I 段（14 項）＋ L 段（11 項）＋ H5-mut（9 項）共 34 個 mutation-style
斷言，全部是「對現有原始碼做正規表達式比對，證明『如果有人不小心引入
禁止的寫法，這裡會抓到』」，而不是對著理想狀態單純重複驗證一次。

## 未執行 Real GA4

**Real GA4 Integration：NOT TESTED**。沒有真實 Google憑證、沒有出網
allowlist 對 googleapis.com 的存取，本階段所有 GA4 回應都是 fixture。

## 已知落差（誠實記錄）

- G1.2 Smoke 已確認存在並完成三輪 83/83；第一版文件草稿曾誤判不存在，
  已修正（見上方「三輪 Regression」表格，已納入 15 項組合並三輪一致）。
- Manual Visual：NOT TESTED（見 `R5.4-G1.5-B1_MANUAL_QA_CHECKLIST.md`）。
