# G1.6-A2-P1 — Zeabur Enablement Plan（規劃文件，尚未執行）

本文件只列出「未來要設定的環境變數名稱與安全範例」，**不代表已經設定，
也不代表已經啟用**。目前正式環境狀態仍是：

```
TRUST_PROXY                 未設定（Trust Gate 已 PASS，建議值見下方，但尚未寫入）
GEO_VISITOR_IP_ENABLED      false
GEO_VISITOR_IP_PROVIDER     disabled
```

## 未來環境變數清單

| 變數 | 建議值 | 狀態 |
|---|---|---|
| `TRUST_PROXY` | `1` | **建議可以設定**——Client IP Trust Gate 已 PASS（見 A2-T1 稽核文件），這是本計畫中唯一建議「現在就可以做」的變更，且與 Provider 選型完全獨立 |
| `GEO_TRUSTED_IP_HEADER` | 不設定 | 維持不設定，Trust Gate 判定不需要 |
| `GEO_VISITOR_IP_ENABLED` | `true` | **暫不設定**——待使用者選定 Provider 並取得 API Key 後才設定 |
| `GEO_VISITOR_IP_PROVIDER` | `ipapi`（若採用最少改動方案） | **暫不設定** |
| `GEO_VISITOR_IP_API_KEY` | `<向 Provider 取得的正式金鑰>` | **暫不設定，且本文件/ZIP/任何交付物不會包含真實金鑰** |
| `GEO_VISITOR_IP_TIMEOUT_MS` | `2500`（沿用既有預設） | 待啟用時設定 |
| `GEO_VISITOR_IP_CACHE_TTL_SECONDS` | `86400`（沿用既有預設） | 待啟用時設定 |
| `GEO_VISITOR_IP_FAILURE_CACHE_TTL_SECONDS` | `900`（沿用既有預設） | 待啟用時設定 |
| `GEO_CACHE_SECRET` | 一組隨機字串（例：本地執行 `openssl rand -hex 32` 產生，不要用本文件裡的示範值） | **建議在啟用前先設定好**，即使 Provider 還沒選定；越早設定，cache 語意越早穩定 |

## 架構保證（沿用既有，不新建）

啟用時的完整路徑維持不變：

```
routes/analytics.js
  → analyticsLog.insertEvent()
    → geoResolver.resolveVisitorGeoCached()
      → geoSanitizer.getTrustedClientIp()   ← Trust Gate 已驗證這裡會拿到 Public IP
      → geoProviders/index.js
        → geoProviders/ipapi.js（或新增的其他 adapter，若選 B/C/D 路線）
      → normalizeTaiwanGeo()
    → geoVisitLog.logGeoVisit()
```

若選定 A（ip-api Pro）：**不新增檔案**，只在既有 `utils/geoProviders/ipapi.js`
補上 IPv6 支援分支（目前刻意回傳 `IPV6_UNSUPPORTED`）。

若選定 B/C（IPinfo／IPdata）：新增 **一個** `utils/geoProviders/<provider>.js`，
介面照抄 `ipapi.js` 的 `lookupVisitorGeo(ip, options)` shape，在
`utils/geoProviders/index.js` 的 `PROVIDERS` 白名單註冊，**不修改**
`geoResolver.js`／`geoSanitizer.js`／`index.js` 的既有邏輯與呼叫方式。

若選定 D（MaxMind 本地資料庫）：同樣新增一個 adapter 檔案，但內部改為讀取
本地 `.mmdb` 檔案（需要 `maxmind` npm package + 部署時準備資料庫檔案），
對外介面仍然是相同的 `lookupVisitorGeo(ip, options)` 形狀，`geoResolver.js`
完全不需要知道底層是 HTTP 呼叫還是本地查詢。

## Store Toggle

沿用既有機制（若既有 Admin Settings 已有訪客 IP 粗略地區判定開關，見
`R5.4-G1.6-A2` 系列文件第十二節）：

- 全店預設維持 `disabled`。
- 啟用時**只在 `store_001` 開啟**做正式環境試跑，觀察一段時間的
  Unknown Rate／Cache Hit／district 命中率後，再決定是否推廣到其他店家。
- 不得因為 Client IP Trust Gate PASS 就自動全店開啟。

## Privacy Gate 複查清單（本輪僅複查，未變更任何程式碼）

- [x] raw IP 不進 DB — 沿用既有 `utils/geoVisitLog.js`／`analyticsLog.js`
      白名單寫入邏輯，未變更
- [x] raw IP 不進 log — 本輪新增的診斷端點也遵守同一原則（見
      `routes/admin-geo-diagnostic.js` 只印固定字串，不插入任何請求資料）
- [x] raw IP 不進 API response — 既有 `sanitizeGeoForOutput` 邏輯未變更；
      本輪新增的診斷端點輸出也是白名單欄位
- [x] raw IP 不進 browser — 同上
- [x] XFF 不儲存 — 未變更
- [x] Provider key 不進 frontend — Provider 呼叫全部在
      `utils/geoProviders/*.js`（Server-side），從未傳給前端
- [x] Provider key 不進 ZIP — 本文件與所有交付物都只列變數名稱，無真實值
- [x] Provider lat/lng 不當 Exact — 既有規則未變更，Marker 座標繼續只來自
      A1.2 官方 Representative Point Catalog
- [x] 歷史 Unknown 不回填 — 未變更，A2 系列從一開始就明確排除這件事
- [x] GA4 不回填 visitor — 未變更

**誠實聲明**：若日後選用任何外部 HTTP Provider（ip-api Pro／IPinfo／
IPdata），單次查詢時，該 Provider 會接收到要解析的訪客 IP，這是 IP
geolocation 服務本質上無法避免的事。

## 本輪未執行的事項（依使用者指示）

- 未購買任何方案
- 未要求使用者提供任何 API Key
- 未設定 `GEO_VISITOR_IP_ENABLED=true`
- 未在正式環境呼叫免費商用受限端點
- 未開始 G2
