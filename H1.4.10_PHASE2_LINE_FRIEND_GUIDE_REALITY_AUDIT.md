# H1.4.10 Phase 2 — friend_entry／friend_checkout Reality Audit

範圍：H1.4.10 Phase 1（68/68 PASS）完成後的同一工作目錄，繼續做
「friend_entry / friend_checkout × 加入官方 LINE 免登入引導」。

---

## 一、現有 Gate 架構盤點（修改前）

| 項目 | Production 現況 |
|---|---|
| `line_member_gate_enabled` | 既有布林開關 |
| `line_member_gate_mode` | 既有 enum，只有 `disabled` / `checkout` / `entry` |
| `line_member_require_friend`（後台 checkbox 標示「要求加入官方帳號」） | 既有，供 `checkout`／`entry` 模式判斷是否要求好友資格（`ensureFriendRequirement()`） |
| `line_member_allow_skip` | 既有，僅 `entry` 模式生效 |
| `line_add_friend_url` / `line_member_add_friend_url` | 既有雙欄位，`_buildLineMemberGateConfig()` 已有既定優先順序解析 |
| `openFriendAddPage()` | 既有，`checkout`/`entry` Gate 專用：優先 `liff.requestFriendship()`，失敗才 fallback 開網址 |
| `showMemberGate()` / `requireMemberBeforeCheckout()` / `requireMemberOnEntry()` | 既有「強制」Gate：回傳 Promise，直到使用者完成登入才 resolve |

**結論：`checkout`／`entry` 是「登入 Gate」——顧客不完成 LINE 登入就無法繼續（`entry` 可選擇性 allow_skip，`checkout` 完全不可略過）。這與本階段新增的 `friend_entry`／`friend_checkout`（柔性、免登入、隨時可略過）在語意上是兩種完全不同的機制，不是同一套邏輯的變體。**

## 二、`#goCheckoutBtn` 真實觸發點盤點

`openCheckoutStep(event)`（`public/line-order.html` / `line-shipping.html`）是 H1.4.9 explicit-button-only 的唯一入口：

```
真實 click 事件（event.type==='click' && event.currentTarget===goBtn）
→ _enterCheckoutStage()（純 UI 切換，不送事件）
→ 同 cart_id 去重後送出 checkout_click
```

既有 `checkout` gate mode 的登入要求則是掛在 **`submitOrder()`**（送單前），不是掛在 `openCheckoutStep()`。這代表 Phase 2 的 `friend_checkout` 若要依附「真正結帳點擊」，觸發時機點與既有 `checkout` gate mode 的觸發時機點（送單前）並不相同——這是刻意的設計，`friend_checkout` 依需求文件明確定義為「顧客按下前往結帳、進入結帳頁之後」的柔性引導，而不是送單前的強制檢查。

## 三、事件白名單與 metadata sanitizer 架構

`utils/analyticsLog.js` 的 `EVENT_WHITELIST` + `routes/analytics.js` 的 `SERVER_ONLY_EVENTS`／逐事件 metadata sanitizer（`sanitizeFulfillmentMetadata` → `sanitizeCartSnapshotMetadata` → `sanitizeLiffAutoIdentifyMetadata`）已經是既有、經過驗證的 pipeline 架構，Phase 2 只是在同一條 pipeline 尾端加上第三層 `sanitizeFriendGuideMetadata`，未新建平行機制。

## 四、重要架構區分（本文件必須記錄，非單純 bug fix）

1. **`checkout`／`entry` 是登入 Gate；`friend_entry`／`friend_checkout` 是非阻塞的加好友引導。** 前者回傳 Promise 並且擋住流程直到完成登入；後者是 fire-and-forget 的 UI 呼叫，呼叫端完全不 `await` 它來決定是否可以繼續。這個差異決定了兩者不能共用 `showMemberGate()`/`requireMemberBeforeCheckout()` 的 Promise-gating 架構，必須是新的、結構上就不可能阻塞的函式（`maybeShowFriendEntryGuide()` / `maybeShowFriendCheckoutGuide()`，同步回傳一個描述性物件，不是 Promise）。
2. **純加好友（按下「加入官方 LINE」）不等於網站 LINE 身分歸戶。** 按鈕點擊只會呼叫 `openFriendGuideLink()` 開啟外部連結，絕不呼叫 `liff.requestFriendship()`（那需要 LIFF 登入狀態，與「免登入」定義衝突）、絕不呼叫 `liff.login()`、絕不寫入或竄改 `member_session`／`is_friend`。真正的好友狀態與身分歸戶，仍完全交給既有 Webhook `follow`/`unfollow` 與 LIFF `getFriendship()` + 後端 `verify()`（Phase 1 的被動辨識）機制決定，Phase 2 沒有新增、也沒有修改任何一條這方面的判斷邏輯。
3. **LIFF Auto Identify（Phase 1）與 Friend Guide（Phase 2）是兩個獨立機制，只在讀取「已知好友狀態」時有交集。** `knownFriendStatus()` 讀取的是 Phase 1 `saveMemberSession()` 寫入的、後端已驗證過的 `is_friend` 欄位；Friend Guide 本身完全不觸發、也不依賴 Auto Identify 是否執行過——即使 Auto Identify 關閉或失敗，Friend Guide 仍能在「好友狀態未知」的情況下正常顯示與略過。

## 五、本階段實際修改的檔案

1. `public/js/line-member-gate.js` — 新增 Friend Guide 共用模組（`maybeShowFriendEntryGuide` / `maybeShowFriendCheckoutGuide` / `openFriendGuideLink` / dedupe / known-friend 短路 / 缺網址 fallback）
2. `routes/settings.js` — `gate_mode` 白名單新增 `friend_entry`/`friend_checkout`；friend-only mode 放寬 `liff_id`/`login_channel_id` 必填規則
3. `utils/analyticsLog.js` — 新增 3 個事件白名單
4. `routes/analytics.js` — 新增 `sanitizeFriendGuideMetadata()`，串入既有 pipeline
5. `public/line-order.html` — 整合 `friend_entry`（進站）與 `friend_checkout`（`#goCheckoutBtn` 真實點擊之後）
6. `public/line-shipping.html` — 同上
7. `public/index.html` — Admin Settings UI：入口模式下拉選單新增 2 個選項、friend mode 說明、`require_friend` 語意區分說明
8. `public/js/app.js` — `updateLineMemberGateModeUI()` 新增，`loadLineMemberGateSettings()` 呼叫它同步 UI 狀態
9. `scripts/run-h1-4-10-phase2-line-friend-guide-runtime.js` — 新增 Phase 2 專用回歸測試

## 六、Frozen Contract 確認

- H1.4.9：`scripts/run-h1-4-9-checkout-order-summary-runtime.js` 修改前後皆 `67/67 PASS`。
- Phase 1：`scripts/run-h1-4-10-phase1-liff-identity-runtime.js` 修改前後皆 `68/68 PASS`。
- `checkout_click` 的送出邏輯（`_trackEvent('checkout_click', ...)` 那幾行）本身**一個字元都沒有修改**；`maybeShowFriendCheckoutGuide()` 的呼叫點插在它之後，純附加，不改變其因果、去重或送出時機。
