# R5.3-A3 — Analytics Data Flow Audit

## 1. 目的

稽核「Analytics Dashboard 有正確資料，但 Geo Intelligence 顯示 Geo 訪客/
加購/結帳全部為 0」的根因，並確認 Geo Intelligence 是否仍以 `geo_visit_log`
作為統計來源。

## 2. 稽核方法

1. 全專案搜尋 `INSERT INTO analytics_events`，確認寫入路徑數量。
2. 直接對真實 sql.js DB 執行 `insertEvent()`，追蹤同一次呼叫是否同步寫入
   `geo_visit_log`。
3. 檢查 Geo Intelligence 前端（`geo-heatmap-ui.js`／`geo-visitor-layer.js`）
   實際觸發 API 呼叫的時機點。
4. 模擬「Analytics 已有資料、Geo 顯示 0」的情境，找出對應的程式碼路徑。

## 3. 稽核結果

### 3.1 寫入路徑：只有一個進入點

```
grep -rn "INSERT INTO analytics_events" utils/ routes/ middleware/
→ 只有 utils/analyticsLog.js 一處（`insertEvent()`）
```

**結論：本專案沒有第二套寫入 `analytics_events` 的邏輯**，所有 POS／
LINE 點餐／外送／LINE Pay／會員等流程最終都經過同一個 `insertEvent()`
函式。

### 3.2 `geo_visit_log` 目前仍是 Geo Intelligence 的主要統計來源

`utils/geoEventEngine.js` 的 `getGeoEventFunnel()`、`utils/geoVisitLog.js`
的 `getGeoVisitSummary()`/`getGeoVisitAreas()`/`getRecentGeoVisits()`
全部查詢 `geo_visit_log`，**沒有**改回直接查 `analytics_events`（見下方
第 5 節說明為什麼這是正確設計，不是需要修正的問題）。

### 3.3 `insertEvent()` 每次呼叫都會同步寫入兩張表

```js
// utils/analyticsLog.js insertEvent()
const insertResult = db.run(`INSERT INTO analytics_events (...) ...`);
// ↓ 同一次呼叫，緊接著：
logGeoVisit(db, { store_id, visitor_id, session_id, event_name, ...,
                   source_event_id: insertResult.lastInsertRowid });
```

實測（乾淨測試 DB）：呼叫一次 `insertEvent()`，`analytics_events` 與
`geo_visit_log` 各增加一筆對應資料，欄位（`visitor_id`/`session_id`/
`event_name`/`city`/`district`/`is_unknown`）完全一致。**對於本輪之後的
所有新事件，兩張表天生同步，沒有分歧。**

### 3.4 找到的兩個真正落差（根因）

#### 落差 A：Geo Event Engine 的資料抓取被綁在「非預設」的 UI 狀態後面

`public/js/geo-heatmap-ui.js` 的 `geoHeatUiRegisterContext()` 與
`geoHeatUiSwitchTab()`（切到 Heatmap 分頁時）**只有在使用者手動把 Layer
切到「訪客熱區 Visitor Layer」時**，才會呼叫
`geoVisitorFetchAndRender()` 去打 `/api/analytics/geo/visitor-log`。
Heatmap 分頁預設 Layer 是「訂單熱區 Order Heatmap」——如果使用者從未手動
切換過，Geo Event Engine 的 API **從來沒有被呼叫過**，畫面上任何顯示
Geo 訪客數字的地方自然永遠是初始值（0 或空）。

這完全可以解釋「Analytics 已經有資料，但 Geo Intelligence 顯示 0」的
症狀——**不是資料源錯誤，是這個 UI 路徑從未主動抓過資料**。

**已修正**：`geoHeatUiRegisterContext()` 與 `geoHeatUiSwitchTab()` 改成
只要 Heatmap 分頁是啟用狀態，不論目前顯示哪個 Layer，都主動呼叫一次
`geoVisitorFetchAndRender()`，確保 Geo Event Engine 的資料一律是最新的。

#### 落差 B：歷史資料缺口（Backfill Gap）

`geo_visit_log` 的同步 hook 是 R5.3-A1.2 才加入 `insertEvent()` 的。任何
在這個 hook 存在**之前**就已經寫進 `analytics_events` 的事件，從來沒有
機會觸發過這個 hook，所以完全沒有對應的 `geo_visit_log` 列。如果一個
商店在導入這個 hook 之前已經累積了 Analytics 資料，Dashboard 讀
`analytics_events`（看得到全部歷史資料），但 Geo Intelligence 讀
`geo_visit_log`（只看得到 hook 部署之後累積的資料）——兩者天生會不一致，
直到有新事件進來為止。

**已修正**：新增一次性、冪等的 `scripts/backfill-geo-visit-log.js`，用
既有的 `logGeoVisit()`（跟即時寫入路徑同一個函式，不是第二套邏輯）把
`analytics_events` 裡「還沒有對應 `geo_visit_log` 列」的歷史事件補寫
一次。新增 `geo_visit_log.source_event_id` 欄位（additive migration）
追蹤「這筆是同步自哪一筆 analytics_events」，讓這支腳本可以重複執行、
不會重複計數。

## 4. 是否需要把 Geo Intelligence 的 KPI 改為直接查 `analytics_events`？

**稽核結論：不需要，而且這麼做會違反需求本身列出的限制。**

- 第 3.1～3.3 節已證實：`geo_visit_log` 與 `analytics_events` 對於所有
  新事件是**同步的**（同一次 `insertEvent()` 呼叫寫入兩張表，用同一份
  Geo 解析結果、同一套 `visitor_key` 去重規則）。這不是「兩套不同的
  統計來源」，而是「一套來源、兩個用途不同的儲存位置」——
  `analytics_events` 是完整事件明細，`geo_visit_log` 是攤平過、針對 Geo
  查詢優化過索引的**衍生快取**。
- 若改成「Visitors/AddToCart/Checkout/Orders/Conversion 全部直接對
  `analytics_events` 建立新查詢」，等於要重新實作一套跟
  `utils/geoEventEngine.js` 平行的聚合／去重／時間範圍／Store Isolation
  邏輯——這正是需求文件本身禁止的「重複建立第二套統計邏輯」。
- 真正的根因是上面找到的落差 A（UI 從未主動抓取）與落差 B（歷史資料沒
  backfill），兩者都已經用**最小、不新增統計邏輯的方式**修正。

`geo_visit_log` 的定位維持不變：它是 Analytics Event Engine（`insertEvent()`）
的 **Geo 專用衍生快取**，供 Recent Geo History／Geo Timeline／Geo
Ranking／Choropleth 查詢使用，**本來就不是**獨立於 Analytics 之外的
第二套系統——它的每一筆資料都可以用 `source_event_id` 追溯回對應的
`analytics_events` 列。

## 5. Single Source of Truth 現況

```
使用者行為
   │
   ▼
insertEvent()  ← 唯一寫入進入點（utils/analyticsLog.js）
   │
   ├──► analytics_events（完整事件明細，Dashboard／Funnel／購物車分析／
   │                       AI Analytics／ROI Analytics 皆讀這裡）
   │
   └──► geo_visit_log（Geo 專用衍生快取，攤平＋預先索引，Geo Intelligence
                        的 Visitors/AddToCart/Checkout/Orders/Revenue/
                        Conversion/Cart Abandonment 皆讀這裡，但資料來源
                        與寫入時機跟 analytics_events 完全同步）
```

Dashboard、Funnel、Geo Intelligence 目前都只有一個共同的寫入源頭
（`insertEvent()`），符合「必須共用同一套 Analytics Event Engine」的
要求。AI Analytics／ROI Analytics 目前也是讀 `analytics_events`（本輪
未變更、未新增查詢邏輯），同樣共用同一個寫入源頭。
