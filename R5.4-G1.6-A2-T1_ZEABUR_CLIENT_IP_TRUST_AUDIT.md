# G1.6-A2-T1 — Zeabur Client IP Trust Verification

範圍：**只驗證信任模型，不啟用 IP Geo Provider**。
`GEO_VISITOR_IP_ENABLED` 全程維持 `false`（未變更）。
本階段沒有新增第二套 Resolver／Provider／Cache——`utils/clientIpTrustDiagnostic.js`
是唯讀分類工具，獨立於 `utils/geoSanitizer.js`／`utils/geoResolver.js`，
彼此互不呼叫、互不耦合。

## 正式 Zeabur 部署實測結果（G1.6-A2-P1 回填）

以下為使用者在真實 Zeabur 正式環境，透過
`GET /api/admin/geo/client-ip-trust-diagnostic`（Super Admin Token）
實際測試取得的結果。不含任何 raw IP／Token／完整 Header。

**Phase 1 — `TRUST_PROXY` 未設定（基準行為）**
```
trust_proxy_configured=false
socket_ip_scope=private
req_ip_scope=private
xff_present=true
xff_hop_count=1
xff_hop_scopes=["public"]
```
解讀：Zeabur 確實在 Node process 前面有一層代理（`socket_ip_scope=private`
證實這不是直接暴露），且該代理**有**附加一段公開的 XFF（`xff_hop_scopes=
["public"]`），但因為 `TRUST_PROXY` 未設定，Express 不採信任何 XFF，
`req.ip` 因此退回 `socket.remoteAddress`（代理自己的內部位址），維持
`private`。這正是 A2-T1 稽核當時預期的「安全但看不到真實訪客 IP」狀態。

**Phase 2 — `TRUST_PROXY=1`**
```
trust_proxy_configured=true
socket_ip_scope=private
req_ip_scope=public
xff_present=true
xff_hop_count=1
xff_hop_scopes=["public"]
```
解讀：設定 `TRUST_PROXY=1` 後，Express 採信 XFF 最右（第 1 跳）的值作為
`req.ip`，且該值分類為 `public`——**代表 Zeabur 只有單一代理跳數，且該
代理層會把自己的位址與訪客真實位址分開處理**，`req.ip` 成功變成公開的
訪客 IP。`socket_ip_scope` 維持 `private` 不變，符合預期（代理本身的
TCP 連線來源）。

**Single Sentinel Spoof Test**
（客戶端自行帶入 `X-Forwarded-For: 198.51.100.77`，`TRUST_PROXY=1`）
```
xff_hop_count=2
xff_hop_scopes=["public","public"]
sentinel_seen=true
sentinel_position=leftmost
req_ip_is_sentinel=false
```
解讀：Zeabur **附加**（append）而非**覆寫**（overwrite）客戶端自行塞入的
XFF——客戶端偽造的 Sentinel 被保留在 leftmost，變成 2 段；但 Zeabur 自己
在 rightmost 又加上了一段（訪客真實 IP）。Express 在 `TRUST_PROXY=1`
下只信任「最右第 1 跳」，也就是 Zeabur 自己附加的那一段，**不會**採信
客戶端偽造的 leftmost 值。`req_ip_is_sentinel=false` 證實偽造無效——
符合本文件三、判定情況 B（「Sentinel 被保留在 leftmost，但 req.ip 使用
Zeabur 附加的 rightmost Client IP」）。

**Network Fingerprint 比對**
```
家用 Wi-Fi：      f4e180aa9ec185d5
手機 4G／5G 熱點： 40af5719fe85858b
```
兩者不同，符合預期（不同網路出口 IP → 不同 fingerprint，且 fingerprint
本身不可逆推回真實 IP，process 重啟即失效）。

## 已完成（本機驗證，非 Zeabur 正式環境）

用真實啟動的 server（`npm ci` + `node server.js`）與真實 HTTP 請求測試，
不是只跑單元測試：

1. **未帶 Token** → `401`（Super Admin 驗證正確擋下）
2. **帶無效 Token** → `401`
3. **帶合法 Super Admin Token + Sentinel XFF**
   （`X-Forwarded-For: 198.51.100.77, 203.0.113.5`，`TRUST_PROXY` 未設定）：
   ```json
   {
     "trust_proxy_configured": false,
     "trusted_header_configured": false,
     "socket_ip_family": 4,
     "socket_ip_scope": "loopback",
     "req_ip_family": 4,
     "req_ip_scope": "loopback",
     "xff_present": true,
     "xff_hop_count": 2,
     "xff_hop_scopes": ["public", "public"],
     "sentinel_seen": true,
     "sentinel_position": "leftmost",
     "req_ip_is_sentinel": false,
     "ephemeral_client_fingerprint": "7a28b2f28320dd43",
     "process_started_at": "..."
   }
   ```
   **結論（本機）**：`TRUST_PROXY` 未設定時，即使客戶端在 XFF 塞入
   Sentinel，`req.ip` 完全不受影響（本機測試環境下等於
   `socket.remoteAddress`，即 `127.0.0.1`）。這證明**目前程式碼的預設狀態
   本身對 XFF 偽造免疫**——風險只會在「日後為了取得真實 IP 而貿然設定
   `TRUST_PROXY` 或 `GEO_TRUSTED_IP_HEADER`，卻沒先確認 Zeabur 拓樸」時
   才出現，而這正是本階段要防止的事。

## 尚未完成（必須在 Zeabur 正式部署上執行，本機無法替代）

以下項目**只能**在真實 Zeabur 正式部署，透過
`GET /api/admin/geo/client-ip-trust-diagnostic`（Super Admin Token）
實際發送請求後才能取得，本機環境（無 Zeabur 反向代理）無法模擬：

- [ ] Zeabur 正式部署的 `socket_ip_scope`／`socket_ip_family`
      （用來確認 Zeabur ingress 是否確實以代理形式連進 Node process，
      預期應為 `private`，若為 `public` 代表沒有代理層，或代理未正確轉發）
- [ ] Zeabur 正式部署的 `req_ip_scope`
      （**Gate 判定的核心欄位**——在 `TRUST_PROXY`／`GEO_TRUSTED_IP_HEADER`
      都未設定的狀態下，這裡會是 `socket.remoteAddress` 的分類；若已是
      `public`，代表 Zeabur 未加代理層或 Node 直接暴露）
- [ ] Sentinel Spoof Test 在真實 Zeabur 環境的結果
      （用外部工具例如 `curl -H "X-Forwarded-For: 198.51.100.77" ...`
      對正式網域發送請求，觀察 `sentinel_position` 與 `req_ip_is_sentinel`）
- [ ] `xff_hop_count` 在正式環境的實際跳數（Zeabur 是否只加一層、還是
      客戶端塞的內容會原封不動被轉發到最後）
- [ ] Wi-Fi／行動網路／無痕視窗四組真實請求的 `ephemeral_client_fingerprint`
      比對（見 MANUAL_TEST_CHECKLIST）

## Zeabur 官方文件現況

Zeabur 官方文件表示其 ingress／edge 網路使用標準 `X-Forwarded-For`
機制轉發客戶端來源 IP。**但本文件不能只憑官方文件宣稱就判定安全**——
文件不會告訴我們：這一層是否會「覆寫」客戶端自己塞入的 XFF 內容，
還是單純「附加」在後面；也不會告訴我們實際的代理跳數是 1 跳還是多跳
（例如若專案另外還接了 Cloudflare，跳數會不同）。這正是本階段要求
「以本次正式部署測試為準」的原因，不接受僅憑文件推定。

## 目前推薦設定（暫定，待正式測試結果修正）

- `TRUST_PROXY`：**暫不設定**（維持 `false`）。待 Zeabur 正式測試證實
  「只有一層可信代理、且該層會正確覆寫/附加 XFF」後，才評估設為 `1`。
- `GEO_TRUSTED_IP_HEADER`：**不設定**。目前沒有平台文件佐證 Zeabur
  提供任何客戶端無法覆寫的專屬 header（不像 Cloudflare 的
  `CF-Connecting-IP`），在有這類佐證前不得設定。

## Gate 判定（更新後）

**Client IP Trust Gate：PASS**

1. req.ip 是 Public Client IP — **PASS**（`TRUST_PROXY=1` 時
   `req_ip_scope=public`，Phase 2 實測證實）
2. Wi-Fi／行動網路 fingerprint 合理不同 — **PASS**
   （`f4e180aa9ec185d5` vs `40af5719fe85858b`）
3. socket 維持代理特徵但 req.ip 隨 Client 改變 — **PASS**
   （`socket_ip_scope` 在 Phase 1/2 都維持 `private`，`req_ip_scope`
   隨 `TRUST_PROXY` 設定改變，且會隨網路環境變化）
4. 偽造 Sentinel 不會成為 req.ip — **PASS**（Zeabur 附加而非覆寫 XFF，
   Express 只信任最右 1 跳，`req_ip_is_sentinel=false`）
5. Proxy hop count 穩定 — **PASS**（`xff_hop_count=1`，Sentinel 測試中
   變成 2 段但這是客戶端自己加的，Zeabur 自身固定只加 1 跳）
6. 不需要信任任意客戶端 Header — **PASS**（結論不需要
   `GEO_TRUSTED_IP_HEADER`，`TRUST_PROXY=1` 配合 Zeabur 固定單跳代理即可
   取得 Public IP，不必額外信任任何 Header）
7. 診斷未洩漏 raw IP — **PASS**
8. Security Scan PASS — **PASS**（12/12 Smoke，`node --check` 全過，
   401/401/200 權限驗證正確）

**正式推薦設定**：
```
TRUST_PROXY=1
# GEO_TRUSTED_IP_HEADER 不設定
```

## 重要：這是 Trust Gate PASS，不是 Provider Gate PASS

`TRUST_PROXY=1` 一旦寫入 Zeabur 正式環境變數，代表**日後**
`GEO_VISITOR_IP_ENABLED=true` 時，`resolveVisitorGeoCached()` 會拿到真實
訪客 Public IP 去查 Provider——但這件事本身**不會**因為這份文件更新就
發生：`GEO_VISITOR_IP_ENABLED` 與 `GEO_VISITOR_IP_PROVIDER` 依然是
`false`／`disabled`，是否啟用完全取決於 Provider Source Gate（見
`R5.4-G1.6-A2-P1_PROVIDER_COMPARISON.md`）判定結果与使用者決定。

## 已執行的自動化驗證

- `node --check` on `utils/clientIpTrustDiagnostic.js`、
  `routes/admin-geo-diagnostic.js`、`server.js` — 全部通過
- `scripts/smoke-g1-6-a2-t1-client-ip-trust-diagnostic.js` — **12/12 PASS**
  （scope/family 分類、sentinel 三種位置、fingerprint 穩定性與差異性、
  輸出欄位白名單、raw IP 不外洩、trusted_header 只回布林值）
- 真實 server 啟動 + HTTP 請求驗證：401（無 token）／401（無效 token）／
  200（合法 Super Admin token）皆符合預期

## 下一步

需要你（或有權限的人）在 Zeabur 正式部署上，用 Super Admin Token 呼叫
`GET /api/admin/geo/client-ip-trust-diagnostic`，並依
`R5.4-G1.6-A2-T1_MANUAL_TEST_CHECKLIST.md` 執行四組網路環境測試 + Sentinel
Spoof Test，把結果回填後我再依此判定 `TRUST_PROXY` 最終建議值與
Gate 是否可以 PASS。**在此之前不會設定/建議啟用任何 Provider。**
