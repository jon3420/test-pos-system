# R5.4-G1.6-GA4-H1.2-UNIQUE-ADMIN — Manual QA Checklist

**狀態：以下全部項目預設 NOT TESTED（本輪為自動化 QA 交付，尚未部署到正式環境）。**

部署後請依序執行並逐項回填 PASS／FAIL：

| # | 項目 | 狀態 |
|---|---|---|
| 1 | 部署 H1.2（`fix18-10-hotfix30-B5-R5.4-G1.6-GA4-H1.2-UNIQUE-ADMIN-QA-full.zip`） | NOT TESTED |
| 2 | Ctrl+Shift+R 強制重新整理 | NOT TESTED |
| 3 | 重新登入 POS | NOT TESTED |
| 4 | `GET /api/geo-live/ga4-realtime` 回應 HTTP 200 | NOT TESTED |
| 5 | 回應 `success=true` | NOT TESTED |
| 6 | 產生／等待 GA4 出現 `Pingzhen District` | NOT TESTED |
| 7 | 確認回應 `summary.mapped_counties` 正確增加（該筆不再算入 unmapped） | NOT TESTED |
| 8 | 確認回應 `summary.unmapped_city_rows` 不含 Pingzhen District | NOT TESTED |
| 9 | 確認對應 county 的 `county_code === "68000"` | NOT TESTED |
| 10 | 確認對應 county 的 `county_name === "桃園市"` | NOT TESTED |
| 11 | 產生／等待 GA4 出現 `Yangmei District`，確認映射桃園市 | NOT TESTED |
| 12 | 產生／等待 GA4 出現 `Banqiao District`，確認映射新北市 | NOT TESTED |
| 13 | 確認未知／非白名單、非唯一名稱仍出現在 `unmapped`（不被誤映射） | NOT TESTED |
| 14 | 確認非台灣（非 TW）資料出現在 `excluded_non_tw_rows`，不進入地圖 | NOT TESTED |
| 15 | 確認地圖圖層仍為**縣市級聚合**顯示（不是行政區精確 Marker，不宣稱 GPS） | NOT TESTED |
| 16 | 確認 GA4 Auth（H1.1）仍正常，`/api/geo-live/ga4-realtime` 未回 401 | NOT TESTED |
| 17 | 瀏覽器 Console 無本輪引入的新錯誤（尤其 `taiwanGeoNormalize`／`ga4Realtime` 相關） | NOT TESTED |

---

## Pingzhen Production Gate（正式 Gate，需部署後實測才可標記）

只有在**部署後**再次向 `GET /api/geo-live/ga4-realtime` 取得包含 `Pingzhen District` 的真實資料，且回應確認：

```
mapped_counties    = 1（該回應中對應的合理數字，視同時段實際資料而定，但 Pingzhen 這筆不再落在 unmapped）
unmapped_city_rows 不含 Pingzhen District
county_name        = 桃園市（對應 county_code = 68000）
```

才可將以下欄位由 `READY FOR MANUAL DEPLOYMENT VERIFICATION` 改標為 `PASS`：

- **Pingzhen Production Gate**：目前狀態 = `READY FOR MANUAL DEPLOYMENT VERIFICATION`

在此之前，即使自動化 Aggregator 測試（本文件所屬交付已完成、82/82 PASS）已經用正式回報的原始數字（`activeUsers=1, eventCount=2`）重現過同樣結果，**仍不得**在本檔案標記 Production Gate 為 PASS——自動化重現證明的是「邏輯正確」，Production Gate 證明的是「部署後真實環境確實如此運作」，兩者不可互相取代。

---

## 邊界重申（避免部署人員誤解）

- 本輪修正後的地圖圖層依然是 **County Aggregation Layer（縣市聚合層）**，`Pingzhen`／`Yangmei`／`Banqiao` 等鄉鎮市區流量最終都會顯示成其所屬縣市（桃園市／新北市）的加總數字，**不會**出現行政區邊界或行政區層級的獨立色塊/Marker。
- 不代表個別訪客的精確位置，不代表 GPS 座標。GA4 位置本身即為 Google 端 IP 推估。
- 本輪**沒有**修改 H1 的行政區精確點位圖層（A1.2 Authoritative Admin Point Catalog／`geo-ga4-h1-panel.js`），也**沒有**把舊即時圖層升級成行政區精確點位圖層。
- Store Isolation：`SINGLE_STORE_ONLY`（沿用既有架構，本輪未變更）。
- POS 個別訪客地圖映射：`NOT IMPLEMENTED` / `OUT OF SCOPE`（本輪未實作，不在範圍內）。
