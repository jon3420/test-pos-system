# H1.4.3 GA4 Data Reconciliation Report

## Part A — Automated Synthetic Reconciliation (completed this round)

This section is produced entirely from `scripts/run-g1-6-ga4-h1-4-3-data-lineage-runtime.js`,
run against a real temporary SQLite database and the real production
service code — not against any hardcoded UI reference number.

**Exact range under test:** `2026-08-03` ～ `2026-08-09`

**Property / Store:** synthetic test store `store_001`, GA4 property
`111111111`, stream `211111111` (test fixtures only — not a real property).

**Metric:** `activeUsers` (confirmed as the only field written to the
persisted `active_users` column — see Reality Audit §1 and static audit
Category D; `totalUsers`/`sessions`/`newUsers`/`events` are never written
into that column).

**Dimension:** `country` + `region` + `city` (GA4 audience report
dimensions), resolved through `normalizeGa4Location()` into
Taiwan county/district identity where possible.

**Fixture → pipeline trace:**

| Location | Raw (GA4 fixture) | Normalized | Persisted (`active_users`) | Heatmap read | Dashboard read |
|---|---|---|---|---|---|
| Zhongli District (中壢區) | 41 | county=桃園市, district=中壢區, status=ok | 41 | 41 | 41 |
| Taoyuan District (桃園區) | 46 | county=桃園市, district=桃園區, status=ok | 46 | 46 | (not separately re-asserted; same read path) |
| Banqiao District (板橋區) | 32 | county=新北市, district=板橋區, status=ok | 32 | 32 | (same read path) |
| Japan / Tokyo / Shibuya | 3 | status=overseas_or_other | 3 | 3 (label disambiguated) | — |
| United States / California / Los Angeles | 2 | status=overseas_or_other | 2 | 2 (label disambiguated) | — |
| (not set) / (not set) / (not set) | 1 | status=unknown | 1 | 1 | — |

**Cross-view identity check (中壢區, this exact range):**

- Heatmap `GET /history` read: `active_users = 41`
- Dashboard `GET /history` read (independent call, same range): `active_users = 41`
- Both rows trace to the **same** `synced_at_utc` and the **same**
  `raw_location_key` (`taiwan||taoyuan city||zhongli district`) — confirmed
  identical, not merely coincidentally equal.
- Rendered output check: `geoGa4H1BuildTooltip()` (Heatmap) and
  `_geoDashboardGa4BuildTooltip()` (Dashboard) — both real production
  render functions — each produce text containing "活躍使用者：41" for this
  row.

**No cross-snapshot summation check:** a separate `today` sync was run for
the same district with a deliberately unrelated fixture value
(`active_users=999`). Re-reading the original 7-day range afterward still
returned `41` — confirming the 7-day value is not derived by adding daily
snapshots together, and that different range identities are fully
isolated from each other in storage and read.

**Marker coverage vs. value check:** of the 6 fixture rows, 3 (the Taiwan
districts) received a `marker_point` and 3 (2 overseas + 1 unknown) did
not — confirming that lacking a marker is a resolvable-coverage question,
independent of whether a row's `active_users` value is correct. The 3 rows
that did get markers retained their correct values.

**Conclusion of Part A:** for a controlled, fully-known exact range and
snapshot, the automated pipeline is internally consistent end-to-end:
Raw → Normalized → Persisted → Heatmap → Dashboard all report the same
number, traceable to the same persisted row.

## Part B — Production Manual Reconciliation: PENDING

This section cannot be completed automatically — it requires a live
Google Analytics UI session and a live deployment of H1.4.3, with a human
setting the same date range in all three surfaces.

**Do not use this section to draw pipeline conclusions until the ranges are
confirmed identical.** The originally-reported screenshots already show a
range mismatch on their face: Google Analytics was set to
`2026/08/03 ～ 2026/08/09`, while the POS Dashboard screenshot shows
`GA4 區域概況｜近7天` for `2026/08/04 ～ 2026/08/10` — a different window by
construction (Dashboard's default "近7天" as of 2026-08-10 correctly
computes 08/04–08/10; it was never re-pointed at the same custom range as
the Google UI). **A value difference between non-identical date windows is
expected, not a defect.**

### Manual re-verification procedure (to run after deployment)

1. Google Analytics UI → set custom range `2026/08/03` ～ `2026/08/09`.
2. POS Dashboard → GA4 Range Control → 自訂 → `2026/08/03` ～ `2026/08/09`.
3. POS Heatmap → GA4 Historical → 自訂 → `2026/08/03` ～ `2026/08/09`.
4. For each of the three surfaces, record: `range_start`, `range_end`, and
   the visible sync timestamp (`synced_at`) — not just the numeric value.
5. Only compare numeric values across surfaces where `range_start`,
   `range_end`, **and** `synced_at`-derived snapshot identity all match.

### Manual QA Reference (Google Analytics UI screenshot, for human
comparison only — not hardcoded into any production code or automated test)

| District | Google Analytics UI — Active Users |
|---|---|
| Taoyuan District (桃園區) | 46 |
| Zhongli District (中壢區) | 41 |
| Banqiao District (板橋區) | 32 |
| Luzhu District (蘆竹區) | 11 |
| Guishan District (龜山區) | 10 |
| Yangmei District (楊梅區) | 10 |
| Zhongshan District | 8 (verify country/city context before assuming this is Taipei's 中山區 — see Reality Audit §6 on region/city ambiguity; do not assume without checking) |

Range for the table above: `2026/08/03 ～ 2026/08/09`.

**Status: PENDING MANUAL DEPLOYMENT VERIFICATION.** This table exists only
to support the human reconciliation pass above. It must never be copied
into production code, and must never become a hardcoded "expected" value
in any automated test.
