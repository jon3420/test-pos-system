# H1.4.10 Phase 4B — RECOVERY CONSENT × LINE PUSH DELIVERY Reality Audit

## 核心原則（貫穿整個 Phase 4B）

Phase 4A 回答「誰尚未完成？」。Phase 4B 回答「這個人是否可以合法、安全地透過 LINE 提醒？」。Recovery Job 存在**不代表**可以 Push。以下十六點是這輪反覆驗證、且部分推翻最初假設後才確立的架構事實。

## 1. Recovery Job ≠ Push Permission

`cart_recovery_jobs` 只是狀態機（Phase 4A）。要真的發 LINE Push，必須同時滿足 `evaluateLineRecoveryEligibility()` 的全部 14 項條件：master enabled、LINE enabled、job due、job pending/waiting、attempt<max、consent granted、可信 line_user_id、好友、channel token 存在、LIFF ID 存在。任一不成立即拒絕，理由碼白名單化，不外洩內部細節。

## 2. Friend ≠ Consent；3. LINE Login ≠ Consent；4. Add Friend Click ≠ Consent

四件事完全分開，各自獨立的資料表/欄位：LINE 身分辨識（Phase 1 `member_session`）、加好友引導（Phase 2 `friend_entry`/`friend_checkout`，只是引導 UI，不代表已加好友）、Recovery consent（本輪新增 `cart_recovery_consents`，顧客主動勾選）、實際 Push（`utils/linePush.js` + eligibility）。Consent checkbox 預設永遠 unchecked。

## 5. member_session 是唯一 client→server identity authority

`routes/cart-recovery.js` 的每一支 POST 端點都只信任 `verifyMemberSession(member_session, storeId)` 解析出的 `line_user_id`，即使 client 夾帶 `line_user_id`/`order_id`/`cart_id`/`total`/`amount` 也完全忽略（結構上不讀取，不是「讀了但覆蓋」）。

## 6. Cart/Checkout Restore 需要 Authoritative Snapshot

`resolveRestorableCartSnapshot()` 只信任 `cart_updated`/`cart_restored` 的完整快照（`utils/cartSnapshot.js`），找不到就誠實回 `restorable:false`，Processor 絕不假裝購物車還在。

## 7. Legacy Add/Remove 估算只供 Analytics，不供 Restore

`getLegacyCartItemsMap()`（SUM(add_to_cart)−SUM(remove_from_cart)）繼續給 Dashboard 估算使用，但 Recovery Restore 明確拒絕使用它——它無法反映商品被替換/合併後的真實最終狀態。

## 8. Variant 目前 Defined-but-never-populated

Reality Audit 確認 `public/line-order.html` 的 `_buildCartTrackingItems()`（`cart_updated` metadata 唯一寫入來源）永遠 hardcode `variant:null`，全專案沒有任何一處賦予真實 variant 值，`recomputeCart()` 也沒有 variant-aware 邏輯。`resolveRestorableCartSnapshot()` 因此 fail-closed：任何非空 variant → `restorable:false, reason:'unsupported_variant'`，不 Push。

## 9. Payment Recovery 恢復 Existing Order，不建立第二張 Order

`payment_abandoned` 的真實情況是 `submit_order` 已經存在。`POST /api/cart-recovery/resume-payment` 只對 token 綁定的既有 `orders` row 發起新的 LINE Pay Request（`utils/linePayService.js`），完全不呼叫任何建立訂單的 API，不接受 client 提供的 `order_id`/`total`/`items`。

## 10. Recovery payment_started 由 Backend Authority 寫入

Recovery Payment 頁本來就不 restore 購物車，前端可能完全沒有正確的 `cart_id`。改為 `routes/cart-recovery.js` 的 `/resume-payment` 在 LINE Pay Request 真正成功後，用 token 綁定的 `cart_id`/`order_id`（server-side，非 client 提供）呼叫 `logServerEvent()`，metadata 只有 `payment_method:'linepay'`。Frontend 完全不重複送 `payment_started`。

## 11. sent 必須可以 Converted

第一輪實作時 `markRecoveryConverted()` 的 `WHERE status IN ('pending','waiting')` 遺漏了 `'sent'`——這代表「提醒已送出但顧客之後才成交」這個 Phase 4B 商業上最重要的情境，job 會永遠卡在 `sent`，Reminder Conversion Rate 永遠算不出來。已修正為包含 `'sent'`（`failed`/`not_contactable`/`not_configured` 刻意不動，保留原始 delivery outcome）。

## 12. 成交後 Recovery Token 立即失效

`invalidateRecoveryResumeTokens()` 掛在同一個 centralized `onAnalyticsEvent()` 的 purchase/payment_success 分支，不在 `routes/linepay.js`/`routes/line-orders.js`/`routes/line-shipping.js` 各寫一份。只作用於 `purpose='recovery_resume'`，完全不觸碰既有 checkout handoff token。`POST /restore` 另外做 defense-in-depth：直接查對應 job 最新狀態，只有 `status='sent'` 才允許 restore（`pending`/`converted`/`cancelled` 一律拒絕，理由統一為 `already_completed`）。

## 13. Recovery Frontend 必須在 Normal restoreCart() 之後執行

這是本輪 Reality Audit 抓到的第二個真實 bug：最初 `_initLineMemberGateFromShopData(shopData).then(handleRecoveryResumeFromUrl)` 在既有正常 `restoreCart()`（hotfix22-F，還原永久保留的本地購物車）**之前**執行，若 Recovery 先還原，後面的正常 restoreCart() 會把它蓋掉。修正為：先 `await` 既有正常 `restoreCart()`，再 `await` member gate init promise，最後才觸發 Recovery bootstrap，讓 Recovery（如果有）成為最後、權威的購物車狀態。用專門的 ordering test（預先塞一個 qty=9 的舊本地購物車，Recovery 回應 qty=5，驗證最終確實是 qty=5）驗證過。

## 14. Recovery 不強迫 liff.login()

沿用 Phase 1 Passive Auto Identify／既有 member_session。沒有可信身分時只顯示錯誤提示，不呼叫 `liff.login()`、不 fallback 到 `visitor_id`、不把 token 本身當身分依據。一般點餐功能完全不受影響。

## 15. Phase 4B 沒有 Scheduler

`processDueLineRecoveryJobs()` 只是「可以被呼叫」的 service function，Phase 4B 沒有接 cron/setInterval/unauthenticated process endpoint。

## 16. n8n 留 Phase 4C

本輪完全沒有觸碰任何排程觸發機制，`processDueLineRecoveryJobs()` 由誰在什麼時機呼叫留給下一階段決定。

## 額外發現：LINE Pay Signing SSOT 與 Secret 洩漏

- 第一輪重構後 `routes/linepay.js` 與 `utils/linePayService.js` 各自定義了一份幾乎一樣的 LINE Pay v3 簽章公式（`channelSecret+uri+body+nonce`），違反單一 SSOT 原則。統一抽到 `utils/linePayClient.js`，`routes/linepay.js`／`utils/linePayService.js`／`routes/payment-gateways.js` 三處全部改為 import，不再各自定義。
- 過程中發現 `routes/linepay.js` 既有的連線測試 debug log 原本印出 `messagePreview: message.slice(0,60)`——`message` 正是簽章 preimage，開頭就是完整 Channel Secret，等於把商家的 LINE Pay 密鑰印進 server log。已移除 `signLinePayPost()`/`signLinePayGet()` 回傳值中的 `message` 欄位（呼叫端本來就不需要，也不該拿得到）。

## 測試 Harness 教訓（不影響 production，但影響本輪如何驗證 production）

- 最初的 Payment Resume Concurrency Guard `WHERE status IN ('bound','opened','payment_requested')` 允許 `payment_requested` 被重新 claim，這是第一輪並發測試抓到的**真實** production bug（不是測試巧合），修正為只允許 `('bound','opened')`，另外新增獨立的 `POST /payment-cancelled` 明確處理「取消後允許重試」，不靠放寬 claim 條件。
- Frontend jsdom test harness 最初把 Recovery API 回應在 `resetRecoveryApiState()` 之後才設定，導致 mock 被自己洗掉；`_trackEvent` spy／`liff.login` spy 掛載時機太晚，會漏掉 bootstrap 早期的呼叫。重寫為：回應在 `new JSDOM()` 之前就準備好、per-環境 state（不共用全域 counter）、Analytics 攔截在 HTTP 層（`/api/analytics/events`）而非晚掛的 `_trackEvent` spy、`liff` stub 在 `beforeParse` 就存在。這些全部是 harness 修正，過程中也額外抓到一個真實的 double-click production bug（DOM `disabled` 屬性本身不足以阻止 jsdom 合成事件重複觸發 handler，已在兩頁的付款按鈕 handler 加上明確的 re-entrancy guard）。
