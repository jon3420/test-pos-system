# H1.4.10 Phase 4D — RECOVERY ANALYTICS DASHBOARD Reality Audit

**狀態：WIP ANALYTICS DASHBOARD ｜ AUTOMATED CORE ACCEPTANCE PASS（59/59）｜ MANUAL QA PENDING**

這是刻意的 WIP（work-in-progress）標記，不是 DONE。目前只交付並驗證了 overview 端點的核心正確性（funnel／resolved metrics／TTR／order-based revenue dedup／JWT store isolation）與最小的 Admin UI 唯讀渲染，兩者都有真實 runtime test 覆蓋。Cohort/Segment、CSV 匯出、趨勢圖、真實資料量下的效能與人工對帳，全部**未實作或未驗證**，見下方與 `H1.4.10_PHASE4D_RECOVERY_DASHBOARD_MANUAL_QA.md`。

## Scope Decision（誠實記錄）

本輪 Phase 4D 刻意只交付一個**最小可用、完全真實、無估算**的垂直切面：`GET /api/cart-recovery-dashboard/overview` 加上一張只讀的 Admin UI 摘要表。Cohort/Segment 分析、CSV 匯出、更完整的漏斗視覺化、時間序列趨勢圖，全部**未實作**，不在本輪範圍內，也沒有假裝已經做了。這是刻意的範圍控制決策：與其在有限時間內攤開做十件事、每件都做得不完整或未驗證，不如把一件事做到「數字是真的、不是猜的、每個欄位都有測試證明」。

## 1. Order ID Linkage 是本輪設計的關鍵限制

Audit `utils/cartRecovery.js` 確認：`cart_abandoned`／`checkout_abandoned` 這兩個 stage 的 job，`submit_order` 事件發生時只會呼叫 `cancelRecoveryJobs()`（取消），不會像 `_scheduleStage()` 的 refresh 路徑那樣回填 `order_id`——因為對這兩個 stage 而言，`submit_order` 從語意上就是「結束」而不是「refresh」。這代表：

- `cart_abandoned`／`checkout_abandoned` 的 job row 幾乎不會有 `order_id`，無法可靠 JOIN `orders` 表算出「這筆提醒真的追回多少營收」。
- `payment_abandoned` 不同：job 建立時（`schedulePaymentRecovery`，來自 `payment_started` 事件分支的 `resolveAuthoritativeOrderForPaymentStart()`）一定帶著 server-authoritative 的 `order_id`，可以可靠 JOIN。

**因此本輪 Recovered Revenue 只對 `payment_abandoned` 計算，`cart_abandoned`／`checkout_abandoned` 明確回傳 `revenue: null, reason: 'no_reliable_order_linkage'`**，不用任何估算公式（例如「假設平均客單價」之類）去填補空缺。前端也原樣顯示「無法計算」，不在前端層另外編一個數字出來。

## 2. Conversion Rate 分母排除「還在進行中」的 job

`conversion_rate_of_resolved` 只計算「已經有最終結果」的 job（`converted`/`cancelled`/`failed`/`not_contactable`/`not_configured`），刻意排除 `pending`/`waiting`/`sent`——因為這些狀態代表「還沒真的知道結果」，如果被算進分母，會讓成交率被大量「還在等待中」的 job 稀釋、產生誤導性的低數字。

## 3. Time to Recovery 只對「真的送過提醒且之後轉換」的 job 計算

`avg_minutes_to_recovery` 限定 `channel='line' AND sent_at<>'' AND status='converted' AND converted_at<>''`，用 SQLite `julianday()` 做精確的時間差計算（已用一組刻意控制在 10 分鐘差距的測試資料驗證，結果精確等於 10，不是約略值）。沒有符合條件的樣本時回傳 `null`，不是 `0`——`0` 分鐘會被誤讀成「秒買」，是比沒有資料更糟的誤導。

## 4. Reminder → Converted ≠ 因果關係

`reminder_then_converted` 只是「這個 job 曾經被標記 sent，後來也 converted 了」的**狀態關聯**，不是嚴謹的因果推論（無法排除「反正本來就會買」的可能性，需要 A/B 測試或其他實驗設計才能做因果推論，這遠超本輪範圍）。Admin UI 文案與後端註解都刻意用「成交率」而非「歸功於提醒的轉換率」這種暗示因果的措辭。

## 5. 只讀、不寫入

`routes/cart-recovery-dashboard.js` 整支路由只有 `GET`，沒有任何寫入操作，`requireStaffJwt` 保護，沿用既有 `routes/line-analytics.js` 的驗證慣例（管理者 JWT，非顧客 `member_session`）。

## 6. Test Extraction Tooling 的一個真實教訓（本輪過程中發現並修正）

新增 Admin UI 面板時，`scripts/run-h1-4-10-phase4b-recovery-full-runtime.js` 原本用來擷取「購物車找回」卡片 HTML 片段的非貪婪正則 `<div .../[\s\S]*?<\/div>\s*<\/div>/` 因為卡片內部巢狀 div 變多，在第一組「連續兩個 `</div>`」處就提早結束，導致擷取範圍少了 n8n 那一段。用真正的 tag-balance 掃描（`extractBalancedDiv()`）驗證後確認：**production 的 HTML 本身巢狀結構完全正確**，純粹是測試用的抽取正則太脆弱。已改用真正的括號配對演算法取代，不再依賴「第一次出現連續兩個 `</div>`」這種脆弱假設。這件事本身也提醒一個更廣的原則：字串/正則式的 HTML 抽取在內容變複雜後容易悄悄失效而不觸發任何測試失敗（因為原本沒有任何斷言依賴那段被漏掉的內容），本輪順手把這個抽取邏輯改成不會再犯同樣錯誤的寫法。
