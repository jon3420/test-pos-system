# R5.4-G1.5-B2.5 — District Normalization Reality Audit

fix18-10-hotfix30-B5-R5.4-G1.5-B2.5 — Taiwan District-to-County
Normalization Hotfix

## 1. Longtan District 為何 unmapped

GA4 Realtime `city` 維度對台灣常回傳「行政區」層級的英文名稱（如
`Longtan District`），不是縣市名稱。`utils/ga4Realtime/index.js` 的
`_aggregateCityRows()` 呼叫 `normalizeCounty(city)`——這個函式（
`utils/taiwanGeoNormalize.js`）只認得 `COUNTY_ALIASES` 裡列出的**縣市層級**
別名（`Taoyuan`／`Taoyuan City`／`桃園市`／`桃園縣`等），完全沒有「行政區→
上層縣市」的對照邏輯，所以 `normalizeCounty('Longtan District')` 回 `null`，
該筆 row 掉進 `unmapped`。

## 2. Taoyuan District 為何 unmapped

同上，原因相同：`Taoyuan District` 是行政區層級英文名稱，不在
`COUNTY_ALIASES` 裡，`normalizeCounty()` 回 `null`。

## 3. 現有 canonical county 格式

`data/taiwan-administrative-areas.json` 的 `counties[]` 每筆包含
`county_code`（5 碼字串）、`county_name`（中文全名）、`county_name_en`。
`utils/taiwanGeoNormalize.js` 對外回傳的 county row 就是這個資料集裡的原始
物件（`getCountyByCode()`／`normalizeCounty()` 回傳值），供
`_aggregateCityRows()` 直接取用 `county.county_code`／`county.county_name`
組進 `counties[]` 陣列。

## 4. 桃園市使用的 county_code

`68000`（`getCountyByCode('68000').county_name === '桃園市'`，已在資料集
與既有 G1.5-A/B1 測試中反覆驗證過）。

## 5. 現有 alias map

`COUNTY_ALIASES`（`utils/taiwanGeoNormalize.js`）——縣市層級別名表，含中文
全名、簡體/正體差異（臺／台）、英文（`Taoyuan`／`Taoyuan City`）、舊制名稱
（`桃園縣`）。**刻意不含**裸 `Hsinchu`／`Chiayi`（見第 7 節）。這張表只服務
`normalizeCounty()`，被 GA4 aggregator、以及專案內其他呼叫
`normalizeCounty()`／`resolveTaiwanAdministrativeArea()` 的流程共用（訂單
履行地址解析等）。

## 6. 是否已有 District Parent Map

**修正前：沒有。** `utils/taiwanGeoNormalize.js` 有一套完整的
「縣市＋行政區」雙層解析引擎（`resolveTaiwanAdministrativeArea()`／
`normalizeSubdivision()`），但那套引擎的設計前提是「呼叫端會同時提供縣市
與行政區兩個欄位」（例如訂單履行地址的 `city`+`district`）。GA4 Realtime
的 `city` 維度只給**一個**字串、沒有對應的縣市欄位可以當
`countyHint`——在沒有 `countyHint` 的情況下呼叫
`normalizeSubdivision('Longtan District')`／`normalizeSubdivision('Taoyuan
District')` 會發生什麼事，見下一節（這正是本次稽核最重要的發現）。

## 7. 模糊名稱保護

現有 `normalizeSubdivision(value, countyHint)`：沒有 `countyHint` 時，若
該名稱在全國資料集中對應到**多筆**候選（歧義），回傳 `null`，不猜測
（`candidates.length === 1 ? candidates[0] : null`）。

**重大發現（本輪稽核最重要的部分，必須誠實記錄）**：實際查詢
`data/taiwan-administrative-areas.json` 的結果顯示：

| 查詢字串（normalize 後） | 全國候選 |
|---|---|
| `longtan` | 僅一筆：桃園市龍潭區 |
| `longtan district` | 僅一筆：桃園市龍潭區 |
| `taoyuan` | **兩筆**：桃園市桃園區、高雄市桃源區 |
| `taoyuan district` | **兩筆**：桃園市桃園區、高雄市桃源區 |

即：**`Taoyuan District` 在全國行政區資料集中，本來就同時對應到「桃園市
桃園區」與「高雄市桃源區」**——桃園（Táoyuán）與桃源（Táoyuán）中文不同字、
臺灣華語發音相同，英文音譯皆為 `Taoyuan`／`Taoyuan District`，是資料集
本身既有的全國唯一性衝突，不是本輪新增的錯誤，也不是猜測失誤——用既有
`normalizeSubdivision('Taoyuan District')`（不帶 `countyHint`）呼叫，會因為
`candidates.length === 2` 而回傳 `null`（沿用既有的「歧義就不猜」保護）。

這代表：**若把 `Taoyuan District` 當成安全、無歧義的別名直接對應到桃園市
（如本次需求文件所指示），是一個有意識的業務取捨，不是單純的技術正確
性判斷**——詳見第八節「本輪修正範圍」與
`R5.4-G1.5-B2.5_DISTRICT_NORMALIZATION_FIX.md` 的完整說明與風險評估。
`Longtan District` 沒有這個問題，全國唯一，可以安全地當明確別名。

## 8. 本輪修正範圍

基於第 7 節的發現，本輪的實作刻意做了以下範圍限定：

1. **新增獨立的 `DISTRICT_PARENT_ALIASES` 白名單與
   `normalizeDistrictToParentCounty()` 函式**（`utils/
   taiwanGeoNormalize.js`），不修改、不擴充既有 `COUNTY_ALIASES`／
   `normalizeCounty()`／`resolveTaiwanAdministrativeArea()`／
   `normalizeSubdivision()`——這些既有函式的行為（含 Hsinchu／Chiayi
   裸名稱保護、既有的縣市／行政區雙層解析）**完全不變**。
2. 新白名單只包含兩組明確字串（大小寫、空白皆正規化後比對）：
   - `Longtan District`／`Longtan Dist.`／`Longtan`／`龍潭區` → 桃園市
     （全國唯一，無歧義風險）。
   - `Taoyuan District`／`Taoyuan Dist.`／`桃園區` → 桃園市（**已知與
     高雄市桃源區的英文名稱衝突，是刻意的、已記錄的業務決策**，理由見
     `R5.4-G1.5-B2.5_DISTRICT_NORMALIZATION_FIX.md` 第五節）。
3. 這個新函式只被 `utils/ga4Realtime/index.js` 的
   `_aggregateCityRows()` 當作 `normalizeCounty()` 失敗後的 fallback
   呼叫，**刻意不接入**訂單履行地址解析、外送地址解析等「錯誤分類會造成
   實際業務後果」（例如外送距離計算、外送分區）的流程，把這個取捨的
   影響範圍限定在「GA4 IP 推估、本來就標示為僅供趨勢分析、非精確定位」
   的地圖圖層。
4. 不做任何「去掉 `District` 字尾、剩下文字直接當縣市」的通用字串處理；
   只有白名單裡明確列出的字串才會被對應，任何其他行政區名稱（包含未來
   可能出現的其他縣市行政區）維持現狀（`unmapped`，不猜測）。
5. Hsinchu／Chiayi 的既有模糊名稱保護完全不受影響（新函式的白名單不含
   任何 Hsinchu／Chiayi 相關字串）。

---

*本文件不含任何真實 Property、Stream、Service Account、Token 或其他憑證
內容。*
