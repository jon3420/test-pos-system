# R5.4-G1.4.1 — Coverage Dark Card, Metric-aware Rendering & Metric Sync Hotfix

## 1. Coverage 白條 Root Cause

### 症狀
使用者截圖確認：Coverage Explanation 卡片顯示為一條白色橫條，卡片內文字是極淺色，幾乎不可讀。

### Root Cause
`public/css/geo-heatmap.css` 的 `.geo-heat-coverage-explanation-text` 基礎規則：

```css
.geo-heat-coverage-explanation-text {
  background: #f8fafc;   /* 近白色，寫死 */
  /* 沒有自訂 color，繼承 body 的淺色文字 */
}
```

G1.3.1 當時「修正」的方式，是額外加上：

```css
[data-theme="dark"] .geo-heat-coverage-explanation-text,
.geo-live-theme-dark .geo-heat-coverage-explanation-text {
  background: #1e293b;
  color: #e2e8f0;
}
```

### dead CSS selector

全專案 `grep -rn "data-theme\|geo-live-theme-dark" public/js/*.js public/*.html` 結果：**零筆命中**（CSS 檔案本身除外）。沒有任何程式碼會把 `data-theme="dark"` 屬性或 `.geo-live-theme-dark` class 加到任何 DOM 元素上。這兩個選擇器**永遠不會被瀏覽器選中**，是死規則。

### 真實 Theme Architecture

`public/css/main.css` 的 `:root`：

```css
:root {
  --bg-panel: #1a1a1a;
  --bg-card: #222222;
  --text-primary: #f0f0f0;
  --text-secondary: #999;
  --border: #333;
  ...
}
```

這是**全站唯一固定深色主題**，沒有 light/dark 切換機制。`geo-heatmap.css` 其餘規則（`.geo-heat-summary`、`.geo-heat-controlbar`、`.geo-heat-ctl-btn`……）全部直接用 `var(--bg-panel, #1a1a1a)`／`var(--text-primary, #f0f0f0)` 之類的變數＋深色 fallback，沒有任何 theme-gating。`.geo-heat-coverage-explanation-text` 是這個檔案裡唯一一個沒有遵循這個慣例、且用了錯誤 gating 機制的規則。

### 修法

移除 `[data-theme="dark"]`/`.geo-live-theme-dark` gating，直接改用跟本檔案其餘規則一致的寫法：

```css
.geo-heat-coverage-explanation-text {
  background: var(--bg-card, #1e293b);
  color: var(--text-primary, #e2e8f0);
  border-left: 3px solid var(--border, #475569);
  border-radius: 8px;
  padding: 10px 12px;
}
.geo-heat-coverage-explanation-text:empty,
.geo-heat-coverage-explanation-note:empty { display: none; }
```

不需要等一個不存在的 toggle，直接生效。三種既有 `data-state` accent 顏色（`no_business_data`／`no_geo_data`／`partial_coverage`）保留。`full_coverage`／`error` 這兩個 `data-state` 目前 JS（`_geoHeatBuildCoverageExplanationText()`）沒有任何路徑會產生，本輪**沒有**為它們新增對應 CSS——避免重演「寫了但永遠不會被觸發」的死規則。

## 2. Metric Eligibility Root Cause

### 症狀
使用者截圖確認：切到 Visitors 指標、Visitors=0 時，中壢區的 Marker/Circle/常駐標籤仍然顯示；Ranking 顯示 0，但地圖上仍有點。

### Root Cause

`geoHeatRenderLayer()` 原本的篩選：

```js
const plottable = (areas || []).filter((a) =>
  a.coordinate_source === 'order_centroid' &&
  typeof a.lat === 'number' && typeof a.lng === 'number');
```

只檢查「這個行政區有沒有座標」，完全沒有檢查「目前選的 metric 在這個行政區是不是真的有資料」。因為 `order_centroid` 座標只從履約訂單算出來，跟 Orders/Revenue 綁在一起，切到 Visitors／Add to Cart／Checkout 時，同一批 Orders 的座標點會原封不動被畫出來（只是數值變成 0）。

這也是「Metric 切換後地圖殘留舊點」這個回報症狀的**實際成因**——不是 Metric Sync state 沒同步，而是重繪時的篩選條件本身沒有分辨 metric（見第 4 節）。

## 3. Resolver

新增 `geoHeatGetAreaMetricValue(area, metric)`：單一權威 metric value 取值函式，內部呼叫既有的 `geoHeatSafeNumber()` 做安全轉型（`null`/`undefined`/`NaN`/`Infinity`/非數字字串一律安全 fallback 為 0；有限數字忠實回傳，含負數與小數）。`geoHeatRenderLayer()`（地圖）與 `geoHeatBuildRanking()`（排行榜）改為共用這個函式，不再各自 inline 判斷。

Metric 對應的實際欄位名稱（沿用既有架構命名，非規格書字面欄位名）：

| Metric | 欄位 |
|---|---|
| visitors | `visitors` |
| add_to_cart | `add_to_cart` |
| begin_checkout（Checkout） | `begin_checkout` |
| orders | `orders`（completed orders） |
| revenue | `revenue` |
| conversion | `conversion`（= orders / visitors，`visitors=0` 時強制為 0） |

## 4. Eligibility

新增 `geoHeatIsAreaEligibleForMetric(area, metric)`，規則：

1. `metric` 必須是 `GEO_HEAT_METRICS` 列舉值之一（本輪第二次迭代新增此檢查，見第 12 節「已知限制」呼應的修正記錄——擴充 Smoke 時發現未知 metric 名稱會被誤判為合法，已補上）。
2. `coordinate_source` 必須是 `'order_centroid'`。
3. `lat`/`lng` 必須是 finite number，範圍 `-90≤lat≤90`、`-180≤lng≤180`，且不是 `(0,0)`。
4. `visitors`／`add_to_cart`／`begin_checkout` 三個 metric 在目前架構下**結構性完全沒有座標收集機制**（`analytics_events` 表沒有 lat/lng，見 `geo-heatmap.js` 開頭既有註解與 `GEO_HEAT_METRICS_WITHOUT_COORDINATES`），任何行政區對這三個 metric 一律不合法，不論數值是多少。
5. `orders`／`revenue`：座標合法即合法，0 值仍是有效資料（不得用 truthy 判斷）。
6. `conversion`：額外要求 `visitors > 0`（有效分母）才合法；`visitors=0` 時 conversion 沒有意義，不得畫成 0%。

只有通過這個函式才能建立 Circle／Marker／Permanent Label／Hover Tooltip；`geoHeatBuildRanking()` 的 `has_coordinate` 欄位（決定 Ranking 項目要不要顯示「目前尚無可用座標」提示）也改用這個函式，而不是原本單純的 `coordinate_source` 判斷。

## 5. Ranking / Pan / Auto-fit

- **Ranking**：`geoHeatBuildRanking()` 的 `value`/`ratio` 改用共用 resolver，`has_coordinate` 改用共用 eligibility——確保 Ranking 顯示的「有無座標」跟地圖上實際會不會畫點是同一個判斷依據。
- **Pan（Ranking click）**：`geoHeatSelectArea()` 原本用 `area.coordinate_source !== 'order_centroid'` 判斷要不要 `panTo`，改用 `!geoHeatIsAreaEligibleForMetric(area, geoHeatState.metric)`，避免對目前 metric 不合法的座標 `panTo`（例如 Visitors 模式下點擊中壢區不會 pan 過去）。
- **Auto-fit bounds**：**目前架構下 `geoHeatRenderLayer()` 沒有實作 auto-fit bounds**（`fitBounds()` 從未被呼叫）。這點在 Baseline Reality Audit 與 Smoke 測試中都誠實記錄，沒有假裝驗證了一個不存在的功能。

## 6. Render Lifecycle

每次 `geoHeatRenderLayer(areas, metric, display)` 呼叫：

1. `group.clearLayers()`（既有邏輯，未變）。
2. `display === 'ranking_only'` 時提前 return（不畫任何東西）。
3. 用 `geoHeatIsAreaEligibleForMetric()` 篩出 `plottable`（本輪修正核心）。
4. 對每個 plottable area：用共用 resolver 取值 → 建 Circle/Marker → `bindTooltip`（hover 完整資訊）→ `L.tooltip({permanent:true})`（常駐行政區名稱標籤）→ `group.addLayer()`。

`clearLayers()` 在篩選之前執行，所以「先清空、才用新 metric 重新篩選並畫」順序正確，不會有殘留。

## 7. Metric Sync 最終結論

`geoHeatUiSyncMetricFromGlobal()`／`geoHeatUiSetMetric()`（G1.3 既有邏輯）**本身沒有問題**：

- 有 reentrancy guard（`_geoMetricSyncInProgress`），避免上下無限互相觸發。
- 全域 `geoVisitorState.metric` 是唯一權威來源，`geoHeatState.metric` 是被動同步對象。
- `GEO_EVENT_TO_HEATMAP_METRIC`／`GEO_HEATMAP_TO_EVENT_METRIC` 雙向對照表由單一來源反轉產生，不會兜不起來。
- `geoHeatUiSetLayer()`（Layer Switch）**沒有**寫入 `geoHeatState.metric`，Layer Switch 不會重設 Metric。

畫面上看起來像「Sync 錯誤」的症狀，實際上是第 2 節描述的 Metric-aware Rendering Bug——舊 Orders Marker 沒有依照新 Metric 過濾就繼續顯示，並非 Sync state 本身寫錯。

## 8. 新屋區 Tooltip 來源

`grep -n "新屋" public/js/*.js` 找不到任何硬編碼字串。實際來源是 `public/js/geo-intelligence-map.js` 的 GeoJSON 行政區多邊形圖層：

```js
no_data_label: '暫無資料',
...
function _geoBuildTooltipContent(areaId) {
  const row = geoMapState.rows.find(...);
  if (!row) return `${label}：${geoMapStatusText('no_data_label')}`;
  ...
}
...
layer.bindTooltip(_geoBuildTooltipContent(areaId));
```

- 屬於**這個 Choropleth 多邊形圖層自己的 hover tooltip**（`layer.bindTooltip(...)`，不是 `L.tooltip({permanent:true})`），滑鼠移過去才顯示。
- **不是** `geo-heatmap.js` 的 Permanent Label。
- **不會**進入 `geoHeatBuildRanking()` 的 Ranking 列表（兩個模組完全獨立，沒有交集）。
- **不會**建立 `L.marker()`。
- 屬於「可保留、只能 hover 顯示、不可被算作 Visitor Marker、不可影響 Ranking」的合法情境，**不需要修改**。本輪確認 `geo-intelligence-map.js` 完全未被本輪任何修改觸碰。

## 9. G1.4.1 Scope Guard Layer

`scripts/lib/geo-heatmap-g131-scope-guard.js` 新增第三層 `GEO_HEATMAP_G141_ALLOWED_ADDITIONS`（6 項，全部是精確字串比對＋還原）：

1. `g141-metric-eligibility-block`：新增的三個純函式（`geoHeatMetricSupportsCoordinate`／`_geoHeatIsValidLatLng`／`geoHeatGetAreaMetricValue`／`geoHeatIsAreaEligibleForMetric`）連同說明註解整段。
2. `g141-ranking-uses-shared-resolver-and-eligibility`：`geoHeatBuildRanking()` 的 `value`/`ratio`/`has_coordinate` 三行改寫。
3. `g141-render-layer-uses-shared-resolver`：`geoHeatRenderLayer()` 的 `value` 取值改寫。
4. `g141-plottable-filter-metric-aware`：`plottable` 篩選改寫。
5. `g141-selectarea-eligibility-check`：`geoHeatSelectArea()` 的 panTo 判斷改寫。
6. `g141-export-eligibility-helpers`：`module.exports` 新增三個函式匯出。

`computeScopedBaselineCheckForSource()` 改為三層鏈式還原：`reconstructG141Layer()` → `reconstructG14Layer()` → `reconstructPristine()`，最終 hash 仍必須等於 `PRISTINE_BASELINE_SHA256`（`8f3ec8c0ae76f84825bc0e2e1a481002109244763741a16a2981d17d0cfc710d`，R5.3-A2/A1.2 原始基線，本輪未修改）。實際執行 `computeScopedBaselineCheck()` 回傳 `ok: true`。

## 10. Known Limitations（誠實記錄，非隱藏）

1. **Auto-fit bounds 不存在**：目前架構沒有這個功能，不是本輪範圍。如果未來新增，必須套用同一個 `geoHeatIsAreaEligibleForMetric()`。
2. **`full_coverage`／`error` 兩個 Coverage data-state 目前沒有任何 JS 路徑會產生**：CSS 沒有對應樣式規則，屬於誠實現況，不是遺漏——避免重演死 CSS。
3. **Conversion metric 的地理意義**：`conversion = orders / visitors`，但 `visitors` 本身結構性沒有座標（見第 4 節），地圖上顯示的 Conversion 熱點座標其實來自 `order_centroid`（履約訂單位置），不是「這個行政區的訪客轉換率」的精確地理分佈，只是「有履約紀錄的行政區，搭配全店 visitors 分母算出的轉換率」。這是既有架構限制，本輪沒有改變這個語意，只是修正了「有沒有分母才能畫」的判斷。
4. **Manual Visual 完全未驗證**：見 Manual QA Checklist。
5. **本輪 Regression 三輪皆在同一個容器內連續執行**：沒有跨機器／跨時區的驗證，`no time-related flaky` 的結論僅限於本次執行環境。
