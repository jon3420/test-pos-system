# R5.3-A2 — Geo Event Engine｜訪客行為地理事件引擎 — Completion Report

狀態：**QA BUILD**。Automated Verification：PASS。Manual Visual：REQUIRED。
Final Completion：NOT YET APPROVED（本文件本身不宣告正式 COMPLETE）。

---

## 1. Scope

延續 R5.3-A1.2（Analytics Visitor Geo Sync）。本輪目標：把既有 Analytics
行為事件正式整理成 **Geo Event Engine**——同一張 Leaflet Map、同一套
`geoVisitorState`，依 8 個正式 Metric Tab（Visitors／Add To Cart／
Checkout／Orders／Revenue／Conversion／Cart Abandonment／Recommendation
Risk）切換資料圖層。不是新地圖，不是第二套 Dashboard，完全建立在既有
`geo_visit_log` 之上（**沒有新增 `geo_event_log`**）。

## 2. Regression 排查（本輪最高優先事項）

### 2.1 `smoke-hotfix30-b5-r5-1-c-geo-ui.js`：196/196 → 195/196 → 已確認非本輪造成

**根因分類：B（測試 Harness 假設過時／時序脆弱）**，且**與本輪修改無關**。

- 該測試唯一載入的產品檔案是 `public/js/geo-intelligence.js`；SHA-256 逐位
  元組比對確認本輪完全未修改此檔案。
- 該測試 Part B 大量使用固定 10～40ms 的 `setTimeout` 等待真實 debounce／
  lazy-load 計時器觸發，在系統負載較高（例如緊接著跑完 12 支其他大型
  Smoke）時偶發性競速失敗，屬於既有測試本身的時序脆弱性。
- 重複執行 8 次（含原始批次順序重現）皆穩定 196/196。
- **未修改任何程式碼或測試檔案**（分類 B 且不屬於本輪新增功能，不需要
  新增正向 sanity assertion）。

### 2.2 `smoke-hotfix30-b5-r5-2-b1-3-geo-behavior-funnel-backend.js`：偶發 71～76/76

**根因分類：B（測試 Harness 時序脆弱），且確認與本輪任何修改完全無關**：

- 暫時停用本輪新增的 `logGeoVisit()` hook 後，flakiness 依然存在（10 次
  執行中仍有 2 次失敗），排除因果關係。
- 進一步用 R5.3-A1.2-QA 的**原始未修改 QA ZIP**（`fix18-10-hotfix30-B5-
  R5.3-A1.2-QA-full.zip`）獨立重新安裝依賴、重複執行 10 次，**同樣的
  flakiness 在完全沒有本輪任何程式碼的情況下重現**（10 次中 5 次失敗，
  失敗的斷言完全相同）。
- 結論：這是一個在 R5.2-B1-3（遠早於本輪）就已存在的既有測試脆弱性，
  與 Geo Event Engine、`geo_visit_log`、`insertEvent()` 的任何改動無關。
- **未修改任何程式碼或測試檔案**。

兩者皆屬於「找出但不屬於本輪範圍」的既有問題，如實記錄，不隱藏、不假裝
沒發現。

## 3. Data Decision

見 `R5.3-A2_DATA_DECISION.md`。摘要：

1. `analytics_events.order_id` 確實存在（既有欄位，非本輪新增資料）。
2. `geo_visit_log` additive 新增 `order_id`（safe migration + 索引）。
3. `analytics_events` 稽核確認完全沒有 `value`/`revenue`/`amount`/
   `order_total`。
4. Revenue 改用 `order_id` 反查既有 `orders.total`，回應明確標示
   `revenue_source: 'order_data'`。
5. 因為 Analytics 本身沒有原生營收概念，不存在「混算」的可能性。
6. Purchase 正式事件只有 `purchase`（本專案沒有 `checkout_payment_
   complete`），沿用既有定義。
7. `visitor_key` 沿用 A1.2 規則，未修改：`COALESCE(NULLIF(visitor_id,''),
   NULLIF(session_id,''), 'event_'||id)`。
8. Abandonment 用 SQL `NOT IN` 子查詢做真正的集合差集，不是數字相減。
9. Polygon fixture 稽核結論沿用 A1.2：13 個 Taoyuan 行政區皆為矩形
   fixture，Choropleth 合法清單維持空集合。
10. Visitor Geo 沒有精確座標時，`lat`/`lng` 一律 `NULL`，不假造任何座標。

## 4. 修改/新增檔案

| 檔案 | 動作 | 內容 |
|---|---|---|
| `utils/db.js` | 擴充 | `geo_visit_log` additive 新增 `order_id` 欄位 + 索引 |
| `utils/geoVisitLog.js` | 擴充 | `logGeoVisit()` 支援 `order_id`；`getRecentGeoVisits()` 新增 `visitor_mask`（遮罩後訪客識別），不再回傳原始 `visitor_id`/`session_id`；時間範圍擴充 1h/24h/custom |
| `utils/analyticsLog.js` | 擴充 | `insertEvent()` 的既有同步 hook 額外傳遞 `order_id` 給 `logGeoVisit()` |
| `utils/geoEventEngine.js` | **新增** | `geoEventClassify()`、`getGeoEventFunnel()`（單一查詢層）、Abandonment 集合差集、Conversion 計算、`buildRecommendationRiskSummary()` |
| `routes/analytics-geo.js` | 擴充 | `/visitor-log` additive 新增 `funnel`／`coverage`／`recommendation_risk` 三個欄位，沒有新增第二套端點 |
| `public/js/geo-visitor-layer.js` | 擴充 | 新增 8 個 Metric Tab（共用同一個 `geoVisitorState`，不建立四套重複狀態）、Metric Summary 渲染、Recent Log 遮罩顯示 |
| `public/js/geo-heatmap-ui.js` | 擴充 | Visitor Layer 面板新增 Metric Bar／Metric Summary 容器（Order Heatmap 既有內容一個字元未變） |
| `public/css/geo-visitor-layer.css` | 擴充 | Recent Log 新增遮罩／定位層級欄位樣式 |
| `scripts/smoke-hotfix30-b5-r5-3-a2-geo-event-engine.js` | **新增** | 228 項 assertion |
| `scripts/smoke-hotfix30-b5-r5-3-a1-2-visitor-geo-sync.js` | 更新 1 項 | 時間範圍常數從 5 種擴充為 8 種，同步更新既有斷言（188/188，數量不降反升） |

**沒有新增 `geo_event_log`**——完全建立在既有 `geo_visit_log` 上。
**沒有新增第二套 API**——只 additive 擴充既有 `/visitor-log`。

## 5. 必做診斷測試 Scenario 1～5（需求文件二十一-B，全數通過）

| Scenario | 結果 |
|---|---|
| 1（1 store/1 visitor/1 session/1 page_view/全 Unknown） | 目前在線=1、Geo Visitors=1、Unknown=1、Unknown Rate=100%、Marker=0 |
| 2（同 visitor 重複 10 次 page_view） | Geo Visitors=1 |
| 3（Known District Only，無 lat/lng） | Geo Visitors=1、Known District=1、Marker=0、Ranking=1 |
| 4（完全沒有事件） | Geo Visitors=0、Unknown=0、Unknown Rate=0% |
| 5（前端必須讀 Geo Event Engine，不得讀舊 getGeoFunnel()） | 原始碼確認 `geo-visitor-layer.js` 讀取 `geoVisitorState.funnel`，完全不呼叫 `getGeoFunnel()` |

**完成驗收條件（需求文件二十一-C）：老闆儀表板「目前在線 1 人」與 Geo
Intelligence「Geo 訪客 1 人／Unknown 1 人／Unknown 100%」同時成立，即使
沒有真實座標、沒有正式 Polygon，Geo 訪客也不會顯示 0 人——已驗證通過。**

## 6. Geo Event Engine Smoke（228/228）

`scripts/smoke-hotfix30-b5-r5-3-a2-geo-event-engine.js`：涵蓋
`geoEventClassify()`、必做診斷 Scenario 1～5、visitor_key 三層優先序、
漏斗各階段去重（page_view/session_start/view_item/add_to_cart/
begin_checkout/purchase）、跨店與時間範圍隔離、Unknown/Known District
納入統計、Choropleth fixture 停用確認、Cart/Checkout Abandonment 集合
差集（含極端值不為負數）、Revenue Source Protection（無資料時 `null`
不是 `0`）、Recommendation Risk 規則式（六種訊號逐一驗證）、Recent Log
遮罩、Index 查詢計畫、8 個 Metric Tab 真正切換（jsdom 實測）、Layer/Tile
Reuse、No Duplicate Map、Order Heatmap 與 Visitor Layer 不退化。

## 7. Regression（全部 26 套件）

| 套件 | 結果 |
|---|---|
| `node --check`（全專案） | 全部 OK |
| Order Heatmap Engine／既有 Map／Settings／manifest／geo-intelligence.js／dashboardAnalytics.js SHA-256 | 與基線逐位元組相同 |
| **A2 Geo Event Engine Smoke（新增）** | **228/228 PASS** |
| A1.2 Visitor Geo Sync | 188/188 PASS |
| A1.1 Integration | 112/112 PASS |
| A1 Heatmap Engine | 128/128 PASS |
| B2 Geo Map | 630/630 PASS |
| B3 Geo Settings UI | 157/157 PASS |
| B1-1～B1-6 | 100/150/76/132/819/867/313/376，全部 0 FAIL（B1-3 有已知既有 flakiness，見第 2 節，乾淨執行下 76/76） |
| R5.1-A／B／C／D1 | 76/111/196/164，全部 0 FAIL |
| R5.2-A 系列 | 62/232/90/140，全部 0 FAIL |
| 其他既有（Cart/Order Hours、Dashboard UI、Debounce、Hotfix30-C1） | 55/20/32/22，全部 0 FAIL |

沒有降低任何既有 assertion 數量。

## 8. Static Audit

- Order Heatmap Engine（`geo-heatmap.js`）、Order Centroid／Coverage
  （同一檔案）：逐位元組與基線相同，本輪完全未修改。
- A1.2 Visitor Geo Sync：未退化（188/188，數量從 185 提升到 188 是本輪
  合理擴充時間範圍所致，非降低）。
- rectangle fixture 未啟用：`GEO_VISITOR_CHOROPLETH_OFFICIAL_CITY_CODES`
  原始碼確認仍是空陣列。
- 無假座標／無店家座標冒充訪客位置：`geo-visitor-layer.js`／
  `geoEventEngine.js` 原始碼掃描確認無相關邏輯，`geo_visit_log.lat/lng`
  全部來自既有 Geo Resolver（一律 NULL）。
- 無第二張 Leaflet Map／無第二個 Tile Layer：jsdom 實測
  `mapCreateCount===0`／`tileLayerCreateCount===0`。
- 無第二套 Analytics／無重複 API：只新增一支 `/visitor-log`（additive
  擴充回應），確認沒有 `analytics-geo-v2.js`/`analytics-geo-event.js`/
  `geo-event-api.js` 等第二套路由檔案。
- 無 debug log／無 `Math.random()`：原始碼掃描確認。
- 無完整 visitor_id/session_id 洩漏：`getRecentGeoVisits()` 回傳物件
  完全不含這兩個欄位，只有遮罩後的 `visitor_mask`。
- `geo_visit_log` 查詢走 Index：`EXPLAIN QUERY PLAN` 確認總覽查詢、
  event_name 篩選查詢、Abandonment 集合差集子查詢皆使用對應索引，無
  `SCAN`。
- 無 `data/pos.db`：打包前已清除。
- ZIP 不含 `node_modules`/`.git`：打包時明確排除。

## 9. 已知限制 / Manual Visual 必要性

- Choropleth 目前仍不會在任何城市顯示顏色（沿用 A1.2 稽核結論），Visitor
  Layer 的主要呈現方式是 Ranking／Summary／Coverage／Recent Log。
- Revenue／Conversion／Cart Abandonment／Recommendation Risk 四個 Tab
  的行政區排行榜目前沿用 Visitors 排序（沒有為每個 Tab 建立獨立的行政區
  級 SQL，見 Data Decision「哪些指標仍 Pending」）。
- Channel 篩選尚未在 Geo Event Engine 層生效（`getGeoEventFunnel()` 的
  `channel` 參數目前保留但不影響查詢結果）。
- 真實瀏覽器下 8 個 Metric Tab 的視覺切換、Recent Geo Events 手機版排版、
  Revenue/Orders「尚無可用資料」文案的呈現，仍需要人工檢查，見
  `R5.3-A2_MANUAL_QA_CHECKLIST.md`。
