> **H1.4.7 supersession note｜2026-08-12**
>
> 本文件保留 H1.4.6 發布當時的歷史結果，不回寫或改造原始驗收紀錄。
> H1.4.7 已有意將結帳流程拆分為兩階段：點擊 cartBar／openCartSheet
> 只開啟購物車並送出 view_cart；只有使用者點擊獨立「前往結帳」
> 才顯示顧客資料表單並送出 checkout_click。
> legacy begin_checkout 不再由兩個正式點餐頁送出。
> 原有欄位驗證、LINE 登入、submit_order、付款及錯誤處理路徑仍沿用。
> 因 H1.4.7 supersession，現行 H1.4.6 static audit 已由 66 項更新為
> 72 項；本文內原有數字仍代表 H1.4.6 當時的歷史交付結果。
> 最新契約與驗證結果請見 H1.4.7_TWO_STAGE_CHECKOUT_DELIVERY_REPORT.md。

# H1.4.6-PRODUCT-DETAIL-CHECKOUT-FLOW 交付報告

版本：`fix18-10-hotfix30-B5-R5.4-G1.6-GA4-H1.4.6-PRODUCT-DETAIL-CHECKOUT-FLOW-QA-full`

---

## 1. Reality Audit

在動手修改前，實際檢視了程式碼（不是憑上一輪摘要判斷），確認：

- LINE 外帶／外送**共用同一個** `public/line-order.html`、同一份 `buildCard()`／購物車／結帳流程（既有架構，本輪未拆成兩份）。
- 宅配是獨立的 `public/line-shipping.html`（既有架構，商品卡樣式/購物車模型與 line-order.html 相似但本來就是不同檔案）。
- **商品資料真實欄位**：`id / name（含 effective_line_name）/ image（line_image_url）/ description（line_description）/ price（effective_line_price）/ display_cat_id / sale_status,line_sold_out / 販售時段欄位 / line_spec（純文字，例如 "200g"）`。**沒有**規格／加料的結構化資料表。
- 購物車格式：`cart[productId] = { product, qty }`（兩頁一致），只有「同商品疊加數量」，沒有規格/加料變體概念。
- 既有結帳入口：`openCartSheet()`，本輪未變動。
- 既有 Analytics 共用 helper：`_trackEvent()`（送 internal API）＋ `AnalyticsPlatforms.trackPlatformEvent()`（分派 GA4／Meta Pixel），兩頁各自有一份 `_trackEvent()`，但都呼叫同一份 `public/js/analytics-platforms.js`。
- 外送地址/地圖/距離/外送費、優惠券、LINE 登入、送單，均為既有模組，本輪未觸碰。
- H1.4.5 runner（`scripts/run-regression-g1-6-ga4-h1-4-5.js`）在執行期動態解析 H1.4.4 runner 原始碼取得 inherited suite 陣列，本輪沿用同一手法解析 H1.4.5。

## 2. 原流程與限制

**修改前**：商品卡本身沒有任何「查看詳情」的互動，點擊圖片/名稱沒有反應；只有「快速加入」按鈕（或 `[-] qty [+]`）。GA4 只有 `page_view / add_to_cart / begin_checkout / payment_started / purchase`，完全沒有 `view_item`。

**限制**：商品資料沒有規格／加料結構（見第 11 節），因此商品詳情只能顯示「已存在的」欄位，不能加入可選規格。

## 3. 完成後的操作流程

```
商品卡曝光（IntersectionObserver）→ internal view_product
點擊商品卡主體／圖片／名稱／Enter/Space
  → openProductDetail() → ProductDetailModal.open()
  → onOpen 觸發一次 → internal + GA4 view_item
  → 顯示大圖／名稱／介紹／價格／數量調整／即時小計
點擊「加入購物車」→ 呼叫既有 addCart()（line-order）／changeQty()（line-shipping）
  → 真正成功才顯示「已加入 ✓」並關閉 Modal → add_to_cart
點擊固定購物車列「前往結帳」→ 既有 openCartSheet()（非空購物車才觸發）→ begin_checkout
→ 既有結帳流程（外帶/外送/宅配分流不變）→ purchase（後端既有邏輯）
```

快速加入 `[+]` 與 `[-] qty [+]` 完全保留原行為，不開啟 Modal（`closest('.qty-ctrl, .add-btn')` / `closest('.qty-btn')` 排除）。

## 4. Production 修改檔案清單

**新增**：
- `public/js/product-detail-modal.js` — 共用商品詳情元件
- `public/css/product-detail-modal.css` — 對應樣式

**修改**：
- `public/line-order.html` — 載入共用元件、商品卡點擊/鍵盤開啟詳情、`openProductDetail()`、`_trackViewItem()`、`_trackEvent()` 支援 `items[]`/`value` 透傳
- `public/line-shipping.html` — 同上（`openProductDetail()`、`_trackViewItemShipping()`、`_bindProdCardDetailOpen()`）
- `public/js/analytics-platforms.js` — `GA4_EVENT_MAP` 新增 `view_item: 'view_item'`（META_EVENT_MAP 未變動）
- `utils/analyticsLog.js` — `EVENT_WHITELIST` 新增 `'view_item'`

**測試／報告修改檔案清單**：
- 新增 `scripts/run-g1-6-ga4-h1-4-6-runtime.js`（真實 jsdom 執行）
- 新增 `scripts/static-audit-g1-6-ga4-h1-4-6.js`
- 新增 `scripts/run-regression-g1-6-ga4-h1-4-6.js`
- 修改 `scripts/static-audit-g1-6-ga4-h1-4-5.js`（3 項過期 view_item 契約 → 見第 10 節）
- 修改 `scripts/run-g1-6-ga4-h1-4-4-view-item-runtime.js`（Cat G/H/I/K/M → 見第 10 節；並修正測試本身 2 個真實 bug：jsdom SHOP_DATA 是 `let` 頂層變數而非 window 屬性、window.scrollTo not-implemented 噪音）
- 修改 `scripts/static-audit-g1-6-ga4-h1-4-4.js`（3 項過期 view_item 契約 → 見第 10 節）
- 修改 `scripts/run-regression-g1-6-ga4-h1-4-4.js`（更新 h1-4-4-view-item-runtime 的預期 pass/total：24→25，因新增 1 項「Modal 真的有開啟」斷言）
- 新增本檔案

未修改：POS 收銀、後台商品管理、老闆端 Dashboard 商品卡、商品價格來源、歷史訂單、資料庫結構、外送費/優惠券/付款/LINE 登入核心邏輯。

## 5. 商品詳情資料來源

100% 沿用既有商品資料（`allProducts` / `SHOP_DATA.products`），未建立第二套商品資料、未呼叫額外 API。價格直接讀 `effective_line_price || price`（line-order）／`p.price`（line-shipping），未疊加任何規格/加料價差。

## 6. LINE 外帶、外送及宅配接入方式

- 外帶/外送：`line-order.html` 單一份 `buildCard()` + `openProductDetail()`（依 `getProductAvailableModes()` 判斷可購買性，兩模式共用同一套規則）。
- 宅配：`line-shipping.html` 獨立的 `openProductDetail()`，依 `quota_remaining` 判斷售完。
- 兩頁**共用同一份** `product-detail-modal.js`/`.css`，不是各自複製一套 Modal。

## 7. 固定購物車列與既有結帳流程的關係

`cartBar`（件數 `#cartCnt`、總額 `#cartBarTotal`）與點擊進入 `openCartSheet()` 完全是**既有**實作，本輪未改寫；`openCartSheet()` 空購物車時提早 return，非空才觸發 `begin_checkout`（同 cart_id 去重一次）。三種通路的結帳分流（外帶/外送欄位、宅配收件資料）均為既有邏輯，未被觸碰。

## 8. Analytics 事件契約

| 事件 | 觸發時機 | 送出者 |
|---|---|---|
| `view_product` | 商品卡進入可見畫面（IntersectionObserver, threshold 0.5） | `_setupViewProductObserver` |
| `view_item` | 使用者主動點擊/鍵盤開啟且 Modal 成功開啟（`ProductDetailModal.open()` 的 `onOpen`） | `_trackViewItem` / `_trackViewItemShipping` |
| `add_to_cart` | `addCart()`/`changeQty()` 真正成功加入後 | `_trackAddToCart` |
| `begin_checkout` | 點擊購物車列進入 `openCartSheet()`（非空購物車，去重一次） | 既有邏輯，未變 |
| `purchase` | 既有後端訂單成立語意 | 未變 |

驗證重點（均已通過 runtime + static suite）：`view_product` 未映射 GA4 `view_item`；商品卡曝光不送 `view_item`；快速加入不送 `view_item`；同一次開啟內部重繪/resize 不重複送；關閉後再開可再送一次；GA4 `items[]` 含 `item_id/item_name/price/quantity:1/item_category`（有資料時）；未在頁面散落新的 `gtag()`，一律走 `AnalyticsPlatforms.trackPlatformEvent()`。

## 9. H1.4.5/H1.4.4 過期契約被取代的原因與範圍

H1.4.4/H1.4.5 當時商品詳情尚不存在，因此有多處測試把「`view_item` 完全不得存在」寫死成 Hard Gate。H1.4.6 正式新增商品詳情後，這個前提不再成立。**只**修改了以下 6 處已確認過期的斷言，其餘所有斷言（H1.4.5 曝光率公式/文字、H1.4.4 的 Meta Pixel 保護、GA4 即時地圖語意等）完全未被放寬或刪除：

| 檔案 | 舊斷言 | 新契約 |
|---|---|---|
| `static-audit-g1-6-ga4-h1-4-5.js` | GA4_EVENT_MAP 不得有 view_item key | 必須有 `view_item: 'view_item'` |
| 同上 | line-order.html 不得出現 view_item 字面字串 | view_item 只能出現在商品詳情路徑，不在清單曝光 observer 區塊內 |
| 同上 | line-shipping.html 同上 | 同上 |
| `static-audit-g1-6-ga4-h1-4-4.js` | GA4_EVENT_MAP 不得有 view_item key | 同上 |
| 同上 | line-order.html 不得出現 view_item | `_trackViewItem` 函式存在（合法送出點） |
| 同上 | line-shipping.html 同上 | `_trackViewItemShipping` 函式存在 |
| `run-g1-6-ga4-h1-4-4-view-item-runtime.js` Cat G/H/I/K/M | 點擊卡片／任何操作後 view_item 恆為 0 | 點擊卡片主體開啟詳情剛好送 1 次；清單曝光/快速加入/重複 init 相對該次之後計數不變 |

## 10. 規格／加料資料模型限制（誠實保留）

- 目前資料庫**只有**文字型 `line_spec`（例如 "200g"），**沒有**結構化的規格／加料資料表、後台編輯器、單選/多選/必選邏輯、加價公式、後端重新計價驗證。
- 本輪**沒有**虛構任何前端規格/加料選項或假公式；`product-detail-modal.js` 沒有讀取任何 `options/addons/modifiers` 欄位（已由 static suite 斷言鎖定）。
- `line_spec` 只用於既有卡片展示，未被 `openProductDetail()` 當成可計價欄位使用。
- 商品詳情的價格 100% 來自既有 `price` 欄位，前端未自訂價格；後端 `routes/analytics.js` 對 `product_id` 的型別/範圍驗證未被放寬。
- 真正的規格/加料系統（辣椒/青蔥香菜等）需要另開下一版，建立：後端資料模型、後台選項管理、單選/多選/必選規則、加價公式、購物車識別、訂單明細、後端重新計價驗證、歷史資料相容。**本輪明確不做。**

## 11. 測試結果（本次會話實際執行，非模擬）

| Suite | PASS | FAIL | TOTAL | exit |
|---|---:|---:|---:|---:|
| H1.4.6 runtime（真實 jsdom） | 37 | 0 | 37 | 0 |
| H1.4.6 static/UI/Analytics | 66 | 0 | 66 | 0 |
| H1.4.5 exposure runtime | 31 | 0 | 31 | 0 |
| H1.4.5 static audit（含 3 項取代後契約） | 40 | 0 | 40 | 0 |
| H1.4.4 view-item runtime（含 5 項取代後契約） | 25 | 0 | 25 | 0 |
| H1.4.4 static audit（含 3 項取代後契約） | 23 | 0 | 23 | 0 |

`node --check` 全部通過（見下方 regression 摘要）。

## 12. Regression 三輪精確結果（真實連續執行，非複製）

Runner：`scripts/run-regression-g1-6-ga4-h1-4-6.js`（繼承 H1.4.5 final 74 suites + 本輪新增 2 suites = **76 unique suites**，路徑正規化去重通過）。

| Round | UNIQUE SUITES | PASS | FAIL | TOTAL | exit | residue |
|---|---:|---:|---:|---:|---:|---:|
| 1 | 76 | 7702 | 0 | 7702 | 0 | 0 |
| 2 | 76 | 7702 | 0 | 7702 | 0 | 0 |
| 3 | 76 | 7702 | 0 | 7702 | 0 | 0 |

Round-to-round consistency: **CONSISTENT**　assertionMismatch: **0**

原始完整輸出保留於工作目錄外：`/home/claude/regression_logs/full_3round.log`（不隨 ZIP 打包）。

## 13. Manual QA Checklist（誠實分級）

**重要聲明**：本次會話**沒有**使用真實手機、LINE LIFF、GA4 DebugView 或 Meta Pixel Helper，以下一律標示「待人工驗證」，只提供驗證方法，不宣稱已完成。已由自動測試（jsdom）驗證的項目另外標註。

| 尺寸 | 外帶 | 外送 | 宅配 | 狀態 |
|---|---|---|---|---|
| 360×800 | 待人工驗證 | 待人工驗證 | 待人工驗證 | CSS 規則已由 static audit 驗證存在（bottom sheet/safe-area/object-fit），實機顯示待驗證 |
| 390×844 | 待人工驗證 | 待人工驗證 | 待人工驗證 | 同上 |
| 412×915 | 待人工驗證 | 待人工驗證 | 待人工驗證 | 同上 |
| 768×1024 | 待人工驗證 | 待人工驗證 | 待人工驗證 | 桌機置中樣式規則已驗證存在，實機待驗證 |
| 1440×900 | 待人工驗證 | 待人工驗證 | 待人工驗證 | 同上 |
| LINE LIFF 內建瀏覽器 | 待人工驗證 | 待人工驗證 | 待人工驗證 | 完全待驗證 |

**自動測試已驗證**（jsdom，非真實瀏覽器）：Modal 開/關（遮罩/X/Esc）、qty +/-、小計計算、maxQty 上限、無圖 placeholder、無介紹隱藏區塊、快速連點防護（300ms）、blocked 商品禁止加入、onOpen 只觸發一次、長文字不噴錯、鍵盤 Enter/Space 觸發（DOM 事件層級，非真實鍵盤裝置）。

**待人工驗證項目**：真實手機觸控體驗、iOS Safari/Android Chrome safe-area 實際渲染、LINE LIFF 內建瀏覽器相容性、長商品名稱/介紹在各尺寸下的實際排版、售完/預約明日商品的視覺呈現。

### GA4 DebugView

**驗證方法**（尚未執行）：
1. 於瀏覽器安裝 GA4 DebugView 擴充功能或在 URL 加上 `?_dbg=1`（依專案既有設定）。
2. 開啟 GA4 後台的 DebugView 頁面，篩選對應 GA4 資源。
3. 於 line-order.html/line-shipping.html 點擊商品卡開啟詳情，確認 DebugView 出現 `view_item` 事件，`items[]` 含 `item_id/item_name/price/quantity/item_category`。
4. 確認商品卡單純曝光（滑動經過）不產生 `view_item`。
5. 確認快速加入不產生 `view_item`。

**真實結果**：尚未進入正式 GA4 資源查看，**未驗證**。

### Meta Pixel Helper

**驗證方法**：安裝 Meta Pixel Helper 瀏覽器擴充功能，開啟頁面確認既有 `PageView/ViewContent/AddToCart/InitiateCheckout/Purchase` 事件正常觸發，且沒有新增非預期事件。

**真實結果**：尚未使用 Meta Pixel Helper 驗證，**未驗證**。（static audit 已從程式碼層級確認 `META_EVENT_MAP` 未被本輪修改。）

## 14. Production Diff（相對原始 H1.4.5 ZIP）

```
新增：
  public/js/product-detail-modal.js
  public/css/product-detail-modal.css
  scripts/run-g1-6-ga4-h1-4-6-runtime.js
  scripts/static-audit-g1-6-ga4-h1-4-6.js
  scripts/run-regression-g1-6-ga4-h1-4-6.js
  H1.4.6_PRODUCT_DETAIL_CHECKOUT_FLOW_DELIVERY_REPORT.md

修改：
  public/line-order.html
  public/line-shipping.html
  public/js/analytics-platforms.js
  utils/analyticsLog.js
  scripts/static-audit-g1-6-ga4-h1-4-5.js       (3 項過期契約取代)
  scripts/static-audit-g1-6-ga4-h1-4-4.js       (3 項過期契約取代)
  scripts/run-g1-6-ga4-h1-4-4-view-item-runtime.js  (5 項過期契約取代 + 2 個測試 harness bug 修正)
  scripts/run-regression-g1-6-ga4-h1-4-4.js     (更新繼承的預期 pass/total 數字)

未修改：POS 收銀、後台商品管理、老闆端 Dashboard、商品價格來源、歷史訂單、
        資料庫結構、外送費/優惠券/付款/LINE 登入核心邏輯、Meta Pixel 既有事件、
        H1.4.5 曝光率相關公式與文字。
```

## 15. 尚未完成或待人工項目

- 真實手機/LINE LIFF/桌機瀏覽器的視覺與觸控驗證（見第 13 節）
- GA4 DebugView 實際結果（尚未進入正式 GA4 資源）
- Meta Pixel Helper 實際結果
- 規格／加料系統（明確排除於本輪，見第 10 節）

## 16. ZIP 大小與 SHA-256、打包後驗證結果（本輪實際執行）

**驗證日期**：本次會話（2026-08-11）

> 注意：為避免「把 ZIP 自己的 hash 寫回 ZIP 內容」造成 hash 自我循環，下面記錄的是
> **上一輪打包結果**的 SHA-256（即本次驗證所檢查的那一份 ZIP）。本報告更新後會被
> 重新打包成最終交付 ZIP，最終 ZIP 的 SHA-256 以對話最後回覆及外部
> `.sha256` checksum 檔為準，不等於下面這個數字（這是預期中的正常現象，不是錯誤）。

### 16.1 打包後驗證所檢查的 ZIP（上一輪）

- 檔名：`fix18-10-hotfix30-B5-R5.4-G1.6-GA4-H1.4.6-PRODUCT-DETAIL-CHECKOUT-FLOW-QA-full.zip`
- 精確大小：3,945,504 bytes（3.76 MB）
- SHA-256：`e2633098936997583dcd1fe94dae31369795ddf57115424b0888960c268ea1b9`
- ZIP 內總檔案數：711 files

### 16.2 禁止檔案掃描結果

對 ZIP 清單（`unzip -l`）與解壓後目錄分別掃描 `node_modules / .env / .env.* / *.db / *.sqlite* / *.log / cache / coverage / checkpoint / *.pid / regression_logs`：**未發現任何違禁內容**。掃描一度出現的關鍵字命中（`scripts/smoke-hotfix30-b5-r5-4-g1-3-metric-sync-coverage.js`、`R5.4-G1.3.1_COVERAGE_TOTAL_DARK_THEME_FIX.md`）經人工確認為既有 Production 檔名裡剛好含有 "coverage" 字樣（暗色主題/指標同步功能），與測試覆蓋率殘留無關，不是違禁內容。`.env` 家族只有合法的 `.env.example`（範例憑證，非真實憑證）。

### 16.3 全新目錄解壓驗證

使用 `mktemp -d` 建立全新暫存目錄解壓（未解壓回原專案，未重用先前測試目錄）：

- ✅ `package.json` 位於解壓根目錄，沒有多包一層資料夾
- ✅ `scripts/run-regression-g1-6-ga4-h1-4-6.js` 存在
- ✅ `H1.4.6_PRODUCT_DETAIL_CHECKOUT_FLOW_DELIVERY_REPORT.md` 位於根目錄
- ✅ `public/js/product-detail-modal.js`、`public/css/product-detail-modal.css`、`public/line-order.html`、`public/line-shipping.html` 均存在

### 16.4 解壓版本測試結果（實際執行，非沿用打包前數字）

依賴安裝方式：`npm ci --no-audit --no-fund`（lockfile-exact 安裝，僅安裝於暫存解壓目錄，**未**重新包入 ZIP）。

`node --check` 全數通過的檔案（12 個）：`product-detail-modal.js`、`analytics-platforms.js`、`utils/analyticsLog.js`、`run-g1-6-ga4-h1-4-6-runtime.js`、`static-audit-g1-6-ga4-h1-4-6.js`、`run-regression-g1-6-ga4-h1-4-6.js`、`static-audit-g1-6-ga4-h1-4-5.js`、`static-audit-g1-6-ga4-h1-4-4.js`、`run-g1-6-ga4-h1-4-4-view-item-runtime.js`、`run-regression-g1-6-ga4-h1-4-4.js`、`run-regression-g1-6-ga4-h1-4-5.js`、`run-g1-6-ga4-h1-4-5-exposure-runtime.js`。

| Suite | PASS | FAIL | TOTAL | 與打包前是否一致 |
|---|---:|---:|---:|---|
| H1.4.6 runtime | 37 | 0 | 37 | ✅ 一致 |
| H1.4.6 static/UI/Analytics | 66 | 0 | 66 | ✅ 一致 |
| H1.4.5 exposure runtime | 31 | 0 | 31 | ✅ 一致 |
| H1.4.5 static audit（更新後） | 40 | 0 | 40 | ✅ 一致 |

### 16.5 解壓版本完整 regression runner（實際執行一輪）

```
UNIQUE SUITES: 76
Round 1: PASS=7702  FAIL=0  TOTAL=7702  exit=0  residue=0
```

與打包前三輪的數字（7702/0/7702）**完全一致**。

### 16.6 背景程序與殘留確認

驗證完成後確認：無測試子程序持續執行、無 server process 殘留、無新增 PID/lock 暫存檔（掃描到的 `.pwd.lock`／`uv-setuptools-*.lock` 為沙盒環境既有系統檔案，與本次測試無關）、runner 明確回報 `residue=0`。暫存解壓目錄已於確認結果後清除；原專案、正式 ZIP、regression 三輪原始紀錄（`/home/claude/regression_logs/`）均未刪除。

### 16.7 打包前三輪 regression（回顧）

| Round | UNIQUE SUITES | PASS | FAIL | TOTAL | exit | residue |
|---|---:|---:|---:|---:|---:|---:|
| 1 | 76 | 7702 | 0 | 7702 | 0 | 0 |
| 2 | 76 | 7702 | 0 | 7702 | 0 | 0 |
| 3 | 76 | 7702 | 0 | 7702 | 0 | 0 |

### 16.8 Manual QA / LIFF / GA4 DebugView / Meta Pixel Helper

維持第 13 節的誠實分級：**全部「待人工驗證」**，本次會話未使用真實手機、LINE LIFF、GA4 DebugView 或 Meta Pixel Helper。

