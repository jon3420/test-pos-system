# R5.3-A2 — Geo Event Engine — Data Decision

本文件記錄本輪所有「資料是否真實存在」的稽核結論，以及對應的正式決策。
所有決策原則：**不假造資料，不確定就顯示明確的「尚無資料」狀態，不得為了
畫面好看而蒙混。**

---

## 1. `analytics_events.order_id` 確實存在

稽核：`PRAGMA table_info(analytics_events)` 確認 `order_id` 欄位本來就
存在（`utils/analyticsLog.js` 的 `insertEvent()` 早就支援呼叫端傳入
`order_id`，用於 `submit_order`/`purchase` 事件）。**這不是本輪新增的
欄位，是既有真實資料。**

## 2. `geo_visit_log` additive 新增 `order_id`

`geo_visit_log` 當初建立時沒有同步存這個既有欄位。本輪用 safe migration
（`ALTER TABLE geo_visit_log ADD COLUMN order_id TEXT` + 對應索引
`idx_geo_visit_log_store_order`）補上，資料來源是 `insertEvent()` 呼叫時
既有的 `order_id` 參數（`utils/analyticsLog.js` 的同步寫入 hook 原樣傳遞），
不是本輪臆測或反查產生。沒有 `order_id` 的事件，此欄位一律 `NULL`。

## 3. `analytics_events` 沒有正式 revenue/value/amount 欄位

稽核 `PRAGMA table_info(analytics_events)` 全部欄位：完全沒有
`value`/`revenue`/`amount`/`order_total`。**Analytics 事件本身沒有任何原生
金額資料可用。**

## 4. Revenue 改讀 `orders.total`，並標示 Order Data

`orders` 表本來就有 `total`（結帳總額，POS/LINE 下單流程既有欄位）。
`utils/geoEventEngine.js` 的 `getGeoEventFunnel()` 用本輪新增的
`geo_visit_log.order_id` 反查 `orders.total` 加總，回傳欄位
`revenue_source: 'order_data'` 明確標示來源。前端 Revenue Tab 顯示
「資料來源：Order Data」，不會讓人誤以為是 Analytics 原生統計。

## 5. 不混算 Analytics Revenue

因為第 3 點已確認 Analytics 沒有原生營收欄位，`revenue` 永遠只可能來自
Order Data 或 `null`（尚無可用營收事件資料），兩種來源不會同時存在、不會
相加，沒有「混算」的可能性。

## 6. Purchase 正式事件定義

稽核 `EVENT_WHITELIST`（`utils/analyticsLog.js`）：本專案只有 `purchase`
一個正式購買完成事件，**沒有** `checkout_payment_complete`。
`utils/geoEventEngine.js` 的 `PURCHASE_EVENT_NAMES` 只包含 `'purchase'`，
沿用既有定義，不重複計算、不發明不存在的事件名稱。

同理，`view_item` 在本專案的對應事件是 `view_product`（本專案用這個名稱
代表「瀏覽商品」，語意相同，名稱不同）；`session_start` 在本專案的
`EVENT_WHITELIST` 中不存在，分類表仍保留它是為了未來相容，目前不會有任何
事件命中這個分類。

## 7. `visitor_key` 正式規則（沿用 A1.2，未修改）

```sql
COALESCE(NULLIF(visitor_id,''), NULLIF(session_id,''), 'event_' || id)
```

本輪全部漏斗指標（Visitors／View Item／Add To Cart／Checkout／Purchase／
Cart Abandonment／Checkout Abandonment）一律共用 `utils/geoVisitLog.js`
匯出的同一個 `VISITOR_KEY_SQL`，沒有另外維護一份去重邏輯，也沒有改回只用
`session_id`。

## 8. Abandonment 使用集合差集，不是數字相減

`Cart Abandonment Visitors` = 「有 `add_to_cart` 事件」的 `visitor_key`
集合，扣掉「同一時間範圍內有 `purchase` 事件」的 `visitor_key` 集合
（SQL `NOT IN` 子查詢）。`Checkout Abandonment Visitors` 同理，用
`begin_checkout` 對 `purchase` 做差集。集合差集天生不會產生負數，不需要
額外的 `max(0, ...)` 兜底（但 `_countSetDifference()` 仍保留一道
`Math.max(0, ...)` 作最後防線）。本輪沒有另外建立「30 分鐘／1 小時／
24 小時」的放棄時間窗定義——放棄與否完全由「同一次查詢的時間範圍內是否
出現過 purchase」決定，沒有獨立的第二套時間窗規則。

## 9. Polygon Fixture 禁用（沿用 A1.2 稽核結論，未變更）

`public/data/geo/taiwan/taoyuan-districts.geojson` 的 13 個行政區逐一
驗證後，全部是軸對齊矩形（4 個角點座標兩兩相同），是測試用 fixture，不是
官方合法行政區邊界。`public/js/geo-visitor-layer.js` 的
`GEO_VISITOR_CHOROPLETH_OFFICIAL_CITY_CODES` 本輪原封不動維持空陣列，
**沒有為了畫面好看而啟用 fixture。**

## 10. Visitor Geo 無精確座標時不畫假點

`utils/geoResolver.js` 的 `resolveVisitorGeo()`（IP 推定）只回傳
city/district 名稱，從不回傳 `lat`/`lng`。`geo_visit_log.lat`/`lng`
對這類事件一律是 `NULL`。`geo-visitor-layer.js` 完全沒有：
- 计算行政區中心點
- 使用店家座標冒充訪客位置
- 使用矩形 fixture 的幾何中心
- 使用 `Math.random()` 產生任何座標

三層定位策略（需求文件十一）：A. 真實 lat/lng → CircleMarker（目前
Visitor Geo 永遠沒有，維持 0）；B. city/district 已知且有合法 Polygon →
Choropleth（目前允許清單為空，維持 0）；C. city/district 已知但無合法
Polygon → 只進 Ranking/Summary/Coverage/Recent Log；D. Unknown → 只進
Summary/Coverage/Recent Log。

---

## 哪些指標可以正式顯示

- **Visitors**：正式可用（`page_view`，`session_start` 保留相容但目前
  不會觸發）。
- **Add To Cart**：正式可用（`add_to_cart`）。
- **Checkout**：正式可用（`begin_checkout`）。
- **Orders（Purchase Visitors）**：正式可用（`purchase`）。
- **Purchase Orders（訂單數）**：正式可用，但**只有在該時間範圍內確實有
  帶 `order_id` 的 purchase 事件時**才顯示數字；否則明確顯示「尚無可用
  訂單識別資料」，不得顯示 0 或拿 Purchase Visitors 代替。
- **Revenue**：正式可用，但只在有對應 `orders.total` 可查時顯示，並標示
  「Order Data」；否則顯示「目前沒有可用營收事件資料」。
- **Conversion／Cart Conversion／Checkout Conversion**：正式可用，分母為
  0 一律回傳 0，不會是 NaN/Infinity。
- **Cart Abandonment／Checkout Abandonment**：正式可用（集合差集）。
- **Recommendation Risk**：正式可用，但標示「規則式計算，非 AI」；樣本量
  （`visitors < 10`）不足時顯示 `Insufficient Data`，不產生假建議。

## 哪些指標仍 Pending（本輪明確不做）

- **Ranking 依 Revenue/Conversion/Cart Abandonment/Recommendation Risk
  排序**：目前這四個 Tab 的行政區排行榜沿用 Visitors 排序（沒有為每個
  Tab 建立獨立的行政區級 SQL，見需求文件六「不得為每個 Tab 建一套
  SQL」），只有 Visitors／Add To Cart／Checkout／Orders 四個 Tab 有各自
  真正對應的行政區排序依據。
- **Official Taiwan Polygon Pack／ROI Heatmap／Delivery Zone
  Heatmap／AI Recommendation／CRM Geo／開店選址**：明確排除在本輪範圍外
  （使用者指示不得開始）。
- **Channel 篩選在 Geo Event Engine 層級**：`geo_visit_log` 目前沒有儲存
  channel 欄位，`getGeoEventFunnel()` 的 `channel` 參數目前保留但不生效，
  只有既有 Dashboard（`geo-intelligence.js`）本身的 Channel 篩選持續生效。
