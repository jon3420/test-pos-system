# R5.4-G1.6-A1.2 — Catalog Architecture

fix18-10-hotfix30-B5-R5.4-G1.6-A1.2

## Build Flow

```
data/geo/authoritative/sources/*.zip（官方原始 ZIP，不進 deploy ZIP）
  ↓ unzip（人工／一次性）
data/geo/authoritative/extracted/{county,township}/*.{shp,shx,dbf,prj,cpg,...}
  ↓ scripts/build-authoritative-admin-points.js（一次性 Build Tool）
data/geo/authoritative/taiwan-admin-representative-points.v1.json（Catalog）
  ↓ utils/authoritativeAdminPointCatalog.js（Runtime Resolver，Server 啟動時惰性載入）
utils/geoVisitLog.js（resolveAreaRepresentativeMarker／getGeoLiveMarkerModel）
  ↓
Visitor Layer（area.marker）／Dashboard（/api/geo-live/marker-model）
```

Production Server **不會**在啟動時重新解析 SHP——`build-authoritative-
admin-points.js` 只在人工執行（或未來 CI pipeline）時跑一次，產出的
Catalog JSON 才是 Runtime 真正讀取的檔案。

## Source Files

- `data/geo/authoritative/sources/`：官方原始 ZIP（本輪工作目錄內存在，
  但**不放入 deploy ZIP**——見打包排除清單）。
- `data/geo/authoritative/SOURCE_MANIFEST.json`：記錄 Dataset ID／
  Provider／License／Archive SHA-256／CRS／DBF 欄位等來源資訊。

## CRS Transform

- Source: `EPSG:3824`（GCS_TWD97[2020]，datum D_TWD_1997，ellipsoid
  GRS_1980）。
- Target: `EPSG:4326`（WGS84）。
- Library: `proj4`（Node）。
- 每個座標點都明確呼叫 `proj4(TWD97_2020_PROJ4, WGS84_EPSG4326, [lon,
  lat])` 轉換，`transform_performed: true` 記錄在輸出 Catalog 的
  `coordinate_transform` 欄位。
- 措辭：抽樣轉換結果未觀察到可量測位移，**不宣稱兩個 CRS 定義相同**。

## Encoding

`shapefile`（Node，mbostock/shapefile）預設用 `windows-1252` 解 DBF
文字欄位，不會自動讀取 `.cpg`。已修正：Build Tool 先讀取對應的 `.cpg`
（本次資料集為 `UTF-8`），明確傳入 `{ encoding }` 給
`shapefile.open()`，不硬編碼猜測。

## point_on_surface

用 `@turf/turf` 的 `turf.pointOnFeature()`，保證輸出點落在
Polygon／MultiPolygon 內部（不是幾何幾何 centroid，也不是
bounding-box center——凹多邊形／離島 MultiPolygon 這兩種情況下，
純幾何 centroid 或 bbox center 都可能落在多邊形外或海上）。

## Containment Validation

每個 Representative Point 算出後，用 `turf.booleanPointInPolygon()`
二次驗證真的落在對應幾何內；驗證失敗直接 Build Fail（不進 Catalog，
不 fallback 到其他座標來源）。

Invalid Geometry（`turf.area()` 算出 0 或非 finite）會嘗試
`turf.buffer(feature, 0)` 安全修復一次；修復後仍無效則 Build Fail。

## Deterministic Hash

- `content_sha256`：對「排除 `generated_at`／`content_sha256` 自身」的
  核心內容算 SHA-256，同一組官方來源＋同一份程式碼，任何時間重新執行
  都應得到相同值（已實測：兩次獨立 Build，`content_sha256` 完全相同）。
- Key 排序：`counties`／`districts` 皆用 `Object.keys().sort()` 排過；
  但 ECMAScript 規格規定「整數樣式 key」（不含前導零的數字字串）一律
  強制數值遞增列舉，不受這裡的 `.sort()` 影響——只有「非整數樣式 key」
  （例如帶前導零的 `"09007"`／`"09020"`，金門縣／連江縣）才會照這裡
  指定的順序排在後面。這不影響 Determinism（順序仍完全可重現），只是
  不是單純字母排序，已在 `build-authoritative-admin-points.js` 原始碼
  註解中記錄這個細節，供未來維護者參考。

## Catalog Schema

```json
{
  "schema_version": 1,
  "source_manifest_sha256": "...",
  "generated_at": "...",
  "content_sha256": "...",
  "coordinate_system": "EPSG:4326",
  "coordinate_transform": { "source_crs": "...", "target_crs": "...", "library": "proj4", "library_version": "...", "transform_performed": true, "note": "..." },
  "point_method": "point_on_surface",
  "majia_correction_status": "reference_only",
  "counties": { "<county_code>": { "county_code", "county_name", "county_name_en", "lat", "lng", "source": "nlsc_official_boundary", "accuracy": "county_centroid", "point_method": "point_on_surface" } },
  "districts": { "<district_code>": { "district_code", "district_name", "district_name_en", "county_code", "county_name", "lat", "lng", "source", "accuracy": "district_centroid", "point_method" } }
}
```

22 counties／368 districts，跟官方 Feature 數完全一致。

## Runtime Load／Status

`utils/authoritativeAdminPointCatalog.js` 惰性載入＋cache（`_loadCatalog()`
只在第一次呼叫時讀檔／驗證，之後重用）。`getCatalogStatus()` 回傳
`{ available, schema_version, source, county_count, district_count,
generated_at, source_manifest_sha256, catalog_sha256, error_code }`。

## Failure Degradation

Catalog 檔案不存在／JSON 壞掉／schema 不支援／manifest hash 不符
→ `available: false` + 對應 `error_code`（`catalog_missing`／
`catalog_invalid`／`catalog_schema_unsupported`／
`catalog_hash_mismatch`／`catalog_coordinate_invalid`），**不 throw**，
Server 正常啟動，Exact Marker 完全不受影響，Estimate Marker 安全停用。

---

*本文件不含任何真實憑證、Property、Stream 或 Token 內容。*
