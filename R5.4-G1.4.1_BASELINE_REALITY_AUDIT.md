# R5.4-G1.4.1 Baseline Reality Audit

本文件記錄本輪（fix18-10-hotfix30-B5-R5.4-G1.4.1）開始前後，透過**實際執行程式碼**（而非讀檔名或讀 completion report 文字）確認的真實狀態。凡是本文件中的數字，皆為本次工作階段中實際 `node` 執行結果，非引用先前對話宣稱的數字。

## 1. 原始 ZIP 沒有 G1.4.1

`fix18-10-hotfix30-B5-R5_4-G1_4-QA-full.zip` 解壓縮後，頂層資料夾為 `fix18-10-hotfix30-B5-R5.4-G1.4`。全專案 `grep -rl "G1.4.1"` 結果為空——沒有任何 G1.4.1 相關程式碼、文件、Smoke 或 Static Audit。最新版本是 G1.4。

## 2. 原 ZIP 實際存在的 G1.4 檔案

- `public/js/geo-heatmap.js`（569 行，含 `geoHeatComputeDrawableState`、`geoHeatRenderLayer`、Permanent Label 建立邏輯）
- `public/js/geo-heatmap-ui.js`（872 行，含 Metric Sync、Coverage Explanation、Order/Visitor Overlay）
- `public/css/geo-heatmap.css`（273 行，含 Coverage Explanation 樣式）
- `scripts/smoke-hotfix30-b5-r5-4-g1-4-map-label-rendering.js`
- `R5.4-G1.4_COMPLETION_REPORT.md`／`R5.4-G1.4_MANUAL_QA_CHECKLIST.md`／`R5.4-G1.4_MAP_LABEL_RENDERING_FIX.md`

這些檔案確實存在，不是宣稱。

## 3. 原 G1.4 Smoke 的真實 assertion count

`scripts/smoke-hotfix30-b5-r5-4-g1-4-map-label-rendering.js` 依賴 `jsdom`，但工作目錄沒有附帶 `node_modules`（ZIP 正確排除）。

- **未安裝 jsdom 時**實際執行：只跑到 5 個 `node --check`/檔案可讀性檢查就因 `require('jsdom')` 失敗而提前印出 Summary，顯示 `PASS: 5 FAIL: 0 TOTAL: 6`（第 6 項是 `MANUAL REQUIRED`，不計入 PASS/FAIL）。這個數字**不能**代表這支 Smoke 有做行為驗證。
- 本輪執行 `npm install`（jsdom 已在原始 `package.json` 的 `devDependencies` 宣告，見第 8 節）之後，**同一支檔案**實際執行結果變成 **147 PASS / 2 FAIL / 149 TOTAL**（2 個 FAIL 是 Scope Guard 對「目前檔案」的 hash 比對，因為當時已經開始疊加 G1.4.1 的修改）。

也就是說：這支 Smoke **本身寫得沒問題**（真的用 jsdom + 假 Leaflet 執行產品函式），問題出在「jsdom 沒裝的時候，摘要數字會被誤讀成『幾乎全過』」。這是本輪必須誠實記錄、也已經修正的認知落差。

## 4. 哪些測試原本只保護 dead CSS（真正的 Bug 來源）

`geo-heatmap.css` 的 `.geo-heat-coverage-explanation-text` 規則，G1.3.1 當時的修法是加上：

```css
[data-theme="dark"] .geo-heat-coverage-explanation-text,
.geo-live-theme-dark .geo-heat-coverage-explanation-text { background: #1e293b; ... }
```

實際 `grep -rn "data-theme\|geo-live-theme-dark" public/js/*.js public/*.html` 全專案結果：**除了這段 CSS 本身，沒有任何一行程式碼會把 `data-theme="dark"` 屬性或 `.geo-live-theme-dark` class 加到任何 DOM 元素上。** 這個 App 也沒有 Light/Dark 切換機制——`public/css/main.css` 的 `:root` 直接把 `--bg-panel`/`--text-primary` 等變數定成深色系，是全站唯一固定主題。

換句話說，G1.3.1 加的「Dark Theme 修正」是**永遠不會被瀏覽器選中的死規則**。實際生效的只有沒有 gating 的基礎規則：`background:#f8fafc`（近白色）+ 沒有自訂 `color`（繼承 body 的淺色文字），這就是使用者截圖看到的「白色橫條、文字幾乎看不見」。

以下測試檔案原本斷言這段死 CSS「必須存在」，等於在保護這個 Bug，本輪已全部改為驗證修正後的真實狀態：

- `scripts/smoke-hotfix30-b5-r5-4-g1-3-1-coverage-total-dark-theme.js`（assertion 41-48，共 8 項）
- `scripts/smoke-hotfix30-b5-r5-4-g1-3-2-regression-guard.js`（assertion 37、98、99，共 3 項）
- `scripts/static-audit-g1-3-1.js`（check 21、22、23，共 3 項）
- `scripts/static-audit-g1-3-2.js`（check 42、43，共 2 項）

## 5. 哪些功能原本已存在（本輪未新增）

- `geoHeatComputeDrawableState()`：G1.4 既有的五態分類器，本輪未修改邏輯。
- Permanent District Label（`L.tooltip({permanent:true})`）：G1.4 既有，本輪未移除。
- Hover Tooltip（`marker.bindTooltip(...)`）：G1.4 既有，本輪未移除。
- `clearLayers()` 每次重繪前清空：G1.4 既有，本輪未移除。
- Order/Visitor Layer exclusivity（`_geoHeatUiApplyLayerExclusivity`）：G1.2 既有。
- `geoHeatUiSyncMetricFromGlobal()`／`geoHeatUiSetMetric()`：G1.3 既有，**本輪確認邏輯本身正確**（見第 6 節）。
- Scope Guard 第一層（G1.3.1）、第二層（G1.4）：既有，本輪未修改內容，只是疊加第三層。

## 6. 本輪產品修正新增了什麼

在 `public/js/geo-heatmap.js` 新增（Scope Guard G1.4.1 Allowlist 第 1 項）：

- `geoHeatGetAreaMetricValue(area, metric)`：單一權威 metric value resolver。
- `geoHeatMetricSupportsCoordinate(metric)`：判斷 metric 是否結構性有座標收集機制。
- `_geoHeatIsValidLatLng(lat, lng)`：座標型別／範圍／0,0 驗證。
- `geoHeatIsAreaEligibleForMetric(area, metric)`：單一權威 eligibility 判斷（含 metric 合法性檢查——本輪第二次迭代新增，見第 12 節）。

並將以下既有邏輯改為呼叫上述共用函式（Allowlist 第 2-5 項）：

- `geoHeatBuildRanking()` 的 `value`/`ratio`/`has_coordinate` 改用共用 resolver/eligibility。
- `geoHeatRenderLayer()` 的 `plottable` 篩選、`value` 取值改用共用 resolver/eligibility。
- `geoHeatSelectArea()` 的 panTo 判斷改用共用 eligibility。
- `module.exports` 新增匯出上述三個函式。

在 `public/css/geo-heatmap.css` 修正 `.geo-heat-coverage-explanation-text` 規則：移除對不存在的 `[data-theme="dark"]`/`.geo-live-theme-dark` 的依賴，改用專案既有的 `var(--bg-card,...)`/`var(--text-primary,...)`/`var(--border,...)`（與本檔案其餘規則一致的慣例）。

在 `scripts/lib/geo-heatmap-g131-scope-guard.js` 新增第三層 `GEO_HEATMAP_G141_ALLOWED_ADDITIONS`（6 項），並將 `computeScopedBaselineCheckForSource()` 改為三層鏈式還原（G1.4.1 → G1.4 → G1.3.1 → pristine）。

## 7. 本輪修正的 Test Harness

除第 4 節列出的 dead-CSS 斷言更新外：

- `scripts/smoke-hotfix30-b5-r5-4-g1-4-map-label-rendering.js`：assertion 128-130 改用新的三層 Scope Guard 鏈（原本只還原兩層，本輪已在檔案新增第三層後過期）。
- `scripts/static-audit-g1-4.js`：check 8（plottable 篩選寫法）、check 30（Scope Guard 鏈式呼叫順序）改為驗證修正後的真實程式碼。

新增（非修正）：

- `scripts/smoke-hotfix30-b5-r5-4-g1-4-1-dark-card-metric-rendering.js`：149 個真實執行的行為斷言。
- `scripts/static-audit-g1-4-1.js`：56 個靜態檢查。

## 8. jsdom 是否原本已在 package.json

**是。** 原始 ZIP 的 `package.json`（timestamp 與其他原始檔案相同，未被本輪修改）`devDependencies` 已宣告 `"jsdom": "^24.1.3"`。本輪執行 `npm install`（無 `--save`）只是把已宣告的 devDependency 實際安裝進 `node_modules`，因為原始 ZIP 正確地沒有附帶 `node_modules`。**本輪沒有修改 `package.json` 或 `package-lock.json`。**

## 9. 是否為了測試臨時安裝依賴

`jsdom` 本身不是臨時安裝的「額外」套件（見第 8 節），但這個工作目錄先前沒有 `node_modules`，本輪透過 `npm install` 讓它存在。這個 `node_modules` 目錄**只存在於本次工作階段的容器內**，不納入交付 ZIP（見第 10 節與清理章節）。

## 10. node_modules 不納入 ZIP

打包前已執行 `rm -rf node_modules` 並在 `unzip -l` 驗證 ZIP 內容不含 `node_modules/` 路徑（見 ZIP 驗證章節）。

## 11. 哪些數字已實際執行證實

以下數字全部是本次工作階段中直接執行 `node <script>` 取得，並重複執行三輪確認數字一致（見 Regression 章節）：

| Suite | PASS | FAIL | TOTAL |
|---|---|---|---|
| G1（geo-live）| 212 | 0 | 212 |
| G1.1 | 91 | 0 | 91 |
| G1.2 | 83 | 0 | 83 |
| G1.3 | 117 | 0 | 117 |
| G1.3.1 | 115 | 0 | 115 |
| G1.3.2 | 148 | 0 | 148 |
| G1.4 | 148 | 0 | 148 |
| G1.4.1（新）| 149 | 0 | 149 |
| A7 | 87 | 0 | 87 |
| A5 | 20 | 0 | 20 |
| A5 live e2e | 16 | 0 | 16 |
| A4 | 110 | 0 | 110 |
| A2 | 229 | 0 | 229 |
| A1.2 | 189 | 0 | 189 |
| B2 | 620 | 0 | 620 |
| B3 | 157 | 0 | 157 |
| Static Audit G1 | 30/30 | — | — |
| Static Audit G1.2 | 37/37 | — | — |
| Static Audit G1.3 | 32/32 | — | — |
| Static Audit G1.3.1 | 40/40 | — | — |
| Static Audit G1.3.2 | 48/48 | — | — |
| Static Audit G1.4 | 52/52 | — | — |
| Static Audit G1.4.1（新）| 56/56 | — | — |

全專案 217 個 `.js` 檔案 `node --check` 全數通過。

Scope Guard `computeScopedBaselineCheck()`／`computeScopedBaselineCheckForSource()`／`runBehavioralInvariants()` 三個函式皆已直接呼叫驗證，回傳 `ok: true`。

## 12. 過程中發現並修正的真實問題（不是憑空冒出的，是執行測試時真的抓到的）

1. **CSS 白色橫條**：dead selector，見第 4 節。
2. **Visitors=0 仍畫 Marker**：`geoHeatRenderLayer()` 原本只看 `coordinate_source`，未檢查 metric 有效性。
3. **`geoHeatGetAreaMetricValue` 漏掉 export**：Static Audit 執行後才發現 `module.exports` 忘記加這個函式，當場修正。
4. **Scope Guard 重複 allowlist 項目**：在編輯 `geo-heatmap-g131-scope-guard.js` 新增第三層時，一次 `str_replace` 操作意外複製出重複的 array item，導致 `reconstructG141Layer` 內同一個 id 出現兩次、其中一次必然 count=0 而 FAIL。執行測試時發現並清理。
5. **Eligibility 未檢查 metric 合法性**：擴充 Smoke 到 149 項時，新增的邊界測試（不合法 metric 名稱）發現 `geoHeatIsAreaEligibleForMetric()` 對未知字串會誤判為合法，補上 `GEO_HEAT_METRICS.includes(metric)` 檢查。
6. **測試誤用 `dom.window.CONST_NAME`**：Metric Sync 補強測試一開始誤用 `dom.window.GEO_HEATMAP_TO_EVENT_METRIC`，但 `const` 頂層宣告在 jsdom 的 `window.eval()` 語意下不會附加到 `window` 物件（只有明確呼叫 `window.xxx = ...` 的，例如 `geoHeatState`，才會出現）。改用 `require()` 的 module.exports 驗證同一份對照表。
7. **`data/pos.db` 由多支測試在執行期間動態建立**：`smoke-hotfix30-b5-r5-4-g1-live-geo.js`、`...g1-3-1...`、`...g1-3-2...`、`...g1-4-map-label...`、`...a5-visitors...`、`verify-a5-live-e2e.js`、`...a2-geo-event-engine...`、`...a1-2-visitor-geo-sync...` 這幾支測試會實際開一個 sqlite DB 連線到 `data/pos.db`。這是這些測試**既有的設計**（本輪未新增此行為），不是本輪引入的污染，但代表「no test DB」這類 Static Audit 檢查必須在這些測試跑完之後、打包之前執行才有意義；本輪 Regression 已把 DB 清除步驟安排在每輪最後與 Static Audit 之前。

## 13. 哪些 Manual Visual 仍未驗證

**全部未驗證。** 本次工作階段沒有真實瀏覽器環境，所有「畫面看起來如何」的判斷都只能透過程式碼邏輯與自動化測試推論，不能取代真人眼睛看畫面。詳見 `R5.4-G1.4.1_MANUAL_QA_CHECKLIST.md`，狀態一律標記為 **NOT TESTED**。使用者先前提供的截圖只能列為 **Pre-fix Browser Evidence**（修正前的問題證據），不能作為 Post-fix PASS 的依據。

## 14. 誠實限制聲明

- G1.4.1 專屬 Smoke 最終為 149 個真實斷言，超過規格要求的「至少 140」，但這是逐項新增有意義的邊界/行為條件湊出來的，過程中曾經因為測試本身的錯誤（見第 12 節第 6 項）而非產品錯誤導致短暫 FAIL，已修正並如實記錄，不是一次寫對。
- 本文件與其餘三份文件皆基於**這個工作階段內**的執行結果，不引用任何先前對話中出現、但未在目前工作目錄重新驗證過的數字（例如先前對話提到的「147/147」等，本輪一律重新執行取得目前真實數字，如與先前提及數字不同，以本文件實際執行結果為準）。
