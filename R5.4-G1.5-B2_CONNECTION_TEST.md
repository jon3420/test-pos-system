# R5.4-G1.5-B2 Connection Test

fix18-10-hotfix30-B5-R5.4-G1.5-B2 — GA4 Realtime Connection Test

## Read-only Test

`utils/ga4Realtime/connectionTest.js` 的 `runGa4ConnectionTest(db,
storeId)` 完全只讀：讀已儲存設定 → 發兩個 Realtime Report 呼叫 → 回傳
sanitized 結果。不寫入一般 Realtime data cache（`utils/ga4Realtime/
index.js` 的 `_cache`），只保存自己的 `last_test_at`/`last_test_result`
非敏感摘要。

## Stored Config Only

一律用 `getGa4RealtimeConfig(db, storeId)` 取得 Property/Stream，完全不
接受 Body 傳入的覆寫（`routes/geo-live.js` 的 `POST
/ga4-realtime-test` handler 沒有讀取 `req.body` 的任何欄位）。

## Summary Request／City Request／Window 30／Visitors／Stream Filter

沿用 G1.5-A 既有的 `buildGa4RealtimeSummaryRequest`／
`buildGa4RealtimeCityRequest`，固定 `windowMinutes: 30`／
`metric: 'visitors'`，`streamId` 帶入 `config.streamId`（若有設定）。

## Connected With Data／Connected Without Data

```
summaryResult.rows[0].metricValues[activeUsersIdx].value > 0
  → connected:true, has_recent_data:true
否則
  → connected:true, has_recent_data:false（仍是成功，不是錯誤）
```

## Error Mapping

| error_code | 情境 |
|---|---|
| `credential_unavailable` | SDK 不可用 |
| `credential_invalid` | （既有 client.js 分類） |
| `permission_denied` | Google 回 401/403 |
| `property_not_found` | Google 回 404 |
| `stream_filter_invalid` | Google 回 `INVALID_STREAM` |
| `quota_limited` | Google 回 429 |
| `ga4_timeout` | 逾時 |
| `ga4_unavailable` | 其他未分類錯誤（fallback） |

## Rate Limit／retry_after_seconds

同店 30 秒內最多一次真正的 Google 呼叫；超過限制直接回上一次結果並標記
`rate_limited:true`，附上 `retry_after_seconds`（`Math.max(0,
Math.ceil((30000 - elapsedMs) / 1000))`，保證非負整數）。前端
`geoGa4SettingsTestConnection()` 優先採用這個值決定 cooldown 顯示秒數，
沒有值時才 fallback 固定 30 秒。

## Single-flight

同店併發呼叫共用同一個 Promise（`_inFlightTest` Map，以 storeId 為
key）；已用 Smoke 驗證：同店 10 個併發呼叫只產生 1 組（summary+city＝2
次）Google 呼叫，不同店各自獨立。

## Quota

Request 帶 `returnPropertyQuota:true`（沿用 G1.5-A 既有 request
builder），但 Connection Test 回應本身不回傳任何 quota 相關欄位——這是
Realtime data endpoint（`/ga4-realtime`）才有的 `quota_status`
概念，Connection Test 只關心「連得上連不上」。

## Sanitization

回應**不含**：城市名稱列表、逐城市 activeUsers 明細、raw Google
`rows`、raw `propertyQuota` 物件、Property ID、Stream ID、任何憑證
資訊。只回傳布林旗標（`connected`／`sdk_available`／
`credential_available`／`property_accessible`／`stream_filter_valid`／
`realtime_request_ok`／`has_recent_data`）、`rows_count`（純數字，不是
明細）、`tested_at`、`message`、`error_code`。

## No automatic live test

`geoGa4SettingsInit()`（Tab 切入時）只呼叫 `GET
/api/settings/ga4-realtime`，從不呼叫 Connection Test；後者只綁在
「測試連線」按鈕的 `onclick`。

## Real GA4 Status

**NOT TESTED**。沙箱環境沒有真實 Google Cloud 憑證，也沒有出網
allowlist 對 `googleapis.com` 的存取。所有驗證都透過假 GA4 client 完成。
