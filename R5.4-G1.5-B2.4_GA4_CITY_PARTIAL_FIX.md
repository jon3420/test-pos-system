# R5.4-G1.5-B2.4 — GA4 City Partial Fix Report

fix18-10-hotfix30-B5-R5.4-G1.5-B2.4 — GA4 City Request Compatibility & Partial
Success Hotfix

## 1. 正式環境現象

使用者回報：Settings 頁面「測試連線」顯示成功，但地圖圖層（正式資料
Endpoint）回 502，無法載入。詳細技術背景見
`R5.4-G1.5-B2.4_CITY_REQUEST_REALITY_AUDIT.md`。

## 2. Connection Test PASS

修正前：`POST /api/geo-live/ga4-realtime-test` 回 `connected:true`（Summary
成功即回成功，未檢查 City 結果）。

## 3. Data Endpoint 502

修正前：`GET /api/geo-live/ga4-realtime` 只要 City Request 失敗，整個回
`HTTP 502`（跟 Summary 失敗同等處理）。

## 4. Root Cause 範圍

Connection Test 與正式 Data Endpoint 對「City Request 失敗」判斷不對稱：
Connection Test 忽略、Data Endpoint 不忽略。詳見 Reality Audit 第 7 節。
**未證實**四維 City Request 本身是 Google 端拒絕的直接原因（無 Raw Google
Error 佐證），本輪修正的立足點是「聚合器從未使用 cityId／country，移除它們
本身就是合理的相容性風險縮減，與是否證明它們是 502 根因無關」。

## 5. Connection Test False Positive

`utils/ga4Realtime/connectionTest.js` 重寫 `_runTestNow()`：

- **Case A**（Summary 成功＋City 成功）：`connected:true`，
  `summary_request_ok:true`，`city_request_ok:true`，
  `realtime_request_ok:true`，`error_stage:null`。
- **Case B**（Summary 失敗）：`connected:false`，`summary_request_ok:false`，
  `city_request_ok` 依實際結果，`realtime_request_ok:false`，
  `error_stage:'summary'`。
- **Case C**（Summary 成功＋City 失敗）：`connected:false`，
  `property_accessible:true`，`stream_filter_valid:true`，
  `summary_request_ok:true`，`city_request_ok:false`，
  `realtime_request_ok:false`，`error_stage:'city'`，
  `message:'GA4 基本連線成功，但城市區域資料請求失敗。'`
  （不再與 Case B 共用「完全連不上」的訊息）。

`rows_count` 於 City 失敗時固定為 0；`has_recent_data` 一律只從 Summary
結果計算，不依賴 City rows 是否存在。回應不含 raw rows／raw quota／
Property／Stream／Credential。

## 6. Summary Request

無 dimensions，官方 `activeUsers`／`eventCount`／（依 metric）
`screenPageViews`，唯一的 `total_active_users_ga4` 來源。本輪未變更其邏輯。

## 7. City Request

維度自四維（`city`／`cityId`／`country`／`countryId`）縮減為二維（`city`／
`countryId`）。`_aggregateCityRows()` 同步更新為只讀取這兩個維度，並保持對
四維舊 fixture／舊 cache 的向後相容（`indexOf` 找不到的維度回 -1，安全跳過，
不假設 `cityId`／`country` 存在）。

## 8. Request Pair

新增 `utils/ga4Realtime/requestPair.js` 的 `runGa4RealtimeRequestPair()`，
單一實作同時供 `connectionTest.js`（不重試）與 `index.js`
（`_runWithRetry` 包裝）共用，內部 `Promise.all` 平行送出 Summary／City，
各自安全記錄 `stage/code/retryable/window/metric/elapsed_ms`，不記錄
Property／Stream／Credential／Raw Error／Raw Response。

## 9. Minimal Dimensions

見第 7 節。理由：地圖只需要城市名稱做台灣縣市正規化、只需要 countryId 排除
非 TW、`cityId`／`country` 從未被使用、縮減回應大小與 Realtime Dimensions
相容性風險。

## 10. Aggregator Backward Compatibility

B2.4 Smoke D51/D52 已用真實函式呼叫驗證：新兩維格式與舊四維格式（含舊
cache entry 可能殘留的形狀）都能正確聚合，不會因為維度縮減而讓舊資料解析
失敗。

## 11. Partial Success

`utils/ga4Realtime/index.js` 的 `_fetchAndBuildPayload()`：

- Summary 失敗：仍 `throw new Ga4RealtimeError(..., 502, 'summary')`，
  Route 回非 200。
- Summary 成功＋City 成功：`status:'fresh'`，正常寫入 Full Cache。
- Summary 成功＋City 失敗：回傳（不 throw）`status:'partial'`，
  `HTTP 200 success:true`，保留正確的 Summary 數值，`counties:[]`，
  `unmapped:[]`，`error_code:'city_request_failed'`，`error_stage:'city'`，
  `notices` 含專屬提示文字與既有 IP 推估免責聲明。不使用假縣市資料、不用
  店家地址替代、不畫 Marker／Circle。

## 12. Partial Cache Policy

Cache 寫入條件新增 `&& payload.status !== 'partial'`；
`_storeLastSuccessAt`／`_storeLastErrorCode`（完整成功時間戳記／錯誤代碼）
同樣跳過 partial 更新，避免 status endpoint 誤導使用者以為地圖資料剛更新過。

**Partial Cache 專項驗證（獨立腳本，非 Regression 套件內）**：

- 場景 A：Full Success → 正常寫入 Cache，`status:'fresh'`，
  `cacheWritten:true`。
- 場景 B：同 Cache Key，City 失敗 → `status:'partial'`，
  `is_cached:false`，`is_stale:false`；Full Cache entry
  的 `JSON.stringify(data)` 在 Partial 發生前後逐 byte 相同
  （`fullCacheIntact:true`），且 entry 仍存在（`fullCacheStillExists:true`）。
- 場景 C：下一次 Summary+City 全成功 → `status:'fresh'`，`fetched_at`
  正確更新（`fetched_at_changed:true`），counties 正常換新。
- 場景 D：沒有既有 Full Cache，City 失敗 → `status:'partial'`
  正常回前端，`cacheEmpty:true`，`is_cached:false`；不觸發快速重試（沿用
  既有 Auto Refresh 60s/120s/停止機制，未新增任何加速重試邏輯）。

## 13. Full Cache 不被覆蓋

見第 12 節場景 B 的實測結果：Partial 完全不寫入、不覆蓋、不刪除既有 Full
Cache entry。

## 14. Frontend Partial State

`public/js/geo-ga4-realtime-layer.js`：

- `geoGa4NormalizeResponse()` 通過 `status:'partial'` 與 `error_stage`。
- `geoGa4StatusMessage()` 新增 partial 專屬文案：「已取得 GA4 即時總覽，但
  城市區域資料暫時無法載入。」
- `geoGa4RenderNoticesHtml()` 對 partial 跳過一般「查無城市資料」空狀態文案
  （避免誤導：City 是「失敗」不是「Google 沒資料」）。
- `geoGa4MapOverlayMessage()` 對 partial 回 `null`（不顯示「載入失敗」地圖
  覆蓋層；Summary 卡片與 toolbar 正常顯示、Refresh 按鈕仍可用）。
- `public/js/geo-ga4-settings.js` 的 `geoGa4SettingsRenderTestResult()`
  優先顯示 `error_stage==='city'` 的專屬訊息，不被一般 error_code 對照表
  覆蓋成「完全連不上」的說法。

## 15. Error Stage

`Ga4RealtimeError` 新增 `stage` 屬性（`'summary'|null`；City 單獨失敗已轉
Partial Success，不會走到這個例外分支）。`routes/geo-live.js` 的錯誤回應
新增安全 `stage` 欄位，維持只回
`success/code/stage/message/retryable/status` 六個安全欄位，不回
stack/rawError/credential。

## 16. Safe Diagnostics

見 Reality Audit 第 13 節。

## 17. 5／30 分鐘

未變更，見 Reality Audit 第 17／18 節。

## 18. B2.4 Smoke 139/139

`scripts/smoke-hotfix30-b5-r5-4-g1-5-b2-4-ga4-city-partial.js`：
**139 PASS ／ 0 FAIL ／ 139 TOTAL**，連續三次一致。涵蓋 Request Builder、
Request Pair、Connection Test、Aggregation、Full Success、Partial Success、
Partial Cache、Summary Failure、Frontend、Mutation Negative 十個區塊。

## 19. B2 Settings 187/187

`scripts/smoke-hotfix30-b5-r5-4-g1-5-b2-ga4-settings.js`：原 181 項中的
E78（City Request 維度數量斷言）為合理 Contract 變更（四維→二維），已更新
斷言內容並新增 5 項更強驗證（E78b-f：`summary_request_ok`／
`city_request_ok`／`connected`／`error_stage`／專屬 message），
以及 K174 因 Partial Cache 條件變更需要放寬到「同一個 if 仍以 generation
比對為必要條件之一」（Category B）。最終 **187 PASS ／ 0 FAIL ／ 187
TOTAL**。未刪除任何既有斷言，也未降低 TOTAL。

## 20. Regression 三輪

使用新建的 `scripts/run-regression-b2-4.js` Regression Runner，依序執行
23 支既有 Smoke／Static Audit（B2.4／B2.3／B2.2／B2.1／B2 Settings／B2
Static Audit／B2a／B1／B1 Static Audit／G1.5-A／G1.5-A Static Audit／Geo
Map Settings／Geo Settings UI／G1 geo-live／G1.4.1／G1.4／G1.3.2／G1.2／
A2／A1.2／Static Audit G1.4.1／G1.4／G1.3.2），加上對 7 個本輪修改的原始碼
檔案 `node --check`。

**三輪結果完全一致，全部 PASS，FAIL=0，exit=0**：

| Suite | Round 1/2/3 |
|---|---|
| B2.4 Smoke | 139/139/139 |
| B2.3 Smoke | 75/75/75 |
| B2.2 Smoke | 95/95/95 |
| B2.1 Smoke | 85/85/85 |
| B2 Settings Smoke | 187/187/187 |
| B2 Static Audit | 82/82/82 |
| B2a Smoke | 106/106/106 |
| B1 Smoke | 168/168/168 |
| B1 Static Audit | 71/71/71 |
| G1.5-A Smoke | 140/140/140 |
| G1.5-A Static Audit | 77/77/77 |
| Geo Map Settings | 620/620/620 |
| Geo Settings UI | 157/157/157 |
| G1 geo-live | 212/212/212 |
| G1.4.1 | 149/149/149 |
| G1.4 | 148/148/148 |
| G1.3.2 | 148/148/148 |
| G1.2 | 83/83/83 |
| A2 | 229/229/229 |
| A1.2 | 189/189/189 |
| Static Audit G1.4.1 | 56/56/56 |
| Static Audit G1.4 | 52/52/52 |
| Static Audit G1.3.2 | 48/48/48 |

每輪前清除 `data/pos.db`；輪次結束後確認無殘留 DB。Runner exit code 0。

## 21. Security Scan

全面掃描工作目錄（排除 `node_modules`／`.git`）搜尋
`BEGIN PRIVATE KEY`／`END PRIVATE KEY`／`private_key_id`／
`iam.gserviceaccount.com`：**0 筆命中**（`node_modules` 內的 SDK 原始碼／
型別定義有出現這些字面值，屬於第三方套件本身程式碼，非使用者憑證，且
`node_modules` 排除於 ZIP 之外）。

`client_email`／`private_key`／`access_token`／`refresh_token` 的所有命中
逐一人工核對：全部是程式變數名稱、denylist 陣列項目、或說明性註解／文件
文字，**沒有一筆是真實憑證內容**。`.env.example` 的 GA4 相關變數全部是空白
placeholder。工作目錄中找不到任何使用者先前誤上傳的 Service Account JSON
檔案。

## 22. Manual Visual：NOT TESTED

見 Reality Audit 第 19 節。

## 23. Real GA4

- Credential：PASS（使用者回報，本輪未重新驗證，程式邏輯層面憑證載入
  路徑未變更）
- Permission：PASS（同上）
- Connection Test：PASS（同上，但需注意：Case C 修正後，若正式環境真的是
  「Summary 成功＋City 失敗」，重新測試後 Connection Test 的回應會變成
  `connected:false`，這是本輪修正後的正確行為，不是新的故障）
- Map Data Endpoint：**尚待部署驗證**——本輪修正基於程式碼邏輯稽核與
  Smoke／Regression 測試，尚未在正式 GA4 Property／Stream 上重新確認
  `status:'partial'` 分支真的解決了使用者回報的 502。

---

*本文件不含任何真實 Property、Stream、Service Account Email、Key ID、
private key 或 Base64 憑證內容。*
