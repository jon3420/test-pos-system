# R5.4-G1.6-GA4-H1 — Test Report

## A. Test Scope

不同測試層各自獨立通過，**不假裝合併成單一腳本結果**：

1. QA Harness — Temp DB + Mock Adapter，走真實 Production Sync Service。
2. Credential Guard — 無憑證/Fake Client/Reject Promise 六種場景。
3. Frontend Runtime — 真實 jsdom + Fake Leaflet + Fake fetch，載入真實
   `geo-ga4-h1-panel.js`。
4. Targeted Smoke — 真實 HTTP 整合測試 + Mutation Negative（24 項）。
5. Static Audit — Regex 掃描真實 Production 檔案（剝除註解後）。
6. Shared GA4 Client Regression — 既有 G1.5-A/A1.2.1/A1.1/B2.4/B2.5/geo-live
   套件，驗證本輪修改沒有破壞既有功能。
7. Full Regression — 上述所有層 + 其餘既有套件，31 個 Suite × 3 輪。
8. Security Scan — 582 個檔案關鍵字掃描。
9. ZIP Verification — 見 G 節，PENDING（本文件產出時尚未執行，見文件
   下方回填章節）。
10. Clean Extraction — 見 G 節，PENDING。
11. Manual QA — 見 `R5.4-G1.6-GA4-H1_MANUAL_QA_CHECKLIST.md`，NOT TESTED。
12. Production GA4 Verification — 未對真實 GA4 Property 執行過，
    READY FOR MANUAL DEPLOYMENT VERIFICATION。

## B. GA4-H1 Test Results（本輪新增五層）

| 測試層 | Script | Expected | Actual | Exit Code | 連續執行次數 | Result |
|---|---|---|---|---|---|---|
| QA Harness | `scripts/run-g1-6-ga4-h1-qa.js` | 22/22 | 22/22 | 0 | 3（本輪 fresh rerun 再確認 1 次，共 ≥3） | PASS |
| Credential Guard | `scripts/smoke-hotfix30-b5-r5-4-g1-6-ga4-h1-credential-guard.js` | 17/17 | 17/17 | 0 | 3+ | PASS |
| Frontend Runtime | `scripts/run-g1-6-ga4-h1-frontend-runtime.js` | 73/73 | 73/73 | 0 | 3+ | PASS |
| Targeted Smoke | `scripts/smoke-hotfix30-b5-r5-4-g1-6-ga4-h1.js` | 108/108 | 108/108 | 0 | 3+ | PASS |
| Static Audit | `scripts/static-audit-g1-6-ga4-h1.js` | 166/166 | 166/166 | 0 | 3+ | PASS |

**誠實揭露**：Targeted Smoke 最初的需求目標是單一腳本 PASS≥260，本輪已
正式改採「跨測試層覆蓋」驗收原則（見對話紀錄），108/108 是 Targeted
Smoke 這一層真實、不重複計數的 Assertion 數，Static Audit 的 166 與
Frontend Runtime 的 73 是完全獨立的另外兩層，三者不得相加後宣稱成單一
數字。

## C. Shared GA4 Client Regression

| Suite | Script | Expected | Actual | Result |
|---|---|---|---|---|
| G1.5-A Smoke | `scripts/smoke-hotfix30-b5-r5-4-g1-5-a-ga4-backend-correctness.js` | 140/140 | 140/140 | PASS |
| G1.5-A Static Audit | `scripts/static-audit-g1-5-a.js` | 77/77 | 77/77 | PASS |
| G1 geo-live | `scripts/smoke-hotfix30-b5-r5-4-g1-live-geo.js` | 212/212 | 212/212 | PASS |
| A1.2.1 Smoke | `scripts/smoke-hotfix30-b5-r5-4-g1-6-a1-2-1-time-and-marker-qa.js` | 190/190 | 190/190 | PASS |
| A1.2.1 Static Audit | `scripts/static-audit-g1-6-a1-2-1.js` | 106/106 | 106/106 | PASS |
| A1.2.1 QA | `scripts/run-g1-6-a1-2-1-manual-qa.js` | 41/41 | 41/41 | PASS |
| B2.4 | `scripts/smoke-hotfix30-b5-r5-4-g1-5-b2-4-ga4-city-partial.js` | 139/139 | 139/139 | PASS |
| B2.5 | `scripts/smoke-hotfix30-b5-r5-4-g1-5-b2-5-district-normalization.js` | 76/76 | 76/76 | PASS |
| A1.1 Smoke | `scripts/smoke-hotfix30-b5-r5-4-g1-6-a1-1-runtime-wiring.js` | 160/160 | 160/160 | PASS |
| A1.1 Static Audit | `scripts/static-audit-g1-6-a1-1.js` | 90/90 | 90/90 | PASS |

### Category B 修正記錄

**原 Contract**（`scripts/static-audit-g1-5-a.js` check #2）：
`client.js` 整份檔案不得出現 `.runReport(` 呼叫。

**新 Contract**：`runGa4RealtimeReport()` 仍只能使用
`client.runRealtimeReport()`；本輪新增的獨立函式 `runGa4Report()` 合法
使用 `client.runReport()`（支援 Historical 查詢，沿用同一個 Singleton
Client，不建立第二個 Client）。

**處理方式**：原 Assertion 沒有被刪除，改成只檢查
`runGa4RealtimeReport()` 這個函式自己的函式體是否還是只呼叫
`runRealtimeReport`，精準對應原始意圖（防止 Realtime 路徑被靜靜換成
Historical API），同時允許新函式合法使用 `runReport`。修正後
77/77（原 76/77）。

## D. Full Regression

Runner：`scripts/run-regression-g1-6-ga4-h1.js`（本輪最新一次 fresh 執行）：

```
node --check: OK
Suites per round: 31
3 rounds all green: YES
3 rounds consistent: YES
data/pos.db residue: no
Exit Code: 0
```

| Round | Suite Count | PASS Count | FAIL Count | Exit Code | Residue Check | Result |
|---|---|---|---|---|---|---|
| 1 | 31 | 31 | 0 | 0 | 無殘留 | PASS |
| 2 | 31 | 31 | 0 | 0 | 無殘留 | PASS |
| 3 | 31 | 31 | 0 | 0 | 無殘留 | PASS |

「PASS Count」欄位是指「該輪 31 個 Suite 裡有幾個 Suite 整體判定為 PASS」
（Suite 層級，不是 Assertion 層級）。

**Assertion 總數**：31 個 Suite 各自的 `pass=N` 加總，由 Runner 對 Round 1
的真實輸出逐行加總得出 **3,916**，這是 Runner 對真實輸出的計算結果，
不是文件回報估計值（可用
`sed -n '/ROUND 1/,/ROUND 2/p' <regression-output> | grep '^\[PASS\]' |
awk -F'pass=' '{split($2,a," "); sum+=a[1]} END {print sum}'` 重現）。

## E. Real Bugs Found and Fixed

### 1. Route Validation／Rate Limit Ordering

- **Symptom**：`POST /api/analytics/ga4-geo/sync` 對同一店連續呼叫（一次
  合法、緊接一次打錯 range）時，第二次回 429 而非 400 `invalid_range`。
- **Root Cause**：Rate Limit 檢查寫在輸入驗證之前。
- **Classification**：Production Bug。
- **Fix**：新增 `_validateSyncBody()`，移到 Rate Limit 檢查之前執行。
- **Verification**：Targeted Smoke 真實 HTTP 整合測試（Category Z）。

### 2. Missing Credential → implicit ADC unhandledRejection／Process Crash

- **Symptom**：完全未設定憑證時呼叫 Realtime Sync，整個 Node process
  crash。
- **Root Cause**：`_getClient()` 無條件嘗試建立真正的
  `BetaAnalyticsDataClient`，google-auth-library 隱含 ADC 解析在 Zeabur
  環境下產生脫勾的背景 Promise。
- **Classification**：Production Bug（Critical——會讓整個服務不可用）。
- **Fix**：`_getClient()` 只在「要建立新 Client」且
  `credentialStatus().available===false` 時直接安全失敗；`_setClientForTest()`
  注入路徑不受影響。
- **Verification**：Credential Guard Suite 17/17（含 `unhandledRejection`
  listener 監控），既有 G1.5-A Suite 140/140 重跑確認未被破壞。

### 3. Server-side marker_point 缺口

- **Symptom**：API 只回傳 district/county 代碼，Frontend 無合法管道取得
  座標（Catalog 是 Server-only）。
- **Root Cause**：設計缺漏，未在 API 層 attach 代表點。
- **Classification**：Production Gap（非既有功能被破壞，是本輪自己的
  設計缺口，本輪內部發現並補上）。
- **Fix**：`getRealtimeGeoSummary()`/`getRangeGeoStats()` 加上
  `resolveMarkerPoint()`，回傳 `marker_point`/`marker_accuracy`。
- **Verification**：Static Audit（`Marker: attached server-side...`）+
  Frontend Runtime #28/#29。

### 4. Static Audit require-time side effect 污染 Regression stdout parser

- **Symptom**：Regression Runner 對 Targeted Smoke 解析出
  `pass=166`（Static Audit 的數字），不是 Smoke 自己的 108。
- **Root Cause**：`static-audit-g1-6-ga4-h1.js` 在模組頂層直接執行並
  `console.log` 全部檢查結果；Smoke 腳本 `require()` 這個模組做 Mutation
  Negative 測試時，副作用把 Audit 的摘要行也印進 Smoke 的 stdout，
  Regression Parser 抓到第一個符合 Pattern 的行。
- **Classification**：Test Environment／Fixture Bug（Category C）。
- **Fix**：把「執行並列印」包成 `runAudit()` 函式，只在
  `require.main === module` 時呼叫；`require()` 這個模組不再有印出
  副作用。
- **Verification**：修正後 Regression Runner 對 Smoke 正確解析出
  108/108。

### 5. Smoke／QA Harness Temp DB Cleanup 缺少 exit safety net

- **Symptom**：Regression Runner 偵測到 `/tmp` 殘留 `ga4-h1-smoke-*.db`
  檔案。
- **Root Cause**：Temp DB 清理只寫在 `main()` 正常結束路徑的最後一行，
  若中途以非預期方式終止，清理永遠不會執行。
- **Classification**：Test Environment／Fixture Bug（Category C）。
- **Fix**：加上 `process.on('exit', ...)` 保底清理（幾乎所有終止路徑都
  會觸發），跟原本的 unlink 疊加不衝突。
- **Verification**：修正後重跑 Full Regression 三輪，`data/pos.db
  residue: no`，`/tmp` 無殘留檔案。

### 6. Search／Sort Frontend Product Gap

- **Symptom**：初版 Frontend Runtime 報告「行政區搜尋／表格排序」為
  NOT IMPLEMENTED。
- **Root Cause**：GA4-H1 原始 UI 規格（需求文件第十五節）本來就要求
  這兩個功能，初版遺漏。
- **Classification**：Production Gap（本輪內部發現並補齊，不是既有功能
  被破壞）。
- **Fix**：實作搜尋（trim/大小寫不敏感/中英文）與排序（13 欄位，
  `localeCompare('zh-Hant')`，null-last），且刻意只重繪 `<tbody>` 保留
  搜尋框焦點。
- **Verification**：Frontend Runtime 新增 20+ 項真實測試（#43–#63），
  Static Audit 新增 12 項。

### 7. Mutation Test Authoring Bugs（#9／#10）

- **Symptom**：兩個 Mutation Negative（Property Cross-read／Unknown
  Forced Guess）一開始沒有真的證明「移除保護後測試會 FAIL」。
- **Root Cause**：Mutation 目標字串沒有對到真實 Production 原始碼（一個
  搜尋了剝除註解後的字串，另一個用的測試輸入在到達被 mutate 的分支前就
  被更早的分支短路掉）。
- **Classification**：Test Authoring Bug（Category C）。
- **Fix**：改用真實檔案逐字比對的目標字串，並針對 #10 實際把 mutate 後
  的模組寫到暫存檔重新 `require()` 執行，證明 mutated 版本真的會錯誤
  猜成中壢區，而真實版本仍正確回 unknown。
- **Verification**：Smoke Mutation Negative #9/#10 現為真實通過（不是
  恆真斷言）。

## F. Security Scan

Scanner：`scripts/security-scan-g1-6-ga4-h1.js`。

| 項目 | 結果 |
|---|---|
| Files Scanned | 582 |
| Real Credential Hits | 0 |
| Mock／QA Hits（允許） | 19 |
| Denylist Literals（允許） | 61 |
| Safe Identifier／Docs（允許） | 527 |
| Consecutive Runs | 3 |
| Result | **PASS** |

初次掃描曾出現 12 個命中，逐一人工檢視後全部確認為測試 Fixture
（例如 `'fake-access-token-xyz'`、`'should_not_leak_either'`）或程式碼
識別符（例如 SQL column 名稱 `property_id`、錯誤碼常數
`INVALID_ID_TOKEN_AUDIENCE`），非真實 Secret；已將這些安全樣式加入
Scanner 的分類規則後重新掃描，得到上表的最終結果。本文件不列印任何
命中內容。

## G. Pending Gates（已回填）

| Gate | 狀態 |
|---|---|
| ZIP Verification | **PASS** |
| Clean Extraction | **PASS** |
| Production GA4 Gate | READY FOR MANUAL DEPLOYMENT VERIFICATION |
| Manual Browser QA | NOT TESTED |

---

## H. 回填章節（ZIP／Clean Extraction 真實結果）

| 項目 | 結果 |
|---|---|
| Security Scan on Workdir | PASS（582 files, 0 real hits, 3x consistent） |
| Security Scan on Staging | PASS（0 real hits, 3x consistent） |
| Security Scan on ZIP Extraction | PASS（0 real hits, 3x consistent） |
| ZIP Filename | `fix18-10-hotfix30-B5-R5.4-G1.6-GA4-H1-QA-full.zip` |
| ZIP Size | 3,417,302 bytes（約 3.4 MB） |
| ZIP SHA-256 | `c1da984089c533ea96ce6cc42b412bd9e7d040c466fcf5cdec45cb2b8e86a075` |
| `unzip -t` | "No errors detected in compressed data" |
| Single top-level dir | 確認為 `fix18-10-hotfix30-B5-R5.4-G1.6-GA4-H1/`，無 A2-T1 舊頂層、無雙層、無 traversal、無絕對路徑 |
| npm ci（Clean Extraction，全新目錄，全新 node_modules） | **PASS**（exit=0，使用 ZIP 內 package-lock.json，added 476 packages，無 drift） |
| Clean Extraction Core Tests（24 個 Suite，逐一列於 §十一） | **全部 PASS，exit=0，數字與 Workdir 完全一致** |
| Clean Extraction Regression（`run-regression-g1-6-ga4-h1.js`，1 輪） | **PASS**（31/31 suites, exit=0, node --check OK, 無殘留） |
| Clean Extraction Residue Check | 無 `data/pos.db`、無 Temp DB、無 `unhandledRejection` listener 殘留 |

### Clean Extraction Core Tests 逐項結果

| # | Suite | Expected | Actual | Exit | Result |
|---|---|---|---|---|---|
| 1 | GA4-H1 QA Harness | 22/22 | 22/22 | 0 | PASS |
| 2 | Credential Guard | 17/17 | 17/17 | 0 | PASS |
| 3 | Frontend Runtime | 73/73 | 73/73 | 0 | PASS |
| 4 | Targeted Smoke | 108/108 | 108/108 | 0 | PASS |
| 5 | Static Audit | 166/166 | 166/166 | 0 | PASS |
| 6 | A2-T1 Smoke | 12/12 | 12/12 | 0 | PASS |
| 7 | G1.5-A Smoke | 140/140 | 140/140 | 0 | PASS |
| 8 | G1.5-A Static Audit | 77/77 | 77/77 | 0 | PASS |
| 9 | G1 geo-live | 212/212 | 212/212 | 0 | PASS |
| 10 | A1.2.1 Smoke | 190/190 | 190/190 | 0 | PASS |
| 11 | A1.2.1 Static Audit | 106/106 | 106/106 | 0 | PASS |
| 12 | A1.2.1 QA | 41/41 | 41/41 | 0 | PASS |
| 13 | A1.2 Smoke | 251/251 | 251/251 | 0 | PASS |
| 14 | A1.2 Static Audit | 125/125 | 125/125 | 0 | PASS |
| 15 | Catalog Verify | 57/57 | 57/57 | 0 | PASS |
| 16 | A1.1 Smoke | 160/160 | 160/160 | 0 | PASS |
| 17 | A1.1 Static Audit | 90/90 | 90/90 | 0 | PASS |
| 18 | A1 Smoke | 50/50 | 50/50 | 0 | PASS |
| 19 | Order Heatmap | 128/128 | 128/128 | 0 | PASS |
| 20 | Dashboard Rewire | 101/101 | 101/101 | 0 | PASS |
| 21 | B2.5 | 76/76 | 76/76 | 0 | PASS |
| 22 | B2.4 | 139/139 | 139/139 | 0 | PASS |
| 23 | Geo Map Settings | 620/620 | 620/620 | 0 | PASS |
| 24 | Geo Settings UI | 157/157 | 157/157 | 0 | PASS |

`node --check` 對所有本輪新增/修改檔案：全部 OK（Clean Extraction 環境，
全新 `node_modules`）。

## I. 最終 Gate（依真實結果）

| Gate | 結果 |
|---|---|
| GA4 Reality Gate | 依 Reality Audit 記錄（部分 READY FOR MANUAL DEPLOYMENT VERIFICATION） |
| GA4 Property Binding Gate | **PASS** |
| GA4 Store Isolation Gate | **SINGLE_STORE_ONLY** |
| Shared GA4 Client Regression Gate | **PASS** |
| Credential Guard Gate | **PASS** |
| Frontend Runtime Gate | **PASS** |
| Realtime Snapshot Gate | **PASS** |
| Historical Range Gate | **PASS** |
| Non-additive Metrics Gate | **PASS** |
| Administrative Normalization Gate | **PASS** |
| Aggregate Marker Gate | **PASS** |
| Privacy Gate | **PASS** |
| Code Gate | **PASS** |
| Mock Runtime Gate | **PASS** |
| Security Gate | **PASS** |
| ZIP Verification Gate | **PASS** |
| Clean Extraction Gate | **PASS** |
| Production GA4 Gate | **READY FOR MANUAL DEPLOYMENT VERIFICATION**（未對真實 GA4 Property 部署驗證） |
| Manual Browser QA | **NOT TESTED** |
| POS Individual Visitor Mapping | **NOT IMPLEMENTED / OUT OF SCOPE** |

不寫：Multi-store GA4 PASS／Production GA4 PASS／Individual Visitor
Mapping PASS／Manual Browser PASS——以上四項在本輪範圍內皆未達成可以
這樣宣稱的真實驗證基礎。
