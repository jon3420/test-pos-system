# H1.4.10 Phase 4B — RECOVERY CONSENT × LINE PUSH DELIVERY Manual QA

沒有真實 LINE 官方帳號、LIFF 手機環境、LINE Messaging API、LINE Pay Sandbox/正式環境，以下全部標記 **PENDING MANUAL QA**，不得標成 PASS。Sandbox 自動化測試（本輪 Full Suite / Targeted 已涵蓋等價邏輯路徑）與真機 QA 分開看待。

## A. 一般點餐，Consent 預設未勾

**操作**：從官方 LINE LIFF 正常點餐，加入商品。

**預期**：Consent checkbox 顯示但預設未勾選；不勾選、正常結帳，不應收到任何 Recovery Push。

狀態：**PENDING MANUAL QA**

## B. 已識別會員勾選 Consent，放棄購物車

**操作**：已識別會員身分，勾選「若本次尚未完成訂單，同意透過 LINE 提醒我一次」，放棄購物車。人工執行 `processDueLineRecoveryJobs()`。

**預期**：收到一次 LINE reminder（購物車還保留著…）。

狀態：**PENDING MANUAL QA**

## C. Cart Recovery Link

**操作**：點擊 LINE reminder 中的 Recovery Link。

**預期**：回到原購物車，商品數量正確，不自動進入結帳頁（不自動觸發 `checkout_click`）。

狀態：**PENDING MANUAL QA**

## D. Checkout Recovery

**操作**：對已經前往結帳但未送單的購物車重複上述流程。

**預期**：回到結帳頁 UI，不新增 `checkout_click`（原本第一次進結帳頁時已經記錄過）。

狀態：**PENDING MANUAL QA**

## E. Shipping Cart Recovery

**操作**：在冷藏宅配（`line-shipping.html`）重複 C 的流程。

**預期**：回到官方宅配 LIFF，正確還原宅配購物車。

狀態：**PENDING MANUAL QA**

## F. Shipping Checkout Recovery

**操作**：在冷藏宅配重複 D 的流程。

**預期**：正確恢復宅配結帳頁 UI。

狀態：**PENDING MANUAL QA**

## G. Payment Recovery

**操作**：LINE Pay 已開始但未完成付款，放棄後從 LINE reminder 點回來。

**預期**：顯示「LINE Pay 付款尚未完成」面板，不自動跳轉 LINE Pay，需真實點擊「繼續 LINE Pay 付款」才前往。

狀態：**PENDING MANUAL QA**

## H. LINE Pay Cancel Return

**操作**：在 G 的基礎上，進入 LINE Pay 後按取消返回。

**預期**：回到 Recovery 頁，顯示「繼續 LINE Pay 付款」，不自動重新發起付款請求。

狀態：**PENDING MANUAL QA**

## I. Payment Success

**操作**：完成 G 的付款流程。

**預期**：`payment_success`／`purchase` 正常記錄，Recovery payment job 變 `converted`。

狀態：**PENDING MANUAL QA**

## J. 再次點擊舊 Recovery Link

**操作**：I 完成後，再次點擊同一則舊的 LINE reminder。

**預期**：連結已失效，顯示安全提示，不重複還原/不重複付款。

狀態：**PENDING MANUAL QA**

## K. 取消 Consent

**操作**：勾選 Consent 後又取消勾選，放棄購物車。

**預期**：不收到 LINE reminder。

狀態：**PENDING MANUAL QA**

## L. 取消好友／封鎖官方帳號

**操作**：已勾選 Consent 但之後取消加好友或封鎖官方帳號，放棄購物車。

**預期**：不收到 LINE reminder（好友狀態不可信或非好友時一律不 Push）。

狀態：**PENDING MANUAL QA**

## M. LINE Channel Token 未設定

**操作**：店家尚未設定 `line_channel_token`，其餘條件皆符合。

**預期**：Recovery Job 記為 `not_configured`，不假裝已送出（`sent`）。

狀態：**PENDING MANUAL QA**

## N. 快速雙點「繼續 LINE Pay 付款」

**操作**：在 Payment Recovery Panel 快速連續點擊兩次。

**預期**：只建立一筆 LINE Pay Request（Backend atomic claim + Frontend re-entrancy guard 雙重防護，Sandbox 自動化測試已驗證此邏輯路徑，真機仍需確認實際使用者體驗上沒有明顯延遲/閃爍問題）。

狀態：**PENDING MANUAL QA**

## O. 既有一般點餐（外帶／外送／宅配）不受影響

**操作**：完全不使用 Recovery 功能，正常走外帶/外送/宅配全流程下單。

**預期**：所有既有行為（H1.4.9/Phase 1/2/3/4A 涵蓋範圍）完全正常，Consent checkbox 存在但不影響任何既有流程。

狀態：**PENDING MANUAL QA**（Sandbox 自動化已用 67+68+83+111+89 個既有 regression 測試涵蓋，此處是最後一層真機確認）

---

## Admin 後台確認（可在測試/正式環境直接操作，不需要真實 LINE 環境）

- [ ] 後台「購物車找回」設定卡正確顯示，預設全部關閉。
- [ ] 開啟「啟用購物車找回」後，「啟用 LINE 一次提醒」開關才可勾選。
- [ ] 修改延遲分鐘數／最大提醒次數後儲存，重新整理頁面確認數值保留。
- [ ] 輸入超出範圍的數字（例如 9999 分鐘）儲存後，確認畫面/後端回傳值已被限制在合理範圍內。
- [ ] 確認畫面上「LINE Messaging：已設定/未設定」只顯示布林狀態，開發者工具檢查網路請求與頁面原始碼皆不含完整 `line_channel_token`。
