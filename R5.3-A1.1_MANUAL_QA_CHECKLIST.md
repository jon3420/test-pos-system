# R5.3-A1.1 — Geo Intelligence Heatmap Dashboard Integration — Manual QA Checklist

狀態：R5.3-A1.1：QA BUILD。Automated Verification（Integration Smoke 112/112、
全部 Regression 0 FAIL）：PASS。Manual Visual：**REQUIRED（本清單尚未執行）**。
Final Completion：NOT YET APPROVED。

> 跟 R5.3-A1 那一版不同：Heatmap 這次**已經正式接上 Dashboard UI**，以下
> 全部項目都可以直接在真實瀏覽器測試，不需要再等後續版本。

---

## 一、Heatmap Tab 切換

- [ ] 開啟 Geo Intelligence Dashboard，最上方看得到「儀表板」／「Heatmap」
      兩個分頁按鈕，預設停在「儀表板」（原本的畫面不變）。
- [ ] 點「Heatmap」分頁：畫面正式切換出 Control Bar + 地圖 + Summary +
      Coverage + Ranking + Legend，不是空白或「即將推出」之類的
      Placeholder。
- [ ] 來回切換「儀表板」／「Heatmap」數次：地圖不會閃爍重載、tile 不會
      重新請求（可用瀏覽器開發者工具的 Network 面板確認沒有重複的
      tile.openstreetmap.org 請求）。
- [ ] 鍵盤：Tab 鍵可以切到分頁按鈕，方向鍵/Enter 可以切換分頁（若瀏覽器
      預設 tab 鍵行為即可達成也算通過）。

## 二、Control Bar

- [ ] Metric：Visitors / Add to Cart / Checkout / Orders / Revenue /
      Conversion 六個按鈕都在，點擊後地圖圓點大小/顏色與 Ranking 排序
      立即依新 metric 更新。
- [ ] Display：Circle / Marker / Ranking Only 三個按鈕都在；切到
      Ranking Only 時地圖上完全沒有任何點，但 Ranking 清單正常顯示。
- [ ] Channel：All / Facebook / Google / LINE / Direct 五個按鈕都在，
      切換後跟既有 Dashboard 其他區塊（KPI／排行榜）的 Channel 篩選
      同步變成一致的值（不是各自獨立的兩套篩選）。
- [ ] Heatmap On/Off 開關：關閉後地圖上的熱點消失（但 Ranking/Summary/
      Coverage 仍有內容，不是整段消失），重新打開後恢復。
- [ ] 排版在桌面（≥1200px）／平板（768~1199px）／手機（<768px）下都不會
      過度擁擠或被截斷。

## 三、Circle/Marker 呈現

- [ ] 只有真的有外送訂單座標的行政區才出現地圖圓點/marker，其餘行政區
      地圖上完全沒有點（不是透明點、不是 0 半徑點）。
- [ ] 圓形大小隨 metric 數值變化，且即使某一區數值遠高於其他區
      （outlier），也不會整張圖被一個圓蓋住（sqrt 正規化效果目視確認）。
- [ ] 顏色四級（綠/黃/橘/紅）在 Dark Theme 背景下清楚可辨識，且有文字
      Legend（不是只靠顏色）。
- [ ] Hover／Tooltip：顯示行政區名稱、六個 metric 數值、Channel、座標
      來源、資料準確度、Coverage，畫面上完全沒有 `undefined`/`null`/
      `NaN`/`Infinity` 字樣。
- [ ] 選中（Selected）狀態的圓點邊框明顯不同於未選中／hover 狀態。
- [ ] 切到 Heatmap 分頁時，原本 Dashboard 的行政區色塊（Choropleth）會
      變淡但**不會消失**（確認沒有被整個移除，只是視覺上讓 Heatmap 圖層
      更清楚）。

## 四、Ranking 同步

- [ ] Heatmap 分頁自己的 Ranking 清單顯示**所有**行政區（含沒有座標
      的），沒有座標的那些顯示「目前尚無可用座標」，不是消失也不是
      空白。
- [ ] 點 Ranking 項目 → 地圖 `panTo` 到對應熱點（若有座標）／若無座標，
      顯示提示文字，不跳出任何錯誤訊息或 console error。
- [ ] 點地圖熱點 → 對應 Ranking 項目高亮（`aria-selected="true"`）。
- [ ] 再次點擊同一個熱點/排名可取消選取。
- [ ] 鍵盤：Tab 到 Ranking 項目後按 Enter/Space 可以達到跟滑鼠點擊
      同樣的效果。

## 五、Summary／Coverage Card

- [ ] Heatmap 上方 Summary 顯示：熱度最高／最高成交／最高營收／最高
      轉換／有座標行政區數／無座標行政區數，數字與 Ranking 一致。
- [ ] Coverage Card 六張卡片（Visitors/Add to Cart/Checkout/Orders/
      Revenue/Conversion），Visitors/Add to Cart/Checkout 明確顯示
      「尚未收集訪客座標」，Orders/Revenue/Conversion 顯示實際百分比
      與「依外送訂單位置聚合」的說明文字。
- [ ] 商家不會被誤導以為 Visitors/Add to Cart/Checkout 的 0% 是「這幾天
      剛好沒人」——文字清楚說明是功能限制，不是資料異常。

## 六、Loading / Error

- [ ] 切換 Metric／Channel／Date range 時，短暫看得到 Loading 提示，
      資料回來後自動消失（不會卡住轉圈）。
- [ ] 手動模擬 API 失敗（例如暫時關閉網路、或用開發者工具 Block
      Request 擋掉 `/api/analytics/geo/fulfillment`）：畫面顯示友善的
      中文錯誤訊息，不是白畫面，瀏覽器 Console 沒有噴出未捕捉的
      Error/Exception。
- [ ] 錯誤發生後恢復網路，重新切換一次 Metric，能正常恢復顯示資料
      （不需要重新整理整個頁面）。

## 七、Responsive（390 / 768 / 1024 / 1440）

- [ ] 390px（手機窄版）：Control Bar 換行不擁擠，Ranking 清單排在地圖
      下方（不是並排擠壓）。
- [ ] 768px（平板）：Summary/Coverage 與 Ranking 兩欄改成單欄堆疊，
      閱讀順暢。
- [ ] 1024px：介於平板與桌面之間的版面過渡自然，沒有元素重疊。
- [ ] 1440px（桌面）：Summary/Coverage 與 Ranking 維持雙欄，地圖與
      Control Bar 不會過寬到不成比例。

## 八、Dark Theme

- [ ] Heatmap Tab／Control Bar／Legend／Summary／Coverage／Ranking／
      Loading／Error 在目前的深色主題下文字與背景對比清楚可讀，沒有
      任何區塊看起來像是「忘了套用深色樣式」的亮色殘留。

## 九、Accessibility

- [ ] 用鍵盤（不用滑鼠）完整操作一次：切 Tab → 切 Metric/Display/
      Channel → 選 Ranking 項目 → 切回 Dashboard 分頁，全程可達成，
      焦點樣式（focus ring）清楚可見。
- [ ] 用螢幕閱讀器（或瀏覽器 Accessibility 檢查工具）確認 Tab／
      Control Bar 按鈕／Ranking 項目／On-Off 開關都有正確的
      role／aria-selected／aria-checked。

## 十、Store Isolation（若環境可切換多店測試）

- [ ] 切換到另一家店家後，Heatmap 分頁的地圖熱點／Ranking／Summary／
      Coverage 全部更新成新店家的資料，沒有殘留前一家店的熱點或選取
      狀態。

## 十一、目前已可部署？

- [ ] 以上全部項目皆已人工在真實瀏覽器（建議至少涵蓋一款桌面瀏覽器 +
      一款行動裝置瀏覽器）驗證通過後，才可以把本清單的狀態改成
      「Manual Visual：PASS」，並回頭更新
      `R5.3-A1.1_COMPLETION_REPORT.md` 開頭的 Final Completion 狀態。
- [ ] 在完成以上手動驗證之前，本輪的「是否可以部署」結論僅代表
      **自動化驗證（Integration Smoke + 全部 Regression）通過**，
      不等於「已完成正式視覺驗收」。
