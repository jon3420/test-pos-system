# H1.4.10 Phase 4B — RECOVERY CONSENT × LINE PUSH DELIVERY Implementation Report

## 新增資料表 / Migration（`utils/db.js`，全部 additive）

- **`cart_recovery_consents`**：`id`／`store_id`／`cart_id`／`visitor_id`／`session_id`／`line_user_id`／`status`（granted/revoked）／`consent_text_version`／`source`／`granted_at`／`revoked_at`／`created_at`／`updated_at`。UNIQUE(`store_id`,`cart_id`)，4 個索引。
- **`line_cart_handoff_tokens`** additive 欄位：`purpose`（預設 `'checkout'`，safe migration，既有 checkout handoff row 完全不受影響）、`recovery_cart_id`、`resume_type`（預設 `'cart'`）、`recovery_page_type`（預設 `''`）。
- 全部 `CREATE TABLE/INDEX IF NOT EXISTS` 或 `ADD COLUMN ... DEFAULT`，未 `DROP` 任何既有表。

## Settings（沿用既有 settings 架構，未新增第二套設定 backend）

| Key | Default |
|---|---|
| `cart_recovery_enabled` | `'0'` |
| `cart_recovery_line_enabled` | `'0'` |
| `cart_recovery_cart_delay_minutes` | `60` |
| `cart_recovery_checkout_delay_minutes` | `30` |
| `cart_recovery_payment_delay_minutes` | `15` |
| `cart_recovery_max_attempts` | `1` |

未升級舊店預設全部關閉（master OFF + LINE OFF），不會突然開始 Push 顧客——這是 release blocker 等級的要求，已確認。

## Backend 模組

- **`utils/cartRecoveryConsent.js`**（新增）：`grantConsent`/`revokeConsent`/`getConsentStatus`/`hasValidConsent`。Upsert on `(store_id,cart_id)`；revoke 保留 row（audit history）；grant 同時安全補綁同 cart 的 `cart_recovery_jobs.line_user_id`。
- **`utils/linePush.js`**（新增）：純 LINE Messaging API push transport，不做 eligibility 判斷，永不 throw，永不記錄完整 token。
- **`utils/linePayClient.js`**（新增）：LINE Pay v3 簽章/設定 primitives 唯一 SSOT（見 Reality Audit）。
- **`utils/linePayService.js`**（新增）：`requestLinePayPayment()`，Recovery Payment Resume 專用，只信任 DB order，import `linePayClient.js` 的 primitives。
- **`utils/cartRecoveryDelivery.js`**（新增）：`evaluateLineRecoveryEligibility()`（14 條件）、`resolveRecoveryTarget()`／`resolvePageType()`／`resolveRestorableCartSnapshot()`（Recovery Target Resolver + Variant fail-closed）、`buildRecoveryUrl()`（LIFF-only，無 LIFF 回 `missing_liff_id`，不 fallback 一般網址）、`buildRecoveryMessageText()`、`processDueLineRecoveryJobs()`（含 send 前二次 recheck + race token cancellation）。
- **`utils/lineCheckoutHandoff.js`**（既有檔案，additive 擴充）：`createRecoveryResumeToken()`／`restoreRecoveryToken()`（含 page/UID/store/purpose/expiry/job-status 多重驗證）／`consumeRecoveryToken()`／`cancelRecoveryToken()`／`claimPaymentResumeTokenForRequest()`（atomic claim，只允許 `bound`/`opened`）／`markPaymentResumeRequested()`／`releasePaymentResumeClaim()`／`resetPaymentResumeForRetry()`（獨立的 cancel-return reset，不靠放寬 claim）／`invalidateRecoveryResumeTokens()`（成交後 centralized 失效）。既有 checkout handoff 函式一字未改。
- **`utils/cartRecovery.js`**（既有檔案，兩處修正）：`markRecoveryConverted()` 加入 `'sent'` 到可轉換狀態集合；`onAnalyticsEvent()` 的 purchase/payment_success 分支加掛 `invalidateRecoveryResumeTokens()` 呼叫。

## Routes

- **`routes/cart-recovery.js`**（新增，掛載於 `/api/cart-recovery`，`requireStore+requireFeature('line_order')`）：
  - `POST /consent`：只信任 `verifyMemberSession()` 解析出的 UID。
  - `POST /restore`：token 驗證 + job-status defense-in-depth recheck（只允許 `sent`）。
  - `POST /resume-payment`：atomic claim → DB order 二次確認（不 paid/未 converted）→ `requestLinePayPayment()` → backend 寫 `payment_started` → 回 `payment_url`。
  - `POST /payment-cancelled`：獨立的 cancel-return reset。
- **`routes/settings.js`**：新增 `CART_RECOVERY_KEYS`（6 個 key）。
- **`routes/analytics.js`**：新增 `sanitizePaymentStartedMetadata()`（`payment_method` 白名單）。
- **`routes/linepay.js`**：改 import `linePayClient.js` 的 signing primitives（業務邏輯、既有 API contract 一字未改）；移除連線測試 debug log 的 Channel Secret preimage 洩漏。
- **`routes/payment-gateways.js`**：LINE Pay 連線測試改用同一份 SSOT primitives，其餘（其他支付方式、Admin API contract）未動。
- **`server.js`**：掛載 `/api/cart-recovery` 路由。

## Frontend

- **Consent UI**（`public/line-order.html`／`public/line-shipping.html`）：checkbox 預設 unchecked，只在有效 `member_session` 且 `cart_recovery_enabled+cart_recovery_line_enabled` 皆為 `1` 時啟用；change 立即 POST `/api/cart-recovery/consent`，失敗 UI 回復原狀，不阻塞點餐。
- **Recovery Resume Bootstrap**（兩頁）：`_getRecoveryTokenFromUrl()`（token 只存短期記憶體，立即 `history.replaceState` 從 URL 移除）、`handleRecoveryResumeFromUrl()`（同一 page load 最多執行一次，接在既有正常 `restoreCart()` 之後、`member gate init` promise 完成之後才觸發——見 Reality Audit 第 13 點的 ordering 修正）。
- **Cart/Checkout Resume**：reuse 既有 `ORDER_CART_KEY`/`SHIP_CART_KEY` + `restoreCart()`（不新造第二套 cart mapping）；`openCartSheet({silent:true})` 或 `openCartSheet({step:'checkout'})`（reuse 既有 Checkout Handoff 的 silent+進 checkout 模式），不產生 `checkout_click`/`checkout_submit_click`/`submit_order`。
- **Payment Recovery Panel**：不 restore 購物車，page load 零請求，只有真實按鈕點擊才 `POST /resume-payment`，成功後 `_navigateRecoveryPayment(url)` 導頁，不重複送 `payment_started`（backend authority）。按鈕 handler 有明確 re-entrancy guard（`if(primaryBtn.disabled) return;`）。
- **Cancel-Return**：`linepay=cancel` + `recovery_token` → 先 `POST /payment-cancelled`，成功後顯示付款面板，不自動重新請求付款。

## Admin Settings UI

- **`public/index.html`**：「購物車找回」設定卡（`cartRecoveryCard`），沿用既有 settings tab 架構，包含說明文案、兩個開關、四個數字欄位、LINE Messaging 就緒狀態（boolean only，不回填 token）。
- **`public/js/app.js`**：`CART_RECOVERY_DELAY_DEFAULTS`／`updateCartRecoverySettingsUI()`（LINE 開關依 master 開關 disable/hint）／`loadCartRecoverySettings()`／`saveCartRecoverySettings()`（save 前 clamp 到與 backend 一致的範圍：delay 5–1440、max_attempts 1–3）。全部走既有 `/api/settings` PUT，未新增第二套 settings API。

## New Test Files

1. `scripts/run-h1-4-10-phase4b-recovery-resume-targeted.js`（92 assertions，backend）
2. `scripts/run-h1-4-10-phase4b-recovery-frontend-targeted.js`（50 assertions，frontend jsdom）
3. `scripts/run-h1-4-10-phase4b-recovery-full-runtime.js`（42 assertions：Admin UI + 兩個完整 end-to-end scenario + Composition Gate）

## Frozen Contracts Preserved

- H1.4.9 `checkout_click` canonical contract：67/67 PASS，一字未改。
- Phase 1 Passive LIFF Auto Identify：68/68 PASS。
- Phase 2 `friend_entry`/`friend_checkout`：83/83 PASS。
- Phase 3 `checkout_submit_click`/`checkout_validation_failed`/`payment_started`/`payment_success` server authority：111/111 PASS。
- Phase 4A 三階段狀態機（`cart_abandoned`/`checkout_abandoned`/`payment_abandoned`）：89/89 PASS。
- GA4 `begin_checkout`／Meta `InitiateCheckout` 映射未變；Recovery 相關程式碼完全不呼叫任何第三方 Analytics 平台函式，不新增 GA4/Meta 事件。
