# R5.2-B3 Geo Intelligence Settings UI — COMPLETION REPORT

## Status

```
R5.2-B3：QA BUILD
Automated Verification：PASS
Manual Visual：REQUIRED / NOT TESTED
Final Completion：NOT YET APPROVED
```

This is a **QA build** for real-browser manual acceptance testing. It is **not** the final release. Once Manual Visual passes in a real browser, this report will be updated and `fix18-10-hotfix30-B5-R5.2-B3-full.zip` will be built separately — this QA build (`fix18-10-hotfix30-B5-R5.2-B3-QA-full.zip`) does not replace it.

## 版本
- 工作目錄：`fix18-10-hotfix30-B5-R5.2-B3`
- 基礎版本：`fix18-10-hotfix30-B5-R5.2-B2`（Geo Map Core SaaS Baseline，本輪未重新設計）

---

## ⚠️ 重要限制先說明（誠實揭露，見下方 Manual Visual 章節）

本輪的執行環境是純伺服器端沙盒，**沒有真實瀏覽器（無 Chromium/Playwright 執行檔，也沒有網路權限下載一個）**。因此無法完成需求文件二十要求的「以真實 Browser 驗證」。

已完成的替代驗證：
1. 完整自動化 Smoke（jsdom + Leaflet test double）——157/157 PASS（含本輪新增的 401 root cause 回歸防護）。
2. 針對需求文件六點名的「世界地圖水平重複／灰色區塊／loading 卡住」風險類別，直接**閱讀原始碼**排查已知 Leaflet 成因（見下方「程式碼層級視覺風險排查」），修正兩個屬於 B3 Preview 自己程式碼、有明確技術根據的防禦性問題。
3. 逐項靜態檢查 DOM id、CSS breakpoint、ARIA 屬性是否「存在於程式碼中」。

**沒有完成、也不會謊報為 PASS 的項目**：任何需要人眼在真實瀏覽器裡實際看畫面、實際用滑鼠/觸控/鍵盤操作、實際看 DevTools Console/Network 面板的項目，一律標記 **NOT TESTED**，列在下方「Manual Visual Validation」章節。

本報告狀態為 **QA BUILD**，供人工於真實瀏覽器驗收。驗收通過後才會建立正式 `fix18-10-hotfix30-B5-R5.2-B3-full.zip`。

---

## 一、程式碼層級視覺風險排查（需求文件六）

在無法用真實瀏覽器確認畫面的前提下，針對六點名的具體風險類別做了原始碼層級的成因分析：

| 風險類別 | 排查結果 | 處置 |
|---|---|---|
| 世界地圖水平重複平鋪 | Leaflet 在 `noWrap` 未設定時，容器異常寬或 zoom 異常低時會重複平鋪世界地圖。B3 Preview 的 zoom 限制在 5~18、容器寬度為一般設定頁卡片寬度，數學上在合法範圍內不會觸發，但這是零副作用的防禦性設定 | 在 B3 Preview 自己的 tileLayer 加上 `noWrap: true`（只改 B3 檔案，未動 Core 的 `geoInitMap()` tileLayer） |
| Loading overlay 卡住 | 原本設計是「第一次成功套用 viewport 後才隱藏」，但如果 `GET /api/settings/geo-map` 失敗，程式碼從未走到那一步，overlay 會永遠停在「地圖載入中…」，卻沒有反映真正發生了什麼 | 設定載入失敗時，把 overlay 文字改成「設定載入失敗，暫無法顯示預覽」，不再留著一句實質上說謊的「還在載入」訊息 |
| invalidateSize 時機 | Leaflet 已知問題：容器剛從 `display:none` 變成可見的同一個 tick 內立刻 `invalidateSize()`，瀏覽器可能還沒真正完成 reflow，量到的仍是舊尺寸，導致 tile 計算錯誤 | 在既有的立即呼叫之外，額外用 `setTimeout(fn, 0)` 延後一個 tick 再呼叫一次 `invalidateSize()`（只改 B3 Preview 自己的 lifecycle 函式） |
| container hidden 時初始化 | 確認 `switchSettingsTab()` 的既有邏輯是「先把 panel 設為 `display:block`，再呼叫各 Tab 的資料載入 hook」，`geoMapSettingsInit()` 是在 panel 已經可見之後才被呼叫，不是在隱藏狀態下建立 Leaflet map | 不需修正（既有 `switchSettingsTab()` 順序已經是對的） |
| fitBounds 時機／maxBounds/noWrap | 見上方 noWrap；`fitBounds()` 只在 `geoResolveInitialViewport()` 回傳 `type==='bounds'` 且 bounds 通過合法性檢查時才呼叫，不會對無效物件呼叫 | 不需修正 |
| CSS height | 已檢查 `geo-map-settings.css`：桌面 420px／768px 以下 320px／480px 以下 300px，三個斷點都有明確 `height`，沒有依賴內容自動撐高 | 不需修正 |

以上兩項修正（`noWrap`／loading overlay 文字／`invalidateSize` 延後一個 tick）**全部只改動 `public/js/geo-map-settings.js`（B3 自己的 Preview 邏輯）**，沒有觸碰：
`geoResolveInitialViewport()`／`geoResolveBoundarySource()`／`geoLoadBoundaryData()`／`geoInitMap()`／GeoJSON Manifest 架構／store_id 隔離／`/api/settings/geo-map` 核心行為。

修正後新增 2 項 smoke assertion（B-81／B-82，見下方 Smoke 結果），驗證「延後一個 tick 確實再呼叫一次 invalidateSize()」與「載入失敗時 overlay 文字誠實反映失敗」——這是自動化能驗證的部分（呼叫是否發生、文字是否正確），但**畫面上實際看起來是否還原、是否真的沒有灰色區塊，仍需要真人在真實瀏覽器確認**，見下方 Manual Visual 章節標記為 NOT TESTED。

---

## 二、Manual Visual Validation

**測試環境：無法建立（沙盒無真實瀏覽器、無下載瀏覽器執行檔的網路權限）。**

以下逐項依需求文件二十的格式記錄，凡是需要真人在真實瀏覽器操作/觀察畫面的項目，一律標記 **NOT TESTED**（不得寫 PASS，見需求文件二十）：

| 項目 | 狀態 | 說明 |
|---|---|---|
| 桌面 1440px 實機畫面 | NOT TESTED | 無真實瀏覽器可用 |
| 平板 1024px／768px 實機畫面 | NOT TESTED | 同上 |
| 手機 390px 實機畫面 | NOT TESTED | 同上 |
| Tab 按鈕可點擊／Panel 正確顯示 | 部分驗證 | jsdom 驗證了 DOM 結構與 `switchSettingsTab()` 邏輯（B-12~B-19 等），但實際點擊手感／視覺呈現 NOT TESTED |
| Geo Tab ↔ 其他 Tab 反覆切換不重複綁定/不重複建圖 | 部分驗證 | jsdom 驗證了 `geoMapSettingsInit()` 重複呼叫不重建 `L.map()`（B-40／B-41），真實瀏覽器下的視覺確認 NOT TESTED |
| 四種模式欄位顯示/隱藏正確 | 部分驗證 | jsdom 驗證了 DOM `style.display` 切換邏輯（B-12~B-19），實際畫面排版 NOT TESTED |
| Preview Map Lifecycle（只建立一次、layer 正確替換、無疊加） | 部分驗證 | jsdom mock 驗證了呼叫次數與行為（B-6/B-37/B-38/B-40/B-41），真實 Leaflet／真實 tile 渲染結果 NOT TESTED |
| 世界地圖重複／灰色區塊／Resize／DevTools 裝置切換 | NOT TESTED | 這是最需要真人眼睛判斷的項目，本輪只能做到程式碼層級成因排查（見上表），無法確認實際畫面 |
| 店家位置模式（含 0／null／超出範圍座標） | 部分驗證 | 驗證邏輯層級（A-6~A-14、B-44/B-45 fallback 行為），真實畫面呈現 NOT TESTED |
| 行政區模式（manifest/GeoJSON 來源、縣市切換、多選、inline error） | 部分驗證 | jsdom 驗證了資料流與 DOM 渲染（B-20~B-27），真實畫面/滑鼠互動 NOT TESTED |
| 自訂範圍模式（合法/非法 bounds、套用目前預覽範圍） | 部分驗證 | 驗證邏輯層級（A-15~A-21），真實 Leaflet bounds 物件互動 NOT TESTED |
| 依分析資料模式 | 部分驗證 | Preview 端目前沒有掛真實分析資料查詢（見下方已知限制），fallback 行為已驗證（B-44/B-45），真實資料情境 NOT TESTED |
| Zoom 驗證（含 slider/number 同步） | 部分驗證 | 驗證邊界值與 debounce 行為（A-1~A-5、B-42/B-43），slider 實際拖曳手感 NOT TESTED |
| Save 完整流程（含 F5 Refresh 後設定仍保留） | 部分驗證 | PATCH 白名單/失敗保留輸入已驗證（B-46~B-53），**F5 整頁重新整理後設定仍保留**這件事本質上需要真實瀏覽器＋真實後端持久化驗證，NOT TESTED |
| 重新載入 vs 還原系統預設（按鈕語意） | 已驗證（邏輯層級） | B-54~B-59 驗證了「重新載入只呼叫 GET」「還原預設不呼叫 PATCH」，按鈕文字本身在 HTML 內容也已核對（非模糊字眼），視覺呈現 NOT TESTED |
| Geolocation（允許/拒絕/timeout/不支援/重複點擊） | 部分驗證 | B-60~B-64 驗證了成功/拒絕/不支援三種分支，真實瀏覽器 geolocation 權限彈窗與使用者互動 NOT TESTED |
| Keyboard／Accessibility（Tab/Shift+Tab/Enter/Space/方向鍵） | 部分驗證 | 驗證了 DOM 屬性存在（fieldset/legend/label/aria-describedby/role/aria-live，B-66~B-74），**實際鍵盤操作手感與 focus 可視性** NOT TESTED |
| Dark Theme 視覺一致性 | NOT TESTED | 只能確認 CSS 使用既有 `var(--bg-*/--accent/--border/...)` 變數（未新增 Bootstrap 或第二套主題），實際顏色渲染 NOT TESTED |
| Console 0 error／Network 面板檢查 | NOT TESTED | 沒有真實瀏覽器可觀察 |

**結論：Manual Visual 未完成，本報告狀態為 QA BUILD：**

```
R5.2-B3：QA BUILD
Manual Visual：REQUIRED / NOT TESTED
Final Completion：NOT YET APPROVED
```

---

## 二之一、Hotfix 1：GET/PATCH /api/settings/geo-map 401 Root Cause

**症狀**：真實瀏覽器人工驗收時，`GET /api/settings/geo-map` 回傳 401 Unauthorized，畫面顯示「設定載入失敗，暫無法顯示預覽」。

**Root Cause**：`public/js/geo-map-settings.js` 對 `/api/settings/geo-map`（GET／PATCH）與 `/api/settings/store-location`（PATCH）三處呼叫，直接使用瀏覽器原生 `fetch()`，沒有經過全站共用的 `apiFetch()`（`public/js/app.js`）。`apiFetch()` 會自動補上：
- `Authorization: Bearer <token>`（若已登入）
- `x-store-id`（次要授權路徑，供 JWT 失效時使用）

而後端 `middleware/storeGuard.js` 的 `requireStore`（掛在 `server.js`：`app.use('/api/settings', requireStore, require('./routes/settings'))`，`geo-map` 與 `store-location` 是**同一個 router、同一個掛載點**，不是掛錯 Router）在完全沒有 `Authorization`／`x-store-id`／`query.store_id` 時，會直接回 `401 NO_STORE_TOKEN`。裸 `fetch()` 什麼授權資訊都不會帶，所以固定 401；`store-location` 之所以「正常可用」，純粹是因為既有呼叫（例如 `saveStoreLocationSettings()`）本來就是用 `apiFetch()`。

**逐項比對結果**（對照排查清單）：

| 檢查項目 | 結果 |
|---|---|
| middleware 是否一致 | 一致——兩者都掛在同一個 `requireStore`，同一個 router |
| `req.storeId` 是否存在 | `requireStore` 內部邏輯完全相同，問題不在後端如何解析 storeId，而在前端根本沒送出任何可解析的來源 |
| Session/Cookie/Authorization/Header 是否遺漏 | 是——`geo-map-settings.js` 的三個呼叫點完全沒有 `Authorization`／`x-store-id` header，`store-location` 的既有呼叫（走 `apiFetch()`）有 |
| Route 是否掛錯 Router | 否——`geo-map` 端點就定義在 `routes/settings.js` 內，和 `store-location` 同一個檔案、同一個 router、同一個 `app.use()` 掛載點 |
| GET／PATCH 哪個 401 | 兩者皆 401（因為兩者都繞過了 `apiFetch()`） |
| 前端 `apiFetch()` 是否沒有沿用 | 是——這就是唯一的差異點，其餘（token、store_id 判斷邏輯）後端完全沒變 |

**修改檔案**：僅 `public/js/geo-map-settings.js`（前端），**沒有修改** `routes/settings.js`／`middleware/storeGuard.js`／任何 Geo Map Core 檔案——後端路由/授權邏輯本來就正確，兩個端點本來就在同一個 router、同一個 middleware 下。

**修改內容**：把 3 個呼叫從裸 `fetch()` 改成既有的 `apiFetch()`，完全沿用 `saveStoreLocationSettings()` 既有的授權流程，沒有另外發明第二套授權（`apiFetch()` 內部已自動加上 `Content-Type`／`Authorization`／`x-store-id`，呼叫端不需要再手動指定 header）。

未變動：`/data/geo/taiwan/manifest.json` 與 boundary GeoJSON 的 `fetch()`——這兩個是 `express.static` 提供的公開靜態檔案，本來就不需要授權，維持原樣是正確的。

**新增 regression 防護**（B-83~B-91 共 9 項）：重現「未登入」情境並確認優雅處理、驗證「已登入」情境下 GET／PATCH 皆成功、靜態掃描原始碼鎖定三個呼叫點必須是 `apiFetch()`。同時修正了 smoke test 的 fetch mock（先前沒有檢查授權 header，才會沒抓到這個 bug），讓它如實重現 `requireStore` 的 401 判斷邏輯。

**修正後重新驗證**：GET／PATCH 皆成功。B3 Smoke 由 148 增加到 **157** 項，全數 PASS。

---

## 三、修正清單（本輪）

| 檔案 | 修正內容 | 分類 |
|---|---|---|
| `public/js/geo-map-settings.js` | Preview tileLayer 加 `noWrap: true` | B3 UI 防禦性修正，非 Core |
| `public/js/geo-map-settings.js` | `invalidateSize()` 延後一個 tick 再呼叫一次 | B3 UI 防禦性修正，非 Core |
| `public/js/geo-map-settings.js` | 設定載入失敗時，Preview loading overlay 文字改為誠實反映失敗 | B3 UI 防禦性修正，非 Core |
| `public/js/geo-map-settings.js` | **Hotfix 1**：GET/PATCH `/api/settings/geo-map`、PATCH `/api/settings/store-location` 改用既有 `apiFetch()`，修正 401 root cause | 前端授權流程修正，非 Core、非後端 |
| `scripts/smoke-hotfix30-b5-r5-2-b3-geo-settings-ui.js` | 新增 B-81／B-82（視覺風險修正）與 B-83~B-91（401 root cause 回歸防護）共 11 項 assertion | Smoke 擴充 |

**未修改**（依需求文件確認）：`geoResolveInitialViewport()`／`geoResolveBoundarySource()`／`geoLoadBoundaryData()`／`geoInitMap()`／GeoJSON Manifest 架構／store_id 隔離／`/api/settings/geo-map` 核心行為／`routes/settings.js`／`utils/geoMapScope.js`（皆以檔案 mtime 核對，本輪 session 內未被寫入）。

---

## 四、Smoke 結果

```
B3: scripts/smoke-hotfix30-b5-r5-2-b3-geo-settings-ui.js
總計：157 項，PASS 157，FAIL 0
```

```
B2: scripts/smoke-hotfix30-b5-r5-2-b2-geo-map.js
總計：621 項，PASS 621，FAIL 0
```

## 五、Regression 結果

| 套件 | 結果 |
|---|---|
| B1-6A | 376/376 PASS |
| B1-5 | 313/313 PASS |
| B1-4.6 | 867/867 PASS |
| B1-4.5 | 819/819 PASS |
| B1-4 | 132/132 PASS |
| B1-3 | 76/76 PASS |
| B1-2 | 150/150 PASS |
| B1-1 | 100/100 PASS |
| R5.1-C | 196/196 PASS |

**全部 PASS，0 新增 regression。**

## 六、Static Audit

- Core 四個檔案（`geo-intelligence-map.js`／`geo-intelligence.js`／`routes/settings.js`／`utils/geoMapScope.js`）本輪 session 內 mtime 未變動，確認未被非必要修改
- 無 hardcoded 桃園（僅存在於註解，說明「不得 hardcode」）
- 無 hardcoded `store_001`
- 無第二套 Geo Settings 框架（沿用既有 `PATCH /api/settings/geo-map`／`PATCH /api/settings/store-location`）
- Preview 使用獨立的 `L.map()` 實例（`geoMapSettingsState.preview`，命名空間與 Core 的 `geoMapState` 完全分離）——這是**有意為之的第二個、獨立的 Leaflet 小工具**（Settings 頁面自己的預覽地圖），不是「重複的 Core」；兩者互不讀寫對方的 state，也不共用同一個 DOM 容器
- 無 arbitrary external GeoJSON URL／無 path traversal（Preview 的 `fetch(src)` 呼叫全部先經過 Core 的 `geoResolveBoundarySource()` 安全查表）
- 所有設定讀寫沿用既有 `req.storeId` 隔離（本輪未新增任何查詢路徑）
- 無 debug log（`console.log`/`console.debug` 搜尋為空）
- 無暫時性測試碼殘留於 production 檔案

**Static Audit：PASS**（限於程式碼層級可驗證範圍；Network 面板層級的「無 404／無重複請求」仍屬於 Manual Visual NOT TESTED 範圍）

---

## 七、已知限制

1. **Manual Visual 完全未完成**（本輪最大的已知限制，見上方章節）。
2. 依分析資料模式（`data_bounds`）在 Settings Preview 中沒有掛真實的分析資料查詢——目前 Preview 只驗證了「無資料時 fallback 台灣」的行為，尚未接上真實 `/api/geo/funnel` 等資料源計算真實 data bounds（這在 Dashboard 端的 Core 已經有實作；Settings Preview 是否需要接上同一份資料，或維持目前的「僅示意」設計，需要產品決策，本輪未擅自擴大範圍）。
3. F5 整頁重新整理後設定是否正確保留，依賴後端資料庫持久化與頁面重新載入流程整合，本輪只驗證了 API 呼叫層級（PATCH 成功寫入、GET 能讀回），未驗證真實瀏覽器 F5 後的端到端行為。
4. 沿用 B2 既有限制：manifest 目前只登記桃園市，其餘縣市尚未提供 GeoJSON。

---

## 最終判定

```
R5.2-B3：QA BUILD
Automated Verification：PASS
Manual Visual：REQUIRED / NOT TESTED
Final Completion：NOT YET APPROVED
```

自動化可驗證的部分（Smoke／Regression／Static Audit／程式碼層級視覺風險排查／401 Root Cause 修正）已全數完成並綠燈。本輪產出 `fix18-10-hotfix30-B5-R5.2-B3-QA-hotfix1-full.zip` 供真人於真實瀏覽器依 `R5.2-B3_MANUAL_QA_CHECKLIST.md` 逐項驗收（含最前面新增的 Hotfix 1 驗證項目）。舊版 `fix18-10-hotfix30-B5-R5.2-B3-QA-full.zip`（含 401 bug）已作廢，請勿使用。驗收通過後才會更新本報告為 COMPLETE，並建立正式的 `fix18-10-hotfix30-B5-R5.2-B3-full.zip`（QA ZIP 不取代正式 ZIP）。
