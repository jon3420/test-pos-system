# R5.4-G1.5-B2.3 Runtime Endpoint Audit

fix18-10-hotfix30-B5-R5.4-G1.5-B2.3 — GA4 Legacy Endpoint Removal & Runtime
Request Unification

## 方法

全面搜尋整個專案原始碼（`public/`、`routes/`、`utils/`、`server.js`、
所有 `.html`、`scripts/`、`*.md` 文件），關鍵字：

```
ga4-visitors
ga4_visitors
geoGa4Visitors
fetchGa4Visitors
loadGa4Visitors
refreshGa4Visitors
legacyGa4
ga4-events
ga4-map
ga4-summary（僅檢查是否為獨立 Route/Endpoint，DOM id 的 -ga4-summary 不算）
```

搜尋指令（`grep -rn`／`grep -rin`，排除 `node_modules`）：

```
grep -rin "ga4-visitors\|ga4_visitors\|fetchga4visitors\|loadga4visitors\|refreshga4visitors" .
grep -rn "ga4-events\|ga4-map\|ga4-summary" --include="*.js" --include="*.html" .
grep -rn "router\.\(get\|post\)('/ga4" routes/
```

## 一、ga4-visitors 出現的檔案／行號

**零筆。** 在整個專案（`public/`、`routes/`、`utils/`、`server.js`、
`public/index.html`、`scripts/` 底下所有 Smoke／Static Audit、以及全部
`.md` 文件，包含 `R5.4-G1.5_IMPLEMENTATION_REALITY_AUDIT.md`、
`R5.4-G1.5-A_API_CONTRACT.md`、`R5.4-G1.5-B1_CONTRACT_CONFORMANCE_AUDIT.md`
等既有文件）中，**完全找不到 `ga4-visitors`／`ga4_visitors` 這個字串**，
不管是產品程式、測試 fixture、註解、還是文件。

| 項目 | 結果 |
|---|---|
| 是否為產品程式 | 不適用（不存在） |
| 是否為測試 fixture | 不適用（不存在） |
| 是否為註解／文件 | 不適用（不存在，連歷史文件都沒提過這個名字） |
| 誰在正式頁面呼叫它 | 沒有任何本專案原始碼呼叫它 |
| 呼叫時機 | 不適用 |
| 是否與 geo-ga4-realtime-layer.js 重複 | 不適用（沒有第二套實作） |
| 是否有第二個 GA4 bootstrap | 沒有找到 |
| 是否有舊 timer／auto refresh | 沒有找到獨立於 `geoGa4State.autoRefreshTimer` 的第二個 timer |

## 二、`ga4-events`／`ga4-map`／`ga4-summary` 檢查

搜尋結果只匹配到 DOM id 字面（`${containerId}-ga4-summary`，
`geo-ga4-realtime-summary` CSS class）與既有測試裡的
`document.getElementById('geo-db-ga4-summary')`，這些是既有 UI 容器 id
命名慣例的一部分（跟 `-ga4-toolbar`／`-ga4-status`／`-ga4-notices` 同一組），
**不是** 獨立的 API Route 或 Endpoint。後端 `routes/` 底下沒有任何
`/ga4-events`、`/ga4-map`、`/ga4-summary` 路由。

## 三、後端實際存在的 GA4 相關 Route

```
routes/settings.js:799:  router.get('/ga4-realtime', ...)      → GET /api/settings/ga4-realtime（Settings 專用，見 B2.1）
routes/geo-live.js:255:  router.get('/ga4-realtime', ...)       → GET /api/geo-live/ga4-realtime（本圖層唯一資料端點）
routes/geo-live.js:279:  router.get('/ga4-realtime-status', ...) → GET /api/geo-live/ga4-realtime-status
routes/geo-live.js:291:  router.post('/ga4-realtime-test', ...) → POST /api/geo-live/ga4-realtime-test（Connection Test）
```

**`/api/geo-live/ga4-visitors` 這個路由在本專案的 `routes/geo-live.js`
裡完全不存在**，Express 層面沒有任何 handler 會處理這個路徑。

## 四、結論：Console 抓到的 Legacy Request 不是本專案原始碼產生的

由於 `ga4-visitors` 這個字串在整份原始碼（包含所有歷史文件）裡查無
任何蹤跡，可以排除「程式裡同時存在新舊兩套 GA4 Endpoint 並存或接錯線」
的假設——**沒有第二套實作可以移除，因為它從來不在這個 codebase 裡**。

這代表 Chrome Console 觀察到的 `GET /api/geo-live/ga4-visitors?refresh=0`
其中一種可能性最高的解釋是：**使用者瀏覽器實際載入的前端 JS，跟這份
原始碼裡的 `geo-ga4-realtime-layer.js` 不是同一份檔案**——可能是部署環境
（Zeabur）尚未真正套用最新版本、CDN／瀏覽器快取住了更舊的一份 bundle，
或正式站點目前跑的是這份原始碼上游、更早期（在 Contract 定案為
`ga4-realtime` 之前）的某個原型版本。詳見下方「六、正式 HTML 載入來源」
與「八、Service Worker／Browser Cache Audit」。

## 五、正式 HTML 載入來源檢查

`public/index.html` 目前實際載入順序（節錄 GA4 相關區段）：

```html
<script src="/js/app.js?v=fix18-10-hotfix22"></script>
...
<link rel="stylesheet" href="/css/geo-ga4-realtime-layer.css?v=fix18-10-hotfix30-B5-R5-4-G1-5-B1">
<script src="/js/geo-ga4-realtime-layer.js?v=fix18-10-hotfix30-B5-R5-4-G1-5-B2-3"></script>
<link rel="stylesheet" href="/css/geo-ga4-settings.css?v=fix18-10-hotfix30-B5-R5-4-G1-5-B2-3">
<script src="/js/geo-ga4-settings.js?v=fix18-10-hotfix30-B5-R5-4-G1-5-B2-3"></script>
```

檢查結果：

| 檢查項 | 結果 |
|---|---|
| 1. `geo-ga4-realtime-layer.js` 只載入一次 | ✅ 是（`grep -c` 全檔只出現一次 `<script src="/js/geo-ga4-realtime-layer.js`） |
| 2. 舊 `ga4-visitors` script 是否仍載入 | ✅ 否，`index.html` 找不到任何提及 `ga4-visitors` 的 `<script>`／`<link>` |
| 3. inline script 是否仍呼叫舊 Endpoint | ✅ 否，`index.html` 裡沒有任何 inline `<script>`（沒有 `<script>...ga4...</script>` 內嵌區塊）呼叫 `ga4-visitors` |
| 4. 頁面 bootstrap 是否啟動兩套 GA4 loader | ✅ 否，只有 `geo-ga4-realtime-layer.js` 一套（`geoGa4FetchAndRender`／`geoGa4FetchStatus`／`geoGa4FetchData`），沒有第二套 |
| 5. settings save hook 是否呼叫舊 loader | ✅ 否，`geo-ga4-settings.js` 存檔成功後只呼叫 `geoGa4NotifySettingsChanged()`（定義於 `geo-ga4-realtime-layer.js`），沒有呼叫任何 `ga4-visitors` 相關函式 |
| 6. layer switch 是否呼叫舊 loader | ✅ 否，`geo-heatmap-ui.js` 的 layer 切換分支只呼叫 `geoGa4FetchAndRender()`／`geoGa4Deactivate()` |
| 7. auto refresh 是否呼叫舊 loader | ✅ 否，`_geoGa4ScheduleAutoRefresh()` 排程呼叫的是 `_geoGa4RunFetch()`（同一份檔案內部函式） |
| 8. manual refresh 是否呼叫舊 loader | ✅ 否，`geoGa4Refresh()` 一樣呼叫 `_geoGa4RunFetch()` |

## 六、`app.js?v=fix18-10-hotfix22` 是否為快取問題

**是選項 A：只是舊 cache-buster 字串，不是快取問題。** `app.js` 是全站
共用的 Store JWT／`apiFetch()` 基礎設施檔案，本輪與過去所有 GA4 相關
hotfix（B1／B2／B2.1／B2.2）都**沒有修改過 `app.js` 的內容**，所以它的
cache-buster 版本號停留在很早期的 `fix18-10-hotfix22` 是正確且符合預期
的——版本號代表「這個檔案最後一次真正被修改的版本」，不是「目前系統的
整體版本」。`apiFetch()` 函式本身（B2.2 已完整稽核過其行為）與這個版本
標籤無關，是同一份檔案內容。

## 七、發現的版本標籤不一致（本輪修正）

檢查發現：`geo-ga4-realtime-layer.js` 的 `<script>` 版本已經是
`fix18-10-hotfix30-B5-R5-4-G1-5-B2-3`，但配套的
`geo-ga4-realtime-layer.css` 的 `<link>` 版本卻還停留在較舊的
`fix18-10-hotfix30-B5-R5-4-G1-5-B1`——同一個功能模組的 JS／CSS 版本號
不同步。這是本輪在版本稽核中發現、需要修正的不一致（見 Fix Report
「十二、Cache-buster」章節），修正為兩者統一使用
`fix18-10-hotfix30-B5-R5-4-G1-5-B2-3`。

## 八、Service Worker／Browser Cache Audit

搜尋結果：

```
find . -iname "*service-worker*" -o -iname "sw.js" -o -iname "*workbox*"
grep -rln "serviceWorker\|navigator.serviceWorker\|CacheStorage\|caches.open" public/
```

**專案裡完全沒有 Service Worker、沒有 `sw.js`、沒有 Workbox、沒有任何
`navigator.serviceWorker` 或 `caches.open()` 呼叫。** 唯一命中的
`manifest.json`（`public/data/geo/taiwan/manifest.json`）是台灣行政區
GeoJSON 資料的索引檔，跟 PWA Manifest／離線快取無關。

**結論：本專案沒有 Service Worker、沒有應用層快取機制**——若正式環境
瀏覽器仍拿到舊版 JS，來源只會是：（a）瀏覽器自身的 HTTP Cache（受
cache-buster 字串影響）、（b）CDN／反向代理層的快取、或（c）部署平台
（Zeabur）尚未把最新版本的靜態檔案真正推上線。這些都不是本專案程式碼
可以直接控制或修復的層面，只能透過「確保每次有實質修改都換一個新的
cache-buster 字串」來降低風險，並在部署驗證時要求 Hard Refresh
（`Ctrl+Shift+R`）或 DevTools 停用快取來排除瀏覽器端因素。

## 九、502 的可能來源（無法在原始碼中重現）

由於 `/api/geo-live/ga4-visitors` 在這份原始碼裡不存在對應的 Express
Route，本機重現測試中對這個路徑發送請求，Express 會落到全站的
404 Not Found（沒有比對到任何 Router），**不會產生 502**。502 通常代表
反向代理／平台層級認為後方服務無回應或無效回應，這種行為超出這份
原始碼可以重現或解釋的範圍，只能記錄「無法在原始碼中重現此 502，需要
由部署環境（Zeabur Console／Log）進一步確認正式環境當下實際跑的是哪一
份 build」，不做沒有根據的臆測。

## 十、後續建議

1. 部署驗證時，除了本輪的程式碼修正，**必須同時確認 Zeabur 上的
   Build/Deploy 記錄，確認正式環境目前部署的版本確實包含
   `geo-ga4-realtime-layer.js`（唯一使用 `ga4-realtime` Contract 的
   版本）**，而不是更早期的某個 build。
2. 使用者側請求 Hard Refresh（`Ctrl+Shift+R`）或清除瀏覽器快取後
   重新測試，排除瀏覽器 HTTP Cache 因素。
3. 本輪已把 `geo-ga4-realtime-layer.css` 的 cache-buster 補齊到跟
   `.js` 一致的 `B2-3`，往後任何一次修改都必須同步更新 JS 與 CSS 的
   版本號，避免再次出現不同步的情況。
