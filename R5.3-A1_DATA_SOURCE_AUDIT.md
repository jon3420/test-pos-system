# R5.3-A1 — Geo Intelligence Heatmap：資料來源盤點（Section 三／四）

範圍：本輪只做需求文件「三、資料來源」與「四、區域中心點策略」要求的盤點，
**尚未新增/修改任何 Heatmap 程式碼**。這是為了先確認「哪些座標是真的、哪些
根本不存在」，避免在錯的資料基礎上蓋 UI／API／Smoke（跟上一輪 B3.1 GeoJSON
Fixture Audit 同一個理由：先把地基照清楚，再決定要不要蓋、怎麼蓋）。

---

## 1. 現有 Analytics 資料表與欄位盤點

實際檢查 `utils/db.js` 的 schema migration（不是猜檔名）：

### `analytics_events`（Visitor 事件層級：visitors / add_to_cart / begin_checkout）
- 有：`geo_country` / `geo_region` / `geo_city` / `geo_district` / `geo_postal_code`
- 有：`geo_source`／`geo_confidence`／`geo_resolution`／`geo_accuracy`／`geo_provider`
- 有：`geo_county_code`／`geo_subdivision_code`（官方行政區代碼，read-time normalize）
- **沒有 `lat`／`lng` 欄位。完全沒有座標，只有城市/行政區字串。**
- `geo_source` 目前唯一填法：IP-based 推定（`geo_provider` 記錄哪個 Provider，例如 ipapi），屬於「三、5. 哪些只有 IP 推定區域」——**視覺化用途下通常只到城市/區級精度，不是門牌等級**。

### `orders`（履約/訂單層級：orders / revenue）
- 有：`fulfillment_geo_city`／`fulfillment_geo_district`／`fulfillment_geo_source`／
  `fulfillment_geo_confidence`／`fulfillment_geo_resolution`／`fulfillment_geo_county_code`／
  `fulfillment_geo_subdivision_code`（履約地址解析結果，字串/代碼，同樣沒有座標）
- 有：`delivery_lat`／`delivery_lng`（**TEXT，真正的數字座標**）
  - 寫入位置：`routes/line-orders.js`、`routes/delivery.js`——只在
    `order_mode === 'delivery'` 且顧客實際提供座標時才寫入
    （程式碼裡是 `isDelivery ? String(parseFloat(delivery_lat)||'') : ''`）。
  - `order_mode='shipping'`（宅配）與 `pickup`／`dine_in` 等其他模式：**不會有
    delivery_lat/delivery_lng**，即使 `fulfillment_geo_district` 有值。
  - 這是「五、履約實際地址」裡**唯一真的帶座標**的資料來源，但覆蓋率明確
    受限於「這筆訂單是不是外送模式、顧客當時有沒有給座標」。

### 商店本身
- `store_lat`／`store_lng`：單一店家座標（Settings），不是每個行政區各一個。

### 行政區資料集
- `data/taiwan-administrative-areas.json`：只有 `county_code`／`county_name`／
  `subdivision_code`／`subdivision_name` 等**名稱與代碼**，**沒有任何座標欄位、
  沒有 centroid、沒有 geometry**（已用 `grep -ri centroid` 全專案搜尋，零筆
  命中）。
- `public/data/geo/taiwan/taoyuan-districts.geojson`：上一輪 B3.1 audit 已確認
  是矩形 bounding box fixture，不是正式行政區 polygon，**不能拿來算 centroid**
  （這正是本輪需求文件「不得把 Bounding Box Fixture 當正式行政區 Heatmap」
  明文禁止的用法）。

### 既有查詢邏輯（`utils/geoAnalyticsQueries.js`，實際讀原始碼確認）
- `getGeoFunnel()`：Visitor 端漏斗（visitors/add_to_cart/begin_checkout/…），
  `GROUP BY vga.geo_city, vga.geo_district`——**已經是現成的、依行政區彙總的
  visitors/add_to_cart/checkout 人數**，不需要另建一套算法。
- `getGeoFulfillment()`：訂單端，`GROUP BY fulfillment_geo_city,
  fulfillment_geo_district`，已回傳 `completed_orders`／`revenue`／
  `average_order_value`／`average_distance_km`——**已經是現成的、依行政區彙總
  的 orders/revenue**，同樣不需要另建一套口徑。這兩個函式的 SELECT 目前都
  **沒有選 `delivery_lat`/`delivery_lng`**（不是資料庫沒有，是查詢語句沒選），
  屬於「若欄位足夠、不需新增重複 API，只需擴充既有查詢」的情況。

---

## 2. 對照需求文件三的七個問題（逐項回答）

1. **哪些資料已有 lat/lng**：只有 `orders.delivery_lat`/`orders.delivery_lng`，
   且只在 `order_mode='delivery'` 且顧客有提供座標時才有值。
2. **哪些只有 city/district**：`analytics_events`（Visitor 事件全部）與
   `orders.fulfillment_geo_district`（履約地址字串，即使沒有 delivery_lat/lng
   也可能有這欄）。
3. **哪些可聚合到 district**：Visitor 端（`geo_city`/`geo_district`）與訂單端
   （`fulfillment_geo_city`/`fulfillment_geo_district`）都已經是可
   `GROUP BY` 的欄位，且既有查詢已經在做（見上）。
4. **哪些只有 IP 推定區域**：`analytics_events` 全部（`geo_source`/
   `geo_confidence`/`geo_accuracy` 標記其推定方式與信心），這批完全沒有座標，
   只有區級字串。
5. **哪些是履約實際地址**：`orders.fulfillment_geo_*`（字串/代碼），其中只有
   `order_mode='delivery'` 這個子集，才可能同時有 `delivery_lat/delivery_lng`
   真座標可用。
6. **哪些可用於 Heatmap**（需要座標畫點）：目前**只有** `orders.delivery_lat/
   delivery_lng`（delivery 模式訂單）這一種座標來源是真的、非推定、非 fixture。
7. **哪些不可用或準確度不足**：
   - 行政區 centroid：完全不存在（沒有資料，不是精度問題，是「零」）。
   - Bounding box fixture 中心點：存在但明確被本需求文件禁止使用。
   - Visitor 端 IP 推定區域：有區級字串，但沒有座標，且精度僅供分類，不適合
     當成「這個訪客的地圖座標」使用。
   - `shipping`/`pickup`/`dine_in` 訂單：即使有 `fulfillment_geo_district`
     字串，也沒有 `delivery_lat/lng`。

---

## 3. 對照需求文件四的座標優先序——實際可用性

| 優先序 | 策略 | 本專案現況 |
|---|---|---|
| 1 | 真實事件 lat/lng 聚合中心 | **不存在**（`analytics_events` 沒有 lat/lng 欄位） |
| 2 | 真實訂單 delivery_lat/delivery_lng 聚合中心 | **唯一可用的真實座標來源**，但只覆蓋 `order_mode='delivery'` 且顧客有提供座標的訂單 |
| 3 | 已存在的 district centroid | **不存在**（無此資料集） |
| 4 | store-relative data centroid | 只有單一 `store_lat/store_lng`，不是各行政區各一個，用它冒充「行政區中心」等同 hardcode，違反本需求文件「不得 fallback 到 store 位置冒充區域位置」 |
| 5 | 無座標時不畫地圖點，只保留 Ranking | 對「訂單/營收之外」的每一個行政區都會落到這裡 |

### 這代表實際能畫的 Heatmap，範圍比需求文件列的 8 個問題窄很多

- **可以誠實回答（有真座標）**：「哪個區域最多人成交」「哪個區域營收最高」——
  只限**曾經有 `delivery` 模式訂單且顧客提供過座標**的行政區，coordinate_source
  可標記為 `order_centroid`，confidence 依聚合筆數可訂為 medium/high。
- **不能誠實畫成地圖圓點（沒有座標可用）**：「哪個區域最多人瀏覽」「哪個區域
  最多人加入購物車」「哪個區域最多人開始結帳」「哪個區域只是看很多但不成交」
  ——這幾個問題目前**完全沒有座標依據**，只能留在 Ranking 文字列表，
  `coordinate_source: unavailable`。
  - 唯一能讓這些也上地圖的做法，是「借用同一個行政區、來自訂單資料算出的
    centroid，去標示這個區域的 visitors/add_to_cart/checkout 數字」——這在技術上
    不算捏造座標（座標仍然是真實訂單位置的聚合），但語意上是「用訂單分布位置
    代表整個行政區」的近似，且只對**同時有訂單資料**的行政區才有位置可借；
    對「只有瀏覽/加購但從未真正外送成交過」的行政區（也就是問題 8「只是看很多
    但不成交」最想凸顯的那批），恰好完全沒有座標可借，只能停在 Ranking。
- Conversion（orders/visitors）本身依賴 visitors 分母，即使能算出比例，若該
  區沒有訂單座標，一樣沒有地圖點可畫，只能是 Ranking 數字。

---

## 4. 結論與建議（本輪停在這裡，尚未寫任何 Runtime 程式碼）

1. **Root Cause 對齊**：跟上一輪 GeoJSON Boundary Audit 同一個教訓——目前
   「座標」這個維度在專案裡，唯一真實存在的來源非常窄（只有 delivery 訂單的
   `delivery_lat/delivery_lng`），其餘都只是字串分類或完全不存在。在盤點清楚
   之前寫 Circle/Marker Heatmap、Normalize/Radius/Intensity Helper、
   120–180 項 Smoke，等於是在還沒確定「畫什麼座標」之前就先蓋好整套顯示邏輯，
   容易重蹈「先蓋 Runtime 後來才發現資料是假的」的覆轍。
2. **建議的 API 擴充方向**（本輪只列方向，不代表已實作）：
   - 擴充 `getGeoFulfillment()` 的 SELECT，加入
     `AVG(CAST(delivery_lat AS REAL))`／`AVG(CAST(delivery_lng AS REAL))`
     （只在 `delivery_lat <> '' AND delivery_lng <> ''` 的列上平均），作為該
     行政區的 `order_centroid`，並回傳實際聚合筆數供 confidence 判斷
     （例如 ≥5 筆 high、1–4 筆 medium、0 筆 unavailable）。
   - `visitors`/`add_to_cart`/`begin_checkout` 沿用 `getGeoFunnel()` 既有數字，
     但**明確標記這些 metric 沒有自己的座標**——地圖模式下，這些數字只能顯示在
     「已經有 order_centroid 的那些行政區」的 Tooltip 裡（一併顯示），不能單獨
     為只有瀏覽數據、沒有訂單資料的行政區生出一個地圖點。
   - 沒有 `order_centroid` 的行政區，一律進 Ranking-only，不進地圖。
3. **在動工前需要確認的產品決策**（會直接決定 UI 呈現，不是技術細節）：
   - 是否接受「第一版地圖只畫得出『有外送訂單且有座標』的行政區，其餘（含
     問題 8 最在意的『只看不買』區域）只能在 Ranking 文字列表看到」這個範圍？
   - 若不接受，代表需要先有其他真實座標資料源（例如之後在前端 visitor
     首次造訪時實際請求瀏覽器地理定位、或改善 delivery 訂單座標蒐集率），
     這屬於資料蒐集面的另一輪工作，不是本輪 Heatmap 顯示層可以生出來的。

本輪到此停止：只交付這份資料盤點，**不建立 Circle/Marker Heatmap、不建立
Normalize/Radius/Color Helper、不擴充 API、不寫新 Smoke、不建立任何 Heatmap
Runtime**。是否依上述「範圍 2」（僅 order_centroid 可畫地圖點、其餘 Ranking-only）
繼續往下做，還是先處理座標蒐集面的問題，請先確認方向。
