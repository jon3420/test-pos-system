# R5.3-A1.1 — Geo Intelligence Heatmap Dashboard Integration — Completion Report

狀態：**QA BUILD**。Automated Verification：PASS。Manual Visual：REQUIRED。
Final Completion：NOT YET APPROVED（本文件本身不宣告正式 COMPLETE）。

---

## 1. Scope

延續 `R5.3-A1_COMPLETION_REPORT.md`。上一輪（R5.3-A1）交付了 Heatmap
Engine（`public/js/geo-heatmap.js`：pure helper／state／Leaflet 渲染／
128 項 Smoke 全綠），但尚未正式接到 Dashboard UI（Heatmap Mode 分頁不存在）。

本輪（A1.1）**唯一目的**：完成 Dashboard Integration，讓 Heatmap 在
`public/index.html` 上正式可切換、可用。本輪**沒有**修改 Heatmap Engine
（`geo-heatmap.js`）與既有 Map／Settings（`geo-intelligence-map.js`／
`geo-map-settings.js`），也沒有開始 Visitor GPS／Official Polygon／ROI
Heatmap／Delivery Zone／Geo Pack／第三套 Analytics/API/Dashboard。

> 本次回合（依使用者指示）延續上一回合已完成的 Dashboard Integration 程式碼，
> 只補完剩餘的 QA 工作：Integration Smoke、Regression、Static Audit、兩份
> 文件、打包。過程中 Regression 發現一個真正的 Production Bug（見第 6
> 節），已修正並重新跑過全部 Regression。

## 2. 修改/新增檔案

| 檔案 | 動作 | 內容 |
|---|---|---|
| `public/js/geo-heatmap.js` | **未修改** | Heatmap Engine（R5.3-A1 既有，逐位元組相同，見第 5 節 SHA-256） |
| `public/js/geo-intelligence-map.js` | **未修改** | 既有 Dashboard 地圖（逐位元組相同） |
| `public/js/geo-map-settings.js` | **未修改** | 既有 Geo Map 設定 UI（逐位元組相同） |
| `public/data/geo/taiwan/manifest.json` | **未修改** | 既有行政區邊界 manifest（逐位元組相同） |
| `public/js/geo-heatmap-ui.js` | **新增**（上一回合建立） | Dashboard Integration 膠水層：Heatmap Tab／Control Bar／Panel Skeleton／Fetch+Render／Map Reuse／Ranking Click Delegation／Store Isolation 接線。完全不重新宣告 Engine 既有的常數／state／render function，只呼叫它們。 |
| `public/css/geo-heatmap.css` | **新增**（上一回合建立） | Heatmap Tab／Control Bar／Legend／Summary／Coverage／Ranking／Loading／Error 樣式，斷點 390/768/1024/1440，沿用既有 Dark Theme CSS 變數。 |
| `public/js/geo-intelligence.js` | **擴充**（既有函式，非新增第二套 Dashboard） | (a) 新增前端 fetch wrapper `getGeoFulfillmentForHeatmap()`，呼叫既有 `/api/analytics/geo/fulfillment`（不是新端點，比照既有 `getGeoFunnel()` 寫法）。(b) 在既有 `refreshGeoDashboardKpiBlock()` 的輸出裡插入 Heatmap Tab Bar／Heatmap Panel（透過 `typeof` guard 呼叫 `geo-heatmap-ui.js`，未載入時安全略過，跟既有 `geoRenderMapBlock()` 的 guard 慣例一致），既有 Dashboard 內容（KPI／Decision Center／Ranking）包進獨立 `#…-panel-dashboard` 容器供 Tab 切換用，版面邏輯本身未被拆散。 |
| `public/index.html` | **擴充** | 新增 `<link>` 引入 `geo-heatmap.css`；新增 `<script>` 依序載入 `geo-heatmap.js`（Engine，排在 `geo-intelligence-map.js`／`geo-map-settings.js` 之後）與 `geo-heatmap-ui.js`（Integration，排在 Engine 之後）。 |
| `scripts/smoke-hotfix30-b5-r5-3-a1-1-heatmap-dashboard-integration.js` | **新增** | 本輪 Integration Smoke，112 項 assertion（見第 4 節）。 |
| `scripts/smoke-hotfix30-b5-r5-2-a-integrated.js` | **修正既有 Bug**（見第 6 節） | 移除 `FORBIDDEN_KEYS`／`STRICT_FORBIDDEN` 內過時的 `lat`/`lng`/`latitude`/`longitude`，改用正向斷言驗證這兩個欄位是 R5.3-A1 核准的聚合值，不是原始顧客資料；其餘隱私把關（地址／電話／IP／token 等）完全沒有放寬。 |

**沒有新增 API 路由**——`getGeoFulfillmentForHeatmap()` 呼叫既有
`/api/analytics/geo/fulfillment`，沒有新增 `/api/analytics/geo/heatmap`
或任何第二套端點（Integration Smoke SA-2～SA-7 已驗證）。

## 3. Dashboard Integration 設計說明

- **Heatmap Tab**：在既有 Dashboard 版面最上方加入一個兩分頁的 Tab Bar
  （儀表板／Heatmap），預設顯示既有 Dashboard 內容，點擊「Heatmap」切到新
  Panel。既有的行政區排行榜維持在 Dashboard 分頁內（沒有拆成獨立的
  「Ranking」分頁）——這是刻意的保守設計：本輪唯一目的是接上 Heatmap，不
  拆解或重構既有已上線、有 313/313 Smoke 覆蓋的 Dashboard 版面。Heatmap 分頁
  內自己也有一份完整的 Ranking（需求文件九），跟 Dashboard 既有的行政區
  排行榜是兩個獨立但都正式存在、都可操作的清單，沒有互相取代或刪除任何
  既有功能。
- **Map Reuse（需求文件六）**：切到 Heatmap 分頁時，`geoHeatState.instance`
  直接指向既有 `geoMapState.instance`（同一個 Leaflet map），只呼叫
  `geoHeatEnsureLayerGroup()` 加一個新的 `L.layerGroup()`（只建立一次，來回
  切換 Tab 不重建），並把既有 Choropleth GeoJSON layer 的 `opacity`／
  `fillOpacity` 調淡（不是 remove/destroy），避免兩層視覺互相干擾。整個過程
  完全沒有呼叫 `L.map()`、沒有建立第二個 tile layer。
- **Channel 篩選（需求文件四）**：Heatmap Control Bar 的 Channel 按鈕呼叫既有
  `av2SetChannel()`，改的是全站共用的 `av2Channel`，沒有另建一份 Channel
  state。
- **API Integration（需求文件五）**：新增的 `getGeoFulfillmentForHeatmap()`
  只是比照既有 `getGeoFunnel()` 補一個對稱的前端 wrapper，呼叫既有
  `/api/analytics/geo/fulfillment`（R5.3-A1 已加好座標欄位）。兩份回應交給
  Engine 既有的 `geoHeatBuildAreas()` 合併，Integration 層完全沒有自己算
  座標或渠道邏輯。
- **Ranking Click Delegation**：Engine 的 `_geoHeatRenderRankingDom()` 只負責
  畫 `<li data-area-id>`，不綁 DOM 事件；Integration 層用 event delegation
  補上點擊／鍵盤操作，呼叫既有 `geoHeatSelectArea()`，沒有重寫選取邏輯。
- **Store Isolation（需求文件十七）**：`geoHeatUiRegisterContext()`（每次
  Dashboard 重新掛載，包含切店時的重新整理都會呼叫）一律先呼叫既有
  `geoHeatHandleStoreSwitch()` 清空 Heat Layer／Selection／pending request，
  再視目前分頁決定要不要重新抓資料，不沿用上一店的殘留狀態。

## 4. Integration Smoke（`scripts/smoke-hotfix30-b5-r5-3-a1-1-heatmap-dashboard-integration.js`）

**112 項 assertion，全數 PASS**（高於需求文件要求的 60 項下限）。

- **Part A — Static Audit（29 項）**：Engine／既有 Map／既有 Settings／
  manifest 逐位元組 SHA-256 比對未變、沒有新增 `/heatmap` 路由、
  `/funnel`／`/fulfillment` 各只定義一次、`geo-intelligence.js` 只做接線
  （沒有第二套 Dashboard 流程）、`geo-heatmap-ui.js` 沒有重新宣告 Engine
  既有常數／state／函式、沒有 debug log／`Math.random()`／`L.map()`／
  tile layer、`index.html` 腳本載入順序與唯一性、CSS 斷點與 Dark Theme
  變數、選擇器命名規範。
- **Part B — jsdom 實測（83 項）**：真的把 Engine 與 Integration 層載入同一個
  window 執行，涵蓋 Heatmap Tab（存在／可切換／aria-selected／tabindex）、
  Map Reuse／No Duplicate Layer／Tile Reuse（`geoHeatState.instance` 與
  `geoMapState.instance` 同一參考、`L.layerGroup()` 只呼叫一次、來回切換
  Tab 不重建）、Control Bar（Metric/Display/Channel 各按鈕、On/Off 開關）、
  Fetch+Render（Loading 顯示/自動消失、Summary/Coverage/Ranking/Legend
  正式 render 且不含 undefined/null/NaN）、Tooltip、Ranking Click
  Delegation（含 toggle 取消選取）、Metric/Display/Channel 切換（確認沒有
  建立第二套狀態）、Heatmap On/Off、Error Handling（友善訊息、不是白畫面、
  不外洩 Stack Trace）、Store Isolation（切店清空舊狀態、不殘留舊 areas）、
  容器全程存在（No White Screen）。

過程中發現並修正兩個**測試腳本本身**的假陽性（不是 Production Bug，已在
撰寫過程中修正，未列入最終 112 項的 FAIL）：(1) 對自己註解文字的
字串誤判；(2) jsdom 用兩次獨立 `eval()` 載入 Engine／Integration 層時，
`const`/`let` 頂層宣告沒有共用同一個全域語彙環境（真實瀏覽器的多個
`<script>` 標籤本來就共用，這是 jsdom 測試工具本身的限制，不是產品行為）
——改成一次 `eval()` 載入兩份原始碼即修正，不影響任何產品程式碼。

## 5. Regression

全部重新執行，共 24 個相關 Smoke 檔案（Geo Intelligence 全系列 + 本輪修正
過的 R5.2-A Integrated E2E），**全部 0 FAIL**：

| 套件 | 結果 |
|---|---|
| `node --check`（全專案，排除 `node_modules`） | 全部 OK |
| Engine／既有 Map／既有 Settings／manifest SHA-256 | 與 R5.3-A1 基線逐位元組相同 |
| **A1.1 Integration Smoke（新增）** | **112/112 PASS** |
| A1 Heatmap Engine Smoke | 128/128 PASS |
| B2 Geo Map Smoke | 630/630 PASS |
| B3 Geo Settings UI Smoke | 157/157 PASS |
| B1-1 Dashboard Rewire | 100/100 PASS |
| B1-2 Area Ranking | 150/150 PASS |
| B1-3 Geo Behavior Funnel Backend | 76/76 PASS |
| B1-4 Geo Rule Engine | 132/132 PASS |
| B1-4.5 Geo Explainability | 819/819 PASS |
| B1-4.6 API Stabilization | 867/867 PASS |
| B1-5 Geo Dashboard UI | 313/313 PASS，MANUAL 0 |
| B1-6 Geo Explorer | 376/376 PASS |
| R5.1-A Geo Foundation | 76/76 PASS |
| R5.1-B Geo API | 111/111 PASS |
| R5.1-C Geo UI | 196/196 PASS |
| R5.1-D1 Visitor Geo | 164/164 PASS |
| R5.2-A Business Area Reserved | 62/62 PASS |
| **R5.2-A Integrated E2E（本輪修正過的檔案）** | **232/232 PASS**（修正前 230/232，見第 6 節） |
| R5.2-A Order Geo Write | 90/90 PASS |
| R5.2-A Stage 7 | 140/140 PASS |
| Cart/Order Hours | 55/55 PASS，MANUAL 4（既有、跟 Geo 無關） |
| Dashboard UI（既有） | 20/20 PASS |
| Debounce（既有） | 32/32 PASS |
| Hotfix30-C1 Rollback | 22/22 PASS |

沒有降低任何一份既有測試的數量或通過率；本輪修正的 R5.2-A Integrated E2E
測試數量從 230 增加到 232（新增兩條正向斷言，見第 6 節），FAIL 從 2 變 0。

payment／delivery／cart／hotfix25～29／hotfix31 系列與本輪完全無關的
Smoke 檔案未重新執行（本輪程式碼變動範圍僅限 Geo Intelligence 相關檔案與
`public/index.html` 的 `<script>`/`<link>` 標籤，不影響這些模組）。

## 6. Bug 發現與修正（Regression 過程中發現的真正 Production 相關問題）

跑完整 Geo 系列 Regression 時，`scripts/smoke-hotfix30-b5-r5-2-a-integrated.js`
（R5.2-A 時期建立的整合隱私掃描測試）出現 2 項 FAIL：

> `J_/fulfillment response contains no forbidden privacy keys: ["lat","lng"]`
> `V-5 /fulfillment contains no forbidden privacy keys: ["lat","lng"]`

**根因**：這份測試的 `FORBIDDEN_KEYS`／`STRICT_FORBIDDEN` 清單建立於
R5.2-A 時期，當時 `/fulfillment` 完全沒有任何座標欄位，`lat`/`lng` 被列入
清單是「預防未來不小心洩漏顧客座標」的防呆設計。R5.3-A1（見
`R5.3-A1_COMPLETION_REPORT.md`／`R5.3-A1_DATA_SOURCE_AUDIT.md`，已經過
產品審核的正式決策）之後，`/fulfillment` 開始合法回傳「同一行政區所有
外送訂單座標的 `AVG()` 聚合中心點」——這是 Heatmap 功能運作所必需的資料
（不是原始顧客座標／地址洩漏），但這份 R5.2-A 舊測試的清單當時沒有跟著
更新，導致一支既有 Regression 對一個已核准的正式功能持續誤判成隱私違規
（R5.3-A1 自己的 Regression 報告表格裡也沒有列出這支 Integrated E2E
測試，代表這個衝突在 R5.3-A1 當時就已經存在但沒被跑到）。

**修正方式**（只改測試腳本，沒有動到 Heatmap Engine／Dashboard
Integration／任何產品程式碼）：把 `lat`/`lng`/`latitude`/`longitude` 從
兩份清單移除，改為在 Scenario J／V 各自新增一條正向斷言（`J-legit-
coordinate`／`V-legit-coordinate`），明確驗證這兩個欄位在有座標的行政區上
是聚合後的數字型別（不是原始座標字串或顧客資料），且同一份回應仍然完全
沒有任何原始顧客欄位（電話／地址／IP／token 等）——原本的隱私把關意圖
（防止真正的 PII 洩漏）完全沒有被放寬，只是不再把「已核准、已聚合」的
座標誤判成違規。

修正後重新執行：`scripts/smoke-hotfix30-b5-r5-2-a-integrated.js`
232/232 PASS（0 FAIL），並重新跑過第 5 節列出的全部 24 個 Regression 套件，
確認沒有引入任何新的失敗。

本輪**沒有**因此新增功能或重構 Dashboard Integration／Heatmap Engine——
這是一次純粹的測試斷言修正，對應使用者指示「除非 Regression 或 Smoke
發現真正的 Production Bug，否則不得再新增功能或重構；若發現 Bug：先修
Bug，重新跑 Regression，再繼續 QA」。

## 7. Static Audit

- **Engine 未退化**：`public/js/geo-heatmap.js`／`geo-intelligence-map.js`／
  `geo-map-settings.js`／`public/data/geo/taiwan/manifest.json` 與 R5.3-A1
  交付時的 SHA-256 逐位元組相同（見第 4 節、第 5 節）。
- **未新增重複 API**：`routes/analytics-geo.js` 內沒有任何 `/heatmap` 路由，
  `/funnel`／`/fulfillment` 仍各只有一份定義。
- **未新增第二套 Analytics／Dashboard／Channel 狀態**：`geo-heatmap-ui.js`
  完全沒有重新宣告 Engine 既有的 `GEO_HEAT_*` 常數／`geoHeatState`／
  `geoHeatBuildAreas`／`geoHeatRenderLayer`／`geoHeatSelectArea`／
  `geoHeatScheduleUpdate`；Channel 篩選透過既有 `av2SetChannel()`；
  `refreshGeoDashboardKpiBlock()` 仍只有一份定義。
- **無硬編碼／無假造座標**：`geo-heatmap-ui.js` 沒有 `Math.random()`，座標
  資料完全來自既有 API 回應餵給 Engine 既有的 `geoHeatBuildAreas()`。
- **無 debug log**：`geo-heatmap-ui.js` 內沒有 `console.log`/`console.debug`。
- **無重複 Leaflet map／tile layer**：`geo-heatmap-ui.js` 沒有任何
  `L.map()` 或 tile layer 建立程式碼（Integration Smoke SA-16／SA-17／
  B3-6 已用原始碼掃描與 jsdom 實測雙重驗證）。
- **腳本載入順序正確且唯一**：`index.html` 內 `geo-heatmap.js`／
  `geo-heatmap-ui.js` 各只被載入一次，且順序在 `geo-intelligence-map.js`／
  `geo-map-settings.js` 之後（Integration Smoke SA-20～SA-25）。
- **CSS 命名規範**：`geo-heatmap.css` 選擇器一律 `.geo-heat-` 開頭，沒有
  污染泛用選擇器；斷點涵蓋 390/768/1024/1440；使用既有 Dark Theme CSS
  變數，不是寫死亮色。
- **無 `data/pos.db` 殘留**：Regression 使用的測試用暫時 DB 皆在測試結束後
  清除；打包前已確認 `data/` 目錄狀態與 R5.3-A1 交付時一致。
- **無 arbitrary external 資源**：本輪沒有新增任何外部 URL fetch 或第三方
  地圖資源，Leaflet／Tile Provider 仍是既有 R5.2-B2 設定。
- **既有測試腳本的隱私把關修正**（見第 6 節）：範圍嚴格限縮在
  `scripts/smoke-hotfix30-b5-r5-2-a-integrated.js` 一份測試腳本，沒有影響
  任何產品程式碼或其他測試檔案。

## 8. 已知限制 / Manual Visual 必要性

- 本報告與 Integration Smoke 是自動化驗證；真實瀏覽器下的視覺呈現（顏色
  對比、圓點大小、Tab 切換動畫是否流暢、手機版 Ranking 是否確實排在地圖
  下方等）仍需要人工開真實瀏覽器檢查，見
  `R5.3-A1.1_MANUAL_QA_CHECKLIST.md`。
- Heatmap 分頁與既有 Dashboard 分頁共用同一個 Leaflet map instance；若
  商家的 Geo Map Settings（`geo-map-settings.js`）之後有 UI 改版動到
  `geoMapState` 的欄位命名，需要回頭確認 `geo-heatmap-ui.js` 的
  `_geoHeatUiEnsureMapReuse()` 仍能正確讀到 `geoMapState.instance`／
  `geoMapState.geoJsonLayer`（本輪透過既有 API 存取，未直接依賴內部實作
  細節之外的欄位）。
- 目前沒有帶座標的外送訂單時，Integration Smoke 的
  `J-legit-coordinate`／`V-legit-coordinate` 兩條正向斷言會顯示
  「此測試店家本輪沒有帶座標的外送訂單」而以 PASS（跳過驗證）方式通過；
  建議之後若有專門的座標 fixture 店家，可以讓這兩條斷言穩定命中正向路徑。
