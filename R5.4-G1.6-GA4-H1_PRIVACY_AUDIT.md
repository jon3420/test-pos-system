# R5.4-G1.6-GA4-H1 — Privacy Audit

| # | 要求 | 驗證方式 | 結果 |
|---|---|---|---|
| 1 | 不保存 user_pseudo_id | Static Audit（`No user_pseudo_id persisted`）+ Mutation Negative #15 | PASS |
| 2 | 不保存 GA client_id | Static Audit（`No client_id column`）+ Mutation Negative #16 | PASS |
| 3 | 不保存 POS visitor_id 對照 | Static Audit（schema 掃描 ga4_geo_* 三表無 visitor_id 欄位）+ Mutation Negative #3 | PASS |
| 4 | 不寫 geo_visit_log | Static Audit + QA Harness + Mutation Negative #4/#5 | PASS |
| 5 | 不保存 raw IP | Schema 掃描（三表無 ip/raw_ip 欄位） | PASS |
| 6 | 不保存 Service Account JSON | Security Scan（見下） | PASS |
| 7 | 不保存 Access Token | Security Scan | PASS |
| 8 | 不回傳 Property ID | Route/Status 回應欄位掃描（Static Audit `/status response never includes a property_id field`） | PASS |
| 9 | 不回傳 Raw GA4 Response | Static Audit + Mutation Negative #17 | PASS |
| 10 | Frontend 不呼叫 Google API | Static Audit + Mutation Negative #18 | PASS |
| 11 | Frontend 不包含 Credential | Static Audit + Mutation Negative #19 | PASS |
| 12 | Marker 是 Aggregate Marker | `marker_accuracy` 恆為 `district_aggregate`/`county_aggregate`，從無 `exact` | PASS |
| 13 | Marker Point 來自官方 Catalog | `resolveMarkerPoint()` 只查 `authoritativeAdminPointCatalog`，Frontend 只讀 `row.marker_point`（唯讀） | PASS |
| 14 | GA4 Layer 不修改 POS Visitor Layer | Frontend Runtime Test #25/#26/#27（POS Exact/Estimate/Order Marker 不被 GA4 清除動作影響） | PASS |
| 15 | IP Provider 維持 disabled | `.env.example` 仍為 `GEO_VISITOR_IP_ENABLED=false`／`GEO_VISITOR_IP_PROVIDER=disabled`，本輪未修改 | PASS |

## UI 固定聲明

Toolbar 與 Table 皆固定顯示：

> 此資料為 GA4 城市彙總，並非個別訪客實際位置。

（`checkFrontendFixedDisclaimer` + `checkTooltipAlwaysIncludesDisclaimerLine`
+ `checkTableFooterDisclaimerPresent` 三項 Static Audit 檢查逐一驗證出現
位置：Toolbar／Tooltip／Table Footer。）

## 資料性質邊界

本功能處理的是 GA4 Server 端回傳的「城市彙總」數字，不是 POS 個別
visitor_id 事件。`ga4_geo_realtime_snapshots`/`ga4_geo_range_stats` 與
`geo_visit_log`（POS Visitor Layer）是兩套完全獨立、互不交叉引用的資料
表，Server 端沒有任何程式碼路徑把兩者 JOIN 或回填。

## Privacy Gate

**PASS**（15/15 逐項有對應的自動化驗證，非僅文件聲明）。
