# R5.4-G1.5-B2 Settings Architecture

fix18-10-hotfix30-B5-R5.4-G1.5-B2 — GA4 Settings UI, Secure Store Configuration & Connection Test

## Existing Settings Architecture（沿用，未另建第二套）

既有 `settings(store_id, key, value)` key-value 表；`routes/settings.js`
既有多個 GET/PATCH endpoint 都是這個切法（例如既有 `geo-map`）。本輪
`ga4-realtime` 完全沿用這個模式，沒有新增 DB Schema。

## Six Keys

`ga4_realtime_enabled`／`ga4_realtime_property_id`／
`ga4_realtime_stream_id`／`ga4_realtime_single_property_mode`／
`ga4_realtime_cache_seconds`／`ga4_realtime_auto_refresh_enabled`
（`GA4_REALTIME_SETTINGS_KEYS`，`utils/ga4RealtimeConfig.js`）。

## Read API

`GET /api/settings/ga4-realtime` — 見 `R5.4-G1.5-A_API_CONTRACT.md` 完整
欄位表。只讀，不觸發任何 Google API。

## Write API

`PATCH /api/settings/ga4-realtime` — 只接受 6 個白名單欄位。

## Allowlist

`validateGa4RealtimeSettingsPatch(body)`：白名單外欄位、`store_id`、
`credentials`、`private_key`、`access_token`、`refresh_token`、
`client_email` 一律 400 拒絕（`forbiddenKeys` 明確列出，不是漏網之魚才
被順便擋掉）。

## Validation

Property/Stream 純數字（空字串允許）；Cache 30~300；
`enabled=true` 且非 single-property mode 時 Property/Stream 必填；
`single_property_mode=true` 受 `GA4_REALTIME_SINGLE_STORE_MODE` 部署層級
開關限制。

## Raw DB Transaction（本輪最重要的修正）

### 為何共用 `db.transaction()` helper 不適用這個路徑

`utils/db.js` 提供一個共用的 `db.transaction(fn)` helper，內部呼叫
`sqlDb.run('BEGIN')` → `fn(arg)` → `sqlDb.run('COMMIT')`（失敗時
`ROLLBACK`）。這個 helper 本身沒有問題，**問題出在如果 `fn` 內部呼叫的是
「包裝過」的 `db.run()`**（`utils/db.js` 的 `wrap()` 回傳物件上的
`run()` 方法）——這個方法每次呼叫都會執行 `save()`：

```js
run(sql, params = []) {
  const stmt = sqlDb.prepare(sql);
  stmt.run(...);
  ...
  save(); // ← 問題所在：sqlDb.export() + 寫檔
  return { ... };
}
```

`save()` 呼叫 `sqlDb.export()`。實測證實：`sqlDb.export()` 在 sql.js
（WASM SQLite binding）底層會**提早結束**目前開啟中的 SQL Transaction。
後果：
1. 若迴圈中途失敗才呼叫 `ROLLBACK`，會直接丟出
   `cannot rollback - no transaction is active`（因為 transaction 早就
   在前一次 `save()` 時被結束了）。
2. 更嚴重的是，**第一筆寫入其實已經因為 `export()` 被意外
   autocommit**，就算 ROLLBACK 語法本身沒出錯，也救不回已經提早 commit
   的資料——完全不是原子操作。

已用獨立腳本重現（節錄）：
```
BEGIN ok
UPDATE via wrapper ok
ERROR: cannot rollback - no transaction is active
```

### raw `db._db` transaction 的依據

`utils/db.js` 本身在別的地方（hotfix7 categories 表重建遷移）已經用同一種
安全寫法解決過同樣的問題：直接操作 `db._db`（原始 sqlDb，wrap() 回傳物件
的 `_db` 屬性）——`BEGIN` → 一連串 `prepare()/run()/free()`（完全不呼叫
包裝過的 `db.run()`，過程中不觸發任何 `save()`）→ `COMMIT` → 只在
COMMIT 成功後呼叫**一次** `db._save()`。本輪的 PATCH handler
（`routes/settings.js`）採用完全相同的模式：

```js
const rawDb = db._db;
try {
  rawDb.run('BEGIN');
  keysToWrite.forEach((k) => {
    const stmt = rawDb.prepare('UPDATE settings SET value=? WHERE store_id=? AND key=?');
    stmt.run([...]);
    const changes = rawDb.getRowsModified ? rawDb.getRowsModified() : 0;
    stmt.free();
    if (!changes) { /* INSERT OR IGNORE，同樣用 rawDb.prepare */ }
  });
  rawDb.run('COMMIT');
  db._save();
} catch (writeErr) {
  try { rawDb.run('ROLLBACK'); } catch (rollbackErr) { /* 安全忽略 */ }
  return res.status(500).json({ success: false, message: '設定儲存失敗，請稍後再試' });
}
```

**本輪只修復 GA4 Settings PATCH 這一條路徑**，沒有修改
`utils/db.js` 的共用 `db.transaction()` helper 本身，也沒有修改其他既有
使用這個 helper 的呼叫端——那屬於更大範圍的共用元件修復，不在本輪範圍
內，需要另外排期並重跑全專案回歸。

### 驗證結果（獨立於三輪 Regression 矩陣之外）

- **A. 全部成功**：6 個 key 都寫入成功 → COMMIT → 讀回全部是新值。
- **B. 第一筆寫入失敗**：ROLLBACK 執行成功、不拋出「no transaction
  active」錯誤、原設定完全保持（實測：模擬第 1 次 UPDATE 就拋例外，
  最終讀回的 property/stream 仍是**寫入前**的原始值）。
- **C. 中途第三筆寫入失敗**：ROLLBACK 執行成功，前兩筆（本來看似已經
  執行過 `stmt.run()`）連同第三筆一起被撤銷，讀回的三個 key 全部維持
  **寫入前**的原始值（不是「前兩筆保留、第三筆缺席」的半套狀態）。
- **D. COMMIT 本身失敗**：本輪測試環境（sql.js in-memory + 單一
  process）沒有能可靠模擬「COMMIT 語句本身失敗」的方式（sql.js 的
  COMMIT 在磁碟/記憶體都正常時幾乎不會失敗）；已誠實記錄為
  **未直接測試**這個特定子情境，但邏輯上：若 `rawDb.run('COMMIT')`
  本身拋出例外，會被同一個 `try/catch` 捕捉，走進與情境 B/C 完全相同的
  `ROLLBACK` 分支，不會呼叫 `db._save()`、不會 `return
  res.json({success:true})`、不會呼叫
  `invalidateGa4RealtimeCacheForStore()`——這是從程式碼路徑本身可以確定
  的行為，不是臆測。

## Rollback

見上。`catch` 區塊呼叫 `rawDb.run('ROLLBACK')`，外層再包一層
`try/catch`（因為某些「BEGIN 本身就失敗」的邊界情況下 ROLLBACK 自己也
可能丟例外，安全忽略，不影響回傳 500 給前端）。

## Commit-then-invalidate

`invalidateGa4RealtimeCacheForStore(storeId)` 只在
`rawDb.run('COMMIT')` 與 `db._save()` 都執行完、且沒有走 `catch` 分支的
情況下才會被呼叫（寫在 try block 之外、緊接著 try block 之後的下一行）。
失敗路徑直接 `return`，不會執行到這一行。

## Store Isolation

所有讀寫都用 `req.storeId`，SQL 帶 `WHERE store_id=?`；cache key／
generation map／connection test 的 rate limiter 與 in-flight map 都以
`storeId` 為 key，已用 Smoke（B2 Smoke D/F 段）驗證 Store A 的任何操作
不影響 Store B。

## Generation Guard

`utils/ga4Realtime/index.js` 的 `_storeGeneration`（`Map<storeId,
number>`）：`invalidateGa4RealtimeCacheForStore()` 遞增；
`getGa4RealtimeData()` 在 fetch 開始時捕捉 `generationAtStart`，fetch
完成時比對，不相符就不寫 cache、不更新 `last_success_at`/
`last_error_code`。已用 Smoke 驗證：Store A 在一個慢速 in-flight fetch
進行期間變更設定並 invalidate，該筆舊 fetch 完成後不會污染新的 cache
狀態，也不影響 Store B 的 cache/generation。

## Settings UI

見 `public/js/geo-ga4-settings.js`（G1.5-B2a 已完成，本輪未重寫，只做
QA／文件）。

## Save UX

驗證 → PATCH → 成功 toast + 重新 GET → `geoGa4NotifySettingsChanged()`
→（若目前在 GA4 Layer）清舊 Polygon/Summary/Timer 並強制重新 fetch。

## Auto Refresh Integration

`geoGa4State.configAutoRefreshEnabled` 由 activate 時的一次 status GET
決定；`_geoGa4ScheduleAutoRefresh()` 尊重這個 flag，`false` 時完全不排程
（手動 Refresh 仍可用）。Cache 秒數（後端 `_cache` TTL）與 Auto Refresh
秒數（前端 60/120，依 `quota_status`）是兩個獨立概念，沒有混用。

## Permission Reality（誠實記錄，不得省略）

**目前每店 Store Session 即具備該店設定權限，與既有 Geo Map Settings
相同；若未來加入店員角色分級，需要另外增加 `settings-admin`
permission。** 這不是本輪的缺口，是整個 Store Settings 系統既有的一致
行為（`middleware/adminGuard.js` 的 `requireAdminMode` 是完全不同概念，
SaaS 平台層級的 License CRUD 開關，跟店內角色無關），本輪沒有、也不應該
為了 GA4 這一個功能就另外發明一套角色系統。

## Security

見 `R5.4-G1.5-B2_TEST_REPORT.md` 的 Security Scan 章節。

## Known Limitations

- 沒有店員角色分級（見上）。
- COMMIT 本身失敗的情境沒有直接測試（見上「驗證結果」D 項）。
- `invalidateGa4RealtimeCacheForStore()` 不會主動中止已經送出的 Google
  API 呼叫（沒有可靠的取消機制），只保證結果不會寫回 cache。
- Real GA4 Live Test／Manual Visual：NOT TESTED（見
  `R5.4-G1.5-B2_MANUAL_QA_CHECKLIST.md`）。
