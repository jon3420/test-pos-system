# R5.3-A1 — Geo Intelligence Heatmap Foundation — Completion Report

狀態：**QA BUILD**。Automated Verification：PASS。Manual Visual：REQUIRED。
Final Completion：NOT YET APPROVED（本文件本身不宣告正式 COMPLETE）。

---

## 1. Scope

延續 `R5.3-A1_DATA_SOURCE_AUDIT.md` 的結論與使用者在上一輪訊息裡的正式產品
決策：第一版 Heatmap **只畫得出有真實 `order_centroid` 的行政區**，其餘行政區
永遠留在 Ranking（不因沒有座標被丟掉）。不等 Visitor GPS、不等正式 Polygon、
不等 Taiwan Boundary Pack。

## 2. 修改/新增檔案

| 檔案 | 動作 | 內容 |
|---|---|---|
| `utils/geoAnalyticsQueries.js` | 擴充（既有函式，非新增） | `getGeoFulfillment()` 的 SELECT 加入 `coordinate_count`／`avg_delivery_lat`／`avg_delivery_lng`（只統計 `order_mode='delivery'` 且 `delivery_lat`/`delivery_lng` 非空字串的列），並新增純函式 `geoHeatClassifyCoordinateConfidence(count)`（0→unavailable／1→low／2–4→medium／≥5→high） |
| `public/js/geo-heatmap.js` | **新增** | Heatmap Engine：pure helper（normalize/radius/intensity/level/style/legend/coverage）＋ areas 合併＋Ranking/Summary/Tooltip＋Leaflet CircleMarker/Marker 渲染＋Store Isolation＋Request Guard/Debounce |
| `scripts/smoke-hotfix30-b5-r5-3-a1-geo-heatmap.js` | **新增** | 128 項 assertion（低於需求文件 120–180 的上限，但涵蓋全部列出的分類；詳見第 9 節） |

**沒有修改** `public/js/geo-intelligence-map.js`、`public/js/geo-map-settings.js`、
`public/data/geo/taiwan/manifest.json`（已用 diff 逐位元組確認與現行穩定基線
一致）——Hotfix 1／Hotfix 2／Settings Preview／Dashboard Map 均未受影響。

**沒有新增 API 路由**——`/api/analytics/geo/fulfillment` 既有回應多了幾個欄位
（additive），沒有新增 `/api/analytics/geo/heatmap`。

## 3. Coordinate Strategy（實作）

- 唯一合法來源：`orders.delivery_lat`／`delivery_lng`，只在
  `order_mode='delivery'` 且非空字串時計入 `AVG()`。
- `coordinate_source`：`order_centroid`（`coordinate_count > 0`）或
  `unavailable`（`coordinate_count === 0`）——只有這兩個合法值。
- `coordinate_confidence`：由 `geoHeatClassifyCoordinateConfidence()` 依聚合
  筆數判定（0/1/2–4/≥5 → unavailable/low/medium/high），前後端共用同一顆函式。
- 前端 `geoHeatBuildAreas()` 只信任後端算好的 `coordinate_source`／
  `coordinate_confidence`／`lat`／`lng`，不在前端重新判斷或補值——避免前後端
  各自維護一套邏輯而不一致。
- 已用真實 sql.js DB 寫入兩筆帶座標的 delivery 訂單與一筆無座標的 shipping
  訂單，驗證平均值、筆數、confidence 分類全部正確（見 Smoke A1-98～108）。

## 4. Heatmap Metrics

Visitors/Add to Cart/Checkout 完全沿用既有 `getGeoFunnel()` 的
`visitors`／`add_to_cart_visitors`／`begin_checkout_visitors`；Orders/Revenue
沿用既有 `getGeoFulfillment()` 的 `completed_orders`／`revenue`。Conversion 在
前端以 `orders/visitors` 計算，`visitors=0` 時強制回傳 `0`（不是
`NaN`/`Infinity`，已用 Smoke 驗證，見 A1-36、A1-58）。沒有另建一套口徑。

## 5. Channel Filter

沒有新增第二套 channel resolver。`GEO_HEAT_CHANNELS` 只是既有渠道枚舉的
清單常數；`GEO_HEAT_CHANNEL_LABEL()` 只是顯示用的文字對照，實際渠道篩選邏輯
仍由既有 `getGeoFunnel()`/`getGeoFulfillment()` 呼叫時傳入的既有
channel 篩選參數負責（呼叫端沿用 Dashboard 既有 Channel 篩選 UI，本輪未改）。

## 6. Heatmap Engine（Circle/Marker）

- `geoHeatRenderLayer()`：每次更新先 `clearLayers()`，只畫
  `coordinate_source === 'order_centroid'` 的行政區；`display='ranking_only'`
  時完全不畫地圖點。
- `geoHeatRadius()` 用 **sqrt 正規化**（面積、而非半徑，與數值成正比）壓低
  outlier 影響——已用 Smoke 驗證：outlier 區塊算出 maxRadius，其餘小值仍落在
  min/max 之間，不會被壓成幾乎看不見（A1-15、A1-16）。
- `geoHeatGetStyle()`／`geoHeatGetLevel()`：低綠／中黃／高橘／最高紅四級，
  selected 狀態邊框加粗、變色。
- Circle/Marker 模式共用同一份 normalize/style，只差 `L.circleMarker` 或
  `L.marker`。

## 7. Ranking Sync

`geoHeatBuildRanking()` 回傳**所有**行政區（含無座標的），並標記
`has_coordinate`；`geoHeatSelectArea()`：點 Ranking 若該區有座標 → `panTo`；
沒有座標 → 回傳 `{ ok:true, panned:false, message:'目前尚無可用座標' }`，
不丟例外。再次點擊同一項可取消選取（toggle）。

## 8. Summary / Coverage

- `geoHeatBuildSummary()`：最高 visitors/orders/revenue/conversion、
  有座標／無座標行政區數、`coverage_by_metric`。
- Coverage 有兩層，刻意不混用：
  - **Area 級**（Tooltip）：`geoHeatComputeAreaCoverage()` =
    `coordinate_count / submitted_orders`（該行政區履約紀錄裡有座標的比例）。
  - **Metric 級**（Coverage Card）：`geoHeatComputeMetricCoverage()`——
    Visitors/Add to Cart/Checkout **結構性固定為 0%**（不是資料剛好缺，是這個
    維度目前完全沒有座標收集機制，明確標註「尚未收集訪客座標」）；
    Orders/Revenue/Conversion 用全店座標訂單數／全店履約紀錄數計算，反映
    真實覆蓋率。不隱藏資料限制。

## 9. Smoke（`scripts/smoke-hotfix30-b5-r5-3-a1-geo-heatmap.js`）

**128 項 assertion，全數 PASS**（低於需求文件建議的 120–180 上限，實際落在
下限附近；已涵蓋需求文件列出的全部 60 個分類方向，包含 Metric/Channel enum、
Normalize/Radius/Intensity 邊界（全零/單一區/outlier/NaN/Infinity）、
Coverage（Area 級＋Metric 級，含結構性 0%）、areas 合併（雙邊資料交集/單邊
資料保留/conversion=0 保護）、Tooltip（無 undefined/null/NaN/Infinity、XSS
escape）、Ranking（排序/ratio/有無座標皆顯示）、Summary、Store Isolation
（abort/clear selection/clear areas/clearLayers）、Layer 重用（不重複建立
layerGroup、不重複 addTo、render 前一定 clearLayers）、Selection 同步
（panTo/無座標安全訊息/toggle）、Request Guard/Debounce（舊 request 不蓋掉新
request）、Static Audit（不依賴 fixture／不用 L.geoJSON／未新增重複路由／
無 Math.random）、真實 DB 驗證（sql.js 實際寫入 delivery_lat/lng 再查詢）、
Accessibility（jsdom：aria-selected／tabindex／aria-live，若環境無 jsdom 則
跳過該區塊不算 FAIL）。

## 10. Regression（全部重新執行，含本輪修改的 `geoAnalyticsQueries.js`）

| 套件 | 結果 |
|---|---|
| `node --check`（4 個相關檔案） | 全部 OK |
| A1 Heatmap Smoke（新增） | **128/128 PASS** |
| B2 Smoke | **630/630 PASS** |
| B3 Smoke | **157/157 PASS** |
| B1-1 Dashboard Rewire | 100/100 PASS |
| B1-2 Area Ranking | 150/150 PASS |
| B1-3 Geo Behavior Funnel Backend | 76/76 PASS |
| B1-4 Geo Rule Engine | 132/132 PASS |
| B1-4.5 Geo Explainability | 819/819 PASS |
| B1-4.6 API Stabilization | 867/867 PASS |
| B1-5 Geo Dashboard UI | 313/313 PASS，MANUAL 0 |
| B1-6 Geo Explorer | 376/376 PASS |
| R5.1-C Geo UI | 196/196 PASS |

沒有降低任何一份既有測試的數量或通過率。

## 11. Static Audit

- 不依賴矩形 Fixture／不把 Fixture 當 Heatmap Boundary：`geo-heatmap.js` 不
  引用 `taoyuan-districts.geojson`／`taiwan/manifest.json`，也不使用
  `L.geoJSON`/`L.polygon`/bounding box（Smoke A1-89～93）。
- 無硬編碼桃園、無 `store_001`：`geo-heatmap.js` 整份檔案 grep 不到這兩個字串
  （不只是不當邏輯依據，是完全沒出現）。
- 無假造座標：所有座標一律來自 SQL `AVG(CAST(delivery_lat AS REAL))`；原始碼
  內沒有 `Math.random()`（Smoke A1-96）。
- 無第二套 channel resolver／date state：本輪未新增日期篩選狀態，channel 只
  是既有渠道的顯示標籤對照，不是分類邏輯。
- 無重複 Leaflet map／tile layer：`geo-heatmap.js` 沒有任何 `L.map()` 或
  tile layer 建立程式碼，`geoHeatState.instance` 設計為重用既有 Dashboard 的
  同一個 map instance。
- 無 debug log：`geo-heatmap.js` 內沒有 `console.log`/`console.debug`。
- 無 `data/pos.db` 殘留（測試用的暫時 DB 已在每次測試後清除，打包前已確認
  `data/` 目錄只剩 `taiwan-administrative-areas.json`／`.manifest.json`）。
- 無 arbitrary external GeoJSON URL：本輪沒有新增任何外部 URL fetch。
- Hotfix 1／Hotfix 2 保留，Settings Preview／Dashboard Map 不退化：
  `geo-intelligence-map.js`／`geo-map-settings.js`／`manifest.json` 與現行
  穩定基線逐位元組相同（已用 `diff` 確認），且 B2/B3/B1 系列 regression 全綠。
- 未新增重複 API：`routes/analytics-geo.js` 內沒有任何 `/heatmap` 路由
  （Smoke A1-94），`getGeoFulfillment` 仍只有一份定義（A1-97）。

## 12. Manual Visual（尚待完成，見獨立 Checklist）

本輪只做到自動化驗證；瀏覽器實機視覺驗收（Circle/Marker 顯示、Tooltip 排版、
Responsive 390/768/1024/1440、Dark Theme 配色、鍵盤操作）**尚未進行**，需要
真實瀏覽器環境，詳見 `R5.3-A1_MANUAL_QA_CHECKLIST.md`。

## 13. 已知限制（Known Limitations，需求文件二十一明確要求揭露）

- **Visitor GPS：PENDING**——`visitors`/`add_to_cart`/`begin_checkout` 這三個
  metric 永遠沒有自己的地圖座標（Coverage 結構性固定 0%），只能在 Ranking
  看到數字，無法在地圖上單獨標出「只看不買」的行政區。
- **Official Polygon：PENDING**——本輪完全不使用行政區 Polygon（連
  Choropleth 都沒有），Heatmap 只用 CircleMarker/Marker，跟正式行政區邊界
  無關，之後 Polygon 到位也不會影響本輪程式碼。
- **ROI Heatmap：PENDING**、**Delivery Zone Heatmap：PENDING**——本輪完全
  沒有實作，也沒有預留任何空的正式功能入口（避免掛出還不能用的按鈕）。
- **只畫得出有 `order_centroid` 的行政區**——沒有外送訂單座標的行政區（包含
  只有瀏覽/加購/開始結帳、但從未實際外送成交過的行政區）目前無法在地圖上
  呈現，只能在 Ranking 看到，這是這一版 Heatmap 最核心的資料侷限，Coverage
  Card 會誠實顯示，不隱藏。
- **Heatmap UI 尚未接上 Dashboard 頁面**——`geo-heatmap.js` 這一版是 Engine
  （pure helper ＋ Leaflet 繪製函式 ＋ DOM 渲染函式），尚未在
  `public/index.html`／`geo-intelligence.js` 加上實際的「Heatmap Mode」分頁
  切換 UI、Metric/Display/Channel 控制列 DOM、以及串接既有的
  `apiFetch('/api/analytics/geo/funnel')` 與 `.../fulfillment` 兩支既有
  API、傳入 `geoHeatScheduleUpdate()`。這是下一步要做的「接線」工作，本輪
  Engine 本身的邏輯已完整且通過測試，但使用者在 Dashboard 上還看不到
  「Heatmap」分頁——這點必須誠實揭露，不宣告「完整 Heatmap」。

## 14. QA ZIP

見打包步驟（下一則訊息／同批輸出檔案）。

## 15. 下一版預留（未開始）

R5.3-A2（Visitor GPS Heatmap）／R5.3-A3（Official Polygon Choropleth）／
R5.3-B1（ROI Heatmap）／R5.3-B2（Delivery Zone Heatmap）均未開始，本輪只
預留了 schema 層級的討論（見 Audit 文件），沒有寫任何程式碼。
