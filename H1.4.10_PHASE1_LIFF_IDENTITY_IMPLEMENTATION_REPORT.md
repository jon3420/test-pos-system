# H1.4.10 Phase 1 — LIFF 核心會員歸戶 Implementation Report

## 一、目標資料鏈（已達成）

```
LINE User → member_session → visitor_id → session_id → cart_id → order_id
```

- LIFF Client 內、已登入、`auto_identify_enabled=1` → 背景辨識，不彈窗、不呼叫 `liff.login()`。
- 非 LIFF Client（Chrome／Safari／Facebook／Instagram／廣告入口）→ 保持匿名，正常下單。

## 二、新增／修改檔案清單

| 檔案 | 修改類型 | 內容 |
|---|---|---|
| `public/js/line-member-gate.js` | Additive | `tryPassiveLiffIdentification()`、`_passiveVerifyWithBackend()`、`_buildAutoIdentifyMetadata()`、`_emitAutoIdentifyEvent()`、`_reasonCodeFromTokenCode()`、`_reasonCodeFromVerifyResult()`；`initLineMemberGate()` 新增選填第 4 參數 `opts.skipLoginCallback` |
| `routes/line-member.js` | Additive | 新增 `POST /link-context`；`/verify` 內 additive 補記 `line_liff_auto_identify_success/failed`（僅當 `gate_stage==='liff_auto_identify'`） |
| `routes/settings.js` | Additive | `LINE_MEMBER_KEYS` 新增 `line_member_auto_identify_enabled`、`line_shipping_liff_id` |
| `routes/analytics.js` | Additive | `SERVER_ONLY_EVENTS` 新增 success/failed 兩個事件；新增 `LIFF_AUTO_IDENTIFY_EVENTS`／`LIFF_AUTO_IDENTIFY_REASON_CODES`／`sanitizeLiffAutoIdentifyMetadata()`，串入既有 metadata sanitize pipeline |
| `utils/analyticsLog.js` | Additive | `EVENT_WHITELIST` 新增 5 個事件名稱 |
| `public/line-order.html` | Additive | `_buildLineMemberGateConfig()` 新增 `auto_identify_enabled`；`shouldInitLiff` 邏輯；`skipLoginCallback` 判斷；`_linkLineMemberContext()`；`_trackAddToCart()`／Handoff restore 完成後呼叫補綁定 |
| `public/line-shipping.html` | Additive | 同上（無 Handoff 分支）；`line_shipping_liff_id` fallback |
| `scripts/run-h1-4-10-phase1-liff-identity-runtime.js` | 新增 | Phase 1 專用回歸測試（68 項） |

**沒有刪除或修改任何既有函式的既有呼叫簽章**（`initLineMemberGate` 新參數為選填，未傳等同原行為）。

## 三、新增 Settings

| Key | 預設 | 說明 |
|---|---|---|
| `line_member_auto_identify_enabled` | `'0'`（未設定視為關閉） | 「由 LINE LIFF 進入時自動辨識會員」，與 `line_member_gate_enabled` 完全獨立 |
| `line_shipping_liff_id` | 空字串 | 冷藏宅配專用 LIFF ID；空值時 `line-shipping.html` fallback 回 `line_member_liff_id` |

兩者皆為既有 key-value `settings` 表新增列（**沒有新增資料表、沒有 schema migration**），寫入路徑沿用 `PUT /api/settings` 既有白名單機制（`LINE_MEMBER_KEYS`）。

## 四、新增 API

### `POST /api/line-member/link-context`

- Request：`{ member_session, visitor_id, session_id, cart_id }`
- 絕不接受／使用 `line_user_id` 欄位（即使夾帶也完全忽略，身分只認 `member_session`）
- `verifyMemberSession(member_session, req.storeId)` 失敗 → `200 { success:false, reason:'invalid_session' }`（不 500，不阻擋流程）
- 成功 → 呼叫既有 `linkMemberSession()`（idempotent upsert）
- Rate limit：沿用 `/verify` 既有 in-memory bucket 機制，key 額外加 `link-context|` 前綴避免互相干擾

## 五、新增 Analytics Events

| Event | 送出端 | 說明 |
|---|---|---|
| `line_liff_auto_identify_started` | 前端 | 6 條件全部符合、即將呼叫 verify |
| `line_liff_auto_identify_success` | **後端**（SERVER_ONLY） | `/verify` 內確認 `gate_stage==='liff_auto_identify'` 且驗證成功才寫入 |
| `line_liff_auto_identify_skipped` | 前端 | 條件不符（`reason_code` 見下表） |
| `line_liff_auto_identify_failed` | 前端（Token 未就緒）／**後端**（verify 失敗，SERVER_ONLY） | 依失敗發生位置分流 |
| `line_member_context_linked` | 前端 | 觀察性事件：已呼叫 `/link-context`（不代表一定綁定成功，`reason_code=context_missing` 時代表未成功） |

`reason_code` 白名單（`sanitizeLiffAutoIdentifyMetadata()` 強制）：
`already_identified` / `auto_identify_disabled` / `missing_liff_id` / `not_in_line_client` / `not_logged_in` / `liff_init_failed` / `id_token_missing` / `id_token_expired` / `verify_failed` / `network_error` / `context_missing` / `unknown`

metadata 欄位白名單：`page_type` / `environment` / `gate_mode` / `reason_code` / `is_in_client` / `is_logged_in` / `has_existing_session` / `has_cart`。不含 Token／UID／電話／地址／member_session。

## 六、`initLineMemberGate()` 契約變更（Additive）

```js
initLineMemberGate(config, ids, onEvent, opts)
```

| 情境 | `skipLoginCallback` |
|---|---|
| checkout Gate（`gate_enabled=true, gate_mode='checkout'`） | 不傳（`false`） |
| entry Gate（`gate_enabled=true, gate_mode='entry'`） | 不傳（`false`） |
| Cart Handoff（`cartToken` 存在，line-order 限定） | 不傳（`false`） |
| hotfix25 login-resume（實際上是上面幾種情境之一觸發後的行為） | 不傳（`false`） |
| **純 Auto Identify**（`gate_enabled=false` 且無 handoff） | `true` |

## 七、身分安全確認

- 前端絕不儲存 Access Token／ID Token／完整 LINE UID／raw `line_user_id` 到 localStorage（只有 `member_session` 與遮罩後的 profile）。
- `/link-context` 完全不信任前端送來的任何身分欄位，只透過 HMAC 簽章的 `member_session` 取得可信 `line_user_id`。
- 未新建第二套 JWT／第二套 `line_member_sessions`／第二套 CRM。

## 八、已知限制（誠實揭露，留給 Phase 2 或之後）

1. 後台目前沒有「由 LINE LIFF 進入時自動辨識會員」的可視化開關 UI；設定需透過 `PUT /api/settings` 直接寫入 `line_member_auto_identify_enabled`。
2. `line-shipping.html` 目前沒有 Cart Handoff（`cart_token`）流程，因此只有「被動辨識成功」與「首次建立 cart_id」兩個補綁定時機（`line-order.html` 多一個 Handoff restore 完成的時機）。
3. `friend_entry` / `friend_checkout` / cart recovery / n8n / LINE Push / CRM 擴充 完全未動，留給後續 Phase（依你的指示，本輪刻意不做）。
