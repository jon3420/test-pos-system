# R5.4-G1.6-A1.2 — Authoritative Source Reality Audit

fix18-10-hotfix30-B5-R5.4-G1.6-A1.2 — Authoritative Administrative
Representative Points & Region-only Marker Payload

## 1. 官方來源

使用者上傳兩個由政府資料開放平臺／內政部國土測繪中心下載的完整 ZIP：

| | Dataset 7442 | Dataset 7441 |
|---|---|---|
| 名稱 | 直轄市、縣(市)界線(TWD97經緯度) | 鄉(鎮、市、區)界線(TWD97經緯度) |
| 檔名 | 直轄市_縣_市_界線1140318.zip | 鄉_鎮_市_區_界線1140318.zip |
| Archive SHA-256 | `9ff4d337e03a711d7e17caaf4d0c46a0fe2c6bd3660b321a2e35123030d8491c` | `c5dc6c0f9a8cc1aad6758a0a6fb81b203718b37f42c5aa8e684262ca24a7d9dd` |
| Feature 數 | 22 | 368 |

兩個 SHA-256 已對使用者上傳的原始檔案位元組直接計算，且
`scripts/build-authoritative-admin-points.js` 每次執行都會重新計算並跟
`SOURCE_MANIFEST.json` 記錄值比對，不符就直接 Build Fail（不是憑印象
記一次就假設永遠正確）。

## 2. Dataset ID／Provider／License 驗證範圍（誠實記錄）

**Dataset ID（7442／7441）、提供機關（內政部國土測繪中心）、授權方式
（政府資料開放授權條款第1版）— 這些是使用者上傳檔案時提供的來源聲明。**

本輪執行環境的網路白名單（見 `network_configuration`）不包含
`data.gov.tw`／`nlsc.gov.tw`，因此**沙箱本身無法連線該平台頁面**，獨立
核對資料集描述文字、授權頁面逐字內容。這在上一輪
（`R5.4-G1.6-A1.2_SOURCE_ACQUISITION_BLOCKER.md`）已經確認過，本輪沒有
改變。

但本輪**確實**對「使用者上傳檔案內部隨附的 Metadata」做了獨立驗證
（不是只採信使用者聲明，而是實際打開檔案核對）：

- `.shp.xml`（ArcGIS FGDC 格式 metadata）內的 `lineage` 記錄明確包含
  「國土測繪中心」「內政部」「行政區域界線維護」等處理歷程字串，跟
  聲明的提供機關一致。
- Feature 數（22 縣市、368 鄉鎮市區）跟臺灣實際行政區數量完全一致。
- 縣市／鄉鎮市區 bounding box 涵蓋太平島（約北緯 10.37 度）、東沙群島、
  金門、馬祖——這些是中華民國實際管轄但地理上偏遠的離島，一般隨手拼湊
  或第三方簡化資料集不太可能完整涵蓋，是支持「這是完整官方資料集」的
  間接證據。
- DBF 屬性欄位（`COUNTYCODE`／`COUNTYNAME`／`COUNTYENG`／`TOWNCODE`／
  `TOWNNAME`／`TOWNENG`）跟本專案既有 `taiwan-administrative-areas.json`
  的欄位慣例（`county_code`／`subdivision_code` 格式）完全吻合（見第 12
  節交叉比對）。

**驗證狀態總結：archive hash／DBF 欄位／CRS／Feature 數屬「本輪環境可
獨立重現驗證」；Dataset ID／Provider 名稱／License 名稱屬「使用者來源
聲明 + 隨附 Metadata 佐證，但沙箱本身無法連線官網頁面做最終逐字核對」。**
兩者性質不同，本文件刻意分開陳述，不混為一談。

## 3. DBF 欄位

- County：`COUNTYID`／`COUNTYCODE`／`COUNTYNAME`／`COUNTYENG`。
- Township：`TOWNID`／`TOWNCODE`／`COUNTYNAME`／`TOWNNAME`／`TOWNENG`／
  `COUNTYID`／`COUNTYCODE`。

皆已用 `pyshp`（Python，初步驗證）與 `shapefile`（Node，正式 Build Tool
使用的函式庫）分別讀取，欄位名稱與筆數一致。

## 4. CRS

`.prj` 內容（county／township／Town_Majia_Sanhe 三個檔案皆相同）：

```
GEOGCS["GCS_TWD97[2020]",DATUM["D_TWD_1997",SPHEROID["GRS_1980",6378137.0,298.257222101]],PRIMEM["Greenwich",0.0],UNIT["Degree",0.0174532925199433]]
```

用 `pyproj.CRS.from_wkt()` 辨識為 **EPSG:3824**（TWD97 [2020] 地理座標
系統）。目標座標系統為 **EPSG:4326**（WGS84）。

**措辭修正（本輪已依指示更正）**：抽樣座標點（以 121.5645, 25.0330 為例）
分別用 `pyproj.Transformer` 與 Node `proj4` 執行 EPSG:3824 → EPSG:4326
轉換，兩者輸出結果與輸入座標差異為 0（浮點精度內）。**這只能說「依目前
使用的 CRS 定義與轉換函式庫，抽樣轉換結果未觀察到可量測位移」，不能寫成
「EPSG:3824 就是 EPSG:4326」或「TWD97 完全等於 WGS84」**——這是兩個不同
定義的座標系統，只是此次抽樣轉換沒有觀察到位移，不代表在所有情況、所有
函式庫版本下都恆等。Production Builder（`scripts/build-authoritative-
admin-points.js`）仍明確執行 `proj4()` 轉換呼叫（`transform_performed:
true` 記錄在 Catalog 內），不是省略轉換直接複製數字。

## 5. 瑪家鄉修正檔（Town_Majia_Sanhe）稽核

鄉鎮市區 ZIP 內額外包含 `Town_Majia_Sanhe.*` 與
`修正清單_11403.xlsx`。**不得只憑檔名判斷「整筆替換瑪家鄉」**——實際用
`openpyxl` 打開這份 Excel，三個分頁（縣市界／鄉鎮市區界／村里界）記錄的
修正對象是：

- 新竹縣尖石鄉／苗栗縣泰安鄉／臺中市和平區（「與縣市界共線」的邊界
  釐整）
- 臺南市善化區（村里界釐整）

**完全沒有提到瑪家鄉（10013280）或三和。**

用 Shapely 分析 `Town_Majia_Sanhe.shp` 與主檔案（`TOWN_MOI_1140318.shp`）
瑪家鄉 Polygon 的幾何關係：

- `equals`：False
- `main.contains(corr)` / `corr.contains(main)`：皆 False
- `intersects`：True，交集面積 ≈ corr 面積的 92%
- `symmetric_difference` 面積 ≈ main 面積的 8.6%

兩者是實質不同、但大部分重疊的邊界版本，不是浮點誤差，也不是單純誰包含
誰。**因為隨附的官方修正清單完全沒有記載這個檔案的用途或套用方式**，依
規則不得自行合併（replacement／union／difference+union 皆無根據）。

**決策：`majia_correction_status = "reference_only"`**——Catalog 的瑪家鄉
Representative Point 完全用主檔案幾何算出，`Town_Majia_Sanhe.shp` 僅讀入
供比對記錄，不影響任何輸出（Catalog 仍是 368 筆，不是 369 筆）。

## 6. 不能只刪除安全過濾（Dashboard `if (!coord) continue`）

見 `R5.4-G1.6-A1.2_REGION_PAYLOAD_REALITY_AUDIT.md` 完整分析。

## 7. Exact 可用性

`utils/geoVisitLog.js` 的 `getGeoLiveMarkerPoints()`（既有 Exact Marker
查詢）本輪**完全未修改**，`/api/geo-live/markers` 既有 contract 保持
100% 相容。

## 8. Estimate 現況

見 `R5.4-G1.6-A1.2_IMPLEMENTATION_REPORT.md`——已解除 A1.1 的
BLOCKED 佔位，Estimate Marker 現在由官方 Catalog 提供真實座標。

## 9. Gate 判定

**Official Source Gate：PASS**（本輪最終判定，理由見第 1-4 節；Dataset
ID／Provider／License 屬使用者聲明+檔案內部證據佐證，已誠實標註驗證
範圍）。

---

*本文件不含任何真實憑證、Property、Stream 或 Token 內容。*
