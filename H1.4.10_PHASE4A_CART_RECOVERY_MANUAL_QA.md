# H1.4.10 Phase 4A — CART RECOVERY STATE ENGINE Manual QA

Phase 4A 本身不寄送任何訊息（無 LINE Push／n8n／優惠券），所以 Manual QA 重點是「狀態機是否忠實反映真實顧客行為」，而不是「顧客有沒有收到提醒」（那是 Phase 4B 之後的範圍）。以下項目建議在正式環境用後台資料庫查詢（或未來的 Recovery Dashboard）確認，目前尚無 UI 可直接查看。

## 前置設定

透過 `PUT /api/settings` 設定：
```
cart_recovery_enabled = 1
cart_recovery_cart_delay_minutes = 60（或依店家需求調整）
cart_recovery_checkout_delay_minutes = 30
cart_recovery_payment_delay_minutes = 15
```

## A. 一般加購未結帳

**操作**：顧客加入購物車商品，60 分鐘內都沒有任何動作。

**預期**：
- [ ] `cart_recovery_jobs` 出現一筆 `stage=cart_abandoned, status=pending`
- [ ] `due_at` ≈ 最後一次 `add_to_cart` 時間 + 60 分鐘
- [ ] 顧客若在 60 分鐘內又加了商品，`due_at` 應該順延（不是從第一次加購開始算）

## B. 前往結帳但沒填完資料

**操作**：顧客加購後按「前往結帳」，之後就沒有再操作（沒按確認下單）。

**預期**：
- [ ] 原本的 `cart_abandoned` job 變成 `cancelled`
- [ ] 出現一筆 `stage=checkout_abandoned, status=pending`
- [ ] 若顧客有按過「確認下單」但驗證失敗（例如忘記填電話），`checkout_abandoned` 的 `last_event` 應該更新為 `checkout_validation_failed`，且**不會**多出第二筆 checkout job

## C. 現金訂單成立

**操作**：顧客填完資料、選現金付款、成功送出訂單。

**預期**：
- [ ] `cart_abandoned`／`checkout_abandoned` 皆變成 `cancelled`
- [ ] **沒有**任何 `payment_abandoned` job（現金不經過線上付款確認流程）

## D. LINE Pay 訂單成立但尚未完成付款

**操作**：顧客選 LINE Pay、成功建立訂單、跳轉到 LINE Pay 付款頁，但這時候先不要操作。

**預期**：
- [ ] `cart_abandoned`／`checkout_abandoned` 因 `submit_order` 變成 `cancelled`
- [ ] 出現一筆 `stage=payment_abandoned, status=pending`，`order_id` 是這筆訂單真正的編號
- [ ] `recoverable_value` 等於這筆訂單的 `orders.total`（不是購物車頁面上顯示的任何暫存金額）

## E. LINE Pay 付款成功

**操作**：接續 D，完成真實付款。

**預期**：
- [ ] `payment_abandoned` job 變成 `converted`
- [ ] 這筆 job 不再出現在「目前可追回」的查詢結果中

## F. LINE Pay 放棄付款

**操作**：接續 D，直接關閉 LINE Pay 頁面或取消付款，20 分鐘後都不再回來操作。

**預期**：
- [ ] `payment_abandoned` job 仍是 `pending`（沒有被任何事件誤取消）
- [ ] 這是 Phase 4A 最核心要能表示的情境——後續 Phase（LINE Push 等）就是要根據這筆 job 去做提醒

## G. 同一顧客多次加購/清空重來

**操作**：顧客加購、清空購物車、重新加購。

**預期**：
- [ ] 第一次加購產生的 `cart_abandoned` 應該還在（除非有 `checkout_click`/`submit_order` 等事件將其取消）
- [ ] 若第二輪加購使用同一個 `cart_id`，不應該產生第二筆 `cart_abandoned`（沿用 idempotency）

## H. 多店家資料隔離

**操作**：若同一 POS 系統上有多家店，確認 A 店的加購/結帳行為不會影響 B 店的 recovery job。

**預期**：
- [ ] 直接查 `cart_recovery_jobs` 表，確認每筆記錄的 `store_id` 正確對應，沒有跨店污染

---

## 尚待項目（Phase 4B 之後）

- 實際發送 LINE Push 提醒（本階段完全沒有寄送任何訊息）
- Recovery consent（顧客同意才提醒）
- n8n 排程執行（本階段只建立「狀態」，沒有排程器去真正「執行」提醒）
- Recovery Dashboard（後台可視化查看可追回名單、金額、轉換率）
- Admin Settings UI（`cart_recovery_*` 目前只有 backend contract，沒有後台可視化開關）
