# H1.4.10 Phase 3 — CHECKOUT-SUBMIT-DIAGNOSTICS + PAYMENT-SUCCESS Manual QA

以下項目需要真實 LINE Pay Sandbox／真機瀏覽器才能驗證，無法在 sandbox 內自動化。

## A. Missing Name

**操作**：進入結帳頁，姓名留空，按「確認下單」。

**預期**：
- [ ] `checkout_submit_click` +1
- [ ] `checkout_validation_failed`：`reason_code=missing_name`
- [ ] 不建立訂單

## B. Retry

**操作**：接續 A，補上姓名，電話留空，再按一次「確認下單」。

**預期**：
- [ ] `checkout_submit_click` 再 +1（累計 2 次，證明非永久去重）
- [ ] 第二筆 `checkout_validation_failed`：`reason_code=missing_phone`

## C. Cash Success

**操作**：填完全部必要資料，付款方式選「現金」，按「確認下單」。

**預期**：
- [ ] `checkout_submit_click` +1
- [ ] `checkout_validation_failed` 不增加
- [ ] 訂單成立
- [ ] `payment_started` 依既有流程正常記錄一次
- [ ] `payment_success` 不增加（現金訂單本來就不該有這筆事件）

## D. LINE Pay Request

**操作**：付款方式選「LINE Pay」，按「確認下單」。

**預期**：
- [ ] `checkout_submit_click` 正常送出
- [ ] LINE Pay 付款請求成功建立（`payment_started` 記錄、成功導向 LINE Pay 付款頁）
- [ ] 此時 `payment_success` 尚未增加（只是建立請求，不是付款成功）

## E. LINE Pay Success

**操作**：接續 D，在 LINE Pay 頁面完成真實付款。

**預期**：
- [ ] LINE Pay Confirm 成功後，`payment_success` +1
- [ ] 訂單的 `payment_status` 更新為 `paid`
- [ ] Dashboard／後台可查到該筆 `payment_success` 事件的 `metadata.value` 與訂單金額一致

## F. LINE Pay Cancel

**操作**：接續 D，在 LINE Pay 頁面按「取消付款」，返回點餐頁。

**預期**：
- [ ] `payment_success` 不增加
- [ ] 購物車內容仍保留（未被清空）
- [ ] 可以重新選擇付款方式再次嘗試

## G. LINE Pay Failure

**操作**：讓 LINE Pay Confirm 回傳失敗（例如卡片被拒、Sandbox 測試失敗情境）。

**預期**：
- [ ] `payment_success` 不增加
- [ ] 訂單的 `payment_status` 不可被標記為 `paid`
- [ ] 顧客看到清楚的付款失敗提示，並可重新嘗試

## H. Duplicate / Refresh

**操作**：LINE Pay 付款成功後，重新整理成功頁面，或模擬 LINE Pay callback 重送（例如瀏覽器返回鍵再前進）。

**預期**：
- [ ] `payment_success` 不重複增加（後台查詢該 `order_id` 的 `payment_success` 事件數仍是 1）
- [ ] 頁面正常顯示訂單成立/付款成功畫面，不報錯

---

以上 A～H 全數需要正式環境或 LINE Pay Sandbox 真實走一次才能最終確認；sandbox 自動化測試（Phase 3 Test Report）已涵蓋等價的邏輯路徑（explicit-button-only 因果、validation reason 正確性、payment_success 的 server-authoritative 與 idempotent 特性、GA4/Meta 映射隔離），本文件是最後一層真機/真實金流驗證。
