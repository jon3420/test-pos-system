# R5.3-A7 — Geo KPI Single Source Integration — Completion Report

**狀態：QA BUILD — 非 Final Release**

## 1. Scope

「Geo 訪客／Geo 加購／Geo 結帳／Geo 訂單／Geo 成交率／Geo 辨識率／Unknown
比例」這組 KPI 卡片，正式資料來源從 `vm.funnel`（`getGeoFunnel()`）改為
`geoVisitorState.funnel`（Geo Event Engine）。不修改 UI 結構、不刪除
`getGeoFunnel()`/`/api/analytics/geo/funnel`、不新增 API、不新增第二套
state、不觸碰 Order Heatmap 與座標規則。

## 2. 根因（R5.3-A6 已診斷，本輪未重新分析）

`utils/geoAnalyticsQueries.js` 的 `_visitorGeoAttributionCTE()`：
`visitor_geo_earliest` CTE 用 `WHERE (geo_city IS NOT NULL OR geo_district
IS NOT NULL)` 把 Unknown 訪客整筆排除在 CTE 之外（不是分類成 Unknown，是
完全不存在），導致 `getGeoFunnel()` 的 `areas` 陣列缺這一筆，KPI 卡片顯示
0，即使老闆儀表板「目前在線」與 Analytics 轉換漏斗都正確顯示 1。

## 3. Before / After Data Flow

見 `R5.3-A7_DATA_SOURCE_MIGRATION.md` 完整版本，摘要：

```
Before: analytics_events → _visitorGeoAttributionCTE() → getGeoFunnel()
        → vm.funnel → computeGeoDashboardKpi(vm) → KPI 卡片
After:  geo_visit_log → getGeoEventFunnel() → geoVisitorState.funnel
        → geoAdaptEventEngineFunnelForKpi() → KPI 卡片（同一個未修改的
        geoBuildKpiSummaryCards()）
```

## 4. 修改檔案

| 檔案 | 修改內容 |
|---|---|
| `public/js/geo-intelligence.js` | 新增 `geoKpiSourceStatus()`／`geoAdaptEventEngineFunnelForKpi()`／`_geoRenderKpiLiveHtml()`／`_geoKpiLoadingCardsHtml()`／`_geoKpiErrorCardsHtml()`／`_geoLegacySubstringKpiHtml()`／`geoIntelligenceOnEventEngineUpdate()`；`refreshGeoDashboardKpiBlock()` 內 KPI 卡片改用新函式；`computeGeoDashboardKpi()`／`geoBuildKpiSummaryCards()`（helper_text 措辭微調，其餘不變）加註 `legacy area aggregation only` |
| `public/js/geo-visitor-layer.js` | `geoVisitorState` 新增 `status` 欄位；`geoVisitorFetchAndRender()` 追加狀態轉換與更新 hook 呼叫；`geoVisitorHandleStoreSwitch()`／`_geoVisitorResetStateForTest()` 重置 `status` |
| `scripts/smoke-hotfix30-b5-r5-2-b1-1-dashboard-rewire.js` | 更新一項過時斷言（原本驗證 vm.funnel 加總值 67 出現在 KPI 卡片，本輪起這是被刻意移除的舊行為，改為驗證 loading 佔位與「不含 67」） |
| `scripts/smoke-hotfix30-b5-r5-3-a7-geo-kpi-single-source.js` | 新增，87 項 assertion |
| `R5.3-A7_DATA_SOURCE_MIGRATION.md`／`R5.3-A7_COMPLETION_REPORT.md`／`R5.3-A7_MANUAL_QA_CHECKLIST.md` | 新增文件 |

未修改：`utils/geoEventEngine.js`、`utils/geoVisitLog.js`、
`utils/geoAnalyticsQueries.js`、`routes/analytics-geo.js`、
`routes/analytics.js`、`public/js/geo-heatmap.js`、
`public/js/geo-intelligence-map.js`。

## 5. KPI Source Migration

`refreshGeoDashboardKpiBlock()` 內：

```js
// Before
const kpiCards = geoRenderKpiSummaryCards(geoBuildKpiSummaryCards(
  { visitors: kpi.visitors, ... }, vm.quality, vm.overview,
)) + `...5-card 相容區塊，同樣讀 kpi.*...`;

// After
const kpiCards = `<div id="${containerId}-geo-kpi-live">${_geoRenderKpiLiveHtml(vm.overview)}</div>`;
```

`kpi`（`computeGeoDashboardKpi(vm)`）本身保留，繼續供 `isEmpty` 判斷／
`geoBuildEmptyStateMessage()` 等行政區聚合用途使用，只是不再驅動 KPI 卡片。

## 6. Adapter 設計

`geoAdaptEventEngineFunnelForKpi()` 是唯一的轉換點：讀
`geoVisitorState.{status,funnel}`，輸出 `geoBuildKpiSummaryCards()` 既有
期望的 `{visitors, add_to_cart_visitors, begin_checkout_visitors,
submitted_order_visitors, conversion_rate}` + `{identified_rate,
unknown_rate}` 形狀。`geoBuildKpiSummaryCards()` 本身完全未修改（純函式，
既有 unit test 原樣重跑全數通過）。8-card 主卡片與 5-card 舊子字串相容
區塊（`_geoLegacySubstringKpiHtml()`）共用同一次 adapter 呼叫結果，不各自
轉換，避免同畫面出現兩個不同數字。

## 7. Loading／Ready／Error／Empty

`geoKpiSourceStatus()` 四態：

- **idle/loading**：`geoVisitorState.status !== 'ready'/'error'` 或
  `funnel` 尚未載入 → 卡片顯示「載入中…」文字，不顯示 0。
- **ready**：`status==='ready' && funnel.visitors>0` → 顯示真實數字。
- **empty**：`status==='ready' && funnel.visitors===0` → 顯示真實的
  0（跟 loading 用不同文字區分，需求文件五）。
- **error**：`status==='error'`（無論 `funnel` 是否有殘留舊資料）→ 顯示
  「無法載入」，絕不 fallback 顯示殘留數字或舊 `getGeoFunnel()` 數字。

`geoVisitorFetchAndRender()` 每次成功／失敗都呼叫
`geoIntelligenceOnEventEngineUpdate()`，只更新 `#{containerId}-geo-kpi-live`
子樹，不需要整頁重繪，也不需要手動切 Heatmap Tab。

## 8. Unknown 規則

沿用 A1.2／A5 既有的 `getGeoEventFunnel()` 定義（本輪未修改）：Unknown
訪客一律計入 `visitors` 總數，`unknown_visitors = visitors -
known_district_visitors`，`coverage = known/visitors`；`visitors=0` 時
`unknown_rate` 明確為 0（不是 100%）。地圖端 Unknown 不畫假 Marker（沿用
`geo-visitor-layer.js` 既有規則，未修改）。

## 9. Legacy `getGeoFunnel()` 保留範圍

行政區 Top 3／排行榜／Choropleth／Decision Center／Recommendation
Risk／`GET /api/analytics/geo/funnel` 路由本身。詳見
`R5.3-A7_DATA_SOURCE_MIGRATION.md` 第 4 節對照表。原始碼中對應位置皆有
`legacy area aggregation only` 註解。

## 10. Store Isolation

`geoVisitorHandleStoreSwitch()` 切店時同步把 `geoVisitorState.status`
重置為 `'idle'`（新增，原本只清 `summary/funnel/areas/recent`），避免
新店資料尚未抓到的空檔誤顯示上一店的 ready/error 狀態。

## 11. Date／Channel 同步

`refreshGeoDashboardKpiBlock()`（日期／渠道／縣市／店家切換與手動重新整理
的既有共同入口）本來就會在每次呼叫時重跑 `geoHeatUiRegisterContext()` →
`geoVisitorFetchAndRender()`（既有既有呼叫鏈，本輪未改動這段觸發邏輯本
身），因此上述操作都會觸發一次新的 `/visitor-log` 請求。已知限制見第 16
節。

## 12. A7 Smoke

`scripts/smoke-hotfix30-b5-r5-3-a7-geo-kpi-single-source.js`：**87/87
PASS**，涵蓋 Adapter 四態、情境 A～E、Store Isolation、Metric 切換不需
重新 fetch、Static Audit（原始碼層級）、Regression Guard（舊
`getGeoFunnel().areas=[]` 時新引擎仍正確顯示非 0 數字）。

## 13. 完整 Regression

全部 B5 系列 smoke test 重跑，**exit code 0 / FAIL 0**：

| 套件 | 結果 |
|---|---|
| R5.1-A Geo Foundation | 76/76 |
| R5.1-B Geo API | 111/111 |
| R5.1-C Geo UI | 196/196 |
| R5.1-D1 Visitor Geo | 164/164 |
| R5.2-A Business Area Reserved | 62/62 |
| R5.2-A Integrated E2E | 232/232 |
| R5.2-A Order Geo Write | 90/90 |
| R5.2-A Stage 7 | 140/140 |
| R5.2-B1-1 Dashboard Rewire | 101/101（含本輪更新的 1 項斷言） |
| R5.2-B1-2 Area Ranking | 150/150 |
| R5.2-B1-3 Geo Behavior Funnel Backend | 76/76 |
| R5.2-B1-4-5 Geo Explainability | 819/819 |
| R5.2-B1-4-6 API Stabilization | 867/867 |
| R5.2-B1-4 Geo Rule Engine | 132/132 |
| R5.2-B1-5 Geo Dashboard UI | 313/313 |
| R5.2-B1-6 Geo Explorer | 376/376 |
| R5.2-B2 Geo Map | 620/620 |
| R5.2-B3 Geo Settings UI | 157/157 |
| R5.3-A1.1 Heatmap Dashboard Integration | 112/112 |
| R5.3-A1.2 Visitor Geo Sync | 188/188 |
| R5.3-A1 Geo Heatmap | 128/128 |
| R5.3-A2 Geo Event Engine | 228/228 |
| R5.3-A4 Metric Switcher Consolidation | 110/110 |
| R5.3-A5 Visitors Classification Fix | 20/20 |
| R5.3-A7 Geo KPI Single Source | 87/87 |
| R5 Cart/Order Hours | 55 PASS / 0 FAIL / 4 MANUAL（既有，非本輪新增） |
| R5 Dashboard UI | 20/20 |
| R5 Debounce | 32/32 |

`node --check` 全專案 `.js` 檔案通過（含本輪修改的三個檔案）。

## 14. Static Audit

逐項確認結果（詳細指令與輸出見本輪對話紀錄）：

1. ✅ Geo KPI 正式資料來源只有 `geoVisitorState.funnel`（唯一入口
   `geoAdaptEventEngineFunnelForKpi()`）
2. ✅ `_geoRenderKpiLiveHtml()`／`geoAdaptEventEngineFunnelForKpi()` 原始碼
   內不含 `vm.funnel`
3. ✅ adapter 內不呼叫 `computeGeoDashboardKpi()`，無雙來源 fallback
4. ✅ error 狀態（`_geoKpiErrorCardsHtml()`）固定顯示「無法載入」文字，
   不讀取任何 `funnel`/`vm` 數值
5. ✅ `getGeoFunnel()` 僅保留：行政區 area aggregation／舊排行榜／舊
   choropleth／Decision Center／Recommendation Risk（見 Migration 文件表格）
6. ✅ 原始碼含 `legacy area aggregation only` 註解（`computeGeoDashboardKpi()`
   與呼叫點附近）
7. ✅ 路由數量未增加（`routes/analytics-geo.js` 仍是 13 個 `router.get`，
   未新增）
8. ✅ 未新增第二套 state（`geoVisitorState` 是既有 A1.2 狀態物件，本輪只
   加一個 `status` 欄位）
9. ✅ `public/js/geo-heatmap.js`／Order Heatmap Engine 完全未修改
10. ✅ 座標來源規則（`geo-visitor-layer.js` 的 Unknown 不給假座標）未修改
11. ✅ Unknown 不畫假 Marker（沿用既有規則，未改動）
12. ✅ 本輪新增程式碼無 `console.log`／`console.debug`
13. ✅ 本輪新增程式碼無 `Math.random()`
14. ✅ 本輪新增程式碼無硬編碼 `store_001`
15. ✅ 本輪新增程式碼無桃園／中壢作為 production 邏輯來源
16. ✅ 本輪新增的 production 程式碼（`geo-intelligence.js`／
    `geo-visitor-layer.js`）無 fixture 依賴（fixture 僅存在於
    `scripts/` 測試檔案）
17. ✅ 打包前已移除 `data/pos.db`
18. ✅ 無其他測試暫存 DB 殘留
19. ✅ `scripts/diagnose-a6-live.js`（A6 一次性診斷腳本）已移除
20. ✅ 打包排除 `node_modules`／`.git`
21. ✅ Store Isolation 保留且擴充（`status` 欄位也隨切店重置）
22. ✅ Date／Channel 狀態沿用既有 `dashboardDateState`／`av2Channel`
    來源，未新建第二套篩選狀態
23. ✅ Request Guard／Abort／Stale Response Guard（`requestSeq`／
    `AbortController`）完整保留
24. ✅ Loading／Ready／Error／Empty 四態在 `geoKpiSourceStatus()` 明確分離
25. ✅ 頁面初次 mount（`status='idle'`，`funnel=null`）顯示「載入中…」，
    不顯示錯誤的 0

## 15. 已修正的 Regression Bug

實作過程中，`smoke-hotfix30-b5-r5-3-a1-2-visitor-geo-sync.js` 的
`B8-1`（API 失敗時 `geoVisitorState.summary`/`areas` 必須安全降級為
`null`/`[]`）一度失敗——因為第一版實作在請求失敗時只設定新的
`status='error'`，卻不再清空 `summary/funnel/areas/recent`（跟舊行為不
一致）。已修正為：失敗時同時清空這些既有欄位（維持 A1.2 既有行為）與新
`status='error'` 欄位並存，兩者互不衝突（KPI adapter 優先檢查
`status`，不受 `funnel` 是否為 `null` 影響判斷結果）。修正後
A1.2 恢復 188/188。

## 16. Known Limitations

- `geoVisitorFetchAndRender()` 目前只帶 `range`（5m/30m/1h/24h/today/7d/
  30d/custom）查詢 `geo_visit_log`，**不支援** Dashboard 的
  `channel`／精確 `date_from`～`date_to` 上限篩選（`geo_visit_log` 本身
  未儲存 `order_channel`，`resolveTimeRangeSince()` 也只有下限，沒有上
  限）。日期／渠道切換會觸發重新 fetch（同步更新的「觸發」條件已滿足），
  但實際查詢範圍目前仍是粗顆粒的 preset range，不完全精確反映使用者選的
  自訂區間或渠道。這是既有架構的限制，非本輪引入，本輪未擴大也未修正
  （避免變成新增第二套篩選邏輯或修改既有 API 契約）。
- Metric 切換（`geoVisitorSetMetric()`）不觸發重新 fetch（沿用同一份
  已載入的 `funnel`，只是換顯示欄位）——這是正確且刻意的行為，不是限制。

## 17. Manual Visual 狀態

```
QA BUILD
Automated Verification：PASS
Manual Visual：REQUIRED
Final Completion：NOT YET APPROVED
```

真人瀏覽器驗證尚未執行，見 `R5.3-A7_MANUAL_QA_CHECKLIST.md`。

## 18. 是否可以部署

**不建議在 Manual Visual 執行並簽核前部署到正式環境。** 自動化驗證
（Smoke + Regression + Static Audit）全數通過，但需求文件十五要求的真人
瀏覽器驗證尚未執行；本報告不宣告 Final Release。
