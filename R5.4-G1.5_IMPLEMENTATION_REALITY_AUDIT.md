# R5.4-G1.5 Implementation Reality Audit

fix18-10-hotfix30-B5-R5.4-G1.5 — GA4 Realtime Visitor Geo Layer

**狀態：Draft Reality Audit（誠實記錄，非 QA PASS 宣告）。**
本文件只回答「目前程式碼實際做了什麼」，不代表已完成 §三～§二十七 的任何一項。

---

## 1. client.js 真正使用哪個 SDK method？

`utils/ga4Realtime/client.js:95` 呼叫 `client.runRealtimeReport({...})`。
**未使用** `runReport()`。符合需求文件六「不得用 runReport() 代替 Realtime」。

## 2. 是否呼叫 runRealtimeReport？

是（見上）。**但目前只呼叫一次、每次都直接打 API，沒有 cache/single-flight 保護**（那層邏輯在
`index.js`，見第 9-10 項）。

## 3. property 格式是否為 `properties/{id}`？

是：`property: \`properties/${propertyId}\`` — 正確格式。

## 4. 是否真的有 streamId filter？

**沒有。** 目前 request 只有 `dimensions: [city, eventName]` + `metrics: [activeUsers]`，
沒有 `dimensionFilter`、沒有 streamId 概念。這是**多店隔離的關顔缺口**——目前架構完全沒有
store/stream 概念，見第 9 項與 §三。

## 5. 是否有 eventName filter？

**沒有。** `eventName` 目前只是「拿來當一個 row 維度回傳」，不是 filter。沒有 Metric Mapping
（visitors/view_item/add_to_cart/checkout/purchase）任何一種篩選邏輯。

## 6. 是否有 minuteRanges？

**沒有。** 目前 request 完全沒有 `minuteRanges` 欄位，等同讓 API 使用預設視窗（GA4 Realtime
Report 預設沒有 minuteRanges 時是「過去 30 分鐘」，但這是**隱含行為，不是明確指定**）。沒有
5 分鐘／30 分鐘切換的概念。

## 7. 是否有 returnPropertyQuota？

**沒有。** Request 沒有這個欄位，回應也沒有解析 `propertyQuota`。沒有 quota_status。

## 8. activeUsers 是否被錯誤跨列相加？

**是，目前有這個問題。** `index.js` 的 `_aggregateRows()` 對每一列 `r.active_users` 直接
`totalActiveUsers += users`（見 `total_active_users_ga4` 計算），這正是需求文件八明確禁止的
「跨 city row 直接加總 activeUsers」，因為同一使用者可能因為多個 eventName row 被重複計入。
**這是目前草稿中最嚴重的正確性問題，必須用 Summary Request（方案 B）重做，不能維持現狀。**

## 9. cache 是否有 Store Isolation？

**沒有。** `index.js` 的 `_cache` 是單一模組層變數（`let _cache = null`），完全沒有
storeId／propertyId／streamId 進 cache key。目前架構隱含假設「全店家共用同一個 GA4
Property」，這正是需求文件明確要求修正的多店缺陷（見 §三）。

## 10. 是否有 single-flight？

**沒有。** 沒有任何「同一 cache key 的併發 request 共用同一個 in-flight Promise」機制。
併發呼叫會各自直接打 GA4 API。

## 11. Route 是否真的 mount？

**是。** `server.js` 既有 `app.use('/api/geo-live', requireStore, require('./routes/geo-live'))`，
`/ga4-realtime`、`/ga4-realtime-status` 是加在同一個 router 裡，因此掛載路徑正確可達。
但目前 route handler（`routes/geo-live.js:239-252`）**沒有讀取 `req.storeId` 傳給
`getGa4RealtimeCities()`**——呼叫時完全沒有帶入 store 概念，這是 §三 要修的另一半。

## 12. 前端 JS 是否真的被 HTML 載入？

**否。** 實測結果：`grep -rl "geo-ga4-realtime-layer.js" public/*.html` **沒有任何檔案**。
`public/js/geo-ga4-realtime-layer.js` 存在，但目前沒有任何頁面 `<script src=...>` 引用它。
**這代表目前前端程式碼完全不會在瀏覽器中執行，是純草稿。**

## 13. GA4 Layer 是否真的有 UI 入口？

**部分。** `geo-heatmap-ui.js` 的 `geoHeatUiLayerToggleHtml()` 已經多了第三個按鈕
（`data-layer="ga4"`），`_geoHeatUiApplyLayerExclusivity()` 與 `geoHeatUiSetLayer()` 也已經加了
`'ga4'` 分支。但因為第 12 項（腳本未載入），實際頁面上按下去會因為
`geoGa4FetchAndRender`/`geoGa4State` 未定義而被 `typeof ... === 'function'` 守衛跳過，
UI 按鈕會出現，但沒有任何資料或圖層反應。

## 14. 5／30 分鐘按鈕是否存在？

**不存在。** 沒有對應的 DOM/HTML/事件處理常式。這對應第 6 項（minuteRanges 完全未實作）。

## 15. Metric 切換是否存在？

**不存在。** 沒有 visitors/view_item/add_to_cart/checkout/purchase 的切換 UI 或 state。

## 16. status endpoint 是否可用？

**路由可達，但欄位不完整。** `GET /ga4-realtime-status` 存在且回傳
`{enabled, configured, status, last_success_at, last_error_code, success_count, failure_count}`，
但缺少需求文件十六要求的 `credential_available`／`property_configured`／
`stream_configured`／`single_store_mode`／`sdk_available`／`cache_entries`／
`auto_refresh_seconds`。也沒有 `live_check=1` 權限保護分支（因為目前 status 本來就不呼叫
Google API，這點無害地符合「不得消耗 quota」的要求，但只是巧合，不是刻意設計）。

## 17. 縣市 mapping 是否可能誤猜？

**是，目前有風險。** `_aggregateRows()` 直接呼叫既有 `normalizeCounty(r.city)`，這支函式是
R5.1-D 時代為「IP Geo Provider 回傳的 region/city 字串」設計的別名比對表，**沒有針對 GA4
Realtime 特有的英文地名（Taoyuan/New Taipei/Hsinchu City/Hsinchu County/Chiayi City/Chiayi
County 等）驗證過**。尤其 Hsinchu／Chiayi 市縣同名歧義，目前完全沒有 ambiguous 分支，也沒有
`cityId`／`countryId` 輔助判斷（GA4 Realtime 回應目前完全沒有 request 這兩個 dimension，見
第 4-6 項）。**必須視為未驗證，不能假設既有 normalizeCounty() 對 GA4 英文地名可靠。**

## 18. Hsinchu／Taoyuan／New Taipei 等 alias 是否明確？

**未驗證。** 尚未針對這組 alias 寫任何測試（無論單元測試或人工核對 `taiwanGeoNormalize.js`
內建別名表是否涵蓋這些確切字串）。這是 §十九 Smoke Test 第 36-47 項要補的內容，目前完全空白。

## 19. 是否有任何 credential 出現在 public/？

**否，目前乾淨。** `grep -rn "GA4_PROPERTY_ID|GA4_SERVICE_ACCOUNT|GOOGLE_APPLICATION_CREDENTIALS|private_key" public/`
無結果。`public/js/geo-ga4-realtime-layer.js` 只呼叫 `fetch('/api/geo-live/ga4-realtime')`，
沒有任何 Google 端點或憑證字串。

## 20. 是否有第二張 Map／Tile Layer？

**否。** `geo-ga4-realtime-layer.js` 沒有 `new L.Map`／`L.map(`／`L.tileLayer(`／
`new L.TileLayer`（只有註解提及「不得建立」）。程式碼只用
`window.geoMapState.instance` 與 `L.layerGroup()`/`L.geoJSON()`，符合重用既有地圖的要求。

## 21. 哪些項目只是草稿、尚未接線？

- 前端腳本未被任何 HTML 載入（第 12 項）
- 5／30 分鐘、Metric 切換、Auto Refresh、手動 Refresh UI 完全不存在
- Settings UI（Property/Stream/Cache 秒數欄位）完全不存在
- Store Isolation（cache key、route 讀取 req.storeId 並傳入 config）完全不存在
- Single-flight／Retry／Stale Cache Fallback／Quota 完全不存在
- Summary Request（方案 B，避免 activeUsers 跨列相加）完全不存在
- Base64 憑證支援、credential_invalid 分類完全不存在
- 5 份文件（本檔除外）、Smoke（180+ 斷言）、Static Audit（65+ checks）皆未建立

## 22. 哪些項目已能實際執行？

- `client.js` 的 `runRealtimeCityReport()`：在**沒有真實 SDK/憑證**的情況下，
  `isSdkAvailable()`/`_loadSdk()` 會安全回傳 `false`，`getGa4RealtimeCities()` 會 fail-open
  回傳 `status:'not_configured'` 的空 payload——這條 fail-safe 路徑目前是可執行、可驗證的
  （已用 `node -e` 手動確認，見下方）。
- Feature flag 預設值：`GA4_REALTIME_ENABLED` 未設定時 `getGeoFeatureFlags().GA4_REALTIME_ENABLED === false`，可執行、已驗證。
- Route 掛載路徑可達（`/api/geo-live/ga4-realtime`、`/ga4-realtime-status`），但因為
  `requireFeature('reports')` + `requireStore` 中介層需要真實 store/session context，
  本輪未起 server 做 HTTP 層驗證，只做了 static require 檢查。

### 手動驗證紀錄（fail-safe 路徑）

```
$ node -e "
const ga4 = require('./utils/ga4Realtime');
ga4.getGa4RealtimeCities().then(r => console.log(JSON.stringify(r)));
"
```
（見下方指令輸出）confirms `enabled:false, status:'disabled'` when env unset.

---

## 結論

目前草稿是「資料流骨架＋正確的高層設計原則（不假造座標、不建第二張圖、憑證只在後端）」，
但**尚未符合**需求文件的多店隔離、Realtime 官方欄位完整度、activeUsers 去重、cache/
single-flight/retry/quota、前端真正接線、Settings UI、測試覆蓋（Smoke/Static Audit）。

**不宣稱 QA PASS。** 下一步照 §三 起繼續實作，優先順序建議：
1. §八 activeUsers 去重（Summary Request）——現有邏輯是正確性錯誤，優先修正
2. §三 Store/Property/Stream 隔離——目前完全沒有
3. §六 Realtime 官方欄位（minuteRanges/streamId filter/eventName filter/returnPropertyQuota）
4. §十一 前端真正接線（script 標籤、UI 控制項）
5. §十三 Cache/Single-flight/Retry/Stale
6. 其餘章節（Settings UI、文件、Smoke、Static Audit、Regression、打包）

---

## G1.5-A Addendum

fix18-10-hotfix30-B5-R5.4-G1.5-A — GA4 Realtime Backend Correctness & Store Isolation

**狀態：本階段 Smoke/Static Audit/Regression 皆已通過，前端仍未接線（見第 18-20 項）。**

### 1. 原 activeUsers direct-sum Bug 已如何修正

G1.5 草稿版本的 `utils/ga4Realtime/index.js` 對每一列 city row 直接
`totalActiveUsers += r.active_users`，這會因為同一使用者可能對應多個
city/eventName row 而重複計數。G1.5-A 改為：每次 fetch 發出兩個獨立 Google
Realtime Report request（Summary + City），`summary.total_active_users_ga4`
只從無 dimensions 的 Summary Request 讀取，City Request 的結果只用來聚合
`counties[].active_users`（縣市自己的加總），兩者永遠分開回傳，程式碼裡沒有
任何地方把 counties 加總寫進 total_active_users_ga4（見
`utils/ga4Realtime/index.js` 的 `_fetchAndBuildPayload()`，並有對應 Smoke
斷言 C-agg / J1 驗證）。

### 2. Summary Request 與 City Request 的最終結構

- Summary：`dimensions:[]`，`metrics:[activeUsers, eventCount, (visitors 時 +screenPageViews)]`。
- City：`dimensions:[city, cityId, country, countryId]`，`metrics:[activeUsers, eventCount]`。
- 兩者共用同一組 `minuteRanges`（window=5→startMinutesAgo:4/endMinutesAgo:0；
  window=30→startMinutesAgo:29/endMinutesAgo:0）與同一組
  `dimensionFilter`（streamId/eventName，見 `buildGa4DimensionFilter()`），
  且都帶 `returnPropertyQuota:true`。
- 見 `utils/ga4Realtime/requestBuilder.js`。

### 3. Store Settings 真實資料表與讀取方式

既有 `settings(store_id, key, value)` 表（`utils/db.js` 既有 schema，多個
既有 route 如 `routes/delivery.js`、`routes/line-integration.js` 都用同一張
表存任意 key）。`utils/ga4RealtimeConfig.js` 的 `getGa4RealtimeConfig(db,
storeId)` 用 `SELECT key, value FROM settings WHERE store_id=? AND key IN
(...)` 讀取 6 個 `ga4_realtime_*` key。

### 4. 為何不需 DB Migration

`settings` 表本身就是任意 key-value 儲存，新增設定只是「多寫幾個
key」，不需要新表或新欄位。已用 static audit 第 33 項確認
`ga4RealtimeConfig.js`／`ga4Realtime/index.js` 內沒有任何
`CREATE TABLE`／`ALTER TABLE`。

### 5. per-store property／stream 來源

一律從 `req.storeId` 對應的 settings row 讀取
`ga4_realtime_property_id`／`ga4_realtime_stream_id`。SQL 本身帶
`WHERE store_id=?`，物理上不會讀到別的店。Smoke A1-A4 驗證了 Store A/B
的 config 互相隔離（不同 property/stream，且不相等）。

### 6. single-store fallback 的雙重 opt-in

只有「部署層級 `GA4_REALTIME_SINGLE_STORE_MODE=true`」**且**「該店自己的
`ga4_realtime_single_property_mode=true`」同時成立，才會使用環境變數
`GA4_PROPERTY_ID`／`GA4_STREAM_ID` 作為 fallback（見 Smoke A7/A8：只有部署
層級允許、店家沒選擇時，仍然 `configured:false`）。

### 7. Cache Key 完整組成

`getGa4RealtimeCacheKey({storeId, propertyId, streamId, windowMinutes,
metric})` → `${storeId}::${propertyId}::${streamId||'nostream'}::
${windowMinutes}::${metric}`。Smoke E8-E10／J5 驗證不同 store/metric/window
產生不同 key。

### 8. Single-flight 真實行為

`_inFlight` 是模組層 `Map<cacheKey, Promise>`。同一 cache key 併發呼叫共用
同一個 Promise；`.finally()` 保證無論成功/失敗都會刪除 entry。實測：10 個
並行相同 key 請求只產生 2 次 Google API 呼叫（Smoke F1）；不同 store／不同
metric 各自獨立 in-flight（Smoke F3/F4）；一次 reject 後，下一次呼叫可以
正常重新發起（Smoke F5/F6）。

### 9. Retry／Timeout 規則

可重試代碼：429/500/502/503/504/TIMEOUT/ETIMEDOUT/ECONNRESET；不可重試：
400/401/403/404/INVALID_CREDENTIALS/INVALID_PROPERTY/INVALID_STREAM。最多
重試 2 次，退避約 250ms→750ms（測試用 injectable sleep，不真的等待）。逾時
預設 10000ms，可用 `GA4_REALTIME_TIMEOUT_MS` 調整。

### 10. Stale Cache 限制

只有在（a）沒有 fresh cache、（b）有 expired cache、（c）重新抓取失敗時才
會回退成 stale：`is_stale:true`／`status:'stale_cache'`，且保留原本
`fetched_at`（不假裝剛抓到）。401/403/設定錯誤本身會讓 config 直接
`configured:false`，不會走到「有 stale cache 可用但設定錯誤」的模糊狀態。

### 11. Quota 正規化

`normalizeGa4QuotaStatus(a,b)` 取兩個 Request（Summary/City）中較嚴重的
quota 狀態，只回傳 `normal/near_limit/limited/unknown` 四種，`client.js` 的
`_parseQuota()` 不把完整 `propertyQuota` 物件往外傳。

### 12. Route query allowlist

`GET /ga4-realtime` 只接受 `window`（5|30）／`metric`（visitors/view_item/
add_to_cart/checkout/purchase）／`refresh`（0|1），不合法值安全 fallback為
預設值，完全不讀取 `property_id`／`stream_id`／`credentials`（無論放在
query 或 body）。

### 13. Status endpoint 不呼叫 Google API

`GET /ga4-realtime-status` 只讀 config／credential 狀態摘要，不觸發任何
`runGa4RealtimeReport`。`live_check=1` 權限分支本階段刻意不實作（見需求
文件十八：現有 middleware 若無法可靠區分管理者，就不做，留給 G1.5-B／
Settings 階段）。

### 14. package dependency 與 lock 更新

`package.json` 新增 `"@google-analytics/data": "^6.1.0"`，`package-lock.json`
已同步（resolved version `6.1.0`），未手動修改版本號使兩者不一致。

### 15. G1.5-A Smoke 真實結果

`scripts/smoke-hotfix30-b5-r5-4-g1-5-a-ga4-backend-correctness.js` →
**140 PASS / 0 FAIL / 140 TOTAL**，三輪一致（見
`R5.4-G1.5-A_TEST_REPORT.md`）。

### 16. G1.5-A Static Audit 真實結果

`scripts/static-audit-g1-5-a.js` → **77 / 77 OK**，三輪一致。

### 17. 正式三輪 Regression 結果

三輪完全一致（詳細表格見 `R5.4-G1.5-A_TEST_REPORT.md` §9）：G1.5-A Smoke
140/140、G1.5-A Static Audit 77/77、G1.4.1 Smoke 149/149、G1.4 Smoke
148/148、G1.3.2 Smoke 148/148、A2 Smoke 229/229、A1.2 Smoke 189/189、
G1(geo-live) Smoke 212/212、Static Audit G1.4.1 56/56、Static Audit G1.4
52/52、Static Audit G1.3.2 48/48。全部 exit=0，FAIL=0。

### 18. 尚未接線的前端草稿

`public/js/geo-ga4-realtime-layer.js`（G1.5 草稿）仍呼叫舊版
`fetch('/api/geo-live/ga4-realtime')` 並假設回應是 `{by_county:[...],
total_active_users_ga4}` 的舊 shape。本階段（G1.5-A）**沒有修改這個檔案**
（按規格禁止），它與新 backend contract 已經不相容。

### 19. 目前前端 contract 已落後新 backend response

完整欄位對照差異已寫入 `R5.4-G1.5-A_API_CONTRACT.md`（新增章節「前端草稿
差異」），包含新舊 endpoint 用法、`summary`/`counties` 新 shape、
`window`/`metric`/`refresh` 查詢參數等。

### 20. G1.5-B 開始前必須更新的項目

1. 依 `R5.4-G1.5-A_API_CONTRACT.md` 重寫 `geo-ga4-realtime-layer.js` 的
   fetch／render 邏輯（新 summary/counties shape、window/metric 查詢參數）。
2. 補上 5/30 分鐘與 metric 切換 UI（目前完全不存在）。
3. 補上 Settings UI（Property/Stream/Cache 秒數/Auto Refresh）。
4. 補上 Choropleth 視覺樣式接線（cyan 虛線、既有 GeoJSON reuse）。
5. 執行 Manual Visual（需要真實瀏覽器）與 Real GA4 Live Test（需要真實
   憑證），本階段兩者都還是 NOT TESTED。

