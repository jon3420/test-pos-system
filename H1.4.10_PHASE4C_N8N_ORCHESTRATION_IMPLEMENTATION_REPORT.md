# H1.4.10 Phase 4C — RECOVERY N8N ORCHESTRATION Implementation Report

**狀態：CODE COMPLETE ｜ AUTOMATED ACCEPTANCE PASS（152/152）｜ MANUAL N8N QA PENDING**

## Shared Secret Security（本輪 Closeout，SSOT）

- **`utils/cartRecoveryOrchestration.js`**：新增 `validateSharedSecret(value)`，唯一格式判斷來源。正式格式固定為 `crypto.randomBytes(32).toString('base64url')`（43 字元、`^[A-Za-z0-9_-]{43}$`、decode 後精確 32 bytes）。額外三層 defense-in-depth heuristic 擋下明顯弱值：單一字元重複（`repeated_single_char`）、雙字元循環（`repeated_two_char_cycle`）、unique 字元數低於門檻 12（`low_entropy`，同時涵蓋更長週期的簡單循環樣式）——這些是啟發式規則，不是完整的 entropy estimation。
- **`routes/settings.js`**：非空的 `cart_recovery_n8n_secret` 一律呼叫 `validateSharedSecret()`，不合格回 400，不自己重寫長度判斷；空字串代表保留原值。`redactSensitiveSettings()` 對此欄位同樣遮蔽，`GET` 只回 `cart_recovery_n8n_secret_set` 布林值。
- **`routes/cart-recovery-orchestration.js` `/secret/rotate`**：`crypto.randomBytes(32)` 產生後，自我呼叫同一個 `validateSharedSecret()` 二次確認（不因為是伺服器自己產生的就略過驗證），確保手動輸入路徑與自動產生路徑共用同一套判斷標準。

## 新增資料表 / Migration（`utils/db.js`，全部 additive）

- **`cart_recovery_orchestration_requests`**：`id`/`store_id`/`request_id`/`direction`（`inbound_due`/`outbound_wakeup`）/`status`/`http_status`/`error_code`/`requested_at`/`processed_at`/`created_at`，`UNIQUE(store_id, request_id, direction)`。後續 additive 補上 `attempt_count`/`updated_at`（本輪 outbound retry 需要）。

## 新增 Settings（獨立於既有 `n8n_webhook_url`）

| Key | Default |
|---|---|
| `cart_recovery_n8n_enabled` | `'0'` |
| `cart_recovery_n8n_webhook_url` | `''` |
| `cart_recovery_n8n_secret` | `''` |

`routes/settings.js` 額外加上：webhook URL 非空時做 SSRF-safe hostname 檢查（400 拒絕）；secret 非空時要求至少 32 字元（400 拒絕弱 secret）；secret 空字串代表「保留原值」而非清空；`redactSensitiveSettings()` 對 `cart_recovery_n8n_secret` 做同款遮蔽（`cart_recovery_n8n_secret_set` 布林值，GET 永不回明文）。

## Backend 模組

- **`utils/cartRecoveryOrchestration.js`**（新增）：
  - HMAC：`buildCanonicalString`/`sign`/`verifySignature`（`crypto.timingSafeEqual`）/`isTimestampFresh`（±5 分鐘）。
  - SSRF：`isSafeWebhookUrlByHostname`（literal IPv4/IPv6 私有網段黑名單，正確處理 IPv6 URL 的中括號）、`resolveSafeWebhookTarget`（DNS 解析後檢查實際 IP，`isPublicIp` 供 IPv4/IPv6 使用）。**已知限制**：check 與真正 `fetch` 之間沒有 IP pinning，DNS rebinding window 仍存在（見 Reality Audit）。
  - Replay：`recordOrchestrationRequest`（inbound，一次性）、`claimOutboundWakeUp`/`releaseOutboundWakeUpAsFailed`（outbound，三態 `sending`/`processed`/`failed`，支援因暫時性失敗而重試）。
  - `toOrchestrationDueAt`：把 DB 的 `due_at`（確認為 UTC，見 Reality Audit）轉成帶 `Z` 的明確 ISO8601，只用在 n8n payload/HMAC canonical，不改 DB schema。
  - `sendWakeUp`：真正的 `async`/`try`/`catch`（取代原本包不住 Promise rejection 的 `_safe(async()=>{})` pattern）；只有 HTTP 2xx 才算成功；4xx/5xx/timeout/network_error 一律 `sent:false`，並把 outbound claim 標記 `failed`（允許同 `due_at` 重試），完全不觸碰 `cart_recovery_jobs.status`；`AbortController` 8 秒 timeout；`redirect:'error'`。

- **`routes/cart-recovery-orchestration.js`**（新增，factory function 匯出）：
  - `POST /process-due`：不經 `requireStore`，改用 store-scoped HMAC（header/body 的 `version`/`timestamp`/`request_id` 必須完全一致，不再 fallback 混用）；驗簽通過後才二次確認 store 存在；只呼叫既有 `processDueLineRecoveryJobs()`，不複製 eligibility/push/token/conversion 任何邏輯；response 只回聚合數字。
  - `POST /secret/rotate`：經 `requireStore`（Admin 動作），`crypto.randomBytes(32)` 產生新 secret，只有這次呼叫回傳明文一次。
  - **本輪修正的真實 bug**：factory function 原本結尾寫 `module.exports = router;`（在已經是 `module.exports = function(...)` 的函式體內再次賦值，等於 no-op），導致呼叫 `require(...)(requireStore)` 永遠拿到 `undefined`，`app.use()` 掛載時直接 crash。已修正為 `return router;`，並用直接的 `node -e` 診斷腳本驗證修好（不是用猜的）。

- **`utils/cartRecoveryDelivery.js`**（既有檔案修正）：
  - `options.limit` 現在先把三個 stage 的 due 候選**全域依 `due_at ASC, id ASC` 重新排序**才 slice（原本先各自排序再串接會破壞跨 stage 順序，可能讓真正最早到期的 job 被排在後面而被 limit 誤切掉）。
  - `has_more` 直接反映「真正 due 候選總數是否超過這次處理筆數」，不再用 `processed >= limit` 事後猜測。

- **`utils/cartRecovery.js`**（既有檔案，additive）：`_fireWakeUp()` 掛在 job 建立與 `due_at` refresh 的兩個路徑（`_scheduleStage`/`refreshStageActivity`），fire-and-forget、`.catch(()=>{})`，完全 fail-open。

## Workflow Template

- **`n8n-cart-recovery-workflow.json`**（本輪重寫）：`Webhook → Verify(HMAC) → [If Valid] → Respond(202, Immediate ACK) → Wait Until due_at → Build Due Request(新 request_id) → Sign → HTTP Request POS process-due → Merge(context+processor_result) → If has_more(<10 loop guard) → loop back`。Secret 透過 `process.env.POS_RECOVERY_SHARED_SECRET`，`POS_BASE_URL` 同樣透過環境變數，皆無 hardcode。不含任何 PII 欄位、不含 LINE Messaging API 節點、不含 DB access 節點。

## Admin UI

沿用 Phase 4B 的「購物車找回」卡片，新增底部「n8n 自動排程」小節：啟用開關、Webhook URL 輸入、Secret 密碼輸入（空白＝保留原值）、狀態顯示（已設定/未設定，布林值）、「重新產生 Secret」按鈕（呼叫 `/secret/rotate`，只有這次呼叫顯示明文供複製）。`public/js/app.js` 的 `loadCartRecoverySettings()`/`saveCartRecoverySettings()` additive 擴充，新增 `rotateCartRecoveryN8nSecret()`。

## New/Modified Test Files

1. `scripts/run-h1-4-10-phase4c-n8n-orchestration-runtime.js`（新增，123 assertions）
2. `n8n-cart-recovery-workflow.json`（重寫）

## Frozen Contracts Preserved

H1.4.9（67/67）、Phase 1（68/68）、Phase 2（83/83）、Phase 3（111/111）、Phase 4A（89/89）、Backend Recovery Targeted（92/92）、Frontend Recovery Targeted（50/50）、Phase 4B Full Suite（168/168）全部 fresh-run 確認未下降。既有 `n8n_webhook_url`／`n8n-workflow.json` 完全未被觸碰（用 sha256 hash 比對驗證，非僅靠欄位值比對）。
