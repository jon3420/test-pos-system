# R5.4-G1.5-B2 Test Report

fix18-10-hotfix30-B5-R5.4-G1.5-B2 — Full Settings QA, Regression, Documentation & Packaging

## Environment

容器內 Linux 沙箱，無真實 Google Cloud 憑證、無出網 allowlist 對
`googleapis.com` 的存取。所有 GA4 SDK 呼叫透過 `client.js` 的
`_setClientForTest()` 注入假的 `runRealtimeReport()`。

## Node Version

`v22.22.2`。

## Dependency Status

`@google-analytics/data@6.1.0` 已安裝，`package.json`（`^6.1.0`）與
`package-lock.json`（resolved `6.1.0`）版本一致，`npm ci` 可正常安裝
（見下方 ZIP 驗證章節）。本輪沒有新增任何 npm 依賴。

## Smoke／Static Audit 數量

| Suite | 結果 |
|---|---|
| B2 Smoke | 181/181 |
| B2 Static Audit | 82/82 |
| B2a Smoke | 106/106 |
| B1 Smoke | 168/168 |
| G1.5-A Smoke | 140/140 |

## Full Regression Table（三輪）

| Suite | Round1 | Round2 | Round3 |
|---|---|---|---|
| B2 Smoke | 181/181 | 181/181 | 181/181 |
| B2 Static Audit | 82/82 | 82/82 | 82/82 |
| B2a Smoke | 106/106 | 106/106 | 106/106 |
| G1.5-A Smoke | 140/140 | 140/140 | 140/140 |
| B1 Smoke | 168/168 | 168/168 | 168/168 |
| G1.4.1 Smoke | 149/149 | 149/149 | 149/149 |
| G1.4 Smoke | 148/148 | 148/148 | 148/148 |
| G1.3.2 Smoke | 148/148 | 148/148 | 148/148 |
| G1.2 Smoke | 83/83 | 83/83 | 83/83 |
| G1(geo-live) Smoke | 212/212 | 212/212 | 212/212 |
| A2 Smoke | 229/229 | 229/229 | 229/229 |
| A1.2 Smoke | 189/189 | 189/189 | 189/189 |
| Geo Map Settings Smoke（既有，`smoke-hotfix30-b5-r5-2-b2-geo-map.js`） | 620/620 | 620/620 | 620/620 |
| Geo Settings UI Smoke（既有，`smoke-hotfix30-b5-r5-2-b3-geo-settings-ui.js`） | 157/157 | 157/157 | 157/157 |
| B1 Static Audit | 71/71 | 71/71 | 71/71 |
| G1.5-A Static Audit | 77/77 | 77/77 | 77/77 |
| Static Audit G1.4.1 | 56/56 | 56/56 | 56/56 |
| Static Audit G1.4 | 52/52 | 52/52 | 52/52 |
| Static Audit G1.3.2 | 48/48 | 48/48 | 48/48 |

三輪數字完全一致，FAIL=0，exit=0。`node --check` 全數通過：
`ga4RealtimeConfig.js`／`ga4Realtime/index.js`／`connectionTest.js`／
`routes/settings.js`／`routes/geo-live.js`／`geo-ga4-settings.js`／
`geo-ga4-realtime-layer.js`／B2 Smoke／B2 Static Audit 本身。

**§十九補充**：搜尋 `scripts/` 後找到另一個與 `routes/settings.js`
直接相關且真實存在的既有 Smoke——`smoke-hotfix30-b5-r5-2-b3-geo-
settings-ui.js`（157/157），已一併納入三輪矩陣（沒有捏造檔名）。

## Transaction Tests（獨立於三輪矩陣之外）

見 `R5.4-G1.5-B2_SETTINGS_ARCHITECTURE.md` 完整記錄。摘要：A（全部成功）
／B（第一筆失敗）／C（中途第三筆失敗）三種情境都用真實 sql.js DB 驗證
過，ROLLBACK 後原設定完整保留，沒有半套寫入。D（COMMIT 本身失敗）誠實
記錄為程式碼路徑可推論、但沒有直接模擬出的情境（sql.js in-memory 環境
很難讓 COMMIT 本身失敗）。

## Rollback Tests

同上，ROLLBACK 呼叫本身也被驗證過會正確執行（不再出現「no transaction
active」）。

## Generation Tests

B2 Smoke D 段（56-70）：invalidate 只影響對應 store 的 cache／
generation、不同 store 互不影響、舊 in-flight fetch 完成後不會因為
generation 不符而寫回 cache、新 fetch 會使用新的 property/stream/cache
key。三輪矩陣中重複驗證，結果一致。

## Connection Tests

B2 Smoke E 段（71-96）：Summary/City Request 正確送出、window=30、
visitors metric、stream filter、connected 有資料/無資料、7 種
error_code 分類、response 不含 raw rows/quota/憑證。

## Rate-limit Tests

B2 Smoke F 段（97-106）：30 秒 rate limit、`retry_after_seconds`
非負、單店 single-flight（併發只 2 次 Google 呼叫）、不同店隔離、
in-flight map 完成後正確清空（無永久鎖）。

## UI Tests

B2a Smoke（106）+ B2 Smoke G/H/I 段（107-155）：Tab/Panel/六欄位/按鈕/
狀態元素、GET/PATCH/POST 呼叫形狀、驗證規則、Save UX（保留輸入/欄位
錯誤/無 alert）、Connection Test UX（cooldown/錯誤映射）、B1 Layer
整合（Auto Refresh 開關/timer 排程/quota 分級）、Lifecycle（
AbortController/requestSeq/loading reset）。

## Security Scan

掃描 `public/`／`routes/`／`utils/ga4Realtime*`／`.env.example` 內
`private_key`／`client_email`／`access_token`／`refresh_token`／
`Service Account` 等字樣的所有命中處，逐一人工分類：全部是（a）UI 文案
明確告知使用者「本頁不顯示也不儲存 private key」、（b）程式註解說明
邊界、（c）`forbiddenKeys` denylist 陣列本身必須列出這些字串才能拒絕
——沒有任何一處是真實憑證值外洩。另外掃到
`public/js/app.js`／`public/js/line-member-gate.js` 含 `access_token`，
人工確認是既有 LINE LIFF OAuth 處理，與 GA4 無關，不是本輪引入也不是
洩漏。

## DB Cleanup／Timer Cleanup

三輪中每輪開始前清除 `data/pos.db`／GA4 cache／in-flight／generation
map／connection-test rate limiter；每輪結束後確認這些全部歸零、沒有
`data/pos.db` 殘留。jsdom 測試每個區塊使用獨立 window，process 結束前
強制 `process.exit()`，避免任何測試用 timer 讓 process 掛住。

## package／lock

`@google-analytics/data` 版本一致（見上）；本輪未新增依賴，
`package.json`/`package-lock.json` 沒有變動需要同步。

## 未執行項目

- **Real GA4 Live Test：NOT TESTED**（無真實憑證/出網環境）。
- **Manual Visual：NOT TESTED**（見
  `R5.4-G1.5-B2_MANUAL_QA_CHECKLIST.md`）。
- Transaction 情境 D（COMMIT 本身失敗）未直接模擬，只有程式碼路徑推論
  （見 Settings Architecture 文件）。
