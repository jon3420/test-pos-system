# H1.4.10 Phase 3 — CHECKOUT-SUBMIT-DIAGNOSTICS + PAYMENT-SUCCESS Test Report

## 四層 Regression Gate（fresh-run）

```
Gate 1 Phase 3： TOTAL=111 PASS=111 FAIL=0
Gate 2 Phase 2： TOTAL=83  PASS=83  FAIL=0
Gate 3 Phase 1： TOTAL=68  PASS=68  FAIL=0
Gate 4 H1.4.9 ： TOTAL=67  PASS=67  FAIL=0
```

## Phase 3 Test Coverage

### checkout_submit_click

- 直接 `submitOrder()`（無 event）→ 0（line-order／line-shipping 皆驗證）
- 傳其他元素的 click event → 0
- 真實 `#subBtn` click → 1
- 真實 `#submitBtn` click → 1
- 單一 fresh env 內連續兩次真實點擊（第一次缺姓名、第二次補姓名但缺電話）→ 總數累積到 2，證明**沒有**永久 cart_id 去重

### checkout_validation_failed

完整 validation matrix 全數 PASS（line-order：`missing_name`／`missing_phone`／`empty_cart`／`missing_fulfillment`／`mode_unavailable`／`product_mode_conflict`／`missing_payment`／`missing_date`／`missing_time`／`missing_address`／`address_not_resolved`／`delivery_fee_pending`／`order_cutoff`／`line_member_required`；line-shipping：`empty_cart`／`minimum_order_not_met`／`missing_name`／`missing_phone`／`missing_address`／`missing_payment`），每個 case 各自 `checkout_submit_click=1` 且 `checkout_validation_failed=1`，`reason_code` 皆正確。

特別驗證：同一次 attempt 同時缺 `name`/`phone`/`payment` 三個欄位 → `checkout_validation_failed` 仍只有 1 筆，`reason_code` 是第一個真正擋下送出的 `missing_name`（不是三筆全送）。

另驗證：`friend_checkout` 模式下即使 Gate 判斷失敗，也**不會**產生 `line_member_required`（非阻塞 Gate 的既有語意保留）。

### Successful Submit

`line-order`（cash）與 `line-shipping`（cash）皆驗證：`checkout_submit_click=1`、`checkout_validation_failed=0`、既有 `payment_started` 契約不變（cash 送出時仍記錄一次）、`successScreen` 正確顯示（既有送單成功流程未被破壞）。

### Privacy

測試資料刻意塞入姓名「王小明」、電話「0912345678」、地址「桃園市測試路123號」、假 LINE UID「U_SECRET_TEST」、假 Token「SECRET_TOKEN」。結果：`checkout_submit_click`／`checkout_validation_failed` 的 metadata 皆不含上述任何字串，且 metadata 的所有 key 都在白名單內（`checkout_stage`/`submit_trigger`/`order_mode`/`item_count`/`cart_value`/`reason_code`/`has_cart`/`payment_method_type`）。

## Client Forgery

真實 HTTP `POST /api/analytics/events`，`event_name: payment_success`：回傳 **HTTP 400/403**，DB `SELECT COUNT(*) WHERE event_name='payment_success'` 前後皆為 **0**。同時確認 `checkout_submit_click`／`checkout_validation_failed` 仍可由 client 正常寫入（HTTP 200，`success:true`）。

## LINE Pay Request

`POST /api/linepay/request` 成功取得 `payment_url`；該筆 order 的 `payment_success` count = **0**。

## LINE Pay Confirm Failure

Stub `returnCode='9999'`：`payment_success` count = **0**，`purchase` count = **0**，`orders.payment_status` 未被標記為 `paid`，HTTP 仍安全導回（302，不 500）。

## LINE Pay Confirm Success

Stub `returnCode='0000'`：`orders.payment_status` 更新為 **paid**，`payment_success` count = **1**，`purchase` count = **1**（既有 authoritative 語意）。

## Authoritative Value

測試建立 DB order（`total=1234`）。確認：送給 LINE Pay 官方 Confirm API 的 request body `amount=1234`（來自 DB，非任何前端/query 傳入值）；`payment_success` 事件的 `metadata.value=1234`；metadata 只含白名單欄位（`order_number`/`payment_provider`/`payment_method`/`currency`/`value`），不含 channel secret／transactionId／customer data。

## Duplicate Confirm

同一 order 對 `/confirm` 呼叫兩次（皆回 `0000`）：`payment_success` count 仍 **1**，`purchase` count 仍 **1**（既有 dedupe 機制持續生效，未重複累加）。

## GA4 / Meta

三個新事件（`checkout_submit_click`/`checkout_validation_failed`/`payment_success`）皆**無** GA4 standard mapping、**無** Meta standard mapping（靜態掃描 `GA4_EVENT_MAP`/`META_EVENT_MAP` 確認）。既有 `checkout_click → begin_checkout`／`checkout_click → InitiateCheckout` 映射契約字元未變。

## Existing Regression Sweep（本輪實際執行）

| Script | 結果 |
|---|---|
| `run-h1-4-7-analytics-contract-runtime.js` | 61/61 |
| `run-g1-6-ga4-h1-4-7-two-stage-checkout-runtime.js` | 113/113 |
| `run-regression-g1-6-ga4-h1-4-7.js` | B2.1=85/85, B2.2=95/95, B2.3=75/75（全部子套件 PASS） |
| `smoke-hotfix29-c.js` | 54/54，MANUAL=2（既有，非本輪新增） |
| `smoke-hotfix30-b5-r5-2-a-order-geo-write.js` | 90/90 |
| `smoke-hotfix30-b5-r5-debounce.js` | 32/32 |
| `smoke-hotfix30-c1-rollback.js` | 22/22（純文字輸出格式，非本工具標準格式，人工確認「總計：22 通過，0 失敗」） |

## Pre-existing Failure（誠實隔離，非本輪 regression）

`smoke-hotfix30-b5-r5-2-a-integrated.js` 的 **M3**（`/cart-attribution`）：

```
[GeoAnalytics] query failed: Cannot read properties of undefined (reading 'has')
FATAL: Cannot read properties of undefined (reading 'district_ranking')
    at main (.../smoke-hotfix30-b5-r5-2-a-integrated.js:516:44)
```

已用**未修改的原始 zip 重新展開後獨立執行**二次確認：相同 FAIL、相同行號（`utils/cartSnapshot.js`／`utils/cartGeoAttribution.js` 既有 bug，與本輪 checkout submit / payment success 程式碼路徑完全無關）。**不算 Phase 3 regression。**
