# H1.4.10 Phase 4D — RECOVERY ANALYTICS DASHBOARD Test Report

**狀態：WIP ANALYTICS DASHBOARD ｜ AUTOMATED CORE ACCEPTANCE PASS（59/59）｜ MANUAL QA PENDING**

## 十層 Fresh Regression Gate（fresh-run）

```
Gate 1  Phase 4D                   : TOTAL=59  PASS=59  FAIL=0
Gate 2  Phase 4C                   : TOTAL=152 PASS=152 FAIL=0
Gate 3  Phase 4B Full Suite        : TOTAL=168 PASS=168 FAIL=0
Gate 4  Backend Recovery Targeted  : TOTAL=92  PASS=92  FAIL=0
Gate 5  Frontend Recovery Targeted : TOTAL=50  PASS=50  FAIL=0
Gate 6  Phase 3                    : TOTAL=111 PASS=111 FAIL=0
Gate 7  Phase 4A                   : TOTAL=89  PASS=89  FAIL=0
Gate 8  Phase 2                    : TOTAL=83  PASS=83  FAIL=0
Gate 9  Phase 1                    : TOTAL=68  PASS=68  FAIL=0
Gate 10 H1.4.9                     : TOTAL=67  PASS=67  FAIL=0
```

## Phase 4D Test Coverage（59 assertions，current；historical checkpoint 演進：20 → 40 → 59）

### 原始核心切面（D1-D19，historical checkpoint 中即已存在）

- **D1/D2**：無 JWT → 401；合法 staff JWT → 200 + `success:true`。
- **D3a-e**：funnel 計數與真實 seed 資料完全吻合（`cart_abandoned` 的 pending/sent/converted 各 1，`checkout_abandoned` 的 cancelled 1，`payment_abandoned` 的 converted 3）。
- **D4a/b**：`reminder_then_converted`（JOB-BASED metric）正確要求 `channel='line' AND sent_at<>''`——`payment_abandoned` seed 了 3 筆 converted，其中 1 筆從未真正送出提醒（`channel=''`），驗證這 1 筆被正確排除，只算 2 筆。
- **D5a/b**：`recovered_revenue.payment_abandoned`（ORDER-BASED metric）正確只計算「有真實 `order_id` 連結」的那筆訂單（888 元），另一筆 `order_id=''` 的 converted job 被正確排除在營收加總之外。
- **D6a/b**：`cart_abandoned`／`checkout_abandoned` 明確回傳 `total: null, reason: 'no_reliable_order_linkage'`——驗證的重點是「誠實回傳無法計算」而非「回傳一個看似合理但其實是猜的數字」。
- **D7**：`days` 參數上限 clamp 到 90（傳 9999 驗證）。
- **D9/D10**：`conversion_rate_of_resolved` 正確排除 in-flight 狀態（`checkout_abandoned` 只有 1 筆 resolved(cancelled)，rate=0%；`cart_abandoned` 的 pending/sent 被排除在分母外，只剩 1 筆 converted 進分母，rate=100%）。
- **D11-D13**：`avg_minutes_to_recovery` 用一組刻意控制在剛好 10 分鐘差距的資料驗證計算結果精確等於 `10`；同時驗證 sample_size 正確排除沒有 `converted_at` 時間戳的舊資料；`checkout_abandoned` 完全沒有符合條件的樣本時回傳 `null`（不是 `0`）。
- **D8**：response 完整序列化後不含任何 seed 資料中使用過的原始 `cart_id`/`order_id` 字串（確認是聚合數字，不是逐筆明細）。

### resolved/converted_count 明確曝光（D14-D17，第二輪擴充）

- **D14**：`reminder_stats` 明確曝光 `converted_count` 欄位（不只是內部算完就丟）。
- **D15/D16**：`converted_count` 數值與 `funnel.converted` 一致性交叉驗證。
- **D17**：`resolved_count`/`converted_count` 在全部種子資料皆為 converted（無 cancelled/failed）時應相等。

### Invalid TTR Exclusion（D18-D19）

- **D18**：`converted_at < sent_at` 的異常時序資料（模擬時鐘漂移／髒資料）不得進入 `sample_size`（只算正常的那 1 筆，不是 2 筆）。
- **D19**：排除異常 row 後，`avg_minutes_to_recovery` 仍精確等於 10（沒有被負數時長拉低平均值）。

### 真正 JWT Cross-store Isolation（I1-I7）

不再使用 app-level `req.storeId` stub（那個手法會被 route 內建的 `requireStaffJwt` 覆蓋，見下方「本輪過程中發現並修正的問題」），全部改用 `jwt.sign({role:'staff', store_id:<target store>}, ...)` 產生真實 JWT，由真正的 `requireStaffJwt` 決定 `req.storeId`：

- **I1-I3**：store A 的真實 staff JWT → 只看得到 store A 的 funnel（2 筆 pending）與 revenue（888），不含 store B 的任何資料。
- **I4-I6**：即使 URL 帶 `?store_id=store_iso_b`，store A 的 JWT 仍然只回傳 store A 的資料——query string 的 store_id 被完全忽略（route 本身只讀 `req.storeId`，從不讀 `req.query.store_id`）。
- **I7**：response 完整序列化後不含 store B 的 `store_id`／revenue／`cart_id` 等任何識別資訊。

### Order-based Revenue Dedup（REV1/REV2）

- **REV1**：同一張訂單（total=888）被兩筆各自獨立的 `cart_recovery_jobs` 指向 → `order_count=1`，`revenue=888`（不是 1776，證明沒有把同一筆訂單金額加兩次）。
- **REV2**：兩張不同訂單，金額剛好都是 888 → `order_count=2`，`revenue=1776`（證明沒有誤用 `SUM(DISTINCT o.total)`，那個寫法會在兩張訂單同金額時把其中一張憑空丟掉）。

### Metric Grain 分離驗證（MIX1-3）

同一張訂單被 2 筆 job 指向的情境下，同時檢查兩種指標：`reminder_then_converted`（JOB-BASED）數到 2，`recovered_revenue.order_count`（ORDER-BASED）數到 1——明確證明兩個指標的分母不同，程式碼路徑沒有被混用成同一份計算。

### Canonical order_id Safety（CANON1-3）

Audit 確認 `routes/line-orders.js`／`routes/line-shipping.js` 建立訂單時 `id`/`uuid` 兩欄一律綁同一個 `uuid` JS 變數，`orders.id` 是 `TEXT PRIMARY KEY`（全表唯一）。因此 Dashboard SQL 的 `o.uuid = order_id OR o.id = order_id` 在 schema invariant 保證下不可能同時匹配兩張不同訂單。CANON1-3 用兩張 id/uuid 各自獨立的訂單直接證明：`order_count=2`，`revenue=333`（111+222，正確分別加總，非誤判成同一筆）。

### Admin UI Runtime Acceptance（UI-0 至 UI-14）

真實 jsdom 執行，擷取 production `public/index.html` 的完整卡片內容（`extractBalancedDiv()`，非字串搜尋）與真實 `public/js/app.js` 的 `loadCartRecoveryDashboard()` 原始碼：

- **UI-0/UI-1/UI-2**：卡片區塊、`cartRecoveryDashboardPanel` 容器、重新整理按鈕的 `onclick` 綁定皆存在。
- **UI-3/UI-4**：找到真實函式原始碼，確認呼叫既有 `apiFetch()`（不自建 fetch/JWT 邏輯）。
- **UI-5**：原始碼掃描確認前端對 `rev.total`／`conversion_rate_of_resolved`／`avg_minutes_to_recovery` 沒有做任何數值運算（區分了合法的字串串接顯示格式化與禁止的算術重新推導）。
- **UI-6/UI-7**：真的呼叫 `apiFetch` 且網址正確。
- **UI-8**：`revenue:null` → 畫面顯示「無法計算」。
- **UI-9**：成交率與樣本數一起顯示（`100%（1/1）`，不是孤立的百分比）。
- **UI-10/UI-11**：`avg_minutes_to_recovery:null` 時畫面不出現「0 分鐘」（用負向前瞻正則排除「10 分鐘」子字串誤判）；真實值 10 → 正確顯示「10 分鐘」。
- **UI-12/UI-13**：API 例外（fetch throw）與 `success:false` 回應都安全顯示固定錯誤文字，不 crash 整個頁面、不洩漏例外堆疊。
- **UI-14**：換一組不同的 backend 金額（54321），畫面顯示的數字跟著改變——證明是真的渲染 backend 給的值，不是寫死的樣板。

## 本輪過程中發現並修正的問題（誠實記錄，跨多輪 checkpoint 累積）

**問題一（HTML 抽取工具脆弱性）**：新增 Admin UI 面板後，`scripts/run-h1-4-10-phase4b-recovery-full-runtime.js` 原有的卡片 HTML 抽取正則因為巢狀 div 變複雜而提早截斷（抓不到 n8n 那段）。用真正的 tag-balance 掃描驗證後確認 **production HTML 本身完全正確**，純屬測試抽取工具的脆弱性。已修正為 `extractBalancedDiv()`（真正的括號配對演算法），重新執行 Full Suite 確認在抽取範圍修正後（包含 n8n 段落）依然 168/168 全數通過，證明修正沒有引入新的行為差異，只是讓測試抽取本身更可靠。

**問題二（誤診 SQL 為 sql.js quirk，實際是測試 JWT 錯誤）**：REV2（兩張不同訂單同金額）第一輪失敗，`order_count=1` 而非預期的 `2`。初步懷疑 revenue 查詢的巢狀 subquery+JOIN 在長時間、大量交錯查詢的測試環境下有 sql.js/WASM 層不穩定聚合的問題，改寫成 JS-side Set 去重版本——但改寫後問題依然存在，證明與 SQL 無關。真正根因：Dashboard route 是 `router.get('/overview', requireStaffJwt, handler)`，`requireStaffJwt` 是**路由層級中介層**，會用 JWT payload 的 `store_id` claim **覆蓋**任何 app-level 預先設定的 `req.storeId`。新測試區塊用 app-level stub 設定 `req.storeId` 卻仍送出綁定 `store_001` 的舊 token，導致 stub 被 `requireStaffJwt` 悄悄蓋掉。修正測試改用正確簽發的 JWT 後，最初的 DB-side SQL（子查詢 DISTINCT + JOIN）立刻正確通過，證明 SQL 從未有問題；已把 revenue 查詢改回 DB-side 版本（不需要為不存在的疑慮保留更複雜的 JS-side 寫法）。這個發現也直接促成了本輪 I1-I7（真正 JWT cross-store isolation）測試改用正確簽發的 JWT，而不是繼續依賴容易被中介層覆蓋的 app-level stub。

## Related Smoke Regression（實際執行，fresh-run）

| Script | 結果 |
|---|---|
| `run-h1-4-7-analytics-contract-runtime.js` | 61/61 |
| `run-g1-6-ga4-h1-4-7-two-stage-checkout-runtime.js` | 113/113 |
| `smoke-hotfix29-c.js` | 54/54，MANUAL=2（既有） |
| `smoke-hotfix30-b5-r5-2-a-order-geo-write.js` | 90/90 |
| `smoke-hotfix30-b5-r5-debounce.js` | 32/32 |
| `smoke-hotfix30-c1-rollback.js` | 22/22 |

## Baseline Known Failure（誠實記錄，非本輪 regression）

`smoke-hotfix30-b5-r5-2-a-integrated.js` 的 **M3**：`Cannot read properties of undefined (reading 'district_ranking')`，`utils/cartSnapshot.js`／`utils/cartGeoAttribution.js` 既有 bug，錯誤訊息與行號與歷次 Phase 記錄完全相同，與 Recovery Dashboard 程式碼路徑無關，Phase 4D 未觸碰。
