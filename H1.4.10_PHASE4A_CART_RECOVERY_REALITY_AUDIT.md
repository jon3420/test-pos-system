# H1.4.10 Phase 4A — CART RECOVERY STATE ENGINE Reality Audit

## 一、修改前確認事項

1. **`analytics_events` schema**：`store_id`／`visitor_id`／`session_id`／`cart_id`／`order_id`／`event_name`／`metadata_json` 等既有欄位齊全，`logServerEvent()`/`insertEvent()` 是唯一寫入函式（`utils/analyticsLog.js`）。
2. **`cart_id` 產生**：前端頁面本地產生（`_getCartId(createIfMissing)`），首次 `add_to_cart` 時建立。
3. **`add_to_cart`／`checkout_click`／`checkout_submit_click`／`checkout_validation_failed`**：皆為 client `_trackEvent()` → `POST /api/analytics/events` → `routes/analytics.js` → `insertEvent()`。
4. **`submit_order`**：`routes/line-orders.js`／`routes/line-shipping.js` 在訂單建立成功後呼叫 `logServerEvent()`，`order_id` 使用後端建立訂單時的 `uuid`（**server-authoritative，非前端提供**）。
5. **`purchase`**：非 LINE Pay 訂單在 `routes/line-orders.js` 建單當下視為成交立即寫入；LINE Pay 訂單改由 `routes/linepay.js` 的 `/confirm` 成功分支才寫入。
6. **`payment_success`**（Phase 3）：唯一寫入點在 `routes/linepay.js` 的 `/confirm`，`returnCode==='0000'` 分支，`value` 來自 DB `orders.total`。

## 二、本輪最重要的架構發現（Root Cause，非單純 sanitizer 問題）

### 問題

第一輪 Phase 4A 測試撰寫時假設「client `payment_started` 事件會帶有 `order_id`」，並在 `onAnalyticsEvent()` 內直接信任 `fields.order_id`。但實際 Audit `public/line-order.html`／`public/line-shipping.html` 的 `_trackEvent('payment_started', ...)` 呼叫點後發現：**前端從未、也不應該傳送 `order_id`**——`payment_started` 在既有 Analytics 契約裡本來就是 cart-centric 事件（沿用 `_trackEvent()` 既有的 `cart_id`/`visitor_id`/`session_id` 組合，`order_id` 欄位對這個事件從未有過賦值）。

於是真實 HTTP 測試（`POST /api/analytics/events`，`event_name=payment_started`）呼叫成功（HTTP 200），但：
- `analytics_events` 用 `order_id` 查不到這筆事件（因為它本來就是 `NULL`）；
- `cart_recovery_jobs` 完全沒有建立（因為 `onAnalyticsEvent()` 原本的邏輯要求 `orderId || cartId` 至少一個非空，`cartId` 有值理論上該建立，但用 `order_id` 當 idempotency key 的判斷邏輯讓查詢對不上）。

### 為什麼不能簡單地讓 `routes/analytics.js` 接受 `req.body.order_id`

client 可以在 `POST /api/analytics/events` 的 body 裡填寫**任意** `order_id`——包括別人的訂單編號。若直接採信，會造成：
- Recovery job 綁錯訂單（把顧客 A 的 payment_abandoned 誤植到訂單 B）
- `recoverable_value` 讀到錯誤金額
- 跨使用者的錯誤提醒（未來 Phase 4B 若接上 LINE Push，可能把提醒發給錯的人）
- 整個 payment recovery 的安全邊界被破壞

這與 Phase 3 已經確立的「`payment_success` 只能 server-authoritative」原則是同一等級的安全問題，只是這次發生在 `payment_started` 的 `order_id` 欄位上。

### 正確架構：利用真實 LINE Pay 事件順序做 cart→order 解析

Reality Audit 確認的真實順序：

```
checkout_click
→ checkout_submit_click
→ submit_order   [server-authoritative，有 store_id + cart_id + order_id]
→ payment_started [client event，只有 store_id + cart_id，沒有可信 order_id]
→ payment_success
→ purchase
```

`submit_order` 一定早於 `payment_started`，且 `submit_order` 的 `order_id` 是後端建立訂單時寫入的 `uuid`，完全不受前端控制。因此新增 `resolveAuthoritativeOrderForPaymentStart(db, storeId, cartId)`：

1. 用 `store_id + cart_id` 回查最近一筆 `submit_order` 事件，取得它的 `order_id`。
2. 再用這個 `order_id` 去 `orders` 表二次確認（`store_id` 相符、真的存在該筆訂單），並取得 `orders.total` 作為唯一 authoritative 金額來源。
3. 任一步驟找不到 → 回傳 `null`，`onAnalyticsEvent()` 安全跳過，不建立 `payment_abandoned`、不猜測。

`payment_started` 事件本身傳入的任何 `order_id`／`value`／`cart_value` 欄位，在這條路徑裡**完全被忽略**——不是「先看有沒有，沒有才 fallback」，而是從架構上根本不读取這些 client 可控欄位來做 Recovery 判斷。

### 兩個系統的職責明確分離

- **Analytics `payment_started`**：cart-centric client event，`order_id` 維持既有的 `NULL`（沒有改變 Analytics 歷史契約，這支事件的既有語意完全不變）。
- **Recovery `payment_abandoned`**：server-resolved order-centric state，`order_id` 是 Recovery Engine 自己用 `cart_id → submit_order → orders` 這條可信路徑解析出來的，不是 Analytics 事件本身携帶的。

這是本輪最重要的設計決策，直接决定了 Phase 4A 的安全邊界是否成立。

## 三、其他既有事實確認

- 目前沒有既有 scheduler／cron／worker 基礎設施——Phase 4A 只建立「狀態」與「查詢」，不涉及排程執行（下一階段才需要）。
- 現有 CRM（`crm_segments`／`crm_action_targets`）與本輪 `cart_recovery_jobs` 是完全獨立的兩張表，沒有互相依賴，符合「不重建第二套 CRM」的要求。
- `line_user_id` 的唯一可信來源仍是 `verifyMemberSession()`（`routes/analytics.js` 既有機制），Recovery Engine 沒有新增第二套身分驗證。
