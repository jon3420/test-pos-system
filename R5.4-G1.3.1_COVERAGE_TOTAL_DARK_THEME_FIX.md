# fix18-10-hotfix30-B5-R5.4-G1.3.1 — Coverage Business Total & Dark Theme Hotfix

本文件記錄本輪修正的兩個真實瀏覽器人工驗收問題：
1. Coverage Explanation 把「有訂單但無 Geo」誤顯示成「沒有訂單資料」。
2. Coverage Explanation 在 Dark Theme 下白底／白字，視覺破版。

本輪範圍嚴格限定在上述兩點，不重做 G1／G1.1／G1.2／G1.3，不修改 DB Schema、
訂單／營收定義、Channel／Date Range 定義、Store Isolation 規則。

---

## 1. Root Cause

`_geoHeatMetricTotals()`（`public/js/geo-heatmap-ui.js`）原本這樣計算「Business
Total」：

```js
// 修正前
const total = list.reduce((s, a) => s + (Number(a.submitted_orders) || 0), 0);
```

`list` 就是 `geoHeatState.areas`，而 `areas` 的來源是
`getGeoFulfillment()`（`utils/geoAnalyticsQueries.js`）——這支查詢從一開始
設計上就是「Order Heatmap 可以畫的區域」，SQL 條件明確寫著：

```sql
WHERE ... AND order_mode IN ('delivery','shipping') AND fulfillment_geo_source IS NOT NULL
GROUP BY fulfillment_geo_city, fulfillment_geo_district
```

也就是說，`areas`／`submitted_orders` 從一開始就**只涵蓋「外送／宅配、且有
履約地理資料來源」的訂單**。外帶訂單、或雖然是外送但完全沒有地理資料的訂單，
從一開始就不會出現在這個查詢結果裡——這不是 Bug，是這支查詢本來的設計目的
（它只負責回答「地圖上畫得出來的區域有哪些」）。

真正的 Bug，是 G1.3 把這個「只涵蓋可畫地圖訂單」的加總，直接拿來當作
「全店訂單總數」使用。真實案例：

- 今日 1 筆外帶訂單，NT$150，無 Geo 資料
- `areas` 因為上述 SQL 條件被完全排除（外帶不在 `order_mode IN
  ('delivery','shipping')` 範圍內）→ `areas = []`
- `_geoHeatMetricTotals()` 算出 `total = 0`
- `_geoHeatBuildCoverageExplanationText()` 看到 `total <= 0`，判定為
  `no_business_data`，顯示「目前沒有符合條件的訂單資料」

但實際上店家今天明明有 1 筆訂單、NT$150 營收，只是這筆訂單沒有地理資料。
正確的狀態應該是 `no_geo_data`（有業務資料，但沒有 Geo），不是
`no_business_data`（完全沒有業務資料）。

## 2. 為什麼 geoHeatState.areas 不能代表 Business Total

`areas` 是「Geo Drawable 子集合」，不是「全店總量」。任何直接對 `areas` 做
`reduce()` 加總得到的數字，語意上永遠是「在地圖上畫得出來的那一部分」，
不是「這段期間店家實際發生的全部業務量」。這兩個概念在 Coverage = 100% 時
剛好數字相等，容易讓人誤以為兩者是同一件事；但只要 Coverage < 100%（真實
案例是 0%），兩者就會分岔，繼續把 areas 加總當 Business Total 使用一定會
算錯。

## 3. getGeoFulfillment() 原 Geo SQL 的限制

```sql
FROM orders
WHERE ${ORDERS_BASE_WHERE} AND created_at BETWEEN ? AND ?
  AND order_mode IN ('delivery','shipping') AND fulfillment_geo_source IS NOT NULL
  ${cityClause}${districtClause}${sourceClause}${confClause}${areaClause}${chOrd.sql}
GROUP BY fulfillment_geo_city, fulfillment_geo_district
```

這段 SQL（`rows` 查詢）本輪**完全沒有修改**——它繼續只負責「Geo Drawable」
子集合，這是正確且必要的行為（Order Heatmap 本來就只能畫有地理資料的訂單）。
本輪只是在它旁邊，另外新增一支不受這些限制的查詢。

## 4. 新 Business Total Query

在 `getGeoFulfillment()` 內新增一支獨立查詢，計算「全店訂單數／營收」：

```sql
SELECT COUNT(*) AS business_total_orders,
       COALESCE(SUM(CASE WHEN ${ORDERS_PAID_EXPR} THEN total ELSE 0 END),0) AS business_total_revenue
FROM orders
WHERE ${ORDERS_BASE_WHERE} AND created_at BETWEEN ? AND ?${chOrd.sql}
```

與既有 `rows` 查詢的差異只有：**移除** `order_mode IN
('delivery','shipping')`、`fulfillment_geo_source IS NOT NULL`，以及
city／district／source／confidence 這些「地理區域篩選」子句（因為 Business
Total 是全店總量，不是某個特定區域的統計，跟區域篩選在概念上不相關）。

## 5. 與 Dashboard 統計口徑比對

逐項核對新查詢與既有 `routes/dashboard.js`（`/api/dashboard`）、既有 `rows`
查詢共用的定義：

| 項目 | Dashboard `/api/dashboard` | 既有 `rows`（Geo Drawable） | 新 `business_total_*`（本輪） |
|---|---|---|---|
| store_id | `req.storeId`（Store Isolation） | 同左 | 同左，同一個 `storeId` 參數 |
| 日期起訖／時區 | `created_at BETWEEN` local range | `range.startLocal`/`range.endLocal` | 同一組 `range.startLocal`/`range.endLocal`，未新建時區換算 |
| Channel filter | 依既有 channel 篩選 | `chOrd.sql`/`chOrd.params` | 沿用同一個 `chOrd` 變數，未新建第二套 |
| 訂單有效狀態 | `status!='void'` 等 | `ORDERS_BASE_WHERE` | 沿用同一個 `ORDERS_BASE_WHERE` 常數，逐字元相同，未重寫 |
| 取消／作廢／退款 | 依 `order_status`/`status` 排除 | 同左 | 同左（`ORDERS_BASE_WHERE` 內建 `status!='void' AND (order_status IS NULL OR order_status!='cancelled')`） |
| revenue 定義 | `status IN ('completed','modified')` 才計入 | `ORDERS_PAID_EXPR` | 沿用同一個 `ORDERS_PAID_EXPR` 常數，逐字元相同 |
| 外帶／外送／宅配 | 全部納入 | 只納入 delivery/shipping | **本輪新查詢移除此限制**，外帶／外送／宅配全部納入（這正是本輪要修的差異） |
| Store Isolation | `req.storeId`，不接受 query 覆蓋 | 同左 | 同左，同一個 `storeId` 參數，並以真實 DB fixture 驗證跨店不互相污染 |
| SQL 參數順序 | — | `[storeId, range.startLocal, range.endLocal, ...extraParams, ...chOrd.params, limit, offset]` | `[storeId, range.startLocal, range.endLocal, ...chOrd.params]`（子集合，順序前綴完全一致，只是少了 area 篩選相關參數，因為新查詢本來就不套用那些篩選） |

結論：新查詢**只移除了 Geo 專屬限制**（`order_mode`／`fulfillment_geo_source`
／地理區域篩選），其餘訂單定義、營收定義、Channel／Date Range 定義、Store
Isolation 規則，全部逐字元沿用既有常數與既有變數，沒有建立第二套統計口徑。

## 6. API additive fields

`getGeoFulfillment()` 回傳物件新增兩個欄位，其餘既有欄位（`page`／`limit`／
`areas`／`takeout_no_fulfillment_address`）完全不變：

```js
return {
  page, limit,
  business_total_orders: Number(businessTotalRow.business_total_orders) || 0,
  business_total_revenue: Number(businessTotalRow.business_total_revenue) || 0,
  areas: rows.map((r) => { /* 完全不變 */ }),
  takeout_no_fulfillment_address: Number(takeoutRow.c) || 0,
};
```

`routes/analytics-geo.js` 的 `/fulfillment` route 透過既有 `_safeHandler()`
直接把整個回傳物件包進 `{ success: true, data }`，其後處理函式
`_enrichAreaFields()` 只掃描 `AREA_ARRAY_KEYS`（`areas`／`top_areas`／`rows`／
`alerts`／`district_ranking`／`source_area`）這幾個已知陣列鍵名，不會誤把
新的純數字欄位當成區域陣列處理，路由層不需要任何修改。

## 7. Backward Compatibility

- 新欄位一律用 `typeof x === 'number'` 判斷是否存在，不用 truthy 判斷——
  `business_total_orders = 0` 是合法值，不會被誤判成「欄位不存在」。
- `areas` 陣列結構、既有每個 area 物件的欄位、排序、分頁欄位
  （`page`/`limit`）完全不變。
- `geoHeatScheduleUpdate()`（`public/js/geo-heatmap.js`）維持雙模式：
  - 舊呼叫方式：`fetchAreasFn` 回傳純陣列（`Array.isArray(result)` 為
    true）→ 沿用 G1/G1.1/G1.2/G1.3 既有行為，`businessTotals` 不更新。
  - 新呼叫方式：回傳 `{ areas, businessTotals }` → 額外把 `businessTotals`
    寫入 `geoHeatState.businessTotals`。
  - 兩種模式共用同一個 `seq`／stale-request guard，不會有「慢的舊 request
    後到反而覆寫新資料」的問題。

## 8. Frontend data flow

```
getGeoFulfillmentForHeatmap() API 回應
  └─ fulfillmentJson.data.business_total_orders / business_total_revenue
       └─ geoHeatUiFetchAndRender() 的 fetchAreasFn 回傳
            { areas: geoHeatBuildAreas(...), businessTotals: { orders, revenue } }
              └─ geoHeatScheduleUpdate() 寫入 geoHeatState.businessTotals
                   └─ _geoHeatUiRenderCoverageExplanation()
                        └─ _geoHeatMetricTotals(areas, metric, geoHeatState.businessTotals)
                             └─ total 優先取 businessTotals，欄位不存在才 fallback 回舊的 areas 加總
                             └─ drawn 語意完全不變，仍是 areas 的 coordinate_count／revenue 加總
```

## 9. Coverage 四態（沿用 G1.3 既有四態設計，本輪不重做狀態機，只修正
   `total` 的資料來源）

| 狀態 | 條件 | Orders 文案 | Revenue 文案 |
|---|---|---|---|
| `no_business_data` | `business_total <= 0` | 「目前沒有符合條件的訂單資料」 | 「目前沒有符合條件的營收資料」 |
| `no_geo_data` | `business_total > 0` 且 `geo_drawable <= 0` | 「今日已有 1 筆訂單，但目前沒有訂單包含可用的地理資料，因此無法顯示地圖熱區。」 | 「目前已有營收 NT$150，但目前沒有任何營收可歸屬到地理區域。」 |
| `partial_coverage` | `0 < geo_drawable < business_total` | 「10 筆訂單中有 4 筆可顯示於地圖，Coverage 40%」 | 同上句型，Revenue 版本 |
| `partial_coverage`（100%） | `geo_drawable == business_total` | 同上句型，`Coverage 100%` | 同上句型，Revenue 版本 |

## 10. Dark Theme Root Cause

`.geo-heat-coverage-explanation-text`（`public/css/geo-heatmap.css`）原本：

```css
.geo-heat-coverage-explanation-text {
  background: #f8fafc;   /* 寫死的淺色背景 */
  /* 沒有自己的 color */
}
```

這個區塊**完全沒有任何 `[data-theme="dark"]` 覆寫規則**。Dark Theme 開啟時，
全站 Dark Theme 樣式會把文字顏色改成淺色／白色，這段文字繼承了那個淺色，
但背景仍然停在寫死的 `#f8fafc`，變成「淺底淺字」，對比嚴重不足，也就是
使用者回報的「白底／白字」。

## 11. CSS 修正

比照專案既有慣例（`public/css/geo-live-layer.css` 的
`[data-theme="dark"]` / `.geo-live-theme-dark` 雙選擇器＋深灰藍色票），
新增：

```css
[data-theme="dark"] .geo-heat-coverage-explanation-text,
.geo-live-theme-dark .geo-heat-coverage-explanation-text {
  background: #1e293b;
  color: #e2e8f0;
  border-left-color: #475569;
}
/* 各 coverage state 的 accent border 色票（no_business/no_geo/partial/full） */
```

- Light Theme 既有規則（`background:#f8fafc`）完全保留，未被覆蓋或刪除。
- Dark Theme 新規則不使用 inline style，selector specificity 靠
  `[data-theme="dark"] .class` 與 `.geo-live-theme-dark .class`（單一 class
  + attribute selector，跟既有 `geo-live-layer.css` 慣例一致），不需要
  `!important`。

## 12. Known Limitations

- `_geoHeatBuildBusinessOpportunityEmptyText()`／
  `_geoHeatBuildDeliveryOptimizationText()` 是 G1.3 既有函式，本輪沒有在
  任何既有渲染路徑中發現它們被實際呼叫（沒有找到對應的 UI 掛載點）；本輪
  維持現狀，不新增功能、不新增呼叫路徑，只確認這兩個函式本身邏輯未被破壞。
- Business Total 的 Channel／Date Filter 對齊靠「共用同一個 `chOrd`／
  `range` 變數」保證；如果未來有人在 `rows` 查詢新增獨立的區域篩選（例如
  只查特定 district），Business Total 不會跟著套用該篩選（因為概念上
  Business Total 本來就該是全店總量，不受區域篩選影響）——這是刻意設計，
  不是遺漏。
- Manual Visual（真實瀏覽器 Dark/Light Theme 截圖驗收）狀態請見
  `R5.4-G1.3.1_MANUAL_QA_CHECKLIST.md`。
