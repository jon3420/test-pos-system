# H1.4.10 Phase 1 — LIFF 核心會員歸戶 Reality Audit

範圍：`fix18-10-hotfix30-B5-R5.4-G1.6-GA4-H1.4.9-CHECKOUT-SYNC-ORDER-SUMMARY-full`
→ Phase 1（D/E/F/I/J：LIFF 被動辨識 + link-context）

---

## 一、現有元件盤點（修改前）

| 需求文件提到的名稱 | Production code 實際狀態 |
|---|---|
| `initLineMemberGate()` | 已存在，`public/js/line-member-gate.js`。負責 `liff.init()` + 內部無條件呼叫 `handleLineMemberLoginCallback()`。 |
| `getFreshLineIdToken()` | 已存在。前端先檢查 ID Token 是否即將過期，避免送出注定失敗的 verify。 |
| `verifyWithBackend()` | 已存在。**內部在 Token 過期時會呼叫 `recoverLineLoginSession()` → `liff.login()`**（見下方「重大發現」）。 |
| `linkMemberSession()` | 已存在，`utils/lineMemberStats.js`。依 `(store_id, line_user_id, visitor_id)` upsert，本來就 idempotent。 |
| `verifyMemberSession()` | 已存在，`utils/lineMemberSession.js`。HMAC 簽章 + `store_id` 比對，跨店天生被拒。 |
| `_currentMemberSession()` | 已存在，`public/line-order.html` / `line-shipping.html`。 |
| `line_members` | 已存在資料表。 |
| `line_member_sessions` | 已存在資料表。 |
| `/api/line-member/verify` | 已存在，`routes/line-member.js`。 |

**結論：以上元件全部沿用 production 既有架構，Phase 1 未重建任何一套「第二套」機制。**

## 二、現有 LIFF 初始化入口

- `public/line-order.html` 的 `_initLineMemberGateFromShopData()`：修改前只在 `gate_enabled` 或 `cartToken`（Handoff）存在時才呼叫 `LineMemberGate.initLineMemberGate()`。`gate_mode==='disabled'` 且無 `cartToken` 時完全不初始化 LIFF。
- `public/line-shipping.html` 同構，但沒有 Handoff cartToken 流程。

## 三、現有 verify 流程

`POST /api/line-member/verify`：驗證 ID Token → 查好友狀態 → `upsertMemberProfile()` → `linkMemberSession()` → 簽發 `member_session` → 回傳。所有事件寫入走 `logServerEvent()`，白名單機制在 `utils/analyticsLog.js` / `routes/analytics.js`。

## 四、現有 member_session 儲存方式

前端：`localStorage['line_member_session_' + store_id]`，只存簽章過的 `member_session`、遮罩後 profile、`is_friend`、`expires_at`。絕不存 Token / 完整 UID。

## 五、現有 visitor/cart 綁定位置

`linkMemberSession(db, storeId, lineUserId, {visitor_id, session_id, cart_id})` 是唯一綁定入口，`/verify` 內已經呼叫一次。**修改前沒有獨立的「補綁定」API**——若 `verify` 當下 cart_id 還是空的，之後建立 cart_id 就不會再回頭綁定，這正是 Phase 1 section I 要解決的落差。

## 六、本階段實際修改的檔案

1. `public/js/line-member-gate.js` — 新增 `tryPassiveLiffIdentification()`、`_passiveVerifyWithBackend()`、`initLineMemberGate()` 新增選填 `opts.skipLoginCallback`
2. `routes/line-member.js` — 新增 `POST /link-context`；`/verify` additive 補記 `line_liff_auto_identify_success/failed`
3. `routes/settings.js` — 新增 `line_member_auto_identify_enabled`、`line_shipping_liff_id`
4. `routes/analytics.js` — 新增事件白名單分流、`sanitizeLiffAutoIdentifyMetadata()`
5. `utils/analyticsLog.js` — 新增 5 個事件名稱進 `EVENT_WHITELIST`
6. `public/line-order.html` — `shouldInitLiff`、`_linkLineMemberContext()`、`_trackAddToCart()` 補綁定、Handoff restore 完成後補綁定
7. `public/line-shipping.html` — 同上（無 Handoff 分支），另外 `line_shipping_liff_id` fallback
8. `scripts/run-h1-4-10-phase1-liff-identity-runtime.js` — 新增 Phase 1 專用回歸測試

---

## 七、重大架構發現（本輪過程中發現，非預先假設）

### 問題

在把 `shouldInitLiff` 擴大為 `gate_enabled || auto_identify_enabled || cartToken` 之後，撰寫自動化測試時發現：**只要 `initLineMemberGate()` 被呼叫，它內部一定會無條件執行既有 hotfix25 的 `handleLineMemberLoginCallback()`**。這支既有函式的觸發條件只看 `isLiffAvailable(storeId) && global.liff.isLoggedIn()`——**完全不檢查 `isInClient()`**，而且送出的 `gate_stage` 固定寫死是 `'callback'`。

這代表：一旦 `gate_enabled=false`、`auto_identify_enabled=true`，光是初始化 LIFF 就會讓既有 callback 機制自己搶先做一次 verify（`gate_stage='callback'`），接著本輪新增的 `tryPassiveLiffIdentification()` 又可能再做一次（`gate_stage='liff_auto_identify'`）。兩者：

1. 語意重疊，事件被錯誤分類（真正的被動辨識反而被算成「登入返回驗證」）。
2. 舊機制不檢查 `isInClient()`，不符合本階段規格明確要求的 6 條件缺一不可。
3. 有機率造成同一次頁面載入送出兩次 `/verify` 請求。

### 解法（設計決策，非單純 bug fix）

**保留既有 callback 行為為預設（100% 向下相容）**，只新增一個選填參數作為「例外開關」：

```js
initLineMemberGate(config, ids, onEvent, opts)
// opts.skipLoginCallback：預設 undefined（等同 false）
```

- **未傳這個參數** → 行為與修改前逐字元相同（`handleLineMemberLoginCallback()` 照常執行）。所有既有呼叫點（checkout Gate、entry Gate、Cart Handoff、hotfix25 login-resume）**完全不改動呼叫方式**，不受本輪影響。
- **只有一種情況會傳 `{ skipLoginCallback: true }`**：頁面判斷「這次初始化 LIFF 的唯一理由是 `auto_identify_enabled`」——即 `gate_enabled===false` 且（line-order 額外要求）`cartToken` 也不存在。此時交由本輪新增、真正滿足 6 條件檢查（含 `isInClient()`）且送出正確 `gate_stage='liff_auto_identify'` 的 `tryPassiveLiffIdentification()` 獨家負責 verify，避免語意衝突與重複請求。

判斷式：

```js
// line-order.html
const skipLoginCallback = !_lineMemberGateConfig.gate_enabled && !cartToken;
// line-shipping.html（無 Handoff 分支）
const skipLoginCallback = !_lineMemberGateConfig.gate_enabled;
```

### 為什麼不直接修改 `verifyWithBackend()` 本身

`verifyWithBackend()` 在 Token 過期時呼叫 `recoverLineLoginSession()`（進而 `liff.login()`）是 hotfix25 checkout/entry Gate 的既有、經過完整測試的正常行為（使用者主動要求登入時，Token 過期理應自動嘗試恢復）。被動辨識絕對不能觸發登入導頁，因此新增了完全獨立、不含任何 `login()`/`recoverLineLoginSession()` 呼叫的 `_passiveVerifyWithBackend()`，而不是在共用的 `verifyWithBackend()` 內加 if/else 分支去分裂兩種語意——這樣可以保證 Gate 既有路徑的程式碼路徑「一行都沒有變」，被動辨識路徑則從第一行開始就不可能觸碰 `login()`。

---

## 八、Frozen Contract 確認

`scripts/run-h1-4-9-checkout-order-summary-runtime.js` 修改前後皆為 `TOTAL=67 PASS=67 FAIL=0`（本文件與 Test Report 皆附上兩次 fresh-run 結果）。本輪未修改 `view_cart` / `checkout_click` / `openCheckoutStep()` / `_enterCheckoutStage()` / `backToCartStep()` / order summary / CTA 金額同步 / GA4 `begin_checkout` 映射 / Meta `InitiateCheckout` 映射 任何一行程式碼。
