# G1.6-A2-T1 — Manual Test Checklist（正式 Zeabur 部署）

前置：部署本次變更（新增 `routes/admin-geo-diagnostic.js` +
`utils/clientIpTrustDiagnostic.js` + `server.js` 一行掛載），
**不要**變更 `GEO_VISITOR_IP_ENABLED`／`GEO_VISITOR_IP_PROVIDER`／
`TRUST_PROXY`／`GEO_TRUSTED_IP_HEADER`，一律維持目前預設值，這樣才能看到
「什麼都沒設定」時的真實基準行為。

用你的 Super Admin 帳號取得 Bearer Token（`POST /api/super-admin/login`）。

## Part 1 — 基準行為（不帶任何測試 Header）

- [ ] `GET https://<正式網域>/api/admin/geo/client-ip-trust-diagnostic`
      （帶 `Authorization: Bearer <token>`，不加任何自訂 header）
- [ ] 記錄：`req_ip_scope`（應為 `public` 才代表拿到真實客戶端 IP；若為
      `private` 代表拿到的是 Zeabur 內部代理 IP）
- [ ] 記錄：`socket_ip_scope`（若為 `private`，代表確實有代理層介於
      Zeabur edge 與這個 Node process 之間）
- [ ] 記錄：`xff_present`／`xff_hop_count`（Zeabur 是否本來就會附加自己
      的 XFF，即使客戶端完全沒送）

## Part 2 — Sentinel Spoof Test

用**你自己的終端機／Postman**（不是瀏覽器，瀏覽器可能不給自訂
X-Forwarded-For）對正式網域發送：

```
curl -H "Authorization: Bearer <token>" \
     -H "X-Forwarded-For: 198.51.100.77" \
     https://<正式網域>/api/admin/geo/client-ip-trust-diagnostic
```

- [ ] 記錄 `sentinel_seen` / `sentinel_position` / `req_ip_is_sentinel`
- [ ] 判定對照使用者需求文件二的 A/B/C 三種情況，填入下方「判定結果」

再測一次，這次帶兩段：

```
curl -H "Authorization: Bearer <token>" \
     -H "X-Forwarded-For: 198.51.100.77, 198.51.100.77" \
     https://<正式網域>/api/admin/geo/client-ip-trust-diagnostic
```

- [ ] 記錄 `xff_hop_count`（若變成 2，代表 Zeabur 沒有清掉客戶端自己塞的
      重複值，只是附加；若還是維持 1 或客戶端的值整段被取代，代表 Zeabur
      有做覆寫或清洗）

## Part 3 — 不同網路環境（各自呼叫一次，不加任何自訂 header）

對每一種環境，記錄 `ephemeral_client_fingerprint`／`req_ip_scope`／
`socket_ip_scope`／`xff_hop_count`：

- [ ] 電腦 Wi-Fi
- [ ] 手機行動網路（關閉 Wi-Fi，純 4G/5G）
- [ ] 手機 Wi-Fi（同一支手機，切回 Wi-Fi）
- [ ] 電腦無痕視窗（同一台電腦、同一個 Wi-Fi）

檢查：

- [ ] 電腦 Wi-Fi 與手機行動網路的 fingerprint **不同**
- [ ] 電腦 Wi-Fi（一般視窗）與電腦無痕視窗的 fingerprint **是否相同**
      （若相同，代表 fingerprint 正確反映「同一個網路出口 IP」而非瀏覽器
      層級的識別，符合設計預期；記錄結果即可，兩種結果都不算異常）
- [ ] 所有四組請求的 `req_ip_scope` 皆為 `public`（若某一組是
      `private`/`loopback`，代表該路徑的代理設定或本機網路有異常）
- [ ] `xff_hop_count` 在四組請求間一致（代表 Zeabur 的代理層數穩定，不是
      有時候多一層、有時候少一層）

## 判定結果（填寫後回報）

- Sentinel 情況：`A` / `B` / `C`（見審核文件二）
- 建議 `TRUST_PROXY` 值：______
- 是否需要 `GEO_TRUSTED_IP_HEADER`：是／否，若是，header 名稱：______
- Client IP Trust Gate：PASS／BLOCKED

## 完成後

把上述勾選與記錄值回報即可（**不要貼真實 IP**，只需要
public/private/loopback／hop count／fingerprint 字串本身，這些都已經是
安全等級的資訊）。我會依此更新
`R5.4-G1.6-A2-T1_ZEABUR_CLIENT_IP_TRUST_AUDIT.md` 的 Gate 判定，並決定
`TRUST_PROXY` 最終建議值。**此階段完成前不會設定/購買/假設任何 Provider，
不會開始 G2。**
