# R5.3-A1.2 — Analytics Visitor Geo Sync — Completion Report

狀態：**QA BUILD**。Automated Verification：PASS。Manual Visual：REQUIRED。
Final Completion：NOT YET APPROVED（本文件本身不宣告正式 COMPLETE）。

---

## 1. Scope

目標：讓 Geo Intelligence 不再只依賴 `orders.delivery_lat/delivery_lng`。
建立 Analytics → Geo Visit Log → Geo Intelligence 的完整同步，讓商家即使
尚未產生訂單，也能看到訪客地理分布（Visitor Layer）。

第一原則：**不修改既有 Order Heatmap／Order Centroid／Order Coverage**（全部
保持原狀，見第 7 節逐位元組稽核），不建立第二套 Dashboard／Analytics API，
不使用假座標。

## 2. 新增/修改檔案

| 檔案 | 動作 | 內容 |
|---|---|---|
| `utils/db.js` | 擴充 | 新增 `geo_visit_log` 資料表 + 4 個索引（safe migration，`analytics_events` 完全未被修改） |
| `utils/geoVisitLog.js` | **新增** | 寫入（`logGeoVisit`，fail-open）＋查詢（`getGeoVisitSummary`／`getGeoVisitAreas`／`getRecentGeoVisits`）＋時間範圍解析＋正式 `visitor_key` 去重規則（見第 4 節） |
| `utils/analyticsLog.js` | 擴充 | `insertEvent()` 主寫入成功後，額外呼叫 `logGeoVisit()` 同步寫入 `geo_visit_log`（獨立 try/catch，fail-open，不影響 `analytics_events` 本身） |
| `routes/analytics-geo.js` | 擴充 | 新增 **一支**端點 `GET /api/analytics/geo/visitor-log`（掛在既有 Geo Analytics router 上，不是第二套 API） |
| `public/js/geo-visitor-layer.js` | **新增** | Visitor Layer（Choropleth-only，不畫 CircleMarker，不建立行政區中心點/店家座標/假座標）。完全獨立於 Order Heatmap Engine，不 import/覆寫其任何常數或 state |
| `public/js/geo-heatmap-ui.js` | 擴充 | 新增 Layer 切換列（訂單熱區／訪客熱區），Order Heatmap 既有內容原樣包在新的 `#-order-layer` 容器內，一個字元未變；新增 `#-visitor-layer` 區塊 |
| `public/css/geo-visitor-layer.css` | **新增** | Layer 切換、Recent Visitor Log 面板樣式 |
| `public/index.html` | 擴充 | 新增 `<link>`/`<script>`，`geo-visitor-layer.js` 排在 `geo-heatmap-ui.js` 之後載入 |
| `scripts/smoke-hotfix30-b5-r5-3-a1-2-visitor-geo-sync.js` | **新增** | Visitor Geo Sync Smoke，185 項 assertion |
| `scripts/smoke-hotfix30-b5-r5-2-a-integrated.js` | 已於前一輪修正 | 與本輪無關（見 R5.3-A1.1 報告） |

## 3. 座標與 Choropleth 決策（使用者本輪明確指示）

稽核結論：`public/data/geo/taiwan/taoyuan-districts.geojson` 的 13 個行政區
逐一驗證後，**全部是軸對齊矩形**（4 個角點座標兩兩相同），是測試用
fixture，不是官方合法行政區邊界。因此：

- Visitor Layer **完全不畫 CircleMarker**、不建立行政區中心點、不使用店家
  座標或矩形中心假造座標——IP Geo 只給 city/district 名稱，從不給座標，
  `geo_visit_log.lat`/`lng` 一律為 `NULL`（欄位保留給未來真的有座標來源時
  使用）。
- 改用 **Choropleth**，但只有「正式合法 Polygon」的行政區才可著色：
  `GEO_VISITOR_CHOROPLETH_OFFICIAL_CITY_CODES` 目前刻意留空（見
  `public/js/geo-visitor-layer.js` 開頭註解），因此**目前沒有任何城市會
  被上色**——這是忠實反映真實資料狀態，不是 bug。之後若有正式邊界資料，
  只需要把該城市的 `city_code` 加進這個常數。
- 沒有合法 Polygon 可上色的行政區，仍完整保留在 Ranking／Summary／
  Coverage／Recent Visitor Log（Integration Smoke A17-18/A17-19、B5-1~B5-4
  已驗證）。

## 4. A7-3 根因調查與正式 visitor_key 規則（本輪重點）

### 4.1 根因

上一輪 Visitor Geo Sync Smoke 的 `A7-3`（「同一 session 重複 10 次
page_view，不得灌高 Geo Visitors」）出現 1 項 FAIL。依指示逐項排查：

1. **測試重用了同一 `store_id`（`STORE_X`）跨 A3／A4／A6／A7／A8 好幾個
   情境**，且測試資料在整支腳本執行期間從未清除——這是根因主因。
2. Session/Visitor ID 本身沒有問題：10 次 `page_view` 確實都用同一組
   `session_id='sessDup'`／`visitor_id='visDup'`。
3. `geo_visit_log` 沒有重複寫入同一事件：10 次呼叫確實各自產生 10 筆列
   （`A7-1` 已驗證），只是聚合查詢正確把它們去重成 1 個 session。
4. **`A7-3` 原本斷言 `dupSummary.geo_visitors >= 4`**——這個門檻本身估算
   錯誤：到執行到 A7 為止，`STORE_X` 累積的實際 distinct session 只有 3 個
   （`sessA`／`sessU`／`sessDup`），不是 ≥4。**這是測試斷言寫錯，不是
   product SQL 的 bug**。

用最小測試資料重現（1 store／1 session／1 visitor／10 筆 page_view／同一
行政區／同一時間範圍，獨立乾淨的 store_id）：確認 `geo_visitors` 正確為
`1`，`geo_visit_log` 確實寫入 10 筆事件列——原本的 session_id 去重邏輯本身
是對的。

### 4.2 排查過程中發現的真正產品層級落差

在依指示覆查「正式去重規則」時，另外發現一個**真正的產品 SQL 問題**：
舊版 `getGeoVisitSummary()`／`getGeoVisitAreas()` 一律用
`COUNT(DISTINCT session_id)`。重現測試證實：**同一 `visitor_id`、不同
`session_id`（例如同一人隔天回訪、開新分頁）會被誤算成 2 個不同訪客**——
這違反需求文件二的正式規則（「不得把 visitor_id 與 session_id 同時各算
一人」）。

### 4.3 正式 visitor_key 規則（已修正產品程式碼）

`utils/geoVisitLog.js` 新增單一 SQL 運算式 `VISITOR_KEY_SQL`，
`getGeoVisitSummary()`／`getGeoVisitAreas()` 全部改用它，不再各自維護一份
去重邏輯：

```sql
COALESCE(NULLIF(visitor_id,''), NULLIF(session_id,''), 'event_' || id)
```

規則：
- `visitor_id` 非空 → 用 `visitor_id`（同一人跨多個 session 仍算 1 人）。
- `visitor_id` 缺失但 `session_id` 非空 → 用 `session_id`（fallback）。
- 兩者皆缺失 → 用該筆事件自己的 `id` 當唯一 key（不會被誤併成同一人，也
  不會跟其他缺值列混在一起）。
- Geo Visitor／AddToCart／Checkout／Orders 一律 `COUNT(DISTINCT visitor_key)`
  搭配對應 `event_name` 篩選。

**修正的是產品程式碼**（`utils/geoVisitLog.js`），因為這是真實存在、會在
正式環境影響統計正確性的落差，不是單純測試污染。同時依指示修正了測試腳本
本身的情境隔離問題（見第 4.4 節）。

### 4.4 測試修正

- 每個去重相關情境改用專屬、獨一無二的 `store_id`，不再共用 `STORE_X`。
- 新增 12 項需求文件四要求的正式斷言（`A9-KEY-1`～`A9-KEY-12b`），逐一涵蓋：
  同 visitor 不同 session／visitor 缺失同 session／visitor 缺失不同
  session／多次 view_product／add_to_cart／begin_checkout／purchase 去重／
  Unknown 訪客去重／已知但無座標行政區去重／跨店隔離／時間範圍疊加／
  Recent Log 保留原始紀錄但 Summary 去重。
- 沒有為了讓測試通過而放寬任何預期值——`A7-3`／`A7-4` 現在斷言的是精確值
  （`=== 1`），不是模糊下限。

## 5. Visitor Geo Sync Smoke（`scripts/smoke-hotfix30-b5-r5-3-a1-2-visitor-geo-sync.js`）

**185 項 assertion，全數 PASS**（需求下限 180 項）。

- Part A（139 項）：Migration／Index／`insertEvent()` 同步寫入／Unknown
  處理／Fail-Open（寫入與讀取兩側）／正式 `visitor_key` 去重規則（12 項
  新增情境）／Dashboard 統計／不矛盾狀態／Time Filter／依行政區聚合／
  Recent Visitor Log／Store Isolation／Performance（Index，非全表掃描，
  含 `getGeoVisitAreas()` 的 GROUP BY 查詢）／API Route／Static Audit／
  module 匯出完整性／其他既有事件類型同步／Choropleth 合法性清單稽核。
- Part B（46 項）：jsdom 實測——Layer Toggle（Order Heatmap 內容原樣保留
  於 DOM，只是隱藏）／Visitor Range Bar／Choropleth（No Marker／No
  Centroid／Legitimate Polygon Only／Marker-Layer Reuse／No Second
  Leaflet Map）／Tooltip（定位層級／來源／精確座標三行）／Error
  Handling／Store Isolation（UI 層）／Coverage／Tooltip 純函式邊界情境。

## 6. Regression

全部重新執行，共 24 個相關 Smoke 檔案，**全部 0 FAIL**：

| 套件 | 結果 |
|---|---|
| `node --check`（全專案） | 全部 OK |
| Order Heatmap Engine／既有 Map／Settings／manifest SHA-256 | 與基線逐位元組相同 |
| **A1.2 Visitor Geo Sync Smoke（新增）** | **185/185 PASS** |
| A1 Heatmap Engine Smoke | 128/128 PASS |
| A1.1 Integration Smoke | 112/112 PASS |
| B2 Geo Map Smoke | 630/630 PASS |
| B3 Geo Settings UI Smoke | 157/157 PASS |
| B1-1～B1-6 | 100/150/76/132/819/867/313/376，全部 0 FAIL |
| R5.1-A／B／C／D1 | 76/111/196/164，全部 0 FAIL |
| R5.2-A 系列（含 Integrated E2E） | 62/232/90/140，全部 0 FAIL |
| Cart/Order Hours、Dashboard UI、Debounce、Hotfix30-C1（既有、跟 Geo 無關） | 55/20/32/22，全部 0 FAIL |

## 7. Static Audit

- **Order Heatmap Engine 未修改**：`public/js/geo-heatmap.js`／
  `geo-intelligence-map.js`／`geo-map-settings.js`／manifest.json 與基線
  SHA-256 逐位元組相同。
- **Order Centroid／Order Coverage 未修改**：兩者都定義在
  `geo-heatmap.js`（`geoHeatComputeAreaCoverage`／`geoHeatComputeMetricCoverage`），
  該檔案本輪零修改。
- **rectangle fixture 未被用於 Choropleth**：`GEO_VISITOR_CHOROPLETH_OFFICIAL_
  CITY_CODES` 原始碼確認是空陣列，且逐一驗證 `TAO`/`TPE`/`KHH`/`TXG` 目前
  皆不具上色資格。
- **Visitor Layer 今日不畫假點**：`geo-visitor-layer.js` 沒有 `L.circleMarker`
  呼叫、沒有任何座標生成邏輯，`lat`/`lng` 全部來自資料庫既有欄位（一律
  `NULL`）。
- **不使用行政區中心點／不使用店家座標**：原始碼掃描確認無相關計算邏輯。
- **不使用 `Math.random()`**：`geoVisitLog.js`／`geo-visitor-layer.js` 掃描
  確認皆無。
- **不新增第二張 Leaflet Map**：`geo-visitor-layer.js` 無 `L.map()`/`tileLayer()`
  呼叫（原始碼掃描 + jsdom 實測 `mapCreateCount===0`/`tileLayerCreateCount===0`
  雙重驗證）。
- **不新增第二套 Analytics**：只新增一支 `/visitor-log` 端點，掛在既有
  Geo Analytics router 上；`refreshGeoDashboardKpiBlock` 仍只有一份定義。
- **`geo_visit_log` 查詢使用 Index**：`getGeoVisitSummary`／`getGeoVisitAreas`
  （含 GROUP BY）／`getRecentGeoVisits` 的 `EXPLAIN QUERY PLAN` 全數確認
  `SEARCH ... USING INDEX`，無 `SCAN`。
- **Unknown 不被排除**：`geo_visitors` 統計把 Unknown 訪客計入分母與分子，
  Ranking/Coverage/Recent Log 均正常顯示 Unknown 列。
- **Store Isolation 正常**：`geo_visit_log` 所有查詢一律以 `store_id` 篩選；
  跨店相同 `visitor_id` 已驗證不會被誤合併（`A9-KEY-10a/10b`）。

## 8. 已知限制 / Manual Visual 必要性

- Choropleth 目前不會在任何城市顯示顏色（見第 3 節，忠實反映真實資料
  狀態），需要之後有正式行政區邊界資料才會生效；在此之前，Visitor Layer
  的主要呈現方式是 Ranking／Summary／Coverage／Recent Visitor Log。
- 真實瀏覽器下的 Layer 切換視覺過渡、Range Bar 排版、Recent Visitor Log
  在手機版的可讀性，仍需要人工檢查，見
  `R5.3-A1.2_MANUAL_QA_CHECKLIST.md`。
