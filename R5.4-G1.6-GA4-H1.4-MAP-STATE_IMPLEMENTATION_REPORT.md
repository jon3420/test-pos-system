# R5.4-G1.6-GA4-H1.4-MAP-STATE — Implementation Report

Baseline：`fix18-10-hotfix30-B5-R5.4-G1.6-GA4-H1.3-EVENT-COMPAT-QA-full.zip`

## 1. Scope / Baseline

本輪標題：Geo Intelligence Dashboard/Heatmap Map Layer State Isolation ＋
Dashboard GA4 Regional Overview ＋ Historical Extended Range Query。

## 2. 使用者原始症狀

- Dashboard 直接開啟時地圖沒有 GA4 marker。
- Heatmap → GA4 Historical render 之後切回 Dashboard，紫色 marker 殘留在
  Dashboard 地圖上。
- F5 之後 marker 又消失。

## 3. Reality Root Cause

Dashboard 與 Heatmap **共用同一個 Leaflet map instance**（`geoMapState.instance`，
既有刻意架構，不是 bug）。`geoHeatUiSwitchTab(..., 'dashboard')` 過去只呼叫
`_geoHeatUiRestoreChoropleth()`，從未把 Heatmap 端已經 `addLayer` 上共用地圖
的 4 組 LayerGroup（Order／Visitor／GA4 Realtime／GA4 H1 Historical）移除。

## 4. 為何 F5 讓 marker 消失

F5 是整頁重新載入，所有前端 state 與 Leaflet map instance 本身重新建立，
`geoHeatUiState.activeTab` 預設 `'dashboard'`，Heatmap 從未被啟用過，GA4
Layer 從未 `addLayer`。這是「重新開始」，不是「修好了」——資料庫、GA4
同步、Marker Catalog 全程沒有涉入。

## 5. Dashboard 原本的樣子

`geoMapState.geoJsonLayer`，資料來源 `vm.funnel.areas`（訂單/session
轉換率 choropleth），完全沒有 GA4 marker。

## 6. Dashboard GA4 Overlay 是 New View Capability

不是修既有 bug，是本輪新增的能力：`public/js/geo-ga4-dashboard-layer.js`。

## 7. Layer Ownership Architecture

| Layer | Owner |
|---|---|
| Dashboard Choropleth | `geoMapState.geoJsonLayer`（既有，保留） |
| Dashboard GA4 Historical | `dashboardGa4State.layerGroup`（新） |
| Heatmap Order | `geoHeatState.layerGroup` |
| Heatmap Visitor | `geoVisitorState.choroplethLayerGroup` |
| Heatmap GA4 Realtime | `geoGa4State.layerGroup` |
| Heatmap GA4 Historical (H1) | `geoGa4H1State.markerGroup` |

`geo-heatmap-ui.js` 新增 `_geoHeatUiCleanupForDashboard()` 集中處理
Heatmap→Dashboard 的四組 LayerGroup 移除／`geoGa4Deactivate()`／
`GeoGa4H1Panel.destroy()`，不散落多次 `map.removeLayer(...)`。

## 8. No Second Map

全程只有一個 `L.map()`（`geo-intelligence-map.js`，本輪未修改）。Mutation
`DASHBOARD-SECOND-LEAFLET-MAP` 專門證明 Dashboard 模組沒有建立第二張地圖。

## 9-13. Extended Range（single／90d／180d／this_year／last_year／custom）

`public/js/geo-range-resolver.js`（新檔）：`resolveGeoHistoricalRange(mode, options)`
純函式支援 10 種 mode，全部輸出 `{ok, mode, apiRange, startDate, endDate,
dayCount, displayLabel}`。90d/180d 用 `today - 89`／`today - 179`（inclusive）。

## 14. Taipei Calendar Semantics

沿用既有集中 Timezone Helper（`utils/dateTime.js` 的
`getTaipeiCalendarDateString()`，Node 端；`public/js/date-time-format.js`
新增同名瀏覽器端函式，逐行對照演算法一致，兩者用
`scripts/run-g1-6-ga4-h1-4-timezone-parity-runtime.js` 做 parity 驗證）。

## 15-16. CUSTOM_RANGE_MAX_DAYS：92 → 365 span

`resolveRangeWindow()` 的 `span` 是 exclusive 日期差（`start===end` 時
`span=0`），不是 inclusive calendar days。`inclusiveCalendarDays = span + 1`。
原本 92 的門檻會擋掉 180d／今年／去年這三個新 preset。正確調整值是 **365**
（不是字面上看起來對稱的 366）：`span<=365` 允許最多 366 個 inclusive
calendar days（涵蓋閏年全年），`span=366`（367 inclusive days）才會被拒絕。

## 17. Resolver → apiRange='custom' Mapping

`today/yesterday/7d/30d` 維持原生 `apiRange`；`single/90d/180d/this_year/
last_year/custom` 全部收斂成 `apiRange='custom'` + 明確 `start_date`/
`end_date`，不新增 endpoint。

## 18. Persistence Identity 用真正 start/end

`ga4_geo_range_stats` 的 `UNIQUE(store_id, property_id, range_start_date,
range_end_date, raw_location_key, metrics_version, event_mapping_version)`
本來就是用實際日期做身分識別，不是 `range_key='custom'` 這種粗粒度標籤。
Stage 4.2 用真實 SQLite + `mockAdapter` 驗證 90d/180d/多個 custom range
互不覆蓋、互不錯讀（`scripts/run-g1-6-ga4-h1-4-persisted-range-runtime.js`，
23/23）。

## 19. No DB Schema Change

`utils/db.js` 在完整 `diff -rq` 對照 H1.3 baseline 下**逐位元組相同**，沒有
新增欄位、沒有新增 migration。

## 20. GeoRangeControl Architecture

`public/js/geo-range-control.js`（新檔）：`GeoRangeControl.mount(containerId,
{state, onChange})`。純 UI 關注點（render／mode 切換／驗證訊息／day count），
不做 fetch／Leaflet／Sync；驗證結果完全來自 `resolveGeoHistoricalRange()`。
Dashboard 與 Heatmap H1 Historical 共用同一份控制項程式碼，各自傳入獨立的
`rangeState` 物件參考，State Isolation 由「傳了不同物件」這件事本身保證。

## 21. H1 Read/Sync 用同一個 Resolved Range

`geo-ga4-h1-panel.js` 新增 `_geoGa4H1ResolveRange()`，Historical Read
（`geoGa4H1Refresh`）與 Manual Sync（`syncHandler`）唯二都呼叫它，兩者對
同一個 range selection 使用完全相同的 `apiRange`/`startDate`/`endDate`。

## 22-24. Dashboard GET-only / apiFetch / No Automatic Sync

`geo-ga4-dashboard-layer.js` 只 `GET /api/analytics/ga4-geo/history`，透過
既有 `apiFetch`（不 bare fetch，H1.1 曾因 bare fetch 造成 Production 401，
本輪沒有重犯）。永不 `POST sync`、不碰 Realtime endpoint、不直接呼叫
Google Client——這些都在 Mutation（`DASHBOARD-CALLS-SYNC`／
`DASHBOARD-BARE-FETCH`）與 Static Audit（Category M）裡用原始碼層級與
行為層級雙重驗證。

## 25. Friendly Labels vs Actual Date Range

Dashboard 標題用口語化 Friendly Label（「近 7 天」等 8 個 preset 對照表），
底下小字仍保留 `resolveGeoHistoricalRange()` 算出的實際日期區間，兩者
語意分開、互不取代，沒有回頭修改 Stage 3 已凍結的 Resolver 資料層 Contract。

## 26. Original Bug Runtime 結果

`scripts/run-g1-6-ga4-h1-4-map-state-runtime.js`（99/99）精準重播三個
原始生產環境 bug 場景：

- **ORIGINAL-1**：Direct Dashboard Load——Fresh 環境，`activeTab=dashboard`，
  完全不進 Heatmap，Dashboard 自己 GET persisted 7d rows，4 個 marker
  正確出現。
- **ORIGINAL-2**：Heatmap H1 → Dashboard——H1 markerGroup 確認先
  `map.hasLayer===true`，切到 Dashboard 後變成 `false`，Dashboard 自己的
  layerGroup 變成 `true`，且 marker 內容來自 Dashboard 自己的 GET，不是
  沿用 H1 的 markerGroup。
- **ORIGINAL-3**：Reload Determinism——全新 jsdom/state/map，同一份
  persisted data，Direct Load 的 marker count／label／range 與第一次
  Direct Load 完全一致。

## 27. Manual Sync Late Completion Leakage（Stage 6 發現）

Map State Runtime 撰寫過程中發現：Manual Sync 的 POST 沒有掛
AbortController（跟 Historical Read 用的 `currentAbort` 是不同請求），
`GeoGa4H1Panel.destroy()` 無法取消它。若使用者在 Sync 進行中切到
Dashboard，Sync 晚完成時仍會呼叫 `onChange()` 觸發 `geoGa4H1Refresh()`，
重新建立 `markerGroup` 並 `addLayer` 回共用地圖——這正是本輪從頭處理的
那類 Marker Leakage，只是發生在 Sync 而不是 Read 路徑。

## 28. Destroyed Guard（第一層）

`geoGa4H1State.destroyed`：`destroy()` 設 `true`、`init()` 解除為
`false`；Manual Sync 完成時檢查這個旗標，`true` 就不觸發 `onChange()`。

## 29. ABA Lifecycle Risk

單靠 `destroyed` 布林值有 ABA 風險：`destroy()` 後很快又 `init()`
（同一個 panel 快速重新 activate），`destroyed` 被 `init()` 重設回
`false`，讓「更早那一輪」尚未完成的 Sync 誤以為自己還屬於現在這個
session。

## 30. lifecycleGeneration Guard（第二層）

新增 `geoGa4H1State.lifecycleGeneration`，`init()`／`destroy()` 各自
單調遞增。Manual Sync 開始時 capture `capturedGeneration`，完成時同時
檢查 `destroyed===false` 且 `lifecycleGeneration===capturedGeneration`，
兩者都成立才處理結果（含 status 文字／onChange／marker 三種 side
effect，不是只擋 `onChange()`）。

`scripts/run-g1-6-ga4-h1-4-lifecycle-aba-runtime.js`（22/22）與
Mutation 的 `H1-DESTROYED-GUARD-REMOVED`／
`H1-LIFECYCLE-GENERATION-CHECK-REMOVED`／
`H1-GENERATION-CAPTURE-TIMING-WRONG` 三個獨立 scenario，各自證明兩層
防護都不是裝飾。

## 31. Server-Side Sync 可以完成，Stale Browser UI 不得套用結果

本輪的立場：Server 端已經正常完成同步、DB 也正常更新——這是允許的。
只是「已經是舊 session」的瀏覽器端不再把它套進任何 UI（不 render、不改
status、不觸發 refresh）。本輪沒有為了取消已經送出的 Server-side
request 去重寫 Sync API 或加 AbortController 到 POST /sync。

## 32-34. Mutation / Static / Full Regression 結果

見 Test Report（`R5.4-G1.6-GA4-H1.4-MAP-STATE_TEST_REPORT.md`）。

## 35a. Browser Entry Wiring Finding（Stage 7C）

**分類：REAL PRODUCTION INTEGRATION GAP**（不是 Test-only bug，不是 stale
expectation，不是 Node/browser 認知落差的無害巧合）。

自動化測試從一開始就是用 jsdom `eval()` 手動組裝依賴順序，或 Node
`require()` fallback 去驗證 Production JS 檔案本身的邏輯——這些方式從未
真正讀取、解析 `public/index.html` 裡實際的 `<script src="...">` 清單。
Production Diff review 第一次直接對 `public/index.html` 做逐位元組 `diff`
時才發現：`geo-range-resolver.js`／`geo-range-control.js`／
`geo-ga4-dashboard-layer.js` 三個 H1.4 新檔案，從沒被加進正式頁面的
`<script>` 載入清單。這三個檔案的頂層程式碼會在「解析當下」用
`typeof resolveGeoHistoricalRange === 'function'` 這類檢查決定要不要走
Node `require` fallback——這個 fallback 只在 Node 測試環境有效，真實瀏覽器
沒有 `require`，也沒有這三支腳本。結論：**在修正之前，真實部署的頁面上
H1.4 的 Range Resolver／Range Control／Dashboard GA4 三項功能完全不會被
啟用**，儘管當時 Static Audit 216/216、Map State Runtime 99/99、Mutation
30/30 全部通過——因為這些測試從來沒有以 `public/index.html` 作為真實入口。

**歷史記錄（如實保留，不覆寫）：**

| 階段 | Static Audit | 說明 |
|---|---|---|
| Pre-entry-gate（Stage 7A 初版） | 216/216 PASS ×3 | Category Q 當時只驗證了 JS 檔案之間彼此的 `typeof` 安全 guard（例如 `geo-range-control.js` 對 `resolveGeoHistoricalRange` 的檢查），**沒有**直接讀取 `public/index.html` 本身，因此無法偵測「檔案根本沒被載入頁面」這種缺口。 |
| Post-entry-gate（Stage 7C 修正後，Final） | **227/227 PASS ×3** | 新增 Q1-Q11：直接解析 `public/index.html` 的 `<script src>` 清單與位置，確認三個新檔案存在、順序正確、沒有重複載入。 |

修正內容：

1. 在 `public/index.html` 插入正確順序的三個 `<script>` 標籤（見上一節）。
2. 新增 `scripts/run-g1-6-ga4-h1-4-browser-entry-runtime.js`（Stage 7C，
   24/24 PASS ×3）：直接解析 `public/index.html` 的真實 `<script src>`
   順序（不是測試自己手刻一份順序），依這個真實順序把對應的
   `public/js/*.js` 內容 eval 進同一個 jsdom window scope，**不提供 Node
   require**（避免真實瀏覽器缺口被 Node fallback 悄悄掩蓋），驗證
   `resolveGeoHistoricalRange`／`GeoRangeControl`／
   `geoDashboardGa4Activate`／`GeoGa4H1Panel` 四個全域符號真的存在且可用，
   並包含 4 個 Negative Control（移除 resolver／移除 control／移除
   dashboard／顛倒 resolver-control 順序，每一個都必須讓對應症狀重現，
   證明正面測試不是巧合）。
3. `scripts/static-audit-g1-6-ga4-h1-4.js` 的 Category Q 新增 Q1-Q11，
   直接讀 `public/index.html` 字串位置做比對，跟原本 Q185-189 的
   JS-level guard 檢查互補，不是取代。

**Final Regression（含此修正）：57 suites × 3 獨立 process，逐行結果完全
一致，exit=0，0 known baseline mismatch，0 residue。**（原本 56 suites ×3
只能算 Pre-Browser-Entry finalization regression，不是最終 package
regression——這份 57 suites 的結果才是。）

## 35b. Production Diff（最終確認，仍是 9 files，Browser Entry Gate 本身沒有新增 Production 檔案）

Production Modified（5）：
`public/index.html`、`public/js/date-time-format.js`、
`public/js/geo-ga4-h1-panel.js`、`public/js/geo-heatmap-ui.js`、
`public/js/geo-intelligence.js`、`services/ga4GeoSyncService.js`

（註：上面刻意列了 6 個檔名，含 index.html——見下方「重要發現」。）

Production Added（3）：
`public/js/geo-ga4-dashboard-layer.js`、`public/js/geo-range-control.js`、
`public/js/geo-range-resolver.js`

**合計 Production Diff：9 files。**

### 重要發現：`public/index.html` 原本沒有被列入 Production Diff 預估

在做本節「以真實 diff 為唯一依據」的檢查時發現：`public/js/geo-range-resolver.js`、
`public/js/geo-range-control.js`、`public/js/geo-ga4-dashboard-layer.js`
三個新檔案**從未被加進 `public/index.html` 的 `<script>` 載入清單**。這三個
檔案的頂層程式碼會在「解析當下」（不是延後到函式呼叫時）用
`typeof resolveGeoHistoricalRange === 'function'` 這類檢查決定要不要
require fallback——這個 fallback 只在 Node 測試環境（`typeof require ===
'function'`）有效，在真實瀏覽器裡沒有 `require`，也沒有這幾個腳本，所以
在修正前，H1.4 整個 Range Resolver／Range Control／Dashboard GA4
功能在真實部署頁面上完全不會生效，儘管所有自動化測試（jsdom eval / Node
require 手動組裝依賴）全部通過。

這是本輪 Static Audit（216 checks）跟所有 Runtime 測試都沒有抓到的缺口，
因為測試從來沒有讀取真實 `public/index.html` 逐一比對載入順序——Category Q
（Script Dependency）只驗證了「程式碼裡有沒有安全 typeof guard」，沒有驗證
「真的頁面有沒有把檔案載進來」。

已修正：在 `public/index.html` 插入正確順序的三個 `<script>` 標籤：

```
date-time-format.js
→ geo-range-resolver.js（新增）
→ geo-range-control.js（新增）
→ geo-intelligence.js
→ ...（既有既有中段檔案不變）...
→ geo-heatmap-ui.js
→ ...
→ geo-ga4-dashboard-layer.js（新增，插在 geo-ga4-h1-panel.js 之前）
→ geo-ga4-h1-panel.js
```

修正後重跑 Full Regression（56 suites，單輪）確認全部維持 exit=0（這組
Runtime 測試本身用 jsdom eval 手動組裝依賴順序，不會因為真實 HTML 修正
而改變結果——這正是這個缺口一直沒被自動化測試發現的原因，Manual QA
Checklist 因此把「Console 無新 Error」跟「Dashboard 真的有 marker」列為
部署後必須人工確認的第一優先項目）。

## 36. Frozen Scope（最終，byte-identical 對照 H1.3 baseline）

`utils/ga4Realtime/requestBuilder.js`、`requestPair.js`、`connectionTest.js`、
`index.js`、`client.js`（GA4 Credential Loader）、`errors.js`、
`utils/taiwanGeoNormalize.js`、`middleware/storeGuard.js`、
`middleware/featureGate.js`、`middleware/adminGuard.js`、
`middleware/licenseGuard.js`、`utils/lineMemberAuth.js`、
`utils/authoritativeAdminPointCatalog.js`、`routes/migration.js`、
`utils/migrationUploadLimit.js`、`utils/db.js`（DB Schema）——全部逐位元組
確認與 H1.3 baseline 相同，`cmp` 零差異。沒有 migrations/ 目錄（DB Schema
管理方式本身就是 `utils/db.js` 內的 `CREATE TABLE IF NOT EXISTS`，本輪
唯一改動只有 `services/ga4GeoSyncService.js` 的 `CUSTOM_RANGE_MAX_DAYS`
常數）。
