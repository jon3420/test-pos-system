# R5.4-G1.6-GA4-H1.2-UNIQUE-ADMIN — Implementation Report

## 一、為什麼不用 368 筆白名單

`DISTRICT_PARENT_ALIASES` 的手動白名單模式每新增一個行政區就要人工判斷、人工新增一行，且需要重複驗證是否全國唯一——這正是全台 368 個鄉鎮市區都可能發生的問題（需求文件已列出 Pingzhen/Yangmei/Zhongli/Bade/Luzhu/Xindian/Sanchong 等，且明說「其他未列入手動白名單的台灣鄉鎮市區」也會遇到）。若逐一手寫 368 筆，等同**複製一份資料集的別名索引**，且未來資料集更新（例如行政區劃調整）時，手動表會與權威資料集脫節，形成「兩份真相來源」的維護風險，也違反需求文件三「不得建立第二套行政區資料」的明確要求。

## 二、為什麼沿用 `_subdivisionNameIndex`

`utils/taiwanGeoNormalize.js` 的 `_buildIndexes()` 早已為 368 筆 subdivision 建立好 `_subdivisionNameIndex`（`normalize(alias) → subdivision[]`），且 `resolveTaiwanAdministrativeArea()` 既有的「裸鄉鎮市區名稱、無縣市提示」分支已經在用同一份索引做「全國是否唯一」的判斷（`uniqueCounties.size === 1` 才回傳，`> 1` 則回傳 `ambiguous`）。這代表：

1. 「全國唯一性」的判斷邏輯與資料**在專案裡已經存在且已被其他正式路徑驗證**，不是新概念。
2. 重用它可以保證 H1.2 的唯一性語意與既有 A1.2/H1 的既有語意**完全一致**，不會出現「同一個名稱在 A 功能是唯一、在 B 功能是歧義」的不一致風險。
3. 完全不需要另外掃描 JSON、另外建索引——降低記憶體用量與維護面。

## 三、`resolveUniqueSubdivisionParentCounty(rawName)` Contract

```
輸入：rawName（string | null | undefined）
輸出：
  { status: 'unknown',   candidates: [] }
  { status: 'ambiguous', candidates: [{county_code, county_name, subdivision_code,
                                        subdivision_name, canonical_name,
                                        administrative_type, source}, ...] }
  { status: 'unique',    county_code, county_name, subdivision_code, subdivision_name,
                          subdivision_type, canonical_name, source: 'authoritative_unique_subdivision' }
```

呼叫端目前只有 `utils/ga4Realtime/index.js`，只消費 `status === 'unique'` 分支的 `county_code`/`county_name`；`ambiguous`/`unknown` 一律視為未解析，交由呼叫端決定（目前是 unmapped）。

## 四、Alias Normalization

輸入正規化只做以下確定性、無猜測的字串處理（依序）：

1. `String(s).normalize('NFC')` — Unicode 安全正規化
2. `.replace(/[\t\n\r]+/g, ' ')` — tab／換行正規化成空白
3. `.trim()` — 前後空白
4. `.replace(/\s+/g, ' ')` — 連續空白折疊成單一空白
5. `.toLowerCase()` — 英文大小寫不敏感

比對命中既有 `_subdivisionNameIndex` 後才視為候選；若原始未命中且字串以 `Dist.`（大小寫不敏感、允許句點）結尾，才嘗試把 `Dist.` 展開成 `District` 後重查一次——這是資料集裡本來就存在的 `District` 別名的**確定性縮寫展開**，不是新增資料、不是模糊比對。除此之外**不做**任何字尾移除（strip）、不做 `.includes()`/`.startsWith()` 之類的包含式比對、不使用 Levenshtein 或任何相似度演算法。

## 五、Unique Candidate Rule

```js
const uniqueCodes = Array.from(new Set(candidates.map((c) => c.subdivision_code)));
if (uniqueCodes.length > 1) { /* ambiguous */ }
```

判斷依據是「候選對應到幾個**不同的** `subdivision_code`」，不是「候選陣列長度」。這正確處理了需求文件五、D 項的情境：同一個 subdivision 若因為資料集內同時存在中文簡稱與英文全名等多筆 alias record 而在索引裡出現多次，這些 record 全部指向同一個 `subdivision_code`，`uniqueCodes.length === 1`，仍視為安全的唯一候選——不會被誤判成歧義。

## 六、Ambiguity Protection

`uniqueCodes.length > 1` 時，回傳 `status: 'ambiguous'` 並附上每個候選的完整資訊（縣市、subdivision、code、來源標記），**但完全不回傳任何可用的 `county_code`/`county_name` 頂層欄位**，呼叫端無法誤用。呼叫端（`ga4Realtime/index.js`）只在 `status === 'unique'` 時才建構 `county` 物件，`ambiguous`/`unknown` 兩種狀態下 `county` 保持未賦值，該筆 row 最終進入 `unmapped`。已用 Mutation Test A/B 驗證：若移除唯一性檢查或在多候選時仍取第一筆，`Taoyuan District`／`Dongshan` 等真實歧義名稱會被錯誤映射（Mutation 版本會 FAIL，正式版本 PASS）。

## 七、Unknown Protection

以下輸入一律回傳 `status: 'unknown'`，不猜測：

- `null`／`undefined`
- 空字串／純空白字串
- 正規化後等於 `(not set)`／`unknown`／`null`／`undefined`（大小寫不敏感）
- 正規化後在 Catalog 索引中查無任何候選（含 `Dist.` 展開後仍查無）的任意字串（例如 `Fake District`）

## 八、Non-TW 排除順序（未修改，僅重申既有既有邏輯）

`_aggregateCityRows()` 的 `countryId !== 'TW'` 排除檢查在原始碼順序上早於**全部**縣市/行政區解析呼叫（`normalizeCounty`／`normalizeDistrictToParentCounty`／`resolveUniqueSubdivisionParentCounty`），命中即 `continue`，`excludedNonTw += 1`。本輪新增的 Unique Resolver **完全沒有機會**在 non-TW row 上被呼又到。已用 Runtime Test（US + Pingzhen District → excludedNonTw=1, counties.length=0）與 Mutation Test 驗證。

## 九、Legacy Override Compatibility

呼叫順序刻意維持：

```
normalizeCounty → normalizeDistrictToParentCounty（LEGACY_EXPLICIT_OVERRIDE）
              → resolveUniqueSubdivisionParentCounty（AUTHORITATIVE_UNIQUE_RESOLUTION）
              → unmapped
```

`normalizeDistrictToParentCounty`（R5.4-G1.5-B2.5 既有函式）**完全未被修改**，其手動白名單 `DISTRICT_PARENT_ALIASES` 也**完全未被修改**（包含已知的 `Taoyuan District` Catalog-ambiguous-but-legacy-explicit 案例）。因為它在呼叫順序上優先，`"Longtan District"`／`"Taoyuan District"` 這兩組既有別名的行為**與修正前逐位元組相同**——已用既有的 `smoke-hotfix30-b5-r5-4-g1-5-b2-5-district-normalization.js`（76/76 PASS，未修改）重新驗證確認無迴歸。

## 十、Aggregator Wiring

```diff
- const county = normalizedCity
+ let county = normalizedCity
    ? (normalizeCounty(normalizedCity) || normalizeDistrictToParentCounty(normalizedCity))
    : null;
+ if (!county && normalizedCity) {
+   const uniqueResolved = resolveUniqueSubdivisionParentCounty(normalizedCity);
+   if (uniqueResolved && uniqueResolved.status === 'unique') {
+     county = { county_code: uniqueResolved.county_code, county_name: uniqueResolved.county_name };
+   }
+ }
```

只在既有兩層 fallback 都未命中時才呼叫 Unique Resolver，且只在 `status === 'unique'` 時才賦值——三個判斷式短路求值的行為與既有 `||` 鏈語意一致，新增的 `if` 區塊是純粹的第三層 fallback，不影響前兩層任何既有行為。

## 十一、Summary Active Users Non-Additive Semantics（重申，未修改）

`totalActiveUsers`／`totalEventCount` 只計算自 `summaryResult`（無 dimensions 的 Summary Request），本輪**未新增、未修改**任何 `counties.reduce(...)` 或等價的「從 City rows 加總」邏輯。已用 Static Audit check（`/totalActiveUsers\s*=\s*summaryRow/` 存在、`/totalActiveUsers\s*=\s*counties\.reduce/` 不存在）與 Mutation Test G 驗證。

## 十二、Mutation Negative（10 組，A–J，全數已在 Smoke Suite 中實作並驗證）

| # | 情境 | 驗證方式 | 結果 |
|---|---|---|---|
| A | 移除 unique candidate count 檢查 | 手寫一份「取第一筆不檢查唯一性」的 mutated 函式，對比 `Taoyuan District` 上 mutated 版本會誤映射、real 版本正確回 ambiguous | PASS |
| B | candidates.length>1 仍取第一筆 | 對 `Dongshan` 驗證 real 版本回 ambiguous（不像 mutation 取第一筆會誤判） | PASS |
| C | 移除 countryId TW 檢查 | `US + Pingzhen District` 驗證 real 版本正確 excludedNonTw=1, counties=0 | PASS |
| D | Fake District 經通用 strip 後被接受 | `resolveUniqueSubdivisionParentCounty('Fake District')` 驗證回 unknown | PASS |
| E | Pingzhen 錯誤 parent 改成新北市 | 驗證 real 版本 `county_name==='桃園市'` | PASS |
| F | Banqiao 錯誤 parent 改成桃園市 | 驗證 real 版本 `county_name==='新北市'` | PASS |
| G | Summary total 改從 City Row 加總 | 驗證原始碼中不存在 `totalActiveUsers = counties.reduce` 這段程式碼 | PASS |
| H | 另建第二套硬編碼 Mapping | 驗證原始碼中不存在「連續多行 `'桃園市':'...'` 5 碼 code→縣市名」硬編碼表樣式 | PASS |
| I | 使用 includes／模糊 matching | 驗證函式本體（去除註解）不含 `.includes(` | PASS |
| J | 跳過 Catalog 使用手動 fallback | 驗證函式本體一定呼叫 `_buildIndexes()` | PASS |

## 十三、Production Diff（完整逐行內容）

### `utils/taiwanGeoNormalize.js`（新增 93 行，全部為新函式與其匯出；既有函式一行未動）

```diff
@@ -138,6 +138,98 @@
   return code ? _byCountyCode.get(code) : null;
 }
 
+function _normSubdivisionAliasInput(s) {
+  if (s === undefined || s === null) return '';
+  let v = String(s);
+  if (typeof v.normalize === 'function') v = v.normalize('NFC');
+  v = v.replace(/[\t\n\r]+/g, ' ');
+  v = v.trim();
+  v = v.replace(/\s+/g, ' ');
+  return v.toLowerCase();
+}
+
+function _subdivisionCandidatesForAlias(normalizedKey) {
+  _buildIndexes();
+  if (!normalizedKey) return [];
+  return _subdivisionNameIndex.get(normalizedKey) || [];
+}
+
+const _UNMAPPABLE_LITERALS = new Set(['(not set)', 'unknown', 'null', 'undefined', '']);
+
+function resolveUniqueSubdivisionParentCounty(rawName) {
+  _buildIndexes();
+  if (rawName === undefined || rawName === null) return { status: 'unknown', candidates: [] };
+  const trimmedRaw = String(rawName).trim();
+  if (!trimmedRaw) return { status: 'unknown', candidates: [] };
+
+  const normalized = _normSubdivisionAliasInput(rawName);
+  if (_UNMAPPABLE_LITERALS.has(normalized)) return { status: 'unknown', candidates: [] };
+
+  let candidates = _subdivisionCandidatesForAlias(normalized);
+
+  if (!candidates.length && /\bdist\.$/.test(normalized)) {
+    const expanded = normalized.replace(/\bdist\.$/, 'district');
+    candidates = _subdivisionCandidatesForAlias(expanded);
+  }
+
+  if (!candidates.length) return { status: 'unknown', candidates: [] };
+
+  const uniqueCodes = Array.from(new Set(candidates.map((c) => c.subdivision_code)));
+  if (uniqueCodes.length > 1) {
+    return {
+      status: 'ambiguous',
+      candidates: candidates.map((c) => ({
+        county_code: c.county_code, county_name: c.county_name,
+        subdivision_code: c.subdivision_code, subdivision_name: c.subdivision_name,
+        canonical_name: c.subdivision_name_en || c.subdivision_name,
+        administrative_type: c.subdivision_type, source: 'authoritative_taiwan_administrative_areas',
+      })),
+    };
+  }
+
+  const sub = candidates[0];
+  const county = _byCountyCode.get(sub.county_code) || null;
+  return {
+    status: 'unique',
+    county_code: sub.county_code,
+    county_name: (county && county.county_name) || sub.county_name,
+    subdivision_code: sub.subdivision_code,
+    subdivision_name: sub.subdivision_name,
+    subdivision_type: sub.subdivision_type,
+    canonical_name: sub.subdivision_name_en || sub.subdivision_name,
+    source: 'authoritative_unique_subdivision',
+  };
+}
+
 function _buildIndexes() {
   ...
@@ -598,6 +690,9 @@
   normalizeDistrictToParentCounty,
   DISTRICT_PARENT_ALIASES,
+  resolveUniqueSubdivisionParentCounty,
   normalizeTaiwanGeo,
   TAOYUAN_DISTRICTS,
```

### `utils/ga4Realtime/index.js`（新增 14 行；既有行為僅在「前兩層都未命中」時才多一層檢查）

```diff
@@ -15,7 +15,7 @@
-const { normalizeCounty, normalizeDistrictToParentCounty } = require('../taiwanGeoNormalize');
+const { normalizeCounty, normalizeDistrictToParentCounty, resolveUniqueSubdivisionParentCounty } = require('../taiwanGeoNormalize');
@@ -161,9 +161,22 @@
-    const county = normalizedCity
+    let county = normalizedCity
       ? (normalizeCounty(normalizedCity) || normalizeDistrictToParentCounty(normalizedCity))
       : null;
+    if (!county && normalizedCity) {
+      const uniqueResolved = resolveUniqueSubdivisionParentCounty(normalizedCity);
+      if (uniqueResolved && uniqueResolved.status === 'unique') {
+        county = { county_code: uniqueResolved.county_code, county_name: uniqueResolved.county_name };
+      }
+    }
```

（完整未截斷 diff 另存於打包內 `scripts/` 旁的 patch 純文字，如需完整逐行內容可對 ZIP 內 `utils/taiwanGeoNormalize.js`／`utils/ga4Realtime/index.js` 與已交付的 H1.1-AUTH 版本直接 `diff` 重現，兩者皆為公開交付內容。）

### 其餘檔案異動（非 Production，範圍符合本輪凍結要求）

| 檔案 | 類型 | 說明 |
|---|---|---|
| `scripts/audit-taiwan-unique-subdivision-aliases.js` | 新增 | Audit script |
| `scripts/smoke-hotfix30-b5-r5-4-g1-5-b2-6-unique-subdivision-normalization.js` | 新增 | Smoke test（82 assertions + Mutation A–J） |
| `scripts/static-audit-g1-6-ga4-h1-2.js` | 新增 | Static audit（36 checks） |
| `scripts/run-regression-g1-6-ga4-h1-2.js` | 新增 | H1.2 Regression Runner |
| `scripts/run-regression-g1-6-ga4-h1.js` | 修改 | 修正兩個 stale expected 數字（73→81、166→190），屬 Category B Outdated Assertion bug fix，見 Test Report 第五節 |
| 4 份 `.md` 文件（本文件與另 3 份） | 新增 | 交付文件 |

## 十四、沒有第二套 Catalog

`data/` 目錄內與行政區相關的檔案僅有 `taiwan-administrative-areas.json` 與 `taiwan-administrative-areas.manifest.json`（本輪一個位元組都未修改），已由 Static Audit check 4／59（Smoke）與 `find`/`ls` 實測確認，無第二份行政區 JSON、無第二套 368 筆硬編碼 Mapping。
