# fix18-10-hotfix30-B5-R5.4-G1.4 — Geo Map Label Rendering & Honest Drawable-State Fix

## 1. Scope

本輪只處理五件事：
1. 有合法 Geo 資料但地圖標示（District Label）未顯示的 Rendering Bug
2. District Label／Exact Marker／Circle／Tooltip 的顯示邏輯
3. Order Heatmap 與 Visitor Layer 切換時 Label 同步顯示/隱藏
4. 沒有 Drawable Geo 時顯示誠實 Overlay，不畫假 Marker
5. Dark Theme 白色空白橫條

不重做 G1／G1.1／G1.2／G1.3／G1.3.1／G1.3.2，不修改訂單／營收統計口徑，
不建立第二張 Map／第二個 Tile Layer，不開始 G2。

## 2. Permanent Label Root Cause

`geoHeatRenderLayer()`（`public/js/geo-heatmap.js`）建立 marker/circle 之後，
只呼叫了：

```js
marker.bindTooltip(geoHeatBuildTooltipContent(area, ...));
```

Leaflet 的 `bindTooltip()` 預設是 **hover-only**——滑鼠移過去才顯示，移開就
消失。地圖靜止不動時，使用者看到的是一個個沒有名字的色點/圖釘，必須逐一
把滑鼠移過去才知道那是哪個行政區。真實使用情境下，這很容易被誤認為
「標示沒有顯示」或「Rendering 壞了」，即使背後的資料與座標其實完全正確。

## 3. 原本只有 Hover Tooltip 的原因

這不是刻意的設計選擇，單純是最初實作時只考慮了「點擊/hover 查看完整
資訊」這個互動情境，沒有考慮「使用者一眼掃過地圖就要能看出每個點是哪個
行政區」這個更基本的可用性需求。兩者不衝突，可以並存。

## 4. Hover Tooltip 與 Permanent Tooltip 差異

| | Hover Tooltip（既有） | Permanent Tooltip（G1.4 新增） |
|---|---|---|
| 觸發時機 | 滑鼠移到 marker 上才顯示 | 一直顯示，不需要互動 |
| 內容 | 完整資訊：行政區、Orders、Revenue、Coverage、Channel | 只有行政區名稱（避免地圖太擠） |
| 綁定方式 | `marker.bindTooltip(content)` | 獨立的 `L.tooltip({permanent:true}).setLatLng().setContent().addTo(group)` |
| 是否互動 | 是（會擋住底下的點擊，Leaflet 預設行為） | 否（`interactive:false`，`pointer-events:none`，不擋 marker 點擊） |

兩者是**兩個獨立物件**，不是同一個 tooltip 的兩種模式——permanent label
是額外新建的 `L.tooltip()` 實例，跟 marker 一起 `group.addLayer()`，不會
互相覆蓋或取代對方。

## 5. geoHeatComputeDrawableState()

新增純函式（`public/js/geo-heatmap.js`），只讀 `areas`／`businessTotals`，
不碰 DOM、不碰 Leaflet，additive，不修改／不取代 G1.3.1 既有的 Coverage
Explanation 四態（那是「文字說明」用的狀態機，這裡是給「地圖要畫什麼」
用的狀態機，兩者用途不同、互不覆蓋）。

```js
function geoHeatComputeDrawableState(areas, businessTotals) { ... }
```

## 6. 五種 Drawable State

| State | 條件 | 地圖上會發生什麼 |
|---|---|---|
| `no_business_data` | 全店這段期間沒有訂單 | 不顯示 Order Overlay（Coverage Explanation 卡片已經講清楚了） |
| `has_business_but_no_drawable_geo` | 有訂單，但沒有任何行政區有已知地理資料 | 顯示 Honest Overlay：「今日已有 N 筆訂單，但目前沒有訂單包含可用的地理資料，因此無法顯示地圖標示。」 |
| `has_drawable_district_only` | 至少一個行政區「知道名稱」但沒有平均座標 | 顯示提示：地圖上畫不出來，請看 Ranking 列表 |
| `has_drawable_exact_only` | 所有已知行政區都有平均座標 | 正常畫 Marker/Circle + Permanent Label，不顯示 Overlay |
| `has_mixed_drawable_geo` | 上面兩種同時存在 | 有座標的正常畫，沒座標的只在 Ranking 顯示，不顯示 Overlay（地圖不是空的） |

## 7. Permanent District Label Render Path

```
geoHeatRenderLayer(areas, metric, display)
  └─ plottable = areas.filter(coordinate_source === 'order_centroid' && lat/lng 為數字)
       └─ 對每個 plottable area：
            ├─ 建立 marker/circleMarker（既有邏輯，未修改）
            ├─ marker.bindTooltip(完整內容)（既有 hover tooltip，未修改）
            ├─ group.addLayer(marker)（既有邏輯，未修改）
            └─ 新增：L.tooltip({permanent:true, className:'geo-heat-map-label', interactive:false})
                       .setLatLng([area.lat, area.lng])   ← 同一組真實座標，不是另外算的
                       .setContent(_geoHeatEsc(area.area_name))
                       group.addLayer(labelTooltip)       ← 跟 marker 掛在同一個 group
```

Label 跟 marker 用同一組真實座標（`area.lat`/`area.lng`，已經是後端
`order_centroid` 真實平均座標），不是另外計算或冒充的座標。

## 8. Exact Coordinate Marker 原則

- 只有 `coordinate_source === 'order_centroid'` 且 `lat`/`lng` 是合法數字
  的行政區才會建立 marker/circle 與 permanent label。
- `coordinate_source` **只信任後端算好的值**，前端不重新判斷、不自己算
  centroid——這是 G1.3.1 就確立的唯一合法座標來源原則，G1.4 沿用，不
  新增第二套判斷。

## 9. Unknown 不畫點

`city`/`district` 都是 `null` 的「未知」行政區，`coordinate_source` 依然
是 `unavailable`（後端沒有座標可算），不會進入 `plottable` 陣列，不會被
畫出任何 marker 或 label。

## 10. Honest Order Overlay

新增 `_geoHeatUiRenderOrderMapOverlay()`（`public/js/geo-heatmap-ui.js`），
對稱於 G1.2 既有的 Visitor Overlay，直接沿用 `geoHeatComputeDrawableState()`
判斷是否需要顯示：

- Orders：「今日已有 1 筆訂單，但目前沒有訂單包含可用的地理資料，因此
  無法顯示地圖標示。」
- Revenue：「目前已有營收 NT$150，但目前沒有任何營收可歸屬到地理區域，
  因此無法顯示地圖標示。」

數字直接取自 `geoHeatState.businessTotals`，跟 G1.3.1 Coverage Explanation
同一組資料來源，不會兩處數字對不上。

## 11. Honest Visitor Overlay

`_geoHeatUiVisitorMapOverlayMessage()` 的主要句子改成跟 Order Overlay
統一的「因此無法顯示地圖標示」句型：

「目前已有 1 位訪客，但尚未取得可繪製到地圖上的地理資料，因此無法顯示
地圖標示。」

原本 G1.2 既有的 `Known District／Exact Coordinate／Unknown／Coverage`
診斷明細**保留**，作為補充說明的第二行，不是被取代。

## 12. Order／Visitor Layer Lifecycle

Permanent Label 不需要獨立維護一份 LayerGroup——它跟對應的 marker 一起
`group.addLayer()`，天生跟著既有的 `group.clearLayers()`／
`_geoHeatUiApplyLayerExclusivity()` 走：

- Layer Switch 時：`_geoHeatUiApplyLayerExclusivity()` 只做
  `map.addLayer(group)`/`map.removeLayer(group)`，不清空 group 內容——
  切走時 marker+label 一起從地圖上消失，切回來時原樣重新出現，不需要
  重新 render。
- `geoHeatRenderLayer()` 每次開頭 `group.clearLayers()`——marker+label
  一起被清空，重畫時一起重新建立，不會殘留。

## 13. Label Cleanup

- `clearLayers()` 會同時清除 marker 與 permanent label（兩者都是
  `group` 的子物件）。
- Rerender（連續呼叫 `geoHeatRenderLayer()` 多次）不會累積：每次都先
  `clearLayers()` 再重新畫，數量恆等於當次 plottable 的筆數。
- Layer Switch 時 Order Overlay／Visitor Overlay 互相清除殘留（已在
  `geoHeatUiSetLayer()` 補上對稱的清理呼叫，見 Known Limitations 之前
  的實作說明）。

## 14. 白色橫條 Root Cause

`.geo-heat-layer-toggle`／`.geo-heat-layer-btn` 這兩個 class 從 G1.2 建立
Order/Visitor Layer 切換按鈕以來，`geoHeatUiLayerToggleHtml()` 一直有
輸出這兩個 class 名稱，但 `public/css/geo-heatmap.css` 裡從來沒有對應的
CSS 規則。瀏覽器對沒有任何 CSS 的 `<button>` 套用預設樣式（多半是淺灰/白
底），在全站 Dark Theme 底下就是一條突兀的白色空白橫條。

## 15. Layer Toggle Dark Theme CSS

比照本檔案其餘控制項（`.geo-heat-ctl-btn`／`.geo-heat-tab-btn`）已經在
用的 CSS 變數＋深色 fallback 慣例補上樣式：

```css
.geo-heat-layer-btn {
  background: var(--bg-card, #222);
  color: var(--text-primary, #f0f0f0);
  border: 1px solid var(--border, #333);
}
.geo-heat-layer-btn.is-active,
.geo-heat-layer-btn[aria-pressed="true"] {
  background: var(--accent, #f5a623);
  color: #1a1a1a;
}
```

全部走 CSS 變數，不寫死 light-only 顏色，跨 Light/Dark Theme 通用，不需要
額外的 `[data-theme="dark"]` 覆寫區塊。

## 16. Fixture Timezone Root Cause

G1.3.1／G1.3.2 既有的真實 sql.js DB fixture 用
`datetime('now','localtime')` 產生 `created_at`。SQLite 的 `'localtime'`
修飾詞跟著「執行環境的作業系統時區」走——本沙盒容器系統時區是 UTC，但
應用程式的日期區間（`utils/dashboardDate.js` `resolveDateRange()`）永遠
用 Asia/Taipei（UTC+8）計算「今天」的起訖邊界。容器 UTC 時間跟 Taipei
時間在跨日附近（Taipei 00:00～UTC 16:00 這段）會落在不同公曆日，fixture
用容器時間寫入的 `created_at` 就可能落在 app 查詢範圍之外，導致
`business_total_orders` 等欄位查回 0——這是 **Test Fixture 時區問題**，
不是 `utils/geoAnalyticsQueries.js`／`utils/dashboardDate.js` 的 Bug，也
完全不影響真實部署環境（真實伺服器時區設定正確時不會有這個落差）。

## 17. geo-fixture-time.js

新增 `scripts/lib/geo-fixture-time.js`，不修改、不重新實作任何正式日期
規則——直接呼叫既有的 `utils/dashboardDate.js` `resolveDateRange()`，
拿到這次查詢真正會用到的 `range.startLocal`／`range.endLocal`，取「正
中間」當作 fixture timestamp，保證在任何情境下都落在查詢範圍內：

```js
function computeFixtureTimestamp(preset = 'today') {
  const range = resolveDateRange({ preset });
  return midpointLocalString(range.startLocal, range.endLocal);
}
```

不依賴 `process.env.TZ`、不依賴容器 OS 時區、不 hardcode 任何日期。已套用
到 `scripts/smoke-hotfix30-b5-r5-4-g1-3-1-coverage-total-dark-theme.js`
與 `scripts/smoke-hotfix30-b5-r5-4-g1-3-2-regression-guard.js` 的 fixture
insert 邏輯，取代所有 `datetime('now','localtime')`。

## 18. G1.4 Layered Scope Guard

`scripts/lib/geo-heatmap-g131-scope-guard.js` 新增第二層 Allowlist
`GEO_HEATMAP_G14_ALLOWED_ADDITIONS`（4 項，跟 G1.3.1 那層
`GEO_HEATMAP_G131_ALLOWED_ADDITIONS` 一樣是精確字串比對）：

1. `geoHeatComputeDrawableState()` 函式本體（含說明註解）
2. `geoHeatRenderLayer()` 上方的 Root Cause 說明註解
3. Permanent Label 建立邏輯（`L.tooltip({permanent:true...})` 那段）
4. `module.exports` 新增 `geoHeatComputeDrawableState`

`computeScopedBaselineCheck()` 內部依序先還原 G1.4 這一層、再還原 G1.3.1
那一層，最後必須等於同一個 `PRISTINE_BASELINE_SHA256`（`8f3ec8c0...`，
R5.3-A2/A1.2 那一輪的原始基線，**沒有被重新計算或更新**）。A2／A1.2 呼叫
的還是同一個函式名稱、同一個回傳格式，完全不需要修改呼叫端——這也是
本輪額外把 `scripts/static-audit-g1-2.js` 那個過時的「geo-heatmap.js
完全不得出現特定字串」檢查一併換成呼叫同一個 Layered Scope Guard 的
原因：同一套機制可以同時服務 A2、A1.2、G1.3.2 Smoke、static-audit-g1-2.js
四個消費端，不必四邊各自維護一份容易被合法修改誤傷的檢查邏輯。

## 19. Regression

見 `R5.4-G1.4_COMPLETION_REPORT.md` 完整表格；三輪完整 Regression 結果
完全一致，A2／A1.2 全綠，G1.3.1／G1.3.2 fixture 時區問題已修正並三次
穩定驗證。

## 20. Static Audit

`scripts/static-audit-g1-4.js`：52/52 OK。額外把
`scripts/static-audit-g1-2.js` 過時的整檔字串比對檢查換成 Layered Scope
Guard 呼叫，check 數量從 25 增加到 37（不降低、且增加了語意型檢查）。

## 21. Known Limitations

- Permanent Label 目前只顯示行政區名稱，沒有顯示數值（避免地圖太擠）；
  完整數值仍要靠 hover tooltip 查看。
- District-only（有訂單但無平均座標）的行政區無法在地圖上用任何形式
  呈現（沒有座標，畫了就是造假）——目前只能在 Ranking 列表看到，這是
  刻意設計，不是遺漏。
- `geo-fixture-time.js` 目前只服務「today」等既有 preset，若未來新增
  自訂日期範圍的 fixture 情境，需要對應擴充（介面已支援任意 preset
  參數，擴充成本低）。
- Manual Visual（真實瀏覽器 Permanent Label／Dark Theme／跨瀏覽器）狀態
  請見 `R5.4-G1.4_MANUAL_QA_CHECKLIST.md`，目前仍是 NOT TESTED。
