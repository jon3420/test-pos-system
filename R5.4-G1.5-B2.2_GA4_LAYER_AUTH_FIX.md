# R5.4-G1.5-B2.2 GA4 Layer Auth Fix

fix18-10-hotfix30-B5-R5.4-G1.5-B2.2 — GA4 Layer Store Authentication Hotfix

以 `fix18-10-hotfix30-B5-R5.4-G1.5-B2.1` 為基礎的緊急修正版。詳細背景與逐項
落地細節見同份交付內的 `R5.4-G1.5-B2.2_AUTH_REALITY_AUDIT.md`；本文件是給
複核者看的修正摘要。

## 一、正式環境重現

1. B2.1 Persistence Fix 已生效：Property／Stream 儲存後不再消失，店家
   啟用 Checkbox 正常保留，Settings 頁 Property／Stream 狀態顯示「已設定」。
2. 使用者切換到「GA4 即時訪客」圖層後失敗，畫面顯示：

   ```
   GA4 即時圖層載入失敗，請重試
   缺少店家登入 token，請重新登入
   ```

## 二、使用者已登入但 GA4 Layer 報 missing token

使用者當下確實已登入 `store_001`，Settings GET／PATCH 也運作正常——這代表
問題不是「真的沒登入」，而是 GA4 Layer 發出的請求本身沒有帶上任何登入
憑證，跟使用者是否登入無關。

## 三、Root Cause

`public/js/geo-ga4-realtime-layer.js`（修正前）有兩個裸 `fetch()`：

1. `geoGa4FetchData()` → `GET /api/geo-live/ga4-realtime`
2. `geoGa4FetchAndRender()` 內 inline status request →
   `GET /api/geo-live/ga4-realtime-status`

`/api/geo-live` 掛載 `requireStore` middleware，沒有 `Authorization` 或
`x-store-id` header 時一律回：

```json
{ "success": false, "error": "NO_STORE_TOKEN", "message": "缺少店家登入 token，請重新登入" }
```

裸 `fetch()` 完全不帶這些 header，所以無論使用者是否登入，這兩個請求
100% 都會被 `requireStore` 擋下，回傳跟正式環境畫面逐字相同的訊息。

## 四、Settings UI 為何正常

`public/js/geo-ga4-settings.js` 從一開始就是透過 `apiFetch()` 呼叫
`/api/settings/ga4-realtime`（GET/PATCH），`apiFetch()` 會自動帶
`Authorization: Bearer <token>` 與 `x-store-id`，所以能通過同一套
`requireStore` 驗證。

## 五、GA4 Layer 為何失敗

同樣的後端驗證機制（`requireStore`），差別只在前端呼叫方式：Settings UI
用 `apiFetch()`，GA4 Layer（修正前）用裸 `fetch()`。這不是「兩套不同的
認證系統」，是同一套系統，GA4 Layer 那邊沒有接上。

## 六、apiFetch 真實 Contract

實測 `public/js/app.js` 的 `apiFetch(url, options={})`：

| 情況 | 回傳 |
|---|---|
| 200／400／500 等非 401/403 | **原生 fetch Response**（呼叫端自己 `await res.json()`） |
| 401 | `{ ok:false, status:401, body }`，`body` 已是解析過的 JSON（parse 失敗則為 `{}`），**沒有 `.json()`** |
| 403 | `{ ok:false, status:403, body }`，同上 |
| Network Error | 底層 `fetch()` reject，`apiFetch()` 本身沒 try/catch，直接往外丟 |
| AbortError | 同上，`name==='AbortError'` 原樣往外丟 |

這是需求文件列出三個選項中的 **C**（成功走原生 Response，401/403 走
特殊 `{ok:false,status,body}` 形狀），不是單一固定形狀。`geoGa4FetchData()`
／`geoGa4FetchStatus()` 的實作先判斷 `res.status === 401 || res.status
=== 403`，再決定要不要呼叫 `.json()`，完全對齊這個真實 Contract（B2.2
Smoke 的 B21-B30 直接呼叫這兩支函式驗證全部六種情況）。

## 七、Status Auth Fix

新增具名函式 `geoGa4FetchStatus()`（取代原本寫死在
`geoGa4FetchAndRender()` 裡的裸 fetch），改用：

```js
const res = await apiFetch('/api/geo-live/ga4-realtime-status', { method: 'GET' });
```

401/403 時安全回傳 `null`，讓呼叫端維持目前 `configAutoRefreshEnabled`
值，不阻擋主要的 Data Request，也不會跟 Data Request 重複顯示同一個
Auth 錯誤兩次。

## 八、Data Auth Fix

```js
const res = await apiFetch(url, { method: 'GET', signal });
if (res.status === 401 || res.status === 403) return geoGa4AuthErrorPayload(res.body);
if (typeof res.json !== 'function') return geoGa4NormalizeResponse(null);
const json = await res.json();
return geoGa4NormalizeResponse(json);
```

`geoGa4AuthErrorPayload(body)` 把 401/403 轉成獨立的
`status: 'auth_error'` payload，跟 GA4 Backend 錯誤（`status:'error'`／
`'disabled'`／`'not_configured'`）完全分開，不共用判斷邏輯與文案。

## 九、Auto Refresh Auth

`_geoGa4ScheduleAutoRefresh()` 排程的 timer 呼叫的是
`_geoGa4RunFetch(containerId, {refresh:false})`，跟手動 Refresh 走同一支
函式、同一條 `geoGa4FetchData()`／`apiFetch()` 路徑，沒有獨立的第二套
fetch 邏輯（B2.2 Smoke E54／G81 驗證）。

## 十、Manual Refresh Auth

`geoGa4Refresh(containerId)` 同樣呼叫 `_geoGa4RunFetch(...,
{refresh:true})`，跟 Auto Refresh 共用（B2.2 Smoke E53／G82 驗證）。

## 十一、AbortController

完全保留。`_geoGa4RunFetch()` 仍在每次呼叫前 `abort()` 舊 controller、
建立新 `AbortController`，`controller.signal` 傳進
`geoGa4FetchData({..., signal})`，再傳進
`apiFetch(url, {method:'GET', signal})`（B2.2 Smoke A7／E55／E56／G73／
G80 驗證，`geoGa4Deactivate()` 仍會 abort）。

## 十二、requestSeq

完全保留。`mySeq = ++geoGa4State.requestSeq` 與
`if (mySeq !== geoGa4State.requestSeq) return;` 未變動（B2.2 Smoke E57／
G79 驗證：較慢的舊請求不會覆蓋較快的新結果）。

## 十三、Auth Error Payload

```js
function geoGa4AuthErrorPayload(body) {
  const b = body || {};
  return {
    ok: false, status: 'auth_error', quota_status: 'unknown',
    fetched_at: null, cache_age_seconds: null, is_cached: false, is_stale: false,
    error_code: b.error || 'AUTH_REQUIRED',
    message: '店家登入狀態已失效，請重新登入後再試。',
    summary: { total_active_users_ga4: 0, event_count: 0, screen_page_views: null, mapped_counties: 0, unmapped_city_rows: 0, excluded_non_tw_rows: 0 },
    counties: [], unmapped: [], notices: [],
  };
}
```

## 十四、Error Separation

新增 `GEO_GA4_ERROR_MESSAGES`（跟 Settings UI 的
`GA4_SETTINGS_TEST_ERROR_MESSAGES` 用語一致）：

| error_code | 文案 |
|---|---|
| `ga4_realtime_disabled` | 店家設定已保存，但伺服器尚未開啟 GA4 即時功能。 |
| `credential_unavailable` | 伺服器尚未設定 GA4 憑證。 |
| `credential_invalid` | GA4 憑證格式錯誤。 |
| `permission_denied` | Service Account 沒有此 GA4 Property 的讀取權限。 |
| `property_not_found` | 找不到此 GA4 Property。 |
| `stream_filter_invalid` | Stream ID 無法套用於目前 Property。 |
| `quota_limited` | GA4 API 暫時達到使用限制。 |
| `ga4_timeout` | GA4 連線逾時，請稍後再試。 |
| `ga4_unavailable` | Google Analytics 暫時無法連線。 |

`geoGa4StatusMessage()`／`geoGa4MapOverlayMessage()`／notices renderer
一律先判斷 `status==='auth_error'`，才判斷其他狀態，兩邊文案互不覆蓋
（B2.2 Smoke D41-D50／C37-C39／G76-G78 驗證：GA4 Backend 錯誤不會被誤判
成 auth_error，反之亦然）。

## 十五、Global Disabled 文案

`status:'disabled'` + `error_code:'ga4_realtime_disabled'` →

> 店家設定已保存，但伺服器尚未開啟 GA4 即時功能。

跟 Settings UI（B2.1）用語一致。

## 十六、Credential Missing 文案

`status:'error'` + `error_code:'credential_unavailable'` →

> 伺服器尚未設定 GA4 憑證。

（Settings UI 對應語意的版本是「GA4 設定已保存，但伺服器憑證尚未設定。」，
GA4 Layer 這裡因為場景不同（Layer 本身沒有「設定」動作，只有「顯示」），
沿用既有 `not_configured` 分支已使用的簡短用語，語意一致、不與 Token
Error 混淆——見 Manual Flow 模擬場景 B。）

## 十七、B2.2 Smoke

`scripts/smoke-hotfix30-b5-r5-4-g1-5-b2-2-ga4-layer-auth.js`：

```
PASS:  95
FAIL:  0
TOTAL: 95
```

連續執行 3 次結果一致。涵蓋 apiFetch Wiring（A1-A20）、Response Contract
（B21-B30，直接呼叫 `geoGa4FetchData()`／`geoGa4FetchStatus()` 驗證 200/
401/403/malformed/Network/Abort/500 全部六種真實情況）、Authentication
（C31-C40）、GA4 Error Separation（D41-D50，涵蓋全部 9 種 GA4 錯誤碼＋
stale_cache）、Lifecycle（E51-E62，含 activate 順序、abort、requestSeq、
deactivate、loading reset）、正式流程場景（F63-F70）、Mutation Negative
（G71-G85，每項都是「若改回錯誤版本就會 FAIL」的真實可執行斷言，不是
純 regex 靜態掃描）。另外以獨立 jsdom 腳本執行需求文件六的場景 A–D
（`window.fetch` 故意設成會直接 throw，證明主要流程完全不依賴裸
`fetch()`），全數 PASS。

## 十八、Regression

`scripts/run-regression-b2-2.sh 3`，每支測試前清除 `data/pos.db`：

| Suite | Round 1 | Round 2 | Round 3 |
|---|---|---|---|
| B2.2 Smoke | 95/95 | 95/95 | 95/95 |
| B2.1 Smoke | 85/85 | 85/85 | 85/85 |
| B2 Smoke | 181/181 | 181/181 | 181/181 |
| B2 Static Audit | 82/82 | 82/82 | 82/82 |
| B2a Smoke | 106/106 | 106/106 | 106/106 |
| B1 Smoke | 168/168 | 168/168 | 168/168 |
| B1 Static Audit | 71/71 | 71/71 | 71/71 |
| G1.5-A Smoke | 140/140 | 140/140 | 140/140 |
| G1.5-A Static Audit | 77/77 | 77/77 | 77/77 |
| Geo Map Settings Smoke | 620/620 | 620/620 | 620/620 |
| Geo Settings UI Smoke | 157/157 | 157/157 | 157/157 |
| G1 geo-live Smoke | 212/212 | 212/212 | 212/212 |
| G1.4.1 Smoke | 149/149 | 149/149 | 149/149 |
| G1.4 Smoke | 148/148 | 148/148 | 148/148 |
| G1.3.2 Smoke | 148/148 | 148/148 | 148/148 |
| G1.2 Smoke | 83/83 | 83/83 | 83/83 |
| A2 Smoke | 229/229 | 229/229 | 229/229 |
| A1.2 Smoke | 189/189 | 189/189 | 189/189 |
| Static Audit G1.4.1 | 56/56 | 56/56 | 56/56 |
| Static Audit G1.4 | 52/52 | 52/52 | 52/52 |
| Static Audit G1.3.2 | 48/48 | 48/48 | 48/48 |

三輪數字完全一致，`FAIL=0`，`exit=0`。`node --check` 全部通過：
`geo-ga4-realtime-layer.js`／`geo-ga4-settings.js`／`geo-heatmap-ui.js`／
`geo-heatmap.js`／`utils/ga4RealtimeConfig.js`／`routes/settings.js`／
以及所有新增/修改的 Smoke 腳本。無 `data/pos.db` 殘留、無 timer 殘留
（jsdom env 各自獨立、每個測試區塊建立新的 `freshEnv()`）、無
`AbortController` 殘留（每個 env 各自獨立的 `geoGa4State`，測試結束不
互相共用）、無 fake fetch/apiFetch 殘留（每個 `freshEnv()` 呼叫都建立
全新 `window`）、無 Store 汙染、無 Credential 殘留。

**既有 B1／B2／B2a Smoke Test 更新說明**：這三份既有測試原本用假的
`window.fetch` 驗證 `geo-ga4-realtime-layer.js` 的行為，因為產品程式碼
已經改用 `apiFetch()`，測試的 mock 機制必須同步更新為同時暴露
`window.apiFetch`（沿用同一份 fixture queue，行為不變），否則測試會因為
「測試環境本身沒有跟上產品程式碼的呼叫方式」而失敗，不是產品邏輯壞掉。
唯一一處**斷言文字**更動：B1 Smoke 的 F1（`disabled` 狀態文案），舊斷言
檢查「尚未啟用」，因需求文件八要求的新文案是「店家設定已保存，但伺服器
尚未開啟 GA4 即時功能。」（不含「尚未啟用」四字），已同步更新斷言為檢查
「伺服器尚未開啟 GA4 即時功能」子字串，其餘既有 Smoke/Static Audit 的
判斷邏輯、fixture、預期數字全數未變動。

## 十九、Manual Visual

NOT TESTED（僅透過 jsdom 驗證 DOM／狀態邏輯，未在真實瀏覽器手動操作）。

## 二十、Real GA4 Live Test

NOT TESTED（未對真實 GA4 Property／Stream／Service Account 憑證做連線
測試，所有驗證皆用假的 `apiFetch`／`fetch` mock）。

## 二十一、Known Limitations

1. 本次修正只處理 GA4 Layer 前端的 Authentication 傳輸層（改用
   `apiFetch()`），未變動：GA4 Backend（`utils/ga4Realtime/*`）、
   `routes/geo-live.js`、`routes/settings.js`、
   `utils/ga4RealtimeConfig.js`、B2.1 Settings Transaction 邏輯、
   `middleware/storeGuard.js`。
2. `geoGa4FetchStatus()` 目前設計為「一次性讀取」，沒有帶
   `AbortController.signal`（跟 Data Request 不同）；這是既有設計延續
   （原本裸 fetch 版本也沒有 signal），非本輪新增的限制，若之後需要在
   快速切換 Layer 時中斷未完成的 status 請求，需另外評估。
3. Credential Missing 文案在 GA4 Layer 與 Settings UI 用語不完全逐字
   相同（見十六），語意一致但字面略有差異，屬於既有兩處各自獨立維護
   訊息的既知現象，未強制合併成單一常數來源。
4. 所有驗證皆在本地 sql.js／jsdom／假 `apiFetch` 環境下進行，未涵蓋
   Zeabur 實際部署環境下的 JWT 簽發、`localStorage` 實際持久化、瀏覽器
   真實 CORS／Cookie 行為等基礎設施層面差異。
