# R5.4-G1.6-GA4-H1 — Reality Audit

GA4 城市歷史統計、即時快照與行政區轉換地圖。本文件誠實記錄現有系統的真實
狀態，區分「已驗證」與「未驗證／READY FOR MANUAL DEPLOYMENT VERIFICATION」。

## 一、現有 GA4 Client 來源

`utils/ga4Realtime/client.js`（R5.4-G1.5-A 建立）是全專案唯一持有 Google
憑證邏輯與 SDK Client 的檔案。本輪（GA4-H1）新增 `runGa4Report()`，沿用
同一個 lazy singleton `_client`，**不建立第二個 Client**。

憑證來源優先序（既有邏輯，本輪未變更）：
1. `GOOGLE_APPLICATION_CREDENTIALS`
2. `GA4_SERVICE_ACCOUNT_JSON_BASE64`
3. `GA4_SERVICE_ACCOUNT_JSON`
4. 都沒有 → 交給 SDK 走隱含 ADC

**本輪修正（Bugfix）**：第 4 種情況在本專案的實際部署環境（Zeabur，非
GCP，沒有 metadata server）下，google-auth-library 嘗試隱含 ADC 解析會
產生一個跟呼叫端 await 鏈脫勾的背景 Promise，reject 後變成
`unhandledRejection`，實測會讓整個 Node process crash。修正後：`_getClient()`
在「真的要建立新 Client」且 `credentialStatus().available === false` 時
直接安全失敗，不嘗試呼叫 SDK。已存在的 `_setClientForTest()` 注入路徑完全
不受影響（Credential Guard Suite 17/17 驗證）。

## 二、Realtime API 使用方式

`runGa4RealtimeReport()` 呼叫 `client.runRealtimeReport()`，dimensions
`country/region/city`，metrics `activeUsers/eventCount`，時間窗
`minuteRanges: [{ startMinutesAgo: 29, endMinutesAgo: 0 }]`（30 分鐘滾動
窗）。

## 三、Historical runReport 使用方式

`runGa4Report()`（本輪新增）呼叫 `client.runReport()`，用 `dateRanges`
指定完整日期區間（today/yesterday/7d/30d/custom），不做「每日呼叫再加總」。

## 四、Audience／Funnel／Commerce 三查詢架構

| 查詢 | Dimensions | Metrics |
|---|---|---|
| Audience | country, region, city | activeUsers, newUsers, sessions |
| Funnel | country, region, city, eventName | eventCount |
| Commerce | country, region, city | transactions, purchaseRevenue |

三支查詢平行送出（`Promise.all`），Server-side 依 `raw_location_key`
（country+region+city 正規化字串）合併。任一查詢失敗仍保存其他成功查詢
的資料並標示 `partial`；三支全部失敗才視為整輪失敗。

**未驗證**：這三組 Dimensions/Metrics 組合是否被正式 GA4 Property 接受，
本輪完全沒有機會對真實 GA4 API 執行過（環境沒有網路可達 Google API，也
沒有真實憑證），因此 Query Compatibility 屬於 **READY FOR MANUAL
DEPLOYMENT VERIFICATION**，不是 PASS。

## 五、Property Binding 方式

完全沿用既有 `utils/ga4RealtimeConfig.js` 的 `getGa4RealtimeConfig(db,
storeId)`：每個 store 的 Property/Stream ID 存在 `settings` 表（key-value，
`WHERE store_id=?`），或在該店明確選擇「使用共用 Property」且部署層級
`GA4_REALTIME_SINGLE_STORE_MODE=true` 時，才 fallback 到環境變數
`GA4_PROPERTY_ID`/`GA4_STREAM_ID`。GA4-H1 沒有新增第二套綁定邏輯。

## 六、Store Isolation 現況

**GA4 Property Binding Gate：PASS**（機制真實存在且被重複使用：
`getPropertyBinding()` 在所有 sync/read 路徑呼叫 `getGa4RealtimeConfig(db,
storeId)`，SQL 全部帶 `WHERE store_id=?`，物理上無法跨店讀取）。

**GA4 Store Isolation Gate：SINGLE_STORE_ONLY**（誠實判定，非 BLOCKED 也非
完整多店 PASS）——原因：
- 每個 store 理論上可以各自綁定不同 Property/Stream（機制支援，程式碼與
  QA Harness Scenario H/K 已驗證 store_002 讀不到 store_001 的資料）。
- 但**目前實際只有本專案測試/開發環境的假設情境**，沒有任何一家真實店
  在正式 GA4 後台建立過獨立 Property 並填入設定——這件事完全在 POS 系統
  之外（GA4 後台操作），本輪程式碼無法也不應該替代驗證。
- 因此「多店可靠隔離」在 Reality Audit 的誠實立場上標示
  SINGLE_STORE_ONLY：機制已就位，尚未有第二家店的真實 Property 資料可
  供交叉驗證。

## 七、是否存在 store_id Custom Dimension

不存在，也不需要——GA4-H1 的 Store Isolation 完全靠「查哪個 Property」
做隔離（一店一 Property 或明確共用），不是靠 GA4 Custom Dimension 在
單一 Property 內部切分店家。若未來要做「多店共用同一個 GA4 Property」，
才需要導入 store_id Custom Dimension；本輪明確排除這個範圍（維持
Single-store Mode 精神，見需求文件三）。

## 八、Scheduler 現況

**沒有可靠的排程器**。GA4-H1 採用需求文件第九節的 Cache-first 策略：
- 頁面載入先顯示 DB 現有快取（`getRealtimeGeoSummary`/`getRangeGeoStats`
  只讀 DB，不主動打 GA4）。
- 手動同步按鈕觸發 `POST /sync`。
- 沒有實作「固定 5 分鐘自動同步」的 in-process timer 或外部排程；
  `GA4_GEO_REALTIME_SYNC_ENABLED`/`_INTERVAL_MINUTES` 等環境變數本輪
  **沒有**被任何程式碼讀取或消費——這是誠實的範圍縮減記錄，不是遺漏
  隱瞞。

## 九、Zeabur Restart 對排程影響

因為沒有 in-process timer，Zeabur 重啟不會遺失排程狀態（沒有狀態需要
保留）。若未來加上排程，必須走外部 Scheduled Job 呼叫受保護的內部
Sync Endpoint（需求文件九之 C），不能假設 in-process timer 存活。

## 十、GA4 時區 vs POS 顯示時區

GA4 Property 本身的時區設定完全在 GA4 後台，本專案無法讀取也未讀取。
`ga4_geo_range_stats.timezone` 欄位固定寫入字面值 `'Asia/Taipei'`，代表
「本系統顯示這批資料時使用的時區」，不代表對 GA4 Property 送出請求時
的實際時區換算已完成驗證——`startDate`/`endDate` 的日曆日邊界是否跟 GA4
Property 自己的時區設定完全對齊，**未經真實 GA4 API 驗證**。

## 十一、實際 Event Mapping

`services/ga4GeoSyncService.js` 的 `EVENT_NAME_TO_COLUMN`：

```
page_view      → page_view_count
view_item      → view_item_count
add_to_cart    → add_to_cart_count
begin_checkout → begin_checkout_count
checkout_click → checkout_click_count
purchase       → purchase_count
```

`view_product_count` 欄位存在於 schema，但**沒有獨立可信來源**——本輪
無法對正式 GA4 Property 確認是否存在跟 `view_item` 不同的 `view_product`
事件，因此該欄位一律留 0，程式碼與 Static Audit 都有明確標註（見
`checkViewProductHonestlyDeferred`），不用 `view_item` 的數字冒充。

## 十二、Production GA4 尚未完成的驗證

- 三組 Query 的 Dimension/Metric 相容性未對真實 Property 驗證。
- Property 時區與程式碼 `startDate/endDate` 換算的真實對齊未驗證。
- Realtime 30 分鐘窗與 Historical Range 交界處的資料一致性未驗證。
- 429/Quota 的真實觸發門檻未驗證（只驗證了程式碼收到 429 分類碼後的
  處理邏輯，不是「真的打到 GA4 Quota 上限」）。
- 瀏覽器/裝置端 Manual QA 未執行（見 Manual QA Checklist）。

## Gate 判定

| Gate | 結果 |
|---|---|
| GA4 Property Binding Gate | **PASS** |
| GA4 Store Isolation Gate | **SINGLE_STORE_ONLY** |
| Production GA4 Gate | **READY FOR MANUAL DEPLOYMENT VERIFICATION** |
